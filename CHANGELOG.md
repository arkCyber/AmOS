# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- **提醒事项 Reminders app (`frontend-ts`, 2026-09-04)**: the iOS-style Reminders (提醒事项) system app. Pure domain kernel `src/lib/reminders.ts` (colour-coded custom lists + built-in inbox, due / all-day scheduling, priority 无/低/中/高, flags, smart views 全部/今天/计划/旗标/已完成, overdue/reached math, demo seed, self-healing `normalize*`, shared `amos.*` persistence, included in the Settings cloud/local backup snapshot); UI `src/components/RemindersApp.tsx` (smart-list + list chips, compose/edit form with native date/time/all-day/priority/flag/list, inline complete-circle toggle, list create/delete). Reaching a due time fires an **OS-wide alert**: new `lib/reminderNotify.ts` (`useDueReminderAlerts`, mounted once in the `Shell`, alongside `useNotificationAlert`) reconciles every ~15 s + on store changes and fires **one** app notification per reminder the first time its due instant is reached, using idempotent persisted `amos.reminderFired` markers — so the badge / arrival banner / haptic / ring work on **any** screen (home, other apps, locked), not only while the Reminders app is open, and opening the app clears them like Messages/Mail, and alerts are suppressed while the Reminders app itself is focused (items already on screen). Registered in `APPS`/`COMPONENTS`; `en`/`zh` i18n. Tests: 8 unit (`reminders.test.ts`) + 5 DOM (`reminders-dom.test.tsx`) + 8 notifier (5 `reminderNotify.test.ts` + 4 `reminderNotify-dom.test.tsx`) + SSR smoke + i18n parity.
- **提醒事项 audit fixes (`frontend-ts`, 2026-09-04)**: editing a scheduled reminder now opens its date/time editor (previously hidden), and the 提醒我 toggle now also appears in edit mode so an unscheduled reminder can gain a schedule while editing; clearing the notes on edit now truly deletes them (a blank no longer keeps old notes); reminders + reminderLists are included in the Settings cloud/local backup snapshot (`SYNC_STORES`). DOM tests cover the edit-schedule + clear-notes fixes.
- **提醒事项搜索 (`frontend-ts`, 2026-09-04)**: in-app search — pure `searchReminders` (title/notes, case-insensitive) plus a header 🔍 toggle that opens a filter narrowing the current smart/list view, with a live match count and ✕ clear; the empty state now distinguishes “无匹配” from “暂无”. i18n zh/en (`reminder.search/noMatch/matches`). Tests: 1 unit + 1 DOM.
- **提醒事项 三连完善 (`frontend-ts`, 2026-09-04)**: (1) **per-list collapsible “已完成”** — inside any list view (incl. the inbox) a header toggles a greyed-out completed group (iOS “show completed”), still un-completable/deletable; (2) **“✓ 全部完成”** quick action completes every pending reminder of the current smart view/list; (3) a past-due reminder can **“⏰ 推迟 1 小时”** from its edit form, pushing dueAt +1 h so the OS notifier re-alerts at the new time. i18n zh/en (`reminder.completeAll` / `reminder.snooze`). DOM tests +3.
- **提醒事项专属图标 (`frontend-ts`, 2026-09-04)**: `AppIcon.tsx` adds an opt-in bespoke first-party face — `isBespokeTile(id)` — so the Reminders tile now renders an Apple-inspired white card + coloured checklist (single tint-independent SVG) instead of the generic emoji-on-gradient face. All other apps keep the uniform tonal tiles unchanged (no drift).
- **语音备忘录 Voice Memos app (`frontend-ts`, 2026-09-04)**: new iOS-style recorder. Pure `lib/voiceMemos.ts` (metadata CRUD, formatting, self-healing normalize + a deterministic WAV generator for demo/test clips) persists `amos.vmemos`; `lib/voiceRecorder.ts` is a clean seam over MediaRecorder/getUserMedia (unavailable/denied errors); `components/VoiceMemosApp.tsx` — big red record/stop with live clock, play/pause via one shared <audio>, inline rename, delete, empty state; capture gated by the OS mic permission ledger. Audio is stored as a **BINARY Blob** in a MediaStore (`lib/mediaStore.ts`: IndexedDB in the shell, in-memory fallback for tests) — codec-compressed (Opus), **no base64 inflation**, and the durable KV store keeps only metadata (`amos.vmemos`). Regenerable demo seeds store no audio bytes at all. Bespoke Apple-style icon: white card + red waveform (`AppIcon` bespoke face). en/zh i18n. Tests: 6 unit + 4 DOM + 2 media-store + SSR smoke. Audit fixes: IndexedDB writes now resolve only when the transaction commits (durable), and navigating away mid-recording stops & releases the microphone. Added (dev-dep `fake-indexeddb`) a real IndexedDB-backend binary round-trip test, and an end-to-end `fake mic → MediaRecorder seam → binary MediaStore → byte-identical read-back` integration test.
- **备忘录 iOS 观感对齐 (`frontend-ts`, 2026-09-04)**: 备忘录列表改为 iOS 样式——普通备忘录折叠为「加粗标题行 + 摘要预览 + 相对时间(今天·昨天·日期) + 置顶/清单标记」，点击展开全文/操作；含清单的备忘录始终展开以便勾选。Apple 黄色便签专属图标（`AppIcon` bespoke face）。新增纯助手 `noteTitle/notePreview/noteDayOf`（`lib/notes.ts`）；i18n `note.yesterday/open/untitled`。Tests: 3 新行助手单测 + 1 DOM（折叠→展开）。
- **QCOM/MTK on-device inference accelerator domain (`amos-ai`, 2026-09-04)**: new `crates/amos-ai/src/accelerator.rs` turns the previously-dead `Config.enable_acceleration`/NPU narrative into a resolved, honest per-chip profile. `SoCVendor::{Qualcomm,MediaTek,GenericAndroid,Host}` + `detect()` (`AMOS_SOC_VENDOR` override; never guesses a vendor on a non-Android host); `Accel::{Auto,Cpu,Vulkan,Metal,Nnapi,Qnn,NeuroPilot,Off}` + `AccelProfile` resolving `AMOS_ACCEL` → on Android `auto` picks **NNAPI** (the portable NPU shared by Snapdragon & Dimensity), macOS → Metal, else CPU; `resolve()` reports `(effective, reason)` so an uncompiled vendor SDK (QNN/NeuroPilot, gated by the new `qnn`/`neuropilot` features) is honestly downgraded to CPU rather than claimed; `llama_args()`/`n_gpu_layers()`/`ollama_hint()` for a llama.cpp/Ollama local runtime. Daemon logs the resolved `vendor/accel/llama_layers` (or the downgrade reason) at startup. Unit tests (6). Design/acceptance: `docs/qcom-mtk-bringup.md`.
- **Honest local GGML engine — `AMOS_GGML_STRICT` (`amos-ai`, 2026-09-04)**: when `AMOS_GGML_STRICT=1` an unavailable GGML engine/model is a surfaced error instead of a silent mock fallback (real deployment never mistakes mock for inference); default stays `off` so the offline/CI fallback (`ggml_command_e2e`) is preserved. Unit test covers strict-error + permissive-fallback.
- **`amos-ai` `qnn`/`neuropilot` feature flags**: gate the vendor-NPU claims in `accelerator.rs` (`compiled_in()`), mirroring the `asr-sherpa` gating pattern.
- **Local GGML engine consumes the accelerator profile (`amos-ai`, 2026-09-04)**: `GgmlBackend` gains an `accel` field + `with_accel(path, profile)` (with `new()` defaulting to `AccelProfile::from_env()`); the daemon's `BackendKind::Ggml::build()` hands the startup-resolved profile to the engine so offload is real, not just logged. Engine invocation is now built by a pure, unit-testable `engine_argv()`: default `allama` keeps `allama run …` untouched (it manages its own offload → `ggml_command_e2e` unaffected), while any `AMOS_GGML_BIN` pointing at a llama.cpp-class CLI gets `-m <model> -p <prompt> -n <max>` + `engine_offload_args()` (`--n-gpu-layers <N>` from the profile; CPU explicitly `0`). Docs `docs/qcom-mtk-bringup.md` updated.
- **Accelerator surfaced truthfully on `get_status` + System UI (`amos-ai` + `amos-proto` + `amos-tauri`, 2026-09-04)**: `StatusReply.accelerator` (field 15) reports the resolved `<vendor>/<accel>` (e.g. `android/nnapi`) **only** when the local ggml engine is actually serving (`EngineState`/`accel_label_for_active`), empty otherwise — no longer just a log line or a hard-coded `gpu_util=0`. `amos-tauri` `DaemonStatus` + `status_once` example carry it; frontend `aiEngine.ts` `EngineView`/`describeEngine` parse it and Settings' AI engine-truth section renders "Accelerator: …" (en/zh `settings.aiAccel`). Tests: `amos-ai` server (mock → empty; gating for ggml-only) + `ai-engine.test.ts`; typecheck clean.
- **Third-party web-bundle runtime host (`frontend-ts` `ExtApp.tsx` + `lib/bundle.ts`, 2026-09-04)**: the installed-app container is no longer a "runtime host not wired" placeholder. When an app was installed from a web-bundle, `ExtApp` fetches its files over `storeBundleResource` (base64), inlines relative `script/link/img` refs into one self-contained document via the new pure `lib/bundle.ts` helpers (`resolveBundlePath`/`listLocalRefs`/`inlineBundle`/`toDataUrl`/`decodeBase64Text`, refusing path escapes), and renders it in a `sandbox="allow-scripts"` `srcdoc` iframe (no same-origin → can't reach the OS shell). Manifest-only installs fall back to a manifest card (`extApp.notWeb`); load errors are surfaced with a reload. en/zh keys `extApp.hostRunning/hostReload/hostLoading/hostError/notWeb`. Pure core unit tests: `bundle.test.ts` (7). Honest boundary: the no-custom-protocol `data:`-inline path suits classic single-page bundles; multi-page/ESM/true-origin bundles still need a future `amos-app://` protocol host (`docs/appstore.md` §4.10).
- **HTML-aware web-bundle inliner hardening (`frontend-ts` `lib/bundle.ts`, 2026-09-04)**: `listLocalRefs`/`inlineBundle` now compute masked spans (bodies of `<script>`/`<style>` and HTML comments) and never treat `src=`/`href=`-looking strings *inside code/CSS/comment text* as real asset references — a bundle's inline JS is preserved verbatim instead of being corrupted by over-eager rewriting. Opening-tag attributes (e.g. `<script src>`/`<img src>`) are still rewritten. Regression test (`bundle.test.ts`).
- **`ExtApp` runtime-host component coverage (`frontend-ts`, 2026-09-04)**: new `ExtApp.test.tsx` (happy-dom + react-dom + fake Tauri bridge) asserts the host renders an installed web-bundle in a `sandbox="allow-scripts"` `srcdoc` iframe, falls back to the verified-manifest card when an app has no web interface, and **degrades gracefully to the manifest card (no iframe/crash) when the bridge call fails**.
- **Ollama accelerator hint (`amos-ai`, 2026-09-04)**: `build_backend_from_env()` now consumes the previously-unused `AccelProfile::ollama_hint()` — when `AMOS_BACKEND=ollama` the daemon logs the resolved `vendor/accel` and the Ollama device hint at startup (Ollama manages its own offload, so this surfaces chip routing instead of CLI flags).
- **`amos-app://` web-bundle protocol host (`amos-appstore`, 2026-09-04)**: new `crates/amos-appstore/src/host.rs` — the missing URI layer for the store's "future custom protocol": `parse_bundle_uri("amos-app://<id>/<path>") -> (id, path)` validates the netloc as an app id (model's slug rule, so `..`/`/`/whitespace can't address a path), and `serve_bundle(root, uri) -> ServedBundle{bytes, content_type, nosniff}` resolves inside that app's own install dir via `serve::resolve_request` (canonicalization + traversal guards) and reads the bytes — ready for a Tauri `amos-app://` protocol handler to write out with the right MIME. `model::valid_id` is now `pub(crate)` and shared. Unit tests (3): URI parse/validate, serving entry + assets, and path-escape rejection. `amos-appstore` lib tests now **39**. Docs `docs/appstore.md` §4.11.
- **Tauri `amos-app://` wiring + bundle-id hardening (`amos-appstore` + `amos-tauri`, 2026-09-04)**: `amos_appstore` now exports `is_valid_app_id`; `amos-tauri`'s `read_bundle_resource(id, path)` validates `id` before `root.join(id)` (the old code had no guard — a malicious id like `..` could select a directory outside the root), and a new pure `read_bundle_uri(root, uri)` + registered command `appstore_bundle_uri` serve a bundle file addressed by an `amos-app://` URI (the single seam a future custom-protocol handler calls). Frontend gains `lib/backend.ts` `storeBundleUri(uri)`. Tests: `amos-appstore` host (3), `amos-tauri` appstore module (**5**, incl. non-slug-id rejection + URI serving + traversal).

- **`StatusReply.governor` — resource-governor live decision on the AI status RPC (2026-09-04)**: `get_status` now also returns `GovernorMetrics` (field 14, `sensor_mode`/`reason`/`cap_inference`/`throttle_background`/`ticks`) so the AI-session/`status_once` path sees the same daemon governing state the `Governor` service exposes. `AiAgentService` gained an optional shared `Arc<Mutex<ResourceGovernor>>` (`set_governor`, wired by `serve()` to the same instance the beat + Governor service use) + `governor_metrics()`; pending baseline (ticks=0) when unattached. `amos-ai` lib **92** (new `governor_metrics_reflects_shared_governor_decision`); clippy `--workspace`/fmt clean.
- **Governor exposes its latest decision (`Governor` service `GetState`, 2026-09-04)**: `GovernorState.decision` (new `GovernorDecision`) reports what the daemon's closed loop is doing — `sensor_mode`, `reason`, `cap_inference`, `throttle_background`, `ticks` — so the System UI can show live governing state. `ResourceGovernor` now records the last tick's `GovernorOutcome` (which gained `cap_inference`/`throttle_background`) + a `ticks` counter (`last_decision()`/`ticks()`). Service unit test asserts decision flips to `power_save`+throttling after a low-battery observe; `amos-ai` lib **91** + governor UDS e2e green; clippy `--workspace`/fmt clean.
- **Daemon-hosted resource governor gRPC service (`amos-ai` + `proto/governor.proto`, 2026-09-04)**: implements `docs/device-bring-up.md` §4 — a `Governor` service (package `amos_governor`) mounted on the daemon's UDS so the System UI / a per-app process host can drive the `ResourceGovernor` closed loop over the wire: `RegisterApp`, `MoveApp` (→Foreground/Background/Cached/…), `UnregisterApp`, `ScheduleJob` (AlarmExact/Deferred), `GetState` (apps + jobs + background count). `serve()` now owns ONE shared `Arc<Mutex<ResourceGovernor>>` used by **both** the periodic beat and the service (so registration feeds the same deciding instance — no duplicate governor). `ResourceGovernor` gained `move_app`/`kill_app`/`app_state`/`app_entries`/`job_entries`; `amos-scheduler::Scheduler::entries`. amos-proto gains the generated `amos_governor` module. Verified offline: `governor_service` unit tests (register→move→schedule→get_state + observe over the same instance + wire enums via `TryFrom<i32>`) and a real-UDS e2e (`tests/governor_rpc_e2e.rs`: register/move/schedule/get_state + NotFound on unknown app). `amos-ai` lib **91**; clippy `--workspace -D warnings` clean.
- **Real-device bring-up checklist `docs/device-bring-up.md` (2026-09-04)**: actionable action-item doc for the three remaining Android bindings — (1) real battery/thermal/power sampling feeding `amos_ai::energy` via `amos-power::android::AndroidBatteryTelemetry` + `amos-profiling::android::AndroidBatteryPowerSource`, (2) a per-app process host driving `amos-applife::AppLifecycle`, (3) binding `amos-scheduler` `next_wake`/`due` to `AlarmManager`/`JobScheduler`/Doze — each with wiring points, seams to reuse, and on-device acceptance criteria; plus a §4 route for a daemon-hosted lifecycle (optional gRPC Governor service). README/bottom-layer-os-audit updated.
- **Real Android battery/thermal telemetry seam `amos-power::android` (2026-09-04)**: feature-gated `android` (`dep:jni`), mirroring the repo's `android.rs` seams. `AndroidBatteryTelemetry` takes a `JavaVM` + app-`Context` global ref, reads the sticky `ACTION_BATTERY_CHANGED` intent (`level`/`scale`/`status`/`temperature`) and folds it into an `amos_power::Telemetry` for the energy/governor ticker; charging = `STATUS_CHARGING|FULL`, temp = tenths→°C, a failed read yields the conservative `BatteryState::default()` (never a fake charge). Instantaneous mW is not on the sticky intent — pair `amos_profiling::AndroidBatteryPowerSource` for live draw. Host-verified: `cargo check -p amos-power --features android` + clippy `-D warnings` clean; added to `make gated-check`. Default (desktop) build unaffected (24 tests green). Runtime (System UI wiring a real `JavaVM`/`Context`) remains device bring-up.
- **Composed resource/energy/lifecycle/scheduler closed loop `amos_ai::governor` (2026-09-04)**: new daemon-side `ResourceGovernor` that ticks `amos-power` (energy decision) → `amos-scheduler` (fire exact alarms, run deferred jobs only when power allows, doze-proxy = `throttle_background`) → `amos-applife` (on `PowerSave` freeze idle `Background` apps to `Cached` tombstone; on recovery + screen-on thaw them; under explicit memory pressure reclaim the LRU cached/background victims). Deterministic `observe(now, telemetry, memory_pressure, maintenance_open) -> GovernorOutcome`; public registration delegates (`register_app`/`background_app`/`schedule`) let a per-app host seed apps/jobs. Reference orchestration tying the three audit-gap kernels into one driver a System-UI/per-app host mounts (`docs/app-lifecycle.md` §5 / `docs/scheduler.md` §7). `amos-ai` lib tests now **88**; `clippy --all-targets -D warnings` + fmt clean.
- **Background-task scheduler + wakeup-alignment core `amos-scheduler` (2026-09-04)**: closes audit gaps §3.P2 #10/#12 ("无后台任务调度器" / 唤醒对齐) with a pure-`std` register of `AlarmExact` vs `Deferred` jobs. `spec` — `JobId`, `JobType` (+keys), `PowerState` with `deferred_runnable()` (charging → run; dozing → only in an open maintenance window; else run). `scheduler::Scheduler` — `register`/`cancel`/`complete`, `due(now, power)` returns exact alarms first then **coalesces all due deferred jobs into one batch** (fewer wakes), `next_wake(now)` gives the next guaranteed exact-alarm tick for a real `AlarmManager`, `counts()`, `kind(id)`. `ScheduledJob::new` + `register` reject inverted windows. Deterministic (caller supplies `now` in ticks + `PowerState`). Design: `docs/scheduler.md`. Tests: 7 lib + 1 integration (`tests/doze_cycle.rs`: alarm fires during Doze at its tick with `next_wake=300`; deferred withheld while idle, coalesced in the next maintenance window); `clippy --all-targets -D warnings` + fmt clean. Real `AlarmManager`/`JobScheduler`/Doze-feed binding + `amos-applife` tombstone-linkage remain caller seams.
- **Energy-governor domain core `amos-power` (2026-09-04)**: closes the audit gap "isolated energy-policy knobs but no closed-loop decision layer" (`docs/bottom-layer-os-audit.md` §3.P2 #11) with a pure-`std`, deterministic power-save decision kernel. Folds `BatteryState` (level/charging/temp) + live `power_mw` (via `amos-profiling::PowerSource`) + foreground/background `Usage` into a `SensorMode` recommendation through `policy::decide` — a **pure rule with entry/exit hysteresis** (low-battery on/off band + thermal exit cushion → no boundary flapping). Output `Decision { sensor_mode, cap_inference, throttle_background, reason }`; `Decision::apply_to(&SensorManager)` pushes the mode into `amos-sensor`'s real manager (reusing `SensorMode`, no duplicated enum). Stateful `EnergyGovernor::observe` carries the last decision for the periodic-ticker caller. `Policy::normalized()` guarantees `critical ≤ save_on ≤ save_off` + `critical_temp > high_temp` even on contradictory input. Priority: thermal-critical > battery-critical > battery-low > thermal-high > power-draw > charging(Performance) > healthy(Balanced); healthy throttles background inference only when the screen is off. Design: `docs/power-policy.md`. Tests: 22 lib + 2 integration (integration drives a real `MockSensorProvider`+`SensorManager`: low-battery `PowerSave` refuses a 30 FPS camera, charger → `Performance` restores); `clippy --all-targets -D warnings` + fmt clean. Daemon/System-UI periodic ticker + real `ACTION_BATTERY_CHANGED`/`EXTRA_VOLTAGE` input wiring remain caller-side seams (`docs/profiling.md` / `docs/power-policy.md` §6).
- **Energy-governor wired into the AI daemon (`amos-ai`, 2026-09-04)**: new `crates/amos-ai/src/energy.rs` `EnergyStore` (mirrors `profiler.rs`) ticks an `amos_power::EnergyGovernor` on the same cadence as the health/profile heartbeats and logs the decision; `serve()` spawns+aborts it alongside the others. Inputs are `AMOS_ENERGY_*` env (host/dev; real battery/thermal HAL is the on-device seam). New `proto/ai_agent.proto` `EnergyPolicy` message + `StatusReply.energy = 13`; `get_status` maps the latest `EnergySnapshot` (mode/reason/cap_inference/throttle_background/ticks); `status_once` example prints it. `amos-ai` lib tests **86** + 4 rpc e2e green; clippy/fmt clean. Honest label: the daemon *runs the governor and reports* the recommendation — driving a real `SensorManager`/engine gating stays a caller seam (`docs/power-policy.md` §6).
- **App/process lifecycle domain core `amos-applife` (2026-09-04)**: closes audit gap §3.P2 #8 ("无应用生命周期管理") with a pure-`std`, transport-agnostic state machine + LMK-proxy reclaim. `spec::AppState` — importance ladder `Foreground(0) < Visible(1) < ForegroundService(2) < Background(3) < Cached/墓碑(4) < Stopped(5)` with stable keys; protected tiers never reclaimed. `manager::AppLifecycle` — registry with LRU ordering (every transition bumps a monotonic seq), ops `launch/go_foreground/go_background/freeze(→Cached)/thaw/start_service/stop_service/stop(保留保存态)/kill(丢弃)`, per-state `counts()`, and a deterministic `reclaim_candidates(budget)` that evicts Cached before Background, least-recently-used first. Integration test drives a 3-process pressure scenario (`[a, c, b]`). Design: `docs/app-lifecycle.md`. Tests: 9 lib + 3 integration; `clippy --all-targets -D warnings` + fmt clean. Runtime (per-app process host / real pressure feed) remains a caller seam.
- **Device-sensor tile in Settings (`frontend-ts`, 2026-09-04)**: new `components/SensorPanel.tsx` shows the daemon's energy mode (switchable Balanced/Performance/Power save → `sensor_set_mode`) plus cameras / GNSS / IMU readouts via `sensor_snapshot`, rendered in Settings (hidden when not bridged / no daemon). en/zh i18n keys; driven by the pure `lib/sensors.ts` normalize helpers. `bun run typecheck` green.
- **System UI sensor/profiling desktop wiring (`amos-tauri` + frontend, 2026-09-04)**: (a) Tauri `sensor_*` bridge — `amos-tauri/src/sensors.rs` `sensor_snapshot`/`sensor_set_mode`/`sensor_acquire` open a `SensorClient` over the daemon UDS (mirrors the telephony bridge) and return serde payloads (cameras/GNSS/IMU/mode); registered in `lib.rs`. (b) frontend `lib/sensors.ts` — typed snapshot + `normalizeSnapshot`/`sensorPixels`/`sensorCameraCount` pure helpers + guarded `sensorSnapshot`/`sensorSetMode`/`sensorAcquire` (null when not bridged) + `__tests__/sensors.test.ts` (4). (c) **`StatusReply.profile` rendered in Settings diagnostics** — `aiEngine.ts` `EngineView`/`describeEngine` now parse the daemon `profile` (decode tok/s, TTFT ms, totals), `apps.tsx` shows it in the AI (engine-truth) section when `decode_runs > 0`, with en/zh i18n keys; `ai-engine.test.ts` +3. Real device `Context` wiring / camera-frame + IMU stream bridges / live `EXTRA_VOLTAGE`→`est_energy_j` remain device bring-up (`docs/sensors.md` / `docs/profiling.md`). Rust clippy `-D warnings` clean; `bun run typecheck` + sensors/ai-engine/settings tests green.
- **Android HAL/power seams landed as compile-gated skeletons (`amos-sensor` + `amos-profiling`, 2026-09-04)**: (a) `amos-sensor/src/android.rs` — `AndroidSensorProvider` behind the `SensorProvider` trait (mirrors `amos-radio`/`amos-telephony` `android.rs`): **GNSS real + synchronous** (`LocationManager#getLastKnownLocation`, needs `ACCESS_FINE_LOCATION`); camera/IMU are *streams* whose `CameraDevice`+`ImageReader` / `SensorEventListener` bridges return an explicit `Provider` error until device bring-up (honest, no pretending). (b) `amos-profiling/src/android.rs` — `AndroidBatteryPowerSource` implementing `PowerSource`: reads `BatteryManager#getLongProperty(CURRENT_NOW)` (µA) × voltage (constructor param, default 3700 mV; pass live `EXTRA_VOLTAGE` on device) → mW; `0.0` when unreadable. Both feature-gated `android` + optional `jni`; `make gated-check` now host-compiles all four device seams (`cargo check -p {amos-radio,amos-telephony,amos-sensor,amos-profiling} --features android`) — verified green, no device/NDK needed. Real stream bridges + System UI `Context` wiring remain device bring-up (`docs/sensors.md` / `docs/profiling.md`).
- **Device-sensor domain core `amos-sensor` (2026-09-04)**: closes the audit gap (next to `amos-audio`/Telephony) with a pure `std` camera / GPS-GNSS / IMU abstraction. `spec` — `SensorKind`, `SensorMode`, `CameraConfig`/`CameraFrame` (RGBA8 / NV21), `GeoFix`/`FixMode`, `ImuSample`/`Vec3` + PowerSave/hardware sampling ceilings; `provider` — dumb `SensorProvider` seam + deterministic `MockSensorProvider` (camera seq/pattern, drifting GNSS fix, IMU cadence); `manager` — `SensorManager` energy policy: single-shot reads always allowed, continuous streams gated by mode (PowerSave ceilings Camera 15 FPS / GNSS 1 Hz / IMU 25 Hz; hardware ceilings always). Real Android HAL + gRPC service bus left as seams (`docs/sensors.md`). Tests: 22; `clippy -D warnings` clean; fmt clean.
- **Inference perf/power profiling domain core `amos-profiling` (2026-09-04)**: starts the roadmap's "Performance profiling and optimization" with a pure-`std` metric kernel for on-device LLM. `types` — `Phase` (PromptEval/Decode) + `Record`; `tracker` — `ProfileTracker` (prompt/decode tokens-per-second, decode ms-per-token, TTFT, `merge`, guarded ÷ → empty gives `None`, never NaN); `power` — `PowerSource` seam + `MockPowerSource` + `energy_joules`; `measure` — `time`/`time_and` around `Instant`; `report` — owned `ProfileReport` (`Display` stable `key: value` lines, `n/a` not NaN). Daemon assembly (`amos-ai` generate path) + real power HAL left as seams (`docs/profiling.md`). Tests: 15; `clippy -D warnings` clean; fmt clean.
- **Wired the two domain cores into the runtime (`proto/sensor.proto` + `amos-ai`, 2026-09-04)**: kills the "code written but not wired" audit criticism. (a) `SensorService` — new `proto/sensor.proto` (`amos_sensor`) → `amos-proto` codegen → `amos-sensor/src/service.rs` (`ListCameras/CaptureCamera/GetGnss/GetImu/GetMode/SetMode/AcquireStream`, mapping the domain `SensorManager` + PowerSave gate to the wire) → **mounted via `add_service` in `amos-ai::serve()`** alongside AiAgent/AndroidManager/Telephony on the shared UDS. e2e: `amos-sensor/tests/sensor_rpc_e2e.rs` (single service over UDS) + `amos-ai/tests/rpc_test.rs` `sensor_service_mounted_and_profile_exposed` (sensor answered on the same socket as AiAgent). (b) Profiling in `get_status` — `amos-ai/src/profiler.rs` `ProfileStore` (shared `ProfileTracker` + TTFT atomics) is fed by the `stream_chat` text-decode path (each turn: streamed tokens + wall; start→first-token as TTFT); `StatusReply.profile` (`ProfileMetrics`, proto field 12) + `status_once` now print it. Honest labels: no tokenizer ⇒ no prompt-token counts; `decode_tokens_per_sec` is end-to-end generated tokens/s to the client. Real Android HAL provider + System UI sensor bridge + battery `PowerSource` remain seams (`docs/sensors.md` / `docs/profiling.md`). Sensor tests 25 + 1 e2e; `amos-ai` lib 82 + rpc e2e; `clippy -D warnings` clean; fmt clean.
- **Finish the profiling/sensor seams (`amos-ai`, 2026-09-04)**: (a) **bidi `Chat`/voice now profiled** — the text-decode loop in the `chat` handler records each completed (non-cancelled) turn into the same shared `ProfileStore` (`record_ttft` on first token + `record_decode(tokens, wall)` after the turn), so text and voice-driven turns both feed `StatusReply.profile`. e2e: `chat_test::bidi_chat_prompt_turn_is_profiled` (chat_test now **8**). (b) **profile into the monitoring cadence** — `ProfileStore::spawn_periodic_log` logs `amos-ai inference profile` (decode_tokens_per_sec / ttft_ms / totals) on the same interval as the health heartbeat, spawned+aborted in `serve()`. (c) **headless acceptance examples** — `amos-ai/examples/sensor_once.rs` (list cameras → capture → GNSS → IMU → mode over the mounted SensorService) and `amos-ai/examples/profile_once.rs` (one `stream_chat` turn → print `StatusReply.profile`). Real Android HAL provider + System UI sensor bridge + battery `PowerSource` remain seams (`docs/sensors.md` / `docs/profiling.md`). `amos-ai` lib 82 + chat_test 8 + rpc_test 4; `clippy -D warnings` clean; fmt clean.
- **Resident-voice DSP tested headlessly (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: `spawn_resident_capture` — the capture→downsample→energy-gate→trailing-silence endpoint loop — now has a unit test driven by `amos_audio::mock::FrameMic` (no daemon/device): loud speech frames stream as `Audio`, trailing silence past the gate fires `AudioEnd`, and exactly one utterance is submitted. Voice module now **10 tests**; full `cargo test -p amos-tauri --lib` = **44 passed**; `clippy -D warnings` clean.
- **Assistant-voice commands made testable (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: factored the duplicated "get active sender → send frame/end/cancel" logic in the `assistant_voice_feed/end/stop` Tauri commands into `VoiceSession` methods (`feed`/`end_utterance`/`stop` + `active_tx`); the commands now delegate (no behavior change, removes the copy-paste). 2 new async tests cover: feed fails before start then delivers an `Audio` frame; `end_utterance` sends `AudioEnd`; `stop` sends `Cancel` and clears the session so a later feed fails. Voice module now **9 tests**; full `cargo test -p amos-tauri --lib` = **43 passed**; `clippy -D warnings` clean.
- **Assistant-voice reader made pure & tested (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: extracted the daemon chunk → event relay logic out of the inline reader loop into a pure `on_chunk(&AgentChunk, session, &mut full) -> Vec<VoiceEvent>` (token accumulate, `done` finalizes & resets via `mem::take`, `error` relayed without finalizing), and rewrote the `VoiceLink::open` reader loop to drive it — no behavior change, now unit-testable headlessly. 4 new tests cover token accumulation, done-reset, error-without-finalize, and empty chunk. Voice module now **7 tests**; `cargo test -p amos-tauri --lib assistant_voice` + `clippy -p amos-tauri --all-targets -D warnings` green.
- **Rate-limiter memory bound (`amos-telephony` `rate.rs`, 2026-09-04)**: `DialRateLimiter` now caps distinct tracked clients (default 256, `with_clients(...)` configurable) and prunes fully-expired windows on access, so a caller flooding fake `x-client-id`s can no longer grow the bucket map without bound (memory-DoS guard). New `with_clients` + test `distinct_clients_are_bounded`. Rust lib now **61**; `clippy -D warnings` clean.
- **Assistant-voice unit coverage (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: added tests for the wired-but-untested voice core — `to_payload` preserves every `VoiceEvent` variant & fields (`listening/token/turn_done/stopped/error`) and `VoiceSession` store/take is single-shot (take returns the started session then clears). Now 3 voice tests (with `has_signal`); `cargo test -p amos-tauri --lib assistant_voice` + `clippy -p amos-tauri --all-targets -D warnings` green. (Module `mod tests` was also moved to file end so `clippy --workspace -D warnings` is fully clean.)
- **Incoming-call banner Mute (`amos-tauri` frontend, 2026-09-04)**: the system incoming-call overlay's answered (talking) banner now offers a **Mute** toggle (🔇/🎙️, aria `Mute`/`Unmute`) like the dialer's own in-call screen; mute state resets on ringing/end/hang-up so it never leaks across calls. New `IncomingCall.test.tsx` case (answer → Mute present → Unmute → back). `tsc --noEmit` clean.
- **P3 real-dial skeleton landed, compile-gated (`amos-telephony`, 2026-09-04)**: `android` feature now = optional `jni` dep; new `crates/amos-telephony/src/android.rs` (gated in `lib.rs`) provides `AndroidTelephonyProvider` + a **separate** `AndroidEmergencyTelephonyProvider` hosted in the System UI/`ROLE_DIALER` process (`AndroidContext(GlobalRef)` + per-thread attach, mirroring `amos-radio`). `dial`/`emergency_call` fire a real `ACTION_CALL` `tel:` intent from the app `Context` and return a provider call id; `subscribe` is wired to an event registry. Honest scope: `answer`/`end`/recording/live `status` return explicit `Provider(P3 device-validated)` errors (no pretending); on-device TODO documents the `ACTION_CALL`→`TelecomManager#placeCall` migration + InCallService/TelephonyCallback wiring. Verified with `cargo check --features android` and `clippy --features android -D warnings`; default desktop/CI builds carry no JNI stack.
- **Ordinary-dial rate limiting, emergency-exempt (`amos-telephony` + `amos-ai`, 2026-09-04)**: new `crate::rate::DialRateLimiter` (per-client fixed-window). `TelephonyService::dial` never throttles **emergency** calls but rate-limits ordinary ones (per `x-client-id` gRPC metadata, else `_default`); over-cap returns `ResourceExhausted` and is audited. The daemon now mounts `demo_server_limited(30)` (`amos-ai/src/server.rs`) so the live UDS service caps ordinary dials at 30/min/client while 110/112 stay exempt; strict/headless test backends stay unlimited. New builders `with_mock_demo_limited` / `demo_server_limited`. Rust package now **60 lib + 2 UDS e2e** green; `clippy -D warnings` clean; `cargo check -p amos-ai` passes.
- **Emergency-dial audit wired in (`amos-telephony`, 2026-09-04)**: new `crate::audit` — a bounded (1024, newest-first) thread-safe `AuditLog` with `AuditEntry{ts,operation,detail,outcome,note}`. `TelephonyService` holds one and records **every parsed dial**: auto-routed/forced emergency dials as `operation="emergency_dial"` (the §5 requirement — rate-limit exempt but never un-audited), regular as `"dial"`, refusals as `Rejected` with the provider note. Unparseable/over-long input is rejected **without** an audit echo (don't reflect attacker-controlled huge text back into logs). Accessible via `TelephonyService::audit_log()`. Rust package now **56 lib + 2 UDS e2e** green; `clippy -D warnings` clean; `cargo check -p amos-ai` passes.
- **Telephony service-hardening audit pass (`amos-telephony`, 2026-09-04)**: `Number` is now **server-authoritatively length-capped** at `MAX_LEN = 18` digits (mirrors the dialer UI's cap) — a crafted gRPC `Dial` can no longer ask a provider to place a pathologically long number. Cap counts canonical digits (ignores separators / leading `+`). Audit also re-confirmed the daemon truly serves telephony (`amos-ai/src/server.rs` mounts `amos_telephony::service::demo_server()` over the shared UDS), so the dialer path is live, not dead code. Rust lib now **52** tests green; `clippy -D warnings` clean.
- **Full-featured dialer (`amos-tauri` frontend, 2026-09-04)**: `PhoneApp` extracted to its own module (`components/PhoneDialer.tsx`, re-exported from `CommsApps`) and upgraded to a tabbed dialer — **Keypad / Recents / Frequent / Emergency**. Recents & Frequent list pages (tap to fill the keypad, select-then-place), and an **Emergency page** with one-tap privileged dial of the shared recognized codes (`lib/emergency.ts::EMERGENCY_NUMBERS`, CN set + universal 112) labelled by function. The in-call surface gains a **Mute** toggle and a **DTMF keypad** (local state until the audio pipeline lands; real mic mute / tone out are `docs/native-voice.md` scope). Emergency page dials go through the OS `telephony_dial(…, emergency: true)` path; no-daemon dials degrade to a localized error. New i18n en/zh keys; new `PhoneDialer.test.tsx` (emergency privileged dial, mute-on-connect, DTMF open/close) + rewritten `PhoneApp.test.tsx` (Recents/Frequent tabs, pick-to-fill); `PhoneRecording`/`MessagesApp`/emergency/lock/i18n suites still green; `tsc --noEmit` clean.
- **Telephony audit + production hardening — Phase A+C (`amos-telephony` + `amos-tauri` frontend + `docs/telephony.md`, 2026-09-04)**: architecture decisions & honest responsibility framing for P3 (real calls must host in the System UI/`ROLE_DIALER` process via JNI `TelecomManager`, **not** a headless daemon hand-writing `/dev/binder`; crash-proof 110/112 routing is a platform/RIL/vendor guarantee a userspace crate must not claim as a "kernel path"). Code: `EmergencyMap` is now jurisdiction-aware — `for_region` (CN/US/GB/IE/AU/NZ/JP/KR/EEA…, every set always carries international 112, deduped) + `quick_dial(region)` + `codes()`; unknown/empty region falls back to `common_global()` so the table is never empty (a misclassified 110/112 as a regular call would be the worst failure). New Rust unit tests (lib now 51, all green). Frontend: lock-screen emergency one-tap number + label come from the shared `lib/emergency.ts::EMERGENCY_QUICK_NUMBER` and recover (re-enabled + `shell.emergencyUnreachable`) instead of sticking on a phantom "calling…" when the phone service is unreachable; `PhoneApp` no-daemon dial now exits calling and shows `phone.dialFailed` instead of lingering with no call id. i18n en/zh `{num}` interpolation. New `emergency.test.ts` + `lockEmergency.test.tsx`; updated `PhoneRecording.test.tsx` no-daemon case to the new graceful-degradation semantics. `tsc --noEmit` clean.
- **Tauri telephony bridge unit tests (`amos-tauri`, 2026-09-04)**: the `telephony.rs` command layer (added during the call-recording/loop work) previously had **no Rust unit tests**. Added tests for its pure snapshot→payload mapping (`call_payload`): state (`Active`/`Ended`/`Dialing`/`Unknown`), direction (`Incoming`/`Outgoing`), recording (`On`/`Off`/`Failed`), and safe degradation for unknown wire values — the mapping the whole phone UI keys off. `cargo test -p amos-tauri` lib unit tests all green (34 run).
- **Telephony broadcast audit fixes (`amos-telephony`, 2026-09-04)**: hardened signalling-consistency gaps found by audit so **every** call-state transition is broadcast on `Watch`. (1) `MockTelephonyProvider::answer` now **broadcasts `Connected`** — answering an incoming call previously left the `Watch` stream silent (contradicting the `telephony_answer` contract). (2) Recording is a **first-class broadcast call-state**: `start_recording`/`stop_recording`/`simulate_recording_failure` emit `ProviderEvent::RecordingChanged`. (3) Recording broadcasts carry the call's **true `direction`** (an answered *incoming* call is not re-tagged Outgoing). (4) A **local hang-up now broadcasts `LocalEnded`** (previously `end()` was silent), making local end symmetric with remote end / answer / record. All verified with new unit tests; telephony unit count now **47** + 2 UDS e2e; `clippy --all-targets -D warnings` clean.
- **Call telephony — end-to-end loop closure (`amos-telephony` + `amos-ai` + `amos-tauri` + `frontend-ts`, 2026-09-04)**: a desktop dial is now actually operable through dial → Active → record → hang up. `TelephonyService` gains demo auto-connect (`demo_server()` used by the `amos-ai` daemon; outgoing *regular* calls reach `Active` after a short ring via the mock "network answers"; emergency calls stay ringing; hung-up-before-connect is ignored) while `mock_server()` stays deterministic for the headless e2e harness. `amos-tauri` opens a **long-lived `Watch` stream** and forwards each call-state event to the WebView as `telephony-event` (`spawn_telephony_watch`, reconnects with bounded backoff), adds `telephony_answer`, and extends `TelephonyCallPayload` with `direction`. `PhoneApp` now subscribes and enters a real "In call"/talking screen when its dialed call connects (enabling the record toggle) and returns to the keypad on End; new **system incoming-call overlay** (`IncomingCall`) shows Ringing → Answer/Decline and, once answered (Active), a compact in-call banner with record toggle + Hang up. i18n en/zh (`phone.talking/incoming/answer/decline/hangup`). Tests: telephony unit now **42** (demo auto-connect → Active) + updated `PhoneRecording.test.tsx` (Active-via-event enables record) + new `IncomingCall.test.tsx` (ringing/answer→recordable banner/decline). Real audio capture + on-device incoming injection remain future audio/device work.
- **Audio HAL Bridge — hardware audio + real local ASR (`amos-audio` + `amos-ai`, 2026-09-04)**: closes the first "彻底断层" at the source level. New **`crates/amos-audio`** (pure-std) is the audio-HAL abstraction: `AudioCapture`/`AudioSink` traits (mono f32), the 16 kHz ASR/wire spec (`AudioSpec`/i16↔f32/downmix/`encode_f32_le`), a streaming `LinearDownsampler` (phase-continuous across chunks), deterministic mocks (`SineMic`/`FrameMic`/`SilenceMic`/`MockSink`/`NullSink` + `mic_to_asr` example), and **`target_os="android"`+feature-gated hand-written direct FFI seams** for `tinyalsa` (AOSP/system, incl. the future call-audio-tap position) and `aaudio` (app-side real-time mic). `amos-ai` gains the **`asr-sherpa`** feature; `ChatAsr::from_env` now selects a real sherpa-onnx streaming recognizer when `AMOS_ASR_BACKEND=sherpa` + `AMOS_SHERPA_MODEL_DIR` (honest `None` fallback, never silently mock). Gated UDS e2e `amos-ai/tests/bidi_sherpa_audio.rs` pushes real speech PCM from `models/sherpa-en-20m/test_wavs/0.wav` over `Payload::Audio` and asserts the recognized transcript is answered. Tests: `amos-audio` **13** unit; `amos-ai` **73** lib (mock + `asr-sherpa`); bidi real-sherpa e2e green; `clippy --all-targets` clean under both feature sets. Design: `docs/audio-hal-bridge.md`. Android FFI seams compile only on an NDK cross-target and remain to be exercised at device bring-up; SIM-call voice interception still needs system-level HAL/audio-route hooks.
- **System UI resident assistant-voice streaming link (`amos-tauri`, 2026-09-04)**: pushes the gap-#1 work one step further — a real mic→daemon streaming path on the System UI side. New `assistant_voice.rs` owns a long-lived bidi `Chat` stream (no prompt) that keeps answering across turns: `VoiceLink` (Tauri-free, headless-testable) streams **mono 16 kHz f32 little-endian `Payload::Audio`** frames and relays `AgentChunk` replies as `VoiceEvent`s; Tauri commands `assistant_voice_start/feed/stop` (managed `VoiceSession`) fan them to the WebView as `assistant-voice-event`. Headless e2e `amos-tauri/tests/assistant_voice_e2e.rs` runs a real daemon (mock ASR) and drives a 48 kHz `amos-audio` sine mic → `LinearDownsampler` → f32-le → `Payload::Audio`, asserting "你好，Amos" is recognized and answered. Frontend bridge wired: `lib/audio.ts` `encodeF32le`/`frameToAssistantChunk`, `lib/voice.ts` `pcmToAssistantChunk`/`parseVoiceEvent` (typed `AssistantVoiceEvent`), `lib/backend.ts` `assistantVoiceStart/Feed/Stop`. Tests: `amos-tauri` lib clippy clean; voice e2e green; TS `voice.test.ts` 14 pass (incl. 3 new) + `tsc` clean. `amos-tauri` now depends on `amos-audio`.
- **Push-to-talk end-of-utterance (`Payload::AudioEnd`, `amos-ai` + `amos-ai` + `amos-tauri`, 2026-09-04)**: adds the missing "I'm done speaking" signal so a voice turn no longer depends solely on the recognizer's own VAD/endpoint. `proto/ai_agent.proto` adds `bool audio_end = 4` to `ClientMessage`; `ChatAsr::finish()` force-finalizes + resets the current utterance; the daemon's bidi `Chat` handles `AudioEnd` by turning whatever was recognized so far into a prompt (semantic card + inference + audit + history unchanged); `VoiceLink::finish_turn()` / Tauri `assistant_voice_end` / TS `assistantVoiceEnd()` expose it client-side. Tests: `ChatAsr::finish` unit (short utterance finalizes + resets); `amos-ai` lib now **74**; bidi UDS e2e `bidi_chat_audio_end_finalizes_a_short_utterance` feeds a sub-endpoint clip then `AudioEnd` → answered; `amos-ai --all-targets` + `amos-tauri` clippy clean; fmt clean; TS `tsc` clean.
- **Resident multi-turn voice (energy-gated `AudioEnd`, `amos-tauri`, 2026-09-04)**: proves the "always-on mic" semantics on the System UI side. `assistant_voice::has_signal` (client energy gate mirroring the frontend `hasSignal`) drives utterance segmentation; a new headless e2e (`resident_stream_answers_multiple_successive_utterances`) runs a real daemon and, on **one resident stream**, streams two synthetic 16 kHz speech bursts separated by silence, submitting each via `AudioEnd` at the client's own trailing-silence gate, and asserts **both utterances are answered**. This is the client-side segmentation loop a device AAudio capture thread will feed. Tests: `assistant_voice_e2e` now **2** (single + multi-turn) pass; `has_signal` unit pass; `amos-tauri --lib` clippy `-D warnings` clean; fmt clean.
- **Official resident capture thread (`ResidentVoiceHandle` + `spawn_resident_capture`, `amos-tauri`, 2026-09-04)**: promotes the segmentation loop into a first-class, stoppable worker thread that a device **AAudio** capture feeds. `spawn_resident_capture` reads an `amos_audio::AudioCapture` on a named thread, down-samples to 16 kHz, pushes `Payload::Audio`, and auto-submits each utterance with `AudioEnd` on a trailing-silence gate; returns a [`ResidentVoiceHandle`] (`stop()` joins, `submitted()` counts) and `VoiceLink::spawn_resident`/`feeder()` convenience. gated e2e `resident_thread_submits_and_answers_two_utterances` runs the official thread against a real daemon and asserts both utterances are answered. Now covered by `gated-check` (`cargo test -p amos-tauri --test assistant_voice_e2e`). Tests: `assistant_voice_e2e` now **3** pass; lib + test clippy `-D warnings` clean; fmt clean.
- **Airplane-mode cascade is atomic (rollback, `amos-radio`, 2026-09-04)**: hardens `RadioManager::set(Airplane, true)` so enabling Airplane is no longer a partially-applied write. It now snapshots the prior state first, and if any cascade step (Airplane→on, then Wi-Fi→off, then Bluetooth→off) fails **after** Airplane is already on, it rolls every radio bit back to the prior snapshot before returning the provider error — no partial Airplane state can leak to the user/provider. Failure-injection tests: a `FlakyProvider` whose Wi-Fi/Bluetooth writes fail on demand proves Wi-Fi-cascade failure, Bluetooth-cascade failure, and the all-success path each leave the provider exactly as expected. Tests: `amos-radio` **13** pass (3 new); `clippy -D warnings` clean; fmt clean.
- **Resident-voice handle hardening (`amos-tauri`, 2026-09-04)**: audit-driven completion of the newest capture code. `spawn_resident_capture` now **refuses sub-16 kHz mic rates loudly** (it only down-samples; 8 kHz previously spawned a worker that silently never streamed). `ResidentVoiceHandle` gains **non-blocking `request_stop(&self)`**, and `stop()` now delegates to it before joining — so callers can avoid blocking a live (e.g. AAudio) mic read when they must not wait. Tests: `assistant_voice` module **12** pass (2 new: sub-16k rejection, request_stop→stop lifecycle); `amos-tauri --lib` clippy `-D warnings` clean; fmt clean.
- **Inbound audio frame bound (memory-DoS guard, `amos-ai`, 2026-09-04)**: the daemon's voice path feeds untrusted `Payload::Audio` bytes straight into `decode_f32_le` (allocating by client length). `ChatAsr::feed_audio` now caps a frame at `MAX_FRAME_SAMPLES` (160k ≈ 10 s @16 kHz) and **drops oversized frames before decode** (warn + `None`), so a malformed/malicious client can't make us allocate an unbounded buffer. Tests: `amos-ai` lib **75** pass (1 new: oversized frame dropped, recognizer still usable after); chat_asr 6; clippy `-D warnings` clean; fmt clean.
- **Assistant-voice double-start hygiene (`amos-tauri`, 2026-09-04)**: `assistant_voice_start` now **tears down any previous active voice session first** (`voice.stop()`), so a second start (e.g. a busy mic button) can't orphan the prior reader task or run two listeners emitting events to the same UI. Tests: `assistant_voice` module **12** pass; `amos-tauri --lib` clippy `-D warnings` clean; fmt clean.
- **Web-bundle installer path-traversal hardening (`amos-appstore`, 2026-09-04)**: closes a real security gap found in audit. `WebInstaller` already relied on the `tar` crate's `..`-rejection, but that only guards *archive entries* — the bundle's entry **`start`** is a path parsed from `amos-app.json` *after* unpack, and `read_file` accepted arbitrary `rel`, so `dir.join("../..")` could probe/read outside the install dir. Added `safe_relative`/`safe_join`: `validate_bundle` and `read_file` now refuse any non-relative/`..`/`.`/absolute/NUL path (clear `unsafe relative path …` error). (The manifest `id` was already confined to a safe slug by `AppManifest::validate`/`valid_id` — audit confirmed it, not a defect.) Tests: `amos-appstore` **36** pass (2 new: traversal `start` refused + decoy untouched; `read_file` refuses `..`/absolute/NUL while subdir reads still work); clippy `-D warnings` clean; fmt clean.


- **Call recording — full-stack capability (`amos-telephony` + `proto` + `amos-tauri` + `frontend-ts`, 2026-09-04)**: 通话录音 as a first-class, contractual feature across every layer. Domain kernel (`session.rs`) gains a `RecordingState { Off, On, Failed }` on `Call` with whitelisted transitions — recording is legal only while a call is **Active** and **non-emergency** (emergency/110-112 lines are a hard refusal), auto-reset to `Off` on hang-up, and a device `Failed` state is surfaceable + retryable. `TelephonyProvider` seam adds `start_recording`/`stop_recording` (provider decides allow/deny via a consent/jurisdiction policy knob; domain denies before the provider is consulted). `proto/telephony.proto` adds a `RecordingState` enum, `CallSnapshot.recording`, and `StartRecording`/`StopRecording` RPCs returning the authoritative snapshot; `CallSnapshot` records carry `recording`. gRPC `TelephonyService` maps it all; Tauri bridge exposes `telephony_start_recording`/`telephony_stop_recording` (+ `TelephonyCallPayload.recording`). `PhoneApp` in-call screen offers a record toggle (start/stop, live "正在录音" indicator) that mirrors the daemon's authoritative state and never claims a recording the OS declined. i18n en/zh (`phone.record*`). Tests: 41 unit (recording domain/provider/service: legality, emergency-forbid, policy denial, failure→retry, end-resets, status-carries-recording, RPC error mapping) + 3 `PhoneRecording.test.tsx` DOM tests. Real audio capture stays with the future audio pipeline; this delivers the state machine + contract + UI control.

- **Messages unread → dock badge (`frontend-ts`, 2026-09-04)**: `MessagesApp` now publishes unread incoming messages as app notifications (`app` = “信息”, 💬, capped at 20), so the home dock “信息” badge and notification center reflect short-message unread (cleared as messages are read/cleared, no spurious writes when all read). DOM tests (`MessagesApp.test.tsx`).

- **Messages / 短信息 hardening (`frontend-ts`, 2026-09-04)**: audit + completion of `lib/messages.ts` — `appendMessage`/`appendQuote` now trim text and refuse blank bodies; `normalizeMessages` caps history at `MESSAGE_CAP` (200) so a conversation can't grow unbounded; added `MESSAGE_CAP` const; `MessagesApp` writes every save through the normalizer, so the cap + self-heal apply live (not just on load). New dedicated `messages.test.ts` covers every export (seed/append/quote/normalize-cap/clear/remove/time&day helpers/unread/markRead/markAllRead/constants).

- **Shared outgoing-call hook (`frontend-ts`, 2026-09-04)**: extracted `lib/useOutgoingCalls.ts`, now used by both `ContactsApp` and `PhoneApp` — it owns call-history state and derives the Frequent ⭐/Recent quick-dial lists (name-resolved from live contacts) and the `recordOutgoing` (write calllog + raise phone notification) side-effect, removing the duplicated logic the two apps previously each inlined. Dedicated hook tests (`useOutgoingCalls.test.tsx`): name-resolved lists, and record→calllog+notification.

- **Phone dialer → call history & notification (`frontend-ts`, 2026-09-04)**: `PhoneApp` now records successful dials into `amos.calllog` (feeding the shared Recent/Frequent strips) and raises a phone notification, matching `ContactsApp`; its keypad also shows "Frequent ⭐" and "Recent" quick chip rows (tap backfills the number, name resolved from contacts). DOM tests (`PhoneApp.test.tsx`).

- **Phone contacts / 通讯录 (`frontend-ts`, 2026-09-04)**: a new `contacts` app (👥) with a production-grade `lib/contacts.ts` domain — pure, immutable, validation-normalizing ops (`normalizeContacts` repairs corrupt stores and dedupes ids, name + at-least-one-number required, cleaned/deduped phones), favorites, search by name or phone digits, name sorting (favorites first), durable under `amos.contacts`. New `ContactsApp` UI: search, add (with validation hint), favorite toggle, two-tap delete, and "call" out via the telephony bridge (graceful "daemon offline" when not bridged). Full unit tests (`contacts.test.ts` — 16) plus DOM interaction tests (`ContactsApp.test.tsx` — 4: list, call-offline, two-tap delete persists, add-validation). Registered in the app grid + i18n en/zh.
- **Contacts edit + duplicate guard + grouping (`frontend-ts`, 2026-09-04)**: `ContactsApp` can now edit an existing contact (✏️ prefill → save via `editContact`); adding is refused when the number already exists (`contactsWithPhone`, digit-normalized incl. `+CC`/bare suffix match → "duplicate" hint); the list is grouped by letter (A–Z / `#`) with colored initial avatars (`contactLetter`/`groupContacts`/`avatarHue`). New pure helpers all unit-tested; contacts.ts stays at 100% function coverage.
- **Call history + recents (`frontend-ts`, 2026-09-04)**: new `lib/calllog.ts` domain — pure, durable `amos.calllog`: `normalizeCallLog` (newest-first, keeps history for frequency stats, caps at 60, repairs corrupt stores), `recordCall`, `recentNumbers` (distinct quick-dial list), `frequentNumbers` ("常用"), `logNameFor`, `callDigits`. Successful bridged calls from `ContactsApp` are recorded (functional update, no stale-closure loss), and "Frequent ⭐" (top-by-frequency) and "Recent" quick-dial strips appear (number → contact-name lookup). Reverse lookup `contactNameFor` added to `lib/contacts.ts`. Unit tests (`calllog.test.ts`) + Contacts DOM tests (bridged-call → recents; repeated dial → frequent chip).
- **Radio / connectivity（`amos-radio` + `amos-tauri` + `frontend-ts`, 2026-09-04）**: new transport-agnostic domain crate `amos-radio` — `RadioMode`/`RadioSnapshot`, a `RadioProvider` seam + deterministic `MockRadioProvider`, and a `RadioManager` owning the policy (Airplane mode cascades Wi-Fi + Bluetooth off; the non-airplane radios are refused while Airplane is on; `set` returns the authoritative snapshot). 10 unit tests. The System UI drives it through in-process `radio_status`/`radio_set` Tauri commands (real Wi-Fi/Bluetooth on the no-UI Android base are Android system services reachable from the System UI APK, so unlike telephony these do not round-trip through the headless daemon), mirroring each snapshot back into the durable `amos.settings` store (cross-window broadcast). Notification-center wifi/bluetooth/airplane tiles go through the backend, with a same-policy `flipRadio` fallback outside Tauri and an authoritative `radioStatus` sync on open. A real Android backend is scaffolded as `AndroidRadioProvider` under the `android` feature (JNI `WifiManager`/`BluetoothAdapter`; `cargo check --features android` validates it compiles; on-device hookup pending). Design: `docs/radio.md`.
- **Status-bar radio indicators (`frontend-ts`, 2026-09-04)**: the top `StatusBar` (home + every app shell) now shows Wi-Fi / Bluetooth / Airplane glyphs (airplane supersedes the others) derived from the shared quick-settings, updating **live** — same-window toggles via a new `STORE_CHANGED_EVENT` write signal, cross-tab via `storage`, and cross-window/bridged via the `store-updated` backend event. New reactive hook `lib/useStoreValue.ts` + pure `radioIcons` selector (unit-tested).
- **Location master switch (`frontend-ts`, 2026-09-04)**: the Notification-Center `location` quick toggle is now a real system-level location-services switch. `locationEnabled`/`flipLocation` encode "default ON, explicit OFF disables", the tile reflects it, and `MapsApp` "Locate" refuses geolocation whenever it is OFF (even if the app already holds the `location` grant) with a localized hint; store-reactive via `useStoreValue`.
- **Do-Not-Disturb (`frontend-ts`, 2026-09-04)**: the Notification-Center `dnd` quick toggle now really silences the OS — when ON (`dndActive`), every unread badge on the home screen / dock is hidden, and the notification-center list is shown muted (a 🌒 "DND" chip replaces the unread count). Home-dock notifications/quick-settings are read reactively via `useStoreValue`, so toggling DND anywhere updates the badges live. With this and Location, none of the quick toggles is purely cosmetic anymore.
- **Persistent DND indicator + ring/vibrate policy (`frontend-ts`, 2026-09-04)**: the `StatusBar` now shows a persistent 🌒 moon while Do-Not-Disturb is active (and a 🔕 when the alert policy mutes alerts), always visible on home and inside every app. New `lib/sound.ts` models the notification sound policy bits — `ring`/`vibrate` under `amos.sound` (both default ON) with `normalizeSound`/`effectiveAlert` (DND mutes both) + a reactive `useAlertPolicy` hook. Unit-tested.
- **Notification-arrival haptic (`frontend-ts`, 2026-09-04)**: a real consumer of the sound policy — when the home screen's unread set grows, HomeDock fires a short `navigator.vibrate` pulse, but only if the *effective* policy allows vibration (so DND or "vibrate off" silences it). Pure gate `shouldVibrateOnArrival` (unit-tested); mount-time existing badges never buzz.
- **Notification-arrival ring (`frontend-ts`, 2026-09-04)**: completes the sound policy by also honouring the `ring` bit — the same arrival path now plays an asset-free two-note chime via the Web Audio API (`lib/notifyTone.ts`, synthesized `OscillatorNode` + gain envelope) only when the *effective* policy allows audible alerts (DND / "ring off" silences it). Pure gate `shouldRingOnArrival` (unit-tested); no-op in SSR/tests/headless.
- **Global arrival alert (`frontend-ts`, 2026-09-04)**: the vibrate + ring arrival logic was hoisted out of `HomeDock` (which only renders on the home screen) into a reusable `useNotificationAlert` hook, mounted once in the `Shell` — so a new notification now pulses/rings on **every** screen (home, inside an app, even the lock screen), still gated by the effective sound policy and DND.
- **Notification banner (`frontend-ts`, 2026-09-04)**: a transient toast now appears near the top whenever a notification arrives (newest one; auto-dismisses ~4 s), layered over every screen via a `NotificationBanner` mounted once in the phone frame. Do-Not-Disturb suppresses the banner (those notifications stay only in the Notification Center). **Tap to acknowledge** — clears that app's notifications (badge disappears) and dismisses the toast; works regardless of which screen is showing. Pure arrival selector `newestAddedNotif` (unit-tested) + DOM test for show/acknowledge.
- **Android radio provider wiring (`amos-tauri`, 2026-09-04)**: `amos-tauri` gained an `android` feature (`["dep:jni","amos-radio/android"]`) and a `RadioBridge::from_android(vm, env, context, airplane)` factory that backs the bridge with `AndroidRadioProvider` (real `WifiManager`/`BluetoothAdapter`) instead of the Mock. `scripts/build-android.sh` now also cross-compiles `amos-radio --features android`. Desktop default is unchanged (`mock_seeded`); `cargo check/clippy --features android` on the host validate the wiring compiles — on-device `run()` swaps in `from_android` with the Activity `JavaVM`/`Context` (docs/radio.md §6).
- **Bell unread badge (`frontend-ts`, 2026-09-04)**: the home `TopBar` 🔔 now shows a live unread count (caps at 99+), hidden while Do-Not-Disturb is on — reactive via `useStoreValue`, matching the dock badges.
- **Test coverage completion (`frontend-ts`, 2026-09-04)**: every exported function in the sound/notification/store modules now has a test — `useAlertPolicy` + `useNotificationAlert` (DOM hook tests: defaults, policy reflection, DND muting, DND silencing of the arrival vibrate), `playNotifyTone` (no-op without Web Audio; synthesizes a chime with a stubbed `AudioContext`), and an explicit `STORE_CHANGED_EVENT` dispatch assertion. `settings.ts` / `sound.ts` / `amosStore.ts` / `useStoreValue` / `notifyTone` pure functions are all covered.
- **Per-function coverage drive (`frontend-ts`, 2026-09-04)**: brought `lib/permissions.ts` (83→100% funcs, durable ledger store round-trip), `lib/files.ts` (97.6→100%, `hasName`), `lib/notes.ts` (97.5→100%, `fmtTime`) and `lib/storeApps.ts` (50→100% funcs: tile cache `set/get/tileById`, `subscribe/notify`, and `loadStoreTiles` via a fake bridge) to full function/line coverage. Together with the already-at-100% pure modules (`bounded`, `maps`, `calculator`, `photos`, `weather`, `providers`, `systemButtons`) the pure-logic layer is now comprehensively covered (all 54 tests across the touched files pass).
- **Component-layer coverage (`frontend-ts`, 2026-09-04)**: per-component `--coverage` sweep (pure presentational ones like `AppIcon`/`Segmented` already 100%). Raised `components/CapabilityGate.tsx` function coverage 71→86% by adding interaction tests for the `Deny`/refused branch, the `onAllowed` callback, and the "deny now → allow later" flow (children stay hidden until granted, nothing persisted on deny).
- **Session-feature component interaction tests (`frontend-ts`, 2026-09-04)**: new DOM tests cover the radio/DND/notification UI branches — `StatusBar` (default wifi/bt glyphs, airplane supersedes with ✈️, DND shows persistent 🌒), `HomeDock` (unread badge appears for an app and is hidden under DND), and `NotificationCenter` (location master starts ON and toggling persists OFF; the dark tile drives the real theme). 6 tests across the three files, all passing.
- **Reactive-store cross-window correctness (`frontend-ts`, 2026-09-04)**: `useStoreValue` now honours the authoritative `store-updated` payload from the Rust store instead of re-reading this window's (possibly stale) `localStorage`, and mirrors it back into `localStorage` for consistency. End-to-end tests simulate a second Tauri window pushing a `store-updated` event and assert the hook re-renders with the authoritative value and correctly resets on removal.
- **Network time calibration (`amos-timesync`, 2026-09-03)**: transport-agnostic `TimeSource` seam (`MockTimeSource` deterministic, `HostClock` offline fallback) feeding a `SyncedClock` that turns an authoritative network time into a signed host-clock offset, applies it to `now()`, rejects implausible times (plausibility window 2000–2100), and atomically persists the last-known-good offset (corrupt/missing file degrades gracefully). A `Timekeeper` runs the periodic sync loop. Real SNTP query is a `ntp` feature (`NtpTimeSource` via `sntpc` + `sntpc-net-std`, blocking work on `spawn_blocking`); a `ntp_probe` example does a live query. Deps: `async-trait`, `thiserror` (+ optional `sntpc`/`sntpc-net-std`).
- **Supervised periodic time sync (`amos-supervisor` `timesync` feature, 2026-09-03)**: optional background wall-clock calibration orchestrated alongside daemon supervision. Default build and behaviour unchanged; enabling the `timesync` feature and `AMOS_TIMESYNC=1` at runtime spawns a periodic `SyncedClock` loop (`AMOS_NTP_SERVER` for real SNTP, `AMOS_TIMESYNC_STATE`/`AMOS_TIMESYNC_INTERVAL_SECS` optional), exposes live calibrated-time status (`TimeSyncHandle::report()`: corrected now / offset / staleness, printed at startup, on SIGUSR1, and before shutdown), and exports the state path to supervised children via `AMOS_TIMESYNC_STATE` so any daemon can load the last-known-good calibrated clock. Added to `make gated-check`.
- **App store core engine (`amos-appstore`, 2026-09-03)**: transport-agnostic `AppManifest` catalog + `Version`, sha256 integrity check, and an `AppStore` engine driving download → verify → install / upgrade / uninstall over a pluggable `StoreProvider` (deterministic in-memory `MockStoreProvider` today) with a JSON-persisted installed registry. Publish contract + developer onboarding in `docs/appstore.md`.
- **App store CLI (`amos-appstore-cli`, 2026-09-03)**: headless terminal driver over the same engine (demo/catalog/search/install/upgrade/uninstall/status), mirroring `amos-mail-cli`; `--catalog <URL>` (with `--features live`) swaps the offline demo for the real HTTP backend.
- **App store HTTP backend (`amos-appstore` `live` feature, 2026-09-03)**: `HttpStoreProvider` fetches a remote `MockCatalog` JSON catalog + downloads packages over HTTP (ureq in `spawn_blocking`); feature-gated so the default build stays offline-green. Both the CLI (`--catalog`, `--features live`) and the Tauri `StoreBridge` (`AMOS_APPSTORE_CATALOG`, `--features appstore-live`) can select it at runtime via a type-erased `Box<dyn StoreProvider>`.
- **App store publisher signing (`amos-appstore`, 2026-09-03)**: Ed25519 signing/verification of app manifests (`DeveloperKey`/`sign_manifest`/`verify_manifest_signature`, `publisher` field); the engine verifies a signed manifest's signature before install/upgrade and refuses mismatches (`BadPublisherSignature`). Dep: `ed25519-dalek`.
- **Web-bundle installer (`amos-appstore`, 2026-09-03)**: `webinstall::WebInstaller` unpacks a verified `tar.gz` web-bundle (`index.html` + assets + `amos-app.json`) into `<root>/<id>/`, validates the entry file, writes `manifest.json`, and uninstalls; `tar` refuses `..`/absolute paths. Deps: `tar`, `flate2`.
- **Web-bundle server resolver (`amos-appstore`, 2026-09-03)**: `serve::{resolve_request, content_type_for}` maps a host request path safely onto a file inside an installed bundle (refuses traversal/`..`, falls back to `index.html`, returns MIME + `nosniff`) — the server-side gate a future custom-protocol host needs.
- **Engine web-bundle install (`amos-appstore`, 2026-09-03)**: `AppStore::with_web_install_dir(dir)` makes `install`/`upgrade` of a `tar.gz` bundle also unpack it to `<dir>/<id>/` (and `uninstall` removes it); a bundle that can't unpack fails the install and isn't recorded. Tests cover unpack→uninstall and broken-bundle rejection.
- **StoreBridge bundle serving (`amos-tauri`, 2026-09-03)**: `StoreBridge` reads `AMOS_APPSTORE_INSTALL_DIR` and configures the engine's web-install dir; new command `appstore_bundle_resource(id, path)` returns a bundle file as base64 + MIME (sanitised by `serve::resolve_request`), so a local web-bundle can be rendered without a custom protocol. Headless-tested.
- **App Store System-UI page (`frontend-ts`, 2026-09-03)**: new "App Store" app (`components/StoreApp.tsx`, registered in `APPS`/`COMPONENTS`) browses the catalog (offline demo or HTTP) and install / update / uninstall through the `store*` bridge, with graceful offline fallback outside the Tauri shell. i18n zh/en.
- **Dynamic home registry for store apps (`frontend-ts`, 2026-09-03)**: store-installed apps become persistent home-screen tiles (`lib/storeApps.ts`, `store:<mid>` ids merged into `amos.home.layout`); `HomeDock`/title/`AppComponent` resolve them and tapping opens a placeholder container (`components/ExtApp.tsx`) until a real runtime host (installer) lands. Store page changes notify the shell live.
- **Durable system state store (`amos-tauri` + `frontend-ts`, 2026-09-03)**: `SharedStore` persists every `amos.*` mutation to disk (`$AMOS_STATE_FILE`, default `~/.amos/state.json`); on boot the shell hydrates localStorage from it, so state recovers even after localStorage is cleared.
- **Permission ledger (`frontend-ts`, 2026-09-03)**: `lib/permissions.ts` — pure, immutable, durable (store-backed `amos.permissions`) grants of sensitive capabilities (camera/microphone/location/notifications) per app; query/grant/revoke + normalize. UI dashboard + enforcement wiring next.
- **Privacy & permissions dashboard (`frontend-ts`, 2026-09-03)**: new "privacy" app (`components/PermissionsApp.tsx`, in `APPS`) lists each capability with its granted apps (revocable) and allow/deny toggles for the built-in apps that request it; changes persist in the durable store.
- **Permission enforcement — camera (`frontend-ts`, 2026-09-03)**: `CameraApp` now refuses to call `getUserMedia` until the "camera" capability is granted for the camera app; an allow/deny overlay prompts first, and denying blocks the feed (retry from the Privacy app). Mic/location call sites still to gate.
- **Reusable capability gate (`frontend-ts`, 2026-09-03)**: `components/CapabilityGate.tsx` exports `useCapability(appId, cap)` + a `<CapabilityGate>` allow/deny overlay + `revokeCapability`; generic i18n (`perm.allow/deny/askAllow/denied`). Ready to apply to mic/location call sites.
- **Permission enforcement — location (`frontend-ts`, 2026-09-03)**: `MapsApp` "Locate" now asks before calling geolocation: ungranted taps show an allow/deny banner, and geolocation only runs once the "location" capability is granted for maps (persisted). Microphone call sites (VoiceMicButton / BackendApps) remain to gate with the same hook.
- **Permission enforcement — microphone (`frontend-ts`, 2026-09-03)**: capture is gated behind the "microphone" capability for both the AI app's mic (`VoiceMicButton`, inline allow/deny bubble) and the interpreter's own capture (`BackendApps.InterpApp`, two-tap consent). None call `getUserMedia({audio})` before the capability is granted; allows persist (revoke via Privacy).
- **Time-sync CLI (`amos-timesync-cli`, 2026-09-03)**: headless driver over the same `SyncedClock` — `now`/`status` read the last-known-good calibrated clock from state (no network), `sync` runs one calibration pass (offline host clock by default; `--server`/`$AMOS_NTP_SERVER` does a real SNTP query behind the `ntp` feature), mirroring the other `*-cli` crates. Reads/writes the same `AMOS_TIMESYNC_STATE` file the supervisor exports, so operators and supervised daemons can consume the calibrated time. Added to `make gated-check`.
- **Daemon health & metrics (`amos-ai`, 2026-09-03)**: new `monitoring` module — `Monitor` keeps lock-free counters (RPCs seen at the gRPC boundary, uptime, periodic heartbeat ticks) and a `Monitor::spawn_periodic` self-health heartbeat that logs each snapshot (interval via `AMOS_METRICS_INTERVAL_SECS`, default 60s). RPCs are counted centrally by a tonic interceptor in `serve()`; `StatusReply` now carries `rpc_total`/`heartbeats`, so any `GetStatus` health probe reads the live metrics over the wire. Unit + real-UDS e2e coverage.
- New features and enhancements in development

### Changed
- Modifications to existing features

### Fixed
- Bug fixes

### Deprecated
- Features planned for removal

### Removed
- Features removed from the project

### Security
- Security-related changes and patches

## [0.1.0] - 2025-09-01

### Added
- Initial release with core architecture
- AI daemon (amos-ai) with gRPC server over UDS
- Tauri 2 System UI (amos-tauri) as gRPC client
- Window manager state machine (amos-wm)
- Protocol buffer definitions (amos-proto)
- Waydroid/APK compatibility layer (amos-android)
- CI/CD workflow with GitHub Actions
- Comprehensive documentation

### Notes
- This is the initial beta release
- API and architecture subject to change before 1.0

---

## Template for new releases

When creating a new release:

1. Update version numbers in `Cargo.toml` across all crates
2. Update version in `tauri.conf.json`
3. Add corresponding section below with:
   - Date in YYYY-MM-DD format
   - Clear categorization of changes
   - Links to related issues/PRs
4. Update any affected documentation

### Commit message format:

```
chore(release): v0.X.Y

- Brief description of major features
- Link to release notes
```

### Release checklist:

- [ ] All tests passing (`make test`)
- [ ] All linting clean (`make lint`)
- [ ] CHANGELOG.md updated
- [ ] Version numbers updated
- [ ] Documentation updated if needed
- [ ] Security audit completed
- [ ] Tagged in git: `git tag v0.X.Y`
- [ ] GitHub release created with release notes
