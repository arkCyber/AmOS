# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

- **剪贴板跨边界同步 P3：宿主半接线（env 门控、默认 inert）（2026-09-11，REQ-A28）**。P2b 之后传输能用了，但**没有任何东西把它接进运行时**——`set_native_sink`/`supervise`/`run_ingest` 仍是待接线的零件。本轮补上宿主半接线并顺手暴露、修掉一个真实缺陷。**(1) `clipboard_guest_link`（新模块）**：`lib.rs::setup` 调 `activate_from_env()` → **只有 `AMOS_GUEST_CLIPBOARD_SOCKET` 指到 socket 才拨号**；未设/空白即**不建线程、不拨号、行为零变化**（desktop/CI 是 no-op；空白不敏感的纯函数 `parse_guest_socket` 有测试）。**(2) 单一共享全双工连接**：一轮会话 = 一条 socket。**ingest 循环是唯一的重连权威**——每次 (重)连 `dial` 一次、`split`、把 writer 半边**发布**给镜像 sink、自己读 reader 半边；连接结束即清除已发布的 writer，断线期间的复制**诚实失败**（`NotConnected`，计入 `push_failures`）而不是另拨一条连接。**这是设计时发现的真实缺陷**：让 sink 与 ingest 循环各自 dial（如 P2b 的 `ReconnectingSink` 直连）会产生两条连接，而 guest 只能服务它 accept 的那条 → guest→host 方向被搁死。故端口改由读循环发布 writer、sink 跟随（`SharedWriter`），`ReconnectingSink` 保留为「writer 自带连接」的独立原语并在文档中写明各自适用面。**(3) 审计**：`GuestLinkStats` 单调计数 push / push 失败 / 不可映射拒发 / ingest / 回显丢弃 / 空文本 / 拒绝 / 确认 / 拨号 / 拨号失败，`clipboard_guest_status` 命令返回 `GuestLinkStatus{armed, connected, socket, reason, stats}`；`handle_guest_msg` 由 `bool` 升级为 `GuestMsgOutcome{Ingested,Echo,Blank,Refused,Ack}`，使审计**复用决策本身**而不另行推断（绝不漂移）。**(4) 前台语义不对称钉死**（§6 要求）：推送发生在**写**时（任何窗口都能复制，行为不变），读共享缓冲仍**仅前台**（`require_foreground`）——后台应用**不能**借 guest 链路抽干别的窗口的剪贴板。**(5) 空闲可停（真实缺陷修复）**：`link::supervise` 原先只在**连接之间**检查 stop 谓词，一条安静的长连接会让 `link::stop()` 的 join **永远挂住**（集成测试首次运行即死锁于此）；现改为**循环顶部与两次读之间都检查**，并把读超时（`WouldBlock`/`TimedOut`）当作「暂无数据」而非错误（阻塞 reader 行为不变，因为永不产生该错误）。**验证**：新增集成测试 **`clipboard_guest_link_e2e` 2/2**——真起 `activate_from_env()` 并在一颗**真实 Unix socket** 上：未连接时 `connected=false`、连接后后台窗口的复制仍镜像到 guest、容器真实复制回流宿主、自推送回显被丢（不重复入栈）、审计计数如实（pushes=1/ingests=1/echoes_dropped=1）、`stop()` 干净退出；`cargo test -p amos-clipboard` **59/59**（新增 `stop_predicate_ends_the_loop_cleanly_without_connecting`、`idle_stream_timeouts_are_retried_and_stop_still_works`）；`cargo test -p amos-tauri --test clipboard_e2e` **5/5**；`cargo check -p amos-tauri --features android --lib` ✓；`clippy --all-targets -D warnings`（amos-clipboard 与 amos-tauri）、`fmt --check` 干净。**诚实边界**：仍属设备侧的只有 Waydroid 宿主↔guest **命名空间桥接**与 guest 进程托管；非 Unix 构建上 `clipboard_guest_link` 诚实报「transport is unix-only」（`docs/clipboard-container-sync.md` §5/§7/§8、`docs/TRACEABILITY_MATRIX.md` REQ-A28）。

- **剪贴板跨边界同步 P2b：真实 Unix 域套接字传输（`amos-clipboard::unix`，2026-09-11，REQ-A28）**。审计发现 P2b 的「真实字节通道」此前**只是文档里的一句委托**——`link::supervise` 只提供传输无关的生命周期编排，仓库里**没有任何传输实现**（`grep UnixStream` 为空）。本轮把它落地，且因为它用 Unix 域套接字，**整条通道在 host 上就是真跑的**（不再是内存替身）：新增 `crates/amos-clipboard/src/unix.rs`（`#[cfg(unix)]`）——**`bind(path)`/`dial(path)`**（`bind` 先移除崩溃 peer 留下的**陈旧 socket 文件**——监听者崩溃不会 unlink 路径——但**拒绝替换非 socket 的真实文件**，绝不静默删文件）；**`split(stream)`**（一条连接全双工，`try_clone` 给出同一 socket 上独立的 reader/writer，ingest 循环与 mirror sink 各持其一、无需共享锁）；**`accept_one`/`serve`**（guest accept 侧）；**`drain`**（读半边，与 `supervise` 同形的有界 4 KiB 循环，干净 EOF 返回字节数、消费者错误原样上传）；**`ReconnectingSink<C>: Write`**（断线后**惰性重拨**、每次 write 至多一次，使宿主 `GuestSink` 熬过 guest 重启；重拨受 `Backoff` 预算约束——连续失败达上限后**不再拨号**，成功即复位预算；**绝不 sleep**，因为它跑在 UI 复制路径上，阻塞重连会冻住一次粘贴）。**重连模型显式写清**：一个会话 = 一条全双工 socket，两个方向同生共死，故 `serve` **一次只跑一个会话**（「一个宿主、一个 guest」的诚实形状）；要重叠连接的调用方自己驱动 `accept_one`。**验证**：`cargo test -p amos-clipboard` 全量 **57/57**（`unix` **8**：bind↔dial 真实 socket 往返、`split` 双向、陈旧 socket 可替换而真实文件被拒、断线后重拨**恰好一次**、**预算耗尽不再拨号**、`serve` 顺序会话、`drain` 干净 EOF/消费者错误）；`cargo test -p amos-tauri --test clipboard_e2e` **5/5**，含新增 **`real_unix_socket_carries_the_clipboard_both_ways`**——宿主半与 guest 代理经 `bind`/`dial`+`split` 架在**真实 Unix socket** 上：宿主复制→guest 剪贴板、guest 真实复制→宿主共享缓冲、自推送零回显、历史恰好 2 条；`cargo check -p amos-clipboard --features android` ✓；`clippy --all-targets -D warnings`（amos-clipboard 与 amos-tauri）、`fmt --check` 干净。**诚实边界**：仍属设备侧的只有 Waydroid 宿主↔guest 的**命名空间桥接**（默认 UNIX 命名空间不互通，需显式挂 socket/vsock 或在 `waydroid shell` 之上铺字节流）、`android` provider 运行时（仅编译门 + Kotlin bridge 契约）、guest 进程托管与 P3 接线/审计——真机 A↔微信互拷的产品级 e2e 未在本环境伪造（`docs/clipboard-container-sync.md` §5/§7/§8、`docs/TRACEABILITY_MATRIX.md` REQ-A28）。

- **相机 producer `buffer is inaccessible` 的真修复（use-after-free，2026-09-11，REQ-A45）**。真机（S5，Android 14）授予 `CAMERA` 后 `amos-camera` 线程曾在 `Nv21` 读 plane buffer 时抛 `IllegalStateException: buffer is inaccessible`；上一轮只把帧编码整段 `try/catch(Throwable)` **丢帧**止血——进程不再 FATAL，但每次失效丢一整帧，等于拿静默黑屏换不崩。本轮定位并修掉根因。**根因（AOSP 证据，非猜测）**：该异常不是「HAL 不给 Java 读」，而是 `java.nio.DirectByteBuffer` 自身的守卫——每个读写先查 `memoryRef.isAccessible`，而 `MemoryRef.free()` 会置 `isAccessible=false; allocatedAddress=0; isFreed=true`，即**底层 native 内存已释放**（Android SDK `sources/android-*/java/nio/DirectByteBuffer.java`）。对 `ImageReader` 的 `Image.Plane.buffer`，内存在 Image `close()`、或 reader `close()`/槽位回收时释放。触发路径：`MainActivity.onStart` 每次回前台都会 `AmosGlue.onStart → CameraGlue.attach`，旧 `attach` 只要 `camera != null` 就先 `detach()` 再重建，而旧 `detach()` 在**调用线程**直接 `reader.close()`，同时 `onImageAvailable` 正跑在 `amos-camera` 线程上——close 释放了在途图像内存，回调继续读 → `buffer is inaccessible`（use-after-free）。**另一处真实缺陷**：旧 `attach` 在 `CAMERA` 未授权时提前 `return`，但 `HandlerThread`/`ImageReader` 已建好——授权前每次 `onStart` 泄漏一份，且泄漏的 reader 仍挂监听器、从第二条线程继续回调。**修复**：(1) `attach` **幂等**——同一相机已在采集直接返回，绝不为「重建」拆活会话；(2) `detach` **串行化到相机线程**——先清字段（排队回调立刻认不出自己而退出），再把 `session/device/reader` 的 close **post 到同一 looper**，最后 `quitSafely()`（已入队的 close 先执行、线程随后退出），looper 已死则内联 close（此时不可能有在途回调）；(3) `onImageAvailable` **陈旧 reader 身份守卫**（`reader !== this.reader` → `acquireLatestImage()?.close()` 后返回，绝不读）；(4) `CAMERA` 检查**前置**，不再泄漏；(5) `onOpened` 若已被拆解（`reader == null`）则关 device；(6) **纪元守卫**——`generation` 每次拆解自增，`CameraDevice`/`CameraCaptureSession` 回调携带各自 epoch 并回查，迟到的 `onDisconnected`/`onConfigured` 绝不写进新一代状态（不把刚开的相机置空、不把已关闭会话塞进 `session`）；(7) 打包数学抽成**不碰 Android 类型的纯函数 `Nv21Packer`**，并修掉旧 `copyRowStrided` 两处缺陷——`buffer.rewind()` 后按 `row*rowStride + col*pixelStride` **无上界绝对读**（会误读短窗口、可能越 `limit()`）；新实现**只在 `limit()` 内读**（读不到的尾字节留 0、绝不抛）、支持 `pixelStride` 1/2 与任意 `rowStride`、luma 连续行走整行批量拷贝、非法几何返回 `null`；(8) `CameraGlue.droppedFrameCount()` 暴露丢弃帧数，`try/catch` 降为纵深防御。**验证**：新增宿主 JVM 单测 `android-glue/tests/.../Nv21PackerTest.kt` **5/5 通过**（`./gradlew :app:testArmDebugUnitTest`，无 Android 类型、无 Robolectric、无需设备）：NV21 **V 先 U 后**交织、I420 紧凑布局、semi-planar U/V 共缓冲（V 从共享 buffer +1 起）、luma 行填充被跳过、**短窗口零填而不抛**、奇数/非正几何返回 `null`；`./gradlew :app:compileArmDebugKotlin`（JDK 17）**BUILD SUCCESSFUL**；入口脚本 `scripts/android-glue-nv21-check.sh`（把 `android-glue/` 源同步进 `gen/` 后跑编译 + 单测）。**诚实边界**：单测覆盖 stride/边界数学，**「native 内存已释放」这条竞态无法在宿主复现**，只能真机验收（本环境 `adb devices` 为空，未执行）——验收步骤写入 `docs/android-glue.md`（logcat 不再出现 `frame encode dropped ... buffer is inaccessible`；反复 HOME→前台后无 FATAL、`droppedFrameCount()` 不持续增长、`dumpsys media.camera` 只有一个 AmOS 客户端；读侧帧 `seq` 递增）。**（`docs/android-glue.md`、`docs/TRACEABILITY_MATRIX.md` REQ-A45）**

- **守护进程资源门：生成准入池接线 + 推理响应缓存（2026-09-11，REQ-A43/A44）**。按 `docs/AEROSPACE_SOFTWARE_AUDIT.md` 对 amos-ai 守护进程复审，确认两处真实缺口并补全。**(1) `AMOS_MAX_SESSIONS` 已写未接线**：`config.rs` 声明并校验 `max_concurrent_sessions`（默认 16），但 `server.rs` 从未读取——并发生成**无上限**（`RateLimiter` 只限每客户端速率与小时 token 配额，不限制同时执行的生成数；N 客户端可并发启动无上限 NPU/GPU 生成，电池设备上的资源耗尽失效模式）。新建 `crates/amos-ai/src/pool.rs`：`GenerationPool` 容量用 `NonZeroUsize` **类型编码**（零槽位不可表示，`try_new(0)` 硬错绝不静默钳位）；**fail-fast 默认**（饱和立刻 `Saturated` 拒绝，确定性；可选 `AMOS_GEN_POOL_WAIT_MS` 有界等待 → `WaitTimeout`，两种拒绝可区分且单调计数）；RAII `PoolPermit`（tokio `OwnedSemaphorePermit`，'static 可移入流任务，卡片路径/断开/错误/正常完成**任何退出路径恰好归还一次**）；接线在 `stream_chat` 安全门后、**任何会话/簿记前**获取（被拒不分配资源），bidi `chat` 每条 Prompt 生成前获取、饱和回错误 chunk + 审计 `Rejected` 并保持连接可重试；gRPC 映射 `ResourceExhausted`，细节含诚实原因。**(2) `cache.rs` 两份审计文档点名缺失**：新建 `crates/amos-ai/src/cache.rs`——`ResponseCache` 有界 LRU+TTL（32 条/300 s/单条 256 KiB，注入时钟确定性测试，超限**拒绝而非截断**——截断的重放等于撒谎，`hits+misses==lookups` 不变量锁定）；**键 = 后端可见的全部输入**（model/max_tokens/prompt/排序后 context 全部键值对，会话谱系键参与——响应绝不在可能分叉的会话间错误共享）；`CachingBackend` 装饰器命中按原 token 序列重放（流式契约不变）、**上游错误（infer 失败或流中途失败）绝不缓存**、metadata 名诚实追加 `+cache` 令 `get_status` 可见、`BackendStats` 纯透传不伪造遥测；**默认关闭**（缓存改变可观测延迟，必须是运营者明确选择）。**测试**：`pool::tests` **8** + `cache::tests` **17** + server 集成 **2**（饱和拒绝不分配会话+释放后可复用+审计含诚实原因；缓存命中逐 token 一致重放）；`cargo test -p amos-ai` **245 例** lib + 全部集成；`cargo test --workspace` **134 个测试二进制全绿（EXIT=0）**；`clippy --all-targets -D warnings`、`fmt --check` 干净（`docs/daemon-resource-gate.md`、`docs/TRACEABILITY_MATRIX.md` REQ-A43/A44）。

- **短信回收站：视图层隐藏/恢复（2026-09-11，REQ-A42）**。平台短信库由**默认短信应用**拥有、AmOS 拿不到真实删除能力（§11.3/§13 的诚实边界），但「删不掉」不该等于「只能看着」——现补上**视图层回收站**：从 AmOS 的一切读取路径（线程列表、正文、徽标、预览）隐藏短信，可单条恢复、可清空（=全部恢复显示），**绝不销毁数据**，UI 与文档都明说「系统短信应用仍保留原短信」。**领域内核** `crates/amos-sms/src/trash.rs`：仅收当前文件夹**可见快照**中存在的行（陈旧 id 三态诚实报「未找到」）、屏蔽发件人拒收、重复回收幂等；**预览改写**——回收线程最新*可见*消息时预览换到回收前上一条可见行，`was_latest` 按「回收前的可见集」（预读快照、排除已回收行）判定，**同线程连续回收正确刷新覆盖**而非被已回收行卡住，线程来了更新消息后覆盖自动失效；上限 256 **FIFO 淘汰=自动恢复显示**（绝不静默吞数据）；持久化版本化 JSON **只存 id+时间戳、绝不存正文**，启动加载、每次变更落盘。**桥** `amos-tauri/src/sms.rs`：进程级 `RwLock` 共享 + 4 命令 `sms_trash_add/_list/_restore/_purge`（add 三态结果，域错误单层映射 `SmsError::Failed`），三条读取路径统一过滤——`sms_snapshot` 走 `apply_trash`、`sms_messages` 逐条剔除、`sms_counts` 走纯函数 `filtered_counts`（块名单+回收站一起扣，**徽标与可见列表由构造一致**）。**前端**：`backend.ts` 4 个类型化封装（`SmsTrashAddResult` 三态）；`MessagesApp.svelte` 真机气泡悬停「移入回收站」+ 工具栏「回收站 (n)」面板（单条恢复/清空/诚实边界提示文案），三态结果以状态条明示；i18n `message.trash*` 中英各 11 键。**验证**：`cargo test -p amos-tauri --lib` **237 例**（`sms::` +6：预览换上一条可见行、整线程隐藏、未知 id 诚实报错、恢复出自然预览+未读徽标、新消息后覆盖失效、落盘/重载 round-trip）；`cargo test -p amos-sms` **55 例**；`clippy --all-targets -D warnings`（host 与 `--features android`）、`fmt --check`、`cargo check -p amos-sms --features android` 全干净；前端 `tsc --noEmit`/`svelte-check` 0 错 0 警、vitest **471 例**全绿（`docs/sms.md` §13、`docs/TRACEABILITY_MATRIX.md` REQ-A42）。
### Added
- **短信余额脱敏：显示路径不透出银行余额（2026-09-11，REQ-A41）**。对 SMS 链路按 `docs/AEROSPACE_SOFTWARE_AUDIT.md` 做航宇级审计时发现的**真实暴露**：银行短信（扣款/到账通知）的核心敏感字段是余额，而「信息」页此前**原样显示**平台短信库返回的每一字节——演示投屏、他人借用手机的场景下余额一览无余。新增领域级脱敏模块 `crates/amos-sms/src/redact.rs`（纯函数、单遍 O(n)、有界窗口、panic-free、未命中零分配 `Cow`）：**规则 A**（任何发件人）——余额关键词（CJK 子串 + 拉丁词边界 + ASCII 大小写不敏感）后，在有界填充窗口内出现的金额（含分组逗号/小数/正负号/`元￥$€£` 后缀）整段替换为 `***`，关键词保持可见（`余额：***`）；**规则 B**（`is_bank_sender` 命中的银行短号，如 95588/95533/HSBC）——正文里任何紧跟 `:`/`：` 的金额一律脱敏。接线走**单一漏斗**：`amos-tauri/src/sms.rs` 的 `sms_snapshot`（线程预览）与 `sms_messages`（正文）是 UI 取短信的仅有两条路，统一改走 `redact_for(发件人, 正文)`；发件人缺失/未知时退化为纯内容规则（脱敏从不依赖发件人已知）；事件路径 `incoming_payload` 本就只带地址不含正文，Kotlin 无需改动；审计日志只记**掩码条数**，绝不记内容；平台短信库原文不动（视图层不改写系统库）。审计中顺带修了一个**确定性缺陷**：关键词匹配的 ASCII 大小写比较此前只折文本侧、漏了关键词侧，`USD`/`INR` 等大写货币词永远匹配不上——改为双侧 `eq_ignore_ascii_case`。**诚实边界**：全角数字（`１２３`）当前**不**识别为金额（文档与测试均写明，不假装覆盖）。**测试**：`amos-sms` 新增 `redact::` **18 例**（规则 A/B、幂等、有界、panic-free 模糊、BMP/多字节边界、固定向量表）；桥 `sms::` **+3**（预览脱敏而存储映射不动、按发件人分级、种子演示数据零掩码=诚实 no-op）；`cargo test -p amos-sms` **41 例**、`cargo test -p amos-tauri --lib` **231 例**；`clippy --all-targets -D warnings`（host 与 `--features android`）、`fmt --check`、`cargo check -p amos-sms --features android` 全干净（`docs/sms.md` §12、`docs/TRACEABILITY_MATRIX.md` REQ-A41）。
- **手机管家补全：内存加速（boost）进入统一审计 trail（2026-09-11，REQ-DC57）**。对手机管家按 `docs/AEROSPACE_SOFTWARE_AUDIT.md` 做航宇级审计时发现的**真实功能缺口**：清理与卸载都写守护进程的统一审计 sink，唯独**强制停止应用的 boost 没有任何审计事件**——`BoostOut` 无 `audit` 字段、领域层无 boost 事件构造、trail 卡的 op 标签表缺 `devcare.boost`，一个有后果的动作在 trail 上完全不可见（DO-178C 可追溯性缺口）。现补全四层：**(a) 领域内核**（`amos-devocare/src/audit.rs`）：新增纯函数 `boost_events(actor, requested, attempts)` 与 `BoostReclaim{id, ok, message}`——聚合一条 `devcare.boost`（`details = "requested=N attempted=A reclaimed=M failed=K"`，计数取自输入本身 ⇒ 聚合与逐条记录不可能互相矛盾；**部分失败即 `error`**，与 clean 同一规则；**`success` 仅当零失败且 `attempted == requested`**——簿记丢失应用时短缺在四数差异里可见，绝不读作完整成功）+ 每条失败一条 `devcare.boost.item`（有界 `MAX_ITEM_RECORDS`、按字符截断、确定性），新增 op 常量 `OP_BOOST`/`OP_BOOST_ITEM` 并从 `lib.rs` 导出。**(b) 桥**（`amos-tauri/src/devcare.rs`）：`devcare_boost` 逐条 kill 时同步构造 `attempts`，governor 可达即构造事件并 `record_audit`；`BoostOut` 新增 `audit: AuditStatus` 如实回报落盘与否；governor 不可达 ⇒ 零事件 + 零值 audit（「未尝试」，**不伪造 trail**）。**(c) 前端**（`lib/devcare.ts`）：`BoostView`/`normalizeBoost` 全量携带 `audit`（缺省降级为 0/0「未尝试」，绝不假装有 trail），`OP_I18N` 补 `devcare.boost(.item)`。**(d) UI + i18n**：`DeviceCareApp.svelte::boost()` 复用共享审计状态（加速后与清理/卸载同样显示「已记录审计 (1/1) / 审计未记录」，未发生时该行不渲染），新增 `care.op.boost`/`care.op.boostItem` 中英标签。**测试**：领域 `audit::tests` **+7**（全成功恰一条记录、部分失败=error+逐条、**未尝试每个请求项则绝不读作成功**、**请求外的尝试可见且非成功**、无事可做是一条诚实的 `requested=0` success、有界 50 + 多字节按字符截断、确定性）；桥 `the_boost_out_reports_whether_it_was_audited`（wire 形状：`audit.recorded/attempted` 存在、全录成时 `reason` 为 `null`、零值 audit 表示未尝试）；前端 `normalizeBoost reports a partial boost honestly` 扩展（audit 全量 + 缺省降级 + **reason 透传** + 畸形 audit 降级）、`auditComplete separates a full boost trail from a partial one`、`opLabelKey` 补 boost 两键；DOM 4 例（加速回报审计、部分失败显示「审计未记录」、未发生不渲染 trail、**governor 可达但无可回收项的空加速仍显示已记录事件**）；`devcare_audit_e2e` 扩到 **5 条记录**——`devcare.boost(.item)` 两 op 也过**真 daemon** 落盘（`error` 拼写与四数 details 读回校验）。**验证**：`cargo test -p amos-devocare` **84 lib + 4 集成**、`cargo test -p amos-tauri --lib` **228**（`devcare::` **28**）、`cargo test -p amos-tauri --test devcare_audit_e2e` 1 例、clippy `--all-targets -D warnings`（default 与 `--features android`）与 `fmt --check` 干净；前端 `bun run check` **EXIT=0**（pure **949**、`test:svelte` **471** / 59 文件、`tsc` 0、`svelte-check` 0/0）（`docs/devcare.md` §3.6/§3.7/§6 REQ-DC57/§7）。
- **修复：日历「编辑重复场次显示错日期」+ 真机（S5 / Android 14）语义层验收（2026-09-11）**。真机验收时发现真实缺陷：`CalendarApp.openEdit` 用**序列锚点** `event.startAt` 播种编辑器，于是点开一个**日常重复**事件在「今天」的场次，编辑器却显示**昨天**（该序列的创建日）的日期——真机上的表现是选中 `9月11日周五` 的 `09:00 团队同步`，编辑器「开始/结束」却都是 `2026-09-10`。**根因**：草稿取自 `event.startAt`，而非用户点开的 `occurrence.startAt`。**修复**：`openEdit` 改为按**用户点开的那一场**（`occ.startAt`/`occ.endAt`）播种日期/时刻；提交时用新增纯函数 `shiftByWallClock(base, from, to)`（`countDays` 整日 + 分钟差，**绝不裸毫秒**）把草稿相对该场次的位移**同等地施加到序列锚点**——日期差走 `addDays`（DST 安全）、时刻差按分钟；`from === to` 返回**逐位相同**的原值（故只改标题/备注不会让序列漂移），非重复事件（`occ === anchor`）行为**完全不变**（回归用例已钉）。编辑器另显示「重复日程：保存将修改整条序列」（新 i18n `calendar.seriesHint`，中英对称），把「编辑整条序列」这一诚实边界显式化。**真机证据**：`adb` + **S5 / Android 14**，经 `uiautomator` 读取 WebView 无障碍树，确认真机日历**日期渲染正确**（标题 `2026年9月`、周一开头 `一二三四五六日`、42 格 `8月31日…10月11日`、今日高亮、每日 `N 个日程`）；`make android-app` 重建安装后复验。**测试**：`calendar.test.ts` 49→**50**（+`shiftByWallClock`：恒等无操作/同日改时刻/跨日保墙钟/月末整日/负向位移）、`svelte-tests/calendar.svelte.test.ts` 32→**36**（编辑器显示所点半场次的日期、不改动则锚点逐位不变、改时刻按墙钟重新锚定、非重复事件行为不变）；`bun run check` **EXIT=0**（pure **948** / 97 文件、`test:svelte` **467** / 59 文件、`tsc` 0、`svelte-check` 0/0、i18n 中英奇偶）（`docs/calendar.md` §3/§5/§6）。
- **日历（第三轮）：周视图 · 独立暴力参考回归 · 审计文档纠偏（2026-09-11）**。对日历内核做安全关键式自审计：先补上**可复现的独立验证**、再关闭一个已记录的 iOS 对齐缺口、并修正文档中的追溯漂移。(a) **独立验证（先审计）**：`calendar.test.ts` 新增「独立暴力参考」用例——用**不设快进、不设上限**的朴素枚举（逐 `k` 调 `shiftOccurrence`）与 `expandOccurrences` 对 **2000 组**随机（重复类型 × 2000–2040 基准 × 0–40 天时长 × 0–1000 天窗口）做**逐场全等**比对（固定种子 PRNG ⇒ 可复现、不抖动），把「重复展开绝不丢场次」这条曾出真实缺陷（长于一个周期的重复事件被快进越过）的性质变成**可回归**检查。另做**一次性 30 000 例**扩展交叉比对（更宽参数谱）得 **0 处不一致**；`normalizeEvents`/`normalizeCalendars` 的**幂等性与不变量**（`endAt>startAt`、全天=本地午夜起、`allDayEnd` 幂等）**20 000 例**、`occurrencesByDay` 与逐日暴力判定 **4 000 例**，均 **0 处不一致**。(b) **功能补全：周视图**（此前 `docs/calendar.md` §2/§6 列为 🟡 未实现）。纯内核新增 `startOfWeek(ms, weekStartsOn)` / `weekDays(anchor, weekStartsOn)`：用**与月网格同一**的区域首周日规则（`back = (getDay() − weekStartsOn + 7) % 7`）推出所在周的 **7 个本地午夜**，逐日走 `addDays`（非裸毫秒）——DST 的 23h/25h 日仍算一天，故恒为 7 格、不漂移，两视图不可能对「一周从哪天开始」分歧。UI 增第三视图页签「周」（`data-view="week"`）：7 个日分区（`data-week-day`、今日 `data-today` 高亮、每日场次计数与空态），复用既有 `eventRow`（延续标签/跨度/日历色一致），点日头即选中该日；‹ › 在周视图**按周翻**（`shiftPeriod`），标题与按钮 aria 随视图切换（`calendar.week`/`calendar.prevWeek`/`calendar.nextWeek`，中英对称），「今天」回到本周。(c) **文档纠偏（审计发现）**：`docs/calendar.md` §5 的 `calendar.svelte.test.ts` 计数长期标注 **24**，实际文件早已 **29** 例（前几轮补测未回填）——本轮更正为实测值并补入周视图用例；同步刷新 §1 分层表、§2 对齐矩阵（新增「周视图」行、月/周/日程三视图 ✅）、§3 周网格确定性约定、§6 诚实边界。**验证**：`bun run check` **EXIT=0**（pure **947** 例 / 97 文件、`test:svelte` **463** 例 / 59 文件、`tsc` 0、`svelte-check` 0/0、i18n 中英奇偶）；`calendar.test.ts` 46→**49**、`calendar.svelte.test.ts` 29→**32**（+3 周视图）；`bun run coverage:gate` 聚合 `src/lib` **93.84% ≥ 90%**，`calendar.ts` **432/432**、`calendarCore.ts` **94/94** 插桩行全执行（lcov 无 `DA:*,0`）（`docs/calendar.md` §5）。
- **手机管家：电量读数改用真机 `BatteryManager`（2026-09-11）**。补上 `docs/devcare.md` §7 路线图的**最后一个待办**，并把「未观测必须如实上报」这条纪律延伸到电量。(a) **Kotlin `DevCareGlue.battery()`**（`android-glue/.../DevCareGlue.kt`）：读**与 `amos-power` 的能量 governor 同一处**的 sticky `ACTION_BATTERY_CHANGED` 广播（`level`/`scale` → `level_pct`、`status` → `charging`，`BATTERY_STATUS_CHARGING`/`FULL`），`thermal_throttled` 取**平台自己的** `PowerManager.currentThermalStatus >= THERMAL_STATUS_MODERATE`（API 29+；更早版本**省略**该字段 ⇒ 未知，绝不伪造成「未降档」）；读不到时返回 `{"error":…}`。`bind()` 自检新增 `self-check battery =>`，使设备日志可决定性验证。(b) **Rust 桥**（`crates/amos-tauri/src/devcare.rs`）：`CareScanner` 新增 `battery()`（**默认未知**，host-fs 后端天然不提供），新增 `BatteryReading{level_pct,charging,thermal_throttled}`（全字段可选）+ 唯一折叠点 `BatteryReading::care()`；`DevCareBridge::report` **以后端读数为准**，只有当后端**全未知**时才回落真实宿主读数（`host_battery.rs`，桌面）——后端一旦答了**任何**字段就是权威，**不拿宿主读数补全一个部分回复**（那会把两个来源混成一个）。(c) **折叠的诚实不对称**：`level_pct`/`charging` **未知 ⇒ 电池区不评估**（把它默认成「未充电」会把 3% 的部分读数伪造成 `care.battery.critical`——凭空造问题）；`thermal_throttled` **未知 ⇒ `false`**（只会**少记**一条热罚分，永远不会制造一条）。即「未知只让少说，不让人多说」。(d) **设备解析**（`devcare_device.rs`）：`AndroidScanner::battery()` + host 可测的 `parse_battery_reply`（缺字段留 `None`、glue 错误/非 JSON ⇒ 全未知，绝不伪造 `0%`）。(e) **主线程保护**：`devcare_report` 随之改为 `async` + `spawn_blocking`（`devcare::offload`）——它的电池读要跨 JNI 进 `BatteryManager`（桌面回退还会 `pmset`），不能跑在 UI 线程。(f) **模板与编译源同步**：`android-glue/` 与 `gen/android/.../DevCareGlue.kt` 两处一致。**验证**：`cargo test -p amos-tauri --lib` **223**（`devcare::` **26**，新增 3 例：部分读数不伪造罚分 / 设备读数驱动报告 / 无法填满 `BatteryCare` 的读数保持未评估）、`--features android --lib` **226**（`devcare_device::` **4**，新增 1 例回包映射与未知保持）；`clippy --all-targets -D warnings`（default 与 `--features android`）干净；Kotlin `./gradlew :app:compileUniversalDebugKotlin` **BUILD SUCCESSFUL**（JDK 17 / Android SDK 36）。**诚实边界**：真机的**运行期**读数仍需 USB 设备按 `docs/devcare.md` §8.2 验收（判据含 `self-check battery =>` 与系统设置里的电量同值、API < 29 上不出现 `thermal_throttled`）；`amos-profiling` 的瞬时功耗（`BatterySample`：电流×电压 ⇒ mW）**未**并入本报告——`BatteryCare` 只承载电量/充电/降档三个事实，塞进一条领域无法解释的功率读数不如不报。

- **手机管家真机 Android 后端 + 存储概览（2026-09-11）**。把上一阶段留作 seam 的「设备 bring-up」变成可用后端，并顺手补上此前被列为「需要新磁盘读数源、否则得引入 unsafe FFI」的**存储概览**——它其实**不需要任何新的 Rust unsafe**：读数在 Kotlin 侧用 `StatFs` 完成，跨 JNI 只走与其它子系统同构的 String↔String JSON。(a) **Kotlin `DevCareGlue`**（`crates/amos-tauri/android-glue/com/amos/ai/glue/DevCareGlue.kt`）：`scanJunk()`（应用自身的 `cacheDir`/`codeCacheDir`/`externalCacheDir` 子树 + `filesDir` 的日志/临时/残留 APK/墓碑/空目录，外加 best-effort 的 MediaStore `Download` 残留安装包；分类规则与 `hostfs` 逐条一致）、`removeJunk(uri)`（私有路径先做 `canonicalFile` 前缀校验、目录只 `delete()` 不递归、`content://` 走 `ContentResolver.delete`）、`installedApps()`（真实 `PackageManager` 库存：`FLAG_SYSTEM` + APK 真实长度，读不到即省略 ⇒ `null`）、`uninstallApp(id)`（`ACTION_DELETE`）、`storage()`（`StatFs(Environment.getDataDirectory())` 的真实总/已用/可用）。(b) **Rust 设备后端** `crates/amos-tauri/src/devcare_device.rs`（feature `android`）：一个 `GlueRef`（`JavaVM` + 全局引用，`Arc` 共享）实现三个 seam——`AndroidScanner`（`CareScanner` + host 可测的 `parse_scan_reply`）、`AndroidCleaner`（`CleanProvider`）、`AndroidPackages`（`PackageSource`），由 JNI upcall `Java_com_amos_ai_glue_DevCareGlue_attach` 安装进同一个 `DevCareBridge`。(c) **桥的 seam 化**：新增 `CareScanner`/`PackageSource`/`StorageTotals` 与 `Removal{Removed,Launched}`；`devcare_apps`/`devcare_uninstall` 在设备上取**后端自己的**库存与卸载，`ACTION_DELETE` 只是**请求**（wire 新增 `launched`，`removed` 保持 `false`，UI 文案「已请求卸载，请在系统弹窗中确认」，**绝不假装已卸载**）；`InstalledEntry` 新增 `system`（设备取自 `PackageManager`，host 恒 `false`）。(d) **新增命令 `devcare_storage`** → `StorageOut{total_bytes,used_bytes,free_bytes,used_pct,measured,backend,groups,reclaimable_bytes,review_bytes}`：测不到时 `measured:false` 且字节全 `null`（UI 显示「—」，绝不伪造 `0 B`）；管家页新增「存储概览」卡片（总量 / 已用条 / 可用 / 分类可回收）。(e) **主线程保护**：`devcare_scan`/`devcare_clean`/`devcare_apps`/`devcare_uninstall`/`devcare_storage` 全部 `async` + `spawn_blocking`（`devcare::offload`），JNI/`PackageManager`/MediaStore 一律离开 UI 线程；`attach` 只安装后端、**不扫描**（见同日的 `### Fixed`）。(f) **manifest**：新增 `QUERY_ALL_PACKAGES`（`tools:ignore`），否则 API 30+ 的库存会被平台过滤（会被**如实**显示为更少应用，而非伪造）。**验证**：`cargo test -p amos-tauri --lib` **227**（`devcare::` **27**）、`--features android --lib` **231**（`devcare_device::` **5**）、`cargo test -p amos-devocare` **81**（lib 77 + 集成 4）；`clippy --all-targets -D warnings`（default 与 `--features android`）干净；前端 `bun run check` **EXIT=0**（`devcare.test.ts` **28 例**，含 `normalizeStorage` 未知≠`0`、`launched`≠`removed`；`device-care.svelte.test.ts` **36 例**，含存储三态与 launched 文案；全仓 `test:svelte` **458/59**）。真机：`make android-app`（Android 14 / S5，`aarch64`）**构建并安装成功**，产物内 `classes7.dex` 含 `DevCareGlue bound (Rust backend attached)` / `self-check storage =>` / `refusing a path outside the app's own directories`，`lib/arm64-v8a/libamos_tauri_lib.so` 导出 `Java_com_amos_ai_glue_DevCareGlue_attach`。**诚实边界**：未 root 零售机的扫描范围仅限应用自身，**不假装是全盘清理器**（文档已明写）。**真机运行期验收（已做，2026-09-11，S5 / Android 14）**：新增两个**显式 opt-in** 的诊断通道——Kotlin 的自检日志（`adb logcat -s DevCareGlue`，标记文件存在时才跑，见 `### Fixed` 第 3 条）与 `devcare_device` 的 bring-up 自检报告（`adb shell run-as com.amos.ai touch files/devcare-selfcheck` → 启动 → `cat files/devcare-selfcheck.out`），后者在真机上逐条证明了 JNI 契约：`storage` 总量与 `df -k /data` **逐字节一致**（247,410,065,408 B）、`scanJunk` 扫到 15 项且**扫到了 Rust 植入的临时文件**、`installedApps` **216 个包 / 215 系统包 / 全带真实尺寸**（与 `pm list packages` 一致）、`removeJunk` 让**文件真的消失**、越界路径**被 Kotlin 侧拒绝**。唯一未做的只剩「打开页面用眼睛看渲染」，步骤见 `docs/devcare.md` §8.4。

### Fixed
- **手机管家设备接线：后端从未被安装（顺序缺陷）+ `app.path()` 主线程自死锁 + 冗长自检每次前台卡 2 秒（2026-09-11，真机 S5 / Android 14 实测发现）**。把 §8.3 的验收真跑在设备上后，抓到三个**只有真机才会暴露**的缺陷——它们此前全部被「Kotlin 自检照常打日志」这种假象掩盖。(a) **设备后端从未挂上**：`DevCareGlue.bind` 在 Activity `onStart` 里经 JNI upcall `Java_com_amos_ai_glue_DevCareGlue_attach` 安装后端，而真机实测该 upcall **早于** Tauri 的 `setup` 钩子，于是 attach 里 `APP.get()` 为 `None` → **静默 `return`**，`DevCareBridge` 始终停在「无后端」（所有 `devcare_*` 命令都只会诚实报「no backend」——但没有用户看得见的线索）。而 Kotlin 那句 `DevCareGlue bound (Rust backend attached)` 是**无条件**打印的，所以日志看起来完全正常。诊断探针第一次给出铁证：`self-check rust => {"app_handle":false}`。修法：把两侧拆成两个 `OnceLock`（`APP` 引导钩子 + `GLUE` JNI attach）+ 一个 `INSTALLED` 原子标志，`install_android_app` 与 attach **都**调用 `install_backend_if_ready()`——**谁后到谁安装**，与顺序无关、幂等。设备复验：第二次前台 `{"app_handle":true,"available":true,"backend":"android","can_clean":true,"glue":true,"installed":true,"scanned_items":0}`。(b) **`app.path()` 在 Android 主线程上自死锁**：`PathResolver` 在 Android 上是**经 WebView JS 的往返**（`plugin:path|resolve_directory`）；从主线程（JNI 回调链）调用它会**等一个只能由同一个主线程跑出来的答复**。新加的探针在 `APP` 首次存在时正好走到 `app.path()`，于是**整条 `self-check rust` 日志彻底消失、且没有任何异常**（挂了一分钟以上）——「静默挂死」比报错更难查。修法：探针**完全避开 `app.path()`**（解析出的路径由**工作线程**写的自检报告给出），并把这条写进代码注释与 `docs/devcare.md` §8.3，作为后续任何 JNI 回调里碰 `app.path()` 的显式警告。(c) **冗长自检在每次 `onStart` 的主线程上跑 ~2 秒**：Kotlin 自检里的 `installedApps()` 设备实测耗时 **1.9–2.1 s**（`08:13:40.195 → 08:13:42.144`），而它原先**无条件**执行 → 每次冷启动/回前台都卡主线程约 2 秒。现改为**标记文件驱动**（`files/devcare-selfcheck` 存在才跑），生产启动零开销；Rust 侧的 bring-up 自检同样只在标记存在时运行，且始终在**独立工作线程**上。**验证**：`cargo test -p amos-tauri --lib` **227**、`--features android --lib` **231**、`clippy --all-targets -D warnings`（default 与 `--features android`）干净、`cargo fmt --check` 0 diff、前端 `bun run check` EXIT=0；真机 `make android-app` 构建安装成功，且自检报告与探针输出即为本次修复的证据（`docs/devcare.md` §8.3）。

- **电量自审计：glue 会在 `status` 缺失时伪造「未充电」+ 越界 `level` 被夹成健康百分比 + `scanPublicDownloads` 静默截断（2026-09-11）**。对刚落地的电量读数做对抗式审计，发现三处真实缺陷，都属「把未知/坏数据说成已知」。(a) **`charging` 凭空为 false（最严重）**：`DevCareGlue.battery()` 原为 `isCharging(intent.getIntExtra("status", 0))`——`getIntExtra` 在字段**缺失**时返回默认值 `0`，而 `0` 不是任何合法的 `BATTERY_STATUS_*`（`BATTERY_STATUS_UNKNOWN` 本身是 `1`），于是「完全没读到充电状态」被写成 `"charging":false`。Rust 侧随后把它当成**已知事实**，在电量 ≤ 20% / ≤ 5% 时生成 `care.battery.low` / `care.battery.critical` 罚分——正是本轮文档写明「未知只能让我们少说」要防的那类**凭空造出的告警**（Rust 侧的 `charging: None ⇒ 不评估` 防线被 glue 的默认值绕过了）。现改为 `chargingFrom(getIntExtra("status", BATTERY_STATUS_ABSENT))`：只有 `CHARGING`/`FULL` ⇒ `true`、`DISCHARGING`/`NOT_CHARGING` ⇒ `false`，`BATTERY_STATUS_UNKNOWN` 与缺失一律**返回 `null` ⇒ 省略该字段**（Rust 侧即「未知 ⇒ 电池区不评估」）。(b) **越界 `level` 被夹紧**：`level > scale` 是一条自相矛盾的广播，原实现靠 Rust 的 `clamp` 变成「100%」，等于把坏数据渲染成**健康**；现 Kotlin 直接判读数失败（`{"error":…}`），Rust 的 `clamp` 仅保留为宿主/其它后端的第二道防线。(c) **`scanPublicDownloads` 静默截断**：它的循环条件 `cursor.moveToNext() && rows < MAX_ITEMS` 封顶的是「已检查行数」并在超限时**静默返回 `false`（=扫描完整）**，与同文件 `walk` 的语义（封顶**上报项数**、超限即 `true` ⇒ 整个扫描报错）不一致——于是一台 `Download` 集合很大的机器会得到一个**被截短却看似完整**的清单（`docs/devcare.md` §5.3 明禁）。现改为走完游标、只在**上报**第 `MAX_ITEMS + 1` 项时判超限，与 `walk` 逐字一致；顺带补注释说明「`SIZE` 读不到时记 `0`」只会**低估**可回收空间（安全方向），不会高估。**验证**：`cargo test -p amos-tauri --features android --lib devcare` **30**（`devcare::` 26 + `devcare_device::` 4；`a_battery_reply_maps_every_field_and_keeps_unknowns_unknown` 新增「`status` 缺失 ⇒ `charging` 未知」与「字段类型不对 ⇒ 当作缺失」两组断言）、`cargo test -p amos-tauri --lib` **223**、`clippy --all-targets -D warnings`（default 与 `--features android`）干净、`cargo fmt --check` 0 diff；Kotlin `./gradlew :app:compileUniversalDebugKotlin` **BUILD SUCCESSFUL**。**诚实边界**：Kotlin 侧行为（`registerReceiver` 取到的 `status`/`level`）仍需真机按 `docs/devcare.md` §8.2 验收；`amos-power/src/android.rs` 的同款调用（`status.unwrap_or(0)` ⇒ `charging=false`）**未**改动——那里的 `BatteryState.charging` 是不带 `Option` 的策略输入，其「未知即按省电处理」是**保守方向**（只会更省电，不会误报故障），与管家「会把未知渲染成用户可见告警」的危害不同。
- **电量自审计（第二轮）：宿主回退沿用旧默认值 + `Not charging` 观测被丢弃 + `Downloads` 靠异常兜底 + sysfs 条目读不懂就整盘放弃 + Linux-only 分支本机验不到（2026-09-11）**。继续对电量链路的**宿主回退与平台分支**审计，发现六处真实缺陷。(a) **宿主回退仍沿用旧的 `unwrap_or(false)`**：`DevCareBridge::report` 里宿主分支直接内联构造 `BatteryCare`，把缺失的 `charging` 默认成 `false`——即上一轮刚在**设备**侧修掉的「未知 ⇒ 未充电 ⇒ 凭空低电量告警」，在**宿主**侧依旧存在（`host_battery` 的 macOS `pmset` 解析确有 `None` 分支）。现抽出**纯函数** `reading_from_host()`，与设备读数走**同一个** `BatteryReading::care()`：缺充电标志 ⇒ 电池区**不评估**。(b) **`/sys/class/power_supply/*/status` 的 `Not charging` 被当成未知**：`host_battery::linux::charging_from_status` 的注释写着「Full / Not charging while still plugged reads as 'plugged'」，但代码只映射了 `"Full"`——文档与实现不一致，且后果被 (a) 的从严语义**放大**：插着电但没在充（充电上限/驱动行为，极常见）的机器会**整块丢掉电池区**。现补 `"Not charging" => Some(true)`（已观测的「插电」，与 macOS 侧 `drawing_ac` 同义）。(c) **该纯映射在非 Linux 主机上根本不编译**（`mod linux` 整体 `cfg(target_os="linux")`），所以这条修复在开发机上**既没编译也无法测试**——正是 `clamp_level` 用 `#[cfg(any(target_os = "linux", test))]` 想避免的死角。现按同一惯例把 `charging_from_status` 提到父模块并加 `cfg(any(linux, test))`，测试改名 `sysfs_charging_status_maps_only_observed_states`（新增 `Not charging`、尾随换行、未知词三个断言），**在 macOS 上真实执行**。(d) **`scanPublicDownloads` 靠异常兜底 API 级差异**：`MediaStore.Downloads` 是 API 29+，而 minSdk 是 26，原实现依赖调用方的 `catch (t: Throwable)` 去吞 `NoSuchFieldError`（预期行为由异常驱动）。现加**显式** `Build.VERSION.SDK_INT < Q ⇒ 跳过`，并在 §8.4 明写「API 26–28 清不到公共下载残留安装包是**平台限制**，不是『没有垃圾』」。(e) **`/sys/class/power_supply` 里读不懂的条目会终止整盘读取**：`linux::read` 用 `?` 读 `type`/`capacity`，任何**非电池且没有 `type` 的条目**（真实机器上常见）都会让整个目录遍历**提前返回 `None`**——与 `charging_from_status` 的「未知只是未知」不同，这里丢的是**一个明明可读的电池观测**（`host_battery` 于是报「未知」）。现改为 `continue` 跳过该条目、继续找（`capacity` 解析失败同理，另一块电池可能没问题）。(f) **Linux 分支在开发机上既没编译也无法测试**（`mod linux` 整体 `cfg(target_os="linux")`）：正是 `clamp_level` 用 `#[cfg(any(target_os = "linux", test))]` 想避免的死角。现按同一惯例把 `charging_from_status` 提到父模块、把 `mod linux` 整体门控为 `cfg(any(target_os = "linux", test))`，两个文件系统测试随之**在 macOS 上真实执行**（新增 `linux_reports_unknown_when_no_battery_is_readable`，并在 `linux_reads_battery_from_a_real_tree` 里先建 decoy 条目——无 `type` 目录 + 非电池 `AC`——再建电池，使它真能当回归探测器）。**验证**：`cargo test -p amos-tauri --lib` **227**（`devcare::` **27**，新增 `a_host_reading_is_folded_without_inventing_a_charger_state`；`host_battery::` **7**，新增两条，全部在本机执行）、`--features android --lib` **230**（`devcare_device::` **4**）、`clippy --all-targets -D warnings`（default 与 `--features android`）干净、`cargo fmt --check` 0 diff、Kotlin `:app:compileUniversalDebugKotlin` **BUILD SUCCESSFUL**。**诚实边界**：macOS `pmset` 分支的 `charging: None` 仍是**真实存在**的路径（`parse_pmset` 的 `else` 分支），但本机跑的是「有电池的 Mac」，无法构造该分支的端到端读数——故只对**纯折叠**与**纯映射/临时目录读取**做了断言，运行期仍按 §8.2 验收。
- **电量自审计（第三轮）：越界档位被夹成具体主张（负数 ⇒ 凭空「电量极低」告警）+ 截断会把 20.6% 说成低电量（2026-09-11）**。对折叠规则本身做最后一轮对抗式审计，发现 `BatteryReading::care()` 用 `level.clamp(0.0, 100.0) as u8` 处理档位，于是**两条编造路径**都成立：(a) **负数档位 ⇒ 凭空造出「电量极低」**：`clamp(-5.0, 0, 100) = 0`，配上 `charging=false` 就直接生成 `care.battery.critical`（UI 文案「电量极低，请立即充电」）——**由一条畸形数据造出的用户可见告警**，正是本轮文档反复强调「未知只能少说」要防的事（glue 在源头已拒 `level < 0`，但 Rust 侧本该是纵深防御，却反过来把越界值**夹成一个具体主张**）。(b) **上越界被夹成「健康」**：`150 ⇒ 100`，把一条自相矛盾的数据说成满电。(c) **`as u8` 截断而非取整**：`20.6 ⇒ 20`，恰好跨过领域的「≤ 20% 即低电量」阈值，把**高于**阈值的读数报成「电量偏低」。现 `care()` 改为：`!finite || !(0.0..=100.0).contains(&level) ⇒ None`（**越界不是读数**，电池区不评估，与 glue 拒绝 `level > scale` 同一条规则），并把档位改为 **`round()`**（`20.6 ⇒ 21`、`42.6 ⇒ 43`）。领域的 `health::assess` 仍有 `min(100)` 兜底，但桥现在不会再给它造出越界值。**验证**：`cargo test -p amos-tauri --lib` **227**（`devcare::` **27**；`a_partial_battery_reading_never_becomes_a_fabricated_finding` 扩为「6 个越界/非有限值（`-1.0`/`-0.5`/`100.5`/`150.0`/`NaN`/`∞`）一律 ⇒ `None`、`0.0` 与 `100.0` 边界仍是真读数、`20.6 ⇒ 21` 取整」；`a_host_reading_is_folded_without_inventing_a_charger_state` 相应改为断言「宿主读数缺标志/越界 ⇒ 不评估」）、`--features android --lib` **230**、`clippy --all-targets -D warnings`（default 与 `--features android`）干净、`cargo fmt --check` 0 diff、`./gradlew :app:compileUniversalDebugKotlin` **BUILD SUCCESSFUL**（只做类型检查，不重建 APK）。**诚实边界**：这仍是**纯折叠层**的断言；真机/宿主**运行期**读数（`BatteryManager` 的实际 extras、`pmset`/sysfs 的真实取值）依旧按 `docs/devcare.md` §8.2 验收；本轮 Kotlin 无改动（`Math.round` 已产出整数、越界也已拒），所以没有任何新的真机产物可验。
- **航空航天级审计（量测真实性 + 部分体检可见）：给缺失尺寸填 `0`、部分报告看起来像完整体检（2026-09-11）**。按本仓自己的适航级标准（`docs/AEROSPACE_SOFTWARE_AUDIT.md`，尤其 **P0-3「禁止用占位值冒充有效量测」**）逐条复核设备管家与电量链路，发现两处同时踩中该规则的缺陷，并补齐可追溯性。(a) **`parse_item` 给缺失尺寸填 `0`**（`v.get("size_bytes").and_then(as_u64).unwrap_or(0)`）**且 glue 对 MediaStore `null` SIZE 填 `0L`**：`JunkItem::size_bytes` 是 `u64`、**表达不了「未知」**，于是「读不到尺寸」被当成一条**真实量测**（正是 P0-3 明令禁止的「填 0 冒充有效值」），后果不只是难看——`reclaimable_bytes` 与清理实际释放的字节都被**低估**，用户在界面上看到的"能清出多少"是错的。现改为**跳过该行**（两侧都是），并让它**计入 `unreadable_dirs`**：这是一个**部分扫描**，UI 明说「N 个目录不可读（扫描不完整）」而不是装作扫干净了；真正的 `0`（空文件/空目录）是真实读数，照常保留。(b) **部分体检渲染成完整体检**：`assessed` 此前**只**被 `reportHasData` 用来判「全空 ⇒ 显示未知」，可是**部分**观测（例如只测了存储）会照常渲染一个自信的分数/等级，毫无「电池根本没测」的痕迹——即「未测 = 健康」，与 `docs/devcare.md` §4.1「未知 ≠ 健康」的承诺相悖。现新增**纯函数** `careAreaSplit`（按 `CareArea` 域枚举序切出「已观测 / 未观测」，未建模的键**既不算已检测也不算未检测**——不发明）与管家页的「已检测 / 未检测」分区（i18n `care.assessed`/`care.notAssessed`，中英对齐），未观测的报告**不显示**该行。**验证**：`cargo test -p amos-tauri --lib` **227**、`--features android --lib` **231**（`devcare_device::` **5**，新增 `a_row_without_a_usable_size_is_left_out_instead_of_sized_zero`：缺 / `null` / 字符串尺寸全跳过且计 partial、真 `0` 保留）；前端 `devcare.test.ts` **29**（`careAreaSplit` 域序/部分/未建模键/`null`）、`device-care.svelte.test.ts` **39**（部分报告列出未检测分区、全检测无告警、未观测不显示该行）；**`make lint` EXIT=0**（`cargo fmt --all --check` + `cargo clippy --workspace --all-targets -- -D warnings` + `tsc --noEmit`）；`bun run check` EXIT=0；Kotlin `:app:compileUniversalDebugKotlin` **BUILD SUCCESSFUL**。**可追溯性**：`docs/TRACEABILITY_MATRIX.md` 补 **REQ-A39**（电量真机化，此前未登记）、**REQ-A40**（本轮两项）、**REQ-B7**（P0-3 延伸）；`docs/AEROSPACE_SOFTWARE_AUDIT.md` §8 登记本轮「发现 → 处置 → 验证」。**诚实边界**：真机**运行期**读数（`BatteryManager` extras / sysfs 实际取值）仍需按 `docs/devcare.md` §8.2 在设备上验收，本轮未重建 APK；**能力代价**已明写——尺寸读不到的残留安装包不在清理候选里（宁可少清一个，也不报一条假字节数）。
- **航空航天级审计发现的确定性缺陷（两处）：`amos-audio` 与 `amos-appstore` 的偶发红（2026-09-11）**。全量 `cargo test --workspace` 与 Gradle 构建**并发**时先后暴露两个**偶发**失败，根因都不在生产代码，而在**用例自身依赖时序/共享状态**——对适航纪律而言，**不可复现的失败等于没有证据**（DO-178C 要求可重复验证）。(a) `amos-audio::ring::tests::concurrent_producer_consumer_loses_nothing`（`producer never EOFs mid-stream`）：`read()` 按文档只在「整个读预算（默认 1 s）内没有样本」时返回 `0`，一个被机器负载饿死的生产者**足以**触发它，于是 `assert!(n > 0)` 断言的是一个**调度事实**；同时生产者按固定 1 ms sleep 推进，`dropped() == 0` 又要求消费者在 512 样本（≈8 ms）内被调度——两者都随负载变化，同一个二进制会无缘无故红/绿。现改为**自我节流**：生产者等**剩余容量**（`pending() + chunk <= capacity()`）再 `push`，使 `dropped == 0` **由构造保证**而非靠运气；消费者把 `0` 读按 `read` 文档语义当作**饥饿**继续排空，附 60 s 上限防死循环。**ring 的生产契约一字未改**（`0` 仍表示「预算内静默」）。(b) `amos-appstore::serve::tests::resolves_index_asset_and_content_type`（`serve.rs:183` 的 `resolve_request(...).unwrap()`）：`installed_dir()` 的临时安装根**只按进程 id** 命名，而同一测试二进制里的用例是**并行**跑的，于是它们共用并互相重装/擦除同一个目录——A 解析路径时 B 刚把它清掉。现按仓内既有的夹具惯例（`hostfs`/`host_battery` 的「**进程 id + 原子序号**」）给每次调用一个独立根目录。**验证**：修复前 `cargo test --workspace` **EXIT=101**（先 `-p amos-audio --lib` 1 failed、后 `-p amos-appstore --lib` 1 failed）；修复后 ring 用例单跑 **12/12**、在 **6 个 CPU 打满的干扰进程**下连跑 **20/20** 全绿（原版本在此负载下必红）；`amos-appstore --lib` **55 passed** 且重复 **10/10**；`cargo clippy -p amos-audio/-p amos-appstore --all-targets -- -D warnings` 干净（顺带修掉 `let mut flush` 的 `unused_mut`）；`cargo fmt --check` 0 diff。登记 `docs/TRACEABILITY_MATRIX.md` **REQ-B10**、`docs/AEROSPACE_SOFTWARE_AUDIT.md` §8。










- **手机管家设备接线：`attach` 在主线程同步扫描（与本仓文档承诺相反）+ 重挂后端沿用旧快照（2026-09-11）**。对设备侧接线做对抗式审计，发现 `DevCareBridge::attach_device` 在安装后端后**立即**调用 `scanner.scan()`；而该函数只由 JNI upcall `Java_com_amos_ai_glue_DevCareGlue_attach` 调用，其来源是 Activity `onStart` 里的 `DevCareGlue.bind`——也就是 **UI 主线程**。对 host-fs 这只是一次本机 walk，但对 Android 后端它就是一次 `scanJunk()` 文件系统遍历（+ MediaStore 查询）跨 JNI 跑在主线程上：既可能掉帧/ANR，也**直接违背三处既有承诺**——`devcare_device.rs` 模块文档「the attach does not scan」与其 `attach` 文档、`DevCareGlue.kt` 的「the junk walk stays lazy so boot never blocks on a filesystem sweep」、`docs/devcare.md` bring-up「`attach` 只安装后端、**不扫描**」。(a) `attach_device` 现只安装后端、**不扫描**，并**清空旧快照**（`scan.clear()` + `unreadable_dirs = 0`）——否则重挂后端会把**上一个**后端的扫描结果冒充成新后端观测到的（P0-3「未观测必须如实上报」）；扫描一律经 `devcare_scan` 的 `offload`→`spawn_blocking` 执行，因此 `devcare_status` 在扫描前如实返回 `available:true, scanned_items:0`（前端 `refresh()` 本就会随后调用 `devcare_scan`）。(b) 测试更新/新增：`a_device_backend_drives_scan_inventory_and_storage` 现断言 attach 后 `scanned_items == 0`、命令执行后才为 `1`；新增 `re_attaching_a_backend_drops_the_previous_snapshot` 锁死「陈旧快照不得穿越重挂」。**验证**：`cargo test -p amos-tauri --lib` **218 例**（`devcare::` **23**，+1）；`cargo clippy -p amos-tauri -p amos-devocare --all-targets -- -D warnings` 干净；`rustfmt --check` 对改动行干净（`devcare*.rs` 的既有 fmt 差异属另一工作流，未触碰）（`docs/devcare.md` REQ-DC49）。**诚实边界**：真机上 `ACTION_DELETE` 的异步确认、`scanJunk()` 的真实耗时仍属设备验收项；此处只保证**不再在主线程扫描**、且不把旧后端的观测冒充成新后端的。
- **`amos-media` Android 后端整项 `load` 的无界分配（2026-09-11）**。上一轮给整项加载加了 `MAX_LOAD_BYTES`（256 MiB）并落到 **HostFs/Mock**（先 stat 再读），但 **`AndroidMediaProvider::load` 漏了**：它把 Kotlin `MediaStoreGlue.loadContent(uri)` 返回的 base64 直接解码，于是 MediaStore 里任意大小的视频/文件都会被整段跨 JNI 编码、再在 Rust 里物化成一个无上限的 `String`/`Vec<u8>`——正是上一轮修掉的那类缺陷，只是换了个后端。(a) 新增纯函数 `check_declared_size(item)`：`load` 在**跨 JNI 之前**用 MediaStore 上报的 `size_bytes` 判定，超出即返回类型化 `TooLarge`（不发生 base64 往返）；(b) 解码后再 `ensure_loadable` 兜底一次，所以尺寸缺失/失真时也**绝不**无界分配（未知尺寸不被默认当作危险，而是解码后按实测字节判定）。**验证**：`cargo test -p amos-media`（lib **38** / api **10** / media_core **24**）全绿；`cargo test -p amos-media --features android` → lib **40**（`android::` **+1**：`a_declared_oversized_item_is_refused_before_the_glue`）；`cargo clippy -p amos-media --all-targets -- -D warnings`（default 与 `--features android`）均干净；`cargo test --workspace --no-fail-fast` **134 个测试二进制全绿（EXIT=0）**（`docs/media.md` §2/§6）。**诚实边界**：Android 后端仍未覆写 `read_range`，其范围读取走默认「有界 `load` 后切片」——真机上的**真流式 seek** 需要 Kotlin 侧新增分段读取 JNI，属设备 bring-up 项，**未在本环境伪造**。
- **手机管家领域内核 + Android glue 自审计：跨组求和溢出、计划去重依赖扫描顺序、扫描超上限被静默截断（2026-09-11）**。继续对设备管家做对抗式审计，发现并修掉三处真实缺陷。(a) **跨组求和会溢出**：`junk` 全模块用 `saturating_add`，但 `JunkReport::reclaimable_bytes` / `review_bytes` 用的是 `.sum()`——一个自相矛盾/敌意的扫描（多项 `size_bytes = u64::MAX`）会让跨组相加在 **debug 构建 panic、release 构建静默回绕**（已用一个独立 `rustc` 程序证实 debug 下确会 panic）。现改为 `fold(0u64, u64::saturating_add)`，并把测试 `analyze_saturates_rather_than_overflowing` 扩展到跨组的 `reclaimable_bytes`/`review_bytes`（修复前该断言 panic）。(b) **`plan` 去重依赖扫描顺序**：同一 `uri` 被报成两个不同类别时，原实现保留「先看到」的 `kind`（`size_bytes` 取最小是对的），于是同一份数据换个扫描顺序就得到不同计划——违反本 crate「同输入⇒同输出」的确定性承诺。现 `kind`/`owner` 也取确定性的最小值（`size_bytes` 保持保守最小），新增 `plan_deduplication_across_kinds_is_independent_of_scan_order`。(c) **Android glue 扫描超上限被静默截断**：`DevCareGlue.scanJunk()` 在凑满 `MAX_ITEMS`（10 000）时就停止并返回数组，而 Rust `analyze` 只拒 `> MAX_JUNK_ITEMS`，所以被裁掉的尾巴**无人知晓**——正是文档 §5.3「破坏性输入从不被静默裁剪」所禁止的（host-fs 后端在超上限时是**硬错**）。现 `walk`/`scanPublicDownloads` 改为**插入前检查**，一旦还有第 `MAX_ITEMS + 1` 项就让 `scanJunk` 回 `{"error":…}`（UI 显示扫描失败，而不是看起来「没有垃圾」）；桥侧另加 **host 可测** 的 `parse_scan_reply` 作为防御纵深：达到上限的数组直接 `TooManyItems`，未知 `kind` 计入 `unreadable_dirs`（部分扫描可观测）。**验证**：`cargo test -p amos-devocare` lib **76**（+1）+ 集成 4；`cargo test -p amos-tauri --lib` **218**；`cargo test -p amos-tauri --features android --lib` → `devcare_device::` **3**（新增）；`cargo clippy -p amos-devocare -p amos-tauri --all-targets -- -D warnings`（default 与 `--features android`）干净；`rustfmt --check` 对改动行干净（`devcare*.rs` 既有 fmt 差异属另一工作流，未触碰）（`docs/devcare.md` REQ-DC50…52）。**诚实边界**：Kotlin glue 的真实运行仍需真机验收（本环境无 Android 运行时）；桥侧 `parse_scan_reply` 的行为已在 host 上以 `--features android` 覆盖。
- **日历重复展开：快进越过窗口，长于一个周期的重复事件丢场次（2026-09-11）**。审计新日历内核 `lib/calendar.ts` 时发现 `expandOccurrences` 的「快进」估算有真实缺陷。一场事件与 `[from,to)` 相交当且仅当 `startAt > from - dur`（半开区间），但快进用的是 `from` 而非 `from - dur`，并用**近似**步长（月按 30 天）估算 `k`——于是对**持续时间长于一个重复周期**的事件，估算值会越过首个仍与窗口相交的场次并把它静默丢掉。**实测**：一个 **20 天长的周重复**事件（1/1 起）在单日窗口 `[1/22,1/23)` 里应返回 **3** 场（1/8、1/15、1/22 —— 前两场的结束日仍覆盖 1/22），修复前只返回 **2** 场（丢了 1/8 那场）。影响面：日/周/月视图与「近期提醒」观察器（`calendarCore.ts` 的 `syncDueEventAlerts`）都走它，长会期重复事件会被**少算甚至漏提醒**。现 (a) 快进阈值改为 `from - dur`（正确的相交下限）；(b) 步长改为各周期的**上界** `MAX_STEP_MS`（日 25h、周 7d+1h、月 31d+1h、年 366d+1h，含 DST 与闰年余量）——上界保证估算值只会落在首个相交场次**之前**，绝不越过它（原步长不具此性质）。**验证**：`bun test src/__tests__/calendar.test.ts` → **46 例**（+1 `a repeat longer than its period never loses an overlapping occurrence`，修复前失败）；`bun run check` **EXIT=0**（bun **938 例 / 97 文件**、`svelte-check` 453 例 / 59 文件、`tsc` 0）（`docs/calendar.md` §3/§5）。**诚实边界**：`MAX_STEP_MS` 是保守上界，因此窗口远在未来时会多迭代极少几场（仍由 `MAX_OCCURRENCES_PER_EVENT` 有界）——用极小的确定性/性能代价换取不断场次的正确性。

- **文档承诺与实现不符（小口径）：`CareReport::assessed` 顺序 + `APP_META` 计数（2026-09-11）**。审计「文档说的」与「代码做的」是否一致时发现两处口径漂移。(a) `CareReport::assessed` 的文档写「in [`CareArea`] order」，而 `health::assess` 是按**探测顺序**（Storage→Battery→Permissions→Apps）push 的，与 `CareArea` 的枚举序（Storage→**Apps**→**Battery**→Permissions）不同——任何按文档假设该数组已排序的消费者都会拿到不同于声明的顺序。现 `assess` 在返回前 `assessed.sort()`（`CareArea` 已 derive `Ord`），文档承诺成立，并新增 `assessed_areas_are_reported_in_care_area_order` 钉住（含「部分观测保持相对序」）。(b) `frontend-ts/src/lib/appMeta.ts` 的模块注释写「for the **25** built-in apps」，而 `APP_META` 实为 **27** 条（`appMeta.test.ts` 本就断言 27；该注释在上一轮改之前就已是 23 vs 24 的旧账）——现更正为 27。**验证**：`cargo test -p amos-devocare` lib **77**（+1）+ 集成 4；`cargo test -p amos-tauri --lib` **218**；`cargo clippy -p amos-devocare --all-targets -- -D warnings` 与 `rustfmt --check` 干净（`docs/devcare.md` REQ-DC53）。
- **助手语音常驻采集：`submitted()` 与 `AudioEnd` 的顺序竞态（2026-09-11）**。全量测试在「工作区测试 + 前端门禁并发跑」的负载下暴露 `assistant_voice::tests::resident_capture_streams_speech_and_endpoints_on_trailing_silence` 偶发失败（期望 `submitted()==1`，实得 0，但同一时刻**已经收到** `AudioEnd`）。排查确认这是**生产代码的一致性竞态**，不只是测试问题：`spawn_resident_capture` 在两处先 `blocking_send(Payload::AudioEnd(true))`、**再** `submitted.fetch_add(1)`，于是消费方收到 `AudioEnd` 后立刻读 `submitted()`（Tauri 的 `resident_voice_status` 正是这样读）可能读到**落后**的计数——「流已收句」与「计数仍为 0」短暂自相矛盾。现改为**先自增再发送**（发送→接收本身构成 happens-before，故任何已观察到 `AudioEnd` 的读者必然看到对应计数）。**验证**：该单测单独连跑 **25/25** 全过；并**复现原失败场景**（`cargo test --workspace` 与 `bun run check` 并发执行）→ 工作区 **134 个测试二进制全绿（EXIT=0，无 panic）**、前端 **EXIT=0**；`cargo test -p amos-tauri --lib assistant_voice::` **18 例**全过；`cargo clippy -p amos-tauri --all-targets -- -D warnings` 与 `rustfmt --check crates/amos-tauri/src/assistant_voice.rs` 干净（`docs/audio-hal-bridge.md` 新增「顺序保证」段）。



- **桌面 UI 真实启动 + 真实浏览器验收（2026-09-11）**。把 System UI 真正跑起来并做端到端验收。(a) **生产包构建**：`vite build` 曾**一次性**报 `[vite:esbuild-transpile] Unexpected "\x03"`；用「去掉 minify」与「esnext 目标」两种探针构建 + 直接对失败 chunk 跑 `esbuild --minify` 逐层排除，确认**源码无控制字符、chunk 干净、esbuild 单独 minify 成功**，且报错片段是**比工作区更旧的 MailApp 版本**——即当时构建**读到了一份被并发改写的旧工作树**，属环境瞬时干扰而非代码缺陷；随后同一命令 `bun run build` **成功**（并已删除所有临时探针配置与产物）。(b) **新增可重复的真实 UI 冒烟 `scripts/smoke-player.mjs`（`bun run smoke:player`）**：起/复用 `vite preview` → 用**真实 Chromium 无头加载生产包**，经 `shell-entry` 的 URL 表面 `shell.html?surface=app&id=player` 挂载播放器，断言 **6 个 marker**（传输 `play`、变速 `rate`、`role="slider"` 进度、真实 `<audio>` 元素、重扫 `rotateCcw`、列表 `musicNote`）与「**无 Tauri 桥 → 合成示例曲回退**」契约，退出码 0/1；实测 **6/6 PASS / 2s**。既有 `bun run smoke:ui` 亦实测 **4/4 PASS**（首页 HomeDock）。(c) **原生窗口实测**：`cargo build --release -p amos-tauri`（54s）后以 `AMOS_MEDIA_ROOT=/tmp/amos-real-media` 启动自包含发行态 UI；`osascript` 查到窗口标题/尺寸 = **"Amos · AI System UI", 480×820**（与 `tauri.conf.json` 一致），`ps eww` 确认进程环境携带该媒体根——即**窗口里列出的就是 ffmpeg 生成的真 mp3/mp4**。(d) **补 Rust 测试 `boot_honours_amos_media_root_and_serves_real_media`**：锁死 `lib.rs` 的启动路径（`AMOS_MEDIA_ROOT` → `hostfs` 后端 → 预授权 → 真文件 `kind=Audio`/`mime=audio/mpeg`/字节可读），防止未来改动断开「设环境变量即可播本地库」。**验证**：`cargo test -p amos-tauri --lib media::` **14 例**、`clippy --all-targets -D warnings` 干净、改动文件 `rustfmt --check` 干净；前端 `bun run check` **EXIT=0**；`smoke:ui` 与 `smoke:player` 均 **EXIT=0**（`docs/media-player.md` §7.4）。


- **手机管家自审计（第二轮）：失败被显示成空、后端错误被吞、审计 hide、审计 outcome 两种拼写（2026-09-10）**。对「手机管家」整屏做对抗式审计（并新写一条**桥→守护进程→磁盘**的 headless e2e），发现并修掉四处真实缺陷。(a) **扫描失败被渲染成「没有可清理的垃圾」**：`devcareScan()` 返回 `null`（根目录不可读 / 扫描超上限）时组件把 `scan` 留空，而「组数为 0」正是空态分支 → 一次**失败**看起来像一次**成功的空结果**，连「可回收」还显示 `0 B`（伪造读数）。现新增 `scanErr`：失败渲染 `care.scanFailed`，可回收量显示「—」。(b) **`status.last_error` 从未被渲染**：`AMOS_DEVCARE_ROOT` 指向不存在的目录时桥在启动扫描就记下错误、`available` 仍为 `true`，UI 却完全没有入口看到它——一个坏后端看起来和一个好后端一样。现概览卡显式渲染后端错误（本地化文案 + 原始诊断挂在 `title`）。(c) **审计读回被绑在清理后端上**：trail 卡片原本在 `{#if available}` 内，于是「**读审计**」这件根本不需要清理后端的事，在**没有后端时恰恰看不到**。现卡片移出该分支。(d) **同一条审计记录有两种 outcome 拼写**：`Outcome` 用 serde 默认（`"Rejected"`）落盘，而 `RecentTrail`/`RecentAudit` 走 `to_ascii_lowercase()`（`"rejected"`）——磁盘与 RPC 对**同一条记录**给出不同 tag，外部消费者（SIEM 导出、日志解析器）会解析失败。现 `Outcome` 统一序列化为**小写**（与 `proto/privacy.proto` 的 `granted|denied|success|rejected|error` 逐字一致），并用 `#[serde(alias)]` **继续接受旧的 PascalCase**，所以归一化之前写下的 trail 依旧可读、不会被当作坏行静默丢弃（对审计日志而言静默丢历史比格式不一致更糟）。**新测试**：`crates/amos-tauri/tests/devcare_audit_e2e.rs` —— 用**桥自己的函数**（`devcare::record_audit` + `privacy_client::perm_recent_trail`）打一个**真 `amos-ai` PrivacyService**（真 UDS + 真 `AMOS_PRIVACY_PATH`），断言：3 条记录（聚合清理 / 失败项 / **被拒绝的卸载**）全部 `recorded`、按 actor 过滤读回且最新在前、部分失败的清理记为 `error` 而非 `success`、别的 principal 的切片为空、磁盘 JSONL 恰好 3 行且含 `"rejected"`、且**时间戳由守护进程盖**（不含 `"ts":0`）。这条测试正是 (d) 的发现者。**验证**：`cargo test -p amos-ai --lib` **217 例**（`audit::` **6**，+1 `outcome_has_one_canonical_spelling_but_reads_the_legacy_one`）；`cargo test -p amos-tauri --lib` **210 例** + `--test devcare_audit_e2e` **1 例**（无 warning）；前端 `device-care.svelte.test.ts` **22 例**（+4：扫描失败非空态、后端错误显式渲染、正常路径无错误、无后端时 trail 仍可见）；`bun run check` **EXIT=0**（`test:svelte` **430 例 / 58 文件**、`tsc` 0、`svelte-check` 0/0）；`clippy --all-targets -D warnings`（三个 crate）与 `fmt --check` 干净（`docs/devcare.md` §3.6、REQ-DC37…41）。
- **手机管家自审计：策略预览与执法不一致（可被 UI 绕过）+ 审计入库健壮性 + 静默失败（2026-09-10）**。对上一轮刚落地的「清理/卸载审计」做对抗式自审计，发现并修掉四处真实缺陷。(a) **卸载策略有两个真相源（可被绕过）**：`devcare_apps` 的策略预览吃**前端传来的** `packages`（含调用方自报的 `system` 标志），而 `devcare_uninstall` 的执法恒按 `system:false` 判定——同一个包可以「列表显示不可卸、执法却放行」，且 UI 能影响策略输入。现**库存由桥所有**：`StoreBridge::installed_inventory()`（新增，顺带带上 manifest 声明的包尺寸）是唯一来源，预览（新增 `policy_preview`）与执法都经**同一个 `policy_package` 构造点**；`devcare_apps` **不再接受任何参数**，前端也不再直接读 `appstore_installed`。测试 `the_preview_and_the_enforcement_agree_for_every_package` 逐包断言「展示的判定 == 执行的判定」。(b) **审计批量会因单条失败丢尾**：`perm_record_audit` 原来用 `?` 提前返回，一条被拒的记录会让**排在它之后的记录全部不发**，而调用方只看到「全部未记录」。现改为逐条发送、只记下**第一个错误**、返回 `{recorded, total, error}`，桥把它折成 `recorded/attempted/reason`。(c) **包尺寸总是 `0 B`（伪造读数）**：清单以前由前端构造且不带尺寸，UI 一律显示 `0 B`；现尺寸取自 manifest，缺失即 **`null`**，UI 显示「—」，并有 `an_unknown_package_size_serializes_as_null_not_zero` 锁住「未知不伪造成 0」。(d) **清单读失败被显示成「没有应用」**（典型静默失败）：`devcare_apps` 失败时 `invoke` 返回 `null`，组件原来把它与空列表合并；现区分错误态并显示「无法读取应用清单」。另补 `the_clean_reply_wire_shape_is_flat` —— 锁定 `CleanReply` 的 `serde(flatten)` **扁平** wire 形状（否则前端会静默读到 0）。**测试**：`cargo test -p amos-tauri --lib` **210 例**（`devcare::` **16**，+4）；前端 `devcare.test.ts` **20 例** + `device-care.svelte.test.ts` **14 例**（+2）；`bun run check` **EXIT=0**（`test:svelte` **422 例 / 58 文件**、`tsc` 0、`svelte-check` 0/0）；`clippy --all-targets -D warnings` 与 `fmt --check` 干净（`docs/devcare.md` §3.6、REQ-DC30…33）。
- **真实音乐/视频验收 + 修「集合默认 kind 冒充真实类型」缺陷（2026-09-10）**。用 **ffmpeg 生成的真题材**（LAME mp3、H.264+AAC mp4、AAC m4a，含 `DCIM/Camera` 与一个非媒体文件）建一棵 Android 布局媒体树，跑通**与播放器相同的领域路径**。(a) **新增真媒体验收探针 `crates/amos-media/examples/probe_media.rs`**：对每个音/视频文件断言 ①**容器魔数**与推断类型一致（`ftyp`/`ID3`+帧同步/`RIFF…WAVE`/`fLaC`/`OggS`/EBML/AAC 同步字——一个"只是扩展名像"的假文件会被抓出来）；②`read_range(off,len)` 与整项切片**逐字节相等**（起始/中间/尾部/EOF/越界/非对齐六种窗口）；③`Range: bytes=0-` 规划出合法 **206**；退出码非零即失败。**实测：6 files, 4 playable, 0 failures**（真机 root 树同一二进制，`adb shell su -c probe_media /storage/emulated/0`）。(b) **真机素材暴露并修掉一个真实缺陷**：`HostFsProvider::list` 原按**所属集合的默认 kind** 报告（`StandardDir::Music` 的默认竟是 `File`）且 `mime` 恒为 `None` → 真实 `.mp3` 被报成"普通文件"、`DCIM/Camera` 里的 `.mp4` 被报成"图片"，只有前端按扩展名兜底能救。现新增纯函数 `mapping::kind_and_mime_for_name`（+`extension_of`，路径感知、点文件不算扩展名）按扩展名推断 kind+MIME，**未知/无扩展名则保留集合默认且不伪造 MIME**；`HostFsProvider::list` 采用之——桥自身即给出真实类型（播放器、文件管理器、Photos 图标同时受益）。(c) 桥层新增真实文件树测试 `hostfs_bridge_classifies_and_streams_real_media`（`.mp3`→Audio/audio/mpeg、`.mp4`→Video/video/mp4、`ftyp` 窗口 == 整项 load）。(d) 前端 `player.test.ts` 新增 5 例，用**这些真实文件名与 bridge 元数据**锁定分类、标题与 blob MIME（非媒体文件永不入队）。**验证**：`cargo test -p amos-media` → lib **43**（+3 mapping、+2 HostFs）、media_core **24**、其它 **10**；`cargo test -p amos-tauri --lib` → **218**（`media::` **13**，+1）；两 crate `clippy --all-targets -D warnings` 干净、改动文件 `rustfmt --check` 干净；`probe_media` **0 failures**（退出码 0）；前端 `bun run check` **EXIT=0**（`test:svelte` **455**，`player.test.ts` **32**）、`coverage:gate` 通过（`docs/media.md` §7、`docs/media-player.md` §7.3）。**诚实边界**：WebView 里的**解码播放**无法无头验证，本轮验证的是播放器的**数据路径**（列表→分类→字节→范围）。

- **提醒管线健壮性缺陷（日历 + 提醒事项）：坏档通知表会让到点提醒永久静默失效（2026-09-10）**。审计日历的 OS 提醒路径时发现：`lib/calendarCore.ts` 的 `syncDueEventAlerts` 与既有的 `lib/reminderCore.ts` 都把存储里的既有通知直接展开（`[...fresh, ...existing]`）。若 `amos.notifications` 是**非数组**（损坏或被外部写坏），展开会抛 `TypeError`，而该异常发生在**写去重标记之前** → 每 15 s 的 tick 都在同一处抛错，**日历与提醒事项的到点通知从此永久不再投递**（错误还被 watcher 的 `try/catch` 吞掉，外表「一切正常」）。现改为写入前跑 `normalizeNotifs`（坏档修复为空表后继续投递）。回归用例 **2 条**（`calendarCore.test.ts`、`reminderNotify.test.ts` 各一），并以 `node -e "[...{},1]"` 实测确认旧写法确为 `TypeError: {} is not iterable`。

- **前端 UI 冒烟测试（真实 headless Chrome）长期失红修复（2026-09-10）**：`crates/amos-tauri/frontend-ts/scripts/smoke-ui.mjs` 的第 4 个标记写死为 `aria-label="search"`，而 `HomeDock` 的搜索胶囊早已改为本地化 `aria-label={t("shell.search")}`（默认 zh 下渲染「搜索」）→ 标记永不命中，脚本每次空等 45 s 后判失败（该目标不在 CI 的 `make smoke` 之内，故长期未被发现）。改用组件**已有的、与语言无关**的钩子 `data-testid="home-search-entry"`。验证：`bun run smoke:ui`（生产构建 + headless Chrome 加载主屏）**4/4 PASS / 2 s / EXIT=0**；并在**干净 worktree**（HEAD 4894443）上复现出同样的 3/4 失败，确认该缺陷非本轮改动引入。**环境注记（诚实记录）**：`smoke:ui` 若与其它 node/vite/esbuild 进程**并行**运行，`vite build` 会因共享 esbuild service 状态偶发 `[vite:esbuild-transpile] Transform failed`（报错 token 每次都不同、且发生在**未被本轮改动**的 `AiApp` 分块上）。串行执行 `bun run check → bun run build → bun run smoke:ui` 连续多次均通过；清理残留进程 + `rm -rf dist node_modules/.vite` 后亦通过。判定为工具链/环境的并发现象，非代码缺陷。

- **拦截一致性补全（第二轮）：未知号码开关的徽标缝隙 + 不可达的执法横幅 + 屏蔽后仍读线程（2026-09-10）**。对上一轮的三处修复继续审计，发现并修掉三处仍会复现 / 仍不可达的缺陷。(a) **徽标比列表多的缝隙没被真正堵死**：`Blocklist::check` 把**无法解析的地址**（字母数字发送者 ID，如 `TM-ALIPAY`）判为 `Unknown` 而非「放行」，因此**只开「拦截未知号码」开关、一条规则都没有**时 `filter_threads` 仍会隐藏该线程；而 `sms_counts` 的守卫只看 `has_sms_rules()` → 退回平台计数，徽标再次多计。现把守卫提炼为 `BlocklistState::filters_sms()`（有短信规则 **或** 未知开关开启），`sms_counts` 改用它；新增 Rust 用例 2 例（字母数字发送者 ID 在开关开启时不计入、逐文件夹等于可见列表长度、关掉开关后平台计数重新生效；守卫由规则或开关任一武装、仅来电规则不武装短信过滤）。(b) **「有来电规则但平台无法执法」时横幅完全不渲染**：`block-role-unavailable` 分支由 `needsCallRole` 守门，而它要求 `role_supported` → 桌面端 / 无角色 Android 上「规则已记录但没有任何东西执行它」看起来像「一切正常」。现横幅改为三段式且互斥穷尽（已生效 / 需授权+授权按钮 / 其余一律渲染不可用说明且不给按钮），移除不可达的 `needsCallRole`，`phone.blockRoleUnsupported` 措辞覆盖三种成因（桌面端 / Android 10 以下 / 原生桥未就绪）；新增 DOM 用例 2 例（不可用态可见且无授权按钮；已持角色只显示生效）。(c) **屏蔽后仍读取被隐藏的线程**：`MessagesApp.refreshReal` 只判断「列表里还有没有别的线程」，活动线程消失且列表为空时仍按旧 id 调 `sms_messages`（真机上该读取会被屏蔽判定拒绝）。现改为在**新列表里**查找活动线程，消失则切到最新线程或清空 `realActiveId`/`realMsgs`/`realErr`，不再读取不可见线程（`messages.svelte.test.ts` 增断言：屏蔽后 `sms_messages` 调用次数不增加）。另删除从未被引用的文案键 `phone.blockAction`（中英）。验证：`cargo test -p amos-tauri --lib` **194 例**（`sms::` **13**、`blocklist::` **10**）；`clippy -D warnings`（host + `--features android`）、`fmt --check` 干净；`bun run check` **EXIT=0**、`test:svelte` **378 例**（+2）、`tsc`/`svelte-check` 0 error、i18n 奇偶保持（`docs/sms.md` §11.4）。
- **拦截一致性补全：文件夹徽标不再计入被屏蔽发件人 + 来电筛选角色状态实时刷新（2026-09-10）**。接续骚扰拦截与「信息」页一键拉黑做审计，修复两处用户可见的不一致。(a) **短信文件夹计数与列表不一致**：`sms_snapshot` 会过滤掉被屏蔽发件人的线程，但 `sms_counts` 直接返回平台计数 → 拉黑一个发件人后列表少一条、标签徽标却仍多一，用户会以为数据丢失。现 `amos-tauri/src/sms.rs` 新增纯函数 `filtered_counts(provider, rules)`：**存在短信规则**时按同一 `filter_threads` 从各文件夹快照派生计数（徽标与可见列表**按构造相等**），无短信规则时仍走平台 `counts()`（零额外读取）。新增 Rust 用例 2 例（屏蔽后收件箱/发件箱各 1、草稿 1 且逐文件夹等于过滤后列表长度；仅来电规则不缩小短信计数）。(b) **来电筛选角色状态在授权后不更新**：`blocklist_request_role` 只负责弹出系统对话框，用户在其**独立 Activity** 里作答的耗时远晚于命令返回，于是横幅一直停在「未获权限」。现 `PhoneApp` 在**回到前台（`visibilitychange`/`focus`）**以及**每次打开「拦截」标签**时重新探测 `blocklist_status`，横幅立即翻转（`cmd role remove-role-holder` 外部撤销也会如实回到「需授权」）。新增 DOM 回归用例 1 例（授权→前台事件→`block-role-held`；撤销→再前台→`block-role-needed`）。(c) **「信息」页屏蔽成功文案过度承诺**：原「已屏蔽 X：其短信不再显示，来电将被拒接」在未持角色时会谎报，改为「其短信不再显示；AmOS 持有「来电筛选」权限时来电将被拒接」（中英同步）。验证：`amos-tauri --lib sms::` **12 例**（+2）、`test:svelte` **376 例**（+1）、`clippy -D warnings`（host + `--features android`）、`fmt --check`、`bun run check`（含 i18n 奇偶、`tsc`/`svelte-check` 0 error）全绿（`docs/sms.md` §11.4）。
- **真机（YY000286/S5/API 34）UI 审计：拦截页渲染错页 + 授权按钮谎报成功（2026-09-10）**。用 WebView DevTools 协议（`adb forward … webview_devtools_remote` → CDP `Runtime.evaluate`）直连真机 WebView 逐屏驱动，发现两个用户可见的真实缺陷。(a) **「拦截」标签把整页「紧急号码」渲染在黑名单面板之上**（即用户反馈的「拦截页面不正常」真因）：`PhoneApp` 的标签链以裸 `{:else}` 结尾（=紧急页），而拦截面板是另起一个独立 `{#if tab === "block"}`，`tab==="block"` 时两个分支**同时成立**；现末端改为 `{:else if tab === "emergency"}` 并把拦截面板并入同一链 `{:else if tab === "block"}`，结构上不可能共渲染（新增 DOM 回归测试双向断言）。(b) **「授予来电筛选权限」返回 `true` 但对话框不出现、角色始终未授予**：角色对话框原先用 **application context** 的 `startActivity`（被 Android 10+ 后台启动规则吞掉），改为从 **前台 Activity** 启动后仍失败——普通 `startActivity` 不建立 caller 身份，PermissionController 立即记 `RequestRoleActivity: Package name cannot be null or empty: null` 并在 ~100 ms 内 self-finish；现 `MainActivity.onStart` 调新增的 `BlocklistGlue.attachActivity(this)` 交出 Activity，`requestScreeningRole` 改用 **`startActivityForResult`**，并补 `Intent.EXTRA_PACKAGE_NAME`。真机复验：点按钮 → 系统「要将Amos设为您的默认来电显示和骚扰电话屏蔽应用吗？」→ 授权后 `role_held=true`、横幅变「来电拦截已生效」，`cmd role remove-role-holder` 反向验证回 `false`。(c) **UI 打磨（真机量测）**：拦截页号码输入框 65×32 → 336×36（原单行在 360px 屏上放不下占位符）、通话记录行内动作 32→40px、筛选 chip/清空/标签栏/信息页工具栏加高至 28–32px、无横向溢出；通话记录无联系人名的行不再把号码打印两遍。(d) 顺带修掉 Android 交叉编译的 `dead_code` 警告（`host_battery::clamp_level` 仅在 macOS/Linux 读取路径使用，加精确 `cfg` 门控）。真机同轮确认：信息页一键拉黑后线程从收件箱消失（7→6）、非法规则诚实报错、通话记录方向/时间/筛选/两段式清空全部正确。测试：`test:svelte` **375 例**（+1 回归）、`tsc`/`svelte-check` 0 error（`docs/sms.md` §11.3）。
- **来电拦截页审计修复：四个真实缺陷（2026-09-10）**。真机反馈「拦截（电话）页面不正常」，逐层审计后修复：(a) **规则库被拆成两个目录（严重）**——`lib.rs::setup` 用 `app_data_dir()`（Android = `Context.getDataDir()`，见 `tauri-2.11.5/.../PathPlugin.kt`），Kotlin `BlocklistGlue.bind` 却配置 `Context.filesDir`；`configure` 会替换路径并重载，两处互相覆盖，且 `AmosGlue.onStart` 只在授予 CAMERA 时执行 → 未授相机时 **UI 加的规则永远到不了来电拦截**。现 Rust 在 Android 上把 `files` 追加到 `app_data_dir()`，与 glue 收敛到同一文件（`configure` 因此幂等、不再清空内存表）。(b) **来电筛选角色不可见、且只有后台申请路径**——新增命令 `blocklist_status`（`has_call_rules/has_sms_rules/role_supported/role_held/role_requestable`，经 JNI 调 Kotlin `screeningRoleSupported/screeningRoleHeldBound`，宿主诚实返回不支持）与 `blocklist_request_role`（前台显式申请 `requestScreeningRoleBound`，绕开 Android 10+ 后台启动限制）；UI 增加状态横幅与「授予来电筛选权限」按钮。(c) **诚实错误被丢弃**——`PhoneApp` 的拦截逻辑原只有 `.then()` 无 `.catch()`，而共享 `invoke()` 把命令失败吞成 `null`（`bridgeDiag()` 留证）→ 非法号码**不报错**；现按仓库约定判 `null`/`bridgeDiag()`：非法规则显示本地化原因、移除/保存失败回滚并提示、加 `blockBusy` 防重复提交。(d) **缺少一键入口**——通话记录「最近」行新增「拦截」按钮（写入 `exact/both` 规则并切到拦截页）。另：`MainActivity.onStart` 无条件 `BlocklistGlue.bind(applicationContext)`（不再受相机门控），`MainActivity.Wiring.kt` 记录该接线以便重生成后重挂。测试：`amos-tauri --lib blocklist::` **9 例**（+3）、`test:svelte` **366 例**（+3；非法规则报错/角色横幅+授权/一键拉黑）、`clippy -D warnings`（host + `--features android`）、`fmt --check`、`tsc`/`svelte-check` 0 error 全绿。

### Added
- **本地多媒体播放器：真·播放本地音乐与视频，音频/视频统一队列（2026-09-10）**。既有 `MusicApp` 是纯演示（曲目写死、1 s `setInterval` 推进、**不发声**、无视频）。本轮新增 `player` 应用，播放**真实本地媒体**并保持同一套工程纪律。(a) **真播放**：`media_list(music|movies|recordings|download)` → `media_load` 字节 → Blob → `<audio>`/`<video>`；object URL 在切曲时 `revokeObjectURL`（含“解析期间又切曲”的竞态回收）。(b) **纯模型 `lib/player.ts`（零 DOM，一次单测）**：`kindFromName` 以**具体 MIME 优先、扩展名兜底**分类（`MediaProvider.kind` 是集合默认值——HostFs 会把 `Download` 里的歌标成 `file`——**不可**用来判可播性），非音视频**不入队列**；`trackFromItem/Memo/Capture` 把桥文件、语音备忘录、相机录像归一成 `PlayerTrack`；`mergeTracks` 去重保序；`buildOrder` 洗牌（Fisher–Yates，`rng` 可注入 → 确定性）；`stepOrderPos`/`skipMode` 走带（`one` 原地、`all` 环绕、`off` 端点停；**手动跳曲把 `one` 视作 `all`**，修掉旧播放器“单曲循环下按下一个不动”的体验缺陷）；`fmtSeconds` 标签。(c) **有界与诚实**：`MAX_PLAY_BYTES=256 MiB` 命中即拒绝缓冲并渲染 `player.tooLarge`（Power-of-10 有界资源）；读字节/取 Blob 失败 → `player.unavailable`、元素 `error` → `player.playbackError`，均**显式渲染**；桥授权拒绝（`media_list` reject）**即使库为空也不回退示例曲**，而渲染“内容不可用”（沿用 `lib/media.ts`「绝不把拒绝塌缩成空」契约）；`play()`/`pause()`/`requestFullscreen()` 全部 try/catch（无头 DOM 不炸渲染）；无桥且确为空时才回退**可发声的合成 WAV 示例**。(d) **UI（Svelte 5 runes）**：视频舞台 + 音频封面、进度拖动/键盘（±5 s）、播放/暂停、上/下一首、随机、循环三态、静音 + 音量、视频全屏、播放列表与来源徽标；新图标 `shuffle/volume/volumeX/maximize/film`；i18n 中英新增 `app.player` + `player.*`；注册进 `APP_META`(27)、Svelte registry(27)、媒体分组。**验证**：`player.test.ts` **19 例** + `player.svelte.test.ts` **5 例**（含“桥拒绝不被示例曲掩盖”）；`bun run typecheck`/`typecheck:svelte` **0 error 0 warning**；`bun run test`（bun ISO）**EXIT=0**；`test:svelte` **447 例 / 59 文件**（+5）；`coverage:gate` **93.85% ≥ 90%**（`docs/media-player.md`、`docs/media.md` §6）。
- **媒体领域内核：范围读取（RFC 7233）+ 有界整项加载 + `media_read_range` 桥命令（2026-09-10）**。为流式播放/大文件铺好**可测内核**，并顺手补上一处**无界分配**缺陷。(a) **新增纯模块 `amos-media/src/range.rs`**：`RangeSpec`/`ResponsePlan` + `parse_range`（开区间 `start-`、闭区间 `start-end`、后缀 `-N`、越界→416、畸形/非 bytes/多范围→忽略并回退全量、空资源→416、`-0`→416、端越界自动夹取）+ `clamp_window`/`window_len`（全域、无下溢）+ `content_range`/`content_range_unsatisfied`/`status_for`/`window_for` + `plan_response`（200/206/413/416 的"先决策后读"）。**18 例**穷举测试。(b) **`MediaProvider::read_range` seam**（默认=有界 `load` 后切片，`get` 切片不 panic）+ 共享窗口数学 `window()`；**Mock** 走 blob 切片、**HostFs 走真 `seek`+`read`**（EOF 短读不是错误、非文件/缺文件诚实报错、并发安全）。(c) **`MAX_LOAD_BYTES`（256 MiB）+ `ensure_loadable`**：整项 `load` 现在**先 stat 再读**，超大文件返回类型化 `TooLarge` 而不是无界分配（此前 HostFs `load` 无任何上限）。(d) **`MediaManager::read_range` 与 `load` 同一读授权门控**（撤销读授权后流式读取同样 `Unauthorized`）。(e) **桥命令 `media_read_range(item, offset, len)`**（`crates/amos-tauri/src/media.rs`，`cmd_*` 核心 + `generate_handler` 注册），与 `media_load` 对称。**验证**：`cargo test -p amos-media` → lib **38**（+18 range、+4 provider）、media_core **24**（+2：授权门控、规划↔provider 端到端一致）、其它 **10**；`cargo test -p amos-tauri --lib media::` → **12**（+1）；`cargo clippy -p amos-media/-p amos-tauri --all-targets -- -D warnings` 均干净；`rustfmt --check` 对改动文件干净（`docs/media.md` §2/§6、`docs/media-player.md` §8）。**诚实边界**：Tauri 自定义协议注册（`register_asynchronous_uri_scheme_protocol`）与平台 URL 形式仍需真机前后端一起验收，**未在本环境伪造**。

- **播放器能力补全：会话恢复 + 播放速度 + 错误重试 + 系统媒体会话（2026-09-10）**。在本地播放器之上补四项「真播放器」才有的能力，全部可离线单测。(a) **会话恢复**（新增纯模块 `lib/playerPrefs.ts`）：持久化 `amos.player`（轨道 id / 播放头 / 音量 / 静音 / 循环 / 随机），重开播放器**回到上次的曲目与位置**；恢复按 **track id 精确匹配**（`resumePosition` 把「已听完」（≥ 总长−3 s）归 0，切歌不会误用旧时间戳），播放头**粗粒度节流**（`shouldSavePosition`，约 5 s/次，避免每 tick 写库）。所有解析为总函数：坏档/缺字段/越界一律回默认，绝不抛。(b) **播放速度**：`PLAYBACK_RATES`（0.5×–2×）预设，`cyclePlaybackRate` 双向环绕且未知值从 1× 重启（绝不 NaN），按钮显示 `formatRate` 标签并写入 `el.playbackRate`。(c) **错误重试**：解析失败的曲目不再是「点了没反应」——错误条给「重试」，播放键在 `mediaErr && url==""` 时也会重试（`retryNonce` 显式加入解析依赖）。(d) **系统媒体会话**（新增 `lib/mediaSession.ts`，注入式）：有 `navigator.mediaSession` 时发布元数据 / 播放态 / 锁屏把手（play / pause / 上一首 / 下一首 / seekto），无 API 或宿主 `setActionHandler` 抛错都**静默 no-op**（不支持 ≠ 播放失败），卸载时清理。i18n 中英新增 `player.rate` / `player.retry`。**验证**：`player.test.ts` **27 例**（+4 速度、+2 恢复位置）、`mediaSession.test.ts` **7 例**、`playerPrefs.test.ts` **10 例**、`player.svelte.test.ts` **11 例**（+3：速度循环、恢复播放头、重试重新解析）；`bun run check` **EXIT=0**（`tsc` 0、`svelte-check` 0/0、`test:svelte` **453 例 / 59 文件**）；`coverage:gate` **93.81% ≥ 90%**（`docs/media-player.md` §7.2）。

- **播放器自审计：修 8 处真实缺陷 + 补刷新/类型徽标（2026-09-10）**。对刚落地的 `player` 做对抗式审计与补全。(a) **刷新会打断播放**（最严重）：rescan 会替换曲目对象数组，解析 `$effect` 因**对象身份**变化重跑 → **重建 object URL 并把播放头清零**；现解析器改以 `$derived trackId`（字符串，`===` 才传播）为依赖并用 `untrack` 读当前对象，同 id 不重载；新增 DOM 测试 `rescan keeps the active track (does not recreate its object URL)` 对比刷新前后的 `blob:` URL 锁死该不变量（修复前该测试失败）。(b) **Blob MIME 取错名字**：字节分支用**已去扩展名的标题**推断 MIME，后端无具体 MIME 时会得到 `application/octet-stream`；改用完整 `item.name`。(c) **循环关闭时曲末卡死**：列表尾 `ended` 只置 `playing=false` 而不回到 0，再按播放会立刻再次 `ended`；现停止即 `currentTime=0`。(d) **相机录像不可见**：设备相机视频在 `DCIM/Camera`，集合列表漏了 `camera`（照片被扩展名/MIME 分类过滤，不入队）；现纳入。(e) **并发刷新竞态**：两次快速刷新可能让较慢的旧结果覆盖新结果；加 `scanSeq` 世代令牌，被取代的扫描返回即丢弃。(f) **“读不到”≠“空”**：`media_list` 返回 `null`（桥中途消失）原先既不算空也不算失败；现与 reject 同等记为不可用（不触发示例曲回退）。(g) **无头 DOM 抛错**：`currentTime` 写入未防护；加 `setTime()` 与 `playEl/pauseEl` 一致 try/catch。(h) **a11y/死键**：音量滑块误用 `player.seek` 作标签；`player.count/audio/video` 三键闲置。现新增 `player.volume`/`player.refresh`（中英）并实际渲染**类型徽标 + 项目数 + 重新扫描按钮**。**验证**：`player.test.ts` **21 例**（+2 `indexOfTrackId`）+ `player.svelte.test.ts` **8 例**（+3：刷新拾取新增媒体、刷新不重建 URL、视频类型徽标）；`bun run check` **EXIT=0**（`tsc` 0、`svelte-check` 0/0、`test:svelte` **450 例 / 59 文件**）；`coverage:gate` 通过（`docs/media-player.md` §7.1）。


- **手机管家：「内存加速」（参考安卓手机管家模式）——策略在领域、权威在 governor、回报不造因果（2026-09-10）**。补上一项手机管家的标配能力（小米/华为/三星的「一键加速」），沿用同一套纪律。(a) **可回收性 by construction**（新增纯领域模块 `amos-devocare/src/boost.rs`）：只命名两个可回收档位——`cached`（墓碑，**先回收**：已冻结且已存状态）与 `background`；`foreground`/`visible`/`foreground_service` **永不入选**，`stopped`/未知/大小写不符一律不碰（「未知＝别动」，不是「大概没事」）；`reclaim_plan` **稳定排序**（cached 先、同档保持输入序），同一快照必得同一计划。(b) **数据与动作各有其主**：内存读数取自既有 `system_health`（`amos-monitor` 采样器），待回收清单取自资源 governor 的 `taskmgr_snapshot`，动作是把 `kill` **逐个**下发 `taskmgr_app_action`——governor 是生命周期的唯一权威，管家只**请求**；两者都被桥包装成 `devcare_memory` / `devcare_boost`（映射为纯函数 `memory_view`，可离线测）。(c) **诚实回报**：`BoostOut{requested, reclaimed, failures, available_before, available_after, governor}`——每条失败都列出（部分成功绝不冒充完整），并且**从不声称「释放了 N MB」**：只给出加速前后的可用内存读数由调用方比较（内存变化必有其他来源，把 delta 归因给本次动作是不成立的因果）。(d) **UI 三态**：不可读 → 显示「—」（不是 `0`）；**没能问到 governor** → 「守护进程未连接，无法管理后台应用」（不是「没有可回收应用」）；正常 → 可回收计数 + 加速按钮（无可回收项时置灰）。内存卡与审计卡一样**不依赖清理后端**。i18n 中英新增 `care.memory*`/`care.boost*`/`care.reclaimableApps`/`care.memoryDelta`。**测试**：`cargo test -p amos-devocare` **79 例**（+5 `boost::`：受保护档位不可回收、仅两档可回收（含 `stopped`/空/大小写）、cached 先于 background、同档稳定且确定性、空集）；`cargo test -p amos-tauri --lib` **213 例**（`devcare::` **19**，+2：内存视图受保护档位不入选且 cached 在前、governor 不可达不伪造读数）；前端 `devcare.test.ts` **30 例**（+5：`normalizeMemory` 全量且丢弃无法命名的行、`memUsedBytes`/`memUsedPct` 不完整读数返回 `null` 且不为零除、`normalizeBoost` 部分失败如实）+ `device-care.svelte.test.ts` **30 例**（+6：读数/可回收计数、加速回报、部分失败、**不可达 ≠ 没有可回收**、不可读显示「—」、空列表禁用按钮）；`bun run check` **EXIT=0**（`test:svelte` **438 例 / 58 文件**、`tsc` 0、`svelte-check` 0/0）；`clippy --all-targets -D warnings`（三个 crate）与 `fmt --check` 干净（`docs/devcare.md` §3.7、REQ-DC45…48）。
- **手机管家：权限审阅改读 daemon 权威（`GrantedAll`）+ 权威不可达与「无授权」严格区分（2026-09-10）**。上一轮把「应用清单」从调用方输入改成桥所有，但**权限审阅仍以**前端本地账本（`amos.permissions`）**为数据源**，而权威在 daemon 的 `PrivacyManager`——本地账本是**缓存**，可能与权威不一致，属同一类「把缓存当权威」的缺陷。(a) **wire**：`proto/privacy.proto` 新增 `AllGrantsRequest`/`AllGrantsReply` 与 `rpc GrantedAll(...)`，一次取回全部持有者（避免 N 次按包查询）。(b) **守护进程**：`PrivacyManager::grants_snapshot()` 返回**按 app id 排序、资源按稳定键排序**的快照，且**空集不列出**（deny-by-default 的语义；撤销到空的 App 直接消失）；`PrivacySvc::granted_all` 映射为 wire。(c) **桥**：`privacy_client::perm_grants_all`；`devcare_permissions` **不再接受任何参数**——行由 daemon 提供，**形状仍由领域内核定**（新增 `permission_review_from`：去重、按 `SensitiveResource::ALL` 排序、有界；领域未建模的键跳过）。返回 `PermReviewOut{rows, authority}`：`authority:"daemon"` 才是权威答案，`"unavailable"`（daemon 不可达）**不是**「没有任何授权」——空列表绝不能被那样渲染。(d) **前端**：`devcarePermissions()` 无参 → `PermReviewView`，`permReviewAuthoritative()` 判权威；管家页权限卡三态渲染（不可达 / 权威且为空 / 有行），并移除对本地账本的直接读取。i18n 中英新增 `care.permNoAuthority`。**测试**：`cargo test -p amos-ai --lib` **218 例**（`privacy_service::` **10**，+1：deny-by-default 为空、只列持有者、撤销后消失、稳定序）；`cargo test -p amos-tauri --lib` **211 例**（`devcare::` **17**，+1：daemon 行去重/排序 + 未建模键跳过 + 空集）；`--test devcare_audit_e2e` 扩展为**真 daemon** 上「授权→`perm_grants_all`→`permission_review_from`」读回；前端 `devcare.test.ts` **25 例**（+2：`normalizePermReview` 全量且默认 `unavailable`、`permReviewAuthoritative` 只在 daemon 回答时为真）+ `device-care.svelte.test.ts` **20 例**（+2：不可达**不显示成**「没有授权」、权威且为空才显示空态）；`bun run check` **EXIT=0**（`test:svelte` **432 例 / 58 文件**、`tsc` 0、`svelte-check` 0/0）；`clippy --all-targets -D warnings`（三个 crate）与 `fmt --check` 干净（`docs/devcare.md` §3.4、REQ-DC42…44）。
- **手机管家：审计 trail 读回闭环 + 「近期操作」视图（2026-09-10）**。上一轮把清理/卸载写进了统一审计 sink，但**没人读得回来**——`perm_recent_audit` 只读隐私裁决的 `AccessRecord` 窗口，不含本 sink 的通用记录（这正是当时写明的诚实边界）。本轮补上读的一侧。(a) **wire**：`proto/privacy.proto` 新增 `TrailReply{records, durable}` 与 `rpc RecentTrail(AuditQuery)`，过滤语义复用 `AuditQuery`（`app_id` → principal，`resource` → resource）；`durable` 的存在是为了让「守护进程没有持久 sink」与「有 sink 但确实没记录」**不可混淆**。(b) **守护进程**：`PrivacyManager::recent_trail` 读**同一个** `AuditFile` 窗口（隐私裁决与管家动作共用一条 sink，走既有 `recent_matching`）；`PrivacySvc::recent_trail` 映射 wire 并带出 `durable`；无 sink 时诚实回 `durable=false` + 空列表。(c) **桥**：`privacy_client::perm_recent_trail`（顺带抽出 `map_audit`，`perm_recent_audit` 与它共用同一条映射，消除重复）；新增 `devcare_trail`——用 `audit::ACTOR` **在 Rust 侧过滤**，所以 actor id 只在这一处定义、**不在 UI 里再写一份**（避免漂移）。(d) **前端**：`lib/devcare.ts` 增 `normalizeTrail`/`opLabelKey`/`outcomeLabelKey`/`hhmm`（未知 op/outcome 回退到 `*.unknown`，绝不把原始 tag 泄进 UI）；管家页新增「近期操作（审计）」卡片，渲染 时间 / 操作 / 资源 / 结果，并**如实区分四种状态**：有条目 / 空 / **无持久 sink**（不显示成「无记录」）/ **读失败**（显示错误，不显示成空）。每次清理/卸载后自动刷新，另可手动刷新。i18n 中英新增 `care.trail*`、`care.op.*`、`care.outcome.*`。**测试**：`cargo test -p amos-ai --lib` **216 例**（`privacy_service::` **9**，+2：真临时 JSONL 里「按 actor / resource 过滤、拒绝可见」与「无 sink ⇒ `durable=false`」）；`--test privacy_audit_e2e` 扩展为**写入 + 读回**（真 UDS + 真 daemon + 真 JSONL：录制两条后 `RecentTrail` 按 actor 读回、最新在前、`rejected` 可见）；`cargo test -p amos-tauri --lib` **210 例**；前端 `devcare.test.ts` **23 例**（+3：`normalizeTrail` 全量、「无 sink 与空列表可区分」、标签键回退、`hhmm` 边界）+ `device-care.svelte.test.ts` **18 例**（+4：渲染本地化 op+outcome、空态、**无 sink 不显示成空**、读失败显示错误）；`bun run check` **EXIT=0**（`test:svelte` **426 例 / 58 文件**、`tsc` 0、`svelte-check` 0/0）；`clippy --all-targets -D warnings`（三个 crate）与 `fmt --check` 干净（`docs/devcare.md` §3.6、REQ-DC34…36）。
- **手机管家：清理/卸载接入统一审计落盘 + 卸载策略不可绕过（2026-09-10）**。「谁在何时删了什么」现在进**同一条**审计 trail，并修掉一个真实的策略绕过缺口。(a) **纯领域审计事件 `amos-devocare/src/audit.rs`**：事件内容由内核决定、离线可测——聚合一条 `devcare.clean`（`success` **仅当零失败**，有失败即 `error`，`details` 带 `planned/freed_bytes/removed/failed`；`planned` 用 `CleanOutcome::attempted()` 的「不跳过任何计划项」不变量）+ 每条失败一条 `devcare.clean.item`（**上限 50 条**，防止 5 000 项失败写 5 000 行）；卸载为 `app.uninstall`，**被拒绝也记 `rejected`**（拒绝必须可见，否则「谁试图删关键包」无从追溯）；`resource`/`details` 按**字符**截断（200/300——按字节会切断 UTF-8），保证 JSONL 每行有界。(b) **守护进程统一 sink（wire + 实现）**：`proto/privacy.proto` 新增 `PrivacyService.RecordAudit`（复用既有 `AuditRecord` 消息）；`PrivacySvc::record_audit` **校验** outcome（未知值 `INVALID_ARGUMENT`，绝不猜成相近值）/`principal`/`op`，并**用守护进程自己的时钟盖章 `ts`**（审计时间不能由调用方回填或预填，客户端传的 ts 被忽略）；`PrivacyManager::record_audit` 把记录 append 到 `amos-ai::audit::AuditFile`——**与隐私裁决镜像到的是同一个 JSON-lines 文件**。**诚实降级**：守护进程未以 `AMOS_PRIVACY_PATH` 启动时没有持久 sink → 回 `ok=false` 并说明原因，调用方据此报告真实状态。(c) **桥与前端**：`privacy_client::perm_record_audit`（批量，返回守护进程确认落盘的条数）；`devcare_clean` 现返回 `CleanReply`（`CleanOut` + `AuditStatus{recorded, attempted, reason}`，`serde(flatten)` 保持扁平 wire）；新增 `devcare_uninstall`——**在 Rust 侧先跑 `UninstallGuard` 再卸载，且策略先于库存查询**（关键包即使不在本地清单也拒绝），拒绝同样入审计；「手机管家」页改走该命令（**不再直接调 `appstore_uninstall`，策略无法被 UI 绕过**），并新增审计状态行：`已记录审计 (n/m)` 或 `审计未记录（守护进程未连接）`——绝不假装有 trail。i18n 中英新增 `care.audited/notAudited/uninstalled/uninstallFailed/notInstalled`。**测试**：`cargo test -p amos-devocare` **74 例**（+8 `audit::`）；`cargo test -p amos-ai --lib` **214 例**（+2 `privacy_service::`：真临时 JSONL 落盘重开可读、无 sink 时 `ok=false`、未知 outcome/空 principal 拒绝）且 `--test privacy_rpc_e2e` 仍绿、**新增 `--test privacy_audit_e2e`**（真 UDS + 真 daemon + 真 JSONL：清理与**拒绝卸载**都落盘、重开文件可读、时间戳由守护进程盖、未知 outcome 被拒）；`cargo test -p amos-tauri --lib` **206 例**（`devcare::` **12**，+5：审计不变量/空事件 no-op/策略先于库存/拒绝入审计/未安装入审计）；前端 `devcare.test.ts` **16 例**（+4）+ `device-care.svelte.test.ts` **12 例**（+2：审计状态展示、卸载经策略命令且未调用 `appstore_uninstall`）；`bun run check` **EXIT=0**（`test:svelte` **420 例 / 58 文件**、`tsc` 0、`svelte-check` 0/0）；`clippy --all-targets -D warnings`（三个 crate）与 `fmt --check` 干净（`docs/devcare.md` §3.6、REQ-DC25…29）。
- **手机管家（Device Care）：Tauri 桥 + 真实文件系统后端 + Svelte 管家页 + 审计补全（2026-09-10）**。把 `amos-devocare` 从「领域内核」推进到**用户可打开、可真实清理**的完整链路，并对内核做了一轮审计修复。(a) **`hostfs` 真实文件系统后端**（新增模块，本 crate **唯一**显式 I/O，其余仍纯 `std`）：`HostFsScanner` 按已建模形状匹配（cache/code_cache/.cache/tmp/temp 子树、`*.apk|apks|xapk|apkm`、`*.log`、`*.tmp|temp|swp|swo`、`*.dmp|hprof`、`.thumbnails`/`thumbnails` 子树、空目录）——**用户媒体/文档永不返回**（测试硬断言 `.jpg/.mp4/.pdf` 不出现在扫描结果）；**有界**（`MAX_SCAN_DEPTH=12`、扫描上限、不可读目录计数进 `HostScan::unreadable_dirs` 使部分扫描不冒充完整）；**不跟随符号链接**（`symlink_metadata`）。`HostFsCleanProvider` 只删**规范化后位于根内**的路径（`is_within` 对**父目录**做前缀校验，`..`/符号链接先解析再判断），目录只走 `remove_dir`（非空目录诚实失败、绝不递归删除），删符号链接只删链接本身；`is_safe_root` 拒绝把 `/` 当根。(b) **审计修复（内核）**：`plan` 现按 `uri` **去重**（同一路径不会产生两次删除；尺寸冲突取**最小值**，永不夸大可回收空间）并拒绝空/空白 `uri`（`DevCareError::Refused` 由此从死变活）；新增 `CleanOutcome::attempted()` 使「不跳过任何计划项」可断言；`JunkKind::from_key`（未知 tag 拒绝，不猜）。(c) **Tauri 桥 `crates/amos-tauri/src/devcare.rs`**（managed + 6 命令 `devcare_status/scan/clean/apps/permissions/report`）：**前端不能指名路径**——`devcare_clean` 只接受**类别 tag** + 审阅确认，并在桥内用**自己的扫描快照**重新 `plan`，未知 tag 直接拒绝（真机/恶意 UI 都无法让清理器删任意路径）；清理后**重扫**快照；未设 `AMOS_DEVCARE_ROOT` 时诚实 `backend:"none"`/`available:false`，且 `devcare_report` 的存储区**保持未评估**（不把「没有后端」报成「0 字节＝健康」——这条正是本轮由测试抓出的真实缺陷）。`devcare_apps`/`devcare_permissions` 让策略在 Rust 侧统一执行（关键包拒绝、未知资源 tag 拒绝）。(d) **Svelte 管家页 `svelte/DeviceCareApp.svelte`（第 26 个内置应用「手机管家」🧹）**：体检卡（**先判 `has_data`**，未观测显示「—」而非 100）、垃圾分类勾选（**只预选 `auto_kinds`**）、一键清理（审阅型需先勾确认，客户端 `validateCleanRequest` 与 Rust 同构）、部分清理/失败**如实显示**、应用区**只对 `allowed` 显示卸载**（被拒显示本地化 `reason_key`）、权限**只读**审阅；注册进 `appMeta`/`appRegistry`/`appGroups`，`en`/`zh` i18n 全量对齐（新增 `care.*` 与 `perm.cap.sms/phone/unknown`），`appMeta`/`app-registry` 计数测试同步 25→26。(e) **前端桥模块 `lib/devcare.ts`**：typed 包装 + **全量 normalizer**（畸形载荷降级为未知/空，**从不抛错**；无法命名的分组被丢弃而非渲染成假「unknown」行）+ 标签键映射（未知 tag 回退到 `*.unknown` 而非把原始 tag 泄进 UI）+ `reportHasData`（守住「不给空报告打 100 分」）。**测试**：`cargo test -p amos-devocare` **67 例**（63 lib + 4 集成；新增 hostfs 11 例含真实临时目录的扫描/删除/根限制/符号链接/深度上限）、`cargo test -p amos-tauri --lib devcare::` **7 例**（全 lib 201）、前端 `devcare.test.ts` **12 例** + `device-care.svelte.test.ts` **10 例**；`bun run check` **EXIT=0**（`tsc` 0、`svelte-check` 0/0、纯套件 OK、`test:svelte` **418 例 / 58 文件**）；`clippy --all-targets -D warnings`（两个 crate）与 `fmt --check` 干净。设计/需求追溯 `docs/devcare.md`（REQ-DC1…REQ-DC24）。
- **手机管家（Device Care）领域内核 `amos-devocare`（2026-09-10）**。补上「平台必须有手机管家（删除软件 / 清理垃圾 / 电池与权限）」留下的**决策层**：AmOS 此前已具备全部子系统（`amos-appstore` 安装卸载、`amos-applife` 进程/回收、`amos-power` 电量/热决策、`amos-monitor` 健康、`amos-media` 存储、daemon 隐私账本），但没有「什么可以安全地做」的单一真相源。新增纯 `std`、离线可测、平台无关的领域 crate `crates/amos-devocare`（对齐 `amos-media`/`amos-blocklist` 惯例；桥与 UI 留作下一阶段）。(a) **清理垃圾**：`spec::JunkKind` 只枚举**可重建**材料（应用缓存 / 残留安装包 / 日志 / 临时文件 / 缩略图 / 崩溃转储 / 空目录，外加**仅可审阅**的过期下载）——**用户媒体、文档、通讯录、短信、应用私有数据根本没有被建模成垃圾**，故不存在「判断写错就删掉用户照片」的代码路径（**安全 by construction**，非运行期兜底）；`analyze` 按 `JunkKind::ALL` 顺序确定性汇总、超 `MAX_JUNK_ITEMS`(10 000) **拒绝而非静默截断**、求和使用 `saturating_add`；`auto_cleanable_kinds` 是「一键清理」唯一合法选择来源；`plan` 拒绝空选择、拒绝未 `acknowledge_review` 的审阅型类别、按 `(kind, uri)` 冻结排序、超 `MAX_CLEAN_BATCH`(5 000) 拒绝并要求分批；`execute` **逐条诚实**（单项失败不中断其余、`freed_bytes` 只计**已确认删除**、`failures` 带 uri/kind/size/原因、`is_partial()` 可判）。(b) **删除软件**：`guard::UninstallGuard` 双层拒绝——**平台自带包**（`system`→`SystemApp`）与**钉死关键包集**（`CRITICAL_PACKAGES`：System UI / Settings / AI 入口 / **手机管家自身** →`Protected`，即使平台允许也拒绝，且 `Protected` 优先），只把 `selectable()` 交给 UI 报价，被拒项给出 `reason_key`；卸载动作仍属 `amos-appstore::uninstall`，本内核只判「许不许」。(c) **权限控制**：`permissions::{review_grants,granted_resource_count,apps_holding}` **只读**——**被拒绝的授权不显示**（视图绝不夸大访问）、无法归因（空 `app_id`）丢弃、去重、按稳定序排序、上限 500；**从不自行授权/撤销**（权威仍在 daemon 隐私账本）。(d) **电池与体检**：`health::assess(&CareInput) -> CareReport` 折成 0–100 分 + 等级（A≥90/B≥75/C≥60/否则 D），罚分为**可复算常数**（存储 ≥2 GiB→20、未充电 ≤5%→30、≤20%→15、热节流→10、敏感授权 ≥20→10、可审阅 App 为 Info 不计罚），且 **`None` = 未观测 ⇒ 该区域不进 `assessed`**——空输入得 `score=100` 但 `has_data()==false`，UI 必须显示「未知」而非满分（P0-3 落地）；不发明「权限从未被使用」这类无数据判断。`lib.rs` 顶部 `#![cfg_attr(not(test), deny(clippy::unwrap_used, expect_used, panic))]` + `#![forbid(unsafe_code)]`。**测试**：`cargo test -p amos-devocare` **50 例**全绿（46 lib 单测 + 4 集成 `tests/devcare.rs`：端到端 `scan→analyze→plan→execute` 含部分失败诚实计数、审阅型需显式确认、管家快照四区域折叠、`CareReport` JSON 往返）；`clippy --all-targets -D warnings`、`fmt --check` 干净。设计/需求追溯 `docs/devcare.md`（REQ-DC1…REQ-DC14）。补上「平台必须有手机管家（删除软件 / 清理垃圾 / 电池与权限）」留下的**决策层**：AmOS 此前已具备全部子系统（`amos-appstore` 安装卸载、`amos-applife` 进程/回收、`amos-power` 电量/热决策、`amos-monitor` 健康、`amos-media` 存储、daemon 隐私账本），但没有「什么可以安全地做」的单一真相源。新增纯 `std`、离线可测、平台无关的领域 crate `crates/amos-devocare`（对齐 `amos-media`/`amos-blocklist` 惯例；桥与 UI 留作下一阶段）。(a) **清理垃圾**：`spec::JunkKind` 只枚举**可重建**材料（应用缓存 / 残留安装包 / 日志 / 临时文件 / 缩略图 / 崩溃转储 / 空目录，外加**仅可审阅**的过期下载）——**用户媒体、文档、通讯录、短信、应用私有数据根本没有被建模成垃圾**，故不存在「判断写错就删掉用户照片」的代码路径（**安全 by construction**，非运行期兜底）；`analyze` 按 `JunkKind::ALL` 顺序确定性汇总、超 `MAX_JUNK_ITEMS`(10 000) **拒绝而非静默截断**、求和使用 `saturating_add`；`auto_cleanable_kinds` 是「一键清理」唯一合法选择来源；`plan` 拒绝空选择、拒绝未 `acknowledge_review` 的审阅型类别、按 `(kind, uri)` 冻结排序、超 `MAX_CLEAN_BATCH`(5 000) 拒绝并要求分批；`execute` **逐条诚实**（单项失败不中断其余、`freed_bytes` 只计**已确认删除**、`failures` 带 uri/kind/size/原因、`is_partial()` 可判）。(b) **删除软件**：`guard::UninstallGuard` 双层拒绝——**平台自带包**（`system`→`SystemApp`）与**钉死关键包集**（`CRITICAL_PACKAGES`：System UI / Settings / AI 入口 / **手机管家自身** →`Protected`，即使平台允许也拒绝，且 `Protected` 优先），只把 `selectable()` 交给 UI 报价，被拒项给出 `reason_key`；卸载动作仍属 `amos-appstore::uninstall`，本内核只判「许不许」。(c) **权限控制**：`permissions::{review_grants,granted_resource_count,apps_holding}` **只读**——**被拒绝的授权不显示**（视图绝不夸大访问）、无法归因（空 `app_id`）丢弃、去重、按稳定序排序、上限 500；**从不自行授权/撤销**（权威仍在 daemon 隐私账本）。(d) **电池与体检**：`health::assess(&CareInput) -> CareReport` 折成 0–100 分 + 等级（A≥90/B≥75/C≥60/否则 D），罚分为**可复算常数**（存储 ≥2 GiB→20、未充电 ≤5%→30、≤20%→15、热节流→10、敏感授权 ≥20→10、可审阅 App 为 Info 不计罚），且 **`None` = 未观测 ⇒ 该区域不进 `assessed`**——空输入得 `score=100` 但 `has_data()==false`，UI 必须显示「未知」而非满分（P0-3 落地）；不发明「权限从未被使用」这类无数据判断。`lib.rs` 顶部 `#![cfg_attr(not(test), deny(clippy::unwrap_used, expect_used, panic))]` + `#![forbid(unsafe_code)]`。**测试**：`cargo test -p amos-devocare` **50 例**全绿（46 lib 单测 + 4 集成 `tests/devcare.rs`：端到端 `scan→analyze→plan→execute` 含部分失败诚实计数、审阅型需显式确认、管家快照四区域折叠、`CareReport` JSON 往返）；`clippy --all-targets -D warnings`、`fmt --check` 干净。设计/需求追溯 `docs/devcare.md`（REQ-DC1…REQ-DC14）。
- **日历（Calendar）：iOS 对齐的月视图 + 日程 + 重复 + 到点提醒（2026-09-10）**。新增第 25 个内置应用「日历」。(a) **纯领域内核 `lib/calendar.ts`**（无 DOM、可确定性单测）：`CalendarEvent`（标题/地点/备注/起止/全天/`repeat`/`alertMinutes`）+ `CalendarGroup`（iOS 式彩色日历，`enabled` = 显示/隐藏）；本地时区、`Date` 基准的日期运算（`startOfDay/addDays/addMonths` 月末夹取、`monthGrid` 6×7 周日开头、`weekdayOrder`）；**有界重复展开** `expandOccurrences`（none/daily/weekly/monthly/yearly，1 步长，无 COUNT/UNTIL/EXDATE —— RRULE 的诚实子集；远早起的日常事件**快进**到查询窗口，且每次展开上限 `MAX_OCCURRENCES_PER_EVENT=2000`，杜绝病态数据导致的死循环）；选择器 `visibleEvents/searchEvents/occurrencesOnDay/groupByDay`；CRUD `addEvent/updateEvent/removeEvent/addCalendar/removeCalendar/toggleCalendar/reassignOrphans`；**坏档归一化** `normalizeEvents/normalizeCalendars`（丢空白标题、去重 id、倒置区间修正、全天吸附午夜、非法 repeat/alert/颜色回退、`EVENT_CAP=1000` 封顶）；种子数据。(b) **OS 级到点提醒 `lib/calendarCore.ts` + `svelte/osCalendarWatcher.ts`**（镜像 reminders/alarms/timer）：`collectDueEventAlerts` 只对**已到提醒时刻**（`now−ALERT_GRACE ≤ alertAt ≤ now`，Grace 15 分钟）且**未被隐藏日历**、**未触发过**的场次发通知；标记按 `事件id@场次开始`（`amos.calendarFired`）→ 重复事件**每场只响一次**、`syncDueEventAlerts` 幂等；`pruneFired` 有界回收。Shell 已挂载观察器（打开「日历」时不发，离开后补发）。(c) **UI `svelte/CalendarApp.svelte`**：月份标题/‹ › 翻月/「今天」、月/日程双视图、42 格月历（今日红字、选中高亮、每格最多 3 个日历色圆点）、选中日日程列表（全天在前）、日程视图按天分组（未来 60 天，重复展开）、搜索（标题/地点/备注）、**日历管理**（新建+10 色调色板、显示/隐藏开关、删除并把其事件回落默认日历）、事件编辑器（标题/全天开关/起止日期时间/所属日历/重复/提醒/地点/备注）、i18n 中英全文案、无障碍（`role=tab/switch`、`aria-*`、`data-*` 钩子）。对齐差距以**诚实边界**记录在 `docs/calendar.md`（无周/年视图、无自定义重复间隔、无参与人/附件、提醒为前台协调器而非进程被杀后的原生唤醒；AndroidAlarmManager 深唤醒另案）。(d) 注册三处单源（`appMeta` 25、`appRegistry` 加载器、`appGroups` productivity）；`appMeta`/`app-registry` 计数测试同步更新。**测试**：`calendar.test.ts` **34 例**、`calendarCore.test.ts` **15 例**（含 store 往返与损坏值不抛错）、`svelte-tests/calendar.svelte.test.ts` **19 例**（月历 42 格/今日唯一、选中日日程顺序、建/改/删、全天开关隐藏时间控件、非法日期拒绝、取消不落库、搜索、视图切换、日历显示隐藏与删除回落、观察器在聚焦时抑制/非聚焦时投递且幂等）；`calendar.ts` 与 `calendarCore.ts` 行覆盖 **100%**（聚合 `src/lib` 94.83% ≥ 门禁 90%）。验证：`bun run check` 全绿（`tsc` 0、`svelte-check` 0 error/0 warning、pure 863 例、`test:svelte` **397 例**（+19）、i18n 中英键奇偶）。
- **日历审计 + 补全（第二轮）：多天全天事件 · 延续标签 · 区域首周日 · 内核清理（2026-09-10）**。对上一轮日历做逐行自审计后补全 5 项能力并修掉 5 处真实缺陷。(a) **多天全天事件（iOS 支持，此前被架构性锁死）**：`lib/calendar.ts` 新增 `allDayEnd(startAt, endRaw)` —— 已是本地午夜的 `endAt` 原样保留（**归一化幂等**，重复加载不会把跨度越滚越大）、非午夜值向上吸附到次日午夜、早于起点则收敛为单日；`normalizeEvents`/`sanitizeDraft` 改用它，全天事件因此可跨任意多天；编辑器「结束」行改为**始终显示日期**（仅时间控件随全天隐藏），显示的结束日是**含末日的最后一天**，提交时 +1 天转回半开（`openEdit` 反向 −1 天，往返精确）。(b) **DST 安全的日历日窗口**：`occurrencesOnDay` 的窗口由 `startOfDay(d) + 86_400_000` 改为 `addDays(startOfDay(d), 1)`；新增 `atLocalTime(day, h, m)` 用 `setHours` 取墙钟时刻（`newEventDraft`/种子事件改用它）。此前在夏令时切换日，裸毫秒相加会把窗口挪到 01:00，把**次日**的全天事件错算到今天。(c) **月历圆点覆盖多天的每一天**：新增 `occurrencesByDay(list, from, to)`（把场次登记到它覆盖的每一天，且只在 `[from,to)` 内游走 → 有界），月历 `byDay` 改用它；日程列表仍按起始日归档（与 iOS 列表一致）。(d) **延续标签（不再显示属于昨天的开始时间）**：新增 `isContinuation(occ, day)`/`spansDays(occ)`，行内时间列对延续日显示 `→ HH:MM`（当天结束）或「延续/Continued」（新 i18n `calendar.continues`），并给出 `起 – 止` 跨度标签与 `data-continuation`/`data-span-days` 钩子。(e) **首周日跟随区域**：`weekStartsOn = locale === "zh" ? 1 : 0`（zh-CN 周一起 / en-US 周日起），月历与星期表头同步。(f) **内核清理**：删除死代码 `startOfNextMonth`（仅测试引用）；UI 的 `deleteCalendar` 改为调用已测试的纯函数 `reassignOrphans`（此前内联重复实现，存在漂移风险）；`lib/appMeta.ts` 陈旧注释「23 built-in apps」更正为 25。**测试**：`calendar.test.ts` 34→**44**、`calendarCore.test.ts` 15→**16**、`svelte-tests/calendar.svelte.test.ts` 19→**24**、`shell.svelte.test.ts` **+1**（真实集成：`open("calendar")` 经 `appRegistry` 动态 import 并真正挂载日历屏，断言 42 个日格）、`reminderNotify.test.ts` **+1**；`calendar.ts` **412/412**、`calendarCore.ts` **94/94** 行覆盖 **100%**。验证：`bun run check` 全绿（`tsc` 0、`svelte-check` 0/0、pure **874** 例 / 94 文件、`test:svelte` **403** 例 / 57 文件、i18n 奇偶）、`coverage:gate` **94.83% ≥ 90%**、`smoke:ui`（真实 headless Chrome）**4/4 PASS**（`docs/calendar.md` §3/§5/§6）。
- **通话记录补全 + 「信息」屏蔽此号码（2026-09-10）**。接续通话记录页与骚扰拦截，补齐两处用户直接可用的一键能力并做审计。(a) **通话记录方向筛选 + 两段式清空**：`lib/calllog.ts` 增 `CallFilter`、`filterHistory(list, filter)`（复用 `callHistory` 的同一归一化与上限；**无方向的旧记录只在「全部」出现，绝不被算作某方向**）、`clearCallHistory()` 具名 seam；`PhoneApp`「最近」页加 全部/来电/去电/未接 chips（`data-filter`）、筛选为空时显示「该筛选下暂无记录」以区别于空日志的「暂无最近通话」（`data-testid="history-empty"`），以及「清空 → 确认清空/取消」的两段式按钮（防误触清库；清空后回到「全部」）。(b) **「信息」页「屏蔽此号码」**：真机线程标题栏新增按钮（仅真机后端显示——离线会话按名字组织，写号码规则无意义），一键写入 `exact/both` 规则到**同一共享规则库**（电话拦截页 / `sms_snapshot` 过滤 / `CallScreeningService` 三处同源）；成功显示状态条（`data-ok="true"`，说明线程被过滤的原因），失败（域拒绝被 `invoke()` 吞成 `null`）显示红色 `role="alert"` 并留 `bridgeDiag()` 证据——**绝不静默假装已屏蔽**。测试：`calllog.test.ts` **+2**（`filterHistory` 方向/neutral 行/上限/垃圾输入、`clearCallHistory`）、`phone.svelte.test.ts` **+3**（筛选收窄、空筛选文案、清空两段式+持久化）、`messages.svelte.test.ts` **+2**（屏蔽成功/被拒两态）；`bun run check`（含 i18n 奇偶、`tsc`/`svelte-check` 0 error、`test:svelte` **374 例**）与 `vite build` 全绿（`docs/telephony.md` §13、`docs/sms.md` §11.2）。
- **通话记录页（Call History）+ 跨应用深链（2026-09-10）**。电话 App「最近」标签从"最近 4 个号码"升级为完整**通话记录页**，每行可 **回电 / 回短信 / 拉黑**。(a) 领域 `lib/calllog.ts`：`CallRecord` 增可选 `direction∈incoming|outgoing|missed`（`normalizeDirection` 白名单，未知值丢弃**不猜**；旧记录无方向保持中性，绝不假装去电），`recordCall(list, number, name?, now?, direction?)`；新增页面辅助 `callHistory`/`missedCalls`/`callWhenLabel`(today|yesterday|date|unknown)/`callDateStamp`/`fmtCallClock`（locale-neutral，便于测试）。(b) 写入端：去电记 `outgoing`；来电 `IncomingCall` 在 `Ended` 时按是否接通过记 `incoming`/`missed`（此前来电一律无方向）。(c) UI：方向图标 + 相对时间（今天/昨天/日期 + HH:MM）+ 未接计数 + 空态与诚实说明（本机上限 60 条）。(d) 跨应用深链 `svelte/appLinks.ts`：`composeSmsTo(number)` 先写 `messages` props 通道（`{composeTo, nonce}`）再切面到「信息」；`MessagesApp` 订阅后在真机态预填真实短信撰写框、宿主/离线态打开（或选中）同号本地会话，探测未完成则暂存待落定，链接**消费即清空**（重开信息不重复触发），全程不声称已发送。测试：`calllog.test.ts` **+6**、`phone.svelte.test.ts` **+2**、`messages.svelte.test.ts` **+1**（并新增 `afterEach` 清理假桥接防设备态泄漏）；`bun run check`（含 i18n 奇偶、`tsc`/`svelte-check` 0 error、`test:svelte` **369 例**）与 `vite build` 全绿（`docs/telephony.md` §13）。
- **骚扰拦截（黑名单）：来电 + 短信（2026-09-10）**。(a) 新域 crate `crates/amos-blocklist`：`Rule{pattern,kind=exact|prefix,channel=call|sms|both,label}` + `Blocklist`（号码规范化、**同号等价**：国家码/前导 0/`00` 形式互换，只丢 1–3 位前导码，绝不误并；前缀最短 3 位；未知号码开关；一地址一条；上限 500 淘汰最旧；JSON 往返且拒绝损坏/超限载荷）。(b) `crates/amos-tauri/src/blocklist.rs`：进程级共享状态 + 命令 `blocklist_snapshot/add/remove/clear/set_unknown/check` + 原子 JSON 持久化（`app_data_dir/blocklist.json`，损坏文件记日志后空表启动）。(c) **短信执法**：`sms_snapshot` 过滤被屏蔽发件人、`sms_messages(…, address)` 命中即拒、**实时收信命中则不发射 `sms-received`**（不通知/不刷新）。(d) **来电执法**：Kotlin `AmosCallScreeningService`（清单 `BIND_SCREENING_SERVICE`）+ `BlocklistGlue`（JNI `shouldBlockCall`/`hasCallRules`/`configure(filesDir)`；原生不可用时失败开放），命中即系统级拒接（`disallowCall+rejectCall+skipNotification`）；存在来电规则且未持 `ROLE_CALL_SCREENING` 时启动后请求角色。(e) UI：电话 App 第 5 个标签「拦截」（增删规则、精确/前缀、渠道、未知号码开关、诚实边界说明）+ 中英 i18n。真机（YY000286）：服务已注册；加 `1069` 前缀 + `+8618616091470` 精确后收件箱 7→3；重启后规则仍在；对被屏蔽线程读取被拒并指出规则；**被屏蔽来信 0 事件**（正常为 2）、解除后恢复且线程回归。真机发现并修复：运营商回环给国内格式 `18616091470`，原精确匹配失败 → 新增 `same_number`/`starts_with_number` 容忍国家码。测试：`amos-blocklist` 14 例、`amos-tauri --lib blocklist::` 6 例、前端 363 例 + 0 error 全绿。
- **短信文件夹全面实现：收件箱 / 发件箱 / 草稿（2026-09-10）**。(a) 域：新增 `crates/amos-sms/src/folder.rs`（`SmsFolder{Inbox,Sent,Draft}` + wire 名 + `android_type()` 1/2/3 + 未知值诚实报错 + `SmsFolderCounts`）；`SmsProvider` 改为 `snapshot(folder)` / `messages(thread_id, Option<folder>)` / `counts()`；Mock 以 `seeded_rows()` 单一数据源派生三个文件夹（收件箱才有未读）。(b) 线协议：`parse_counts`（缺省 0）。(c) 桥：`sms_snapshot(folder)`、`sms_counts`、`sms_messages(threadId, folder?)`，文件夹名在桥层解析（非法值先拒），仍 async + spawn_blocking + 8s 超时。(d) Kotlin：`snapshot(folder)`/`messages(threadId, folder)` 按 `type` 过滤（有界 LIMIT）、`counts()` 三个有界去重线程计数、未知文件夹 → `invalid`。(e) 前端：`smsFolderSnapshot`/`smsCounts`/`smsMessages(id, folder)` + `MessagesApp` 文件夹标签与计数、切换即重读；新增 `lib/smsDrafts.ts`（AmOS 本地草稿：纯函数、一地址一条、上限、`normalizeDrafts` 防损坏）与草稿页（编辑器预填、删除、发送后消费草稿；设备自带系统草稿另列「只读」一节）；i18n 中英。(f) 工程：新增 `make android-app`（先 build dist 再 tauri android build 再 `adb install -g`），修复本轮真机因忘记重建 `dist` 而装出旧 UI 的回归（`beforeBuildCommand` 为空）。真机验收：计数 `{inbox:7,sent:2,draft:0}`、标签「收件箱7/发件箱2/草稿」、发件箱仅含已发送、新建草稿标签变「草稿1」、草稿发送后消费且真实进入发件箱。测试：`amos-sms` 23 例、`amos-tauri --lib sms::` 10 例、前端 `check`（362 例 + 0 error）+ `smsDrafts` 5 例全绿。
- **SMS 真机收发全链路 + 两个跨领域前端修复（2026-09-10）**。(a) **推式接收**：`SmsGlue` 动态注册 `SMS_RECEIVED`（API 33+ **必须 `RECEIVER_EXPORTED`**，实测 `NOT_EXPORTED` 收不到）+ 新增**清单接收器** `SmsReceiver`（`exported=true` + `BROADCAST_SMS` 权限，进程未运行也能唤起）→ JNI upcall → Rust `sms::events` 发 `sms-received` → 前端重读收件箱**并重载当前会话**（新消息即时出现）。(b) **新短信撰写**：真机态「新短信」→ 号码+内容 → `sms_send` → 重读（系统入库，新线程自动出现）+ i18n。(c) **探测竞态修复**：`sms_status` 可能早于 glue `Attach`，改为有界重试（4×1.5s）。(d) **跨领域缺陷 1**：`window.__TAURI_INTERNALS__.listen` 在 Tauri v2 **不存在**（只有 `invoke`/`transformCallback`）→ `subscribe()`/`wm.onLayoutChanged()` 全部静默失效；现由 `bridge()` 合成 `listen`（事件插件 `plugin:event|listen` + `transformCallback`，unlisten 同款）。(e) **跨领域缺陷 2**：仓库无 capabilities → 真机报 `event.listen not allowed ... core:event:allow-listen, core:event:default`；新增 `crates/amos-tauri/capabilities/default.json` 授予 `core:event:default`，**同时解除所有事件驱动 UI 的封锁**（ai-token-received / sensor-data / layout-changed / telephony-event / store-updated 等）。(f) 其他：`android.rs` 增 `name()`+发送前校验+线程 ID 交叉校验；`SmsGlue` 增权限前置检查/有界 LIMIT/多段发送/`error_kind`。真机证据：自发自收回环（`from_me` 双向）、`SmsGlue: SMS_RECEIVED from '18616091470'`、DOM **无手动刷新**出现新消息、UI 发送 `UISEND-…` 成功。测试：`backend.test.ts` 增“无 listen 宿主走事件插件”用例（11 例）；`amos-sms` 19 例、`amos-tauri --lib sms::` 9 例、前端 `check`（360 例 + 0 error）全绿。
- **SMS 工程加固：有界 · 诚实 · 可审计（2026-09-10）**：按航空航天级准则逐层补全 SMS。(a) 新增 `crates/amos-sms/src/validate.rs`：`normalize_address`（剥离分隔符/保留首个 `+`/3..=20 位，拒绝非法字符）、`validate_text`（空/NUL/>1600 字符即报错，绝不静默截断）、`is_gsm7`+`segment_count`（GSM-7 160/153、UCS-2 70/67，非 BMP 按 2 单元计）。(b) `wire.rs` 有界化：载荷 4 MiB/线程 500/消息 1000/正文 16 KiB 上限（超限 `Invalid`）、时间戳 ≥0、非空 id/address、`error_kind`→类型化错误（permission/unavailable/invalid/failed）、`parse_messages_for` 线程交叉校验、`parse_send_reply`（只有 `ok:true` 才算成功）。(c) `SmsError::PermissionDenied` + `kind()` + `SmsProvider::name()`（`MOCK_PROVIDER`）。(d) 桥层 `sms.rs`：命令改 `async` + `spawn_blocking` + 8s `timeout`（同步命令会跑在主线程，阻塞查询会冻结 WebView）、新增 `sms_status`（区分真机/宿主 mock）、发送前域校验 + `tracing` 审计（地址掩码 `***后四位`，正文不入日志）。(e) Kotlin `SmsGlue.kt`：权限前置检查、`snapshot` 单次 `LIMIT 4000` 有界扫描（未读统计窗口已文档化）、`messages` 数字线程 ID 校验 + `LIMIT 1000` 取最新后反转、`send` 复校 + `divideMessage` 真分段（multipart）、错误全部带 `error_kind`（不再把异常吞成空收件箱）。(f) 前端：`smsStatus()`/`smsSnapshotResult()` + UI 三态（宿主 mock 保持本地会话；真实空收件箱显“本机暂无短信”；权限被拒显错误+提示+重试，重试重探后端）。验证：`amos-sms` 19 例、`amos-tauri --lib sms::` 8 例（含卡死 provider 超时）、`clippy -D warnings`、`check --features android`、前端 `typecheck`/`typecheck:svelte` 0 error + `test:svelte` 358 例，全绿（`docs/sms.md` 工程加固节）。
- **真实短信真机打通（2026-09-10，YY000286/S5）**：装 arm64 debug APK 后逐项真机验收并修两处缺陷。(a) **修复 Tauri 参数命名**：`sms_messages` 必须传 camelCase `threadId`（Rust 参数 `thread_id`），原 `lib/backend.ts` 传 `thread_id` 被拒（`missing required key threadId`）→ 线程消息读不出；已修并加 DOM 测试断言锁定。(b) **重构 MessagesApp 真实态**：真实收件箱改用独立状态（`realThreads`/`realMsgs`/`realActiveId` + `probed` 一次性探测 `$effect`），不再混入本地会话/通知 `$effect`（真机下该耦合会整窗卡死）；离线退回本地会话行为不变；新增“刷新”与诚实错误提示 + i18n。(c) 真机结果：`sms_snapshot` 返回真实线程（10010 未读 2 等）、`sms_messages` 返回真实往来、UI 显示 `📡 真实短信` 与真实气泡并可切换会话；`sms_send` 空地址诚实报 `Invalid destinationAddress`（全链路到达 SmsManager，未伪造）。`cargo check/test`、`clippy`、前端 `test`/`typecheck`/`typecheck:svelte`/`test:svelte`(355) 全绿。真实发送一条留用户确认目标后执行（`docs/sms.md`）。
- **真实短信（SMS）设备侧全链路（2026-09-09）**：把 SMS 从 host Mock 接到真机。(a) `crates/amos-sms/src/android.rs`：`AndroidSmsProvider`（`feature android=["dep:jni"]`）持有 `JavaVM` + Kotlin `SmsGlue` 全局引用，经 JNI 以 String↔String JSON 调 `snapshot()`/`messages(threadId)`/`send(address,text)`，复用 `wire.rs` 解析（绝不伪造）。(b) `crates/amos-tauri/android-glue/.../glue/SmsGlue.kt`：`bind(context)` + `external fun attach(this)`；查 `content://sms` 聚合线程（最新一条+未读计数）与线程消息，`SmsManager.sendTextMessage` 发送，无权限/异常诚实返回空或 `{"error":…}`。(c) `AndroidManifest.permissions.xml` 增 `READ_SMS`/`SEND_SMS`/`RECEIVE_SMS`，`AmosGlue.onStart` 绑定 `SmsGlue`。(d) `crates/amos-tauri/src/sms.rs` 增 `#[cfg(feature="android")] mod device`（`DEVICE` OnceLock + JNI upcall `Java_com_amos_ai_glue_SmsGlue_attach`，exactly-once），命令经 `active()` 设备优先、否则回退 Mock；`android` feature 含 `amos-sms/android`。(e) `MessagesApp.svelte`：桥接时展示真实收件箱并 `smsSend` 发送（标题 `📡 真实短信`，隐藏本地新建/删除），离线完全退回本地会话。验证：`cargo test -p amos-sms`(6)、`cargo test -p amos-tauri --lib sms::`(3)、`cargo check {amos-sms,amos-tauri} --features android`、`clippy -D warnings`、前端 `test`/`typecheck`/`typecheck:svelte`/`test:svelte`(355，含新用例) 全绿；真机读取/发送待设备验收（`docs/sms.md`）。
- **真实短信上接层：amos-tauri SMS 桥 + 前端桥函数（2026-09-09）**：在 `amos-sms` 领域地基之上接通宿主侧。(a) `crates/amos-tauri/src/sms.rs`：`SmsBridge`（host= `MockSms::new()` 空收件箱·诚实无伪造；`with_provider` 供设备/测试）持有 `Box<dyn SmsProvider>`，Tauri 命令 `sms_snapshot` / `sms_messages` / `sms_send`（成功返回 `"sent"` 以区分成功/错误/离线），序列化镜像 `SmsThreadOut` / `SmsMessageOut`；`lib.rs` 已 `manage` + 注册。(b) 前端 `lib/backend.ts`：`smsSnapshot()` / `smsMessages(threadId)` / `smsSend(address,text)`（未桥接/出错一律 `null`/`false`）。测试：`amos-tauri --lib sms::` **3 例**（host 空 inbox / seeded 映射 / send 拒空）全绿；`cargo fmt`、`clippy -D warnings`、`tsc` 干净。诚实边界：仍是 host=Mock；真机读收件箱/发送需下一步 Android provider(JNI)+Kotlin `SmsGlue`+权限+Messages 接真线程（`docs/sms.md`）。


- **真实短信领域地基 `amos-sms`（2026-09-09）**：为“端到端真机短信”新增独立领域 crate（对齐 amos-flashlight 范式）：`spec.rs`（`SmsThread`/`SmsMessage`）、`error.rs`（`Unavailable/Failed/Invalid`）、`provider.rs`（`SmsProvider` trait + `MockSms`：`new()`=空收件箱·诚实“host 无短信”，`seeded()`=固定演示收件箱）、`wire.rs`（纯 JSON 契约 `parse_snapshot`/`parse_messages`，缺字段一律 `Invalid` 不伪造）。workspace 注册。测试 **6 例**（empty/seed/mock send 拒空、snapshot/messages 解析与垃圾容错）全绿；`cargo fmt`/`clippy -D warnings` 干净。诚实边界：这只是**领域层地基**；真实读收件箱/发送需后续 Android provider(JNI) + Kotlin `SmsGlue` + 权限 + amos-tauri 桥 + Messages 接真线程，并须在你手机验收（见 `docs/sms.md` 进度与真机清单）。


- **Messages 本地多会话：可增删联系人多线程（前端，2026-09-09）**：把原来写死“单联系人小安”的本地聊天 UI 扩成**多联系人/多会话**——仍是本地离线演示，非真机 SMS（真机短信读取属后续 amos-sms，见 `docs/telephony.md`）。(a) `lib/messages.ts` 新增纯层 `Conversation`/`seedConversations`/`normalizeConversations`/`addConversation`/`removeConversation`/`findConversation`，存储键 `amos.messages.convs`。(b) `MessagesApp.svelte`：顶部会话条可切换线程、常驻“新建会话”输入行（填联系人即建空线程并选中）、每个线程独立收发/清空/删除（>1 才出现“删除会话”，删除后回落到剩余线程）；未读跨线程汇总为通知。测试：纯 `messages.test.ts` 扩至 **11 例**（新增 seed/normalize 垃圾容错与缺 id 回退/add 拒空拒重/remove）；Svelte DOM `messages.svelte.test.ts` 扩至 **6 例**（新增：新建联系人得空线程、线程间消息隔离、>1 时删除会话回落）。验证：Svelte 套件 **56 文件 / 354 全绿**、纯套件 `[bun-iso] test OK`、`tsc`/`svelte-check` 0 错 0 警、dist 重建。


- **Notes 离线自给 + “AI 离线”提示（前端，2026-09-09）**：备忘录核心（新建/编辑/搜索/导入导出/归档等）本就纯本地、不依赖 AI；新增诚实的能力探测 `lib/aiAvailability.ts`（纯：`classifyAiAvailability` 判定 `offline|unknown|mock|real`，`aiIsUnavailable`）——Notes 后台探测一次 daemon `get_status`，AI 不可用（无 Tauri 桥/daemon 挂/mock/degraded）时在顶部显示一行“AI 离线·备忘录可正常用”的**非阻塞**提示（`data-testid=note-ai-offline`），真实引擎在线则隐藏；**绝不**因 AI 缺失阻塞/报错任何备忘录操作。i18n `note.aiOffline`（en/zh）。测试：纯 `aiAvailability.test.ts` **5 例**（offline/unknown/mock/real/degraded 全分支）；DOM `notes.svelte.test.ts` 扩至 **31 例**（新增：无桥时提示出现且仍能新建；桥接真实引擎时提示隐藏）——Svelte 组件套件 350/350、`tsc`/`svelte-check`/纯测试全绿。


- **RAG 向量化后端扩展：Ollama `/api/embed` 支持 + 云端 embedding（`amos-ai`，2026-09-09）**：为“问我的笔记”的真实向量化补齐两条路径。(a) **本地 Ollama**：`OllamaEmbedder` 升级为先打新 `/api/embed`（复数响应 `{"embeddings":[[…]]}`）、404 时回退旧 `/api/embeddings`，并支持 `AMOS_OLLAMA_API_KEY` bearer（token 门控网关上也可用）；`rag_service` 的 `ollama` 分支附上该 bearer。(b) **云端**：新增 `ApiEmbedder`（任意 OpenAI 兼容 `/v1/embeddings`，`Authorization: Bearer`，body `{"model","input"}`→解析 `data[0].embedding`）；`AMOS_RAG_EMBEDDER=api` 选择，base 用 `AMOS_RAG_EMBED_BASE`（默认 OpenAI）、模型 `AMOS_RAG_EMBED_MODEL`（默认 text-embedding-3-small）、key 复用 `AMOS_API_KEY`。`Status.embedder` 标签诚实报 `mock|ollama|api`。验证：`amos-ai --lib` **212 passed**（+5：`/api/embed`/OpenAI 响应解析器、`ApiEmbedder`/`OllamaEmbedder` 对本地 mock HTTP 服务器的传输 round-trip + bearer 头断言），`clippy --all-targets -D warnings` 干净、fmt 清洁。诚实边界：本地/云端真实 embedding 均需对应可用的 key/可达端点（本机 Ollama 鉴权 401 阻断真实调用）；这些代码已把“升级到新接口/加云端”落地并本地 mock 验证，真实语义数值待有 key 实测（见 `docs/vector-db-rag.md`）。


- **AI 原生后端：Anthropic Claude + Google Gemini 适配器（`amos-ai`，2026-09-09）**：为 daemon 新增两个**原生协议**推理后端（非 OpenAI 兼容）。(a) `AnthropicBackend`——POST `/v1/messages`，带 `x-api-key` + `anthropic-version` 头、`stream:true`，SSE `content_block_delta/delta.text` 出词。(b) `GeminiBackend`——`…/models/{model}:streamGenerateContent?alt=sse`，`x-goog-api-key` 鉴权，SSE `candidates[0].content.parts[].text` 出词。(c) `inference/real.rs` 加带自定义头的通用流式 `stream_sse_with_headers` + 纯函数请求体构建/SSE 解析（可单测）；`BackendKind` 加 `Anthropic`/`Gemini` 及 `build()` 分支。(d) `server.rs` env：`AMOS_BACKEND=anthropic|gemini`（复用 `AMOS_API_KEY`/`AMOS_MODEL`，endpoint 空则用官方默认，`is_real_backend`/降级名单纳入二者）。验证：`amos-ai --lib` 207 passed（+5 新：SSE 解析、请求体、metadata），`clippy --all-targets -D warnings` 干净、fmt 清洁；live plumbing：`ai-backend.sh anthropic` → `get_status engine=anthropic model=claude-3-5-sonnet-latest`、`gemini`（本网不可达）→ 诚实 `engine=mock degraded=true`。前端 `providers.ts` 增 `anthropic`/`gemini` 预设（专用引擎、模型默认、无需 endpoint）→ `AiPage`/脚本/supervise 一并接入。诚实边界：两家官方域名在本测试网络下 Gemini 不可达、均未做真实计费调用；Claude/Gemini 的真实应答需相应 key 在可达网络实测。


- **AI 后端多提供商选择（本地 Ollama + 云端 OpenAI/DeepSeek/自定义，2026-09-09）**：把原来只支持「本地 / DeepSeek」的推理后端扩展为 4 选 1——**本地(Ollama)** 与云端 **OpenAI / DeepSeek / 自定义(任意 OpenAI 兼容 `…/v1/chat/completions`)**。(a) 纯前端 `lib/providers.ts`：`AiProviderId = local|openai|deepseek|custom`，`CLOUD_PRESETS` 给 openai(gpt-4o-mini)/deepseek(deepseek-chat) 提供 endpoint+model 默认值、custom 无默认必须填写；`readAiConfig/setAiConfig/envFor` 泛化为「云即 daemon 通用 `api` 后端」，key 依旧不落设置、只走 0600 key 文件。(b) Settings「AI 与智能」页改 4 段选择 + 云字段(模型/接口/Key)，custom 未填接口时阻止保存并提示（不再可能把请求发往空地址）。(c) Rust `ai_backend_switch` 新增透传 `model/endpoint`，云端 provider 统一写 key 到 `~/.amos/ai.key`。(d) `scripts/ai-backend.sh` 泛化云端分支并**持久化 + resume 还原 endpoint/model**（修了旧实现“编辑接口/模型在 UI 改了却不起作用、resume 丢 custom 配置”的真缺口）；`supervise-backends.sh` 同步识别 openai/custom。验证：`providers.test.ts` 扩至 **7 例**（openai/custom round-trip、envFor 泛云、custom 无默认）全绿；`tsc`、`svelte-check` 0 错 0 警；`bun test` 全绿；`cargo check/clippy -p amos-tauri` 干净；live 实证：`ai-backend.sh openai` → config 落 `backend=api+openai endpoint/model`、daemon `get_status engine=api engine_model=gpt-4o-mini`、无参 resume 正确还原、custom 缺接口干净报错。诚实边界：openai/custom 与 deepseek 同走 daemon 通用 `api` 后端，模型质量/可用性取决于所填 endpoint 的 key/额度；真机云端推理仍按部署文档走。README 运维速查更新。


- **React→Svelte 减法·步骤1 + 计划文档（`docs/react-subtraction-plan.md`，2026-09-09）**：确认运行时已纯 Svelte（`index.html→shell-entry→Shell.svelte`），React 为残留源。步骤1——把 `core.test.ts` 从 React `../apps` 迁到 Svelte 单一源 `lib/appMeta`（`apps.tsx` 本是其再导出，行为一致），9 例全绿、React 不再进该图。计划文档给出分组删除集（A 主宿 main/App、B components、C src/lib 下 React-hook 文件含 keepAwake、D apps/i18n/theme .tsx、E ~25 个 React .tsx 测试、F react deps+plugin）、4 个拦路项（coverage ≥90%、keepAwake 纯 bus/store normalize 保纯逻辑、Svelte 消费者确认、E 删除前补 .test.ts 等价）与 5 步顺序/门禁。诚实边界：后续删除仍在规划、未执行；C 组里 `keepAwake` 含纯 reason-bus 需先抽非 React core 才可删。
- **React→Svelte 减法·步骤2（保纯逻辑，不删除）**：抽 React-free **`lib/keepAwakeCore.ts`**（keep-awake reason bus：`screenHeld/heldReasons/assertHold/releaseHold/clearAllHolds/onHoldChange/resetHoldBusForTest`）+ `__tests__/keepAwakeCore.test.ts` **4 例**（清态/排序去重/释放/订阅+退订）全绿；旧 `keepAwake.ts` 改为**委托 core**（单一事实源，避免两套 bus 漂移），`useScreenHold` 改经 `onHoldChange` 订阅，外部 API 经 re-export 不变——React `keepAwake.test.tsx`（bun/happy-dom）**4 pass** 仍绿。审计 `useStoreValue.ts`：是 `amosStore` 上的薄 React 适配器、无独立纯逻辑（Svelte 已直读 `amosStore`），无需再抽。`tsc`、`svelte-check` 0 错 0 警。此步为后续安全删除 C（React-hook lib）/E（React 测试）铺覆盖前提。

- **Notes 偏好：『点按笔记 → 直接进整页编辑器』持久化开关（`lib/notePrefs.ts` + `NotesApp.svelte`，2026-09-09）**：新增独立偏好存储 `amos.notesPrefs`（与笔记内容分离，绝不触发对笔记列表的 normalize/校验）。`lib/notePrefs.ts`：`NotesPrefs{openInEditor}`、纯 `normalizeNotesPrefs`（缺省/非对象/坏布尔→默认 off，绝不 crash）+ `load/save`。`NotesApp`：行点击改走 `rowTap(n)`——`prefs.openInEditor && mode==='all' && !n.state` 时直接 `openEditor`（进整页），否则维持原内联展开（**默认 off 不破坏现有折叠/展开与既有测试**）；新增首屏「点按笔记直接进入整页编辑」checkbox（`bind:checked` 改 `prefs`，`onchange` `saveNotesPrefs`）。测试：纯 `notePrefs` **2 例**（normalize 缺省/合法/垃圾）+ DOM `notes.svelte.test.ts` 扩至 **18 例**（新增：默认行点击=内联展开且无整页 textarea；pref on 行点击=出现整页 textarea；toggle 勾选后 `amos.notesPrefs.openInEditor===true` 落库）全绿；`notes`(18)+`note-editor`(3)=**21 DOM**、`tsc` 与 `svelte-check` 0 错 0 警。



- **Notes 列表 Markdown 感知标题（`NotesApp.svelte` + `lib/markdown.ts`，2026-09-09）**：折叠/选择行标题由 `noteTitle` 升级为 `titleOf = markdownTitleOf(text) || noteTitle(text) || untitled`——笔记以 `# 标题` 开头时，行标题显示“标题”而非把 `#` 当正文首行；无标题回退不变。`notes.svelte.test.ts` 扩至 **13 例** 全绿。

- **Notes Markdown 导入 + 导出 UI（`NotesApp.svelte`，2026-09-09）**：把 md 纯助手接成可用功能。(a) **导入**——新建区加「⇪ md」按钮：把 compose 框内容当粘贴的 Markdown，`parseMarkdownImport` 剥 YAML front-matter + 归一后 `prependNote` 落为一条新笔记（行标题由 md 感知 `titleOf` 显示）、清空 compose、提示「已导入「标题」」。(b) **导出 .md**——展开笔记页脚加「⇩ .md」：`toMarkdownFile`（含 title/created/modified front-matter + 正文）复制到 AmOS 剪贴板并提示（暂无 on-device md 写盘后端，与 txt 无后端时同走剪贴板回退）。测试：`notes.svelte.test.ts` 扩至 **15 例**（新增：⇪ md 导入 front-matter 笔记落库且清空 compose+提示；⇩ .md 按钮存在且点击给回退提示）全绿；`notes`(15)+`note-editor`(3)=**18 DOM**、纯 markdown **14**、`tsc` 与 `svelte-check` 0 错 0 警。诚实边界：导出 .md 目前走剪贴板回退（未有 `notes_export_md` 落盘后端，列为后续）。

- **Markdown 导出器 + 导入/导出对称（`lib/markdown.ts`，2026-09-09）**：审计补全——先前只有“导入侧”（md→note），补 `toMarkdownFile(meta)` 作 Obsidian 侧镜像导出：YAML front-matter（`title` 按需引号转义 + 可选 `created/modified` ISO）+ 正文，与 `stripYamlFrontMatter`/`normalizeMarkdownBody` 可 round-trip。测试：`markdown.test.ts` 扩至 **12 例**（新增导出 front-matter/引号转义/round-trip 还原正文/空正文仍含 front-matter）全绿；`tsc` 干净。

- **Notes 列表 Markdown 感知标题（`NotesApp.svelte` + `lib/markdown.ts`，2026-09-09）**：折叠/选择行标题由 `noteTitle` 升级为 `titleOf = markdownTitleOf(text) || noteTitle(text) || untitled`——笔记以 `# 标题` 开头时，行标题显示“标题”而非把 `#` 当正文首行；无标题回退逻辑不变。测试：`notes.svelte.test.ts` 扩至 **13 例**（新增：预置 `# 阶段计划…` 笔记 → 折叠行含“阶段计划”且不含“# 阶段计划”）全绿；`notes`(13)+`note-editor`(3)=**16 DOM**、`tsc` 与 `svelte-check` 0 错 0 警。

- **Markdown 导入/标题纯助手（`lib/markdown.ts`，2026-09-09）**：为“类 Obsidian / 导入 .md”这条主线铺纯地基（无 Markdown 引擎依赖，全 headless 可测）。`stripYamlFrontMatter`——剥头部 YAML `---…---`（容错前导空行/CRLF；无闭合视为整段 front matter 返回空；无开标记原样返回）；`markdownTitleOf`——优先第一个任意级 `# 标题`，否则取首个非空行并剥 `-`/`*` 列表标记当显示标题；`normalizeMarkdownBody`——LF 归一 + 剥首空行 + trim（剥离 front matter 后空则返回空）。测试 `__tests__/markdown.test.ts` **8 例**全绿（front matter 移除/CRLF/无元数据/未闭合、标题各分支、规范化）。诚实边界：这是将来“粘贴/导入 .md”动作的现成纯能力，UI 导入入口尚未接线；对 `#` 标题取标题、front matter 剥离均按轻量约定，非完整 CommonMark 解析。

- **NoteEditor 审计补全（2026-09-09）**：对整页编辑器最新代码做 audit 并补两处真实缺口。(1) **卸载不丢数据**——`onDestroy` 原为 `deb.cancel()`，编辑器若经非「‹back」路径（外壳切走/父卸载）会丢最后未保存按键；改为 `onDestroy(() => deb.flush())`，任何关闭路径都先落盘。(2) **拒绝对已删除笔记静默假保存**——`commit` 原在 `editNote` 找不到 id（笔记被并发归档/删除）时仍置 `lastSaved/clean` 假装成功；现在先查 `store` 是否存在该 id，不存在则置 `saveErr="笔记已被删除，改动未保存"` 且不写盘（诚实报错，绝不对已不存在的笔记谎称已存）。测试：`note-editor.svelte.test.ts` 扩至 **3 例**（新增：ghost 笔记不在 store → 输入+‹back → 断言 `store` 不含 ghost、不写回）全绿；`notes`(12)+`note-editor`(3)=**15 DOM**；`tsc` 与 `svelte-check` 0 错 0 警。诚实边界：编辑器对“同窗口并发删除”的检测依赖每次 commit 读 store 判存在，属尽力而为的本地一致性，跨实例冲突仍需外部合并策略（超出本组件范围）。

- **Notes 整页编辑器接入列表（`NotesApp.svelte` + `NoteEditor`，2026-09-09）**：按 `docs/notes-editor.md` 把独立 `NoteEditor` 挂进备忘录——用**视图互换**（`editor` 状态置位时在正文位直接渲 `<NoteEditor>`，否则渲原列表，无绝对定位覆盖层）。新增 `editor: Note|null` + `openEditor`/`closeEditor`（close 重读 store 让 auto-save 结果回到列表行）；仅 active 笔记可开（`n.state` 挡归档/回收站）。入口：展开详情页脚在既有内联「编辑」旁加「整页」按钮（`openEditor(n)`）。auto-save 600ms 写回共享 store、`‹ back` flush 后返回列表（保 created/bump ts），与内联 Save/Cancel 语义分开。测试：`notes.svelte.test.ts` 扩至 **12 例**（新增端到端：加 note → 开「整页」→ 编辑器出现 → 输入 → `‹` 返回 → 编辑器消失、列表显示新文本、store 已含新文本），`note-editor.svelte.test.ts` 2 例；合计 **14 DOM** 全绿；`tsc` 与 `svelte-check` **0 错 0 警**。诚实边界：视觉/滚动/桌面键盘手感属运行期观感，需跑 app 确认（结构层已全测）；折叠行点按仍走内联展开，「整页」从展开详情进入。

- **整页编辑器（独立组件 + auto-save 纯逻辑，按 `docs/notes-editor.md`，2026-09-09）**：落地设计里“纯逻辑先行 + 独立组件、暂不改列表”。(a) 纯模块 **`lib/autoSave.ts`**：`createDebouncer(fn,ms,TimerApi?)`——定时器**可注入**（测试用假时钟驱动 coalesce→尾沿触发→cancel→flush-before-leave），`saveStateReducer`（dirty/saved/saving/error），`clockLabel`（HH:MM:SS）。测试 **9 例**（尾沿/合并/取消/flush/no-op、状态机 3 态、补零）。 (b) **`src/svelte/NoteEditor.svelte`**（独立、未接进 NotesApp）：接收一条已有 Note，整篇编辑 + **auto-save（600ms 去抖写回共享 `amos.notes`，保 `created`、bump `ts`，用 `editNote`）**；`‹ back` 先 `flush` 再回调，**绝不丢最后输入**；顶部「正在保存… / 保存于 HH:MM:SS」、工具条复用 `＋任务/☑/缩进续行/实时预览`（`noteEditing`+`fmtInline`）；svelte 5 runes，用闭包一次性捕获不可变 note 身份以避开 `state_referenced_locally`。DOM 测试 **`note-editor.svelte.test.ts` 2 例**（输入+‹back flush 后 store 文本更新且 `ts>旧值`、`created` 保留；预览渲染 `**粗**`+`#工作`）全绿。验证：`notes.svelte.test.ts`（11）+`note-editor.svelte.test.ts`（2）=**13 DOM**、纯合计 **67**、`tsc` 与 `svelte-check` **0 错 0 警**。诚实边界：`NoteEditor` 尚未挂进 `NotesApp`（接入=打开笔记进全幅覆盖层/从列表返回，属改动现有列表的侵入一步，待单独评审后接线）；auto-save 用真实定时器，DOM 测试经 `‹ back` 的 flush 路径覆盖确定性保存语义。

- **文档：整页 Note 编辑器设计（`docs/notes-editor.md`，2026-09-09）**：先定架构再落码，避免与现有内联编辑冲突。要点——编辑器为 `NotesApp` 内**全幅覆盖层**（不进 Shell surface，避免动 `shellState`/返回栈）；**auto-save（去抖 800ms 写回共享 `amos.notes`，保 `created`、bump `ts`）语义专属本编辑器、刻意不提供破坏性取消**（防丢数据；如需撤销用“撤销上次编辑”而非 discard），现有内联 Save/Cancel 语义不动；底部栏/‹ 返回先 flush 再回列表；数据复用 `notes.ts::editNote`/`noteEditing.ts`/`fmtInline`；先建纯逻辑 `lib/autoSave.ts`（可注入时钟的 debounce + saveState 状态机）headless 全测，再落 `NoteEditor.svelte` + `note-editor.svelte.test.ts`（含现有 `notes.svelte.test.ts` 无回归门禁）；移动/桌面共享同一数据路径与保存语义、仅布局与输入手感不同。本文为设计，未写实现。

- **Notes UI：详情时间戳显示“已编辑”并区分创建/修改（`NotesApp.svelte` + i18n，2026-09-09）**：把上一条存储层的 `created/modified` 接到 UI——展开的备忘录详情底部时间戳，当 `editedOf(note)`（有合法 `created` 且 `ts>created`）时在修改时间后追加 `· 已编辑`（i18n `note.edited`，en/zh 已加），并把 `title` 设为该笔记的**创建时刻**（`createdOf`）本地化串；新建（created==ts）不显示。测试：`notes.svelte.test.ts` 扩至 **11 例**（新增两条**预置 localStorage 的确定化**用例：`created<ts` 的已编辑 note 打开后正文含“已编辑”；`created==ts` 的新笔记打开后**不含**“已编辑”），原 9 例无回归；`tsc --noEmit` 与 `bunx svelte-check` 0 错 0 警。诚实边界：时间精度到毫秒——同毫秒内“新建即编辑”判为未编辑属既有 `editedOf` 语义；折叠行只显示最近修改（`stampOf`），创建信息仅在展开详情时经 tooltip 呈现。

- **Notes 存储层：created/modified 区分 + 向后兼容（`lib/notes.ts`，2026-09-09）**：`Note` 增可选 `created`（首次创建时间），`ts` 语义明确为“最近修改”；`makeNote` 同时记 `created=ts`；`normalizeNotes` 对旧存档**向后兼容**——有合法 `created` 则保留、缺失/非数值则回退（不落 `created` 字段，查询时折叠到 `ts`）；`editNote` 沿用对象展开故天然保留 `created`、只 bump `ts`。新增纯助手 `createdOf(note)`（created 缺失回退 ts）与 `editedOf(note)`（仅当带合法 created 且 ts>created 才判“已编辑”，legacy 未知记为未编辑）。测试：`notes.test.ts` 新增 **5 例**（makeNote created、editNote 保留 created 且 editedOf true、normalize 回退 ts、保留合法 created、忽略非数值 created）全绿；`notes+noteEditing` 合计 **58 pass**、`bunx tsc --noEmit` 干净。诚实边界：本改动是纯存储/语义层 + 向后兼容，UI 对“创建于 / 已编辑”的展示（如详情行加 `· 已编辑`）尚未接入，属后续 UI 项。

- **Notes 编辑体验：Tab/Enter 缩进、光标行任务、实时富文本预览（`lib/noteEditing.ts` + `NotesApp.svelte`，2026-09-09）**：为备忘录编辑器加“光标感知”编辑变换与 UI 面板。(a) 新纯模块 **`lib/noteEditing.ts`**——所有函数纯、headless 可测：`noteLineRange`（行边界）、`toggleTaskLineAt`（光标所在任务行 `[ ]⇄[x]`）、`enterContinuesTask`（在 checklist 行按 Enter 续出同缩进新 `- [ ] `，光标后文本搬上新任务行）、`shiftLineIndent`（Tab/Shift+Tab 缩进/反缩进 2 格，含反缩无可去时 no-op、光标夹取）、`prefixTaskAtLine`（把非任务/非列表/非空的光标行变为 `- [ ] `）。(b) `NotesApp.svelte` 编辑区接线——textarea `bind:this` + `onkeydown`（Enter 走任务续行、Tab 缩进、均 `preventDefault` 并经 `queueMicrotask` 还原光标）、新增「☑ 勾选 / ＋ 任务 / 预览」小工具条（前二者按光标行纯变换改 `editVal`）、「预览」开一个**实时富文本预览**块（复用 `fmtInline` 渲染 `**粗**`/`==高亮==`/`~~删除~~`/`[链接]`/`#标签`）；rows 放大并允许纵向 resize，保存/取消时复位 `editEl/previewOn`。测试：**`noteEditing` 纯 12 例**（行边界/勾选/续行含“光标后文本搬行”/缩进含 no-op 与光标/前缀含各类 no-op）全绿；**`notes.svelte.test.ts` 扩至 9 例**（新增：编辑中预览渲染 `**粗**`+`#工作`、☑ 勾选首行 `[ ]→[x]`、＋任务把首行变 `- [ ] `）全绿；`bunx tsc --noEmit` 与 `bunx svelte-check` **0 错 0 警**。诚实边界：光标还原/续行键位由纯逻辑保证、DOM 测试覆盖可无光标确定的行为（行 0）；含焦点/选区的高级键盘行为依赖真实浏览器焦点，属交互精修项。

- **文档：RAG 真机收尾跨文档指针（2026-09-09）**：按“vector-db-rag 为主、device-bringup-checklist 只加指针”原则——`docs/vector-db-rag.md`（主，已含 `Status`/`Recorded on-device measurement`/`On-device daemon bring-up`）保留 RAG 完整状态与实测；`docs/device-bringup-checklist.md` 因是媒体专用清单，仅在其末尾补一节“离线 RAG（非媒体）真机状态 → 指针”，一句话指向 `vector-db-rag.md` 三节，避免把非媒体内容混入媒体清单。

- **真机 RAG 一键复跑驱动：`scripts/android-rag-bringup.sh` + `make android-rag-bringup`（2026-09-09）**：把前面手工的真机收尾步骤做成可复现、诚实的脚本，完全仿 `android-voice-bringup.sh`（`--device/--daemon/--client/--bench/--tcp/--count/--dim` + `--check-prereqs|--dry-run|--apply`）。`--apply` 在一个阻塞式 adb 调用里完成：push 三个 aarch64 二进制 → `bench_arm ${COUNT}x${DIM}`（实测 on-device 检索延迟）→ 起 daemon（TCP loopback + mock embedder）→ `rag_once http://…` 跑 status/index/query/remove → pkill 停 daemon——**一次性返回、不占 adb 会话**（修了“裸后台 daemon 会让 adb shell 不返回”的坑：改为同一条 shell 内 start+sleep+run+pkill）。诚实 verdict：只 claim 自动可验步骤（staging/ARM index bench/daemon+Rag round-trip/offline mock dim=384）；embedding 延迟（Ollama）、语义检索质量、root/system UDS 部署均列为人工 checklist 不编造。Makefile 增 `android-rag-bringup` target + `.PHONY`；文档 `docs/vector-db-rag.md` 增脚本用法。实测 `--apply` 全绿（S5 · YY000286）：bench 20k×384 ≈91 µs/insert / top-5 ≈45.7 ms/query、rag_once round-trip 通过。

- **真机 daemon 端 RAG 链路跑通（S5 SDK34 arm64 · MT6768，2026-09-09）**：把**含 `Rag` 服务的 `amos-ai`** 交叉编译到 `aarch64-linux-android`（release）推上设备，起 daemon（`AMOS_RAG_EMBEDDER=mock`）并用新增的 `amos-ai/examples/rag_once` 客户端在**真机**上走通 UDS/TCP 的 **status→index→自匹配 query→remove** 闭环（实测输出：`indexed note:a dim=384` → `query hits=1 id=note:a score=1.0 passage=…` → `remove=true` → `indexed=0`），离线、无需 Ollama。过程中排到并解决两个真机坑：① 交叉编译带 C 依赖时 `.cargo/config.toml` 只配了 linker，需再导出 `CC_aarch64_linux_android`/`AR_…` + NDK `bin` 进 PATH（cc-rs 才找得到 clang）；② 该 FreemeOS/MTK ROM 在**adb shell（无 root）** 下因 SELinux 拒绝 shell 域在 `/data/local/tmp` bind unix socket（`Permission denied`），改用 daemon **TCP loopback 模式**（`AMOS_TCP_ADDR=127.0.0.1:19090`）即通。`rag_once` 支持参数为 socket 路径**或** `http://host:port`（h2c）。文档 `docs/vector-db-rag.md` 增「On-device daemon bring-up」节（含交叉编译 env、TCP 命令、实测输出）；真机部署（init.rc/system 域）仍可用 unix socket，TCP 模式仅作 adb-shell bring-up 路径。

- **RAG 检索真机实测（S5 SDK34 arm64 · MT6768，2026-09-09）+ bench 打印修正**：把 `bench_arm` 交叉编译到 `aarch64-linux-android`（release）推到设备跑出**真实的 on-device 检索指标**——这正是之前坚持“不编数字、须实测”的那项：2k×64 ingest ≈6.95 µs/insert、top-5 ≈0.82 ms/query；**20k×384 ingest ≈85 µs/insert、top-5 精确 ≈45.5 ms/query**（扁平精确全量扫描、单线程、无 SIMD）。并修 `examples/bench_arm.rs` 结尾误导句：现按 `cfg!(target_os="android")` 如实打印“measured on this Android device”，host 才打“build for aarch64 再跑”的说明（`cargo fmt/clippy` 干净）。这构成“先扁平精确、按实测再决定上 ANN”的真实依据。诚实边界：**embedding 延迟（Ollama `/api/embeddings`）仍待设备装 Ollama+模型后单独实测**，本数字是索引检索非嵌入，文档 `docs/vector-db-rag.md` 已单列记录。

- **`notesRag` 编排纯逻辑 + 状态机（`frontend-ts/src/lib/notesRag.ts`，2026-09-09）**：把 `lib/rag.ts` 的 daemon 检索接口“接成 Notes/Ai 功能”前那块 UI 无关的可验证地基。(a) 稳定命名 `note:<id>`（`noteRagId`/`noteIdFromRagId` 往返 + 拒绝非 note 来源）；(b) `notesToIndex`（按序为每个非空 active note 产 upsert op，幂等）；`notesToRemove`（据 UI 自己簿记的 indexed 集合，算出因删除/归档而应从索引移除的 `note:<id>`，外来 `pdf:*` 不乱动——因 daemon `Rag` 无 List RPC，这层簿记诚实放在前端）；(c) `buildCitedSnippet`（检索 passage → `- …` 引用块，去重、budget 内拼、**至少保首条**）+ `buildRagPrompt`（“拼 Context → 喂本地模型”的中文 Prompt，无笔记可引则回裸问题）；(d) `ragUiReducer` busy/error 状态机（idle/indexing/asking/error，同一时刻只允许一个操作、错误可 reset）。测试：`src/__tests__/notesRag.test.ts` **12 例全绿**（id 往返/索引/移除/引用块 budget 与去重/Prompt/状态机各态），`tsc --noEmit` 干净；与 `rag.test.ts` 合计 **23**。诚实边界：仍是纯逻辑 + seam，`AiApp`/`NotesApp` 的实际挂载调用与真 UDS 链路属运行期接线，见 `docs/vector-db-rag.md`。

- **RAG 测试加固（2026-09-09）**：把刚落地代码的空隙补成“全面测试”。daemon `rag_service` 增 2 单测——`sibling` 路径派生、passages 原子写入往返 + 缺失/损坏文件诚实降级为空（不 crash，可冷启）→ `amos-ai --lib` 升至 **202**；前端 `rag.test.ts` 的离线降级用例扩到**覆盖全部 4 个命令**（`rag_status/rag_query/rag_index/rag_remove` 在无桥时均 `null`，绝不编造命中）。clippy `-D warnings`（含 `--tests`）、fmt 干净。

- **Tauri 桥命令层：`rag_*` 连通 Svelte↔daemon（`amos-tauri/src/rag_client.rs`，2026-09-09）**：让 `lib/rag.ts` 不再指向不存在的命令——新增 4 个 `#[tauri::command]`（`rag_status/rag_query/rag_index/rag_remove`），每个 `RagClient::new(crate::daemon::channel().await?)` 走共享 UDS gRPC `service Rag`，prost reply → `Serialize` 镜像（`RagStatusOut/RagHitOut/RagQueryOut/RagIndexOut/RagRemoveOut`，映射函数纯可测）；`rag_query` 参数名对齐 proto `top_k`（`lib/rag.ts` 同步改 invoke key 为 `top_k`）。无 daemon 时命令返回**描述性错误**（UI 显示“搜索离线”，绝不编造命中）。测试：桥映射单测 **3**（status verbatim / query hit 含 passage / index+remove）；`amos-tauri` clippy `-D warnings`、fmt 干净；前端 `rag.test.ts` **11** + `tsc --noEmit` 仍全绿。至此整条链路就绪：`lib/rag.ts` → `amos-tauri rag_*` → daemon `Rag` UDS；剩纯 UI 接线（`AiApp`/`NotesApp`）与真机实测属运行期/设备期工作，见 `docs/vector-db-rag.md`。

- **Svelte 前端 RAG 纯逻辑客户端（`frontend-ts/src/lib/rag.ts`，2026-09-09）**：为“问我的笔记”铺前端的可测地基，延续仓库“纯 lib + seam + 离线诚实”惯例。(a) 类型 `RagHit/RagQueryResult/RagStatus/RagIndexReply/RagRemoveReply`；(b) 纯函数 `normalizeTopK`（0/负/NaN→默认 5、封顶 8、截断）+ 防御式解析 `parseRagHit/Query/Status/Index/Remove`（畸形字段一律返回 `null` 不抛、畸形 hit 丢弃不致命、非对象/数组/null 拒绝）；(c) seam `ragStatus/ragQuery/ragIndex/ragRemove` 全走 `backend.invoke`（`rag_*` 命令），**Tauri 壳外一律 `null`，绝不编造命中**（尚未实现的桥命令在 headless/未桥接时自然 null）。测试：`src/__tests__/rag.test.ts` **11 例全绿**（top-k 映射、各解析器合法/非法、离线 `ragStatus()` 为 null）；`bunx tsc --noEmit` 干净。诚实边界：真实 UI（`AiApp`/`NotesApp`）+ `amos-tauri` 桥命令（`rag_status/rag_query/rag_index/rag_remove` → UDS gRPC `service Rag`）是**运行时/真机接线**，本步只给纯客户端与解析契约，见 `docs/vector-db-rag.md`。

- **RAG 检索持久化：passage+向量落盘、daemon 重启自动恢复（`amos-ai/src/rag_service.rs`，2026-09-09）**：给上一条 `Rag` gRPC 服务补上「重启即失」这个诚实缺口。(a) 新环境变量 **`AMOS_RAG_STATE`**（base 路径）：每次 `Index`/`Remove` 把向量快照写 `<base>.idx`（走 `RagStore::persist_to`）、passage 文本表**原子写** `<base>.passages.json`（temp sibling + rename，版本号 `version:1`）；失败仅 `tracing::warn`，绝不 crash、绝不谎报成功。(b) 启动 `RagSvc::from_env` 时若 `<base>.idx` 存在则 `RagStore::load` 恢复索引 + `load_passages` 恢复文本；损坏文件诚实 `warn` 并以空库冷启（有 fallback，不 panic）。(c) embedder 选择抽成 `embedder_from_env`/`label_from_env` 复用（mock 默认 / ollama）。测试：新增 UDS e2e **`tests/rag_persist_e2e.rs`**——真 `serve()` 起两次、同一 `AMOS_RAG_STATE`：第一次 index（断言 `.idx`/`.passages.json` 已落盘）→ abort → 第二次起新 daemon → `Status.indexed==1` 且同一 self-match query 仍返回该 id + 原文 passage（证明是真恢复、不是从文件编造）。验证：`amos-ai --lib` 200 passed、两 e2e 各 1 passed、clippy `-D warnings`（含 `--tests`）、fmt 干净。

- **RAG 检索 gRPC 服务落地并挂进 daemon UDS（`amos-ai/src/rag_service.rs` + `proto/ai_agent.proto` `service Rag`，2026-09-09）**：把上一步的检索能力以标准 RPC 暴露给 System UI，真正让“问我的笔记/文件”走通 daemon 那条链路（前端接线另列后续）。(a) **proto**——新增 `service Rag`：`Index`（按 id 索引/编辑重索引）/`Remove`/`Query`（embed + 精确 top-k，返回 `id+score+passage`，passage 供引用与拼 Context）/`Status`（indexed 计数、维度、诚实 embedder 标签），`top_k=0⇒默认 5`、服务端封顶 `MAX_TOP_K=8`。(b) **`RagSvc`/`RagEngine`**——`RagStore<Box<dyn Embedder + Send>>` + 内存 passage 表；embedder 由 `AMOS_RAG_EMBEDDER` 选：默认 `mock`（离线确定性，仅验证 plumbing 非语义）、`ollama` 走 `OllamaEmbedder`（`AMOS_OLLAMA_HOST` + `AMOS_RAG_EMBED_MODEL` 默认 bge-m3）；**blocking embed 在 `tokio::task::spawn_blocking` 内执行**不卡 async daemon；错误经 `VectorDbError→Status` 上抛、poison 锁 `into_inner` 恢复不 panic。(c) **`amos-vector-db` embed.rs 增 blanket `impl Embedder for Box<E>`** 让动态 embedder 可塞进泛型 store。(d) **挂载**——`serve()` 在共享 UDS 上 `.add_service(RagServer::new(RagSvc::from_env()?))`，与 AiAgent/NetGuard/Privacy/Telephony 并列（仿既有 add_service 模式）。诚实边界：passage+向量目前**内存态**，重启即失（持久化为文档列出的下一步）；“retrieve-then-answer” 由调用方组合 `Rag.Query`（带回 passage）再走既有 `AiAgent.StreamChat`。测试：`rag_service` 单测 **5**（index/status/self-match+remove/空库不报错/top-k 封顶/编辑 upsert 不重复）+ UDS e2e **`tests/rag_rpc_e2e.rs` 1**（真 serve()：索引→self-match 检索回 passage→remove→空库查询空）全绿；`amos-ai --lib` 总 **200 passed**、clippy `-D warnings`、fmt 干净（含 proto 重生成）。

- **RAG 检索 daemon 侧接通：`OllamaEmbedder` + `RagStore`（`amos-ai/src/rag.rs`，2026-09-09）**：把上一条 `amos-vector-db` 的纯检索核心接到 daemon，补上「文本→向量」这个对外 seam 的真实实现，对应 `docs/vector-db-rag.md` 原「Next step」。(a) **`parse_embedding`/`embeddings_endpoint`**——纯函数解析 Ollama `/api/embeddings` 响应（缺字段/非数组/空/非数字/**`1e400`→inf 这类非有限值**一律拒绝，诚实契约与 `Vector::new` 同级）。(b) **`OllamaEmbedder`**（实现 `amos_vector_db::Embedder`）——POST `/api/embeddings`，`AMOS_OLLAMA_HOST` 语义对齐 chat 侧 `OllamaBackend`（尾斜杠 trim、可 `with_bearer`、默认 keyless）；blocking `ureq`，文档注明 daemon 须经 `spawn_blocking` 调用；模型不可达/非 2xx/坏 payload 一律 `VectorDbError::Embed` 上抛，**绝不静默降级 mock**。(c) **`RagStore<E: Embedder>`**——embedder + `FlatIndex` 折叠成 daemon 可驱动的 `ingest(id,text)`（首次取向量长度**惰性建索引**）/`remove`/`retrieve(text,k)`（空库返回空结果不造假）/`persist_to`/`persist`/`load`；空库 persist 与未设路径都是诚实错误；编辑同 id 原位 upsert（`ragstore_edit_upserts_in_place` 锁「旧内容不再精确命中」）。测试：**21 个 `rag` 单测**（纯解析 7 + 本地一次性 HTTP server 驱动的 OllamaEmbedder 6：round-trip/bearer/keyless/500/bad-payload/不可达 fail-fast + `RagStore` 8 用确定性 Mock）全绿，`amos-ai --lib` 总 **195 passed**、clippy `-D warnings`、fmt 干净。接线：`amos-ai` 新增依赖 `amos-vector-db`（纯 std，不引入 native/网络）+ `pub mod rag`。诚实边界：真实 embedding 质量/维度/ARM 延迟仍待真机 Ollama 实测；proto retrieve-then-answer RPC 与 Svelte `AiApp`/`NotesApp` 接线是下一步，见 `docs/vector-db-rag.md`。

- **离线本地向量检索核心落地（`amos-vector-db`，2026-09-09）**：对应 `FUNCTIONAL_GAP_ANALYSIS` #7「无 RAG/本地检索」里「有 `amos-pdf-parser` 榨出 chunk，却没有东西把 chunk 喂进可检索的向量索引再捞回 top-k」的**后半段**。新成员 crate `crates/amos-vector-db`：**纯 Rust（serde/serde_json，无 C、无 native、无网络），host + aarch64-linux-android 交叉编译友好**。核心：(1) `FlatIndex`——内存扁平、**维度固定**（构造 `new(dim)`）、`add`/`upsert`（编辑笔记按 id 原位替换，不重复）/`remove`/确定性 `search(query, Metric, k)`；精确扫描，**不预上近似 ANN**（个人库几千 chunk 量级扁平够用，ANN 待真机 `bench_arm` 证明再上）；等分按 id 升序断序，两次构建结果一致（有测试锁）。(2) `metric`——`Cosine`/`Dot`，长度不匹配报错、零向量 cos 返回 `0.0` **绝不 NaN**、维度/有限性检查。（3）`model::Vector::new` 校验非空 id + 非空 + **全有限**，`NaN/inf` 一律拒绝。（4）快照 `snapshot_bytes`/`load_bytes`/`save_to`/`load_from`——`version:1` 版本号 + 逐行维度/有限性/空 id 校验，非法即 `Corrupt`，绝不静默加载毒化索引。（5）`embed` 传输无关 seam——`Embedder` trait + 确定性 `MockEmbedder(dim)`（host 离线管线可跑）；真实 Ollama `/api/embeddings` 适配器**不属于本 core**，放 `amos-ai` daemon（它已持 HTTP client + `AMOS_OLLAMA_HOST`）。`#![forbid(unsafe_code)]` + 生产期 `deny(clippy::unwrap_used/expect_used/panic)`。测试：**35 单测 + 6 集成**（端到端：文本→MockEmbed→`FlatIndex` upsert→磁盘快照→重载→检索；编辑/删除笔记语义；**换 embedding 模型导致维度变化必须硬报错**而非静默；缺失快照 = 诚实 IO error）全绿；`clippy --all-targets -D warnings`、`fmt --check` 干净；`cargo check --target aarch64-linux-android` 本机实测 EXIT 0；新门禁 `make vector-db-check`。诚实边界：**快照只存向量+id，不存原文 chunk**；嵌入质量/维度与 **ARM 真机 embedding 延迟数字**由真机 Ollama 实测，本 core 只给 `examples/bench_arm`（本机 host 实测 2000×64 扁平 top-5 ≈ 0.21 ms/query、ingest ≈1.5 us/insert，仅作方法演示，**非 ARM 承诺**）。文档 `docs/vector-db-rag.md`。

- **常驻原生语音助理（device_mic_* 前端可达 + 同传音频线修复）闭环（2026-09-09）**：把「AAudio 真机采集→本地 sherpa」的宿主可验代码面补完并修正一处真实集成缺陷。(a) **`device_mic_*` 前端可达路径**：`lib/backend.ts` 增 `deviceMicStart/Stop/Status` bridge 包装 + 纯 `lib/deviceMic.ts`（`parseDeviceMicStatus`/`isNativeMicBackend`）+ AI 应用 composer 的 `DeviceMicButton.svelte`（`AiApp.svelte` 挂载）——诚实：浏览器/离线禁用、bridged 无原生麦提示「无原生麦/RECORD_AUDIO 未授权」、不伪造「正在听麦」；**每秒轮询 `deviceMicStatus`** 反映外部启停 + 实时已识别句数（bring-up 可观察）；i18n `ai.deviceMic*` en/zh。(b) **回复上收 AiApp 单一 `assistant-voice-event` `turn_done` sink**：`StreamVoiceButton`/`DeviceMicButton` 都不再自订订阅，杜绝双气泡、DeviceMicButton 不再隐性依赖 StreamVoiceButton；bridged svelte 测试锁「一个 turn_done→恰一个 agent 气泡」。(c) **OS 级 `RECORD_AUDIO` 原生授权 seam**（结构到位待设备/gradle）：`crates/amos-tauri/src/mic_permission.rs`（JS-awaitable `mic_permission_state/request`，host 诚实 `native:false`；Android 上 `isGranted()/request()` + `onResult(granted)` JNI upcall 回填 oneshot）+ `android-glue/…/MicPermissionGlue.kt`（`REQ_MIC=9004`，`PermissionWire.requestNeeded` 启动绑定、`onResult` 增加 `REQ_MIC` 分支；external 调用 `runCatching` 防缺 native feature 崩 boot，已同步 gen）。`cargo check`/`clippy --features android --lib -D warnings` 两配置全绿、`mic_permission` host 单测 2。(d) **常驻 worker 分割锁**：`assistant_voice.rs` 增 `resident_capture_submits_each_utterance_and_final_partial_on_end`（speech→silence→speech→EOF 恰 2 个 AudioEnd），18 例绿。(e) **同传音频线真实 bug 修复**：`InterpApp.svelte` 原把麦克风帧经 `frameToChunk` 以 **PCM16 字节**发给 `interpret_audio`（Rust 端为 `Vec<f32>`，且 `amos_int::Session::feed_audio`/sherpa `push_samples` 都吃 f32）→ 每个 16-bit 样本被拆成两个 f32 值、ASR 输入损坏（测试用 MockPipeline 忽略音频故未暴露）。改为 `audio.ts::frameToInterpChunk`（下采样到 16k 后返回**原始 f32 样本值**），`audio.test.ts` 换新契约断言（48k→16k 值保真、不再出现字节翻倍）。(f) **真机 bring-up 一键驱动器**：`scripts/android-voice-bringup.sh`（`--check-prereqs`/`--dry-run`/`--apply`，诚实只 claim C2/C3 probe 证据、C1/C4–C7 打印人工清单）+ Makefile `android-voice-bringup`。验证：前端 `bun run check` 全绿（`tsc`/`svelte-check` 0/0、**59 文件 333 例**）、Rust `amos-tauri --lib assistant_voice/interpret/mic_permission` 全绿、`bash -n`+`--dry-run` 验脚本。诚实边界：Kotlin `compileDebugKotlin`、真机 C1–C7 与 `REQ_MIC` 对话框仍需设备；`preview6.js` 预览包未重生成。文档：`docs/aaudio-sherpa-bringup.md` §7。


- **审计补全最新 2026-09-09 增量（`amos-android` + System UI，2026-09-09）**：对「per-Activity LMK 生命周期 / 实时传感器流 / AAudio 回调 / LMK surface 对账」整批新增做深度审计，修正三处真实缺陷。(a) **`amos-android::activity_observer::FoldingObserver`**：顶层活动自身 `onStop` 曾因 `top()` 在活动置 `Stopped` 后返回 `None` 而被丢弃——单个前台 Activity 退到后台时包停在 `Pause→Visible`（受保护）、永不被 LMK 回收；现改为更新前捕获 `was_top`，**顶层 Stop 无论其后再无可见活动都照发 `Stop`**（`importance()` 落到 `Background` 可回收），同时保留「非顶层 stopped 兄弟不降级仍在顶层的活动」。新增 2 单测（`stopping_the_foreground_top_backgrounds_the_task` / `top_stop_while_a_sibling_is_still_resumed_is_suppressed`）；`amos-android` 69→**71**。(b) **`LiveSensors.svelte`**：`live.start()` 异步——若卡片在 resolve 前卸载，`onDestroy` 时 `stopFeed` 尚未赋值、resolve 后打开的 `sensor-data` 监听永不关闭（反复进出诊断页会累积泄漏）；现加 `disposed` 守卫，resolve 时若已卸载则立即 `stop()`（与 `Shell.svelte` 既有 LMK watcher 的异步启动守卫一致）。(c) **`AndroidApp.svelte`（生命周期徽标/recents）**：成功 `launch` 后不刷新 `tiers`——启动的应用已成容器前台但徽标不出现、且立刻再点会被绕过「已在运行」守卫而重复冷启；现于成功分支调用 `refreshTiers()`，徽标即时呈现、二次点击被守卫生效。新增 1 DOM 回归测试（launch→徽标出现→二次点按被门控）；`android.svelte.test.ts` 7→**8**。验证：`cargo test -p amos-android`（**71 + 1 e2e**）、`cargo fmt`/`cargo clippy -p amos-android --all-targets -D warnings` 干净、`tsc --noEmit`、`sensor-live.svelte.test.ts` 冒烟全绿。诚实边界：真机 ActivityManager(binder/`dumpsys`) / AAudio / 传感器 producer glue 喂入仍是设备 bring-up seam。

- **System UI 消费 `sensor-data` 实时流：`lib/sensorLive` + `LiveSensors` 卡片（2026-09-09）**：让上一条「传感器实时广播」真正被界面用起来——(a) `frontend-ts/src/lib/sensorLive.ts`：纯 reducer `applySensorLive`（事件驱动刷新，仅接受经 `toSensorData` 校验的事件、计数按族/模式/清空分类）+ 订阅式 `createSensorLive`（`start()` 打开 `sensor-data` listen 并尽力从 `sensor_host_snapshot` seed 能量档/后端；`pushRaw` 走与真实监听相同归一化器；`reset` 仅清零本端计数）；`toHostSeed`/`isSensorDataPayload` 辅助。(b) `svelte/LiveSensors.svelte`：挂进 Settings `DiagnosticsPage` 的小卡——绿色 LIVE 点 + IMU/预览帧/清空计数 + 最近 IMU 样本 + 最近帧元数据 + 能量档实时更新；无样本显示等待提示（不造假）；新增 en/zh 文案 `settings.sensorLive*`。(c) 前端测试：`__tests__/sensorLive.test.ts`（7 项：事件驱动刷新、分族计数、mode/cleared、订阅通知/reset、垃圾防御、guard、host seed）+ `svelte-tests/sensor-live.svelte.test.ts`（1 项 mount 冒烟）。桌面演示：WebView 调 `sensor_host_record_imu`/`record_frame` → `sensor-data` → 卡片实时刷新（不依赖 daemon）。验证：`bun run test` `[bun-iso] test OK`、`bun run typecheck` 与 `bunx svelte-check` 0 error/0 warn、`bun run test:svelte` **59 files / 330 tests 全绿**。诚实边界：卡片反映 System UI 宿主传感器流；真机设备侧 producer glue 仍留待 bring-up（届时推入样本自动驱动此卡）。
- **System UI 传感器实时数据流广播（`sensor-data` 事件，`amos-sensor` + `amos-tauri`，2026-09-09）**：补「传感器数据向 System UI 的实时 Context 总线广播」缺口——除按需快照外，System UI 的 `SensorHost` 现为**实时推送源**：凡向共享流桥推入被接受的 IMU 样本/相机帧、能量档真实切换或 glue detach 清空，都把 `sensor-data` 事件 `emit` 给 WebView（镜像 `telephony-event`/`telemetry-spy-hit`/`clipboard-changed`）。(a) **领域层 `amos-sensor`**：`LiveSensorProvider` 增 Tauri-free 订阅点 `subscribe(Arc<dyn Fn(&StreamChange)>+Send+Sync)`，新增 `StreamChange{Imu|Frame|Cleared}`；`ImuLatest::record` 改返回 `bool`（accept 才通知，陈旧样本不广播）；`record_frame` 仅在校验通过后广播带 `seq` 的最新帧；`notify` 先快照订阅者、**释放锁后**再调用（无死锁/重入）；`clear_samples` 广播 `Cleared`。(b) **System UI `amos-tauri`**：`SensorHost` 增 `manager: Arc<SensorManager>`+`sink: EventSink`，`set_notifier` 装广播、`wire_bus_observer` 把 `StreamChange` 映射成扁平 `SensorHostEvent`（`ts_ms/kind/backend/mode/imu/frame/prev_mode`，`SENSOR_DATA_EVENT="sensor-data"`）；相机事件只带**元数据**（id/尺寸/format/seq，原始帧字节不上 UI 事件总线）；能量档仅在**真实变化**时广播带 `prev_mode`。boot 于 `lib.rs::setup()` 用 `host.set_notifier(handle.emit(SENSOR_DATA_EVENT,…))` 接上 WebView。(c) **前端**：新增 `lib/sensorEvents.ts`（`subscribeSensorData`/`toSensorData` 防御式归一化/`isKind`）+ `__tests__/sensorEvents.test.ts`（7 项）。验证：`amos-sensor` 新增 3 项、`amos-tauri` sensor_host 新增 4 项（live 事件含 mode/backend/元数据、非法/未通告帧不广播、mode 仅变化一次带 prev_mode、未装 notifier 静默 no-op）；`cargo test -p amos-sensor`（37+1 e2e）、`cargo test -p amos-tauri --lib`（165）、`cargo clippy -p amos-sensor/-p amos-tauri --lib -- -D warnings`、fmt、前端 `bun test`+`tsc`、`cargo check -p amos-tauri --features android --lib` 全绿。诚实边界：真机设备侧 producer glue（`SensorEventListener`/Camera2 `ImageReader` `arm` 到同一 bus）仍需 bring-up，届时推入样本自动经本广播达 System UI。文档：`docs/sensors.md` §8。
- **Android LMK-proxy：per-Activity 生命周期契约（`amos-android` + proto，2026-09-09）**：把「每包一 top-Activity」推进到**带活动身份、存活性与 importance 解耦**的可离线模型。(1) `proto/android_compat.proto`：`ActivityEventRequest` 增 `activity_id`(3)；`ActivityEvent` 增 `STARTED`(5)。(2) `lmk.rs`：`Record` 增 `live`+`top`；**只有 `Resume` 升顶层驱动 importance**，`Stop/Pause` 仅作用于顶层（非顶层 stopped 兄弟不改包 tier），`Destroy` 经 `destroy_last` 仅在**最后**存活活动时才拆任务/表面；移除 `mark_alive`，新增 `resume_activity/start_activity/pause_activity/stop_activity`。(3) `service.rs`：`dispatch_activity` 按 `activity_id` 分流（空=legacy 自愈、非空=per-activity 折叠），`Destroyed` 仅在任务确已不存在时发。(4) 新 `activity_observer.rs`：稳定 identity + `FoldingObserver` 顶层折叠（onStart→STARTED / 顶层 Resume/Pause/Stop / onDestroy→带 id）。验证：`amos-android` **69 单测 + 1 e2e**、fmt/clippy 干净、`amos-ai --tests`/`amos-tauri --lib` 对重生成 proto 编译通过。诚实边界：真机 ActivityManager(binder/`dumpsys`) 喂入仍是 seam。文档：`docs/lmk-proxy.md` §7/§8、`docs/DELIVERY_NOTES_2026-09-09-android-lmk-activity.md`。
- **真机 AAudio「硬件采样回调」采集 seam 落地（`amos-audio`，2026-09-09）**：把 AAudio 麦从**阻塞 `read` 拉模式**补上**真实 data-callback（push）驱动**——经 `AAudioStreamBuilder_setDataCallback` 注册回调，AAudio 在其实时线程按新周期把 mono I16 采样推给我们（回调内 I16→f32 宽化、复用 scratch 零稳态分配），喂进新增**纯 std、宿主可测**的有界 `SampleRing`（`src/ring.rs`，Mutex+VecDeque+Condvar，pre-reserved；`read` 有空即返部分、按 ~1 s 饥饿预算仅在回调真停摆时才报 EOF——活麦静音不误判结束）。新增 Android seam `AAudioCallbackCapture`（feature `aaudio` + Android；`Drop` 先 `requestStop` 再 `close`，确保回调停止后才释放共享态；`unsafe impl Send` 单所有者移交），实现同一 `AudioCapture` trait，`source::PlatformMic::open_device()` 的 aaudio 分支**默认改走回调采集**（`AAudioCapture` 阻塞拉仍保留为显式选择）。验证：ring 纯宿主单测 6 例（有序/部分读/满溢丢旧计数/空读按预算 EOF/迟到生产者不误判 EOF/并发无损有序）；host `cargo test -p amos-audio` **24 passed**、clippy `-D warnings` 干净、fmt 干净；Android `cargo clippy --target aarch64-linux-android -p amos-audio --features aaudio --all-targets -- -D warnings` 干净；NDK 交叉 `cargo ndk -t arm64-v8a -P 26 build -p amos-audio --features aaudio --example aaudio_link_smoke` **链接通过**（证明新增 `AAudioStreamBuilder_setDataCallback/setFramesPerDataCallback` 对 `libaaudio.so` 可解析）。诚实边界：运行时采集仍需真机 bring-up（host 无原生麦、不伪造）；见 `docs/audio-hal-bridge.md` §设备 seam / 真机验收。

- **宿主证明「回调采集→常驻 worker」整段契约（`amos-tauri`，2026-09-09）**：为上一轮新增的 AAudio data-callback seam 补首个**经真实消费者**的宿主集成测试——`assistant_voice.rs` 测试内 `RingCapture`（`AudioCapture`，背靠 `amos-audio::ring::SampleRing`，仅当生产者 `open=false` 且已排空才报 EOF，杜绝活麦误判结束）+ 独立生产者线程模拟 AAudio 实时回调周期推语音/尾静音 → 驱动 `spawn_resident_capture`，断言 `Audio` 流出、静音门后 `AudioEnd`、`submitted()==1`。验证：`cargo test -p amos-tauri --lib` **154 passed**（并行重跑稳定）、clippy `-D warnings` 干净、fmt 干净；host 全链路（常驻采集→daemon `Payload::Audio`→**真 sherpa**）由既有 `assistant_voice_e2e`（3/3）与 `amos-ai --features asr-sherpa` `bidi_sherpa_audio`（1/1）保持绿。诚实边界：AAudio 回调的运行时采样仍待真机验收。

- **真机回调运行时探针 example + 纳入 Android 音频门禁（`amos-audio`，2026-09-09）**：新增 `aaudio_callback_probe`（feature `aaudio` + Android；host 上 cfg no-op）——真开 `AAudioCallbackCapture` 读 ~1 s 采样并打印到达样数/峰值/EOF 诊断，供 bring-up 时单独确认 data-callback 在出样再接 System UI。`Cargo.toml` 注册、`scripts/android-audio-check.sh` 在 link-smoke 后加一段编译+链接覆盖。验证：`cargo ndk -t arm64-v8a -P 26 build --features aaudio --example aaudio_callback_probe` 编译+链接通过；`bash scripts/android-audio-check.sh` 全绿（3 ABI aaudio+tinyalsa 编译 + link-smoke + probe）；host/Android clippy `-D warnings` 干净、fmt 干净。诚实边界：探针是**运行时工具**，其“真出样”只代表该回调路径在设备上已能取到 PCM，不等于 ASR/完整 System UI 闭环（后者仍需真机走 `device_mic_start` + sherpa 验收）。

- **真 sherpa 的「AudioEnd 收句」host e2e（`amos-ai`，2026-09-09）**：`bidi_sherpa_audio` 增第二例 `bidi_audio_real_sherpa_finalizes_on_explicit_audio_end`——同一个真 sherpa daemon，但按**常驻 voice worker 的收句方式**：推 `Payload::Audio` 期间语音、随后发显式 `Payload::AudioEnd`（`spawn_resident_capture`/`assistant_voice_end` 即此信号）触发 daemon `AudioEnd → ChatAsr::finish()` force-finalize，断言回包引用真转写（YELLOW）。这补上了此前只验证「sherpa 自带 endpoint（尾静音）自动收句」而未验证「客户端显式 AudioEnd 收句对真 ASR」的缝隙；与 `assistant_voice_e2e`（worker 把这些帧发给 daemon）合起来即在 host 上闭合「常驻采集 worker → 真 sherpa」整段。验证：`cargo test -p amos-ai --features asr-sherpa --test bidi_sherpa_audio` **2/2**、`--lib`（asr-sherpa）**175 passed**、clippy `--features asr-sherpa --all-targets -D warnings` 干净、fmt 干净。覆盖：`gated-check` 本就跑此 test binary，自动纳入，无需改 Makefile。诚实边界：仍是 daemon 侧（真实 sherpa + 真实 wire 帧）；worker 线程运行时与真机 AAudio 采样仍待真机验收。

- **CI 环境漂移修复：统一本地/runner 工具链（2026-09-09）**：修 `gated-native-backends` / `android-audio-seams` / `lint-and-test` 反复红的一类根因——本地 M 系 Mac（brew NDK 23、git-ignored `.cargo/config.toml` 写死 NDK 23/api31/`darwin-x86_64` clang）与 Ubuntu x86 runner 交叉编译链漂移，且 GitHub 默认 runner 22.04→24.04 迁移**移除 `libappindicator3-dev`** 使 Tauri 依赖 apt 安装失败。改动：① `.github/workflows/ci.yml` 全部 job 由浮动 `ubuntu-latest` **钉死 `ubuntu-24.04`**、`libappindicator3-dev`→`libayatana-appindicator3-dev`、原生 gated job 加 `CARGO_NET_RETRY`/`CARGO_HTTP_TIMEOUT`、顶层加 `concurrency.cancel-in-progress`；② 原生工具链容器化（可选）：`.github/docker/ci-android/Dockerfile`（Ubuntu 24.04 预装 Rust stable+rustfmt+clippy、4 个 Android target、**cargo-ndk 4.1.2**、NDK **26.1.10909125**、protoc 28.3、Tauri 依赖）+ `.github/workflows/container-image.yml`（推 GHCR）；设仓库变量 `CI_ANDROID_IMAGE` 后两个原生 gated job 改在容器内跑，未设走 host fallback（等价原绿路径）；③ host fallback 钉版本统一到 `.github/actions/prep-android` 复合 action（与 Dockerfile 同钉，漂移会被本地门禁拦截）；④ 根目录 `deploy.sh`（统一本地入口：**发现** NDK 而非写死、重生成 `.cargo/config.toml` + 导出 `CC/AR/CARGO_TARGET_*_LINKER`，`lint` 子命令执行与 CI `make lint` 完全相同的 `fmt --check` + `clippy --workspace --all-targets -- -D warnings`；另有 `test/android/android-build/docker/ci-local/doctor`）；⑤ `scripts/ci-local-gate.sh` + Makefile `ci-local`/`deploy`/`doctor`：本地校验 shell 语法、workflow/action YAML、Dockerfile↔prep-android 版本钉一致性、可选 docker build。验证：`make ci-local` PASSED（bash -n 全绿、`.github` YAML 全解析、NDK/cargo-ndk 钉一致；docker daemon 未启动故 build 跳过）；`./deploy.sh doctor` 实测定位到本机 brew NDK 23.1.7779620（漂移直接证据）；`.cargo/config.toml` 重生成结果与既有一致。诚实边界：容器模式/镜像真实 `docker build` 未在本机验证（daemon 未起，见 `docs/ci-engineering.md`）。文档：`docs/ci-engineering.md`、README `CI parity` 节。
- **状态栏/系统监控「真实电量 + 诚实蜂窝」分层（2026-09-09）**：把装饰电量和"空白蜂窝"补成真实而诚实的数据链。**电池——桌面真实电量闭合**：`system_health`（daemon→`amos-monitor`）在 `/proc` 缺失的 macOS 桌面本如实 unknown → 顶栏空"—"。新增 `amos-tauri/src/host_battery.rs`（macOS 解析 `pmset -g batt` / Linux 读 `/sys/class/power_supply` / 其余 `None`，纯安全 Rust 无 FFI 无新 crate）+ 命令 `system_host_battery` 注册进 `lib.rs`；前端 `lib/batteryStatus.ts` 增有序 `firstBattery([daemon, host-Tauri, 浏览器 Battery API])`，Svelte(生产) 与 React(dev 回退) 两根 `StatusBar` 及 `AboutPage/SystemPanel/MonitorApp`（经 `lib/system.ts` `systemStatusWithHostBattery()`）均采用——桌面宿主不再空"—"。`batterySvg(percent, cls?, tone)` 增充电绿/低电黄/临界红/unknown 空框（旧调用不传参保持中性）。**蜂窝——诚实态，绝不伪造信号/运营商**：纯 `lib/cellularService.ts`（`no-radio/off/no-signal/connected`，`defaultRadioSignal()`=本机无 modem）；Settings「蜂窝网络」页「服务状态」行 +「关于本机」蜂窝行明示"未接入蜂窝网络（本机无 SIM/调制解调器）"；**受控 seam `svelte/cellularRadio.ts`**——控制中心蜂窝模块仅在真实 modem 源 `radio.present` 才渲染（本机=隐藏不误导，未来真机接线自动出现）。i18n `settings.aboutCellular/cellularService/cellularNone/cellularDataOff/cellularNoSignal/cellularConnected`（en/zh 对齐）。验证：Rust `cargo test -p amos-tauri --lib` **153 passed**（含 `host_battery` 4：pmset 放电/AC充电/无电池行→None/clamp）、clippy `-D warnings` 干净、fmt 干净；前端纯单测新增 `batteryStatus`/`netStatus`/`cellularService` 全绿、`bun run test` `[bun-iso] OK`、vitest **57 文件 / 318**、`tsc`/`svelte-check` 0/0。诚实边界：蜂窝信号/运营商在无真 modem 时一律不造假（iOS 同：无 SIM 不显信号条）；无电池台式机仍如实"—"。文档：`docs/status-battery-cellular.md`。
- **LMK 自保护守卫 `amos-ai::life_guard`（base A，2026-09-08）**：把 `deploy/android/amos.rc` 里 `oom_score_adj -1000` 那条**一次性静态写**补成**运行时验证/上报/自愈**的守护。现状审计结论：init.rc Native Daemon + `oom_score_adj -1000` + 容器反向 `am force-stop` 均已存在（`deploy/android/amos.rc:39-49`、`amos-android` lmk/force_stop、daemon governor `drive_host_decisions`）；唯一真缺口是**守护进程自身从不在运行时确认"防回收"真的生效**。新模块：`ProcFs` seam（读/写 `/proc/self/oom_score_adj`）+ `LifeGuard::{check,ensure,spawn_periodic}`——`check` 无副作用分类 `Protected/Unprotected/Unverifiable/Unsupported`；`ensure` 特权时**写目标并回读确认**才报 `Healed`，无特权给 `Unprivileged`（不伪造）、写完回读不符给 `RecheckFailed`（无静默）；`PlatformProcFs` Linux/Android 真读、无 oom 控制宿主诚实 `Unsupported`。`server.rs::serve()` 增 `life_beat`（与既有 beat 同 cadence、同 shutdown abort，首 tick 即启动验证+自愈）。诚实边界：`-1000` 只防 **LMK 按 OOM 分级回收**，非绝对存活（显式 force-stop / 框架绕过仍可终止）；非特权不假装治愈。验证：`cargo test -p amos-ai --lib life_guard` **12 passed**、`--lib` **162 passed**、clippy `--all-targets -D warnings` 干净、fmt 干净、模块 `#![forbid(unsafe_code)]`。文档：`docs/life-guard.md`（含 base A 设备验收步骤）。

- **LMK 自保护守卫 `amos-ai::life_guard`（base A，2026-09-08）**：把 `deploy/android/amos.rc` 里 `oom_score_adj -1000` 那条**一次性静态写**补成**运行时验证/上报/自愈**的守护。现状审计结论：init.rc Native Daemon + `oom_score_adj -1000` + 容器反向 `am force-stop` 均已存在（`deploy/android/amos.rc:39-49`、`amos-android` lmk/force_stop、daemon governor `drive_host_decisions`）；唯一真缺口是**守护进程自身从不在运行时确认"防回收"真的生效**。新模块：`ProcFs` seam（读/写 `/proc/self/oom_score_adj`）+ `LifeGuard::{check,ensure,spawn_periodic}`——`check` 无副作用分类 `Protected/Unprotected/Unverifiable/Unsupported`；`ensure` 特权时**写目标并回读确认**才报 `Healed`，无特权给 `Unprivileged`（不伪造）、写完回读不符给 `RecheckFailed`（无静默）；`PlatformProcFs` Linux/Android 真读、无 oom 控制宿主诚实 `Unsupported`。`server.rs::serve()` 增 `life_beat`（与既有 beat 同 cadence、同 shutdown abort，首 tick 即启动验证+自愈）。诚实边界：`-1000` 只防 **LMK 按 OOM 分级回收**，非绝对存活（显式 force-stop / 框架绕过仍可终止）；非特权不假装治愈。验证：`cargo test -p amos-ai --lib life_guard` **12 passed**、`--lib` **162 passed**、clippy `--all-targets -D warnings` 干净、fmt 干净、模块 `#![forbid(unsafe_code)]`。文档：`docs/life-guard.md`（含 base A 设备验收步骤）。
- **被动外发审计链 `amos-telemetry-spy`（无感异步审计网卡）→ daemon gRPC → System UI 实时警告（2026-09-09）**：为「12G 物理真机上芯片/App 可能经隐蔽 UDP 盲区外发数据」落地一条**只读、不阻断**的审计链。(a) **新 crate `amos-telemetry-spy`（默认纯 `std`，host 可测）**：identifier（serial/IMEI/Cell ID 字节 token）、scanner（纯函数明文子串启发式 + `Confidence` 分级，短于 3B 忽略）、packet（纯 `std` 的 Ethernet/IPv4/IPv6/TCP/UDP 头解析，不依赖 `pnet_packet`，使 decode+scan 默认即可单测；修 IPv4 越界 panic 漏洞）、signal（`EgressMatch` 高危审计事件）、identity（`DeviceIdentity` seam + `MockIdentity`，样本**显式伪造**）、capture（**`audit` feature**：pnet 抓 `rmnet_data0` → `spawn_stream` 异步桥，阻塞循环跑在 Tokio `spawn_blocking`）；纯 `analyze::match_frame` 组合 glue。诚实边界（承 `docs/anti-telemetry-egress-guard.md`）：只见 AP/App 数据面（非 modem/baseband）、明文子串=低置信启发式非实锤、pcap 需 root/AmOS-AOSP。(b) **gRPC 派发链逐 crate 接线**：`proto/telemetry_spy.proto`（`TelemetrySpyService.Watch(stream EgressHit)` + 默认关闭的测试注入 `SimulateHit`，`AMOS_SPY_ALLOW_INJECT` 门控）→ `amos-proto` 生成 server/client → `amos-ai::telemetry_spy_service`（`TelemetrySpySvc` 持 `tokio::sync::broadcast`，`Watch` 用 `BroadcastStream` fan-out）挂进 `serve()` 共享 UDS；`--features telemetry-spy` 启用 `EgressMatch→EgressHit` 映射 + `TelemetrySpySvc::ingest_match` feed seam（host 可测，真机只需 pnet 打开网卡）→ `amos-tauri::telemetry_spy::spawn_telemetry_spy_watch`（`setup()` 启动，转发 `telemetry-spy-hit`）→ 前端 `lib/telemetrySpy.ts`（防御式 `toSpyHit` + `spyNotif` 写入 `NOTIF_KEY`，**绝不回显 serial/IMEI/Cell ID 明文**）→ Notification Center/横幅。(c) 顺手修 `amos-ai::netguard_service` 测试 3 处 `Request::new(())` 应传 `StatusRequest` 的编译 bug。验证：`amos-telemetry-spy` 默认 23 + audit 24 单测；`amos-ai` telemetry_spy_service 默认 5 / `--features telemetry-spy` 6；真 UDS daemon e2e `tests/telemetry_spy_rpc_e2e.rs` 1（boot `serve()`→订阅 `Watch`→`SimulateHit`→收到 hit）；`amos-tauri` telemetry_spy 2；前端 `telemetrySpy.test.ts` 7 + 全量 `[bun-iso] test OK`；相关 crate clippy `-D warnings` / fmt 全绿。文档：`docs/telemetry-spy.md`、`docs/DELIVERY_NOTES_2026-09-09-telemetry-spy.md`。诚实剩余：真实 pnet 抓网卡 producer 需 rooted/AmOS-AOSP + `audit` feature 的真机（接线草图已给，emit 路径已 host 测试）；默认 host 无 producer → 整链安静不造假。`,



- **离线 RAG 数据提取管道前段落地（`amos-pdf-parser`，2026-09-08）**：对应 `FUNCTIONAL_GAP_ANALYSIS` #7「无 RAG/本地检索」里用户把 PDF 丢给 AI 助手时「本地 7B 看不懂二进制 PDF」的第一步——把文字/表格榨成可喂进 context 的文本 + chunk。新成员 crate `crates/amos-pdf-parser`：**纯 Rust（`lopdf 0.34`，`default-features=false` + `nom_parser`，去 rayon/chrono 保 Android 交叉编译友好）**。核心：(1) `extract` 每页 **font-aware 提取文本**（走 lopdf `extract_text`，认 `Tf`/字体编码/ToUnicode，能扛真实/CJK 文档）；(2) `layout` 轻量文本矩阵追踪 → `table` 几何对齐表检测（`Model/Score` 式生成表），表可 `to_csv`/`to_markdown`；(3) `util::estimate_tokens` 脚本感知**近似 token**（CJK≈1/字符、Latin≈¼/字符，诚实标注是规划器不是 BPE）；(4) `chunk` RAG 分块（页锚 + overlap + 硬性 `est_tokens<=target`，CJK 永不在字符中间切）。软失败局部化：坏页给 warning + 空文本而非整篇失败。API：`PdfParser`/`ParsedPdf`/`ParsedPage`/`Table`/`chunk_document`；bin `amos-pdf-parser`（`--format text|json`、`--chunks`）。测试：**28 单测 + 6 集成（用 lopdf writer 生成真 2 页 PDF：正文页 + 3×2 表格页，走 FlateDecode 解码，验证 text/tables/chunk/路径/非法输入）全绿**；`clippy -D warnings`、`fmt --check` 干净；`cargo run --example make_sample_pdf` 端到端出样。诚实边界：表格几何通过 *lossy ASCII* 解码（CJK 单元格文字回读 page text）、`cm`/精确字宽未建模故列估算仅对「每格自建原点」的生成表可靠、纯扫描/图片 PDF 无文本层需 OCR（文档 `docs/offline-rag-pdf.md`）。

- **`amos-pdf-parser` 强化：font-aware 表格解码 + Android 交叉编译门禁（2026-09-08）**：表格单元格文本不再用 lossy ASCII 兜底——`layout` 解码改为**可插拔**（`rows_from_ops_decoded` 按 `Tf` 当前字体把字节交给解码器，默认 `ascii_text` 兜底），`extract::tables_for_page` 走 `get_page_fonts` + `get_font_encoding` + `Document::decode_text`（认字体编码与 `ToUnicode` cmap，能力与 `extract_text` 同级但保留每格几何）。**真实 CJK/非 ASCII 表格文字现在能保留**（只要字体带编码/ToUnicode）；集成测试在表格加一格 WinAnsi 字节 `0xE9`，断言回读成真 Unicode `é`（而非 `U+FFFD`）。单测 +2（解码传回真 Unicode、解码器收到当前字体名）→ **30 单测 + 6 集成全绿**，clippy `-D warnings`、fmt 干净。新增 **`make pdf-android-check` 门禁**（纯 Rust/lopdf 无 C，`cargo check --target aarch64-linux-android` 无需 NDK linker）+ host 测试，本机实测 EXIT 0。文档 `docs/offline-rag-pdf.md` 诚实边界与验证节同步更新。

- **`amos-pdf-parser` 航空航天级加固：无 unsafe / 无静默数据丢失 / 确定性 / 对抗健壮性（2026-09-08）**：审计修缺。(1) `#![forbid(unsafe_code)]`（lib + bin），并保留生产期 `deny(unwrap/expect/panic)`——解析可报错/告警但**不崩溃**。(2) **消除静默降级**：字体/编码不可解析或解码失败而退回 lossy ASCII 的每个文本元素都被**计数并写入 `ParsedPdf.warnings`**（"N text element(s) decoded with ASCII fallback … may be lossy"）；忽略 warning＝主动接受有损，绝无隐性数据损坏；新增集成测试 `lossy_decode_is_surfaced_not_silent`（无 `Tf` 的 CJK show 必须浮出 warning）。(3) 确定性/幂等：表格检测与分块两次运行逐字节一致、chunk index 连续（新单测）。(4) 对抗健壮性：未知/畸形操作符、缺操作数、极端数值、错误操作数类型均不 panic（新单测 `pathological_operators_do_not_panic_and_are_deterministic`）。(5) 补边界单测：零页文档良构。门禁：**42 测试（35 单测 + 7 集成）全绿**、clippy `-D warnings`、fmt、`aarch64-linux-android` 交叉 check（A=0）。文档 `docs/offline-rag-pdf.md` 新增「Integrity, safety & determinism (aerospace-grade baseline)」一节。
- **Binder 化拨号 + EmergencyMap 紧急路由统一层（`amos-telephony`，2026-09-08）**：`src/android.rs`（feature `android`）把 `ACTION_CALL` intent 起呼替换为 **`TelecomManager#placeCall(Uri,Bundle)`**（System UI `Context` 经 `getSystemService("telecom")` 直取，即 Binder `ITelecomService` 事务的 JNI 等价物）；模块头文档诚实标注：`placeCall` 宿主于 `system_server`，**手写 `/dev/binder` ioctl 毫无崩溃免疫**——"无 SIM/锁屏/无 UI 通 110/112"由 modem/RIL 网络层路由保证，OS 完全卡死属 OEM 硬件。新增 **`crate::route`**（VM-free、主机可测）：`DialRoute`+`route()`+`guard_regular/guard_emergency`；`Mock` 与 Android 两个 provider **共用**此分离：普通拒绝紧急号（新错误 `TelephonyError::MustUseEmergencyPath`）、紧急拒绝普通号（`NotEmergency`），`AndroidTelephonyProvider`/`AndroidEmergencyTelephonyProvider::new` 携带 `EmergencyMap`。验证：`cargo test -p amos-telephony`（lib **68** + e2e **2**）全绿、host `clippy -D warnings` 干净、`cargo check/clippy --features android` 通过、`cargo check -p amos-ai` 通过；`answer`/`end`/录音/实时 `status` 仍诚实返回 `Provider(P3)`，InCallService/TelephonyCallback 桥与锁屏无 UI 紧急拨号留真机/OEM 验收。文档 `docs/telephony.md` §10.4/§12 #9 更新。

- **`sample_health` 加采样间隔拿到真实 CPU 忙碌读数（S5，2026-09-08）**：给 `examples/sample_health.rs` 在两次 `snapshot()` 间加 sleep（默认 400ms，可经 `SAMPLE_HEALTH_SLEEP_MS` 覆盖）。真机（S5）结果：**400ms 窗口 `cpu_busy_window_after_400ms=0.9%`、1200ms 窗口 `4.3%`**——均为有界、合理（空闲 shell）的真实忙碌百分比，证明 `LinuxSystemSampler` 的 CPU delta 忙碌计算在真实 `/proc` 上按窗口正常产出；内存 total 仍与 `/proc/meminfo` 逐字节一致。设备临时文件已清理，fmt/clippy 干净。

- **`amos-monitor --features linux` `sample_health` 真机测试（S5 / Android 14，2026-09-08）**：新增 `examples/sample_health.rs`（读真实 `/proc`，feature `linux`），交叉编译 `aarch64-linux-android` 并 `adb push` 到零售机运行。结果（决定性正向）：`LinuxSystemSampler` 在真机读到真实 `/proc`，`mem_total_bytes = 7,999,995,904 B` 与设备 `/proc/meminfo MemTotal 7,812,496 kB` **逐字节精确一致**（kB→B 换算正确），available 同刻读取、`mem_used_pct=42.2%`。`cpu_busy_window=unknown` 为两次采样几无间隔→delta≈0 的诚实「窗口不足」（要真实忙碌读数需在两次采样间 sleep），非缺陷。设备临时文件已清理。诚实标注：只读 `/proc` 的 linux sampler 在 shell 权限下即可真机验证；写 sysfs 的 `amos-power --features linux`（scaling_max_freq）在非 root shell 下不可测（需 root/基座）。

- **`amos-media` `scan_dir` 真机测试（S5 / Android 14，2026-09-08）**：交叉编译 `amos-media` `scan_dir`（HostFsProvider 真实 `read_dir`）为 `aarch64-linux-android` 并 `adb push` 到零售机运行。结果：(1) 能在 Android userspace 正常编译/加载/运行（不崩溃，exit 0），逐标准集合给计数、未知 tag 有提示、优雅容错；(2) **决定性实证 scoped storage**：`/sdcard/DCIM/Camera` 用 `ls` 可见 **48 个真实 jpg**，而 Rust 原生 `read_dir` 报 **0 items**——验证仓库文档「HostFsProvider 仅限 root/Waydroid 部署，普通 App 在 Android 10+ 被 scoped storage 禁止裸 `read_dir`，须走 MediaStore/Kotlin glue」在真实 Android 14 上成立。设备临时文件已清理。诚实标注：这正是「非 root 零售机用 MediaStore 路径（media_* bridge / AndroidMediaStoreProvider）」的理由，hostfs 是其对照反例。

- **接入手机就绪自检 example + runbook（`amos-clipboard`，2026-09-08）**：为「设备接入后全面测试」备好交付物。(1) 新增可交叉编译的 headless 自检 `examples/on_device_selfcheck.rs`：无 `Context` 依赖，遍历 crate 纯逻辑——agent 宿主推送→guest 应用且不回显 / 真实复制上报 / 协议 encode↔decode / echo guard / `link::Backoff` 指数+封顶+放弃 / min_delay 地板；host 运行 **8/8 全过**、打印 `AMOS_CLIPBOARD_SELFCHECK_OK`。已交叉编译出 `aarch64-linux-android` 二进制（`cargo build -p amos-clipboard --example on_device_selfcheck --target aarch64-linux-android`），可 `adb push` 到 `/data/local/tmp` 在真机 Android userspace 跑。(2) 新增 `docs/clipboard-device-runbook.md`：三步判定设备形态（零售 / no-UI 基座 / Waydroid）+ 每种形态可测范围与诚实标注 + `adb push` 自检命令 + 全部门禁基线；README 文档索引加指向。诚实边界：本 macOS 无 Waydroid；`adb devices` 当前为空（待 USB 调试/RSA 授权），真机运行尚未执行——二进制与步骤已就绪。
- **真机自检通过（`amos-clipboard`，2026-09-08）**：用 Android SDK 的 `platform-tools/adb`（homebrew 的 adb 看不到设备）连接零售 Android `S5`（Android 14 / API 34，`arm64-v8a`，shell 用户，非 AmOS 基座）；`adb push` `on_device_selfcheck` 到 `/data/local/tmp` 并运行，**真机 Android userspace 8/8 全过**，打印 `AMOS_CLIPBOARD_SELFCHECK_OK`（exit 0）——证明该 crate 能交叉编译并真正运行于 Android userspace。运行后清理设备临时文件。设备侧带 `Context` 的 `AndroidClipboardProvider`/Kotlin bridge 与 Waydroid 容器互通（P2b/P3）在零售机上仍无法验证（诚实标注）。**补充：release 优化构建（429 KB）亦真机 push 并连跑两遍均 `AMOS_CLIPBOARD_SELFCHECK_OK`（确定性确认），随后清理设备临时文件。**
- **guest Kotlin bridge 补全（`amos-clipboard` android-glue，2026-09-08）**：续审计发现 guest 代理只有 native 半边（`src/android.rs`），配对 Kotlin bridge（持 `Context`+`ClipboardManager`、调 JNI upcall）在仓库缺失——宿主 System UI 有 `ClipboardGlue.kt` 而 guest 侧没有。补 `crates/amos-clipboard/android-glue/com/amos/ai/glue/AndroidClipboardGlue.kt`：实现 `ClipboardManager.OnPrimaryClipChangedListener`、`bind(context)` 注册监听并 `attach(this)`；作为 native provider 的 bridge 暴露 `primaryText():String?` / `pushText(String)`；`onPrimaryClipChanged`→`onClipboardChanged(text)` 回灌；echo 抑制交由 agent 的 `EchoGuard`（glue 不需再设窗口）。JNI 符号与 `android.rs` upcall 完全对齐（`com.amos.ai.glue.AndroidClipboardGlue_attach/_onClipboardChanged`）。同步 `android.rs` 模块文档 + `docs/clipboard-container-sync.md` §4/§9 指向。诚实边界：此为设备 bring-up 模板（同宿主 `ClipboardGlue.kt` 体例，不在 CI 编译）；运行时需真机打包 `libamos_clipboard.so` 后绑定验证。
- **`provider.rs` Mock 可重入加固（`amos-clipboard`，2026-09-08）**：续审计发现 `MockClipboardProvider::set_primary_text` 在释放锁后**同步**调用监听器；若某监听器在回调里再 `set_primary_text`（真平台是异步、内联不会递归），Mock 会**无限递归 → 栈溢出**。修复：`MockInner` 增 `firing` 重入标志——每次分发至多**一次同步回调**；回调内重入的 set 只更新值+计数、不再嵌套分发（对齐真异步 provider）；最外层分发结束必复位 `firing`。新增回归测试 `mock_reentrant_set_does_not_recursively_overflow`（重入仅 1 次分发、nested 值被记录、set_calls==2、外层后 firing 复位可再正常分发）。验证：`cargo test -p amos-clipboard` **49/49**（+1）、fmt/clippy(all-targets 默认 & android) 干净。
- **clipboard 栈自审计补全（`amos-clipboard` + `amos-tauri`，2026-09-08）**：对最新添加的剪贴板代码做聚焦审计并修两处真问题。(1) `agent.rs` 中 `should_report` 与 `attach` 重复实现同一「是否自回声」判定（漂移风险）——抽出单点 `is_own_echo(guard,text)`（fail-safe：poisoned 锁按自回声抑制），两侧共用。(2) **apply/写失败遗留陈旧 echo note**：`GuestAgent::apply` 与宿主 `GuestSink::push_out` 为压制同步监听器都在写**前**武装 `EchoGuard`，但写失败时 note 未回滚——之后窗口内同文本的**真实**复制会被误判为自回声而丢弃。给 `EchoGuard` 增 `snap`/`restore`，写失败即回滚。另补测试：`agent` 失败路径（新增 `RejectingClipboard` provider：错误透传、失败不留 stale note、成功才武装）、宿主 `FailingWriter` 失败不留 stale note、`proto` 解码器长串帧不丢/不乱序 + 近上限负载往返。验证：`cargo test -p amos-clipboard` **48/48**（+5）、`amos-tauri --lib` **144/144**（+1）、`--test clipboard_e2e` **4/4**；fmt/clippy(all-targets 默认 & android) 干净。
- **`alarmCore.ts` 纯逻辑补测（frontend-ts，2026-09-08）**：审计（gate DA 口径）`src/lib/alarmCore.ts`（OS 级「到期闹钟」提醒层）仅 13.8% 覆盖、无专属测试。新增 `__tests__/alarmCore.test.ts`（13 项，纯逻辑无 DOM/无 store）：`dateKey`/`firedKey`/`normalizeFired`（坏档/数组/非日期串丢弃）；`collectDueRings`（含「当前分钟+enabled+未响+当日未提醒」判定、排除 disabled/已在响/今日已提醒、repeat 白名单命中当日、空 repeat=每天）；`markRung`/`pruneRung`（记录当日、按日/按存活性清理）；`ringsToNotifs`（Clock app id、label 有无→标题/正文、图标兜底 🔔、超长标签截断 60+…）；`nextArmments`（enabled 重新武装下个时刻、disabled 跳过）。验证：`make cov` 覆盖率 **90.82%→92.01%**（5024/5460），`P2-1 gate passed`；`bun run typecheck` clean；`bun run test` 全绿（exit 0，`[bun-iso] test OK`）。
- **前端 `clipboard.ts` 桥接成功路径补测（frontend-ts，2026-09-08）**：审计（gate DA 口径）显示前端 AmOS 全局剪贴板模型 `src/lib/clipboard.ts` 的桥接包装未覆盖——既有测试只测纯 helper 与「未 bridged→false」。给 `__tests__/clipboard.test.ts` 增补 `clipboard bridge wrappers` describe：带窗口桥接桩验证 `clipboardWrite`（有无 source 两形）/`clipboardRead`/`clipboardHistory`（省略 vs 传参的 args 形状）/`clipboardClear`（返回移除计数）路由正确；`onClipboardChanged` 订阅到 `clipboard-changed` 且把元数据 notice 原样转发；`copySelection` 在有桥时返回 true。验证：`make cov` 覆盖率 **90.71%→90.82%**（4959/5460），`P2-1 gate passed`；`bun run typecheck` clean；`bun run test` 全绿（0 fail）。
- **前端 `make cov` 覆盖率门禁补到 ≥90%（frontend-ts，2026-09-08）**：审计发现 P2-1 门禁 `src/lib` 覆盖率仅 **89.15% < 90%**（4870/5463，76 文件）而失败——`backend.ts`（717 行）只 ~22%，大量未覆盖薄 invoke 包装。给 `__tests__/backend.test.ts` 增补一段「薄包装路由到 daemon 命令」测试，覆盖 mail* / appstore_* / store_snapshot / notes_export_txt / scheduler_alarm_* 及 telephony_* / radio_* / flashlight_* / term_*（含 mailSend cc 有无两分支、telephonyDial 普通/紧急、realDial 非字符串→false 路径）。验证：`make cov` 覆盖率 **89.15%→90.71%**（4953/5460），`P2-1 gate passed` / `[bun-iso] coverage OK`；`bun run typecheck` clean；`bun run test` 全绿（`[bun-iso] test OK`，0 fail）。至此 `make test` / `make lint` / `make cov` 三个仓库具名门禁全绿。
- **`link::supervise` 防 flapping 加固（`amos-clipboard`，2026-09-08）**：审计发现「可达但 flapping 的 peer（打开→发一点→即关）」在有用连接后 `reset` 并**零延迟**重连会空转热循环（零字节守卫不覆盖『少量字节即关』）。修复：`Backoff` 增 `min_delay()`（base 地板）；`supervise` 对**携带字节后干净关闭**的连接 `reset` 后按 `min_delay`（恒非零）重连，对持续 corrupt/IO 错误仍指数退避。新增单测：`min_delay` 独立、`flapping_reachable_peer_is_throttled_to_floor_not_zero`（回归）。验证：`cargo test -p amos-clipboard` **43/43**（+2）、clippy(all-targets 默认 & android)/fmt 干净。
- **terminal.rs P0-1 债务清理 + 全量门禁转绿（`amos-tauri`，2026-09-08）**：审计发现 `src/terminal.rs` 生产代码含 9 处 `Mutex::lock().unwrap()`（违反 P0-1，`clippy -D warnings` 全红）。改为非 panic 加锁：新增 `registry_guard()`（`lock().unwrap_or_else(|p| p.into_inner())`，注释说明本 crate 不在持锁下 panic，故 poisoning 不可达、恢复即一致）；`next_id`/读线程 buf 同步改 `unwrap_or_else`。文档 WIRING 段改代码围栏修 doc-overindented；`terminal-pty` feature 下三个尾 `return` 改尾表达式（`cfg(not)` 去除后成函数尾）。另对 `amos-ai/src/server.rs`、`amos-audio/build.rs` 预存格式漂移执行 `cargo fmt --all`（纯空白，`});` 等）。验证：`cargo clippy --workspace --all-targets -- -D warnings` **exit 0**、`cargo fmt --all -- --check` 干净（**`make lint` 转绿**）；`cargo clippy -p amos-tauri --lib --features terminal-pty -D warnings` 干净；terminal 默认单测 **3/3**、真 PTY `real_pty_echo_round_trip`（feature）**通过**；`cargo test -p amos-tauri --lib` **143/143**、`-p amos-clipboard` **41/41**、`--test clipboard_e2e` **4/4**。
- **通道桥离线核心 `amos-clipboard::link`（P2a，2026-09-08）**：宿主↔guest 字节通道桥里**传输无关、可离线全测**的生命周期编排。`link::Backoff`——指数退避重连策略（`base×2^n` 封顶、有限重试预算 fail-safe 死 peer 最终放弃、有用连接 `reset`）；`link::supervise`——阻塞 connect→drain→重连驱动，注入式 `connect`/`on_connected`（每连接重建编解码器，半帧不跨重连存活）/`consume`/`sleep`/`report`；规则：连接失败指数退避、**零字节**连接按失败处理（防 accept-即-drop 空转）、有字节连接 reset 退避后立即重连。另加**内存全双工 e2e** `amos-tauri/tests/clipboard_e2e.rs`：单进程把真实宿主 transport 与真实 guest agent 经字节线接通，验证「宿主复制→guest ClipboardManager 应用无回环」「容器复制→宿主 ingest」「host guard 丢自回显」「双向无损」。验证：`cargo test -p amos-clipboard` 全量 **41/41**（+10 link：Backoff 5 + supervise 5）、`cargo test -p amos-tauri --test clipboard_e2e` **4/4**、`cargo check --features android` ✓、clippy(all-targets 默认 & android)/fmt 干净；`amos-tauri --lib` 保持 **143/143**。设计 `docs/clipboard-container-sync.md` §5/§7（P2a ✅）；可追溯矩阵 `REQ-A28` 更新。诚实边界：P2b 真实 Waydroid socket/vsock 通道、P3 接线/审计/端到端仍待设备验收。
- **guest 侧代理 crate `amos-clipboard`（P1，2026-09-08）**：容器内 headless 剪贴板代理领域内核。`proto`（单源共享协议抽离，宿主经 `amos-tauri clipboard_guest.rs` `pub use`，杜绝两端定义漂移：长度前缀 JSON 分帧 + 流式 `FrameDecoder` + `EchoGuard`）；`provider`（`ClipboardProvider` seam——`primary_text/set_primary_text/set_change_listener`；`MockClipboardProvider` 真值变更才触发监听、同值 no-op 对齐 AOSP；`#[cfg(feature="android")]` `AndroidClipboardProvider` JNI Kotlin-bridge，编译门验证）；`agent`（`GuestAgent<P>`——`apply(PushText)` 镜像进平台剪贴板 + echo 抑制 + 同值 no-op；`attach(out)` 把真实 guest 复制上报为 `ClipboardChanged`，`should_report` fail-safe）。注册为 workspace 成员；`amos-tauri` 依赖之并移除本地重复协议。验证：`cargo test -p amos-clipboard` **31/31**、`cargo check --features android` ✓、clippy(all-targets 默认 & android)/fmt 干净；`amos-tauri --lib` 全量 **143/143**（clipboard_guest 宿主传输 11/11）。设计 `docs/clipboard-container-sync.md` §4/§7（P0+P1 ✅）；可追溯矩阵 `REQ-A28` 更新。诚实边界：`android` provider 仅编译门 + Kotlin bridge 契约；guest 进程托管与真机 A↔微信互拷（P2/P3）待设备验收；`terminal.rs` 既有 fmt/clippy 债务非本次引入。
- **跨系统边界全局剪贴板宿主半 `clipboard_guest.rs`（`amos-tauri`，2026-09-08）**：补形态 A（Waydroid guest 容器，宿主 Webview 与其剪贴板隔离）缺失的宿主↔guest 文本通道的**宿主半**。新模块 `crates/amos-tauri/src/clipboard_guest.rs`——长度前缀(u32 LE) JSON 分帧协议（`HostToGuest::{PushText}`/`GuestToHost::{ClipboardChanged,PushAcked}`）、流式 `FrameDecoder`（分包/粘包/逐字节、零长帧/超限/坏 JSON fail-fast 并复位、缓冲有界防 DoS）、`EchoGuard`（500ms 同文本回显丢弃，把形态 B 只在 Kotlin 的 echo 抑制统一到 Rust 边界，修复制回环）、`GuestSink<W: Write>: ClipboardNative` + `run_ingest<R: Read>`（注入式字节通道，headless 全测）；复用 `mirror_to_native`/`ingest_native_text` 内核 seam。注册 `pub mod clipboard_guest;`（不自动接线——guest 代理/通道为设备侧）。验证：`cargo test -p amos-tauri --lib clipboard_guest` **27/27**、`--lib` 全量 **159/159**、`clipboard_guest.rs` rustfmt/clippy（lib & all-targets）干净。设计/定位判定/guest 代理契约/通道桥方案：`docs/clipboard-container-sync.md`；可追溯矩阵 `REQ-A28`。诚实边界：guest 代理（ClipboardProvider seam）与宿主↔guest 真实 socket/vsock 桥（P1–P3）需真 Waydroid/设备验收，未实现、不伪造；`terminal.rs` 既有 fmt/clippy 债务非本次引入。
- **终端滚动缓冲（scrollback cap，2026-09-08）**：审计发现 TerminalApp 转写无限增长（live 长时间会话会拖垮 DOM）。纯 `terminal.ts` 增 `capLines`（截断到最新 N=1000 行，纯可测）；`TerminalApp` 所有写 `lines` 处（probe/poller 追加/demo 提交/live 提示）统一经 `setLines/appendLines` 走 cap。terminal 纯 **9/9**（+scrollback 截断用例）、terminal.svelte **7/7**、svelte-check 0/0、tsc clean。多会话 UI（新建/切换/关闭 tab、每会话独立 buffer）为下一大块，本轮未并入以避免破坏。全量 pure OK、vitest **55 文件 / 308 例**。
- **真 shell host 集成示例 + decode 光标/进度增强（2026-09-08）**：`examples/term_shell.rs` 走与 TerminalApp 同一套 `term_*` 后端，在 host 跑**真实 `/bin/sh`**：`printf 'echo HI\nexit\n' | cargo run -p amos-tauri --features terminal-pty --example term_shell` 实测回显 `HI-from-host` ✅（真 PTY spawn/write/read/kill 端到端）。`decodeOutput` 改进：区分 CRLF（换行）与**单独 `\r`（回车覆盖行——进度条）**、`ESC[K`/光标按 dropped（行尾 EL 本为 no-op）；`ansi.test.ts` **11/11**（+进度覆盖/CRLF/EL）。验证：前端 pure OK、vitest **55 文件 / 308 例**、tsc/svelte-check 0/0、`cargo check --features terminal-pty` 通过。真机 note：零售 Android 沙盒内应用无 root 时起真实 PTY shell 受限，此 host 示例即真 shell 代码路径的可用验证；设备接入需带 `terminal-pty` 构建 + 系统 shell 权限。
- **TerminalApp live 清理：卸载时 term_kill 会话（2026-09-08）**：审计 live 流式代码发现缺口——离开终端不结束 PTY 会话会泄漏子进程。补 `onDestroy`：live 且会话存在时调用 `term_kill`（无孤儿）。terminal.svelte **7/7**（+卸载触发 term_kill 断言）；全量 pure OK、vitest **55 文件 / 308 例全绿**（+1）、tsc/svelte-check 0/0。
- **TerminalApp live 流式 UI（真 PTY 桥，假桥可无头测，2026-09-08）**：组件加 LIVE 模式——挂载时 `termSpawn` 探测：返回会话 id 则进入真 shell（Enter → `term_write` 发 `cmd\n`；100ms 轮询 `term_read` 输出经 `decodeOutput` 上屏/清屏/保 ANSI 色），否则留在离线 demo（可回退）。假 `term_*` 桥用例验证 spawn→poll→decode：`ESC[2J` 清屏、`\x1b[32mGREEN\x1b[0m` 上屏且带颜色 span。terminal.svelte **6/6**（+live 假桥）。全量 pure OK、vitest **55 文件 / 307 例全绿**（+1）、tsc/svelte-check 0/0。真机接入即：后端带 `terminal-pty` → 自动走 live。
- **终端桥注册 + ANSI 交互解码（2026-09-08）**：`terminal.rs` 5 命令加 `#[tauri::command]` 并注册进 `generate_handler!`（`term_spawn/write/read/kill/resize`，feature 关仍零执行、返回错误）；`backend.ts` 增对应 TS wrapper（`TermOut`/`termSpawn/Write/Read/Kill/Resize`）——真机带 `terminal-pty` 时前端可直接 invoke 真 shell。ANSI 交互：`lib/ansi.ts` 增 `decodeOutput`（CR/LF/CRLF 分行不重复、退格删前字符、`ESC[2J` 全清、保留 SGR 供着色、剥其它 CSI）；`ansi.test.ts` **9/9**（+4 交互用例）。验证：cargo default & terminal-pty 各 terminal:: **3/3**、全量 pure OK、vitest **55 文件 / 306 例**、tsc/svelte-check 0/0。TerminalApp 的 live 流式 UI（轮询 term_read+decodeOutput 上屏）为下一设备集成步（离线无法测）。
- **终端真 PTY 落子 + ANSI 高亮（2026-09-08）**：`src/terminal.rs` 在 `terminal-pty` feature 下实现**真实子进程会话**：`portable-pty` openpty/spawn（工作目录 cwd 的 `/bin/sh`）、读线程泵→环形缓冲、`term_write/read/resize`(TIOCSWINSZ)/`term_kill`(杀子进程+回收)；feature 关仍零执行并干净报错。`cargo test --lib terminal::`：默认 **3/3**、`--features terminal-pty` **3/3**（含真 PTY 跑 `/bin/sh` 回显 `echo PTY_OK` round-trip，host 通过）。前端补 `lib/ansi.ts` 纯 SGR→色 span 解析（bold/30-37/90-97、reset、剥除非 SGR CSI 防控制字符泄漏），`TerminalApp` 渲染时按 span 着色（demo 纯文本不变）；`ansi.test.ts` **5/5**。全量 pure OK、vitest **55 文件 / 306 例全绿**、tsc/svelte-check 0/0。
- **终端 demo 增安全内置命令（date/uname/ver，2026-09-08）**：扩展纯 `lib/terminal.ts`——`date`（注入 now→UTC ISO，可测）、`uname`→amos、`ver`/`version` 为 about 别名；`help` 同步列出。纯 `terminal.test.ts` **8/8**（+date UTC 精确/别名）。全量 pure OK、vitest **55 文件 / 306 例全绿**、tsc/svelte-check 0/0。
- **TerminalApp 体验补全（自动滚动/自动聚焦/新提示行，2026-09-08）**：审计终端前端后完善——每次输出自动 `scrollTop=scrollHeight`（终端始终钉在最新行）、输入框**挂载即聚焦**且**每条命令后自动回焦**（随时可敲）、`#each` 改 index 键避免对象键、空回车也在转写推进一个新提示行。terminal.svelte **5/5**（+输入框开即聚焦/命令后仍聚焦）；全量 pure OK、vitest **55 文件 / 306 例全绿**（+1）、tsc/svelte-check 0/0。
- **终端前端 TerminalApp.svelte（离线安全 demo，2026-09-08）**：软件现含「终端」App（🖥️，注册于 appMeta/appRegistry，分类 utilities，App Library 可打开）。诚实零执行：纯 `lib/terminal.ts` 仅跑内建安全命令集（help/echo/clear/whoami/about/exit），未知/真实 shell 命令一律拒绝并提示"需 terminal-pty 真机构建"，不起任何进程；含 banner、历史(↑/↓、去重、上限)。`TerminalApp.svelte`：滚动回显 + 命令输入 + clear/history。i18n 加 `app.terminal/terminal.demo/terminal.input`（en/zh 各 +3）。计数类测试同步 23→24（appMeta、app-registry EXPECTED 含 terminal）。验证：纯 `terminal.test.ts` **7/7**、terminal.svelte **4/4**、全量 pure OK、vitest **55 文件 / 305 例全绿**（+4）、tsc/svelte-check 0/0、i18n 奇偶一致。真 shell 仍走 device 后端 `terminal-pty`（feature 关默认无执行）。
- **终端 feature 构建验证（2026-09-08）**：`cargo check -p amos-tauri --features terminal-pty` **通过**（真 PTY 分支 + 可选 `portable-pty` 依赖编译绿，`Cargo.lock` 并入）。默认 feature 关仍零执行面。真机 PTY 落子进程（openpty/spawn/读写泵）保持 `TODO(bring-up)`——避免在无法真机验证时引入高风险子进程管理；按 `docs/terminal-design.md` 第 7 节在带 `--features terminal-pty` 的真机/开发环境补全并做集成测试。
- **终端骨架接入构建 + Rust 单测（2026-09-08）**：`src/lib.rs` 加 `pub mod terminal;`，`Cargo.toml` 加可选 feature `terminal-pty = ["dep:portable-pty"]` 与 optional `portable-pty`。模块默认编译（feature 关）且**零执行面**：`cargo check -p amos-tauri` **0 警告通过**；新增 `#[cfg(test)]` 3 例（白名单策略 / 默认关闭时 spawn 被拒 / 未知会话操作干净失败）——`cargo test -p amos-tauri --lib terminal::` **3/3 通过**。真机 feature 开启的 PTY 落子仍为 `TODO(bring-up)`。
- **终端/CLI 设计 + 后端(PTY/沙盒)骨架（2026-09-08）**：审计确认软件目前无通用终端（前端无 Terminal App；Rust 仅在 ai_bridge 跑固定脚本、无 PTY）。产出 `docs/terminal-design.md`（动机/形态/安全模型/数据流/5 个桥命令签名/接线步骤/测试与 DoD）与 `src/terminal.rs` 后端骨架：`TermOut` 信封、会话 token 注册表、`term_spawn/write/read/kill/resize` 5 个 async fn、纯 `allowed()` 白名单。骨架默认关闭（真 PTY 置于 `terminal-pty` feature 后）且**未接入构建**（零运行时执行面、build 不受影响），接线片段在文档/文件头注释；真机 PTY 落子以 TODO 标注。文档+骨架无行为改动 → 无运行时测试，bun/vitest 未变。
- **蓝牙配置页补全（本机名/可被发现/附近设备 demo，2026-09-08）**：审计确认蓝牙此前仅主开关（与 Wi‑Fi 同 RadioPage），无本机配置。补纯 `lib/bluetooth.ts`：`BtCfg{name,discoverable}` + `btInit/setDiscoverable/renameDevice/normalizeBt` + `btGlyph`/确定性 `DEMO_DEVICES`（诚实 demo，真配对/音频需 Android 蓝牙 📱）。`RadioPage` 蓝牙分支（开启且非飞行）加 iOS 风格分组：本机「名称」行 +「可被发现」开关（落库 `amos.bluetooth`）+「附近设备」demo 列表（图标+未配对），附诚实注记。i18n 加 `settings.btDeviceName/btDiscoverable/btSimNote/btNearby/btNotPaired`（en/zh 各 +5）。验证：纯 `bluetooth.test.ts` **6/6**、settings.svelte **14/14**（+开启蓝牙→名称/可被发现/AirPods Pro→切可被发现落库 false）、全量 pure OK、vitest **54 文件 / 301 例全绿**（+1）、i18n 奇偶一致、tsc/svelte-check 0/0。
- **Wi‑Fi 记住密码 + 下次自动连接/复用（2026-09-08）**：iOS「记住密码、下次自动连」落地。纯 `lib/wifi.ts`：`WifiCfg` 增 `passwords`(模拟 Keychain)，`rememberPassword`/`hasPassword`/`connectWithPassword`（输密码即连并记住）、`connectOpenOrSaved`（开放或已有密码的网络可即连）、`autoRejoin`（空闲时自动回连最强「已保存且可连」网络）、`forgetNetwork` 同时**清除密码**、`normalizeWifi` 解析/过滤密码。`RadioPage`：加密网络显示「需密码」→点击弹密码输入（Enter 连接/连接/取消），记住后下次直接自动连接；`$effect` 自动回连已存网络。i18n 加 `settings.wifiNeedsPw/wifiPwFor/wifiPassword/wifiCancel/wifiConnect`（en/zh 各 +5）。验证：纯 `wifi.test.ts` **18/18**、settings.svelte **13/13**（+加密弹窗→输密码→连接+密码落库→忘开放网→**自动回连 AmOS-5G**→再忘→全清断连）、全量 pure OK、vitest **54 文件 / 300 例全绿**、tsc/svelte-check 0/0。
- **Wi‑Fi 已保存网络排序（我的网络，2026-09-08）**：审计记住/忘记代码后对位完善——`sortNetworks` 增可选 `saved`：排序 = 当前网络 > 已保存(记住)网络 > 其它（同 iOS「我的网络」层级），其内仍按信号降序；`RadioPage` 传入 `cfg.saved`。纯 `wifi.test.ts` **12/12**（+记住网络排在更强未知网络前）；settings.svelte 13/13、全量 pure OK、vitest **54 文件 / 300 例全绿**、tsc/svelte-check 0/0。
- **Wi‑Fi 记住网络 + 忘记此网络（2026-09-08）**：iOS 对位补全。纯 `lib/wifi.ts`：`WifiCfg` 增 `saved:string[]`，`connectOpenOrSaved` 连接开放网络即记住，新增 `forgetNetwork`（清当前+移除已存）/`isSaved`/`normalizeWifi`（解析去重 `saved`）。`RadioPage` 当前网络行下新增「忘记此网络」按钮（危险色），点击断开并移出已存。i18n 加 `settings.wifiForget`（en/zh 各 +1）。验证：纯 `wifi.test.ts` **11/11**（+记住/忘记清当前/只忘非当前/已存 normalize 去重）、settings.svelte **13/13**（加入网→已存+已连接→忘记→current=null+saved=[]且不显示已连接）、全量 pure OK、vitest **54 文件 / 300 例全绿**、tsc/svelte-check 0/0。
- **相册 iOS 智能「视频」过滤（2026-09-08）**：图库顶部过滤扩展为 iOS 风格 All / ♥ / 🎬（视频仅在存在录制时显示，All 恒在、♥ 有收藏时显示）。视频视图只列相机录像（`gallery` 重构图库：视频过滤下不推照片；收藏仍仅照片），切换过滤自动退出选择态，`toggleSelectMode` 在视频视图禁用（视频用自身 ♥）。photos.svelte **9/9**（+种子一条 video capture→🎬(1)→仅视频/无照片→切回 All 恢复）。全量 pure OK、vitest **54 文件 / 300 例全绿**（+1）、tsc/svelte-check 0/0。文档 `photos-ios-parity.md` 相册行更新。
- **相机 iOS 录像中拍照（不中断录制，2026-09-08）**：补 iOS 相机「边录像边拍照」交互。`CameraApp.svelte`：Video 录制态显示独立「📷」按钮（`camera.photoWhileRec`），点击 `grabStill`→复用 `doCapture` 抓帧写进 Photos，**录制不中断**（仅 `mode==="video" && recState==="recording" && live` 时可用）。i18n 加 `camera.photoWhileRec`（en/zh 各 +1）。camera.svelte **12/12**（+录像中拍照→Photos+1 且仍显示"录制中"，再按结束）；全量 pure OK、vitest **54 文件 / 299 例全绿**（+1）、tsc/svelte-check 0/0。文档 `camera-ios-parity.md` 对应行 ✅。
- **Wi‑Fi 设置页：附近网络视图 + 纯模型（2026-09-08）**：审计确认 Wi‑Fi 此前仅 radio 开关，**无扫描/网络列表/连接**（真连接需 Android WifiManager，设备级）。补离线层 + 诚实标注：纯 `lib/wifi.ts`（`WifiNet`/`WifiCfg` + `clampSignal`/`signalBars`/`sortNetworks`/`connectOpenOrSaved`/`normalizeWifi`/确定性 `NEIGHBORHOOD`）——仅开放网络或已连接网络可"加入"，**从不伪造需密码的连接**。`settings/RadioPage.svelte` Wi‑Fi 分支加 iOS 风格「当前网络(SSID/已连接✓) + 附近网络(信号条/🔒/扫描)」列表，附"演示列表——真机扫描需系统无线能力"诚实注记。i18n 加 `settings.wifiNearby/wifiScan/wifiSimNote/wifiConnected`（en/zh 各 +4 奇偶一致）。验证：纯 `wifi.test.ts` **9 例**、settings.svelte **13/13**（+邻居列表/开放可连/加密禁用/连上=已连接+落库）、全量 pure OK、vitest **54 文件 / 298 例全绿**（+1）、tsc/svelte-check 0/0。
- **秒表纯逻辑补测（2026-09-08）**：审计发现 `lib/time.ts` 秒表状态机/格式/计圈**此前无任何单测**（时钟逻辑全凭 UI 手动）。补 `timeStopwatch.test.ts` **8 例**锁定 iOS 语义：start/tick 累计、暂停冻结（暂停后 tick 为 no-op）、暂停后续走连续性（暂停间隙不计入）、系统时钟回退时 elapsed 钳 ≥0、reset 复原；`fmtStopwatch` mm:ss.cc 补零与钳制；`lapDeltas`/`fastestLap`/`slowestLap`（iOS ★/● 标定）。全量 pure OK、vitest **54 文件 / 296 例全绿**、tsc/svelte-check clean。审计状态固化件 `docs/ios-parity-audit-status.md`。
- **相机 iOS 点按对焦 + AE/AF 锁定（Phase-2，2026-09-08）**：补 iOS 相机标志性交互。纯 `lib/camera.ts` 新增对焦模型 `FocusPoint/AfState` + `clamp01/focusFromRect/afInit/afTap/afCaption`（单击定 AF 点→同点再点锁定 AE/AF→他处/再点解锁重定，均防抖/越界钳制）。`CameraApp.svelte` 把点按接入**既有 pointer 手势机**（单指快速稳定 pointerDown/Up 判定为点按，拖移/多指排除，避免与捏合/双击变焦冲突），live 下经 `track.applyConstraints({pointsOfInterest})` 尽力落硬件（失败仅 UI 指示）；翻转镜头复位对焦。取景器显示 iOS 式黄框指示（锁定转琥珀 + `AE/AF LOCKED`）。验证：camera-lib **21/21**（+4 对焦）、camera.svelte **10/10**（+pointer 点按→AF 框→二次锁定）、全量 pure OK、vitest **54 文件 / 295 例全绿**（+1）、tsc/svelte-check **0/0**（pointer 承载避免 a11y 误报）。
- **相机 iOS 对齐审计 + 控制环不变量测试（Phase-1，2026-09-08）**：审计确认相机已接近 iOS 满配（镜头翻转+镜像、实时取景、比例/变焦/网格/闪光/自拍计时/连拍、拍照存 Photos、照片/视频滑切、录像→库回放、画质档、HDR 诚实占位），且 `camera.svelte.test.ts`(9)+`camera-lib.test.ts` 已覆盖离线路/实时/录像全链路。产出差距矩阵 `docs/camera-ios-parity.md`（每项验收语义 + `📱` 设备级归属）。补强：把 UI 依赖的 **iOS 控制环不变量**用真实顺序纯测钉死（此前只测了泛型 `cycleAfter`）——闪光 auto→on→off、镜头 back↔front、比例 4:3→方→16:9、自拍计时 0→3→10、变焦步进/界限；防将来误改顺序静默改环。camera-lib **17/17**（+5）；全量 pure OK、vitest **54 文件 / 294 例全绿**、tsc/svelte-check clean。剩余真缺口(夜间/人像/ProRAW/光学变焦/点按对焦 AE-AF)均标 `📱`。

### Fixed
- **计算器：按运算符后主显示显示该运算符而非暂存 `0`（2026-09-09）**：审计定位——reducer 按运算符后 `cur` 暂存为 `0` 是“等下一操作数”的正确内部态，但 UI 主显示区直接取 `st.cur`，于是按 `+` 后大屏出现 `0`（`calcDisplay` 本就隐藏该暂存 0）。给 `CalcState` 加 `justOp` 标志：按运算符置 true、键入操作数/`.`/`%`/`=`/清空置 false；新增纯 `calcPendingOperator(st)` 返回“等待第二操作数的运算符字形”；`CalculatorApp` 主显示在 `justOp` 时显示运算符（如 `+`）而非 `0`，键入第二操作数即切换为数字、`=` 出结果。**第二操作数真为 0**（如 `2+0=`）仍正确显示 `0`。测试：纯 `calculator.test.ts` 扩至 **26 例**（新增：按 `+` 后 `calcPendingOperator`==`+`、`cur` 仍暂存 `0`；输入 `3` 后清除并 `=5`）；Svelte DOM `calculator.test.ts` 扩至 **11 例**（新增：`2`→`+` 显示 `+`、`3` 显示 `3`、`=` 显示 `5`）。验证：纯 26、DOM 11、`tsc`/`svelte-check` 干净。


- **蓝牙页移除 Wi‑Fi 残留「当前网络」行（2026-09-08）**：审计蓝牙新代码发现第一个共用分组在蓝牙页也渲染「当前网络/开」行（该标签属 Wi‑Fi 语义）。改为该行仅 `which==="wifi"` 渲染；蓝牙页只显示主开关 + 下方本机配置。补断言：蓝牙页不含「当前网络」。settings.svelte **14/14**；全量 pure OK、vitest **54 文件 / 301 例全绿**、tsc/svelte-check 0/0。
- **App 宿主滚动：真机可翻看设置/长列表下方内容（2026-09-08）**：审计发现 Shell 的 App 宿主 `<div class="min-h-0 flex-1">` **无滚动容器**且外层 `overflow-hidden`——凡「内容流」类 App（设置/电话/相册/时钟…）超出视口即被裁切，真机上页面滑不动、看不到下方内容。修：宿主加 `overflow-y-auto overscroll-contain`（统一 ScrollView，惠及所有内容流 App）；`SettingsApp.nav` 导航时把滚动容器 `scrollTop=0` 重置到顶（回退 window 滚动，含 SSR 守卫）。补 shell 用例：App 面下存在 `.overflow-y-auto` 且为 `min-h-0` 的有界滚动区。shell **19/19**；全量 vitest **54 文件 / 297 例全绿**（+1）、tsc/svelte-check 0/0。
- **相机手势精化：快速双击不再误触发点按对焦（2026-09-08）**：审计 Phase-2 新代码发现——快速连击（<300ms）会同时触发「点按对焦/AE-AF 锁定」与 `ondblclick` 缩放，与 iOS（双击=缩放、对焦=两次慢点击）不符。修：单指 tap 经 `lastTapMs` 判定——距上次 tap <300ms 视为双击，抑制对焦、交给双击缩放；慢点击（300–500ms）正常聚焦/二次锁定。camera.svelte **11/11**（+双击抑制不锁用例，锁用例加 350ms 间隔）；全量 pure OK、vitest **54 文件 / 296 例全绿**（+1）、tsc/svelte-check 0/0。
- **相册查看器过滤语境修复（2026-09-08）**：审计最新代码发现——查看器翻页/键盘箭头/幻灯片用 `neighborOf(list,…)`（全量 list），♥ 收藏过滤激活时浏览会**跳到非收藏照片**，破坏过滤器语境。改为基于当前集合 `shown`（含查看器计数 `idx` 同步用 `shown`）。非过滤时 `shown===list`，行为不变（无回归）。补 Svelte 用例：预置 1 收藏+1 非收藏，进 ♥ 视图打开收藏断言 prev/next 禁用。photos.svelte **8/8**；全量 vitest **54 文件 / 294 例全绿**（+1）、tsc/svelte-check clean。

### Added
- **相册 iOS 选择态 Phase-2（批量收藏 + 全选，2026-09-08）**：在 iOS 对齐 Phase-1（Days 分组）基础上补齐选择操作。纯 `lib/photos.ts: setFavs(list, ids, on)`（批量设收藏；清除时删除键，保持「非收藏则 fav 缺席」不变量）。`PhotosApp.svelte` 加选择态「收藏所选 ({n})」按钮与「全选/取消全选」切换（尊重当前 ♥ 过滤，`allShownSelected` 判定），操作后退出选择态。i18n 加 `photo.favSelected/photo.selectAll/photo.selectNone`（en/zh 各 +3 奇偶一致）。验证：纯 setFavs 3 例（子集置真/清除/空集合引用不变）、photos.svelte 7 例（+批量收藏→♥(1)、+全选→删除计数=全部→取消清空）、i18n 6、全量 vitest **54 文件 / 293 例全绿**（+2）、tsc/svelte-check clean。文档 `photos-ios-parity.md` 更新对应行 ✅。
- **相册 iOS Days 分组对齐（Phase-1，2026-09-08）**：对照 iOS「照片」审计并产出差距清单 `docs/photos-ios-parity.md`（每条带验收语义/纯测或真机标记）。实现第一个可验证对齐项：图库按本地日 **Days 分组**并显示相对日期头（`今天/昨天/具体日期`）。纯逻辑单源在 `lib/photos.ts`：新增 `dayKey`/`dayIndex`/`dayLabel`/`groupDays`（本地午夜取日、DST 安全、新→旧、同日内保序）；`PhotosApp.svelte` 用派生 `sections` 把网格包成逐节 + 段头（含计数、`role="heading"`），段头经 `t()` 对语言热切。i18n 加 `photo.today/photo.yesterday`（en/zh 奇偶一致）。验证：纯 `photoSections.test.ts` 3 例（key/index/分组/计数/顺序）、photos.svelte 5 例（+段头 + i18n 热切 Today）、全量 vitest **54 文件 / 291 例全绿**（+1）、tsc clean、svelte-check 0/0。

### Fixed
- **计算器 `%` 键语义修复（2026-09-08）**：审计+实证定位 `calcPress` 的 `%` 实现错误——原逻辑总是把当前输入 `/100`，在**挂起运算**上下文给出错误结果（如 `50 + 10 % =` 应为 `55` 实为 `5.1`；`50 × 10 % =` 应为 `250` 实为 `5`；`100 ÷ 4 % =` 应为 `25` 实为 `2500`）。修复为 iOS 语义：有挂起二元运算时，把右操作数变成**左操作数的百分比**（`left × cur / 100`，经 `normalize`+`evalExpr` 解析负/小数），无挂起（独立或 `=` 后）仍为 `/100`。`±` 实证正常未改。测试：纯层拆分为 2 例（独立 `/100` 含 `=` 后；iOS 挂起四则：`+`→55、`×`→250、`÷`→25、`−`→180），Svelte DOM 补 1 例（`50 + 10 % =`→55）。验证：纯 25、Svelte calculator 10/10、全量 vitest **54 文件 / 290 例全绿**（+1）、tsc clean。

### Added
- **电话外拨审计 + Svelte 层外拨补测（2026-09-08）**：审计 `PhoneApp.svelte` 去电链路（`startCall→telephonyDial→回铃→onTelephonyEvent(Active/Ended)→recordOutgoing 去电写日志/通知`）——逻辑完整且已有纯层镜像 `useOutgoingCalls`（20 例）+ calllog/callTone/duration + `phone.svelte.test.ts`（3 例离线 UI，含"无 daemon 拨号→本地化报错"）。补一个此前缺失的 **Svelte 外拨 happy-path**：`svelte-tests/phone-outgoing.svelte.test.ts`（假 telephony 桥）覆盖 拨号→in-call UI(挂断钮)+去电写共享日志 → `Active`⇒"通话中"(时长) → `Ended`⇒复位、日志保留。全量 Svelte vitest **54 文件 / 289 例全绿**（+1）；tsc clean。手机外拨审计结论：功能完整、覆盖充分（剩余 ACTION_CALL/CALL_PHONE 真机路径在 device-bringup 验收）。
- **来电显示完善：未知号码兜底 + 纯逻辑测试（2026-09-08）**：`IncomingCall.svelte` 空/隐藏号码（blocked/unknown）来电不再显示空标题——新增纯逻辑 `lib/callDisplay.ts`：`callerDisplayLabel(peer, contactName, unknownText)`（联系人名 > 号码 > 本地化"未知号码"）、`hasPeerNumber`（非空号码才显示副标题行）；组件用 `contactNameFor` 预解析联系人名 + `callerDisplayLabel`/`hasPeerNumber`。i18n 加 `phone.unknown`（en "Unknown"/zh "未知号码"，en/zh 奇偶一致）。新增 `callDisplay.test.ts` 4 例（联系人优先/号码兜底/纯空白→未知/空白联系人名回退/hasPeerNumber）。验证：svelte-check 0/0、tsc ✓、i18n 6 + callDisplay 4 全绿、bun 全量 OK、incoming-call svelte 4/4、全量 Svelte 288。
- **Glue 源↔gen 审计同步（2026-09-08）**：逐一 diff `android-glue/com/amos/ai/glue` 与 `gen/android/.../glue` 的 12 个共享 .kt——现**全部一致**（早前 CameraGlue/MainActivity.Wiring/MediaStoreGlue 已从 gen 回写源消除漂移）；仅源多 `AlarmGlue.kt`/`AlarmReceiver.kt`（独立闹钟模板，未在本 build 启用，不强拷）。已把此纳入审计结论，避免源过期回退再次发生。
- **真机自检：`media_list camera => 48 items`（不依赖 UI 闭环，2026-09-08）**：在 Kotlin `PermissionWire.ensureAttached` 里 attach 后直接经 `MediaStoreGlue.listCollection("camera")`（即 `media_list camera` 的同一查询）取数并 `Log.i` 计数——设备 logcat 实测 **`AmosMediaWire: self-check media_list(camera) => 48 items`**，即 System UI 的 MediaStore 路径真机返回 DCIM 实拍（早前裸扫 47、期间新增 1 → 48，机制正确/数据真实）。**白屏修复**：旧"bad 进程"缓存 + 过期超大 ABI .so（armv7 219MB 等 staging）致 APK 492MB → `gradlew clean` 后仅 arm64 12MB、**APK 19.5MB**，`adb reboot` 清态后 WebView 渲染（`[amos][dock] mounted`）、attach + self-check 均正常。已把 gen 活版（Wiring/CameraGlue/MediaStoreGlue）**回写**到 `android-glue` 源，避免"源过期回退"隐患。
- **修复既有相机线程崩溃（`CameraGlue/Nv21` 防御式降级，2026-09-08）**：本设备（S5）授予 CAMERA 后 `amos-camera` 线程在 `Nv21` 读 plane buffer 时抛 `IllegalStateException: buffer is inaccessible` 曾致 FATAL。修法（CameraGlue `onImageAvailable`）：帧编码包 `try/catch(Throwable)`，读/编码失败只 `Log.w` **丢帧**并 `finally` 关闭 image——外围 producer 永不崩 System UI（符合其既有注释意图）。同时恢复被过期 `android-glue` 源误覆盖的 gen `CameraGlue.kt` 为可编译自洽版（`startCapture` target 直接用 `r.surface`；`pickSize` 重写无歧义版）。真机复验：**授予 CAMERA + READ_MEDIA_*** 后启动 **无 FATAL**、进程存活（pid 8517）、`AmosMediaWire: media backend attached (MediaStore)` 仍在。**注意**：`android-glue/com/amos/ai/glue/CameraGlue.kt`（源模板）仍为过期/不可编译版（pickSize/Surface 旧写），gen 才是活版本——需相机负责人稍后从 gen 回写源模板。
- **真机运行验收：MediaStore attach 生效（Path B 打通，2026-09-08）**：整包 arm64 debug 构建成功（修磁盘满：删 i686/x86_64 target；`gradlew :app:assembleArmDebug -x rustBuildArmDebug` 用已编 .so 打包）→ 装机 S5 → **manifest 并入媒体权限**（`READ_MEDIA_*`/`READ_EXTERNAL_STORAGE(maxSdk32)`/`WRITE_EXTERNAL_STORAGE(maxSdk28)`，此前 pm grant 报"未请求权限"即此因）→ 授予媒体权限、**撤销 CAMERA**（绕开与媒体无关的既有 `CameraGlue/Nv21 amos-camera` DirectByteBuffer 崩溃）→ 启动，logcat 见 **`AmosMediaWire: media backend attached (MediaStore)`**，进程存活（pid）、无 FATAL —— `AndroidMediaProvider` 已装入 `DEVICE`，`media_*` 走真机 MediaStore。设备侧数据一致性先前已证（裸扫 47 == MediaStore RELATIVE_PATH=DCIM/Camera 47）。**遗留（需单列）**：本设备授予 CAMERA 会触发既有 `Nv21.copyRowStrided` 直缓冲崩溃（与媒体功能无关）；UI 自动化到相册肉眼核对 47 磁贴未做（需 tap 导航）。
- **MainActivity 接入媒体权限/attach（Path B 代码闭环，2026-09-08）**：`gen/android/app/src/main/java/com/amos/ai/MainActivity.kt` `onStart` 增 `PermissionWire.requestMedia(this)`（CAMERA 的 `requestNeeded` 与 `onRequestPermissionsResult→onResult` 转发本已存在）——授权后 `PermissionWire.onResult` 触发 `MediaStoreGlue.attach()` → Rust 装 `AndroidMediaProvider`。gradle `:app:compileArmDebugKotlin` → **BUILD SUCCESSFUL**。至此 Path B 全链路代码（Rust device-first/JNI attach + Kotlin MediaStoreGlue + PermissionWire + MainActivity hook）**全部写出并编译通过**；剩整包构建 + 装机 + 授权 + `media_list` 设备验收（运行时）。
- **审计修正 `MediaStoreGlue.kt`（2026-09-08）**：逐段对齐 Kotlin↔Rust(`android.rs`/`mapping.rs`) 发现并修 2 处真实缺口：① Rust `available_collections` 广告 `music` 但 Kotlin 无映射（`list("music")` 会报错）→ 补 `music`→`MediaStore.Audio` + `RELATIVE_PATH "Music/"` + kind=audio；② Kotlin 原把 camera/screenshots/pictures 全映射到整个 `MediaStore.Images`（会列出**全部**图片而非仅该目录）→ `listCollection` 现按 `RELATIVE_PATH LIKE <dir>%` 过滤，使 `camera` 仅返回 `DCIM/Camera/`、`download` 仅 `Download/` 等（未知目录不过滤）。已同步 `android-glue/`(源) 与 `gen/android/`(编译) 两份，重跑 `./gradlew :app:compileArmDebugKotlin` → **BUILD SUCCESSFUL**。
- **Path B Kotlin 编译验证通过（2026-09-08）**：把 `android-glue/MediaStoreGlue.kt` 复制进生成工程 `gen/android/app/src/main/java/com/amos/ai/glue/MediaStoreGlue.kt`，用 **JDK17 + `ANDROID_SDK_ROOT` + offline** 跑 `./gradlew :app:compileArmDebugKotlin` → **BUILD SUCCESSFUL**（35 任务，31 executed；仅既有文件的弃用/未用警告，MediaStoreGlue.kt 无错误）——Kotlin 实装通过 Android 工程类型检查。剩余 Path B：attach 接线 + REQ_MEDIA 授权 + 整包安装 + 设备 list/save/load 验收。
- **`MediaStoreGlue.kt` Kotlin 完整实装（2026-09-08）**：把 `crates/amos-tauri/android-glue/MediaStoreGlue.kt` 从桩写成完整可审阅实装：`MediaPermissions`（`readPermissionSet` 按 API 33+ 细粒度 / ≤32 走 `READ_EXTERNAL_STORAGE`、`hasReadAccess`、`request`=REQ_MEDIA）+ `listCollection`（`ContentResolver.query`，Images/Audio/Video/Downloads，列 _ID/DISPLAY_NAME/RELATIVE_PATH/MIME/SIZE/DATE_ADDED，产出 serde 形状 JSON）+ `saveCollection`（MediaStore `insert`+`RELATIVE_PATH`+`IS_PENDING`→`openOutputStream` 写字节→清 `IS_PENDING`，API≥29 免权限；API<29 走 error 明确不接线）+ `loadContent`（`openInputStream`→base64）。文件头写明 JSON 契约、映射对齐（`mapping.rs`）与 **COMPILE/VERIFY 路径**（并入 `gen/android` 后由 `cargo tauri android` / `./gradlew :app:compileDebugKotlin` 完成 Kotlin 类型检查；Rust 侧 `cargo check -p amos-media --features android` 验 JNI 契约）。验证：`cargo check -p amos-media --features android` ✓（方法名/签名与 Rust 下行一致）。诚实边界：本机无 `kotlinc`、未做整包重装，Kotlin 编译需并入生成工程跑 gradle（文档已给路径）；真实 list/save/load 需设备授权后经 System UI `media_*` 验收。`docs/device-bringup-checklist.md` §B 与状态表已更新（B 行：Kotlin 实装就绪·待并入/编译/设备）。
- **设备实测 `scan_dir`：裸扫 DCIM 47 项 / 7.68ms ✅（2026-09-08，S5 SDK34 arm64）**：真机（`adb -s YY000286`）以 **`shell` 非 root** 运行我们交叉编译的 `scan_dir`（本仓库直连 NDK clang 方式，非 cargo-ndk），扫 `/storage/emulated/0` 的 `DCIM/Camera`+`Download` 共 **47 项 → 7.68ms**，JSON 正确含真实绝对路径 `uri`/`size_bytes`/`ts`(mtime) —— **低于架构师 10ms 目标，裸 `read_dir` 通路验证通过**（`HostFsProvider` + `scan_dir` 例程在真机成立）。说明：该 ROM 对 adb `shell` 未收严 scoped storage，故无需 root；App 进程路径仍走 MediaStore（Path B，待 Kotlin glue）。`docs/device-bringup-checklist.md` §A 已记录实测与可复跑命令（并清理了设备上的临时二进制）。
- **HomeDock：dock 内拖拽发 `move`（常态度可重排 dock，2026-09-08）**：依既定目标撤销"dock 无常态拖拽重排"设计，让 HomeDock 支持把 dock 图标拖到另一 dock 图标上时经 propsChannel `home` 发 `move {drag, over}`（由持有布局的 shell 应用重排）。实现：仅在**底部 dock 条**的磁贴启用 `draggable`，`ondragstart` 记录组件内 `dockDrag`（并尽力 `dataTransfer.setData`，headless 无 dataTransfer 也不丢源）、`ondragend` 清理、`ondragover` `preventDefault`、`ondrop` 在 `drag≠over` 时 `emit("move",…)`；普通点击仍走 `handleTap` 开 app，icon **grid** 仍保持 iOS 式翻页拖拽、无常态重排（同步更新顶部设计注释以说明 dock 为例外）。验收：`svelte-tests/home-dock.svelte.test.ts` 原失败例现过，整文件 **10/10**；**全量 Svelte vitest 53 文件 / 287 例全绿**（此前唯一红即此 dock 拖拽例）。`svelte-check` 0/0、`tsc --noEmit` ✓。诚实边界：HomeDock 是受控叶子，本改动保证**事件上抛**；生产外壳如何消费 `move` 把 dock 布局真正重排属外壳/布局域，需其确认消费即可闭环。
- **`lib/externalFiles.ts` 聚合入口 `buildExternalFileView`（2026-09-08）**：新增 Files 屏"即用"API——`buildExternalFileView(raw)` 把 `media_list` 原始负载一次性 normalize（丢弃不可解析）→ 按集合分组（保首见顺序）→ 组内按名排序 → 汇总 `{groups, fileCount, totalBytes}`；空输入返回空视图。`externalFiles.test.ts` 扩至 **10 例**（新增聚合：normalize 丢弃 null/"garbage"、跨集合分组、组内名字序、计数/总量、空输入）。验证：`bunx tsc --noEmit` ✓、`bun test` 10/10。诚实边界：纯展示层，不 mount FilesApp 大屏。
- **`lib/externalFiles.ts` 扩展：分组/去重/跨源合并/最近（2026-09-08）**：在只读外部文件视图模型上补 4 个纯逻辑——`groupByCollection`（按标准集合分组，保首见顺序）、`dedupeExternal`（按 `uri` 去重留首）、`mergeDeduped`（跨源并集，用于把实时 list 与本地 recent 合并）、`recentExternalFiles`（新→旧取 `max`，未知 mtime 置尾、`max≤0` 空）。`externalFiles.test.ts` 扩至 **9 例**（新增分组顺序/空、uri 去重+并集长度、recent 排序与未知 mtime 置尾）。验证：`bunx tsc --noEmit` ✓、`bun test` 9/9。诚实边界：纯展示层，不 mount FilesApp 大屏；跨源合并假设两源同以 `uri` 标识。
- **Files 只读"外部集合"纯视图模型 `lib/externalFiles.ts`（Phase D-纯 lib，2026-09-08）**：给 Files 屏一个与 `amos.files` store **完全隔离**的只读外部文件视图（`Download`/`Pictures` 等标准集合，来自 `media_*` 桥）——只建模展示元数据（name/kind/uri/sizeBytes/ts/mime），绝不触碰本地 store 的增删改语义；纯逻辑、离线可测。函数：`externalGlyph(kind)`、`formatBytes(n)`（"1.5 MB"/"0 B"/"—"容错）、`toExternalFile(item)`、`normalizeExternalFile(raw)`（无桥缺字段容错、绝不 throw）、`sortExternalFiles(files, name|date|size)`、`filterExternalByName`、`totalExternalBytes`。新增 `externalFiles.test.ts` **6 例**（glyph、字节格式化与退化、item→显示节点、normalize 丢弃不可解析、三种排序、名称过滤 + 总量/未知计 0）。验证：`bunx tsc --noEmit` ✓、`bun test` 6/6。诚实边界：本模块**不 mount FilesApp 大屏**（避免与 store 耦合的高风险），是后续 Files 只读外部区可直接消费的纯展示层。
- **Phase D 屏 mount（最小非破坏）：PhotosApp 只读"原生"磁贴条（2026-09-08）**：`PhotosApp.svelte` 在 `media_*` 桥存在时，经 `mediaList(camera/screenshots)` 取原生媒体，`nativePhotoFromItem` 映射为只读 `NativePhoto` glyph 磁贴，在**本地网格上方**渲染一条横向条（`aria-label="native photos"`，`title=文件名`）；仅 `all` 模式（非 favOnly、非多选）显示。**离线/无桥 → 条隐藏，行为与改动前字节一致**（既有 3 例 photos 测试不变）。新增 photos svelte 测试 `shows a read-only native strip when a media bridge serves stills`：用桩 `window.__TAURI_INTERNALS__` 让 `media_list camera` 返回一条原生图，`vi.waitFor` 断言条与 `title=IMG_native.jpg` 磁贴出现（mount 后即清理桩）。验证：photos svelte **4/4**、`svelte-check` 0/0、`tsc --noEmit` ✓。诚实边界：host 上原生磁贴以 kind glyph 占位（mock 无真实像素）；真实缩略图需设备 `media_load` 渲染（Phase C 真机）。
- **设备 bring-up 执行清单 `docs/device-bringup-checklist.md`（2026-09-08）**：把测试机到手后"照着做"的真机步骤沉淀成文档：路径 A（root + ADB `scan_dir` 裸扫 DCIM 验 10ms，交叉编译/ADB 命令）、路径 B（普通 App 走 MediaStore：Kotlin `MediaStoreGlue` ContentResolver 实装 + `media::android_backend`+`attach` 接线 + REQ_MEDIA 授权）、路径 C（Waydroid 直读 `AMOS_MEDIA_ROOT`→HostFs 端到端）；含逐路径状态速查表、验收点、跑完该跑的测试/门禁命令。`docs/media.md` §6 末尾加指向。诚实边界：A/B 需真机，C 可在 Waydroid/桌面直接跑；本清单只组织既有 seam 的执行步骤，不含新增业务代码。
- **`media_*` 命令层逐命令测试（命令核心抽取，2026-09-08）**：把 8 条 `#[tauri::command]`（`media_provider_name/available_collections/grants/grant_read/grant_write/revoke/list/save/load`）的**函数体抽为同签名 `cmd_*` 核心**，命令改为一行委托——使"每条命令的确切行为 + 错误串映射"可**headless 逐命令单测**（`State` 无法脱离运行 app/tauri test harness 构造）。新增 3 条命令级测试：`command_cores_smoke`（provider_name/available/grants、list 授权返回、save→load 往返、revoke→list 返回含 "not authorized" 的拒绝串、grant_read/write 恢复）、`command_cores_deny_without_any_grant`（无授权时 list/save 均为拒绝串）、`command_core_load_reports_missing_content_for_seeded_items`（seed 无字节→load 拒绝串）。`amos-tauri --lib media::` default 与 `--features android` 均 **11 例**。验证：fmt ✓、clippy `-D warnings`（default lib）✓、android feature 测试跑通 ✓。诚实边界：这是"命令核心=命令本体"的等价单测；真正经 Tauri `invoke` 路由（`generate_handler` 注册、WebView 反序列化）仍需运行 app/真机集成验证（仓库既有 e2e 属此范畴）。
- **逐函数 API 覆盖测试 `tests/api_coverage.rs`（2026-09-08）**：为 `amos-media` **每个公开函数**补显式测试钉住（aerospace"每个函数都有测试"）。分模块覆盖：`spec`（`StandardDir` canonical_path/default_kind/all/tag 全 8 变体、`MediaKind::as_str` 全 5、`MediaItem::new` 有效+拒绝）、`provider`（`MAX_SAVE_BYTES`、Mock 四构造器 + trait name/available/list/save/load + load 诚实 Provider 错）、`manager`（`Grant::read/write`、`MediaManager` new/with_grants/provider_name/available/is_granted/grant/grant_read/grant_write/revoke/grants/list/save/load，含 revoke 后 Unauthorized、fresh 无授权）、`mapping`（read/write/needs_permission/mime_for/relative_path + `API_30/API_33`）、`hostfs`（new + trait 全方法，tempdir）、`error`（`MediaError` 全变体 Display）。私密辅助经其服务的公开 API 覆盖；`android.rs` JNI 面编译验证 + 设备验收。`amos-media` default **45 例**（单元 13 + api_coverage 10 + media_core 22）；`--features android` **46 例**。验证：fmt ✓、clippy `-D warnings`（all-targets，含两个集成测试文件与 example）✓。
- **设备 boot 接线审计加固：SwitchableProvider 并发硬化测试 + android-feature 测试跑通（2026-09-08）**：给新增的可变共享 `SwitchableProvider`/`attach` 补 aerospace 并发测试 `switchable_backend_is_safe_under_concurrent_attach_and_reads`（1 写线程 500 次在 mock↔hostfs 间 `attach` 翻转，3 读线程并发 `list`，断言不 panic、结果自洽属同一集合）；并把 `amos-tauri --lib media::` 在 **`--features android`** 下也跑通（8/8）。`amos-tauri` media 测试 default 与 android 均 **8 例**。验证：fmt ✓、clippy `-D warnings`（default lib）✓、media 测试 android 分支编译运行 ✓。
- **设备 boot 接线：可运行期替换后端 + Android attach 路径（2026-09-08）**：`amos-tauri` `media.rs` 引入委托式 `SwitchableProvider`（内部 `RwLock<Arc<dyn MediaProvider>>`，锁毒化 `into_inner` 恢复），`MediaBridge::boot()`（于 System UI 启动、先于 Kotlin glue attach）先挂默认后端（mock / `AMOS_MEDIA_ROOT`→HostFs），设备 glue 随后经新 `MediaBridge::attach(Arc<dyn MediaProvider>)` 把真后端**原地切换**（无需重建 `MediaManager`，命令层持有同一实例），切换后 `pre_grant` 其可用集合。新增 `#[cfg(feature="android")] media::android_backend(vm, env, glue)`——用 System UI 的 `JavaVM`+Kotlin `MediaStoreGlue` 构造 `AndroidMediaProvider` 并包成 `Arc<dyn MediaProvider>`，供 attach。`amos-tauri` `android` feature 增挂 `amos-media/android`（使 `AndroidMediaProvider` 可用）。测试：`amos-tauri --lib media::` **7 例**（新增 `attach_swaps_the_runtime_backend_without_rebuilding_the_manager`：mock 桥 attach hostfs 后，同一 manager 读出真实文件 list/load）。验证：fmt ✓、clippy `-D warnings`（default 与 `--features android` lib）✓、**`cargo check -p amos-tauri --features android` ✓**（验证 android_backend/feature 接线可编译）。诚实边界：真机需设备层在 MediaStoreGlue attach 后调用 `android_backend`+`attach`（Kotlin `ContentResolver` 实装属设备 bring-up，未变）。
- **横向加固：HostFs 并发测试 + 可追溯矩阵（2026-09-08）**：把 aerospace 并发红线套到最新真实文件后端——`src/hostfs.rs` 增 `hostfs_is_safely_shared_across_threads`（`Arc<HostFsProvider>` 8 线程并发 `list` 同一目录，断言一致且 join 不 panic）。`docs/media.md` 补 **§6 需求→测试追踪矩阵**：把媒体子系统每条能力（标准目录/tag/serde、类型化错误、Mock 确定性、save/load、权限门控、HostFs、并发、禁 unsafe、android 映射、命令层、TS 桥）映射到具体测试文件/例名，作为 aerospace 可追溯性证据；并复述工程红线与诚实边界。`amos-media` default **35 例**（单元 13 + 集成 22）。验证：fmt ✓、clippy `-D warnings`（all-targets）✓。
- **航空航天级质量加固 `amos-media`（2026-09-08）**：(1) **安全 Rust 底线**——`lib.rs` 增 `#![cfg_attr(not(feature="android"), forbid(unsafe_code))]`：默认/host 构建一律禁止 `unsafe`（`unsafe impl Send/Sync` 仅存在于 JNI `android` feature 的 `android.rs`），从编译期杜绝未来误引 UB 面（android check 仍绿，因该 seam 是本仓库既有的 JNI 边界）。验证含默认构建 `forbid` 生效。(2) **并发健壮性**——新增集成测试 `manager_is_safely_shared_across_threads_without_panic`：`Arc<MediaManager>` 共享给 8 线程并发 `list(Camera)`，断言每线程看到完整一致集合且 join 不 panic（覆盖锁毒化/数据竞争不炸）。`amos-media` default **34 例**（单元 12 + 集成 22）。验证：fmt ✓、clippy `-D warnings`（default 与 android all-targets）✓、`--features android` check ✓。
- **审计修复 `lib/media.ts` 错误语义（2026-09-08）**：`call` 原先 try/catch 把**所有**命令错误吞成 `null`，导致 Rust 侧精心设计的"未授权→描述性错误"（用于权限提示）与"无桥离线"无法区分——被拒会伪装成离线/空。修复：**离线（无桥）→ `null`；有桥时真实命令错误如实 reject**（Rust 返回的 `Unauthorized` 等字符串原样上抛），并新增 `hasMediaBridge()` 供 UI 判别。更新 `media_list`/`media_load` 等 JSDoc。新增 `media.test.ts`：`hasMediaBridge` 离/在判定、有桥 `mediaList` 返回数据、**有桥被拒时 reject（绝不假空）**（`media`+`photoLibrary` **17 例**）。验证：`bunx tsc --noEmit` ✓、`bun test` 17/17 绿。
- **审计修复 `HostFsProvider`（2026-09-08）**：对最新 `hostfs.rs` 复核修 3 处。① `list` 原把**任何** `read_dir` 错误当"空集合"（会掩盖权限/IO 故障，对扫描工具不诚实）→ 现仅 `NotFound`=空集合，真实 IO 错（如 base 是文件→`NotADirectory`）如实返回 `Provider`。② `save` 未执行 `MAX_SAVE_BYTES` 上限（与 trait 契约/Mock/MediaStore 不一致）→ 补 `TooLarge` 拒绝且**不落盘**。③ `list` 只跳过目录，会把 symlink/特殊文件误报为媒体 → 改为**仅收常规文件**（`is_file`）。新增测试：oversized save 拒绝且磁盘无文件、IO 错误不伪装空、非常规条目（嵌套子目录）不进列表、`save` 拒绝 `../`/空/含分隔符且不越界（`hostfs` 单元 **6 例**）。验证：fmt ✓、clippy `-D warnings`（all-targets）✓、`amos-media` default **33 例**、`--features android` check ✓、`amos-tauri --lib media::` **6 例**。
- **`media_*` 桥后端运行时选择：AMOS_MEDIA_ROOT → HostFs（Phase B+，2026-09-08）**：`amos-tauri` `MediaBridge::boot()` 支持**真实文件系统后端**——设 `AMOS_MEDIA_ROOT=<dir>` 即以 `HostFsProvider` 为该根装配（Waydroid / root 上下文 / 任意开发目录），否则仍回退确定性 mock（desktop/CI 默认不变）。新增 `MediaBridge::mock()` / `MediaBridge::hostfs(base)`（+`pre_grant` 统一预授权可用集合读写）。这让整条 `media_*` 链（list/save/load + 命令层）在 host 上就能对**真实文件**端到端跑通、可测，无需设备。测试：`amos-tauri --lib media::` **6 例**（新增 hostfs 桥端到端：临时目录建 `DCIM/Camera/IMG_real.jpg`→list 返回真路径/大小→load 读回真实字节）。验证：fmt ✓、clippy `-D warnings`（amos-tauri lib）✓、`cargo test` 绿。诚实边界：`AMOS_MEDIA_ROOT` 指向的必须是 root/Waydroid 可直接读的真实根；普通 Android App 路径仍走 MediaStore（Kotlin glue，未变）。
- **真实目录扫描 `HostFsProvider` + `scan_dir` 例程（Phase E 装置，2026-09-08）**：回应架构师"root 测试机 + ADB 二进制扫 DCIM"建议——它正是我方案里预留的 **裸 `read_dir` 通道**（普通 Android 10+ App 受 scoped storage 限制不能用；root/ADB 独立二进制/Waydroid 合法）。`amos-media` 新增 `src/hostfs.rs` `HostFsProvider`（实现 `MediaProvider`，纯 std）：以 base（如 `/storage/emulated/0`）为根，`list(dir)` 用 `std::fs::read_dir` 读标准集合并把**真实路径(uri)/mtime(ts)/大小(size_bytes)**打包为 `MediaItem`（缺目录=空集合非错、仅文件、新→旧）；`save` 落真实文件（`sanitize_name` **拒绝空/`.`/`..`/含分隔符**，杜绝穿越）；`load` 读回字节；`available_collections`=存在即服务。新增 **`examples/scan_dir.rs`**：`HostFsProvider`+`MediaManager` 列集合并 `serde_json` 输出——host 可跑（`cargo run --example scan_dir -- <ROOT> camera download`，实测临时目录 3 文件 ≈215µs 出 JSON），**android 交叉编译 + ADB 推送命令**写在例程头注释（`cargo ndk -t arm64-v8a build … --example scan_dir`）。测试：`amos-media` default **30 例** / `--features android` **31 例**（hostfs 新增 list 真文件 size+mtime/空集合非错、save+load 往返、不安全名拒绝且不越界）。验证：fmt ✓、clippy `-D warnings`（含 example all-targets）✓、`cargo test` 绿、example host 实测扫目录出 JSON ✓。诚实边界：raw `read_dir` 仅 root/ADB 独立进程/Waydroid；普通 App 路径仍走 MediaStore（Kotlin glue）；10ms 指标与真机照片规模待 root 测试机实测（Phase E）。
- **Android MediaStore 真后端骨架（Phase C-device，2026-09-08）**：`crates/amos-media` 加回 `android` feature（`jni`/`base64` 可选、`serde_json` 提为普通依赖），新增 `src/android.rs` `AndroidMediaProvider`（实现 `MediaProvider`）——持有 `JavaVM`+Kotlin `MediaStoreGlue` 全局引用，经 **String↔String JSON JNI 下行**完成 `list`（`listCollection(dirTag)`）/`save`（`saveCollection(dir,name,kind,mime,dataB64)`）/`load`（`loadContent(uri)→{base64|error}`），错误一律映射为显式 `Provider`，绝不伪造。`spec` 增 `StandardDir::tag()`（稳定 wire tag）。`lib.rs` feature 门控导出 `AndroidMediaProvider`。Kotlin 侧新增设备模板 `crates/amos-tauri/android-glue/MediaStoreGlue.kt`（含 `MediaPermissions.request`——**REQ_MEDIA 运行时授权分支**：API 33+ 细粒度 READ_MEDIA_* / ≤32 READ_EXTERNAL_STORAGE，映射对齐 `mapping.rs`；list/save/load 契约桩标注 DEVICE 待接线）。Makefile `gated-check` 增 `cargo check -p amos-media --features android`。测试：default **26 例**；`--features android` **27 例**（+`glue_err` 解析单测）。验证：`cargo check --features android` ✓、clippy `-D warnings`（default 与 android all-targets）✓、fmt ✓。诚实边界：这是 **host 可编译骨架 + 契约**——真机 MediaStore query/insert 需把 Kotlin 桩接上 `ContentResolver` 并在设备授权弹窗后由 System UI 以 `AndroidMediaProvider` 装配 `MediaManager`（boot 接线 + 真机验收，未实现；desktop 仍用 mock）。
- **Android 平台映射知识层 + 媒体权限清单（Phase C-prep，2026-09-08）**：把"设备后端/Kotlin glue 都要用的确定性 Android 知识"编码为纯 Rust、host 可单测。`amos-media` 新增 `src/mapping.rs`：`read_permissions(api)`（API 33+ 细粒度 `READ_MEDIA_IMAGES/VIDEO/AUDIO`；≤32 走 `READ_EXTERNAL_STORAGE`）、`write_permissions(api)`（API 29+ MediaStore insert 免权限；≤28 `WRITE_EXTERNAL_STORAGE`）、`needs_permission(access,api)`、`mime_for(kind)`（image/jpeg、video/mp4、audio/mpeg、download→`application/vnd.android.package-archive`…）、`relative_path(dir)`（`DCIM/Camera` 等，与 MediaStore RELATIVE_PATH 同拼写）；已在 `lib.rs` `pub mod mapping` + crate doc 列出。`crates/amos-tauri/android-glue/AndroidManifest.permissions.xml` 补**媒体权限片段**（READ_MEDIA_* 无条件、READ_EXTERNAL_STORAGE maxSdk 32、WRITE_EXTERNAL_STORAGE maxSdk 28，注释指向 mapping.rs 与 design §7）。测试：`amos-media` **26 例**（新增 mapping 5 例：API 33/≤32 读取分裂、API≥29 写入免权限、needs_permission 读写粒度、mime 映射、relative_path）。验证：fmt ✓、clippy `-D warnings`（amos-media all-targets）✓、`cargo test` 绿。诚实边界：这些是**确定性映射 + 清单声明**（Phase C 的真机 MediaStore 后端 AndroidMediaProvider + Kotlin glue + 运行时 `REQ_MEDIA` 分支仍需真机 bring-up，未实现；权限以 `manifest.permissions` 声明、运行时以 mapping 判定，真正读取需设备授权弹窗后接线）。
- **媒体字节读取 seam `load`（Phase C-host 骨架，2026-09-08）**：给"原生磁贴最终能出图/打开文件"补上最后一环逻辑面。`amos-media` `MediaProvider` 增 `load(&MediaItem)->Result<Vec<u8>>`，Mock 存实际 payload（`save` 时按 `uri` 存 `blobs`，`load` 原样读回）；**诚实边界**：seed/占位项无字节 → 返回描述性 `Provider` 错（"holds no content"），绝不伪造"图片"。`MediaManager::load(item)` 按 item 所属集合做 **read 门控**。`amos-tauri` 增命令 `media_load(item)->Result<Vec<u8>,String>`（`invoke_handler` 注册）。前端 `lib/media.ts` 增 `mediaLoad(item)`（读回字节，无桥/错→null）+ `bytesFromNumbers`（number[]→Uint8Array）。测试：`amos-media` **21 例**（新增 load 原样读回 / seed 无字节诚实 Provider 错 / manager read 门控）、`amos-tauri --lib media::` **5 例**（save→load 往返 + seed load 错误串）、TS **14 例**（bytesFromNumbers 解码、mediaLoad 离线 null）。验证：fmt ✓、clippy `-D warnings`（amos-media all-targets + amos-tauri lib）✓、`cargo test` 绿、`bunx tsc --noEmit` ✓、`bun test` 绿。诚实边界：真机返回真实 `content://` 字节/缩略图需 Android MediaStore Kotlin glue（Phase C 真机部分，未实现）；`media_load` 目前只服务 host mock 的 saved payload。
- **相册原生合并视图模型 `lib/photoLibrary.ts`（Phase D-核心，2026-09-08）**：把 `media_*` 桥返回的原生 `MediaItem`（设备上即 DCIM/Camera 等真实集合）与本地 `amos.photos` 层合一的纯逻辑（design `docs/android-storage-unify.md` §6-D / §G7）。新增：`photoGlyph(kind)`（📷/🎬/🎵/📄/📦 占位脸）、`nativePhotoFromItem`（MediaItem→`NativePhoto`，`NativePhoto extends Photo` 且携带 `kind/name/uri/collection/sizeBytes`，唯一 id=`native:<uri>`）、`mergeMediaGallery(local,native)→{gallery,nativeCount,dropped}`——**按 ts 新→旧**排序、ts 相同时真实本地拍摄>原生>演示渐变图块（稳定）、按 `uri` 去重原生重复项。前端新增 `photoLibrary.test.ts` 6 例：glyph 映射、MediaItem→NativePhoto 形状（fav 不落本地）、本地+原生混排 new→old、uri 去重计数、真实拍摄排原生前、空输入。验证：`bunx tsc --noEmit` ✓、`bun test`（media+photoLibrary）**12/12** 绿。诚实边界：**尚未 mount 进 PhotosApp.svelte**——原生磁贴渲染需真实缩略图字节（设备上 `content://`/MediaStore，Phase C 的 `media_load` 面），host mock 仅有 `mock://` 无像素，故先把合并/排序/去重模型落地并测透；真实设备缩略图接入（Phase D 渲染 + C）后续接。
- **媒体 `media_*` Tauri 命令层 + TS 桥（Phase B，2026-09-08）**：把 `crates/amos-media` 接进 System UI（design `docs/android-storage-unify.md` §6-B）。`amos-tauri` 新增 `src/media.rs`——`MediaBridge`（managed state，持有 `MediaManager`，host 用 `MockMediaProvider::seeded` 装配并把可用集合**预授权读写**以便离线可用；真机将来换 Android MediaStore provider 且授权跟随运行时权限）；命令：`media_provider_name` / `media_available_collections` / `media_grants` / `media_grant_read` / `media_grant_write` / `media_revoke` / `media_list` / `media_save`（`Result<_,String>`，未授权→描述性错误串、UI 映射权限提示，绝不伪装空表），已在 `lib.rs` `pub mod media;`、`.manage(MediaBridge::boot())`、`invoke_handler` 注册；`amos-tauri` 依赖新增 `amos-media`。`Grant` 补 serde（bridge 往返）。前端新增纯 TS `frontend-ts/src/lib/media.ts`：类型对齐 serde snake_case（`StandardDir`/`MediaKind`/`AccessKind`/`MediaItem`/`Grant`）+ `canonicalPath`（DCIM/Camera 等相对路径）+ `normalizeDir/normalizeMediaItem/normalizeGrant`（容忍缺字段、绝不 throw）+ `media*` 命令包装（无桥/命令错误→`null`，UI 显示"外部存储不可用"态）。测试：`amos-media` **18 例**、`amos-tauri --lib media::` **4 例**（boot 预授权/读写往返/未授权错误串/revoke 拦截）、TS `media.test.ts` **6 例**（canonicalPath/normalize/save 参数整形-无桥 null）。验证：`cargo check -p amos-tauri` ✓、clippy `-D warnings`（amos-media all-targets + amos-tauri lib）✓、fmt ✓、`bunx tsc --noEmit` ✓、`bun test` 绿。诚实边界：Svelte Photos/Files 屏**尚未消费** `media_*`（Phase D）；真机 MediaStore 后端（Kotlin glue+权限，Phase C）与 HostFs(root/Waydroid，Phase E）未实现——当前 `media_*` 只服务 host mock，真机需按 design §6-C 至设备验收。
- **媒体/外部存储领域内核 `crates/amos-media`（Phase A，2026-09-08）**：对接标准安卓文件目录的落地第一步（design `docs/android-storage-unify.md` §6-A）。建成与 `amos-sensor`/`amos-radio` 同构的纯 std 领域内核：`spec` 定义安卓标准集合 `StandardDir`（`DCIM/Camera`/`Screenshots`/`Pictures`/`Download`/`Recordings`/`Movies`/`Music`/`Root` + 各自 `canonical_path`）、`MediaKind`（Image/Video/Audio/File/Download）、`AccessKind`（Read/Write）与 `MediaItem`（**opaque `uri`**：真机 `content://` / mock `mock://`，不把字节 base64 进 store），均 serde 可过 Tauri 桥到 UI；`error` `MediaError` 类型化（`Unauthorized{access,collection}`/`NotFound`/`TooLarge`/`InvalidArguments`/`Provider`）；`provider` `MediaProvider` seam（哑读寄存器）+ 确定性 `MockMediaProvider`（`empty`/`from_items`/`seeded`/`single_photo`，`mock-<seq>` 确定性 id、单调 ts）+ `MAX_SAVE_BYTES`(64 MiB) 写上限；`manager` `MediaManager`+`Grant` 权限策略——**默认全无授权**，逐 `(access,collection)` 授权（真机对应 READ_MEDIA_*/MediaStore insert 模型），`list`=读门控、`save`=写门控，未授权返回诚实 `Unauthorized`（**绝不伪装空列表**）、空名单校验先于权限、`grant` 幂等可撤销；Provider 错误（如超上限）在授权后原样透传。已注册进 workspace member + workspace dep。测试：`tests/media_core.rs` 17 例 + error 单元 1 例 = **18 例绿**（spec 路径/serde、Mock 确定性/过滤/空集合/save 校验与上限、manager 默认无授权/读写分离/逐集合授权/幂等撤销/参数先于权限/Provider 错误透传）。验证：`cargo fmt --check` ✓、`clippy -p amos-media --all-targets -- -D warnings` ✓。工程纪律：`deny(clippy::unwrap_used/expect_used/panic)`（生产）、锁毒化用 `into_inner` 不 panic。诚实边界：Android MediaStore 真后端（Kotlin glue + 权限，Phase C）、Tauri `media_*` 命令层（Phase B）、HostFs(root/Waydroid) 与 Svelte 前端对接（Phase D/E）**本轮均未实现**——crate 仅提供 seam，真机行为需按 `docs/android-storage-unify.md` §6-C 至设备验收。
- **自定义分组：对齐 iOS 建组 + 去重/图标 + 主屏 Dock 联动（frontend-ts，2026-09-07）**：新建分组改为 **iOS 式一键默认名**——点「＋」立即建组，默认名「新建文件夹 / New Folder」，已有同名自动编号（…2/3）；建后自动进入**改名并聚焦**输入框（iOS 手感）。`CustomGroup` 增可选 `icon` 与 `lib/customGroups.setGroupIcon`：组视图「图标」调色板选 emoji，组卡空组回退 📁、成员/编辑视图标题显示组图标；**建组/改名大小写不敏感查重**，撞名提示不创建不改名。**一键把组成员加入主屏 Dock（跨壳联动）**：`lib/amosStore.addAppsToDock`（从 page/hidden 移入 dock、去重保序），React 壳 `handleAppLibraryEvent` 与 Svelte 壳 `appLibrary` 通道各自 `dockAdd` → `applyLayout`/`saveLayout`；发送前按 `available` 过滤避免幽灵 id。成员若指向已移除应用，开组/预览用 `knownIds` **核对清理**。资源库加 **CSS-only 入场 fade-up**（移至根容器，避免布局 wrapper）。审计修复：`openCustomGroup/closeCustomGroup` 复位 `renaming/pickingIcon/nameError`，避免 `‹` 退出后再开组误进改名。i18n `appLibrary.newFolder/nameTaken/icon/defaultIcon/toDock`(en/zh)，移除死键 `appLibrary.create`。测试：`custom-groups.svelte.test.ts` 扩到 **18 例**（一键默认名/自动编号/`‹` 后不再误改名回归、撞名拒绝、选图标持久化、setGroupIcon 等），新增 `dock-add.svelte.test.ts`（addAppsToDock 纯逻辑 + 发射 dockAdd）。验证：vitest **277/277**、`svelte-check` 0/0、`tsc` exit 0、bun-iso OK、`vite build` ✓。

- **自定义分组：重命名 + 双端排序 + 误触守卫（frontend-ts，2026-09-07）**：`lib/customGroups.ts` 增 `renameCustomGroup`；组视图成员可排序——鼠标/触控板**拖拽**（`dragstart/over/drop` 用 `moveAppBefore` 前插）或**触屏 ▲/▼ 微调**（`shiftMember` 邻位交换，含 `noUncheckedIndexedAccess` 窄化），均经 `setGroupApps` 持久化；拖放后 400ms 内 `openMember` 抑制杂散 `click` 防误开应用、`dragend` 兜底清拖拽态。组视图操作排增「重命名/排序」，▲▼ 带 `aria-label` 与边界 `disabled`。i18n `appLibrary.renameGroup/save/sort/moveUp/moveDown`(en/zh)。测试：`custom-groups.svelte.test.ts` 扩到 **13 例**。验证：vitest **269/269**、`svelte-check` 0/0、`tsc` exit 0、bun-iso OK、`vite build` ✓。

- **App 资源库收尾（frontend-ts，2026-09-07）**：(a) 自定义分组从「空组」升级为可用文件夹——`CustomGroup` 增加 `apps: string[]`，新增 `lib/customGroups.setGroupApps`；组卡显示前 4 个成员预览（>4 `＋N`，空组显示缺省 📁）；点组进组视图（成员网格可打开应用 / 空态提示），「编辑成员」列出全部可用应用（内置+第三方）逐条 加入/移除 并持久化，「删除分组」移入组内；(b) 各页「返回主屏」统一为 iOS 式**横条「—」**（替换 Svelte 壳应用面/AppLibrary 的 ⌂ 圆形钮，与 React `HomeIndicator` 一致）；(c) 主页 App 资源库入口收敛为 24px 4×4 迷你网格图标、搜索钮加文字「搜索」。i18n `appLibrary.editMembers/emptyMembers/add/remove`(en/zh)。测试：`custom-groups.svelte.test.ts` 扩到 **8 例**（存储增改删、DOM 建组、组内删除、加入/移除成员持久化）。验证：vitest **264/264**、`svelte-check` 0/0、`tsc` exit 0、bun-iso OK、`vite build` ✓。

- **iOS「App 资源库」：分类分组 + 常用工具组 + 实时搜索（frontend-ts，2026-09-07）**：参照 iPhone 主页右侧的 App 资源库，主页图标网格保持不变，新增一个系统面聚合全部应用。**分组**——`src/lib/appGroups.ts`（纯逻辑）：23 个内置应用按 `categoryOf` 归入 通信/媒体/效率与工具/实用工具/系统，第三方（`store:*`）自动落「其他」，隐藏应用不进库。**屏 `src/svelte/AppLibrary.svelte`（runes，受控屏）**：顶部**常用工具**组（recents 动态、dock 兜底）+ 分类小方块文件夹（组名+≤4 迷你图标预览、`＋N` 角标、点开整组网格）+ **iOS 式实时搜索**（跨分组、`<mark>` 高亮、`N 个应用` aria-live 计数、Esc/✕ 清空）；第三方应用亦入分组并可按名搜到。**入口**：主页分页行常驻 2×2 圆点入口 + 末页左滑（React/Svelte 两端 HomeDock 镜像，parity 测试守护）。**架构**：把资源库做成**双壳共享受控屏**——经 `propsChannel("appLibrary")` 下行 `{layout,ext}`、上行 `open(id)/back`，由 **React 生产壳 `App.tsx`**（`svelteEnabled()` 生产为 true；新增 `libOpen` 状态、home `"library"` 事件、`handleAppLibraryEvent`、渲染分支 + surface 日志）与**独立 Svelte `Shell.svelte`**（新增 `library` 面 + 通道喂给/监听）共同托管；`shellState.open()` 现对内置应用记 `pushRecent`。i18n `appLibrary.*`/`group.*`（en/zh）。测试：`app-groups`（纯分组/常用）、`app-library` 13 例（常用/dock 兜底/文件夹/搜索/高亮/Esc/✕/第三方）、`shell-state` 增 `enterLibrary`+open 记 recents、`shell.svelte.test` 增 library 面集成。验证：vitest **67 文件 / 255 例**、`svelte-check` 0/0、`tsc --noEmit` exit 0、bun-iso OK、`vite build` 产出 `AppLibrary-*.js` 懒加载 chunk。诚实边界：常用组仅对内置记 recent（三方不污染）；分组内第三方统一落「其他」而非常规按类；

- **dock「系统监控」App：第 23 个内置应用 + 纯-Svelte 仪表盘（frontend-ts，2026-09-07）**：把散在 Settings 里的系统健康数据提成独立 dock App `id:"monitor"`（名「系统监控 / System Monitor」📈）。**注册全链路**——`lib/appMeta.ts` 元数据单一源（+1，注释 22→23）、`svelte/appRegistry.ts` loader、`apps.tsx` `loadMonitor`+`MonitorEntry`（`SvelteAppHost` 直载，无 React 屏体，契合「退出 React」减法）、`COMPONENTS["monitor"]`；`amosStore.DEFAULT_DOCK` 追加 `monitor`（**新装机默认 dock 可见**）。i18n `app.monitor` + `monitor.*`（总览/CPU/内存/电量/空态文案，en/zh）。**屏 `src/svelte/MonitorApp.svelte`（runes）**：顶部实时总览条——CPU/内存/电量大数字 + 进度条（`role=progressbar`+`aria-valuenow` 可访问），自带 `lib/system` 的 `system_health` 轮询，**自适应节奏**（在线 2.5s 拉取 / 离线 5s 低频探活，daemon 上线自动恢复无需手点）；**空态承接**（未连接系统服务 / 暂无数据 + 重试，整屏不再空白）；下方复用 Settings 同款 `SystemPanel`+`TaskManager` 明细（各自无数据自隐藏，不重复轮询）；`onDestroy` 置 `disposed` 守卫在途请求，卸载后不再写 state。测试：`appMeta` 22→23、`app-registry` +`monitor`、`amosStore` available +`monitor`；新增 `svelte-tests/monitor-app.svelte.test.ts` **5 例**（离线空态 / 桥接无数据 / 总览含 a11y / 手动重试恢复 / fake-timer 探活自动恢复），面板级已由既有 `monitor.svelte.test.ts` 覆盖。验证：`tsc` clean、`svelte-check` 0/0、vitest **64/229**、bun-iso OK、`vite build` 产出 `MonitorApp` 独立 chunk。诚实边界：读数依赖 daemon + 真机 `/proc`（macOS dev 如实 unknown → 显示空态）；「默认上 dock」仅对无持久化布局的首启成立，已存布局设备经 `getLayout` 把新 App 合到主页末页（与既有新增内置 App 一致）。

- **dock「系统监控」收尾：Shell 打开冒烟 + amosLog 埋点 + 真机验收表（frontend-ts/docs，2026-09-07）**：`shell.svelte.test.ts` 新增 monitor dock→app 屏用例（registry 解析 + 标题断言，进 vitest，无需 Chrome）；`MonitorApp.svelte` 补 `[amos][monitor]` 命名空间埋点（`mounted` / `surface=offline|noData|overview` 状态翻转 / `health read failed` warn，`lib/debugLog` 前缀直达 adb logcat），便于真机排查「点了没进屏/进屏没数字/卡空态」；新增 `docs/monitor-dock-verify.md` 真机验收对照表（A1–A10 + 半自动冒烟命令映射 + logcat 排查），README docs 索引挂链。验证：vitest **64/231**、tsc clean、svelte-check 0/0、bun-iso OK、`vite build` ✓。

- **dock「系统监控」React 壳入口冒烟 + 验收表补映射（frontend-ts，2026-09-07）**：新增 `svelte-tests/monitor-react-entry.test.ts`——用既有 React↔Svelte parity 模式在 vitest 下 mount `AppComponent({id:"monitor"})`，验证 **React `COMPONENTS.monitor` → `SvelteAppHost` 动态加载 `.svelte` → 挂载 MonitorApp**（离线空态 + `[amos][monitor]` 埋点照常输出）这条此前无覆盖的共存路径（`app-render`(React SSR) 无法载 `.svelte`，android-parity 走 React 回退）。真机验收表补充该命令。验证：vitest **65/232**。

- **减法期首刀：删除 calculator 的 React 实现（frontend-ts，2026-09-07）**：首个「退出 React」减法——`apps.tsx` 内联的 iOS 计算器 React FC（~166 行 + 其专用 `lib/calculator` import）已整体删除，`CalculatorEntry` 不再有 React 回退，直接 mount Svelte `CalculatorApp.svelte`（与 PROD 唯一实现一致）。同步删除仅喂 React 体的 happy-dom 测试 `calculator-dom.test.tsx` 与已无意义的 React↔Svelte `calculator-parity.test.ts`；覆盖并入 vitest——把原来仅 React DOM 层覆盖的 ± 符号翻转与 AC→C 键标切换两个用例补进 `svelte-tests/calculator.test.ts`（纯逻辑 `src/__tests__/calculator.test.ts` 对共享 reducer 的单测原样保留）。验证：tsc clean、svelte-check 0/0、vitest、bun-iso、`vite build` 全绿，React 主包剔除 calculator 字节。诚实边界：仅在 calculator 的 Svelte 版浏览器全绿后执行；其余屏的 React 体仍保留作回退，待真机逐屏验收后再逐一减法。

- **减法期之二：删除 weather 的 React 实现（frontend-ts，2026-09-07）**：第二个「退出 React」减法——`apps.tsx` 内联的天气 React FC（~106 行 + 其专用 `lib/weather` import）已整体删除，`WeatherEntry` 不再有 React 回退，直接 mount Svelte `WeatherApp.svelte`（与 PROD 唯一实现一致）。同步删除仅喂 React 体的 happy-dom 测试 `weather-dom.test.tsx` 与已无意义的 React↔Svelte `weather-parity.test.ts`；覆盖并入 vitest——把原来仅 React DOM 层显式断言「新增城市落 `amos.weather.cities` store」的持久化用例补进 `svelte-tests/weather.svelte.test.ts`（默认 4 城/℉/增删城/原地 i18n 原已覆盖；纯逻辑 `src/__tests__/weather.test.ts` 对 `lib/weather.ts` 的单测原样保留）。验证：tsc clean、svelte-check 0/0、vitest、bun-iso、`vite build` 全绿，React 主包再剔除 weather 字节。诚实边界：weather 亦为纯离线、浏览器全绿后才减；其余屏的 React 体仍保留作回退。
- **减法期之三：删除 privacy/permissions 的 React 实现（frontend-ts，2026-09-07）**：第三个「退出 React」减法——`components/PermissionsApp.tsx`(React 整文件 ~213 行) 已删除，`PermissionsEntry` 不再有 React 回退，直接 mount Svelte `PermissionsApp.svelte`（与 PROD 唯一实现一致）；`apps.tsx` 移除其 import。删除已无意义的 React↔Svelte `permissions-parity.test.ts`（其离线条目 React==Svelte 行为已由 `svelte-tests/permissions.svelte.test.ts` 的离线 grant/revoke/持久化/无审计断言原样覆盖，无覆盖损失；无 React happy-dom DOM 测试）。Svelte 版与 React 版逐一比对同构（CANDIDATES/CAP_ICON/在线 daemon 审计段/toggle→daemon+ledger 一致）。验证：tsc clean、svelte-check 0/0、vitest、bun-iso、`vite build` 全绿，React 主包再剔除 permissions 字节。诚实边界：permissions 纯离线可用、浏览器全绿后才减；在线 daemon 审计需真机验收（PROD 本就走 Svelte，删除 React 不影响运行行为）。
- **减法期之四：删除 files 的 React 实现（frontend-ts，2026-09-07）**：第四个「退出 React」减法——`components/FilesApp.tsx`(React 整文件 ~478 行) 已删除，`FilesEntry` 不再有 React 回退，直接 mount Svelte `FilesApp.svelte`（与 PROD 唯一实现一致）；`apps.tsx` 移除其 import。核对并确认 Svelte 版功能与 React 版一致（含 multi-select 批删/移动），把原仅 React DOM 覆盖的 multi-select 批删两个用例（逐行勾选批删 / 全选后一键删）补进 `svelte-tests/files.svelte.test.ts`（3→5 例）。删除已无意义的 React↔Svelte `files-parity.test.ts` 与 React happy-dom `files-select.test.tsx`；`app-render.test.tsx`(React SSR smoke) 移除 files 一条与其 import。纯逻辑 `src/__tests__/files.test.ts` 对 `lib/files.ts` 的单测原样保留。验证：先跑 vitest files 套件绿（5/5）再删 React；随后 tsc clean、svelte-check 0/0、vitest、bun-iso、`vite build` 全绿，React 主包再剔除 files 字节。诚实边界：files 纯离线、浏览器全绿后才减；Svelte 版其余行为（rename/move/sort/fav 等）本就双端皆无 DOM 覆盖，仅由纯逻辑单测兜底。







- **主屏 HomeDock 容器迁到 Svelte 5 并接线 + 新增「受控屏」通用基建（frontend-ts，2026-09-06）**：把 home 主屏（小组件 + 4×3 分页图标网格 + 单行底部 dock + 徽标/DND/横滑翻页/拖拽重排/软启动脉冲）由 `components/HomeDock.tsx`(React) 迁到 `src/svelte/HomeDock.svelte`(runes)。新增两件基建：**`lib/appIcon.ts`**（框架无关纯模块，色调/9 组 bespoke SVG 收敛为单一真相源，React `AppIcon.tsx` 与 `svelte/AppIcon.svelte` 同源渲染，产物 CSS 已验证无 Tailwind 裁类）与 **`svelte/propsBus.ts` + `components/SveltePropsHost.tsx`**（通用 React⇄Svelte 受控屏通道：下行 `svelte/store` 原位推 props 不重挂、上行事件回传、卸载 dispose——因 Svelte 5 无 `$setProps`）。`App.tsx` home 分支 `svelteEnabled()? SveltePropsHost : <HomeDock/>`（PROD 走 Svelte / bun·dev 回退 React），壳仍 React。测试：`appIcon.test.ts`、`propsBus.test.ts`、`svelte-props-host.spec.test.ts`、`home-dock.svelte.test.ts`、`home-dock-parity.test.ts` → vitest **164**、svelte-check 0/0、tsc clean、bun-iso OK、`vite build` 产出 `HomeDock` 按需 chunk(gzip ≈3.94 kB)。诚实边界：真机像素/手势需 S5 验收后进入「减法期」删除 React `HomeDock` 体。

- **同一受控屏基建复用于主屏布局编辑 `EditHome`（frontend-ts，2026-09-06）**：把 72 行 React `EditHome` 迁到 `src/svelte/EditHome.svelte`(runes)，复用 `propsBus + SveltePropsHost`（channel "editHome"）——壳仍持有 `layout`，Svelte 编辑时用共享纯 `hideFromHome`/`restoreToHome` 算出下一版 `layout` 并 `emit('change')` 由壳持久化回推、`done` 退出；图标复用 `svelte/AppIcon.svelte`。`App.tsx` editMode 分支 `svelteEnabled()? SveltePropsHost : EditHome`。测试 `edit-home.svelte.test.ts`(5) + `edit-home-parity.test.ts`(3) → vitest **172**、svelte-check 0/0、tsc clean、bun-iso OK、build 产出 `EditHome` 按需 chunk(gzip ≈1.27 kB)。诚实边界：真机验收后进入减法期删除 React `EditHome` 体。
- **Phase-3「退出 React」地基 + 纯-Svelte 顶层 Shell（frontend-ts，2026-09-07）**：建 React-free 三件套——`lib/appMeta.ts`(内置 app 元数据单一真相源，`apps.tsx` 改 re-export 直出，消漂移)、`svelte/appRegistry.ts`(id→.svelte loader，22 屏)、`svelte/shellState.svelte.ts`(surface/浮层/布局 runes 状态)；新增纯-Svelte 顶层 **`Shell.svelte`**(lock/home/edit/app 决策树 + 三浮层 + Banner/Incoming + Esc/Home 键 + HomeDock 拖拽→布局闭环)，并把 HomeDock/EditHome/Recents/Spotlight 的 `appIcon/appTitleKey` 改从 `lib/appMeta` 引入（`src/svelte` 已无 `../apps`）。新增 `focusTrap`(React hook 与 Svelte 共用)与可选纯-Svelte 预览入口 `/shell.html?surface=…`。测试：`appMeta/app-registry/shell-state/shell`(16) 等 → vitest **232**、svelte-check 0/0、tsc clean、bun-iso OK、`smoke:ui` OK。诚实边界：PROD 仍以 React `App` 承载（⑤ 换 main 入口）；真机需 `cargo tauri android dev`(工具链受限)。
- **AI 助手(ai) dock app 迁到 Svelte 5 并接线（frontend-ts，2026-09-06）**：`BackendApps.tsx` 的 React `AiApp` → `src/svelte/AiApp.svelte`；同步移植两个仅 React 的实时语音子组件 `src/svelte/VoiceMicButton.svelte`（tap-to-record→ASR WAV 转写）与 `src/svelte/StreamVoiceButton.svelte`（按住说话流式，`assistant_voice_start/feed/end` + `assistant-voice-event` turn_done）。`apps.tsx` 加 `loadAi`+`AiEntry`，`COMPONENTS["ai"]` PROD 切 Svelte（React 体保留作 bun 回退）。纯逻辑全复用 `lib/backend`(chat/session/history/voice RPC) + `lib/stream`(token/card/session parsers) + `lib/voice`(状态机/ASR 解析/WAV/PCM)。chat_agent 流式 token + 语义 UiCard(天气/音乐/备忘…)落地；会话/历史面板、两段确认清空/复制回答/重发/新建会话/停止接线齐全。离线(无 Tauri 桥)降级：in-browser 提示、发送回显用户行+「未连接守护进程」而非静默、会话空态。新增 `svelte-tests/ai.svelte.test.ts` 6 例 + `voice-buttons.svelte.test.ts` 2 例（离线禁用+离线标题）+ `ai-parity.test.ts` 1 例（React↔Svelte 离线 shell 一致）→ vitest 145。验证：`svelte-check` 0 错 0 警；`tsc` clean；bun `[bun-iso] OK`；`vite build` ✓ 产出 `AiApp` 按需 chunk（gzip ≈6.32 kB）。诚实边界：实时对话/ASR/流式语音需真机 + amos-ai daemon 验收。
- **同传(interpreter) dock app 迁到 Svelte 5 并接线（frontend-ts，2026-09-06）**：`BackendApps.tsx` 的 React 体 → `src/svelte/InterpApp.svelte`（runes），`apps.tsx` 加 `loadInterp`+`InterpEntry`，`COMPONENTS["interpreter"]` PROD 切 Svelte（React 体保留作 bun 回退）。纯逻辑全复用 `lib/interp.ts`（语言目录、语言对+自动朗读偏好、译文记录，写同一共享 store 双端互通）+ `lib/backend.ts` 的 `interpret_start/audio/text/stop/pause/resume` RPC。离线(无 Tauri 桥)自动降级：in-browser 提示、开始/发送给「未连接守护进程」提示而非静默 no-op、偏好/记录照常持久化；在线路径订阅 `interpret-output` → partial/segment_final/utterance/session_ended/error 分类落地 + 自动朗读(`speakText`) + 结束清麦，两段式麦克风授权与 16k 音频喂 `interpret_audio`。修一个 Svelte 陷阱：持久化 `$effect` 只读整个 `prefs` 对象无法感知嵌套 `bind` 修改 → 改逐字段读 `prefs.source/.target/.autospeak` 以正确重触发；语言 `<select>` 用显式 `onchange` 替换对象。新增 `svelte-tests/interp.svelte.test.ts` 6 例（偏好恢复+变更持久化、译文记录渲染/清空/空态 copyAll 禁用、离线开始/发送提示）→ vitest 136。验证：`svelte-check` 0 错 0 警；`tsc` clean；bun `[bun-iso] OK`；`vite build` ✓ 产出 `InterpApp` 按需 chunk（gzip ≈3.74 kB）。诚实边界：实时麦克风/daemon 流需真机 + amos-interp daemon 验收。
- **相机 app 迁到 Svelte 5 并接线（frontend-ts，2026-09-06）**：`components/CameraApp.tsx`(~857 行)→ `src/svelte/CameraApp.svelte`（runes），`apps.tsx` 加 `loadCamera`+`CameraEntry`，`COMPONENTS["camera"]` PROD 切 Svelte（React 体保留作 bun 回退）。纯逻辑全复用 `lib/camera.ts`/`lib/cameraCapture.ts`/`lib/photos.ts`/`lib/permissions.ts`：默认授权即实时取景，翻转/闪光/网格/定时/画质/变焦(点按+滑块+双指捏合)/HDR 占位/连拍/录像库增删播与覆盖层一应俱全。**顺修 React 原版开屏竞态**：feed 就绪晚于 `<video>` 挂载时，Svelte 版以 `$effect` 在元素 bind 后补接 `srcObject`（React 版在空元素上赋流被跳过）。新增 `svelte-tests/camera.svelte.test.ts` 6 例（无摄像头演示壳 + stub `getUserMedia` 的实时快门落 Photos/网格/连拍/画质重取流 + stub `MediaRecorder` 的录像入库）→ vitest 127。验证：`svelte-check` 0 错 0 警；`tsc` clean；bun `[bun-iso] OK`；`vite build` ✓ 产出 `CameraApp` 按需 chunk（gzip ≈5.96 kB）。诚实边界：真实取景像素/手电/录像字节在无摄像头环境不可测，需设备侧验收。
- **备忘录导出/分享为 .txt（frontend-ts + Rust 落盘，2026-09-06）**：跨层实现"单条导出"。前端纯逻辑 `lib/notes.ts` 加 `exportBaseName(now)`（`备忘录-<yyyyMMdd-HHmm>`，仅数字/`-`/中文，文件系统安全）与 `noteExportText(list)`（每条按`标题/时间/正文`成块、`----`分隔、正文原样含任务/#标签/富文本标记）——单测 41 例绿。Rust 新 `note_export.rs`：命令 `notes_export_txt{name,text}` 写 `.txt` 到 `$AMOS_EXPORT_DIR`（缺省 `<tmp>/amos-exports`），含 sanitize（去路径分隔符/防 `..`/保 CJK/不重复加扩展名）与 1MiB 上限；在 `lib.rs` 注册。前端 wrapper `backend.ts::exportTxtFile`（无桥→null），Notes 展开卡底部新增「↧ 导出」按钮 + `role=status` 提示：有后端显示落盘路径，无后端**回退复制到 AmOS 剪贴板**并提示。React(测试)+Svelte(生产)同步，i18n `note.export/exportedTo/exportCopied`(en/zh)。验证：`tsc` clean；svelte-check 0 错 0 警；bun-iso OK；`notes-dom.test.tsx` 9 例（新增导出回退提示）、`notes.svelte.test.ts` 6 例；`vite build` ✓；`cargo test --lib note_export` 5 例绿（sanitize/写盘/扩展名/超大拒绝）。诚实边界：调起**系统分享面板(Android SAF/ACTION_SEND)** 需在真机 Kotlin glue 把 `AMOS_EXPORT_DIR` 指到外部可见目录并接 JNI 下行调用——本机只做到落盘命令 + 回退剪贴板，分享一步待真机联调。

- **备忘录多选/批量操作（frontend-ts，2026-09-06）**：新增批量归档/置顶/删除/恢复/彻底删除，React(测试)+lib+Svelte(生产)三层同步。lib 加纯函数：`setManyState`(按 id 批量改 archived/trash/active)、`setPinned`(批量置顶=置顶组浮顶并 orderPinned，或取消置顶)、`removeMany`(按 id 批量硬删)。UI：列表右上有「选择」进多选，进入后每行变**可选卡(aria-pressed+圆形勾选标记)**、顶部出现已选数与随 tab 变化的批量动作——备忘录[★置顶/归档/删除]、归档[恢复/删除]、最近删除[恢复/彻底删除]，动作按钮未选时 disabled，执行后自动退出多选。i18n 加 `note.select/done`(en/zh)。单测：`notes.test.ts` 增 batch describe（setManyState 归档/恢复/空 ids 原样、setPinned 浮动与取消、removeMany 精确删除）→ 38 例绿；`notes-dom.test.tsx` 增“多选批量归档两条”与“回收站多选批量彻底删除留他人”→ 7 例绿；`notes.svelte.test.ts` 增“多选批量归档两条”→ 5 例绿。验证：`tsc` clean；`svelte-check` 0 错 0 警；bun `[bun-iso] OK`；`npm run test:svelte` 76/76；`vite build` ✓。**复审补全（同日）**：审计发现“切换 tab 时多选状态残留”——selected 跨视图滞留成死选区、动作全 disabled。修复：React tab chip 与 Svelte `switchMode` 在切换视图时**退出多选并清空选中**（多选按 tab 隔离）；并补「全选/全不选」（React `selectAllInView`、Svelte `selectAllInView`，按当前 view 一键选中或反向清空），i18n 加 `note.selectAll`(en/zh)。测试：`notes-dom.test.tsx` 增“全选=已选 n + 切归档 tab 退出多选、出现新选择钮”→ 8 例绿。终态：`tsc` clean；`svelte-check` 0 错 0 警；bun `[bun-iso] OK`；`npm run test:svelte` 76/76；`vite build` ✓。

- **备忘录 #标签 着色 + 按标签筛选（frontend-ts，2026-09-06）**：富文本增强，React 版（供测试）与生产用的 Svelte 版（`NotesApp.svelte`）共用同一 `lib/notes.ts` 逻辑，改动三处同步：① lib：`RichSeg` 新增 `tag` 标记；新增 `tagsOf`（去重、保序、大小写不敏感）、`hasTag`（按完整 token 匹配、非子串）、`filterByTag`；`fmtInline` 第二遍把纯文本里的 `#标签` 切成着色段（不进粗体/高亮/删除线/链接内部）。② UI：all 页在列表筛选下方渲染**标签 chips**（`#名 (条数)`，点选/再点取消），正文里的 `#标签` 以强调色+下划线渲染、可点按直接筛选；置顶/归档等无标签层级无影响，切换归档/回收站会清空标签筛选。③ 加了 `note.clearTag` i18n（en/zh）。单测：`notes.test.ts` 新增 hashtags describe（31 例绿，含 token 边界、CJK、round-trip），`notes-dom.test.tsx` 新增“标签 chips 出现+点选过滤+清除”DOM 用例（4 例绿）。验证：bun `[bun-iso] OK`；vitest 74/74；`tsc` clean；`vite build`（含 Svelte 编译）✓。**复审补全（同日）**：`svelte-check`（`npm run typecheck:svelte`/`check`）揪出 NotesApp 的 1 个类型错误 + 1 个 a11y 警告并修复——`$derived(selTag ? …)` 无法收窄 `string|null`（`selTag` 是可再赋值 state）改为 `$derived.by(() => { const tag = selTag; … })` 收窄；清除标签按钮补 `aria-pressed="true"`；并修复 **React/Svelte 正文点 #标签行为不一致**（Svelte 版漏切回 all，现同步 `mode="all"`+筛选）。测试补齐到 Svelte 生产路径：`svelte-tests/notes.svelte.test.ts` 新增“#标签 chips 出现+点选过滤+✕清除”，`notes.test.ts` 增标点/数字/URL-fragment/`#keep-extra≠#keep`/正文点击等边界用例。终态验证：`svelte-check` 0 错 0 警；`npm run check`(含 Svelte 测试) 75/75；`notes.test.ts` 35、`notes-dom.test.tsx` 5、`notes.svelte.test.ts` 4；`tsc` clean；bun `[bun-iso] OK`；`vite build` ✓。
- **相机默认允许、无确认、直接取景（frontend-ts，2026-09-06）**：需求"打开相机不该再弹权限确认，应默认永远可用并进入拍摄显示画面"。`CameraApp` 彻底移除应用内权限门槛与「允许/拒绝」确认覆盖层：删掉 `perm` 状态机、`allow()`、ask/deny 覆盖 UI 及取景 guard（不再有应用内确认界面），挂载时把相机写入共享授权账本（`grantCap`→`saveLedger`，让其它 CapabilityGate 也视为已授权）；打开即调用 `getUserMedia` 直接进实时取景，可拍照/录像并把照片存 Photos 显示。OS/WebView 首次真实 `getUserMedia` 的授权提示属系统层、无法在前端绕过（拒绝/无设备仍走原有提示+重试）。验证：`tsc` clean；`camera.test.tsx`+`camera-features.test.tsx` 12 例仍绿（含"拒绝→提示+重试"OS 级路径）；bun `[bun-iso] OK`；vitest 74/74；`vite build` ✓。
- **锁屏背景图：可后台添加与配置（frontend-ts，2026-09-06）**：(a) 新增 `Wallpaper.tsx::LockWallpaperCard` 并在 Settings 挂载（与主页壁纸/锁设置同区）——可选内置壁纸、**上传图片**(文件→dataURL)、或贴 URL(仅 http/blob/data:image 安全源)，持久化为 `amos.settings.lockWallpaper`；(b) `LockScreen` 读取该值，设置后用其作**全屏背景图 + 半透明遮罩**（可读性），未设置则维持原有深色毛玻璃外观；(c) 背景图解析复用 `resolveWallpaper/isCustomWallpaper/WALLPAPER_FILES`，dark 取 `<html>.dark`（不依赖 ThemeProvider，避免锁屏单测无 provider 抛错）；(d) i18n en/zh 增 `wp.lockLabel/lock.clear/wp.upload/wp.tooLarge`。审计补全：上传文件为 dataURL 存进共享 `amos.settings`，而 localStorage 配额 ~5MB——加 **≤2.5MB 上限**并在超限时提示（防撑爆/持久化静默失败）。验证：`tsc` clean；bun `[bun-iso] OK`；vitest 74/74；`vite build` ✓。
- **主屏/字号再打磨（frontend-ts，2026-09-06）**：按反馈三改：(a) `src/svelte/*` 应用页小字由 11px 提至 **text-xs(12px)**（扫描后已无 <12px 信息字）；(b) 时钟/天气卡与顶部加 `pt-3` 间距；(c) **左右翻页手势扩到整个主区**（含时钟/天气卡上方也可横滑翻网格页，iOS 主页式）。验证：`tsc` clean；bun `[bun-iso] OK`；vitest 74/74；`vite build` ✓；无头 Chrome 复核：网格 top y≈284(有留白)、在 y=170(卡片区) 左滑 → 网格页0(时钟/设置…)翻到页1(相机/应用商店…)。
- **应用页/主屏字体体检与优化（frontend-ts，2026-09-06）**：体检结论——body 基准 16px、iOS 字体栈已配好；问题主要在**信息型小字**过小与低对比。修：(a) 迁移后各应用页(`src/svelte/*`)最细一档**温和上调**：`text-[9px]→10px`、`text-[10px]→11px`（15×10px、2×9px 收敛，层级保留）；(b) dock/主网格图标标签 `text-[11px]→text-xs(12px)+font-medium`，更清晰。验证：`tsc` clean；vitest 74/74；bun `[bun-iso] OK`；`vite build` ✓；无头 Chrome 复测 dock 渲染正常且标签计算字号 **12px**（截图 /tmp/dock_font.png）。
- **Spotlight 搜索：点击底部🔍 自动聚焦输入框并弹键盘（frontend-ts，2026-09-06）**：审计发现点网格底部 🔍 后输入框**未聚焦**（activeElement 停在按钮，键盘不弹）。修：`SpotlightPanel` 打开时用 ref+effect 经 `requestAnimationFrame` **强制聚焦搜索输入框**（原 `autoFocus` 不够，因点击把焦点留在按钮上）。本地 CDP 验证：点击🔍 → `FOCUS: INPUT「搜索应用…」`（真机即弹软键盘）；输入"时" → 结果实时过滤为**仅「时钟」**。验证：`tsc` clean；bun `[bun-iso] OK`；vitest 74/74；`vite build` ✓。
- **查找入口放到主图标网格底部（frontend-ts，2026-09-06）**：把"搜索/查找"作为 **iOS 风格小圆点🔍放在图标网格底部**（dock 常用栏上方）——非顶部栏，符合苹果主页观感。`HomeDock` 新增可选 `onSearch`，在网格区底部(dock 前)渲染一个居中 🔍 胶囊，点击开 Spotlight；App 主屏传 `onSearch={()=>setSpotOpen(true)}`。本地无头 home 布局 e2e **7/7**：主网格分页、dock 单行含全部常用、顶部无查找栏、网格左滑翻页、底部 🔍(y≈710) 存在、点击弹出 Spotlight 搜索。
- **移除主屏顶部栏，操作并入下拉控制中心（frontend-ts，2026-09-06）**：按需求去掉主屏顶部那条「查找/编辑」栏（`TopBar` 组件删除）。原栏里的 通知🔔/最近⇤/查找🔍/编辑✎/锁定🔒 改收进**下拉弹出的快捷设置(控制中心)**顶部一排小按钮（`NotificationCenter` 新增可选 `onSearch/onRecents/onEdit/onLock`，渲染于标题下方，点击即关 shade 并开对应功能）。主屏顶栏清爽，功能不丢、均经下拉手势可达。验证：`tsc` clean（删除 TopBar 及随之未用 import）；bun 全量 `[bun-iso] OK`；vitest 74/74；`vite build` ✓；本地无头布局 e2e 5/5（主网格分页、dock 单行含全部常用、顶部无查找按钮、网格左滑翻页）。
- **主图标网格横向多页；dock 常用栏改回单行（frontend-ts，2026-09-06）**：按需求把"左右换页"移到**主页面图标网格**而非底部常用栏。(a) `HomeDock` 把图标网格改为**横向分页**（`GRID_PER_PAGE = 4×3 = 12`），网格区左右滑翻页、圆点可点跳页（`data-testid=home-grid/home-dot`）；上方时钟/天气卡与底部 dock 常驻。(b) **底部常用 dock 栏移除横滑分页、改回单行**展示全部常用图标。验证：`dock-paging.test.tsx` 重写为**网格分页 + dock 单行** 2/2；`tsc` clean；HomeDock/dock-drag DOM 仍绿；bun 全量 `[bun-iso] OK`；`vite build` ✓。
- **「唤醒回 dock」真机实测通过（2026-09-06）**：在 S5 上打开电话(拨号页)后按物理电源键息屏 ≥3s，再点亮解 keyguard → App **回到 dock 主页面**（DOCK: 电话/信息/AI助手/同传，分页点 2）。验证该前端策略在真机端到端生效。
- **dock 分页点可点击跳页 + DOM 测试（frontend-ts，2026-09-06）**：dock 分页圆点改为**可点击的跳页按钮**（每点 24px 点击热区，带 `aria-label`/`aria-current`），点击直达对应页；不再是纯展示的 `aria-hidden` span。新增 `__tests__/dock-paging.test.tsx` **2 例**：多页 dock 显示分页点、单页不显示；每页只渲染当前页图标且**点击圆点跳页/跳回**。验证：`tsc` clean；dock-paging 2/2；bun 全量 `[bun-iso] OK`；vitest 74/74；`vite build` ✓。
- **dock 横滑翻页·真机修复（frontend-ts，2026-09-06）**：真机"横滑切 dock 页没实现"（本地无头可用）。审计根因：dock 图标是 `draggable`(HTML5 拖拽重排)，真机触屏上横滑会触发原生 drag 吞掉后续 touchmove，分页器收不到事件。修：(a) 图标改为 **仅精细指针(mouse/trackpad)可拖拽**（`finePointerEnabled()`，`pointer: fine`；无 matchMedia 时回退可拖拽以保桌面/测试重排），触屏图标非 draggable → 横滑不再触发原生 drag；(b) dock 容器加 **`touch-action: pan-y`**，把水平手势交给 JS、垂直滚动仍交给浏览器；(c) 翻页后置短延时自清 sentinel，抑制翻页合成点击误开 App、又不吞下次点按。验证：`tsc` clean；`HomeDock`/`dock-drag` DOM 测试仍绿；bun `[bun-iso] OK`；vitest 74/74；`vite build` ✓；本地无头 CDP 触摸回归：页0(电话/信息/AI/同传)→左滑→页1(邮件)→右滑→页0。
- **来电全屏高优弹层（frontend-ts，2026-09-06）**：真机反馈来电提示太小。把 `components/IncomingCall.tsx` 由「顶部小横幅」改成**全屏、第一优先级(z-[90])来电弹层**，覆盖主屏/App/锁屏之上：Ringing → 全屏渐变深色 + 大来电头像(📞 呼吸)+大字号姓名/号码，底部两个大按钮(✕拒绝红 / 📞接听绿，均带文字)；接听(Active) → 全屏通话页，含 静音🎙️/录音● / 挂断✕ 大按钮。逻辑不动（振铃 AudioContext、来电入 `amos.calllog`、录音、静音、拒绝/挂断）。aria 文案与单测保留。验证：`tsc` clean；`vite build` ✓；`IncomingCall.test.tsx` 3/3（来电→接听可录、拒绝清场、静音切换）；bun 全量 `[bun-iso] OK`；vitest 74/74。**覆盖性验证（真实 Chrome 全 App + 预注入 Tauri 桥桩 + dock 首页注入来电）4/4**：主屏 dock 渲染 → 注入 `telephony-event` Ringing → **全屏来电"来电 📞 138… ✕拒绝 📞接听"盖住 dock**（dock 仍在底层挂载）→ 点接听 → 全屏"通话中…静音/录音"。审计结论：IncomingCall 为全局高优(z-[90])覆盖层、挂于 App 顶层，**在任何界面(含 dock/主屏)都会整屏弹出**；此前"dock 页来电不显示"为真机"来电事件未到达"(daemon 未跑/主屏无模拟入口)，非前端渲染缺陷。
- **屏幕"开"即回 dock 主页面（frontend-ts，2026-09-06）**：需求"屏幕打开时自动显示 dock 页面"。审计：屏幕开关由 `Shell` 的 `lockScreen()`(关→上报 `screen_on=false`) 与 `handleUnlock()`(开→上报 `true`) 统一（顶栏 🔒 与自动息屏共用）；此前 `handleUnlock` 只解锁、不清已打开 App/覆盖层，故在 App 内息屏后唤醒会回到该 App 而非 dock。修：(a) `handleUnlock` 内加 `goHome("unlock")`；(b) 另加 **resume 监听**——页面 hidden→visible（物理电源键唤醒、App 切换回来、Android keyguard 消失）时也 `goHome("resume")` 回 dock（仅真正的隐藏→可见转换触发、首载不触发、不绕过 AmOS 自身 LockScreen）。用 `goHomeRef` 保证总是调最新 `goHome`。补全（同日）：**加可配置开关（默认开）**——`lib/display.ts` 增 `WAKE_HOME_KEY="amos.wakeHome"` + `wakeHomeEnabled`（默认 true）；`Shell` 的 unlock/resume 都经该开关门控（`useStoreValue` 响应式、`wakeHomeRef` 给 mount-once 的 resume 监听用最新值）；**Settings→General 新增「唤醒后回主屏 / Return to home on wake」Switch**（写共享 store，改即时生效）；i18n en/zh 增键；新增 `wakeHomeEnabled` 单测。调优（同日）：原 resume 监听对 `blur/focus` 也触发"回 dock"，会把**非真实息屏**（权限弹窗、通知栏一瞥、短暂失焦）也弹回主屏。现改为**离开时长 ≥ `WAKE_HOME_MIN_MS=1000ms` 才视为真实唤醒**回 dock；纯瞬时 focus 抖动忽略，避免把用户从当前界面弹走。验证：`tsc` clean；`vite build` ✓；bun 全量 `[bun-iso] OK`；vitest 74/74。
- **dock 多页面 + 左右横滑翻页（frontend-ts，2026-09-06）**：dock 每页固定容纳 `DOCK_PER_PAGE=4` 个图标，图标超出即分页；在 dock 栏上**左/右横滑**切换上一页/下一页（水平位移 ≥48px 越阈、fire-once，轻点仍正常打开 App），底部显示分页圆点指示当前页（多于一页才出现）；dock 应用被移除时页码自动回退到有效范围。接线：`components/HomeDock.tsx`（`dockPages` 分块 + `dockPage` state + `onDockStart/Move/End` 横滑）。本地端到端验证（无头 Chrome + CDP 触摸）：默认 5 个 dock 应用分 2 页，页0=电话/信息/AI助手/同传，**左滑→页1=邮件，右滑→回页0**。验证：`tsc` clean；`vite build` ✓；bun 全量 `[bun-iso] OK`；HomeDock/dock-drag DOM 测试仍绿。诚实边界：分页是展示层；把应用加进 dock 仍需已有的编辑/拖拽流程（应用多起来后自然分页）。
- **App 内"底部上推 → 回主屏 dock"（frontend-ts，2026-09-06）**：真机反馈 App 里想上推回 dock 主屏却回不去。审计原因：App 界面底部"上推回家"原先只绑在底部小 HomeIndicator 胶囊上（起点不在胶囊/系统手势区就接不到）。修：`AppShell` 顶部下拉与底部上推都走 `edgeSwipe` 纯逻辑（`edgeAtY`/`pastEdgeThreshold`，fire-once），**从整个顶部/底部边缘带起手、朝手势方向越阈值即 `onBack()` 回主屏**，让 dock 在主屏正常可见；内容区正常手势不受影响（边缘带外不触发）。验证：`tsc --noEmit` clean；`vite build` ✓；bun 全量 `[bun-iso] OK`。诚实边界：App 内容为长滚动列表且贴近底部起手向上滑时可能被当回家手势——若真机出现误触，按需把 App 底部手势收敛到 HomeIndicator 条带内即可。
- **主屏上下沿手势（frontend-ts，2026-09-06，终版交互）**：为 AmOS 定下统一的边缘导航。(a) **顶部下拉** → 打开通知中心/快捷设置（含 🔦 手电筒开/关、✈️ 飞行模式开/关、Wi‑Fi/蓝牙/深色/DND/定位格，均走真实 `radio_*`/`flashlight_*` backend）；(b) **上推** 语义为"回主屏 dock"——在 App 内上推回 dock（见下条），在主屏上推则**不弹任何覆盖面板**，dock 主页面保持可见（Recents 仍可从主屏 ⇤ 顶栏进入）。纯手势判定在 `lib/edgeSwipe.ts`（`edgeAtY`/`pastEdgeThreshold` + `EDGE_TOP_PX=64`/`EDGE_BOTTOM_PX=96`/`EDGE_THRESHOLD_PX=52`），只在细边缘带内起手并朝手势方向越阈值才触发，不与内容滚动冲突。补全（同日）：**快捷设置/Recents/Spotlight 三张 sheet 现渲染在"主屏 + App 内"两层之上**，且 **App 内顶部下拉改为弹快捷设置（手电…）而非关 App**，上推仍回 dock——下拉/上推语义在任何界面一致。验证：`__tests__/edgeSwipe.test.ts` **3 组**；`tsc` clean；bun 全量 `[bun-iso] OK`；`vite build` ✓。
- **App 内"底部上推 → 回主屏 dock"（frontend-ts，2026-09-06）**
- **System UI 真机启动崩溃修复：相机被策略禁用不再崩 boot（`amos-tauri` Kotlin glue，2026-09-06）**：真机 AmOS 一启动即崩（白屏/进程被标 bad、WebView 白）。定位：`MainActivity.onStart → AmosGlue.onStart → CameraGlue.attach → openCamera` 在 **相机被设备策略禁用**（`ServiceSpecificException: Camera "0" disabled by policy`）时同步抛异常，而 attach 只 catch `SecurityException` → onStart 崩溃。修：(a) `CameraGlue.attach` 的 `openCamera` 改 catch `Exception` 并 `detach()` 释放半建资源、`Log.w`（相机禁用/占用/无授权均不再崩 OS UI）；(b) `AmosGlue.onStart` 用 `startQuietly` 逐个隔离 sensor/camera/flashlight 硬件 producer，任一不可用只告警不抛。`gen/android`（编译源）与 `android-glue/`（模板源）两处同步。另排障并记录：正确真机构建命令必须带 `--features android --target aarch64`（漏 feature → `TelephonyGlue.nativeAttach` 符号缺失崩溃；带 feature 单编 arm64 避开 armv7 `-laaudio` 链接失败）。验证：arm64 debug 装到 S5 后启动稳定、相机优雅跳过（`camera attach skipped: CAMERA_DISABLED`）；解锁后主屏/dock 经设备 WebView CDP 读 DOM 确认完整渲染（📞电话/💬信息/AI助手/🌐同传/✉️邮件在 dock）。
- **设备诊断日志 `lib/debugLog.ts` + dock 渲染埋点（frontend-ts，2026-09-06）**：为「真机某界面不渲染」类无法在浏览器复现的问题，新增命名空间日志 `amosLog/amosWarn`（前缀 `[amos][area]`，走 WebView console → adb logcat；`localStorage amos.debug.log=0` 可关）。埋点：`Shell` 每次可见表面变化打 `[amos][shell] surface=lock|app:<id>|edit|home`（主屏 dock 只在非锁/非 app/非编辑态渲染，此项直接判定是否卡在某表面）；`HomeDock` 挂载打 `[amos][dock] mounted {page,dock,…}`，dock 图标列表为空时打 `[amos][dock] …empty…` 并列出被过滤的持久化 id（持久化布局与 APPS 不匹配的常见根因）。验证：tsc clean；vitest svelte **74/74**；bun `[bun-iso] OK`；`vite build` ✓。诚实边界：根因仍待真机 logcat 读数（用户需重打包后回传 `[amos]` 行）。
- **来电振铃音 + 来电通话记录（frontend-ts，2026-09-06）**：真机 daemon 来电路径补两处闭环。(a) **来电振铃**：`components/IncomingCall.tsx` 在来电（`phase==="ringing"`）期间用 WebView `AudioContext` 循环播放 620Hz 振铃（0.5s 响 / 1s 间隔），接听/拒接/挂断即停——补上前一版只做了去电 Dialing 回铃、来电无声的空档。(b) **来电通话记录**：此前仅去电写 `amos.calllog`（PhoneApp/useOutgoingCalls），来电从不落库，导致 Phone「最近/常用」看不到来电。现 `IncomingCall` 用 `surfacedId` ref 追踪「已上屏的来电」，在其 `Ended`（接听/未接/拒接）时经 `readStoreValue`→`recordCall`→`writeStoreValue(CALLLOG_KEY,…)` 写入共享通话记录，`surfacedId` 去重保证每次来电只记一条、重复事件不重复写；React `writeStoreValue` 与 Svelte `createStoreValue` 共用事件桥，PhoneApp「最近/常用」即时刷新。验证：`tsc --noEmit` exit 0；vitest svelte 套件 **74/74**；`vite build` ✓。诚实边界：音频走 WebView（真机若因自动播放/音频焦点无声，需改原生 RingtoneManager/`resume()`，见对话记录）。
- **显示保护 / 自动息屏（auto screen-off，idle → 锁屏 → 省电闭环，2026-09-05）**：真机 OS 需要的不是桌面“屏保动画”，而是“无操作 N 秒后自动熄屏”并让电源策略听到 `screen_on=false`。
  **新 crate `amos-display`（纯 std）**——`ScreenState{On,Off}`/`ScreenChange`；`IdlePolicy`（电池 vs 充电分档超时）+ `ScreenController` 确定性状态机（`touch/probe/sleep/wake`，probe 尊重 hold 与 `hold_screen`，亚秒超时下限 1s 防“首拍即熄”）；**reason-keyed sticky hold**（`set_hold("call"/"nav")/clear_hold/held/holds`，重叠来源各自清除）；`host` 提供 **`AMOS_SCREEN_STATE_PATH` 文件契约**（`on|off`，容忍 `1/0/true/…`，垃圾按 unknown 不猜）。
  **daemon 接线**（`amos-ai::energy`）——`telemetry_from_env` 的 `screen_on` 改为 `resolve_screen_on()`：文件优先（UI 熄屏即 `off`）→ `AMOS_ENERGY_SCREEN_ON` env → 默认 `true`；server 的 ResourceGovernor beat 复用同函数，熄屏即触发“冻结 Background/延后台”。
  **System UI host**（`amos-tauri::display`）——`screen_state_set/get` 命令（原子写共享文件，无路径时诚实报错而非假装），注册进 `lib.rs`。
  **前端**（`App.tsx`/`apps.tsx`/`lib/display.ts`/`lib/keepAwake.ts`）——受持久开关 `amos.displayAutoOffSec` 门控的空闲 watcher；锁屏路径收敛到单一 `lockScreen()`（手动 🔒 与自动熄屏同上报 `off`，解锁/启动重写 `on` 清陈旧文件）；**Settings→General「自动息屏」Segmented（关/15/30/60s）**；读取经 `useStoreValue` **响应式**（运行中改动即时重武装）；**通用 keep-awake reason 总线** `lib/keepAwake.ts`（`assertHold/releaseHold/useScreenHold`，镜像 Rust hold），通话经 `useCallKeepAwake` 接入（Ringing/Active/Dialing hold、End 释放、call-id aware 重叠安全；音乐播放**不**作为 hold，Maps 暂无导航会话故 nav/video 就绪未接）。
  **文档** `docs/display-idle.md`。验证：`amos-display` **13**、`amos-ai` lib **150 + --tests 集成全绿（真 UDS e2e）**、`amos-tauri` lib **101**、三 crate `clippy -D warnings` clean；前端 `tsc` clean + 全量 ISO **607 pass / 0 fail**（新增 display/keepAwake/settings-autooff/telephony-hold 测试）。

- **审计（2026-09-05）· daemon 边界「local-only 名非 OS 资源」锁定**：`privacy_service.rs` 增
  `local_only_capability_name_is_not_an_os_resource` 测试——`notifications`/空串/大小写/带空白等非
  wire-key 一律 `INVALID_ARGUMENT`，钉死前端「notifications 为 local-only」与 daemon 永不将其当作
  OS 资源（无 grant/审计路径）之间的一致性。`amos-ai` lib privacy_service 模块 **5 passed**（+1）；
  clippy `-D warnings` clean。
- **隐私仪表盘「近期访问（OS 权限审计）」视图（frontend-ts，2026-09-05）**：把 daemon 的运行时审计真正显示进 System UI——闭环「审计消费」。`lib/privacyBackend.ts` 增 `DaemonAuditRecord` + `daemonRecentAudit(limit, opts)`（调 host `perm_recent_audit`，离线 `null`）+ 纯 `toAuditViews`（映射 app/资源/granted）；`components/PermissionsApp.tsx` 在线（`bridged()`）时挂载拉取 ≤20 条并按每条渲染 app·资源 + 允许/拒绝标签，离线不拉取（纯本地视图）；`capNameOf` 用 OS 资源=能力名映射本地标签；i18n zh/en 增 `perm.recent.title/live/empty`。验证：`tsc --noEmit` exit 0；`privacy-backend.test.ts` **5 例**（新增 toAuditViews 顺序/映射、daemonRecentAudit 离线 null + 在线按 opts 调 `perm_recent_audit`）。诚实边界：审计来源是 daemon（仅权限 service 自身审计）；安全层/隐私层若共 sink 的其它域事件不在此视图（本视图针对 `PrivacyManager` 决策）。
- **交付说明（2026-09-05）**：新增 `docs/DELIVERY_NOTES_2026-09-05-permissions-audit.md`——沉淀权限/沙箱/审计链路整段交付（对应 `docs/permissions-sandbox-audit-plan.md` Phase 1/2/3(纯 seam)/4/5(host seam)）：可直接使用的 commit message、改动文件、设计决策与「不明即拒」不造假原则、验证计数、诚实边界与真机验收顺序、已知限制。本会话 Rust 侧回归：`amos-proto` check OK、`amos-ai` lib **148 passed** + `privacy_rpc_e2e` **1 passed**、`amos-android` lib **57 passed**、`amos-ai/amos-android/amos-tauri` clippy `-D warnings` clean；前端 `tsc --noEmit` exit 0、permissions/privacy-backend/sandbox-bridge/capability-gate/camera/voice-mic/stream-voice 均 0 fail（按文件隔离验证，`bun-iso-test` 语义）。
- **容器侧 APK 能力账本 seam（`amos-android`，Phase 5 host seam，2026-09-05）**：为未来 per-APK 权限拦截铺「决策数据」。新增 `src/capability.rs`：`SENSITIVE_RESOURCES`（wire-key 与 host `PrivacyManager`/`proto/privacy.proto` 对齐：microphone/camera/contacts/location/storage）、`valid_resource_key`、`CapabilityLedger`（deny-by-default、线程安全 `RwLock<HashMap<pkg, HashSet<key>>>`：`grant`（未知键拒绝返回 false）/`revoke`（单锁内删空即移除包条目）/`revoke_all`/`is_granted`/`granted`（排序）/`packages`），`amos-ai` 零依赖、纯 Rust。注册 `lib.rs pub mod capability;`。+**5 单测**（deny-by-default+未知键拒绝、grant/revoke 往返、包隔离+revoke_all、granted 排序稳定、wire-key 与 host 对齐）。验证：`cargo test -p amos-android --lib capability` **5 passed**；`clippy --all-targets -D warnings` clean、fmt clean（遵守 amos-android P0-1 gate，无 unwrap/expect/panic）。诚实边界：这是**决策数据 seam**——容器内 `AudioRecord`/`Camera.open`/通讯录的 HAL 级拦截与把账本接进容器策略/`am force-stop` 仍是设备/生态工作（`docs/permissions-sandbox-audit-plan.md` Phase 5 未落地部分），未伪造「已拦截」。
- **web-bundle 沙箱能力桥 · 纯裁决 seam（frontend-ts，Phase 3，2026-09-05）**：为「沙箱内三方 web-bundle 请求敏感能力」定义**纯校验 + deny-by-default 裁决 + 应答帧**，可离线单测、与 `lib/bundle.ts` 同风格。(a) `src/lib/sandboxBridge.ts`：`SANDBOX_RESOURCES`（daemon wire-key：camera/microphone/contacts/location/storage；`notifications` 等 local-only 不在其列）；`parseSandboxResource`（未知/非字符串→null）；`validateCapabilityRequest(raw)` 结构化校验 `{type:"capability.request", resource, requestId}`——非对象/错 type/空 requestId/未知资源一律 `invalid`；`decideCapabilityRequest(raw, daemonGranted)` 仅当 daemon `perm_authorize` 为 `true` 才回 `granted:true`，`null`（离线/未知）或 `false` → 拒绝，无效帧无应答——**沙箱三方绝不在含糊时被放行**。(b) `__tests__/sandbox-bridge.test.ts` **5 例**：资源词表与 daemon 对齐、合法帧过检、坏帧/未知资源/空 id 拒绝、daemon true 才放行且离线 null→拒绝、无效帧无应答。验证：`tsc --noEmit` clean；sandbox-bridge 5 passed。诚实边界：这是**纯裁决 seam**——接进真实 `postMessage` 监听 / `amos-app://` 真 origin 宿主仍是设备/协议阶段（`docs/appstore.md`/`docs/permissions-sandbox-audit-plan.md` Phase 3），未在无真 origin 的桌面单页宿主上造假拦截。
- **完整 P2.2：`CapabilityGate`/`PermissionsApp` 改走 daemon 权威（frontend-ts，2026-09-05）**：把敏感能力门的 allow/deny 从「纯本地账本」接到上一步铺好的 daemon 权威 seam。(a) `components/CapabilityGate.tsx::useCapability`：挂载时**仅当 online（`bridged()`）**用 `daemonAuthorize(appId, cap)` 对账——daemon「拒绝」会**覆盖过期本地授权**（真实 OS store 为准）；`allow()` 先 `void daemonGrant(...)`（best-effort）再写本地缓存；离线（无 bridge）或 local-only 能力（notifications）不探询 → 快速本地路径，离线行为与旧版逐字节一致、无闪烁。(b) `components/PermissionsApp.tsx`：每个 grant/revoke toggle 提交 `daemonGrant/daemonRevoke`（离线 no-op），本地账本保留为即时显示缓存；顶部 doc 说明「daemon 权威 + 本地缓存」。(c) 测试：`capability-gate.test.tsx` 新增在线用例——本地账本过期授权 camera 但 daemon `perm_authorize` 返回拒绝 → gate 隐藏 children 出 allow 提示、`perm_authorize` 被调；点 Allow 提交 `perm_grant` 并放行。验证：`tsc --noEmit` clean；capability-gate（含新在线例，5 例）/camera/voice-mic/stream-voice/permissions/privacy-backend **各文件独立跑绿**（直接 `bun test f1 f2…` 合跑会因多个 DOM 测试文件共享 happy-dom 全局产生隔离假阳性——仓库 `bun-iso-test` 是按文件隔离的，本实现离线路径与旧行为一致，未破坏既有用例）。诚实边界：`daemonAuthorize` 离线/local-only 返回 `null`、grant/revoke 离线为 no-op；OS 边界的真实拦截（web-bundle 能力桥、真机 APK HAL）仍是设备/生态层，不在浏览器/桌面层造假。
- **前端权限词汇对齐 + daemon 权威后端 seam（frontend-ts，Phase 2.2/2.3，2026-09-05）**：把前端敏感能力词汇与 daemon `PrivacyManager::Resource` 的 wire-key 对齐，并铺好「OS 门要改走 daemon 权威」的可测 seam。(a) `lib/permissions.ts`：`Capability`/`CAPABILITIES` 补 `contacts`/`storage`（与 Rust `Resource` 的 `contacts`/`storage` 对齐），`notifications` 明示为 **local-only**（无 daemon resource）；`lib/permissions.ts` doc 说明对齐关系。(b) `components/PermissionsApp.tsx`：穷举映射 `CANDIDATES`（contacts/storage 暂无内置门，诚实空数组）/`CAP_ICON`/`capLabel` 补 `contacts`/`storage`，仪表盘新增两节。(c) i18n en/zh 增 `perm.cap.contacts`（Contacts/通讯录）/`perm.cap.storage`（Storage/存储）。(d) 新 `lib/privacyBackend.ts`：`daemonResource(cap)` 1:1 映射到 wire-key（notifications→null）、`daemonAuthorize`（返回权威布尔，**离线 `null` 永不造假**）、`daemonGrant/daemonRevoke`（bridged 才提交、unit 结果即 JSON `null` 故语义为“已提交”）；`permissions.test.ts` 的 known-capability 断言扩到 6；新增 `__tests__/privacy-backend.test.ts` 3 例（映射全覆盖、离线降级、online 调用 `perm_*` 用对齐 key）。验证：`tsc --noEmit` clean；permissions/privacy-backend/capability-gate/camera/voice-mic/stream-voice 测试全绿。诚实边界：这是**词汇对齐 + 权威 seam**——`CapabilityGate`/`PermissionsApp` 的 allow/deny 仍走本地 `permissions.ts` 账本（本地缓存，未改走 `daemonAuthorize`/`perm_*`），完整 P2.2 消费接线、web-bundle 沙箱能力桥（Phase 3）、真机 APK 拦截（Phase 5）仍未落地（文档已列）。
- **gRPC 权限 RPC：PrivacyManager 接成 daemon 权威服务（`proto/privacy.proto` + `amos-proto` + `amos-ai` + `amos-tauri`，2026-09-05）**：把「死 seam」接成可被 System UI 经真实 UDS 调用的**权威裁决/审计**表面（`docs/permissions-sandbox-audit-plan.md` Phase 2）。(a) **proto**：新增 `proto/privacy.proto`（package `amos_privacy`，service `PrivacyService`：`Grant/Revoke/RevokeAll/Authorize/Granted/RecentAudit`，资源用稳定 wire-key `microphone/camera/contacts/location/storage`，未知键由 daemon `INVALID_ARGUMENT` 拒绝——永不弱化 deny-by-default）；`amos-proto` build.rs/lib.rs 注册生成 `privacy_service_server`/`client`。(b) **daemon 服务**：`amos-ai` 新 `src/privacy_service.rs`——`PrivacySvc` 持 `Arc<PrivacyManager>`，`Authorize` 走管理器 `authorize()` 单一咽喉并全量审计；`Granted/RecentAudit` 映射管理器；`AMOS_PRIVACY_PATH` 设则 grant/revoke 后原子落盘（复用 `privacy.rs::save`）、`bootstrap()` 加载既有状态并 `with_durable_audit` 把每个决策镜像到 `<path>.jsonl` 统一审计；挂载进 `server.rs::serve()` 的 builder（与 governor/telephony/sensor 共享同一 UDS）。(c) **host 客户端**：`amos-tauri` 新 `src/privacy_client.rs`——`build_channel()`(UDS) + 命令 `perm_authorize/grant/revoke/granted/recent_audit`（prost 不可 Serialize，`RecentAudit` 返回可序列化 `PermissionAudit` 镜像），注册进 `lib.rs` invoke handler，为前端「隐私」仪表盘/敏感能力门提供 daemon 权威后端。另补 `privacy.rs`：`with_audit_file`/`with_durable_audit` 让 `authorize()` 决策镜像进统一可持久 `AuditFile`。验证：`amos-ai` lib **144→148 passed**（+4 privacy_service：grant/authorize 往返、未知键拒绝、audit 过滤、granted/revoke）；真 UDS e2e `tests/privacy_rpc_e2e.rs` **1 passed**（boot `serve()` → 未知键拒绝 → grant mic → authorize granted / camera denied → Granted 列表 → RecentAudit 两条）；`clippy -p amos-ai --all-targets -D warnings` 干净、fmt 干净；`cargo check -p amos-tauri --lib` clean + clippy `-D warnings` clean。诚实边界：这是**决策/审计权威**——OS 边界执行（拦截三方 APK `AudioRecord`/`Camera.open`、web-bundle 桥）仍是设备/生态工作；前端 `permissions.ts` 账本与其对齐、`CapabilityGate` 改走 `perm_*`（Phase 2.2/2.3）与 web-bundle 能力桥（Phase 3）、真机 APK 拦截（Phase 5）仍未落地（文档已列）。
- **PrivacyManager 可寻址化 + 统一审计落盘（`amos-ai`，2026-09-05）**：把权限域的“死 seam”补成可落盘、且安全/隐私审计统一到同一可持久模型。落点 `docs/permissions-sandbox-audit-plan.md` Phase 1 + 4。(a) **新 `audit.rs`（统一审计模型/落盘 sink）**：`AuditRecord{ts,principal,op,resource,outcome,details}` + `Outcome{Success,Granted,Denied,Rejected,Error}`，把 `security::AuditResult` 与 `privacy::AccessDecision` 折进同一枚举而不丢失语义；`AuditFile` = JSON-lines 追加日志（可持久/可纯内存），`open()` 加载既有合法行、跳过崩溃残留坏行，`log/recent/recent_matching/count/export_json`；含 `From<AuditEntry>`/`From<AccessRecord>` 互转。(b) **`privacy.rs` 持久化**：新增 `FileState`（grants 以稳定 wire-key 存 `Vec<String>` 排序，审计同盘）+ `save(path)`（临时文件+rename 原子写、自动建目录）+ `load(path)`（未知资源键丢弃保 deny-by-default、超 `max_audit` 审计截断到最新）；既有 `new(grant/…)` 内存 API 不变。(c) **`security.rs` 落盘**：`AuditLogger` 增可选 `AuditFile` sink，新增 `new_persistent(max,path)`/`durable_path()`，`log()` 除内存环外 best-effort 追写统一文件（磁盘失败仅告警不致命），既有 `new/default`/方法签名不变。验证：`amos-ai` lib **118→143 passed**（+5 audit +3 privacy +1 security 等；新增 `audit_file_is_send_sync`/`conversions…`/`memory_sink…`/`durable_sink_round_trips…`/`open_skips_torn…`/`save_load_round_trips…`/`load_from_missing_or_garbage…`/`load_trims_oversized…`/`persistent_logger_appends…`）；`clippy --all-targets -D warnings` 干净；fmt 干净。诚实边界：授权/审计仍处内存+可选落盘 seam——把 `PrivacyManager` 接成 daemon 权限 RPC、与前端 `permissions.ts` 账本对齐、web-bundle 沙箱能力桥等仍属计划 Phase 2/3（文档已列）。
- **交付说明（2026-09-05）**：新增 `docs/DELIVERY_NOTES_2026-09-05-clipboard-lmk-power.md`——沉淀本会话全局剪贴板 / power-overlap / LMK G3 三段改动（commit message、改动文件、验证计数、诚实边界、真机验收顺序）。
- **System UI LMK 调试/用户入口（amos-tauri，`docs/android-lmk-e2e.md` G3，2026-09-05）**：把“触发 LMK / ApplyHostDecision”从仅 daemon 侧补成 System UI 可达命令——`src/android_lmk.rs` 新增 `android_lmk_debug(action, package_name?, budget?)` Tauri 命令并经 `AndroidManagerClient`（同 UDS）驱动 daemon：`trigger` → `TriggerLmk{MemoryPressure::Critical, budget}` 返回 victims（`LmkVictimOutcome`）；`apply_freeze/thaw/reclaim` → `ApplyHostDecision(HostAction{Freeze/Thaw/Reclaim}, package_name)`。含纯 `parse_debug_action`/`action_to_host`/`victim_outcome` 映射 + **3 单测**，注册进 `lib.rs`。前端 `lib/lmk.ts` 增 `LmkVictim/LmkDebugOutcome` + `androidLmkDebug(...)` 封装（离线降级 null）。验证：`amos-tauri` lib **95**、`clippy --all-targets -D warnings` 干净、前端 `tsc --noEmit` 干净。诚实边界：daemon 侧真实回收依赖容器 host（真机 bring-up）；本入口使 System UI 可 compile/测试地驱动之（桌面无容器时为诚实 no-op/报错）；另改为**常驻 LMK 开发面板 `components/LmkDebugPanel.tsx`**（Settings 诊断页常挂、不受 governor 内容门控，离线显示“daemon 未连接”）承载「🧨 触发 LMK」+ **刷新**（重拉 `android_lmk_tasks`）+ trigger 的 **`budget`(1..8) 数字输入** + 结果 **note 做成可消失 toast**（4s 自动隐 + ✕ 手关）+ **近期回收 victims 历史回显**（trigger 返回 victims 时记录最近 5 轮、`killed`/`frozen` 标注、可**清空历史**）+ 容器任务**按 `state` 过滤动作**（`cached`→解冻/回收、`background`→冻结/回收、`stopped`→回收、前台/未知不给动作）的按钮（调 `androidLmkDebug("apply_*", package)`）且**全部文案接入 i18n**（新增 `lmk.*`/`lmk.aria*` 键到 en/zh，含 aria/title、en 文案与「aria 随 locale」DOM 校验）；新增 `lmk-debug-panel.test.tsx` **7** DOM 用例；`TaskManager` 回退为纯 governor（其 `task-manager.test.tsx` 2 用例）。
- **amos-power：退化多域拓扑 发现/应用 边界端到端测试（2026-09-05）**：新增 `discover_and_apply_are_stable_on_sparse_noncontiguous_topology`——CPU 编号有洞（缺位）且 big policy 成员**非连续**（cpu2 与 cpu5 共域，但仅低 rep cpu2 有 sysfs 目录、“ghost”高成员无目录）的退化拓扑：`discover` 跳过空洞、把非连续组归为以最小 rep 键控的单一域、不把 ghost 成员重复计入；随后 `discover → plan(PowerSave) → apply` 只写各 rep（cpu0/cpu2），且不凭空生成 ghost 成员目录；另补两纯边界——`discover_surfaces_overlapping_policies_deterministically`（两个 `related_cpus` 策略组交叠时按各自 owner 的 min 如实上浮、不静默合并/丢弃）与 `discover_respects_cpu_max_larger_than_present_cpus`（`cpu_max` 远大于实有 cpu 时只返回实有域、不越界不造幻影）。`amos-power --features linux` lib 测试 **42→46**；并把 overlap 上升为**可选的去重/告警策略（生产 API）**：`LinuxFreqGovernor::discover_with_policy(root, cpu_max, OverlapPolicy) -> Discovery{domains, overlaps}`——`Surface`（默认、向后兼容：交叠域照实上浮并报告）与 `Dedupe`（最早低 rep 认领为权威，丢弃后来交叠 owner 并报告），`discover` 委托 `Surface`；`Overlap{rep_cpu, prior_rep_cpu, cpu}` 精确报告冲突，+1 策略测试；并**接线进 `amos-ai`**：`DvfsDriver::from_cpufreq_root`（`AMOS_CPUFREQ_ROOT` 拓扑发现）改走 `discover_with_policy(root, cpu_max, OverlapPolicy::Dedupe)`，遇交叠 `tracing::warn` 记“域 rep X 重叠 域 rep Y / cpu C，丢弃后来 owner”（异常 sysfs 不再静默）。`amos-power --features linux` lib **46**；`amos-ai` lib **134**（新增 `dvfs_discovery_dedupes_overlapping_policies_and_warns`：注入交叠 tempdir → 断言 Dedupe 域集=仅 cpu0、stray owner 被丢、恰一次 WARN）、`clippy --all-targets -D warnings`/fmt 干净；另新集成测试 `tests/dvfs_overlap_e2e.rs`（独立测试进程、env 隔离）：以**交叠 `AMOS_CPUFREQ_ROOT`** 真起 `amos_ai::server::serve()` → 断言 daemon 正常上线并服务 governor RPC（Dedupe 使交叠启动容错，不崩溃），与 governor.rs 单测互补。
- **全局系统剪贴板服务 + AI 上下文回退 + 前端复制/粘贴（2026-09-05）**：把单一「选区→AI」的 `SystemContext` 扩成**真·跨窗口/跨容器剪贴板**。
  Rust（`amos-tauri` 新 `src/clipboard.rs`）：多格式 `ClipboardPayload`（`text/html/image/uris`，二进制走 base64）、`GlobalClipboard`（有界历史 `HISTORY_LIMIT=32`、seq 单调、同内容合并、入栈前校验），命令 `clipboard_write / clipboard_read / clipboard_history / clipboard_clear` 注册进 `lib.rs` 并以 `Arc` 托管 + `setup` 里 `arm_ingest` 绑定同一实例；**前台读取权限** `require_foreground`（对照 `WmState`：写任意窗口、读/history/clear 仅焦点窗口）；写后广播**仅元数据 `ClipboardNotice`**（不含正文，杜绝后台靠订阅偷读），真正文走前台门控 `clipboard_read`。AI 侧兼容回退：`ask_ai_agent / chat_agent` 优先注入指向本窗口的 `SystemContext`，无则取全局剪贴板最新文本，均落 `AgentRequest.context["system_selection"]`（协议不变，`merge_system_selection` + 3 单测）。Android 容器桥：`src/clipboard_glue.rs`（feature `android`，JNI：`attach`/`onContainerCopy`、`AndroidClipboardSink` 镜像 `pushTextClipboard`）+ 新 `ClipboardGlue.kt`（双向 `attach`/`pushTextClipboard`/`onContainerCopy`，回显 500ms 抑制）接线进 `AmosGlue.onStart/onStop`（真机 bring-up，Rust 已 compile-check）。前端 `lib/clipboard.ts`（类型 + `clipboardWrite/Read/History/Clear` + `onClipboardChanged(ClipboardNotice)` + 纯 `entryText/previewClipboard/copySelection`，离线降级 `null`）+ Notes 编辑器接 **⧉ 复制 / 📋 粘贴**（`clipboardWrite`/`clipboardRead` 打通跨窗口）与 **🕘 历史浮层 `components/ClipboardTray.tsx`**（`clipboardHistory` 列表 + 每次 `clipboard-changed` 通知自动重取 + 点按回填前台 `clipboardRead` 内容）；`backend.ts` 加 `typeof window` 守卫（headless 不再抛 ReferenceError）。文档 `docs/multi-window.md` §3.5 + CHANGELOG 同步。验证：`amos-tauri` lib **92**、`clippy` 默认与 `--features android` 干净、`cargo check --tests` 通过；前端 `tsc --noEmit` 干净 + 新 `clipboard.test.ts` **3** + `clipboard-tray.test.tsx` **3**（DOM，共 6）。诚实边界：容器摄取已通过 `setup` 里的 `set_notifier` 接上 `clipboard-changed` 广播（运行时依赖真机 `AppHandle`），Kotlin 桥与真机行为仍为 bring-up 待验。
- **系统工作状况监控：domain-core `amos-monitor` + daemon/Tauri/前端 全链路（2026-09-05）**：新增 transport-/platform-agnostic crate `amos-monitor`（`docs/system-monitor.md`）——把系统负载（CPU/内存）、电池/功耗与进程生命周期折叠成一个诚实的 `SystemHealth`。`spec`——`SystemLoad/CpuSample/MemoryInfo/BatteryStatus/ProcessSummary/SystemHealth`（缺失=unknown 从不造假，`summary()` 一行日志）；`sampler`——`SystemSampler` seam + 确定性 `MockSystemSampler`（CPU busy% 为两读增量，内部 `Mutex` 保持 `Send+Sync`）；`monitor`——`SystemMonitor` 聚合器 + `process_summary`；`linux` feature——真实 `/proc` 采样（可注入 procfs 根，tempdir 全测，无 root/设备）；`android` feature——门控骨架。纯 std，默认轻量。
  接线：proto `StatusReply.system=16` + `SystemHealth/SystemLoad/SystemBattery/ProcessCounts`；`amos-ai` `system_metrics()` 采样并折叠 governor app 生命周期计数 + **energy store 电量/充电/功耗**（首 tick 前诚实 pending）；Tauri 新桥 `amos-tauri/src/system.rs` `system_health` 命令（纯 `system_status()` 映射）；前端 `lib/system.ts`（`normalizeSystemHealth/memUsedPct/fmtBytes/liveProcesses` + 离线安全 `systemHealth()`）+ Settings `SystemPanel.tsx`（CPU/内存/电量/进程档，无数据显示 “—”，挂于 SensorPanel 旁）+ en/zh `settings.system*`。文档 `ARCHITECTURE.md`/`docs/system-monitor.md` 同步。
  验证：`amos-monitor` **14**（`--features linux`；`android` check/clippy 干净）、`amos-ai` lib **125** + rpc e2e **4**、`amos-tauri` lib **71**、`bun run test` `[bun-iso] OK` + `bun run typecheck` 全绿；三 Rust crate `clippy -D warnings` / fmt 干净。诚实标注：真机 CPU/内存来自 `/proc`（Android/Linux），macOS dev `/proc` 缺失即如实 unknown；系统块电池镜像 energy block（仍以 energy 为权威）。
- **电源与性能优化：真功耗模型 + Energy-Governor → CPU/NPU 频率闭环（2026-09-05）**：
  **`amos-profiling`**（真功耗模型）——新增平台无关、host 可测的 `power::BatterySample{current_ua, voltage_mv}`（`is_valid`/`power_mw` = µA×mV/1e6，无效即 `0.0` 诚实不可用）；`AndroidBatteryPowerSource` 由「固定 3700 mV」改为**每次采样自刷新实时 `ACTION_BATTERY_CHANGED` 粘性广播 `EXTRA_VOLTAGE`**，`est_energy_j` 由此用真实电压估算（`read_mw`/`sample` 仍 `0.0`=不可用）。测试 **23**（另含 `mean_power_mw` 窗口均值功耗与 `ProfileReport::compute_sampled`——直接由 `BatterySample` 数组出 `avg_power_mw`→`est_energy_j`）。
  **`amos-power`**（Energy-Governor → 频率档闭环，纯 std 域 + host seam，不改真机 HAL）——新增 `freq` 域：`ClusterKind{Little,Big,Prime}`/`Cluster`/`FreqCap`/`FreqPlan`、整数百分比 `cpu_percent`/`npu_percent`、`cap_khz`（对 `max_khz==0` 的退化拓扑**不 panic**）、`plan`/`plan_from_decision`（`Decision.sensor_mode` → 各 cluster+NPU 上限）。Balanced **不设小核上限**保 UI/电话响应、限 big/prime ~80% 与 NPU 85%；PowerSave 优先限 Prime→Big→NPU。`FrequencyGovernor`（有状态 DVFS ticker）：`observe(&Telemetry)->Option<FreqPlan>`，仅档位真变才返回，避免每 poll 重复写 sysfs。`linux` feature `LinuxFreqGovernor`：`Some`=写 `scaling_max_freq`，`None`=**读 `cpuinfo_max_freq` 恢复上限**（杜绝 PowerSave 限死后无法解除）；失败如实回报 `FreqApplyReport`，tempdir 可测不 panic。`android` feature `AndroidBatteryTelemetry::snapshot_with_power(&PowerSource)` 把实时功耗折进 `Telemetry.power_mw` 驱动 `PowerDraw` 分支。示例 `governor_freq`（静态映射）、`ticker`（周期 ticker 参考，按变化才 apply）、`live_governor`（完整 host 演示：功耗模型 + `ComposedGovernor`——`SensorManager` 采样门控 + 去抖 `FreqPlan`）；`tests/closed_loop_linux.rs` 走完整 host 闭环（`BatterySample`→功耗→`EnergyGovernor`→`FrequencyGovernor`→sysfs 写 + 恢复）。Makefile `gated-check` 增 `--features linux`。文档 `docs/power-policy.md`/`profiling.md` 同步。验证：`amos-power` 默认 **33 lib + 2 集成**、`--features linux` **42 lib + 2 closed_loop + 2 集成**（后续补全增至）、`--features android` check 干净；两 crate `clippy --all-targets -D warnings` / fmt 干净。后续补全：`LinuxFreqGovernor::discover(cpufreq_root, cpu_max)` 拓扑发现（读 `cpuinfo_max_freq`+`related_cpus` 按共享策略分组去重 → `DiscoveredDomain{rep_cpu,cpus,max_khz}`，`cluster(kind)` 标定）；`with_npu_max` 让 uncapped NPU 恢复到已知硬件上限（与 CPU `cpuinfo_max_freq` 恢复对称，无路径时如实报错）；`profiling::mean_power_mw` 窗口均值功耗喂 `est_energy_j`；`amos-ai::ResourceGovernor::freq_plan` 桥把上一 tick `sensor_mode` 映射成频率计划；`ComposedGovernor` 每 tick 把 `SensorMode` 推进到真实 `SensorManager`（采样门控）并同时返回去抖后的 `FreqPlan`，一行驱动"决策→采样+频率"闭环；**daemon 常驻 DVFS beat**（`amos-ai` 启用 `amos-power/linux`）：`governor::DvfsDriver` 从 `AMOS_CPUFREQ_ROOT` discover CPU 域（启发式 kind，可用 `AMOS_GOVERNOR_CPU_KINDS="0:Little,4:Big"` 覆写）并仅变化时真写 `scaling_max_freq`（env 未设即 inert，累计 applied/failed）；proto `GovernorMetrics` 增 `dvfs_applied`/`dvfs_failed`，`get_status.governor` 上报 DVFS 写计数，`amos-ai` lib 123。剩余（设备 bring-up）：System UI/daemon ticker 采样真电池并注入 chip 拓扑 + cpufreq 写权限；真机部署/验收执行手册见新增 `docs/dvfs-power-bringup.md`（env 开关 `AMOS_CPUFREQ_ROOT`/`AMOS_GOVERNOR_CPU_KINDS`、架构速览、host 验证命令、设备步骤与判据、诚实边界）。

- **任务管理器：`system.apps` 带出 governor 已注册 app 清单（2026-09-05 增补）**：`StatusReply.system.apps`（`repeated AppProcess{id,state}`，state 用 `AppState::key()` 稳定键 foreground/visible/foreground_service/background/cached/stopped）；Tauri `SystemStatus.apps`/`AppProc` 映射；前端 `lib/system.ts` `AppProc` + `normalizeApps`（丢弃畸形项）；Settings `SystemPanel` 在 app 非空时列出 id + 状态档（en/zh `settings.systemApps`）。验证：`amos-ai` lib **125**、`amos-tauri` lib **72**、`bun run test` `[bun-iso] OK` + `bun run typecheck` 全绿。
- **SystemPanel 自动刷新（frontend-ts, 2026-09-05）**：Settings「系统工作状况」tile 在有数据后每 ~2.5s 自动轮询 `system_health`（CPU busy% 是两读窗口增量，需周期采样；`busyRef` 去重防重叠）。仅在已有数据时启 `setInterval`（离线/测试无数据不启、无泄漏），卸载/数据消失即清。验证：`bun run typecheck` + 全量 ISO 套件 `[bun-iso] OK`。
- **energy/governor 组合审计硬化（amos-ai, 2026-09-05）**：审计 `ResourceGovernor::observe`（energy→lifecycle→scheduler 组合）确认无死代码/半接线：PowerSave 冻结闲置 Background、恢复即按 screen_on 解冻 Cached、AlarmExact 恒触发、Deferred 仅电源态允许时跑、memory_pressure 走 LRU 回收、`freq_plan` 由上一档映射。补一条**安全属性锁**测试 `power_save_plus_pressure_never_touch_protected_apps`：PowerSave + 内存压力同时发生也只处理 Background 受害者，Foreground / ForegroundService 永不被冻结或回收。`amos-ai` lib **129**、`clippy --all-targets -D warnings` / fmt 干净。诚实边界：`cap_inference`/`throttle_background` 仍是建议（真 engine/SensorManager 门控是调用方 seam）；冻结仅在 `PowerSave` 档触发（保守设计，避免瞬时 power_draw 时过度墓碑化）。
- **energy/DVFS 审计补全：resident DVFS beat 可配 NPU（amos-ai, 2026-09-05）**：审计发现常驻 DVFS beat 此前把 NPU 相关参数硬编码为空（无 env 可设），`freq_plan`/`apply` 因而永不对 NPU 生效。新增两个可选 env——`AMOS_CPUFREQ_NPU_MAX_KHZ`（u32 kHz，让 governor 按档 Balanced ~85% / PowerSave 更低**算得出** NPU 上限）与 `AMOS_CPUFREQ_NPU_NODES`（逗号分隔的 devfreq max-freq 节点，**真写盘** NPU 上限/恢复）；未设则照旧只做 CPU DVFS。解析收敛为纯函数 `parse_trimmed_u32`/`parse_node_paths`（空/非数/负数/畸形成员→None/丢弃，绝不 panic）+ `opt_env_u32`；新增两条 hermetic 单测。验证：`amos-ai` lib **128**、`clippy --all-targets -D warnings` / fmt 干净。诚实边界：CPU 域照旧 `AMOS_CPUFREQ_ROOT`（`AMOS_GOVERNOR_CPU_KINDS` 覆写 kind）；若只设 MAX 未设 NODES，`apply` 会如实报 `no npu_max_paths`（不静默）。
- **scheduler 过期清理审计补全（amos-scheduler + amos-ai, 2026-09-05）**：审计发现 Deferred 任务若其 `[earliest,latest]` 窗口在始终不满足运行条件（doze/非 maintenance/未充电）下**整体过期**（`now > latest`），会被永挂——不跑也不清，`len()/counts()/entries()` 长期失真。补 `Scheduler::expire(now)`：移除窗口已过的 Deferred 并返回其 id（Exact 闹钟永不过期清除，调用方才是触发权威）；`ResourceGovernor::observe` 每拍调它并把结果如实放进 `GovernorOutcome.dropped`；daemon governor-beat 的 `acted` 日志纳入 `dropped` 计数。`amos-scheduler` lib **8**（+expire 单测：过期的清、有效 Deferred 与 Exact 保留）；`amos-ai` governor:: **8**（+dropped 单测）、lib **130**；两 crate `clippy --all-targets -D warnings` / fmt 干净。
- **governor 过期数上 wire（amos-ai + proto, 2026-09-05）**：`StatusReply.governor`（`GovernorMetrics`）增 `uint64 dropped = 8`——把上轮的“过期 Deferred 清理”反映到诊断面，外部宿主/System UI 可见最近一拍过期了多少；`server.governor_metrics()` 三态均映射（决策态取 `outcome.dropped.len()`，pending 为 0）。新增 `governor_metrics_surfaces_expired_deferred_count`（注册过期窗口→PowerSave tick→`m.dropped==1`），并在决策测试补 `dropped==0` 基线断言。`amos-ai` lib **131**、`clippy --all-targets -D warnings` / fmt 干净。
- **SystemPanel 合并“系统+电源”诊断（amos-tauri + frontend-ts, 2026-09-05）**：`system_health` 一条命令现同时带 `get_status.governor`（`GovernorMetrics`：mode/reason/cap_inference/throttle_background/dvfs_applied/dvfs_failed/dropped）→ serde `SystemGovernor`；前端 `lib/system.ts` 加 `SystemGovernor`/`normalizeGovernor`（缺省/畸形→null）；`SystemPanel` 在进程档下渲染「电源调控」区（mode · reason、限推理/延后台 on/off、DVFS applied/failed、过期 dropped），与 CPU/内存/电量/进程档合成统一的“系统+电源”诊断。i18n en/zh `settings.systemGovernor*`。验证：`system.rs` Rust 6、`amos-tauri` lib **73**；TS typecheck + system **15** + SystemPanel DOM **2** + 全量 `[bun-iso] OK`；clippy 干净。
- **SystemPanel「电源调控」收尾（frontend-ts, 2026-09-05）**：governor 模式本地化（复用 `settings.sensor{Balanced,Performance,PowerSave}`，未知模式回退原始串）；当 DVFS 写失败数 `dvfs_failed>0` 时在该行加 `⚠` 运维警示。SystemPanel DOM 测试 **3**（主用例补 `Power save`/`⚠` 断言）。全量 `[bun-iso] OK` + typecheck 绿。
- **SystemPanel：pending governor 不上「电源调控」区（frontend-ts, 2026-09-05）**：仅当 governor 已跑（`ticks>0`）才显示「电源调控」块——未运行（balanced · pending）不再被当成“正在调控”，避免误导；其它 CPU/内存等数据照常显示。SystemPanel DOM 测试 **3**（新增 pending 隐藏断言）。全量 `[bun-iso] OK` + typecheck 绿。
- **daemon 周期心跳打印系统状况 + 折叠收敛（amos-ai, 2026-09-05）**：`serve()` 新增 `system_beat`——与既有各 beat 同节奏（`AMOS_METRICS_INTERVAL_SECS`）打印一行 `amos-monitor::SystemHealth::summary()`（`cpu/mem/battery/processes`，离线即如实 unknown），无 UI 时运维也能从日志看系统状况；关停时 `system_beat.abort()`。折叠逻辑收敛为 `fold_system_health(sampler, energy, governor)`（`get_status` 与心跳共用同一快照口径），新增访问器 `AiAgentService::system_sampler()`。`amos-ai` lib **126**（+fold 单测）、rpc e2e **4**、`clippy --all-targets -D warnings` 干净。
- **SystemPanel DOM 渲染测试（frontend-ts, 2026-09-05）**：`__tests__/system-panel.test.tsx`（2 项）——fake `__TAURI_INTERNALS__` 下 `system_health` 渲染 CPU/内存/电量/进程档 + per-app 清单；无 daemon（非 bridged）时不渲染（无坏卡）。`afterEach` 卸载即清理自动刷新定时器，无泄漏。全量 ISO 套件 `[bun-iso] OK`。
- **Task Manager decision 模式本地化（frontend-ts, 2026-09-05）**：与 SystemPanel 一致，decision 行把 governor `mode` 经 `settings.sensor{Balanced,Performance,PowerSave}` 本地化（未知回退原始串）；`reason` 保持原始稳定键。DOM 断言改 `Power save`。typecheck + 全量 `[bun-iso] OK`。
- **Task Manager 自动刷新（frontend-ts, 2026-09-05 增补）**：TaskManager 有内容后每 ~3s 自动轮询 `taskmgr_snapshot`（daemon beat 会随时间冻结/解冻/过期 app，registry 需实时）；`busyRef` 去重防重叠；无数据/离线不启 `setInterval`、卸载即清（测试无泄漏）。typecheck + DOM 2 + 全量 `[bun-iso] OK`。
- **CancelJob service 测试补全（amos-ai, 2026-09-05）**：新增 `governor_service::tests::schedule_then_cancel_job_over_the_service`——经 service 直接 schedule→cancel→GetState 断言 job 消失、cancel 不存在 id 返回 `NotFound`。`amos-ai` lib **133**、clippy 干净。
- **任务管理器：job 可取消（amos-ai + amos-tauri + frontend, 2026-09-05 增补）**：给 governor 加 job 能力——proto `CancelJob(JobRef)`；`ResourceGovernor::cancel_job`（`Scheduler::cancel`，无则 `false`，测试：cancel 后只剩 alarm、未知 id 返回 false）；`GovernorService::cancel_job`（不存在 → `not_found`）；Tauri 桥 `taskmgr_job_action(job_id, action=cancel)`（非 cancel → 拒绝）注册进 `lib.rs`；前端 `lib/taskmgr.ts` `taskmgrJobAction` + `TaskManager` job 行加「取消」按钮。DOM 测试补 job-cancel 断言。验证：`amos-ai` governor:: **9**、lib **132**；`amos-tauri` lib **77**；typecheck + 全量 `[bun-iso] OK`；两 crate clippy 干净。诚实边界：仍无 run-now/force-run RPC。
- **Task Manager「任务管理器」面板 + Governor 桥（amos-tauri + frontend-ts, 2026-09-05）**：新增 `amos-tauri/src/taskmgr.rs` Governor 桥（同一 UDS）——`taskmgr_snapshot` 列出 governor 的 apps/jobs + energy 决策；`taskmgr_app_action(app_id, action)` 把 `foreground/background/freeze(cached)/stop(stopped)/kill(unregister)` 驱动进 daemon 共享 `ResourceGovernor`（MoveApp/UnregisterApp），随后回读最新快照。纯 `task_snapshot` 映射 + `app_state_key`/`job_kind_key`/`parse_action`/`action_to_state`（4 单测）。前端 `lib/taskmgr.ts`（类型 + `normalizeTaskSnapshot`/`appIsRunning` + 离线安全 `taskmgr*` 包装，4 单测）+ `components/TaskManager.tsx`（可滚动 app 列表 + 每项 前台/后台/冻结/停止/结束 按钮；可滚动 job 列表（cancel 操作见下）；无数据/非 bridged 隐藏），挂于 Settings `SystemPanel` 下；en/zh `settings.task*`。DOM 测试 2（列表渲染 + 点“freeze”发 `taskmgr_app_action{appId,action}` 并刷新、离线隐藏）。验证：`amos-tauri` lib **77**、clippy/fmt 干净；前端 typecheck + 全量 `[bun-iso] OK`。诚实边界：job 操作在后续迭代已补 `taskmgr_job_action` cancel（`governor.proto CancelJob` + service + tauri + 前端按钮/`task-manager` DOM 测试，见本文件顶部 LMK/G3 条目之后的新条目）；run-now 仍无；桌面 registry 为空时该区自动隐藏（宿主注册 app 后才有内容）。
- **assistant_voice 常驻采集测试去抖动（amos-tauri, 2026-09-05）**：高负载并行跑 `amos-tauri --lib` 时 `resident_capture_streams_speech_and_endpoints_on_trailing_silence`（及其同构 `platform_mic_facade_…`）约 1/5 偶发失败——根因：消费循环「单次 25ms recv 超时即 `break`」，CPU 负载下 worker 两次 push 间偶超 25ms 就在 `AudioEnd` 前提前退出。改为**整体 5s 预算 + 容忍单次空闲超时继续等**（仅收到 `AudioEnd` 或通道关闭 `None` 才停，recv 超时放宽到 50ms）。验证：单线程压测 10/10、全量并行 **6/6**（此前约 1/5 抖）；`amos-tauri --lib` 73、`clippy --all-targets -D warnings` / fmt 干净。
- **Android LMK 端到端验收 runbook（2026-09-05）**：新增 `docs/android-lmk-e2e.md`——先跑无头基线（cargo/frontend 已覆盖逻辑），再列真机/活 Tauri host 的 L2 目视步骤与判定（G1–G5）。诚实标注当前缺口：System UI 无“背景化/触发 LMK”用户入口，G3 需 debug 入口调 `TriggerLmk`/`ApplyHostDecision`；`AMOS_GOVERNOR_MEMORY_PRESSURE=1` 可让 beat 强制回收。README 索引加指向。
- **LMK legacy 表面对账触发扩展（2026-09-05）**: 对账从“仅事件驱动”扩为 **启动即跑 + 周期兜底**。`lib/lmk.ts` 增 `RECONCILE_INTERVAL_MS`(30s) + `startPeriodicReconcile()`（立即 reconcile 一次再每周期一次，返回 stop 清理定时器）；`App.tsx` lmk effect 同时挂 watcher 与 periodic（cleanup 都停）。覆盖「watcher 启动前就 stale 的表面」与「完全错过的事件」。验证：`tsc --noEmit` 干净；全量 ISO 前端套件 `[bun-iso] test OK`（非 bridged 的 effect 提前 return，不启定时器，DOM 测试无泄漏）。
- **LMK legacy 表面对账（reconciliation, 2026-09-05）**: shell 不再只“单向 close”，而是用**权威快照对账**补漏事件。Rust（`amos-tauri`）新增 Tauri 命令 `android_lmk_tasks`（经 daemon `GetLmkSnapshot` 返回存活容器任务 `window_id/package/state`，含重连退避），注册进 `lib.rs`。前端 `lib/lmk.ts`：纯函数 `staleLegacySurfaceLabels(windows, tasks)`（现存 `legacy:*` 中其容器任务已不在存活集者）+ `reconcileLegacySurfaces()`（daemon 快照不可用时不误关：daemon down ≠ 全部死）；`startLmkSurfaceWatcher` 每事件后再 reconcile 一次。验证：`tsc --noEmit` 干净；`lmk.test.ts` **9** 项（+4 对账纯测：补漏、存活保持、无 window_id 不误标、空集）；全量 ISO 前端套件 `[bun-iso] test OK`；`amos-tauri` lib 67、`clippy --lib -D warnings` / fmt 干净。剩余：需活 Tauri host 目视端到端（事件 + 对账）。
- **交付说明文档（2026-09-05）**：新增 `docs/DELIVERY_NOTES_2026-09-05.md`（commit message + 改动清单 + 验证汇总 + 已知限制/遗留 + 后续顺序），并把本会话 LMK-proxy/双向桥/WatchLmk/System-UI 表面拆除整段沉淀；README 文档索引加指向。仿仓库既有 `DELIVERY_NOTES_2026-09-03.md` 格式。
- **LMK/bridge 收尾审计（2026-09-05）**：逐文件核对整条 LMK-proxy + 双向桥 + `WatchLmk` + System UI 订阅。移除两处**无调用方**的公开 API（避免"写了未接线"）：`AndroidManagerService::with_manager_and_lmk`（被 `with_parts`/`with_manager_and_host` 取代、且强制 no-op host 语义误导）、`service::server_with_host`（serve 已改用 `with_parts_and_events`）。更新 `docs/lmk-proxy.md` §6/§8（去掉 `server_with_host`、补 `with_parts_and_events`、验证路径含 amos-tauri 67 与 frontend 5 项）。最终计数：`amos-android` 52+1、`amos-ai` governor 6 + 桥 1 + 反向 1、`amos-tauri` 67、frontend `lmk.test.ts` 5；`clippy --all-targets -D warnings` / fmt / `tsc` / ISO 前端套件全绿。
- **前端 `lmk-surface` 监听 → `wm_close`（`frontend-ts`, 2026-09-05）**: 闭环「容器 Kill → 拆 `amos-wm` legacy 表面」。`src/lib/lmk.ts`：`LMK_SURFACE_EVENT` + `LmkSurfacePayload` + 纯映射 `legacySurfaceLabel`（`legacy:<window_id>`）/`shouldTearDown`（`close_surface` 且带 `window_id`）+ `closeLegacySurface`（`invoke("wm_close",{label})`）+ `startLmkSurfaceWatcher`（订阅 `lmk-surface`，命中即拆除）。`App.tsx` 启动期挂 `startLmkSurfaceWatcher`（仿 hardware-button 订阅 effect，带 cleanup、非 bridged 跳过）。验证：`tsc --noEmit` 干净；新 `__tests__/lmk.test.ts` 5 项纯映射单测；全量 ISO 前端套件 `[bun-iso] test OK`（0 fail）。诚实剩余：端到端需在真起 Tauri host + daemon 上目视验证（GUI 无法离线跑）。
- **System UI 订阅 `WatchLmk`（`amos-tauri`, 2026-09-05）**: 新模块 `crates/amos-tauri/src/android_lmk.rs` —— 仿 `telephony.rs` 的 `spawn_telephony_watch`：`spawn_lmk_watch(app)`（`setup()` 启动）经共享 UDS socket 打开 daemon `WatchLmk` 长流，把每个 `LmkEvent` 转发为 WebView `lmk-surface` 事件（`LmkSurfacePayload{window_id, package_name, kind, close_surface}`），`RECLAIMED`/`DESTROYED` 时 `close_surface=true`，断流带界退避重连。纯映射 `surface_payload` 单测 3 项（reclaimed/destroyed 关表面、frozen/thawed 不关、unknown 无害）。验证：`amos-tauri --lib` 单测 **67** 全绿；`clippy --all-targets -D warnings` / fmt 干净。诚实剩余：**前端 shell 监听 `lmk-surface`** 并把 `close_surface` 映射到 `wm_close("legacy:<window_id>")`（TS crate，需活 Tauri 环境验证）。`docs/lmk-proxy.md` §8 同步。
- **`WatchLmk` 接入 governor-beat：host 主动回收也进同一事件流（`amos-ai`, 2026-09-05）**: 让**所有** LMK 决定都出现在 daemon 的 `WatchLmk` 推送。`serve()` 创建单个共享 `broadcast`，经 `AndroidManagerService::with_parts_and_events` 注入 android 服务，并把 sender 交给 governor beat；`amos-ai::governor_service::drive_host_decisions` 增 `events` 参数，在 reclaimed（先取 `LmkProxy::window_id` 再 destroy）/frozen/thawed 时 `emit_lmk`（`RECLAIMED`/`FROZEN`/`THAWED`）。验证：`amos-ai` 反向驱动集成（`tests/lmk_reverse_drive.rs`）更新为传共享 channel 并断言 beat 回收广播出带 `window_id` 的 `RECLAIMED`；桥集成 + governor lib 全绿；`clippy --all-targets -D warnings` / fmt 干净。剩余：System UI 订阅 `WatchLmk` 并把 `RECLAIMED`/`DESTROYED` 映射到 `wm_close("legacy:<id>")`（UI crate，下一步）。`docs/lmk-proxy.md` §8 同步。
- **`WatchLmk` —— daemon 侧 LMK 事件 server-streaming 推送（`amos-android` + `android_compat.proto`, 2026-09-05）**: 为「daemon→System UI 表面拆除」铺好推送通道。(a) `proto/android_compat.proto` 增 `LmkEventKind{RECLAIMED/FROZEN/THAWED/DESTROYED}` + `LmkEvent{package_name, window_id, kind}` + `AndroidManager::WatchLmk(Empty) → stream LmkEvent`（server-streaming）。(b) `AndroidManagerService` 持 best-effort `tokio::sync::broadcast`（新增 `with_parts_and_events`/`events()`/`emit_event`），在每处**容器侧/System-UI 可见**决定上广播：`TriggerLmk` 的 Kill/Freeze、`ApplyHostDecision` 的 reclaim/freeze/thaw、`OnActivity` Destroy（用新增 `LmkProxy::window_id` 取值带表面）；`WatchLmk` 用 `BroadcastStream` fan-out（`tokio-stream` 增 `sync` feature）。(c) 验证：`amos-android` **52 单测**（+`watch_lmk_streams_reclaimed_event`）+ 1 真 UDS e2e（新增 `WatchLmk` 订阅→`ApplyHostDecision(Reclaim)`→收到 `RECLAIMED` 且带 `window_id`）；`clippy --all-targets -D warnings` 干净；fmt 干净；`amos-ai` 编译通过。诚实剩余：System UI 订阅并把 `RECLAIMED`/`DESTROYED` 映射到 `wm_close("legacy:<id>")`（UI crate，下一步）；daemon governor-beat 的 `drive_host_decisions` 可用 `with_parts_and_events` 接入同一广播（seam）。文档：`docs/lmk-proxy.md` §5/§6/§8、`android-compat.md`、CHANGELOG。
- **邻接审计 + 表面拆除 seam 描述校正（2026-09-05）**：跑跨 crate 回归基线——`amos-wm`（28+2）、`amos-applife`（9+3）、`amos-power`（22+2）、`amos-proto` check、`amos-tauri` lib 对 regenerated proto 客户端编译均绿。审计确认 System UI 侧 `launch_android_app` → `wm.open_surface("legacy:<id>")` 与文档一致，且 `WmState::close`/`wm_close` 已能在状态机层移除 external legacy 表面（`Closed` 对 external 只跳过 host 端真实关闭）。据此把 `docs/lmk-proxy.md` §7 的 seam 措辞从「向 amos-wm 拆除 legacy 表面（需真机）」校正为更准确的「表面拆除原语已存在，真正缺的是 **daemon→System UI 的 kill 推送**（System UI 无订阅 LMK 事件的通道）」。无 Rust 代码改动；其余文档同步。
- **LMK/bridge 审计与补全（2026-09-05）**：对 LMK-proxy 与双向桥做一致性审计并补全两处。 (1) **反向 RPC 的 host 回填缺口**：`AndroidManagerService::apply_host_action`（被 `ApplyHostDecision` RPC 调用）原只作用于容器、不回填 host——若宿主经 wire 主动 reclaim/freeze 而 host 未先动作会失步。现改为按 proxy **实际结果**回填：`Reclaim`→`report_host_killed`、`Freeze`/`Thaw`→`report_host_state(实际态)`（受保护任务被拒冻时如实上报非 Cached，避免把前台 app 误标墓碑）。+2 服务测试：RPC reclaim 上报 `kill`、freeze 保护层不上报 `cached`（后台才 `cached`）。(2) **`drive_host_decisions` 每次 per-id 克隆 managed 集合（O(n²)）**：改为进入循环前快照一次。验证：`amos-android` **51 单测** + 1 真 UDS e2e；`clippy --all-targets -D warnings` / fmt 干净；正向桥、反向驱动、governor lib 全绿。`docs/lmk-proxy.md` 同步。
- **`ApplyHostDecision` RPC —— 反向桥的 wire 入口（`amos-android` + `android_compat.proto`, 2026-09-05）**: 让 System UI / 宿主也能经 gRPC 主动把 host 决定下发容器（镜像 `apply_host_action`），与 daemon beat 的 `drive_host_decisions` 并列。`proto/android_compat.proto` 增 `HostAction` 枚举（FREEZE/THAW/RECLAIM）+ `HostActionRequest` + `AndroidManager::ApplyHostDecision(HostActionRequest)→Empty`；`AndroidManagerService` 实现 `apply_host_decision` handler（映射枚举 + 调 `apply_host_action`），`UNSPECIFIED`→`InvalidArgument`。验证：`amos-android` **51 单测** + 1 真 UDS e2e（新增：relaunch → `ApplyHostDecision(Reclaim)` → snapshot 空 + 记录式 runner 断言第二条 `am force-stop`）；`clippy --all-targets -D warnings` 干净；fmt 干净；`amos-ai` 编译通过。文档：`docs/lmk-proxy.md` §5/§6/§8、`android-compat.md`、CHANGELOG。
- **container ↔ host 反向驱动（daemon 回收决定 → 容器 `am force-stop`，2026-09-05）**: 把桥补成**双向**。(a) `amos-android::lmk` 增 `HostAction{Freeze,Thaw,Reclaim}`；`AndroidManagerService` 增 `with_parts(manager,proxy,host)`（daemon 共享同一批实例）与 `apply_host_action`（反向落地到 proxy + manager force-stop）。(b) `amos-ai::governor_service::GovernorLmkHost` 记录「容器管理」包集合（首注册加入、kill/反向回收移除，`managed_ids()`）；新增 `drive_host_decisions(outcome, host, proxy, manager)`——只对容器管理 app 执行：`reclaimed`→真 `am force-stop`+proxy 移除、`frozen/thawed`→proxy 镜像 tier，host-native app 不误杀。(c) `amos-ai::server.rs::serve()` 用共享 proxy/manager/host 构建 android 服务（`with_parts`），并在 governor beat 每次 `observe` 后调用 `drive_host_decisions` 反向驱动。验证：`amos-android` **47 单测**（+`host_state`/`HostAction` key + `host_reclaim_drops_the_container_task`）+1 真 UDS e2e；`amos-ai` 新增集成 `tests/lmk_reverse_drive.rs`（共享 governor + 记录式 `WaydroidRuntime`：`observe(memory_pressure)` 同时回收容器 WeChat 与 host-native "notes"，反向只对 WeChat 下发 `am force-stop com.tencent.mm`、不误杀 notes，proxy 清空、managed 清空）；桥集成 + 反向集成 + governor lib 全绿；`clippy --all-targets -D warnings` 干净；fmt 干净。诚实剩余 seam：`amos-wm` `legacy` 表面拆除、no-UI `SurfaceControl` teardown、host thaw→容器 surface 唤醒（`docs/lmk-proxy.md` §8）。
- **container ↔ host LMK 桥（`MoveApp` 上送 daemon `ResourceGovernor`，2026-09-05）**: 打通两套 registry。(a) `amos-android::lmk` 增纯映射 `host_state`（host 无可设 `Visible`，容器 `Visible`→host `Foreground` 以保受保护）与 `LmkHost` sink trait + 默认 `NoopLmkHost`。(b) `AndroidManagerService` 增 `with_manager_and_host`/`with_runtime_and_host` + `server_with_host`，并在每处容器转移上报：`launch`→`report_state(Foreground)`、`OnActivity`→`report_state(host_state(tier))`（Destroy→Stopped）、`TriggerLmk` 的 `Freeze`→`report_state(Cached)`、`Kill`→`am force-stop`+`report_killed()`。(c) `amos-ai::governor_service::GovernorLmkHost` 实现 `LmkHost`，绑定**同一个** `Arc<Mutex<ResourceGovernor>>`（现状同则跳过防 LRU 失真；未注册先 `register_app` 再 `move_app`；`kill_app` 移除）。(d) `amos-ai::server.rs::serve()` 用 `server_with_host(runtime, GovernorLmkHost)` 替换原 `server(runtime)`，android 服务与 `Governor` 服务、周期 beat 共享同一 governor。验证：`amos-android` **46 单测**（新增 `host_state` 映射 + 2 项假 `RecordingHost` 上报测试：launch/background/kill、`Pause`→host `foreground` 且永不见 `visible`）+ 1 真 UDS e2e；`amos-ai` 新增**集成测试 `tests/lmk_governor_bridge.rs`**（同一共享 governor 下，launch→host `register→Foreground`、OnActivity Stop→`MoveApp→Background`、TriggerLmk Critical→`kill→移除`，经 `Governor::GetState` 读回一致）；`clippy --all-targets -D warnings` 干净；fmt 干净。诚实方向：这是**容器→daemon 单向 advisory 上送**；反向（host `observe` 决定驱动容器物理 kill / 解冻唤醒 surface）仍留后续（`docs/lmk-proxy.md` §8）。
- **LMK-proxy `Kill` 真正执行到容器 (`amos-android`, 2026-09-05)**: 把上一步留作 seam 的「物理 kill」落地为可离线验证的容器下发。(a) `AndroidController::force_stop` → `waydroid shell am force-stop <pkg>`（`CommandRunner` 注入、可测）。(b) `AndroidRuntime` trait 增默认 `force_stop`（不可杀则 `Err`）；`WaydroidRuntime` 委托 controller、`DemoRuntime` 记录 `stops()`（+访问器）。(c) `EnhancedAndroidManager::force_stop_app`（镜像 `launch_app` 的超时保护）。(d) `service::trigger_lmk` 对每个 `Kill` victim 经 manager 真下发 `am force-stop`（先放锁再 await，best-effort：失败仅日志、不中断整轮）。验证：controller +2、runtime +1、既有 = **43 单测**；真 UDS e2e 用**记录式 `FakeRunner`** 同链断言「launch → `waydroid app launch`、Kill → `waydroid shell am force-stop com.tencent.mm`」；`clippy --all-targets -D warnings` 干净；fmt 干净；`amos-ai` 编译通过。诚实边界：容器侧 Kill 已接线；仍待真机的是 `amos-wm` `legacy` 表面拆除与 no-UI 基座 `SurfaceControl` teardown（`docs/lmk-proxy.md` §7）。
- **Android LMK-proxy / Activity-Task 生命周期代理 (`amos-android/src/lmk.rs`, 2026-09-05)**: 把「容器内 legacy APK 的生死」收到 AmOS 控制下，代理 Waydroid/容器的静默 `lmkd`。(a) **Activity/Task 状态机 + LMK-proxy 纯逻辑**：每启动包一条 `Record`（`window_id`/top-Activity 生命周期/前台服务标记/冻结 `Cached` 标记/LRU `seq`），派生重要性 tier 复用 `amos_applife::AppState`（rank/key 与 `governor.proto` 零漂移）；`launch/resume/pause/stop/destroy/start_service/stop_service/freeze/thaw`。(b) **内存压力裁决**：`plan`（只读不改）/`apply`（决定并就地转移）→ `MemoryPressure{None,Low,Critical}`：Low 冻结 LRU `Background→Cached`（不杀），Critical 回收（杀）LRU `Cached→Background`；保护层（Foreground/Visible/ForegroundService）永不被选；victim 带 `window_id` + `action`。物理 kill/表面 teardown 留作调用侧 seam（`am force-stop` + `amos-wm` teardown，需真机）。(c) **gRPC 接线（additive）**：`proto/android_compat.proto` 新增 `OnActivity`/`GetLmkSnapshot`/`TriggerLmk` + `ActivityEvent`/`MemoryPressure` 枚举；`AndroidManagerService` 持 `Arc<Mutex<LmkProxy>>`，`launch_android_app` 成功即收养该包为前台任务。验证：lmk 12 + service 5 + 既有 24 = **41 单测**全绿，真 UDS e2e 一轮 `launch→OnActivity(Stop)→GetLmkSnapshot→TriggerLmk(Critical)`（victim `window_id`/`killed=true` 就位）；`clippy --all-targets -D warnings` 干净；fmt 干净；`amos-ai` 编译通过。设计/诚实边界：`docs/lmk-proxy.md`。
- **Audit coverage for the new split/demo wrappers (frontend-ts, 2026-09-04)**: added `wmSplitDemo` wrapper coverage (calls `wm_split_demo` and returns its snapshot) and asserted `LAYOUT_CHANGED_EVENT === "layout-changed"` matches the Rust host constant. `wm.test.ts` **15 pass**; `tsc --noEmit` clean. Rust `wm_split_cycle` integration still 1 passed.
- **`SplitDemoButton` dev trigger (frontend-ts, 2026-09-04)**: an out-of-shell dev control (meant for a real multi-window dev window, not the single-page `App.tsx`) that runs `wm_split_demo` on click and reports `idle → running → done/offline/error`. `components/SplitDemoButton.tsx` is presentational + bridge-only (DOM-testable): `__tests__/split-demo-button.test.tsx` (2) covers the click → `wm_split_demo` → `done` path and the offline (`null`) path. Frontend ISO runner `test OK`; `tsc --noEmit` clean.
- **GUI split demo entry (`amos-tauri` + frontend, 2026-09-04)**: `wm_split_demo` (async Tauri command) drives the real multi-window host through a visible cycle — picks the two front split candidates, `enter_split(vertical)`, then animates `resize(40) → swap → resize(60) → swap`, and finally `exit` (maximizes the two windows); each step runs through `finish_layout` (applies to real windows + broadcasts `layout-changed`). Registered in `lib.rs`; frontend `lib/wm.ts` adds `wmSplitDemo()` wrapper. Compile + clippy (`-D warnings`, default & `--features android`) + fmt clean; `tsc --noEmit` clean. Visually verifying needs a live multi-window Tauri app (dev window calls `invoke('wm_split_demo')`).
- **Headless split closed-loop test (`amos-tauri/tests/wm_split_cycle.rs`, 2026-09-04)**: drives the full split cycle at the state/geometry layer — `enter_split(notes,maps,vertical)` → `split_resize(40)` → `split_move(+10)` → `split_swap` → `split_exit` — asserting per-step invariants (panes disjoint & in-screen, primary/secondary mapping flips on swap, split + `split_labels` cleared on exit). `cargo test -p amos-tauri --test wm_split_cycle` passes; `clippy --all-targets -D warnings` clean; fmt clean. Real window geometry still needs a live multi-window Tauri host (this verifies the decisions the host applies).
- **Surfaces rearrange on `layout-changed` (frontend-ts, 2026-09-04)**: proves the shell's surfaces self-rearrange when the host broadcasts a layout change. `useWindowLayout` already auto-syncs its snapshot from the event; wired through `SplitFrame`, `__tests__/split-rearrange-on-event.test.tsx` mounts a harness, then fires a mocked `layout-changed` with a *swapped* snapshot and asserts each surface moved to its new pane rect — with **no extra command** (only the initial `wm_layout_snapshot`). Frontend ISO runner `test OK`; `tsc --noEmit` clean.
- **Swap focuses primary + `layout-changed` broadcast (`amos-tauri` + frontend-ts, 2026-09-04)**: (a) `wm_split_swap` now gives the **new primary** window OS focus (input routing follows the swap). (b) Every mutating layout command goes through `finish_layout`, which applies the split to real windows and **broadcasts a `layout-changed` event** with the authoritative `LayoutSnapshot` so non-initiating windows/surfaces refresh without their own round-trip (`LAYOUT_EVENT` in `wm.rs`; wired into `wm_layout_set_screen`, `wm_split`, `wm_split_resize`, `wm_split_move`, `wm_split_swap`, `wm_split_exit`). Frontend: `lib/wm.ts` adds `onLayoutChanged(handler)` (subscribe + normalize, defensive on non-Promise `listen`); `useWindowLayout` auto-syncs its snapshot from the event. Tests: Rust `wm` lib 6; frontend ISO runner `test OK`; `tsc --noEmit` clean.
- **Primary/secondary split hints for the host/UI (frontend-ts, 2026-09-04)**: `lib/wm.ts` adds pure `primaryWindow(snapshot)` / `secondaryWindow(snapshot)` so a shell can tell which window is the primary pane (swap-aware — they follow the snapshot's `split.primary`). `useWindowLayout` now also returns derived `primary`/`secondary` hints. Tests: `wm.test.ts` **13 pass** (was 12; +1 swap-aware hint case), `tsc --noEmit` clean.
- **`wm_split_exit` restores the split windows (`amos-tauri/src/wm.rs`, 2026-09-04)**: exiting a split now returns the two windows to fullscreen instead of leaving geometry to the host. Added app-free `WmState::split_labels()` (the two labels currently split, pane order); `wm_split_exit` reads them before clearing, then `WebviewWindow::maximize()`s each (honest "back to full" fallback when no exact pre-split geometry was captured). Regression asserts `split_labels` = the pair after `enter_split` and `None` after `exit`. `wm` lib tests still 6, clippy `-D warnings` clean (incl. `--features android`), fmt clean.
- **Split applies to real windows (`amos-tauri/src/wm.rs`, 2026-09-04)**: Option-B multi-window-host step. `WmState::apply_split_to_real(app)` maps each split pane's `Bounds` onto its **real Tauri `WebviewWindow`** (`set_position` + `set_size`, logical px); external surfaces / not-yet-created windows are skipped and per-window failures are ignored (best-effort, honest no-op when no split). The layout commands now take `AppHandle` and re-apply geometry after every mutation: `wm_split`, `wm_split_resize`, `wm_split_move`, `wm_split_swap`, `wm_split_exit`, `wm_layout_set_screen`. Existing app-free state methods/tests are unchanged (`wm` lib tests 6, clippy `-D warnings` clean incl. `--features android`, fmt clean). Real window geometry is compile-checked only — live verification needs a running multi-window Tauri app.
- **Audit + end-to-end hook test for the new split UI (frontend-ts, 2026-09-04)**: audited `lib/wm.ts` / `components/SplitFrame.tsx` / `lib/useWindowLayout.ts` — no logic fixes needed. Added the missing DOM coverage proving the hook drives the layout: `__tests__/use-window-layout.test.tsx` mounts a harness using `useWindowLayout` with a fake `__TAURI_INTERNALS__` bridge, asserts the snapshot loads as `split` (via `wm_layout_snapshot`), then a click on `actions.exit()` round-trips `wm_split_exit` and the state becomes `fullscreen`. Validated with the repo's sanctioned ISO runner (`scripts/bun-iso-test.mjs test` → `[bun-iso] test OK`, per-file DOM isolation) and `tsc --noEmit` clean. Note: a raw all-at-once `bun test` shows many pre-existing DOM failures that are the documented shared-happy-dom-window artifact, not a regression.
- **Shell split rendering — `SplitFrame` + `useWindowLayout` (frontend-ts, 2026-09-04)**: renders two surfaces side by side from the layout snapshot. (a) `lib/wm.ts`: pure `layoutSurfaces(snapshot, labels)` → ordered `SurfacePlacement[]` (split → the two panes; fullscreen → each visible surface whole; non-split windows excluded so they never cover the stage). (b) `components/SplitFrame.tsx`: presentational stage (no Tauri) that mounts only visible surfaces positioned via `paneCss`; DOM-tested (`__tests__/split-frame.test.tsx`) to place `notes`(430px)⇆`maps`(x=438) and drop `photos`. (c) `lib/useWindowLayout.ts`: shell hook owning the snapshot + actions (`setScreen`/`split`/`resize`/`move`/`swap`/`exit`/`refresh`), each replacing state with the authoritative command reply. Frontend tests now 14 pass (wm.test + split-frame.test) + `tsc --noEmit` clean.
- **Audit of the new WM frontend lib (`frontend-ts/src/lib/wm.ts`, 2026-09-04)**: `rectForLabel` previously returned a **fullscreen rect** for *any* label when a split was active — so a third (non-split) surface rendered whole over the split. Now, while a split is active, a surface that isn't one of the two split windows is **hidden** (`rectForLabel` returns `null`); a new `isVisibleSurface(snap, label)` makes the rule explicit (split → only the two panes visible; fullscreen → whole). Updated docs + tests (+1 case: non-split window is hidden, not fullscreen). `wm.test.ts` **11 pass / 28 assertions**; `tsc --noEmit` clean.
- **Frontend multi-window layout lib (`frontend-ts/src/lib/wm.ts` + test, 2026-09-04)**: wraps the Tauri `wm_*` layout/split commands for the UI and adds the pure model to render a split screen. `lib/wm.ts`: typed `LayoutSnapshot`/`SplitLayoutInfo`/`PaneLayout` + wrappers `wmLayoutSnapshot`, `wmLayoutSetScreen`, `wmSplit`, `wmSplitResize`, `wmSplitMove`, `wmSplitSwap`, `wmSplitExit`, `wmSplitCandidates` (return `null` when not inside Tauri, matching other lib modules); `normalizeLayout` (tolerates absent/partial payloads, clamps divider `percent` to 1..=99) and render helpers `splitActive`/`isInSplit`/`paneFor`/`rectForLabel` (pane rect, or full-screen when not split)/`paneCss`/`describeSplit`. Tests: 10 (`__tests__/wm.test.ts`, 23 assertions) — wrappers issue the right command+args and return payload/null-offline, normalize robustness, pane geometry mapping. `bun test` + `tsc --noEmit` clean. Honest boundary: rendering actual two-app surfaces side-by-side still needs a parallel surface model in the shell (see `docs/multi-window.md`); this lib is the command/geometry seam a shell uses to place each surface.
- **Multi-window layout command bridge (`amos-tauri/src/wm.rs`, 2026-09-04)**: exposes the `amos-wm` layout/split domain to the frontend as Tauri commands on the managed `WmState`. New `WmCore` state: `screen` (`Bounds`) + optional `SplitScreen`. App-free methods: `screen`/`set_screen` (a screen change that would silently re-clamp an active split instead ends it), `enter_split(a,b,axis)` (both registered, distinct, non-Launcher; refuses when two min panes don't fit), `split_resize(percent)`/`split_move(delta)` (reject infeasible divider moves), `split_swap`, `split_exit`, `split_candidates_labels`, `layout_snapshot`. Commands: `wm_layout_snapshot`, `wm_layout_set_screen`, `wm_split`, `wm_split_resize`, `wm_split_move`, `wm_split_swap`, `wm_split_exit`, `wm_split_candidates` (serializable `LayoutSnapshot`/`SplitLayoutInfo`/`PaneLayout` with per-window rects + candidates). +2 unit tests (split round-trip; bad-input / shrink-ends-split). `amos-tauri` lib **64 passed**; clippy `-D warnings` clean (default + `--features android`); fmt clean. Honest caveat: this is the layout *state + command* surface — mapping the returned panes onto real per-window geometry (multi real windows or an in-shell renderer) remains the frontend/host step (see `docs/multi-window.md`).
- **Audit of the new WM geometry domain (`amos-wm/src/{layout,split}.rs`, 2026-09-04)**: found and fixed a divider-honesty gap in `SplitScreen::set_screen`. It validated a new screen only via the lenient `split_panes` (which silently clamps the divider to the min-feasible band), so after `resize_to` a *narrower/rotated* screen could be accepted with `percent()` no longer matching the real geometry. Now `set_screen` also requires the current percent to be feasible on the new screen (`percent_feasible`), rejecting it otherwise and keeping the old screen — matching `resize_to`'s never-silently-clamp rule. +1 regression test (`set_screen_never_silently_clamps_the_divider`). amos-wm lib **28 passed**; `clippy -D warnings` clean; fmt clean.
- **Split-screen session + WM split candidates (`amos-wm/src/split.rs`, 2026-09-04)**: binds two [`WindowId`]s to the two panes of a [`layout::split_panes`] layout as a session. `SplitScreen::new` (50/50, `None` if two min panes + gap don't fit), `bounds`/`bounds_for(id)`, `resize_to`/`resize_by` (a divider move that would put either pane below `min` is **rejected, never silently clamped**, so `percent()` always matches real geometry — an audit fix surfaced by the first test), `swap`, `set_screen` (only applied when it still fits), `contains`/`other`. Also `WindowManager::split_candidates()` returns the front two shown non-Launcher windows to seed a split. +7 tests. amos-wm lib **27 passed**; `clippy -D warnings` clean; fmt clean.
- **Desktop geometry / split-screen domain (`amos-wm/src/layout.rs`, 2026-09-04)**: adds the pure-`std` decision layer behind the roadmap's free-resize + split-screen features. `Bounds`/`Size` with `enforce_min` (a resize never shrinks below minimum) and `clamp_into` (a dragged/resized window stays on-screen); `split_panes(screen, axis, gap, percent, min)` returns two disjoint, min-sized panes (Vertical left/right or Horizontal top/bottom) or `None` when the screen cannot host two minimum panes + gap (never a zero/negative pane). Transport-agnostic, matching the WM crate convention — the `amos-tauri` host maps `Bounds` onto the real backend. +6 unit tests. amos-wm lib **20 passed**; `clippy -D warnings` clean; fmt clean.
- **Multi-window WM audit (`amos-wm`, 2026-09-04)**: audited the transport-agnostic window state machine and fixed a state-consistency bug in focus fallback. (a) When the **focused** window was hidden/closed, focus fell back to the previous window but left it in `Shown` state — so `focused()` pointed at a window whose `state_of` was not `Focused` and **no** window was `Focused` (model/state disagreement). Now `promote_fallback_after` promotes the new top of the recents stack to `Focused` on hide/close. (b) Fixed a follow-on: closing a **non-focused (background)** window no longer leaves a stale dead id in the recents/`z_order` (close now always purges the stack entry). (c) Strengthened `assert_wm_invariants` to require `focused()` ⇔ exactly one window in the `Focused` state (both directions) so this class of bug is caught by the 2000-step property test. +3 regression tests (hide promotes fallback to Focused; close promotes Launcher when nothing left; closing a background window purges recents). amos-wm lib **14 passed** (incl. the strengthened property test); `clippy -D warnings` clean; fmt clean; `amos-tauri` consumer compiles.
- **Security & granular privacy audit (`amos-ai/src/security.rs` + new `privacy.rs`, 2026-09-04)**: audited the daemon client-authorization layer and added the sensitive-resource Permissions-Manager domain seam. (a) **Bug fixes in `security.rs`**: `TokenBucket::refill` did `now - last_refill` — an unsigned underflow that panics (debug) if the wall clock regresses — now `saturating_sub`/`saturating_mul`/`saturating_add`; `log_tokens` ignored `check_tokens` and audited **Success** even when a client was over its token quota — now records `Rejected` for over-quota (honesty). (b) **New `privacy.rs`**: a deny-by-default, per-principal capability store over sensitive `Resource`s (`Microphone`/`Camera`/`Contacts`/`Location`/`Storage`) with `grant`/`revoke`/`revoke_all`/`authorize`/`is_granted`/`recent_audit` (bounded runtime audit trail) — the granular Permissions-Manager / third-party-sandbox domain core the System UI will consume (runtime interception of a third-party APK is device/ecosystem work, not faked here). Verified: `amos-ai` lib **118 passed** (was 109; +7 privacy +2 security); `clippy -D warnings` clean; fmt clean.
- **Deep-audit of the accelerator domain (`amos-ai/src/accelerator.rs`, 2026-09-04)**: audited `SoCVendor`/`Accel`/`AccelProfile` and its consumers. Findings documented in-module as honest seams (not silently re-wired, to avoid breaking the repo's deliberate host `--features qnn,neuropilot` cross-compile checks): (1) `effective()/compiled_in()` treat Vulkan/Metal/NNAPI as target-agnostic, so a requested `nnapi`/`metal` off its native OS is reported as chosen, and `auto`+Android→NNAPI means llama.cpp GPU offload rather than a true NNAPI runtime — kept as a device-bring-up decision; (2) `Config.enable_acceleration` (`AMOS_ACCELERATION`) and `AMOS_ACCEL` are two independent knobs whose wiring is pending. Added 5 env-race-free unit tests locking the honest behaviour (resolve CPU/Off/Vulkan/Metal reasons, `enabled:false` never offloads, explicit zero offload for CPU/Off, vendor/accel label, Auto always resolves). accelerator tests now **11** (of 109 `amos-ai` lib); `clippy -D warnings` clean under default and `--features qnn,neuropilot`; fmt clean.
- **Deep-audit of the Option A/B seam code (`amos-audio/src/source.rs`, `amos-sensor/src/stream.rs`, 2026-09-04)**: (a) `amos-audio::source`: add `PlatformMicKind::Mock` to disambiguate an *explicitly injected* host/dev capture (`from_mock` now yields `Mock`) from `Host` (merely "no native backend compiled"); labels/is_native updated with tests. (b) `amos-sensor::stream`: corrected the module doc (only counters/rates are `Atomic`; payload stores are `Mutex`-guarded) and added detach-staleness handling — `ImuLatest::clear()`, `FrameLatest::clear(id)`/`clear_all()`, and `LiveSensorProvider::clear_samples()` (clears samples, keeps advertised cameras) so a glue detach/re-attach never serves stale pre-detach data; +3 tests. Verified: amos-audio 18, amos-sensor **34 lib + 1 UDS e2e**; `clippy -D warnings` clean (incl. `amos-sensor --features android`, `amos-tauri --features android`); fmt clean; `amos-tauri` lib still 62.
- **Kotlin/NDK 生产者 glue 骨架 (`amos-tauri`, 2026-09-04)**: 把 IMU/Camera 的 System UI 真机采集侧（上轮读侧已就绪）落成结构交付。 (a) **Kotlin 模板** `crates/amos-tauri/android-glue/com/amos/ai/glue/`: `SensorGlue.kt`（`SensorEventListener` accel/gyro → native `recordImu`）、`CameraGlue.kt`（`Camera2` 预览 → NV21 `ImageReader` → native `recordFrame`）、`Nv21.kt`（YUV_420_888→NV21）、`AmosGlue.kt`（权限 + attach/detach facade）。(b) **Rust 回调 stub** 新增 `#[cfg(feature="android")] pub mod android_glue;`：`GLUE_BUS: OnceLock<Arc<LiveSensorProvider>>` + `arm(bus)`/`is_armed()` + 两个 `#[no_mangle]` JNI 上送 `Java_com_amos_ai_glue_SensorGlue_recordImu`（标量）与 `..._CameraGlue_recordFrame`（解码 `jbyteArray` 进 store；未 arm 即诚实 no-op）。(c) `SensorHost::producer()` 暴露同一 bus 供 boot `arm`。 验证: Rust 半侧 `cargo check/clippy -p amos-tauri --features android --lib -- -D warnings` 绿 + `android_glue` 2 项纯逻辑单测；Kotlin 半侧结构性交付（本机无 SDK/真机，须 `tauri android init` + 真机验证，JNI 签名按 `com.amos.ai.glue.*`）。设计: `docs/android-glue.md`。
- **System UI 装配点命令入口 — 设备传感器 host + 常驻 mic (`amos-tauri`, 2026-09-04)**: Option 1+2 的 Rust 侧装配。 (a) `sensor_host.rs`: managed `SensorHost`（`amos_sensor::LiveSensorProvider` 流桥 + `SensorManager` 能量策略，与 daemon 无关的 System UI 读侧）+ 命令 `sensor_host_snapshot` / `sensor_host_set_mode` / `sensor_host_record_imu(accel,gyro,temp 数组)` / `sensor_host_record_frame(id,w,h,fmt,fps,bytes)` / `sensor_host_acquire(kind,hz)`；`#[cfg(android)] bind_android(vm,env,context)` 用真 `AndroidSensorProvider` 把同步 `LocationManager` GNSS 接进 snapshot.gnss。 (b) `assistant_voice.rs` 内 `DeviceMic` + 命令 `device_mic_start`（`amos_audio::PlatformMic::open_device()` 开真 AAudio/TinyALSA → daemon `Chat` → 常驻采集线程）/ `device_mic_stop` / `device_mic_status`；`open_platform_mic()` host 上诚实报“无原生麦”。 (c) amos-tauri 新增依赖 `amos-sensor`，`android` feature 转发其 `android`。 验证: `cargo test -p amos-tauri --lib` **61 passed**（原 54，+4 sensor_host +3 device_mic）；`cargo check/clippy -p amos-tauri --lib --features android -- -D warnings` 绿；fmt clean。设计: `docs/device-assembly.md`。剩余(生产者 glue, 需真机, 见该 doc §4): Kotlin/NDK `SensorEventListener`+`Camera2 ImageReader` 推进 `record_*`、引导时真 `Context` 绑 `bind_android`。
- **Latest-sample stream bridge for camera/IMU — real backend + host assembly (`amos-sensor`, 2026-09-04)**: closes the gap where camera/IMU reads returned “not yet wired” with no place for samples to land. (a) New `crates/amos-sensor/src/stream.rs`: thread-safe latest-sample stores `ImuLatest` (newest only; out-of-order pushes dropped) and `FrameLatest` (per-camera; payload validated against `CameraConfig::frame_len`; monotonic `seq`), plus a non-gated `LiveSensorProvider` (`Send + Sync`, implements `SensorProvider`) that reads them — the read-side a device HAL or host/dev feeder pushes into; honest “no sample yet” before the first push, never a fabricated value. (b) `AndroidSensorProvider` now owns a `LiveSensorProvider`: its `camera_configs`/`camera_capture`/`imu_rate_hz`/`imu_sample` delegate to it (GNSS stays real synchronous `LocationManager` JNI) and it exposes producer entries `set_cameras`/`set_imu_rate_hz`/`record_imu`/`record_frame` (+ `imu_store`/`frame_store`) for the on-device `SensorEventListener` / `Camera2` `ImageReader` glue. (c) Host assembly tests (6): cross-thread push → `SensorManager` reads latest, honest before-first error, frame validation + `seq`, `CameraNotFound`, PowerSave gating layered on the live provider. Verified: `cargo test -p amos-sensor` 31 lib (was 25) + 1 UDS e2e; `cargo check/clippy -p amos-sensor --features android --all-targets -- -D warnings` green; fmt clean. Remaining (device-only, producer-side glue in the System UI APK): register `SensorEventListener` + `Camera2`/`ImageReader` and push via `record_*` — see `docs/sensors.md` §6–§7.
- **Platform-mic facade — AAudio/TinyALSA wired into the resident voice loop (`amos-audio` + `amos-tauri`, 2026-09-04)**: Option-A device-bring-up seam that closes "the AAudio capture exists but nothing can hand it to the always-on worker". (a) `AudioCapture` is now **object-safe** (`amos-audio/src/capture.rs`) — its only generic default (`for_each`) moved to a blanket `AudioCaptureExt`, so a capture can live behind `Box<dyn AudioCapture>` (host-validated; the sole other consumer, `amos-tauri`, is unaffected). (b) `unsafe impl Send` on `AAudioCapture`/`AAudioSink`/`TinyAlsaCapture`/`TinyAlsaSink` (sole-owner handoff) so a real device capture satisfies the resident worker's `Send` bound. (c) New `amos-audio::source` facade: `PlatformMicKind::detect()` (honest, `feature`+`target_os`: AAudio preferred over TinyALSA on Android, `Host` on a laptop) + `PlatformMic` (`Send`, one `AudioCapture`) with `open_device()` (opens the real mic at 16 kHz; a host returns a clear "no native backend, use from_mock" error — no silent fake) and `from_mock()` for host/dev/CI. Re-exported at crate root. (d) Host proof the seam feeds `spawn_resident` (`assistant_voice.rs::platform_mic_facade_drives_the_resident_worker`): a `PlatformMic::from_mock` capture streams `Audio` and force-finalizes with `AudioEnd`. Tests: amos-audio 18 (was 13), amos-tauri voice 13, `clippy -D warnings` + `fmt --check` clean on host. Remaining (device-only, per `docs/audio-hal-bridge.md`): NDK cross-build + real-device `open_device()` acceptance.
- **提醒事项 Reminders app (`frontend-ts`, 2026-09-04)**: the iOS-style Reminders (提醒事项) system app. Pure domain kernel `src/lib/reminders.ts` (colour-coded custom lists + built-in inbox, due / all-day scheduling, priority 无/低/中/高, flags, smart views 全部/今天/计划/旗标/已完成, overdue/reached math, demo seed, self-healing `normalize*`, shared `amos.*` persistence, included in the Settings cloud/local backup snapshot); UI `src/components/RemindersApp.tsx` (smart-list + list chips, compose/edit form with native date/time/all-day/priority/flag/list, inline complete-circle toggle, list create/delete). Reaching a due time fires an **OS-wide alert**: new `lib/reminderNotify.ts` (`useDueReminderAlerts`, mounted once in the `Shell`, alongside `useNotificationAlert`) reconciles every ~15 s + on store changes and fires **one** app notification per reminder the first time its due instant is reached, using idempotent persisted `amos.reminderFired` markers — so the badge / arrival banner / haptic / ring work on **any** screen (home, other apps, locked), not only while the Reminders app is open, and opening the app clears them like Messages/Mail, and alerts are suppressed while the Reminders app itself is focused (items already on screen). Registered in `APPS`/`COMPONENTS`; `en`/`zh` i18n. Tests: 8 unit (`reminders.test.ts`) + 5 DOM (`reminders-dom.test.tsx`) + 8 notifier (5 `reminderNotify.test.ts` + 4 `reminderNotify-dom.test.tsx`) + SSR smoke + i18n parity.
- **提醒事项 audit fixes (`frontend-ts`, 2026-09-04)**: editing a scheduled reminder now opens its date/time editor (previously hidden), and the 提醒我 toggle now also appears in edit mode so an unscheduled reminder can gain a schedule while editing; clearing the notes on edit now truly deletes them (a blank no longer keeps old notes); reminders + reminderLists are included in the Settings cloud/local backup snapshot (`SYNC_STORES`). DOM tests cover the edit-schedule + clear-notes fixes.
- **提醒事项搜索 (`frontend-ts`, 2026-09-04)**: in-app search — pure `searchReminders` (title/notes, case-insensitive) plus a header 🔍 toggle that opens a filter narrowing the current smart/list view, with a live match count and ✕ clear; the empty state now distinguishes “无匹配” from “暂无”. i18n zh/en (`reminder.search/noMatch/matches`). Tests: 1 unit + 1 DOM.
- **提醒事项 三连完善 (`frontend-ts`, 2026-09-04)**: (1) **per-list collapsible “已完成”** — inside any list view (incl. the inbox) a header toggles a greyed-out completed group (iOS “show completed”), still un-completable/deletable; (2) **“✓ 全部完成”** quick action completes every pending reminder of the current smart view/list; (3) a past-due reminder can **“⏰ 推迟 1 小时”** from its edit form, pushing dueAt +1 h so the OS notifier re-alerts at the new time. i18n zh/en (`reminder.completeAll` / `reminder.snooze`). DOM tests +3.
- **提醒事项专属图标 (`frontend-ts`, 2026-09-04)**: `AppIcon.tsx` adds an opt-in bespoke first-party face — `isBespokeTile(id)` — so the Reminders tile now renders an Apple-inspired white card + coloured checklist (single tint-independent SVG) instead of the generic emoji-on-gradient face. All other apps keep the uniform tonal tiles unchanged (no drift).
- **语音备忘录 Voice Memos app (`frontend-ts`, 2026-09-04)**: new iOS-style recorder. Pure `lib/voiceMemos.ts` (metadata CRUD, formatting, self-healing normalize + a deterministic WAV generator for demo/test clips) persists `amos.vmemos`; `lib/voiceRecorder.ts` is a clean seam over MediaRecorder/getUserMedia (unavailable/denied errors); `components/VoiceMemosApp.tsx` — big red record/stop with live clock, play/pause via one shared <audio>, inline rename, delete, empty state; capture gated by the OS mic permission ledger. Audio is stored as a **BINARY Blob** in a MediaStore (`lib/mediaStore.ts`: IndexedDB in the shell, in-memory fallback for tests) — codec-compressed (Opus), **no base64 inflation**, and the durable KV store keeps only metadata (`amos.vmemos`). Regenerable demo seeds store no audio bytes at all. Bespoke Apple-style icon: white card + red waveform (`AppIcon` bespoke face). en/zh i18n. Tests: 6 unit + 4 DOM + 2 media-store + SSR smoke. Audit fixes: IndexedDB writes now resolve only when the transaction commits (durable), and navigating away mid-recording stops & releases the microphone. Added (dev-dep `fake-indexeddb`) a real IndexedDB-backend binary round-trip test, and an end-to-end `fake mic → MediaRecorder seam → binary MediaStore → byte-identical read-back` integration test.
- **备忘录 iOS 观感对齐 (`frontend-ts`, 2026-09-04)**: 备忘录列表改为 iOS 样式——普通备忘录折叠为「加粗标题行 + 摘要预览 + 相对时间(今天·昨天·日期) + 置顶/清单标记」，点击展开全文/操作；含清单的备忘录始终展开以便勾选。Apple 黄色便签专属图标（`AppIcon` bespoke face）。新增纯助手 `noteTitle/notePreview/noteDayOf`（`lib/notes.ts`）；i18n `note.yesterday/open/untitled`。Tests: 3 新行助手单测 + 1 DOM（折叠→展开）。
- **QCOM/MTK on-device inference accelerator domain (`amos-ai`, 2026-09-04)**: new `crates/amos-ai/src/accelerator.rs` turns the previously-dead `Config.enable_acceleration`/NPU narrative into a resolved, honest per-chip profile. `SoCVendor::{Qualcomm,MediaTek,GenericAndroid,Host}` + `detect()` (`AMOS_SOC_VENDOR` override; never guesses a vendor on a non-Android host); `Accel::{Auto,Cpu,Vulkan,Metal,Nnapi,Qnn,NeuroPilot,Off}` + `AccelProfile` resolving `AMOS_ACCEL` → on Android `auto` picks **NNAPI** (the portable NPU shared by Snapdragon & Dimensity), macOS → Metal, else CPU; `resolve()` reports `(effective, reason)` so an uncompiled vendor SDK (QNN/NeuroPilot, gated by the new `qnn`/`neuropilot` features) is honestly downgraded to CPU rather than claimed; `llama_args()`/`n_gpu_layers()`/`ollama_hint()` for a llama.cpp/Ollama local runtime. Daemon logs the resolved `vendor/accel/llama_layers` (or the downgrade reason) at startup. Unit tests (6). Design/acceptance: `docs/qcom-mtk-bringup.md`.
- **Honest local GGML engine — `AMOS_GGML_STRICT` (`amos-ai`, 2026-09-04)**: when `AMOS_GGML_STRICT=1` an unavailable GGML engine/model is a surfaced error instead of a silent mock fallback (real deployment never mistakes mock for inference); default stays `off` so the offline/CI fallback (`ggml_command_e2e`) is preserved. Unit test covers strict-error + permissive-fallback.
- **`amos-ai` `qnn`/`neuropilot` feature flags**: gate the vendor-NPU claims in `accelerator.rs` (`compiled_in()`), mirroring the `asr-sherpa` gating pattern.
- **Local GGML engine consumes the accelerator profile (`amos-ai`, 2026-09-04)**: `GgmlBackend` gains an `accel` field + `with_accel(path, profile)` (with `new()` defaulting to `AccelProfile::from_env()`); the daemon's `BackendKind::Ggml::build()` hands the startup-resolved profile to the engine so offload is real, not just logged. Engine invocation is now built by a pure, unit-testable `engine_argv()`: default `allama` keeps `allama run …` untouched (it manages its own offload → `ggml_command_e2e` unaffected), while any `AMOS_GGML_BIN` pointing at a llama.cpp-class CLI gets `-m <model> -p <prompt> -n <max>` + `engine_offload_args()` (`--n-gpu-layers <N>` from the profile; CPU explicitly `0`). Docs `docs/qcom-mtk-bringup.md` updated.
- **Accelerator surfaced truthfully on `get_status` + System UI (`amos-ai` + `amos-proto` + `amos-tauri`, 2026-09-04)**: `StatusReply.accelerator` (field 15) reports the resolved `<vendor>/<accel>` (e.g. `android/nnapi`) **only** when the local ggml engine is actually serving (`EngineState`/`accel_label_for_active`), empty otherwise — no longer just a log line or a hard-coded `gpu_util=0`. `amos-tauri` `DaemonStatus` + `status_once` example carry it; frontend `aiEngine.ts` `EngineView`/`describeEngine` parse it and Settings' AI engine-truth section renders "Accelerator: …" (en/zh `settings.aiAccel`). Tests: `amos-ai` server (mock → empty; gating for ggml-only) + `ai-engine.test.ts`; typecheck clean.
- **Third-party web-bundle runtime host (`frontend-ts` `ExtApp.tsx` + `lib/bundle.ts`, 2026-09-04)**: the installed-app container is no longer a "runtime host not wired" placeholder. When an app was installed from a web-bundle, `ExtApp` fetches its files over `storeBundleResource` (base64), inlines relative `script/link/img` refs into one self-contained document via the new pure `lib/bundle.ts` helpers (`resolveBundlePath`/`listLocalRefs`/`inlineBundle`/`toDataUrl`/`decodeBase64Text`, refusing path escapes), and renders it in a `sandbox="allow-scripts"` `srcdoc` iframe (no same-origin → can't reach the OS shell). Manifest-only installs fall back to a manifest card (`extApp.notWeb`); load errors are surfaced with a reload. en/zh keys `extApp.hostRunning/hostReload/hostLoading/hostError/notWeb`. Pure core unit tests: `bundle.test.ts` (7). Honest boundary: the no-custom-protocol `data:`-inline path suits classic single-page bundles; multi-page/ESM/true-origin bundles still need a future `amos-app://` protocol host (`docs/appstore.md` §4.10).
- **HTML-aware web-bundle inliner hardening (`frontend-ts` `lib/bundle.ts`, 2026-09-04)**: `listLocalRefs`/`inlineBundle` now compute masked spans (bodies of `<script>`/`<style>` and HTML comments) and never treat `src=`/`href=`-looking strings *inside code/CSS/comment text* as real asset references — a bundle's inline JS is preserved verbatim instead of being corrupted by over-eager rewriting. Opening-tag attributes (e.g. `<script src>`/`<img src>`) are still rewritten. Regression test (`bundle.test.ts`).
- **`ExtApp` runtime-host component coverage (`frontend-ts`, 2026-09-04)**: new `ExtApp.test.tsx` (happy-dom + react-dom + fake Tauri bridge) asserts the host renders an installed web-bundle in a `sandbox="allow-scripts"` `srcdoc` iframe, falls back to the verified-manifest card when an app has no web interface, and **degrades gracefully to the manifest card (no iframe/crash) when the bridge call fails**.
- **Ollama accelerator hint (`amos-ai`, 2026-09-04)**: `build_backend_from_env()` now consumes the previously-unused `AccelProfile::ollama_hint()` — when `AMOS_BACKEND=ollama` the daemon logs the resolved `vendor/accel` and the Ollama device hint at startup (Ollama manages its own offload, so this surfaces chip routing instead of CLI flags).
- **`amos-app://` web-bundle protocol host (`amos-appstore`, 2026-09-04)**: new `crates/amos-appstore/src/host.rs` — the missing URI layer for the store's "future custom protocol": `parse_bundle_uri("amos-app://<id>/<path>") -> (id, path)` validates the netloc as an app id (model's slug rule, so `..`/`/`/whitespace can't address a path), and `serve_bundle(root, uri) -> ServedBundle{bytes, content_type, nosniff}` resolves inside that app's own install dir via `serve::resolve_request` (canonicalization + traversal guards) and reads the bytes — ready for a Tauri `amos-app://` protocol handler to write out with the right MIME. `model::valid_id` is now `pub(crate)` and shared. Unit tests (3): URI parse/validate, serving entry + assets, and path-escape rejection. `amos-appstore` lib tests now **39**. Docs `docs/appstore.md` §4.11.
- **Tauri `amos-app://` wiring + bundle-id hardening (`amos-appstore` + `amos-tauri`, 2026-09-04)**: `amos_appstore` now exports `is_valid_app_id`; `amos-tauri`'s `read_bundle_resource(id, path)` validates `id` before `root.join(id)` (the old code had no guard — a malicious id like `..` could select a directory outside the root), and a new pure `read_bundle_uri(root, uri)` + registered command `appstore_bundle_uri` serve a bundle file addressed by an `amos-app://` URI (the single seam a future custom-protocol handler calls). Frontend gains `lib/backend.ts` `storeBundleUri(uri)`. Tests: `amos-appstore` host (3), `amos-tauri` appstore module (**5**, incl. non-slug-id rejection + URI serving + traversal).

- **`StatusReply.governor` — resource-governor live decision on the AI status RPC (2026-09-04)**: `get_status` now also returns `GovernorMetrics` (field 14, `sensor_mode`/`reason`/`cap_inference`/`throttle_background`/`ticks`) so the AI-session/`status_once` path sees the same daemon governing state the `Governor` service exposes. `AiAgentService` gained an optional shared `Arc<Mutex<ResourceGovernor>>` (`set_governor`, wired by `serve()` to the same instance the beat + Governor service use) + `governor_metrics()`; pending baseline (ticks=0) when unattached. `amos-ai` lib **92** (new `governor_metrics_reflects_shared_governor_decision`); clippy `--workspace`/fmt clean.
- **Governor exposes its latest decision (`Governor` service `GetState`, 2026-09-04)**: `GovernorState.decision` (new `GovernorDecision`) reports what the daemon's closed loop is doing — `sensor_mode`, `reason`, `cap_inference`, `throttle_background`, `ticks` — so the System UI can show live governing state. `ResourceGovernor` now records the last tick's `GovernorOutcome` (which gained `cap_inference`/`throttle_background`) + a `ticks` counter (`last_decision()`/`ticks()`). Service unit test asserts decision flips to `power_save`+throttling after a low-battery observe; `amos-ai` lib **91** + governor UDS e2e green; clippy `--workspace`/fmt clean.
- **Daemon-hosted resource governor gRPC service (`amos-ai` + `proto/governor.proto`, 2026-09-04)**: implements `docs/device-bring-up.md` §4 — a `Governor` service (package `amos_governor`) mounted on the daemon's UDS so the System UI / a per-app process host can drive the `ResourceGovernor` closed loop over the wire: `RegisterApp`, `MoveApp` (→Foreground/Background/Cached/…), `UnregisterApp`, `ScheduleJob` (AlarmExact/Deferred), `GetState` (apps + jobs + background count). `serve()` now owns ONE shared `Arc<Mutex<ResourceGovernor>>` used by **both** the periodic beat and the service (so registration feeds the same deciding instance — no duplicate governor). `ResourceGovernor` gained `move_app`/`kill_app`/`app_state`/`app_entries`/`job_entries`; `amos-scheduler::Scheduler::entries`. amos-proto gains the generated `amos_governor` module. Verified offline: `governor_service` unit tests (register→move→schedule→get_state + observe over the same instance + wire enums via `TryFrom<i32>`) and a real-UDS e2e (`tests/governor_rpc_e2e.rs`: register/move/schedule/get_state + NotFound on unknown app). `amos-ai` lib **91**; clippy `--workspace -D warnings` clean.
- **Real-device bring-up checklist `docs/device-bring-up.md` (2026-09-04)**: actionable action-item doc for the three remaining Android bindings — (1) real battery/thermal/power sampling feeding `amos_ai::energy` via `amos-power::android::AndroidBatteryTelemetry` + `amos-profiling::android::AndroidBatteryPowerSource`, (2) a per-app process host driving `amos-applife::AppLifecycle`, (3) binding `amos-scheduler` `next_wake`/`due` to `AlarmManager`/`JobScheduler`/Doze — each with wiring points, seams to reuse, and on-device acceptance criteria; plus a §4 route for a daemon-hosted lifecycle (optional gRPC Governor service). README/bottom-layer-os-audit updated.
- **Real Android battery/thermal telemetry seam `amos-power::android` (2026-09-04)**: feature-gated `android` (`dep:jni`), mirroring the repo's `android.rs` seams. `AndroidBatteryTelemetry` takes a `JavaVM` + app-`Context` global ref, reads the sticky `ACTION_BATTERY_CHANGED` intent (`level`/`scale`/`status`/`temperature`) and folds it into an `amos_power::Telemetry` for the energy/governor ticker; charging = `STATUS_CHARGING|FULL`, temp = tenths→°C, a failed read yields the conservative `BatteryState::default()` (never a fake charge). Instantaneous mW is not on the sticky intent — pair `amos_profiling::AndroidBatteryPowerSource` for live draw. Host-verified: `cargo check -p amos-power --features android` + clippy `-D warnings` clean; added to `make gated-check`. Default (desktop) build unaffected (24 tests green). Runtime (System UI wiring a real `JavaVM`/`Context`) remains device bring-up.
- **Composed resource/energy/lifecycle/scheduler closed loop `amos_ai::governor` (2026-09-04)**: new daemon-side `ResourceGovernor` that ticks `amos-power` (energy decision) → `amos-scheduler` (fire exact alarms, run deferred jobs only when power allows, doze-proxy = `throttle_background`) → `amos-applife` (on `PowerSave` freeze idle `Background` apps to `Cached` tombstone; on recovery + screen-on thaw them; under explicit memory pressure reclaim the LRU cached/background victims). Deterministic `observe(now, telemetry, memory_pressure, maintenance_open) -> GovernorOutcome`; public registration delegates (`register_app`/`background_app`/`schedule`) let a per-app host seed apps/jobs. Reference orchestration tying the three audit-gap kernels into one driver a System-UI/per-app host mounts (`docs/app-lifecycle.md` §5 / `docs/scheduler.md` §7). `amos-ai` lib tests now **88**; `clippy --all-targets -D warnings` + fmt clean.
- **Background-task scheduler + wakeup-alignment core `amos-scheduler` (2026-09-04)**: closes audit gaps §3.P2 #10/#12 ("无后台任务调度器" / 唤醒对齐) with a pure-`std` register of `AlarmExact` vs `Deferred` jobs. `spec` — `JobId`, `JobType` (+keys), `PowerState` with `deferred_runnable()` (charging → run; dozing → only in an open maintenance window; else run). `scheduler::Scheduler` — `register`/`cancel`/`complete`, `due(now, power)` returns exact alarms first then **coalesces all due deferred jobs into one batch** (fewer wakes), `next_wake(now)` gives the next guaranteed exact-alarm tick for a real `AlarmManager`, `counts()`, `kind(id)`. `ScheduledJob::new` + `register` reject inverted windows. Deterministic (caller supplies `now` in ticks + `PowerState`). Design: `docs/scheduler.md`. Tests: 7 lib + 1 integration (`tests/doze_cycle.rs`: alarm fires during Doze at its tick with `next_wake=300`; deferred withheld while idle, coalesced in the next maintenance window); `clippy --all-targets -D warnings` + fmt clean. Real `AlarmManager`/`JobScheduler`/Doze-feed binding + `amos-applife` tombstone-linkage remain caller seams.
- **Energy-governor domain core `amos-power` (2026-09-04)**: closes the audit gap "isolated energy-policy knobs but no closed-loop decision layer" (`docs/bottom-layer-os-audit.md` §3.P2 #11) with a pure-`std`, deterministic power-save decision kernel. Folds `BatteryState` (level/charging/temp) + live `power_mw` (via `amos-profiling::PowerSource`) + foreground/background `Usage` into a `SensorMode` recommendation through `policy::decide` — a **pure rule with entry/exit hysteresis** (low-battery on/off band + thermal exit cushion → no boundary flapping). Output `Decision { sensor_mode, cap_inference, throttle_background, reason }`; `Decision::apply_to(&SensorManager)` pushes the mode into `amos-sensor`'s real manager (reusing `SensorMode`, no duplicated enum). Stateful `EnergyGovernor::observe` carries the last decision for the periodic-ticker caller. `Policy::normalized()` guarantees `critical ≤ save_on ≤ save_off` + `critical_temp > high_temp` even on contradictory input. Priority: thermal-critical > battery-critical > battery-low > thermal-high > power-draw > charging(Performance) > healthy(Balanced); healthy throttles background inference only when the screen is off. Design: `docs/power-policy.md`. Tests: 22 lib + 2 integration (integration drives a real `MockSensorProvider`+`SensorManager`: low-battery `PowerSave` refuses a 30 FPS camera, charger → `Performance` restores); `clippy --all-targets -D warnings` + fmt clean. Daemon/System-UI periodic ticker + real `ACTION_BATTERY_CHANGED`/`EXTRA_VOLTAGE` input wiring remain caller-side seams (`docs/profiling.md` / `docs/power-policy.md` §6).
- **Energy-governor wired into the AI daemon (`amos-ai`, 2026-09-04)**: new `crates/amos-ai/src/energy.rs` `EnergyStore` (mirrors `profiler.rs`) ticks an `amos_power::EnergyGovernor` on the same cadence as the health/profile heartbeats and logs the decision; `serve()` spawns+aborts it alongside the others. Inputs are `AMOS_ENERGY_*` env (host/dev; real battery/thermal HAL is the on-device seam). New `proto/ai_agent.proto` `EnergyPolicy` message + `StatusReply.energy = 13`; `get_status` maps the latest `EnergySnapshot` (mode/reason/cap_inference/throttle_background/ticks); `status_once` example prints it. `amos-ai` lib tests **86** + 4 rpc e2e green; clippy/fmt clean. Honest label: the daemon *runs the governor and reports* the recommendation — driving a real `SensorManager`/engine gating stays a caller seam (`docs/power-policy.md` §6).
- **App/process lifecycle domain core `amos-applife` (2026-09-04)**: closes audit gap §3.P2 #8 ("无应用生命周期管理") with a pure-`std`, transport-agnostic state machine + LMK-proxy reclaim. `spec::AppState` — importance ladder `Foreground(0) < Visible(1) < ForegroundService(2) < Background(3) < Cached/墓碑(4) < Stopped(5)` with stable keys; protected tiers never reclaimed. `manager::AppLifecycle` — registry with LRU ordering (every transition bumps a monotonic seq), ops `launch/go_foreground/go_background/freeze(→Cached)/thaw/start_service/stop_service/stop(保留保存态)/kill(丢弃)`, per-state `counts()`, and a deterministic `reclaim_candidates(budget)` that evicts Cached before Background, least-recently-used first. Integration test drives a 3-process pressure scenario (`[a, c, b]`). Design: `docs/app-lifecycle.md`. Tests: 9 lib + 3 integration; `clippy --all-targets -D warnings` + fmt clean. Runtime (per-app process host / real pressure feed) remains a caller seam.
- **Device-sensor tile in Settings (`frontend-ts`, 2026-09-04)**: new `components/SensorPanel.tsx` shows the daemon's energy mode (switchable Balanced/Performance/Power save → `sensor_set_mode`) plus cameras / GNSS / IMU readouts via `sensor_snapshot`, rendered in Settings (hidden when not bridged / no daemon). en/zh i18n keys; driven by the pure `lib/sensors.ts` normalize helpers. `bun run typecheck` green.
- **System UI sensor/profiling desktop wiring (`amos-tauri` + frontend, 2026-09-04)**: (a) Tauri `sensor_*` bridge — `amos-tauri/src/sensors.rs` `sensor_snapshot`/`sensor_set_mode`/`sensor_acquire` open a `SensorClient` over the daemon UDS (mirrors the telephony bridge) and return serde payloads (cameras/GNSS/IMU/mode); registered in `lib.rs`. (b) frontend `lib/sensors.ts` — typed snapshot + `normalizeSnapshot`/`sensorPixels`/`sensorCameraCount` pure helpers + guarded `sensorSnapshot`/`sensorSetMode`/`sensorAcquire` (null when not bridged) + `__tests__/sensors.test.ts` (4). (c) **`StatusReply.profile` rendered in Settings diagnostics** — `aiEngine.ts` `EngineView`/`describeEngine` now parse the daemon `profile` (decode tok/s, TTFT ms, totals), `apps.tsx` shows it in the AI (engine-truth) section when `decode_runs > 0`, with en/zh i18n keys; `ai-engine.test.ts` +3. Real device `Context` wiring / camera-frame + IMU stream bridges / live `EXTRA_VOLTAGE`→`est_energy_j` remain device bring-up (`docs/sensors.md` / `docs/profiling.md`). Rust clippy `-D warnings` clean; `bun run typecheck` + sensors/ai-engine/settings tests green.
- **Android HAL/power seams landed as compile-gated skeletons (`amos-sensor` + `amos-profiling`, 2026-09-04)**: (a) `amos-sensor/src/android.rs` — `AndroidSensorProvider` behind the `SensorProvider` trait (mirrors `amos-radio`/`amos-telephony` `android.rs`): **GNSS real + synchronous** (`LocationManager#getLastKnownLocation`, needs `ACCESS_FINE_LOCATION`); camera/IMU are *streams* whose `CameraDevice`+`ImageReader` / `SensorEventListener` bridges return an explicit `Provider` error until device bring-up (honest, no pretending). (b) `amos-profiling/src/android.rs` — `AndroidBatteryPowerSource` implementing `PowerSource`: reads `BatteryManager#getLongProperty(CURRENT_NOW)` (µA) × voltage (constructor param, default 3700 mV; pass live `EXTRA_VOLTAGE` on device) → mW; `0.0` when unreadable. Both feature-gated `android` + optional `jni`; `make gated-check` now host-compiles all four device seams (`cargo check -p {amos-radio,amos-telephony,amos-sensor,amos-profiling} --features android`) — verified green, no device/NDK needed. Real stream bridges + System UI `Context` wiring remain device bring-up (`docs/sensors.md` / `docs/profiling.md`).
- **Device-sensor domain core `amos-sensor` (2026-09-04)**: closes the audit gap (next to `amos-audio`/Telephony) with a pure `std` camera / GPS-GNSS / IMU abstraction. `spec` — `SensorKind`, `SensorMode`, `CameraConfig`/`CameraFrame` (RGBA8 / NV21), `GeoFix`/`FixMode`, `ImuSample`/`Vec3` + PowerSave/hardware sampling ceilings; `provider` — dumb `SensorProvider` seam + deterministic `MockSensorProvider` (camera seq/pattern, drifting GNSS fix, IMU cadence); `manager` — `SensorManager` energy policy: single-shot reads always allowed, continuous streams gated by mode (PowerSave ceilings Camera 15 FPS / GNSS 1 Hz / IMU 25 Hz; hardware ceilings always). Real Android HAL + gRPC service bus left as seams (`docs/sensors.md`). Tests: 22; `clippy -D warnings` clean; fmt clean.
- **Inference perf/power profiling domain core `amos-profiling` (2026-09-04)**: starts the roadmap's "Performance profiling and optimization" with a pure-`std` metric kernel for on-device LLM. `types` — `Phase` (PromptEval/Decode) + `Record`; `tracker` — `ProfileTracker` (prompt/decode tokens-per-second, decode ms-per-token, TTFT, `merge`, guarded ÷ → empty gives `None`, never NaN); `power` — `PowerSource` seam + `MockPowerSource` + `energy_joules`; `measure` — `time`/`time_and` around `Instant`; `report` — owned `ProfileReport` (`Display` stable `key: value` lines, `n/a` not NaN). Daemon assembly (`amos-ai` generate path) + real power HAL left as seams (`docs/profiling.md`). Tests: 15; `clippy -D warnings` clean; fmt clean.
- **Wired the two domain cores into the runtime (`proto/sensor.proto` + `amos-ai`, 2026-09-04)**: kills the "code written but not wired" audit criticism. (a) `SensorService` — new `proto/sensor.proto` (`amos_sensor`) → `amos-proto` codegen → `amos-sensor/src/service.rs` (`ListCameras/CaptureCamera/GetGnss/GetImu/GetMode/SetMode/AcquireStream`, mapping the domain `SensorManager` + PowerSave gate to the wire) → **mounted via `add_service` in `amos-ai::serve()`** alongside AiAgent/AndroidManager/Telephony on the shared UDS. e2e: `amos-sensor/tests/sensor_rpc_e2e.rs` (single service over UDS) + `amos-ai/tests/rpc_test.rs` `sensor_service_mounted_and_profile_exposed` (sensor answered on the same socket as AiAgent). (b) Profiling in `get_status` — `amos-ai/src/profiler.rs` `ProfileStore` (shared `ProfileTracker` + TTFT atomics) is fed by the `stream_chat` text-decode path (each turn: streamed tokens + wall; start→first-token as TTFT); `StatusReply.profile` (`ProfileMetrics`, proto field 12) + `status_once` now print it. Honest labels: no tokenizer ⇒ no prompt-token counts; `decode_tokens_per_sec` is end-to-end generated tokens/s to the client. Real Android HAL provider + System UI sensor bridge + battery `PowerSource` remain seams (`docs/sensors.md` / `docs/profiling.md`). Sensor tests 25 + 1 e2e; `amos-ai` lib 82 + rpc e2e; `clippy -D warnings` clean; fmt clean.
- **Finish the profiling/sensor seams (`amos-ai`, 2026-09-04)**: (a) **bidi `Chat`/voice now profiled** — the text-decode loop in the `chat` handler records each completed (non-cancelled) turn into the same shared `ProfileStore` (`record_ttft` on first token + `record_decode(tokens, wall)` after the turn), so text and voice-driven turns both feed `StatusReply.profile`. e2e: `chat_test::bidi_chat_prompt_turn_is_profiled` (chat_test now **8**). (b) **profile into the monitoring cadence** — `ProfileStore::spawn_periodic_log` logs `amos-ai inference profile` (decode_tokens_per_sec / ttft_ms / totals) on the same interval as the health heartbeat, spawned+aborted in `serve()`. (c) **headless acceptance examples** — `amos-ai/examples/sensor_once.rs` (list cameras → capture → GNSS → IMU → mode over the mounted SensorService) and `amos-ai/examples/profile_once.rs` (one `stream_chat` turn → print `StatusReply.profile`). Real Android HAL provider + System UI sensor bridge + battery `PowerSource` remain seams (`docs/sensors.md` / `docs/profiling.md`). `amos-ai` lib 82 + chat_test 8 + rpc_test 4; `clippy -D warnings` clean; fmt clean.
- **Resident-voice DSP tested headlessly (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: `spawn_resident_capture` — the capture→downsample→energy-gate→trailing-silence endpoint loop — now has a unit test driven by `amos_audio::mock::FrameMic` (no daemon/device): loud speech frames stream as `Audio`, trailing silence past the gate fires `AudioEnd`, and exactly one utterance is submitted. Voice module now **10 tests**; full `cargo test -p amos-tauri --lib` = **44 passed**; `clippy -D warnings` clean.
- **Assistant-voice commands made testable (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: factored the duplicated "get active sender → send frame/end/cancel" logic in the `assistant_voice_feed/end/stop` Tauri commands into `VoiceSession` methods (`feed`/`end_utterance`/`stop` + `active_tx`); the commands now delegate (no behavior change, removes the copy-paste). 2 new async tests cover: feed fails before start then delivers an `Audio` frame; `end_utterance` sends `AudioEnd`; `stop` sends `Cancel` and clears the session so a later feed fails. Voice module now **9 tests**; full `cargo test -p amos-tauri --lib` = **43 passed**; `clippy -D warnings` clean.
- **Assistant-voice reader made pure & tested (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: extracted the daemon chunk → event relay logic out of the inline reader loop into a pure `on_chunk(&AgentChunk, session, &mut full) -> Vec<VoiceEvent>` (token accumulate, `done` finalizes & resets via `mem::take`, `error` relayed without finalizing), and rewrote the `VoiceLink::open` reader loop to drive it — no behavior change, now unit-testable headlessly. 4 new tests cover token accumulation, done-reset, error-without-finalize, and empty chunk. Voice module now **7 tests**; `cargo test -p amos-tauri --lib assistant_voice` + `clippy -p amos-tauri --all-targets -D warnings` green.
- **Rate-limiter memory bound (`amos-telephony` `rate.rs`, 2026-09-04)**: `DialRateLimiter` now caps distinct tracked clients (default 256, `with_clients(...)` configurable) and prunes fully-expired windows on access, so a caller flooding fake `x-client-id`s can no longer grow the bucket map without bound (memory-DoS guard). New `with_clients` + test `distinct_clients_are_bounded`. Rust lib now **61**; `clippy -D warnings` clean.
- **Assistant-voice unit coverage (`amos-tauri` `assistant_voice.rs`, 2026-09-04)**: added tests for the wired-but-untested voice core — `to_payload` preserves every `VoiceEvent` variant & fields (`listening/token/turn_done/stopped/error`) and `VoiceSession` store/take is single-shot (take returns the started session then clears). Now 3 voice tests (with `has_signal`); `cargo test -p amos-tauri --lib assistant_voice` + `clippy -p amos-tauri --all-targets -D warnings` green. (Module `mod tests` was also moved to file end so `clippy --workspace -D warnings` is fully clean.)
- **Incoming-call banner Mute (`amos-tauri` frontend, 2026-09-04)**: the system incoming-call overlay's answered (talking) banner now offers a **Mute** toggle (🔇/🎙️, aria `Mute`/`Unmute`) like the dialer's own in-call screen; mute state resets on ringing/end/hang-up so it never leaks across calls. New `IncomingCall.test.tsx` case (answer → Mute present → Unmute → back). `tsc --noEmit` clean.
- **P3 real-dial skeleton landed, compile-gated (`amos-telephony`, 2026-09-04)**: `android` feature now = optional `jni` dep; new `crates/amos-telephony/src/android.rs` (gated in `lib.rs`) provides `AndroidTelephonyProvider` + a **separate** `AndroidEmergencyTelephonyProvider` hosted in the System UI/`ROLE_DIALER` process (`AndroidContext(GlobalRef)` + per-thread attach, mirroring `amos-radio`). `dial`/`emergency_call` fire a real `ACTION_CALL` `tel:` intent from the app `Context` and return a provider call id; `subscribe` is wired to an event registry. Honest scope: `answer`/`end`/recording/live `status` return explicit `Provider(P3 device-validated)` errors (no pretending); on-device TODO documents the `ACTION_CALL`→`TelecomManager#placeCall` migration + InCallService/TelephonyCallback wiring. Verified with `cargo check --features android` and `clippy --features android -D warnings`; default desktop/CI builds carry no JNI stack.
- **Ordinary-dial rate limiting, emergency-exempt (`amos-telephony` + `amos-ai`, 2026-09-04)**: new `crate::rate::DialRateLimiter` (per-client fixed-window). `TelephonyService::dial` never throttles **emergency** calls but rate-limits ordinary ones (per `x-client-id` gRPC metadata, else `_default`); over-cap returns `ResourceExhausted` and is audited. The daemon now mounts `demo_server_limited(30)` (`amos-ai/src/server.rs`) so the live UDS service caps ordinary dials at 30/min/client while 110/112 stay exempt; strict/headless test backends stay unlimited. New builders `with_mock_demo_limited` / `demo_server_limited`. Rust package now **60 lib + 2 UDS e2e** green; `clippy -D warnings` clean; `cargo check -p amos-ai` passes.
- **Emergency-dial audit wired in (`amos-telephony`, 2026-09-04)**: new `crate::audit` — a bounded (1024, newest-first) thread-safe `AuditLog` with `AuditEntry{ts,operation,detail,outcome,note}`. `TelephonyService` holds one and records **every parsed dial**: auto-routed/forced emergency dials as `operation="emergency_dial"` (the §5 requirement — rate-limit exempt but never un-audited), regular as `"dial"`, refusals as `Rejected` with the provider note. Unparseable/over-long input is rejected **without** an audit echo (don't reflect attacker-controlled huge text back into logs). Accessible via `TelephonyService::audit_log()`. Rust package now **56 lib + 2 UDS e2e** green; `clippy -D warnings` clean; `cargo check -p amos-ai` passes.
- **Telephony service-hardening audit pass (`amos-telephony`, 2026-09-04)**: `Number` is now **server-authoritatively length-capped** at `MAX_LEN = 18` digits (mirrors the dialer UI's cap) — a crafted gRPC `Dial` can no longer ask a provider to place a pathologically long number. Cap counts canonical digits (ignores separators / leading `+`). Audit also re-confirmed the daemon truly serves telephony (`amos-ai/src/server.rs` mounts `amos_telephony::service::demo_server()` over the shared UDS), so the dialer path is live, not dead code. Rust lib now **52** tests green; `clippy -D warnings` clean.
- **Full-featured dialer (`amos-tauri` frontend, 2026-09-04)**: `PhoneApp` extracted to its own module (`components/PhoneDialer.tsx`, re-exported from `CommsApps`) and upgraded to a tabbed dialer — **Keypad / Recents / Frequent / Emergency**. Recents & Frequent list pages (tap to fill the keypad, select-then-place), and an **Emergency page** with one-tap privileged dial of the shared recognized codes (`lib/emergency.ts::EMERGENCY_NUMBERS`, CN set + universal 112) labelled by function. The in-call surface gains a **Mute** toggle and a **DTMF keypad** (local state until the audio pipeline lands; real mic mute / tone out are `docs/native-voice.md` scope). Emergency page dials go through the OS `telephony_dial(…, emergency: true)` path; no-daemon dials degrade to a localized error. New i18n en/zh keys; new `PhoneDialer.test.tsx` (emergency privileged dial, mute-on-connect, DTMF open/close) + rewritten `PhoneApp.test.tsx` (Recents/Frequent tabs, pick-to-fill); `PhoneRecording`/`MessagesApp`/emergency/lock/i18n suites still green; `tsc --noEmit` clean.
- **Telephony audit + production hardening — Phase A+C (`amos-telephony` + `amos-tauri` frontend + `docs/telephony.md`, 2026-09-04)**: architecture decisions & honest responsibility framing for P3 (real calls must host in the System UI/`ROLE_DIALER` process via JNI `TelecomManager`, **not** a headless daemon hand-writing `/dev/binder`; crash-proof 110/112 routing is a platform/RIL/vendor guarantee a userspace crate must not claim as a "kernel path"). Code: `EmergencyMap` is now jurisdiction-aware — `for_region` (CN/US/GB/IE/AU/NZ/JP/KR/EEA…, every set always carries international 112, deduped) + `quick_dial(region)` + `codes()`; unknown/empty region falls back to `common_global()` so the table is never empty (a misclassified 110/112 as a regular call would be the worst failure). New Rust unit tests (lib now 51, all green). Frontend: lock-screen emergency one-tap number + label come from the shared `lib/emergency.ts::EMERGENCY_QUICK_NUMBER` and recover (re-enabled + `shell.emergencyUnreachable`) instead of sticking on a phantom "calling…" when the phone service is unreachable; `PhoneApp` no-daemon dial now exits calling and shows `phone.dialFailed` instead of lingering with no call id. i18n en/zh `{num}` interpolation. New `emergency.test.ts` + `lockEmergency.test.tsx`; updated `PhoneRecording.test.tsx` no-daemon case to the new graceful-degradation semantics. `tsc --noEmit` clean.
- **Tauri telephony bridge unit tests (`amos-tauri`, 2026-09-04)**: the `telephony.rs` command layer (added during the call-recording/loop work) previously had **no Rust unit tests**. Added tests for its pure snapshot→payload mapping (`call_payload`): state (`Active`/`Ended`/`Dialing`/`Unknown`), direction (`Incoming`/`Outgoing`), recording (`On`/`Off`/`Failed`), and safe degradation for unknown wire values — the mapping the whole phone UI keys off. `cargo test -p amos-tauri` lib unit tests all green (34 run).
- **Telephony broadcast audit fixes (`amos-telephony`, 2026-09-04)**: hardened signalling-consistency gaps found by audit so **every** call-state transition is broadcast on `Watch`. (1) `MockTelephonyProvider::answer` now **broadcasts `Connected`** — answering an incoming call previously left the `Watch` stream silent (contradicting the `telephony_answer` contract). (2) Recording is a **first-class broadcast call-state**: `start_recording`/`stop_recording`/`simulate_recording_failure` emit `ProviderEvent::RecordingChanged`. (3) Recording broadcasts carry the call's **true `direction`** (an answered *incoming* call is not re-tagged Outgoing). (4) A **local hang-up now broadcasts `LocalEnded`** (previously `end()` was silent), making local end symmetric with remote end / answer / record. All verified with new unit tests; telephony unit count now **47** + 2 UDS e2e; `clippy --all-targets -D warnings` clean.
- **Call telephony — end-to-end loop closure (`amos-telephony` + `amos-ai` + `amos-tauri` + `frontend-ts`, 2026-09-04)**: a desktop dial is now actually operable through dial → Active → record → hang up. `TelephonyService` gains demo auto-connect (`demo_server()` used by the `amos-ai` daemon; outgoing *regular* calls reach `Active` after a short ring via the mock "network answers"; emergency calls stay ringing; hung-up-before-connect is ignored) while `mock_server()` stays deterministic for the headless e2e harness. `amos-tauri` opens a **long-lived `Watch` stream** and forwards each call-state event to the WebView as `telephony-event` (`spawn_telephony_watch`, reconnects with bounded backoff), adds `telephony_answer`, and extends `TelephonyCallPayload` with `direction`. `PhoneApp` now subscribes and enters a real "In call"/talking screen when its dialed call connects (enabling the record toggle) and returns to the keypad on End; new **system incoming-call overlay** (`IncomingCall`) shows Ringing → Answer/Decline and, once answered (Active), a compact in-call banner with record toggle + Hang up. i18n en/zh (`phone.talking/incoming/answer/decline/hangup`). Tests: telephony unit now **42** (demo auto-connect → Active) + updated `PhoneRecording.test.tsx` (Active-via-event enables record) + new `IncomingCall.test.tsx` (ringing/answer→recordable banner/decline). Real audio capture + on-device incoming injection remain future audio/device work.
- **Audio HAL Bridge — hardware audio + real local ASR (`amos-audio` + `amos-ai`, 2026-09-04)**: closes the first "彻底断层" at the source level. New **`crates/amos-audio`** (pure-std) is the audio-HAL abstraction: `AudioCapture`/`AudioSink` traits (mono f32), the 16 kHz ASR/wire spec (`AudioSpec`/i16↔f32/downmix/`encode_f32_le`), a streaming `LinearDownsampler` (phase-continuous across chunks), deterministic mocks (`SineMic`/`FrameMic`/`SilenceMic`/`MockSink`/`NullSink` + `mic_to_asr` example), and **`target_os="android"`+feature-gated hand-written direct FFI seams** for `tinyalsa` (AOSP/system, incl. the future call-audio-tap position) and `aaudio` (app-side real-time mic). `amos-ai` gains the **`asr-sherpa`** feature; `ChatAsr::from_env` now selects a real sherpa-onnx streaming recognizer when `AMOS_ASR_BACKEND=sherpa` + `AMOS_SHERPA_MODEL_DIR` (honest `None` fallback, never silently mock). Gated UDS e2e `amos-ai/tests/bidi_sherpa_audio.rs` pushes real speech PCM from `models/sherpa-en-20m/test_wavs/0.wav` over `Payload::Audio` and asserts the recognized transcript is answered. Tests: `amos-audio` **13** unit; `amos-ai` **73** lib (mock + `asr-sherpa`); bidi real-sherpa e2e green; `clippy --all-targets` clean under both feature sets. Design: `docs/audio-hal-bridge.md`. Android FFI seams compile only on an NDK cross-target and remain to be exercised at device bring-up; SIM-call voice interception still needs system-level HAL/audio-route hooks.
- **System UI resident assistant-voice streaming link (`amos-tauri`, 2026-09-04)**: pushes the gap-#1 work one step further — a real mic→daemon streaming path on the System UI side. New `assistant_voice.rs` owns a long-lived bidi `Chat` stream (no prompt) that keeps answering across turns: `VoiceLink` (Tauri-free, headless-testable) streams **mono 16 kHz f32 little-endian `Payload::Audio`** frames and relays `AgentChunk` replies as `VoiceEvent`s; Tauri commands `assistant_voice_start/feed/stop` (managed `VoiceSession`) fan them to the WebView as `assistant-voice-event`. Headless e2e `amos-tauri/tests/assistant_voice_e2e.rs` runs a real daemon (mock ASR) and drives a 48 kHz `amos-audio` sine mic → `LinearDownsampler` → f32-le → `Payload::Audio`, asserting "你好，Amos" is recognized and answered. Frontend bridge wired: `lib/audio.ts` `encodeF32le`/`frameToAssistantChunk`, `lib/voice.ts` `pcmToAssistantChunk`/`parseVoiceEvent` (typed `AssistantVoiceEvent`), `lib/backend.ts` `assistantVoiceStart/Feed/Stop`. Tests: `amos-tauri` lib clippy clean; voice e2e green; TS `voice.test.ts` 14 pass (incl. 3 new) + `tsc` clean. `amos-tauri` now depends on `amos-audio`.
- **Push-to-talk end-of-utterance (`Payload::AudioEnd`, `amos-ai` + `amos-ai` + `amos-tauri`, 2026-09-04)**: adds the missing "I'm done speaking" signal so a voice turn no longer depends solely on the recognizer's own VAD/endpoint. `proto/ai_agent.proto` adds `bool audio_end = 4` to `ClientMessage`; `ChatAsr::finish()` force-finalizes + resets the current utterance; the daemon's bidi `Chat` handles `AudioEnd` by turning whatever was recognized so far into a prompt (semantic card + inference + audit + history unchanged); `VoiceLink::finish_turn()` / Tauri `assistant_voice_end` / TS `assistantVoiceEnd()` expose it client-side. Tests: `ChatAsr::finish` unit (short utterance finalizes + resets); `amos-ai` lib now **74**; bidi UDS e2e `bidi_chat_audio_end_finalizes_a_short_utterance` feeds a sub-endpoint clip then `AudioEnd` → answered; `amos-ai --all-targets` + `amos-tauri` clippy clean; fmt clean; TS `tsc` clean.
- **Resident multi-turn voice (energy-gated `AudioEnd`, `amos-tauri`, 2026-09-04)**: proves the "always-on mic" semantics on the System UI side. `assistant_voice::has_signal` (client energy gate mirroring the frontend `hasSignal`) drives utterance segmentation; a new headless e2e (`resident_stream_answers_multiple_successive_utterances`) runs a real daemon and, on **one resident stream**, streams two synthetic 16 kHz speech bursts separated by silence, submitting each via `AudioEnd` at the client's own trailing-silence gate, and asserts **both utterances are answered**. This is the client-side segmentation loop a device AAudio capture thread will feed. Tests: `assistant_voice_e2e` now **2** (single + multi-turn) pass; `has_signal` unit pass; `amos-tauri --lib` clippy `-D warnings` clean; fmt clean.
- **Official resident capture thread (`ResidentVoiceHandle` + `spawn_resident_capture`, `amos-tauri`, 2026-09-04)**: promotes the segmentation loop into a first-class, stoppable worker thread that a device **AAudio** capture feeds. `spawn_resident_capture` reads an `amos_audio::AudioCapture` on a named thread, down-samples to 16 kHz, pushes `Payload::Audio`, and auto-submits each utterance with `AudioEnd` on a trailing-silence gate; returns a [`ResidentVoiceHandle`] (`stop()` joins, `submitted()` counts) and `VoiceLink::spawn_resident`/`feeder()` convenience. gated e2e `resident_thread_submits_and_answers_two_utterances` runs the official thread against a real daemon and asserts both utterances are answered. Now covered by `gated-check` (`cargo test -p amos-tauri --test assistant_voice_e2e`). Tests: `assistant_voice_e2e` now **3** pass; lib + test clippy `-D warnings` clean; fmt clean.
- **Airplane-mode cascade is atomic (rollback, `amos-radio`, 2026-09-04)**: hardens `RadioManager::set(Airplane, true)` so enabling Airplane is no longer a partially-applied write. It now snapshots the prior state first, and if any cascade step (Airplane→on, then Wi-Fi→off, then Bluetooth→off) fails **after** Airplane is already on, it rolls every radio bit back to the prior snapshot before returning the provider error — no partial Airplane state can leak to the user/provider. Failure-injection tests: a `FlakyProvider` whose Wi-Fi/Bluetooth writes fail on demand proves Wi-Fi-cascade failure, Bluetooth-cascade failure, and the all-success path each leave the provider exactly as expected. Tests: `amos-radio` **13** pass (3 new); `clippy -D warnings` clean; fmt clean.
- **Resident-voice handle hardening (`amos-tauri`, 2026-09-04)**: audit-driven completion of the newest capture code. `spawn_resident_capture` now **refuses sub-16 kHz mic rates loudly** (it only down-samples; 8 kHz previously spawned a worker that silently never streamed). `ResidentVoiceHandle` gains **non-blocking `request_stop(&self)`**, and `stop()` now delegates to it before joining — so callers can avoid blocking a live (e.g. AAudio) mic read when they must not wait. Tests: `assistant_voice` module **12** pass (2 new: sub-16k rejection, request_stop→stop lifecycle); `amos-tauri --lib` clippy `-D warnings` clean; fmt clean.
- **Inbound audio frame bound (memory-DoS guard, `amos-ai`, 2026-09-04)**: the daemon's voice path feeds untrusted `Payload::Audio` bytes straight into `decode_f32_le` (allocating by client length). `ChatAsr::feed_audio` now caps a frame at `MAX_FRAME_SAMPLES` (160k ≈ 10 s @16 kHz) and **drops oversized frames before decode** (warn + `None`), so a malformed/malicious client can't make us allocate an unbounded buffer. Tests: `amos-ai` lib **75** pass (1 new: oversized frame dropped, recognizer still usable after); chat_asr 6; clippy `-D warnings` clean; fmt clean.
- **Assistant-voice double-start hygiene (`amos-tauri`, 2026-09-04)**: `assistant_voice_start` now **tears down any previous active voice session first** (`voice.stop()`), so a second start (e.g. a busy mic button) can't orphan the prior reader task or run two listeners emitting events to the same UI. Tests: `assistant_voice` module **12** pass; `amos-tauri --lib` clippy `-D warnings` clean; fmt clean.
- **Web-bundle installer path-traversal hardening (`amos-appstore`, 2026-09-04)**: closes a real security gap found in audit. `WebInstaller` already relied on the `tar` crate's `..`-rejection, but that only guards *archive entries* — the bundle's entry **`start`** is a path parsed from `amos-app.json` *after* unpack, and `read_file` accepted arbitrary `rel`, so `dir.join("../..")` could probe/read outside the install dir. Added `safe_relative`/`safe_join`: `validate_bundle` and `read_file` now refuse any non-relative/`..`/`.`/absolute/NUL path (clear `unsafe relative path …` error). (The manifest `id` was already confined to a safe slug by `AppManifest::validate`/`valid_id` — audit confirmed it, not a defect.) Tests: `amos-appstore` **36** pass (2 new: traversal `start` refused + decoy untouched; `read_file` refuses `..`/absolute/NUL while subdir reads still work); clippy `-D warnings` clean; fmt clean.


- **Call recording — full-stack capability (`amos-telephony` + `proto` + `amos-tauri` + `frontend-ts`, 2026-09-04)**: 通话录音 as a first-class, contractual feature across every layer. Domain kernel (`session.rs`) gains a `RecordingState { Off, On, Failed }` on `Call` with whitelisted transitions — recording is legal only while a call is **Active** and **non-emergency** (emergency/110-112 lines are a hard refusal), auto-reset to `Off` on hang-up, and a device `Failed` state is surfaceable + retryable. `TelephonyProvider` seam adds `start_recording`/`stop_recording` (provider decides allow/deny via a consent/jurisdiction policy knob; domain denies before the provider is consulted). `proto/telephony.proto` adds a `RecordingState` enum, `CallSnapshot.recording`, and `StartRecording`/`StopRecording` RPCs returning the authoritative snapshot; `CallSnapshot` records carry `recording`. gRPC `TelephonyService` maps it all; Tauri bridge exposes `telephony_start_recording`/`telephony_stop_recording` (+ `TelephonyCallPayload.recording`). `PhoneApp` in-call screen offers a record toggle (start/stop, live "正在录音" indicator) that mirrors the daemon's authoritative state and never claims a recording the OS declined. i18n en/zh (`phone.record*`). Tests: 41 unit (recording domain/provider/service: legality, emergency-forbid, policy denial, failure→retry, end-resets, status-carries-recording, RPC error mapping) + 3 `PhoneRecording.test.tsx` DOM tests. Real audio capture stays with the future audio pipeline; this delivers the state machine + contract + UI control.

- **Messages unread → dock badge (`frontend-ts`, 2026-09-04)**: `MessagesApp` now publishes unread incoming messages as app notifications (`app` = “信息”, 💬, capped at 20), so the home dock “信息” badge and notification center reflect short-message unread (cleared as messages are read/cleared, no spurious writes when all read). DOM tests (`MessagesApp.test.tsx`).

- **Messages / 短信息 hardening (`frontend-ts`, 2026-09-04)**: audit + completion of `lib/messages.ts` — `appendMessage`/`appendQuote` now trim text and refuse blank bodies; `normalizeMessages` caps history at `MESSAGE_CAP` (200) so a conversation can't grow unbounded; added `MESSAGE_CAP` const; `MessagesApp` writes every save through the normalizer, so the cap + self-heal apply live (not just on load). New dedicated `messages.test.ts` covers every export (seed/append/quote/normalize-cap/clear/remove/time&day helpers/unread/markRead/markAllRead/constants).

- **Shared outgoing-call hook (`frontend-ts`, 2026-09-04)**: extracted `lib/useOutgoingCalls.ts`, now used by both `ContactsApp` and `PhoneApp` — it owns call-history state and derives the Frequent ⭐/Recent quick-dial lists (name-resolved from live contacts) and the `recordOutgoing` (write calllog + raise phone notification) side-effect, removing the duplicated logic the two apps previously each inlined. Dedicated hook tests (`useOutgoingCalls.test.tsx`): name-resolved lists, and record→calllog+notification.

- **Phone dialer → call history & notification (`frontend-ts`, 2026-09-04)**: `PhoneApp` now records successful dials into `amos.calllog` (feeding the shared Recent/Frequent strips) and raises a phone notification, matching `ContactsApp`; its keypad also shows "Frequent ⭐" and "Recent" quick chip rows (tap backfills the number, name resolved from contacts). DOM tests (`PhoneApp.test.tsx`).

- **Phone contacts / 通讯录 (`frontend-ts`, 2026-09-04)**: a new `contacts` app (👥) with a production-grade `lib/contacts.ts` domain — pure, immutable, validation-normalizing ops (`normalizeContacts` repairs corrupt stores and dedupes ids, name + at-least-one-number required, cleaned/deduped phones), favorites, search by name or phone digits, name sorting (favorites first), durable under `amos.contacts`. New `ContactsApp` UI: search, add (with validation hint), favorite toggle, two-tap delete, and "call" out via the telephony bridge (graceful "daemon offline" when not bridged). Full unit tests (`contacts.test.ts` — 16) plus DOM interaction tests (`ContactsApp.test.tsx` — 4: list, call-offline, two-tap delete persists, add-validation). Registered in the app grid + i18n en/zh.
- **Contacts edit + duplicate guard + grouping (`frontend-ts`, 2026-09-04)**: `ContactsApp` can now edit an existing contact (✏️ prefill → save via `editContact`); adding is refused when the number already exists (`contactsWithPhone`, digit-normalized incl. `+CC`/bare suffix match → "duplicate" hint); the list is grouped by letter (A–Z / `#`) with colored initial avatars (`contactLetter`/`groupContacts`/`avatarHue`). New pure helpers all unit-tested; contacts.ts stays at 100% function coverage.
- **Call history + recents (`frontend-ts`, 2026-09-04)**: new `lib/calllog.ts` domain — pure, durable `amos.calllog`: `normalizeCallLog` (newest-first, keeps history for frequency stats, caps at 60, repairs corrupt stores), `recordCall`, `recentNumbers` (distinct quick-dial list), `frequentNumbers` ("常用"), `logNameFor`, `callDigits`. Successful bridged calls from `ContactsApp` are recorded (functional update, no stale-closure loss), and "Frequent ⭐" (top-by-frequency) and "Recent" quick-dial strips appear (number → contact-name lookup). Reverse lookup `contactNameFor` added to `lib/contacts.ts`. Unit tests (`calllog.test.ts`) + Contacts DOM tests (bridged-call → recents; repeated dial → frequent chip).
- **Radio / connectivity（`amos-radio` + `amos-tauri` + `frontend-ts`, 2026-09-04）**: new transport-agnostic domain crate `amos-radio` — `RadioMode`/`RadioSnapshot`, a `RadioProvider` seam + deterministic `MockRadioProvider`, and a `RadioManager` owning the policy (Airplane mode cascades Wi-Fi + Bluetooth off; the non-airplane radios are refused while Airplane is on; `set` returns the authoritative snapshot). 10 unit tests. The System UI drives it through in-process `radio_status`/`radio_set` Tauri commands (real Wi-Fi/Bluetooth on the no-UI Android base are Android system services reachable from the System UI APK, so unlike telephony these do not round-trip through the headless daemon), mirroring each snapshot back into the durable `amos.settings` store (cross-window broadcast). Notification-center wifi/bluetooth/airplane tiles go through the backend, with a same-policy `flipRadio` fallback outside Tauri and an authoritative `radioStatus` sync on open. A real Android backend is scaffolded as `AndroidRadioProvider` under the `android` feature (JNI `WifiManager`/`BluetoothAdapter`; `cargo check --features android` validates it compiles; on-device hookup pending). Design: `docs/radio.md`.
- **Status-bar radio indicators (`frontend-ts`, 2026-09-04)**: the top `StatusBar` (home + every app shell) now shows Wi-Fi / Bluetooth / Airplane glyphs (airplane supersedes the others) derived from the shared quick-settings, updating **live** — same-window toggles via a new `STORE_CHANGED_EVENT` write signal, cross-tab via `storage`, and cross-window/bridged via the `store-updated` backend event. New reactive hook `lib/useStoreValue.ts` + pure `radioIcons` selector (unit-tested).
- **Location master switch (`frontend-ts`, 2026-09-04)**: the Notification-Center `location` quick toggle is now a real system-level location-services switch. `locationEnabled`/`flipLocation` encode "default ON, explicit OFF disables", the tile reflects it, and `MapsApp` "Locate" refuses geolocation whenever it is OFF (even if the app already holds the `location` grant) with a localized hint; store-reactive via `useStoreValue`.
- **Do-Not-Disturb (`frontend-ts`, 2026-09-04)**: the Notification-Center `dnd` quick toggle now really silences the OS — when ON (`dndActive`), every unread badge on the home screen / dock is hidden, and the notification-center list is shown muted (a 🌒 "DND" chip replaces the unread count). Home-dock notifications/quick-settings are read reactively via `useStoreValue`, so toggling DND anywhere updates the badges live. With this and Location, none of the quick toggles is purely cosmetic anymore.
- **Persistent DND indicator + ring/vibrate policy (`frontend-ts`, 2026-09-04)**: the `StatusBar` now shows a persistent 🌒 moon while Do-Not-Disturb is active (and a 🔕 when the alert policy mutes alerts), always visible on home and inside every app. New `lib/sound.ts` models the notification sound policy bits — `ring`/`vibrate` under `amos.sound` (both default ON) with `normalizeSound`/`effectiveAlert` (DND mutes both) + a reactive `useAlertPolicy` hook. Unit-tested.
- **Notification-arrival haptic (`frontend-ts`, 2026-09-04)**: a real consumer of the sound policy — when the home screen's unread set grows, HomeDock fires a short `navigator.vibrate` pulse, but only if the *effective* policy allows vibration (so DND or "vibrate off" silences it). Pure gate `shouldVibrateOnArrival` (unit-tested); mount-time existing badges never buzz.
- **Notification-arrival ring (`frontend-ts`, 2026-09-04)**: completes the sound policy by also honouring the `ring` bit — the same arrival path now plays an asset-free two-note chime via the Web Audio API (`lib/notifyTone.ts`, synthesized `OscillatorNode` + gain envelope) only when the *effective* policy allows audible alerts (DND / "ring off" silences it). Pure gate `shouldRingOnArrival` (unit-tested); no-op in SSR/tests/headless.
- **Global arrival alert (`frontend-ts`, 2026-09-04)**: the vibrate + ring arrival logic was hoisted out of `HomeDock` (which only renders on the home screen) into a reusable `useNotificationAlert` hook, mounted once in the `Shell` — so a new notification now pulses/rings on **every** screen (home, inside an app, even the lock screen), still gated by the effective sound policy and DND.
- **Notification banner (`frontend-ts`, 2026-09-04)**: a transient toast now appears near the top whenever a notification arrives (newest one; auto-dismisses ~4 s), layered over every screen via a `NotificationBanner` mounted once in the phone frame. Do-Not-Disturb suppresses the banner (those notifications stay only in the Notification Center). **Tap to acknowledge** — clears that app's notifications (badge disappears) and dismisses the toast; works regardless of which screen is showing. Pure arrival selector `newestAddedNotif` (unit-tested) + DOM test for show/acknowledge.
- **Android radio provider wiring (`amos-tauri`, 2026-09-04)**: `amos-tauri` gained an `android` feature (`["dep:jni","amos-radio/android"]`) and a `RadioBridge::from_android(vm, env, context, airplane)` factory that backs the bridge with `AndroidRadioProvider` (real `WifiManager`/`BluetoothAdapter`) instead of the Mock. `scripts/build-android.sh` now also cross-compiles `amos-radio --features android`. Desktop default is unchanged (`mock_seeded`); `cargo check/clippy --features android` on the host validate the wiring compiles — on-device `run()` swaps in `from_android` with the Activity `JavaVM`/`Context` (docs/radio.md §6).
- **Bell unread badge (`frontend-ts`, 2026-09-04)**: the home `TopBar` 🔔 now shows a live unread count (caps at 99+), hidden while Do-Not-Disturb is on — reactive via `useStoreValue`, matching the dock badges.
- **Test coverage completion (`frontend-ts`, 2026-09-04)**: every exported function in the sound/notification/store modules now has a test — `useAlertPolicy` + `useNotificationAlert` (DOM hook tests: defaults, policy reflection, DND muting, DND silencing of the arrival vibrate), `playNotifyTone` (no-op without Web Audio; synthesizes a chime with a stubbed `AudioContext`), and an explicit `STORE_CHANGED_EVENT` dispatch assertion. `settings.ts` / `sound.ts` / `amosStore.ts` / `useStoreValue` / `notifyTone` pure functions are all covered.
- **Per-function coverage drive (`frontend-ts`, 2026-09-04)**: brought `lib/permissions.ts` (83→100% funcs, durable ledger store round-trip), `lib/files.ts` (97.6→100%, `hasName`), `lib/notes.ts` (97.5→100%, `fmtTime`) and `lib/storeApps.ts` (50→100% funcs: tile cache `set/get/tileById`, `subscribe/notify`, and `loadStoreTiles` via a fake bridge) to full function/line coverage. Together with the already-at-100% pure modules (`bounded`, `maps`, `calculator`, `photos`, `weather`, `providers`, `systemButtons`) the pure-logic layer is now comprehensively covered (all 54 tests across the touched files pass).
- **Component-layer coverage (`frontend-ts`, 2026-09-04)**: per-component `--coverage` sweep (pure presentational ones like `AppIcon`/`Segmented` already 100%). Raised `components/CapabilityGate.tsx` function coverage 71→86% by adding interaction tests for the `Deny`/refused branch, the `onAllowed` callback, and the "deny now → allow later" flow (children stay hidden until granted, nothing persisted on deny).
- **Session-feature component interaction tests (`frontend-ts`, 2026-09-04)**: new DOM tests cover the radio/DND/notification UI branches — `StatusBar` (default wifi/bt glyphs, airplane supersedes with ✈️, DND shows persistent 🌒), `HomeDock` (unread badge appears for an app and is hidden under DND), and `NotificationCenter` (location master starts ON and toggling persists OFF; the dark tile drives the real theme). 6 tests across the three files, all passing.
- **Reactive-store cross-window correctness (`frontend-ts`, 2026-09-04)**: `useStoreValue` now honours the authoritative `store-updated` payload from the Rust store instead of re-reading this window's (possibly stale) `localStorage`, and mirrors it back into `localStorage` for consistency. End-to-end tests simulate a second Tauri window pushing a `store-updated` event and assert the hook re-renders with the authoritative value and correctly resets on removal.
- **Network time calibration (`amos-timesync`, 2026-09-03)**: transport-agnostic `TimeSource` seam (`MockTimeSource` deterministic, `HostClock` offline fallback) feeding a `SyncedClock` that turns an authoritative network time into a signed host-clock offset, applies it to `now()`, rejects implausible times (plausibility window 2000–2100), and atomically persists the last-known-good offset (corrupt/missing file degrades gracefully). A `Timekeeper` runs the periodic sync loop. Real SNTP query is a `ntp` feature (`NtpTimeSource` via `sntpc` + `sntpc-net-std`, blocking work on `spawn_blocking`); a `ntp_probe` example does a live query. Deps: `async-trait`, `thiserror` (+ optional `sntpc`/`sntpc-net-std`).
- **Supervised periodic time sync (`amos-supervisor` `timesync` feature, 2026-09-03)**: optional background wall-clock calibration orchestrated alongside daemon supervision. Default build and behaviour unchanged; enabling the `timesync` feature and `AMOS_TIMESYNC=1` at runtime spawns a periodic `SyncedClock` loop (`AMOS_NTP_SERVER` for real SNTP, `AMOS_TIMESYNC_STATE`/`AMOS_TIMESYNC_INTERVAL_SECS` optional), exposes live calibrated-time status (`TimeSyncHandle::report()`: corrected now / offset / staleness, printed at startup, on SIGUSR1, and before shutdown), and exports the state path to supervised children via `AMOS_TIMESYNC_STATE` so any daemon can load the last-known-good calibrated clock. Added to `make gated-check`.
- **App store core engine (`amos-appstore`, 2026-09-03)**: transport-agnostic `AppManifest` catalog + `Version`, sha256 integrity check, and an `AppStore` engine driving download → verify → install / upgrade / uninstall over a pluggable `StoreProvider` (deterministic in-memory `MockStoreProvider` today) with a JSON-persisted installed registry. Publish contract + developer onboarding in `docs/appstore.md`.
- **App store CLI (`amos-appstore-cli`, 2026-09-03)**: headless terminal driver over the same engine (demo/catalog/search/install/upgrade/uninstall/status), mirroring `amos-mail-cli`; `--catalog <URL>` (with `--features live`) swaps the offline demo for the real HTTP backend.
- **App store HTTP backend (`amos-appstore` `live` feature, 2026-09-03)**: `HttpStoreProvider` fetches a remote `MockCatalog` JSON catalog + downloads packages over HTTP (ureq in `spawn_blocking`); feature-gated so the default build stays offline-green. Both the CLI (`--catalog`, `--features live`) and the Tauri `StoreBridge` (`AMOS_APPSTORE_CATALOG`, `--features appstore-live`) can select it at runtime via a type-erased `Box<dyn StoreProvider>`.
- **App store publisher signing (`amos-appstore`, 2026-09-03)**: Ed25519 signing/verification of app manifests (`DeveloperKey`/`sign_manifest`/`verify_manifest_signature`, `publisher` field); the engine verifies a signed manifest's signature before install/upgrade and refuses mismatches (`BadPublisherSignature`). Dep: `ed25519-dalek`.
- **Web-bundle installer (`amos-appstore`, 2026-09-03)**: `webinstall::WebInstaller` unpacks a verified `tar.gz` web-bundle (`index.html` + assets + `amos-app.json`) into `<root>/<id>/`, validates the entry file, writes `manifest.json`, and uninstalls; `tar` refuses `..`/absolute paths. Deps: `tar`, `flate2`.
- **Web-bundle server resolver (`amos-appstore`, 2026-09-03)**: `serve::{resolve_request, content_type_for}` maps a host request path safely onto a file inside an installed bundle (refuses traversal/`..`, falls back to `index.html`, returns MIME + `nosniff`) — the server-side gate a future custom-protocol host needs.
- **Engine web-bundle install (`amos-appstore`, 2026-09-03)**: `AppStore::with_web_install_dir(dir)` makes `install`/`upgrade` of a `tar.gz` bundle also unpack it to `<dir>/<id>/` (and `uninstall` removes it); a bundle that can't unpack fails the install and isn't recorded. Tests cover unpack→uninstall and broken-bundle rejection.
- **StoreBridge bundle serving (`amos-tauri`, 2026-09-03)**: `StoreBridge` reads `AMOS_APPSTORE_INSTALL_DIR` and configures the engine's web-install dir; new command `appstore_bundle_resource(id, path)` returns a bundle file as base64 + MIME (sanitised by `serve::resolve_request`), so a local web-bundle can be rendered without a custom protocol. Headless-tested.
- **App Store System-UI page (`frontend-ts`, 2026-09-03)**: new "App Store" app (`components/StoreApp.tsx`, registered in `APPS`/`COMPONENTS`) browses the catalog (offline demo or HTTP) and install / update / uninstall through the `store*` bridge, with graceful offline fallback outside the Tauri shell. i18n zh/en.
- **Dynamic home registry for store apps (`frontend-ts`, 2026-09-03)**: store-installed apps become persistent home-screen tiles (`lib/storeApps.ts`, `store:<mid>` ids merged into `amos.home.layout`); `HomeDock`/title/`AppComponent` resolve them and tapping opens a placeholder container (`components/ExtApp.tsx`) until a real runtime host (installer) lands. Store page changes notify the shell live.
- **Durable system state store (`amos-tauri` + `frontend-ts`, 2026-09-03)**: `SharedStore` persists every `amos.*` mutation to disk (`$AMOS_STATE_FILE`, default `~/.amos/state.json`); on boot the shell hydrates localStorage from it, so state recovers even after localStorage is cleared.
- **Permission ledger (`frontend-ts`, 2026-09-03)**: `lib/permissions.ts` — pure, immutable, durable (store-backed `amos.permissions`) grants of sensitive capabilities (camera/microphone/location/notifications) per app; query/grant/revoke + normalize. UI dashboard + enforcement wiring next.
- **Privacy & permissions dashboard (`frontend-ts`, 2026-09-03)**: new "privacy" app (`components/PermissionsApp.tsx`, in `APPS`) lists each capability with its granted apps (revocable) and allow/deny toggles for the built-in apps that request it; changes persist in the durable store.
- **Permission enforcement — camera (`frontend-ts`, 2026-09-03)**: `CameraApp` now refuses to call `getUserMedia` until the "camera" capability is granted for the camera app; an allow/deny overlay prompts first, and denying blocks the feed (retry from the Privacy app). Mic/location call sites still to gate.
- **Reusable capability gate (`frontend-ts`, 2026-09-03)**: `components/CapabilityGate.tsx` exports `useCapability(appId, cap)` + a `<CapabilityGate>` allow/deny overlay + `revokeCapability`; generic i18n (`perm.allow/deny/askAllow/denied`). Ready to apply to mic/location call sites.
- **Permission enforcement — location (`frontend-ts`, 2026-09-03)**: `MapsApp` "Locate" now asks before calling geolocation: ungranted taps show an allow/deny banner, and geolocation only runs once the "location" capability is granted for maps (persisted). Microphone call sites (VoiceMicButton / BackendApps) remain to gate with the same hook.
- **Permission enforcement — microphone (`frontend-ts`, 2026-09-03)**: capture is gated behind the "microphone" capability for both the AI app's mic (`VoiceMicButton`, inline allow/deny bubble) and the interpreter's own capture (`BackendApps.InterpApp`, two-tap consent). None call `getUserMedia({audio})` before the capability is granted; allows persist (revoke via Privacy).
- **Time-sync CLI (`amos-timesync-cli`, 2026-09-03)**: headless driver over the same `SyncedClock` — `now`/`status` read the last-known-good calibrated clock from state (no network), `sync` runs one calibration pass (offline host clock by default; `--server`/`$AMOS_NTP_SERVER` does a real SNTP query behind the `ntp` feature), mirroring the other `*-cli` crates. Reads/writes the same `AMOS_TIMESYNC_STATE` file the supervisor exports, so operators and supervised daemons can consume the calibrated time. Added to `make gated-check`.
- **Daemon health & metrics (`amos-ai`, 2026-09-03)**: new `monitoring` module — `Monitor` keeps lock-free counters (RPCs seen at the gRPC boundary, uptime, periodic heartbeat ticks) and a `Monitor::spawn_periodic` self-health heartbeat that logs each snapshot (interval via `AMOS_METRICS_INTERVAL_SECS`, default 60s). RPCs are counted centrally by a tonic interceptor in `serve()`; `StatusReply` now carries `rpc_total`/`heartbeats`, so any `GetStatus` health probe reads the live metrics over the wire. Unit + real-UDS e2e coverage.
- New features and enhancements in development

### Changed
- Modifications to existing features

### Fixed
- Bug fixes

### Deprecated
- Features planned for removal

### Removed
- Features removed from the project

### Security
- Security-related changes and patches

## [0.1.0] - 2025-09-01

### Added
- Initial release with core architecture
- AI daemon (amos-ai) with gRPC server over UDS
- Tauri 2 System UI (amos-tauri) as gRPC client
- Window manager state machine (amos-wm)
- Protocol buffer definitions (amos-proto)
- Waydroid/APK compatibility layer (amos-android)
- CI/CD workflow with GitHub Actions
- Comprehensive documentation

### Notes
- This is the initial beta release
- API and architecture subject to change before 1.0

---

## Template for new releases

When creating a new release:

1. Update version numbers in `Cargo.toml` across all crates
2. Update version in `tauri.conf.json`
3. Add corresponding section below with:
   - Date in YYYY-MM-DD format
   - Clear categorization of changes
   - Links to related issues/PRs
4. Update any affected documentation

### Commit message format:

```
chore(release): v0.X.Y

- Brief description of major features
- Link to release notes
```

### Release checklist:

- [ ] All tests passing (`make test`)
- [ ] All linting clean (`make lint`)
- [ ] CHANGELOG.md updated
- [ ] Version numbers updated
- [ ] Documentation updated if needed
- [ ] Security audit completed
- [ ] Tagged in git: `git tag v0.X.Y`
- [ ] GitHub release created with release notes
