# iOS 对齐审计状态（本阶段）

> 方法：每条有**可运行测试**（纯 bun / DOM vitest）或标 `📱` 设备级；不复制已存在功能、不为已满配项造假新增。本文件是会话审计结论的固化件，供复查与排期。
> 健康认证（最近一次全量）：**pure OK（874 例 / 94 文件）· vitest 57 文件 / 403 例 · tsc clean · svelte-check 0/0 · src/lib 行覆盖 94.83%（门禁 90%）· UI 冒烟 4/4（真实 headless Chrome）**。

## 已审计且已对齐（✅，均有测试）
| 模块 | 对齐要点 |
|---|---|
| 计算器 | `%` iOS 挂起运算语义（Phase-fix）、`±`、清除/AC/C、链式、历史、键盘（纯 25 + svelte 10） |
| 相册 | Days 分组+相对日期头（Phase-1）、选择态批量收藏 + 全选/取消（Phase-2）、查看器翻页尊重 ♥ 过滤（fix） |
| 相机 | 镜头翻转/镜像、比例/变焦/网格/闪光/自拍计时/连拍/画质、录像→库、HDR 诚实占位（审计）+ 控制环不变量（Phase-1）+ **点按对焦 AE/AF 锁定**（Phase-2） |
| 电话/来电 | 显示 unknown 兜底、外拨 happy-path、去电记录、状态机（来电/去电） |
| 时钟 | World/秒表(计圈 最快★/最慢●)/定时/闹钟，纯 `time.ts` |
| **日历** | 月/**周**/日程三视图 + 42 格月历 + 彩色日历显隐 + **多天全天（含末日）/延续标签** + 全天/重复(有界子集)/提醒 + 搜索 + **区域首周日** + 事件编辑 + **重复场次编辑（显示所点场次日期、锚点不漂移）**；纯 `calendar.ts` + OS 到点提醒 `calendarCore.ts`（50 + 16 + 36 + 真实挂载 1 例，插桩行全执行 100%，含 2000 例独立暴力参考回归）；**真机 S5/Android 14 语义层验收通过**；差距见 `docs/calendar.md` §6 |
| 媒体/文件/传感器 | 存储统一、Android 桥（设备侧） |

## 剩余真实缺口（按可落地性排序）
### 可离线纯测/补测（下一优先）
1. 各 App 的**过滤/选择语境一致性**抽查（仿相册翻页越界那类）——统一审计 `shown`/`list` 用法。
2. 秒表**暂停态计圈/复位**边角（iOS 秒表允许运行中与暂停时计圈）——若有差异则修。
3. 相机**双击缩放 与 点按锁定 手势互斥**精化（当前双击会同时触发对焦+缩放，属轻微 UX 瑕疵）。

### 需真机 / 系统能力（📱，进 device-bringup）
- 相机：夜间、人像/景深、ProRAW、实况照片、原生光学镜头群、硬件对焦（`pointsOfInterest` 生效验证）。
- 相册：图库年/月/日缩放、全屏捏合/滑动手势、系统分享单、相册写删。
- 分享：真系统分享面板（现为剪贴板占位）。

## 建议
1. 本轮只做了**健康认证 + 审计固化**（无行为改动，工作区干净）。
2. 下一步推荐按上表「可离线」第 1 或第 3 项继续（低成本、纯测可锁），或转向你指定的模块。
