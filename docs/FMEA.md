# AmOS — FMEA 失效模式与影响分析

> **Failure Modes and Effects Analysis**(失效模式与影响分析)
>
> 来源:ARP4754A/ARP4761 民用航空安全方法学 + DO-178C 软件保证框架
>
> 状态:**机读 + 文档化**,由 `scripts/fmea-gen.mjs` 从代码扫描导出
>
> 适用:**所有已识别的安全关键路径**(守护进程、桥、传输、机器人控制)
>
> **诚实声明**:本文件由代码与文档双源扫描生成。**真实部署前的适航审定**要求**独立 FMEA 团队**复核本表;本文件用作工程纪律而非审定证据。

---

## 1. 风险评估尺度

### 1.1 严重性 (Severity, S)

| 等级 | 含义 | 示例 |
|---|---|---|
| **S5 — 灾难性** | 可能致人死亡/系统丧失 | 误急停;UDS 越权访问;电源切断未广播 |
| **S4 — 严重** | 主要功能失效,需操作员干预 | 守护进程崩溃;剪贴板泄露给另一用户 |
| **S3 — 中等** | 次要功能失效,有降级路径 | 推理响应缓存污染;日志轮转失败 |
| **S2 — 轻微** | 用户可察觉但不影响主功能 | UI 文案错误;时区显示偏移 |
| **S1 — 可忽略** | 用户不可见,无操作影响 | 调试日志缺失;诊断计数偏差 |

### 1.2 发生率 (Probability, P)

| 等级 | 含义 | 频次 |
|---|---|---|
| **P5 — 频繁** | 在常规使用中必然发生 | 每次会话触发 |
| **P4 — 偶尔** | 在某些使用模式下发生 | 每周可见 |
| **P3 — 罕见** | 边缘场景下发生 | 每月可见 |
| **P2 — 不太可能** | 在异常配置下发生 | 每季度可见 |
| **P1 — 极不可能** | 仅理论分析可达 | 已知案例中未发生 |

### 1.3 可检测度 (Detectability, D)

| 等级 | 含义 | 检测手段 |
|---|---|---|
| **D5 — 不可检测** | 无任何信号告诉用户/系统 | 静默丢弃;无日志 |
| **D4 — 难以检测** | 仅事后取证才可发现 | 日志文件损坏 |
| **D3 — 可检测** | 需专业工具/读日志 | `get_status.*` 上 wire |
| **D2 — 易检测** | 普通用户可见 | 横幅/通知/UI 变化 |
| **D1 — 自动处理** | 系统自带降级/重试 | 熔断器/超时/重拨 |

### 1.4 风险优先级数 (RPN) = S × P × D

| RPN 范围 | 行动 |
|---|---|
| **≥ 100** | 必须缓解(强制门禁+回归测试) |
| **50-99** | 应缓解(单测覆盖 + 文档化边界) |
| **20-49** | 可接受(纳入监控) |
| **< 20** | 不需行动 |

---

## 2. 关键功能失效模式

### 2.1 守护进程 (amos-ai)

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|---|-----------|----------|
| F-AI-001 | 守护进程进程崩溃 | System UI 失去 AI/翻译/审计/守护能力 | 4 | 3 | 2 | 24 | `amos-supervisor` 崩溃自动拉起;UDS 重拨 | `supervisor-smoke.sh` |
| F-AI-002 | 守护进程内存泄漏 | OOM 杀进程 | 4 | 2 | 3 | 24 | `GenerationPool` 上限;`ResponseCache` LRU+TTL | `pool::tests`, `cache::tests` |
| F-AI-003 | 守护进程未察觉挂起(死锁/长 GC) | 客户端超时但 UI 不知 | 4 | 2 | 4 | 32 | gRPC deadline;`probe_requests_per_second` 独立通道 | `security::probe` 单测 |
| F-AI-004 | 日志 sink 写失败静默 | 事故后无取证依据 | 3 | 2 | 5 | 30 | `LogSinkReport` 上 wire + 计数 | `logfile::tests` |
| F-AI-005 | 推理后端响应截断/乱码 | 用户收到残缺文本 | 3 | 3 | 2 | 18 | 流式契约 + `bytes_per_token` 健康检查 | `inference::tests` |
| F-AI-006 | 后端不可达但声明"已连接" | 永远等待响应 | 3 | 3 | 2 | 18 | `BreakerBackend` 三态熔断 | `breaker::tests` |
| F-AI-007 | 会话累积无界 | 内存耗尽 | 3 | 2 | 3 | 18 | `SessionManager` 上限 100 | `session::tests` |
| F-AI-008 | 池满拒绝但未区分原因 | 无法判断扩容还是重试 | 2 | 3 | 2 | 12 | `Saturated` vs `WaitTimeout` 类型化拒绝 | `pool::tests` |
| F-AI-009 | 审计落盘失败 → 用户被告知"已记录" | 监管不可追 | 4 | 2 | 4 | 32 | `ok=false` 诚实回返;`recorded/attempted/reason` 报告 | `privacy_audit_e2e` |
| F-AI-010 | UDS 异用户访问通过 | 隐私数据被同主机其他用户读取 | 5 | 2 | 5 | 50 | `peercred.rs` 同用户校验 + `AMOS_UDS_PEER` 策略 | `peercred::tests` |
| F-AI-011 | TCP 通道被同主机其他进程访问 | 与 F-AI-010 同形,但跨网络 | 5 | 2 | 4 | 40 | `tcp_auth.rs` token 校验 + 强制环回 | `tcp_token_e2e.rs` |
| F-AI-012 | 资源门告警未送达 | 操作员不知告警 | 3 | 2 | 3 | 18 | `Alerts` 字段上 wire,UI 渲染 | `alerts::tests` |
| F-AI-013 | 速率限制被旁路 | 资源耗尽攻击 | 3 | 3 | 2 | 18 | `validate_probe` 仍走安全层;`Rejected` 写审计 | `security::tests` |
| F-AI-014 | **AI 服务的阈值告警只到 wire ⇒ 操作员不知**(REQ-A449 / REQ-A451):`alerts::AlertTracker` 计算"现在哪些条件成立",但**没有**外发路径;`alerts.rs` 模块文档明写"there is **no delivery**",隔壁 `amos-supervisor`(REQ-A444)已用 `amos-notifier` 把守护进程告警接通了 ⇒ 同一族缺陷的两半(告警真发出 vs 操作员真看见)。**REQ-A451 收口了第二步**(F-DEV-033 同族):上轮的"接线骨架已有,但 `main.rs` 没自动接线 ⇒ `with_notifier_bridge` 只在测试里被调,生产里它就是 `None`,告警永远到不了 wire" | daemon 阈值告警(breaker_open / log_trail_incomplete / engine_degraded / dvfs_failures / power_throttled / pool_rejections)对**没人在轮询**的操作员**完全不可见**;1 Hz 的状态轮询变成"日志一坨、不打到任何地方",同 F-AI-012 但延伸到 **UI 不必轮询**的告警通道 | 3 | 3 | 3 | 27 | ①`notifier_bridge` 值类型:把 `AlertTracker` 的"上次活跃集"与"本次活跃集"做差,**仅在状态转换**触发一次 sink(1 Hz poll ≠ 1 Hz alert);②`severity_to_p_level` 是**唯一**的映射点(Error→P0/Warn→P1);③`notifier_sink`(`feature = "notifier"`)把 bridge 与 `amos_notifier::Dispatcher` 桥接并带 `amos_severity` 标签保留**两端视角**;④`AiAgentService::with_notifier_bridge` 把 bridge 装进服务,`alerts_now` 在 `tracing!` 之外**额外**调用 `bridge.observe(&active)` ⇒ log 与 sink 不可能走偏;⑤**默认构建不增依赖**:bridge 在 sink 为 `None` 时**纯 no-op**,`debug_assert!(self.sink.is_some() \|\| fired == 0)` 守门;daemon 的默认 build 不拉 `amos-notifier`;⑥**自动接线(REQ-A451 / F-AI-014 (a) 步)**:`serve_with_sinks_full` 收 `notifier_sink: Option<Arc<dyn AlertSink>>`,`main.rs` 调 `notifier_sink_from_env()` 读 `AMOS_NOTIFIER`(`1`/`true`/`yes`/`on` 大小写无关,任何未在 allow-list 上的值 — `tru` / `2` — 都会被显式拒绝:不静默 arm)与 `AMOS_NOTIFIER_WEBHOOK`(逗号分隔,`WebhookChannel::try_new` 拒掉的 URL 只 `warn!` 不让 daemon 启动失败);`StdoutChannel::stderr()` 与 dispatcher 的 `with_stderr_fallback(true)` 双保险 — 一个 `AMOS_NOTIFIER=1` 但没 webhook URL 的 box 也能打 stderr 行,跟 supervisor 那边同形 | `notifier_bridge::tests`(8 例:无 sink 0 fire、appear/clear 各 fire 一次、详情变不 fire、多独立各自 fire、复发 fire、稳定的 default);`notifier_sink::tests`(13 例,feature-gated,**新增 11 例 from-env + 1 typo 拒** + 1 真 Recorder → Dispatcher 落 P0/P1);`tests/notifier_alerts_e2e.rs`(8 例,模拟 daemon 状态轮询:健康零 fire / 触发一次 P0 / 持续不再 fire / 恢复发 cleared / 复发再 fire);`tests/notifier_alerts_e2e_v2.rs`(6 例,feature-gated,**新增整链路 `Probe` channel 钉 appear/clear/recurrence/factory 形状**,production-shape dispatcher wiring + `with_stderr_fallback(true)` 的双路径);`server::tests::get_status_drives_the_attached_alert_bridge_on_transitions_only`(1 例,真实 `get_status` RPC 路径驱动整链路,健康/触发/稳定/恢复四态全钉);**真实二进制**:`cargo build -p amos-ai --features notifier` + `AMOS_NOTIFIER=1 AMOS_LOG=info AMOS_SOCKET=/tmp/x.sock target/debug/amos-ai` ⇒ 启动日志 `amos_ai::notifier_sink: amos-ai alerting: notifier sink is armed (AMOS_NOTIFIER=1) notifier=true webhook_count=0` **外加** `amos-ai listening`;`cargo build -p amos-ai`(默认)+ `AMOS_NOTIFIER=1 ... target/debug/amos-ai` ⇒ stderr 一行 `AMOS_NOTIFIER is set but this build has no 'notifier' feature, ...`(默认 build 的"silently off"显式说出来);`rust-unwired-scan` `with_notifier_bridge` 不再报 unwired |
| F-AI-015 | **告警规则覆盖不全 ⇒ 治理动作无人察觉**(F-AI-014 的姊妹面):`alerts.rs` 只覆盖 **6 条**(breaker / engine_degraded / log_trail / pool / power / dvfs);governor 拒绝、隐私被命中、network-guard 阻断、telemetry-spy 抓到的外发 match 都在观察里但**没**触发阈值告警 ⇒ 治理动作在操作员视野之外发生 | 与 F-AI-014 同形:已采集的计数器没有"过阈值"的派生规则,操作员同样看不见 | 3 | 3 | 3 | 27 | **(待做)** —— 新增规则需在 `alerts::RULES` 一一登记,并给出**真实阈值**(不要把"发生过"等同于"应该告警");FMEA 下一轮单独审计 | 暂无 — 登记缺口 |

### 2.1b 告警派发 (amos-notifier)

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|---|-----------|----------|
| F-NOT-001 | **HTTP webhook 的握手只在纸面上能跑通(REQ-A450)**:此前 `webhook.rs` 的端到端只在单测里写过两条(`end_to_end_webhook_round_trip` 与 `end_to_end_500_is_failed`),且每条只看**响应状态**;`tests/end_to_end.rs` 用 `Recorder`,既**不发真网络请求**也不验证 wire body shape;结果是:①请求行/路径若错位(`/v1/alerts` vs `/alerts`)单测看不出来;②`Content-Type: application/json` 若缺失,一些 webhook 接收端会 415 而 webhook 通道把响应**计入 `Sent`**(2xx 是 sent 但 415 不是),导致误报"页面签收";③`Host` 头的真实形态(`host:port` vs `host`)直接影响多 vhost 的路由;④5xx→`Failed` / 4xx→`Dropped` 的语义分桶单测只覆盖一组对照 | 操作员部署到 PagerDuty / Slack / Lark / 飞书之后,任一边对**边界**(`/v1/alerts` vs `/alerts`、`Content-Type` 缺省、`Host` 形式、4xx vs 5xx 分桶)处理不同 ⇒ 告警实际没被接收,**而 dispatcher's metrics 全是 `Sent` 或计数全无**(F-DEV-005 同族:仪器回答了另一个问题) | 3 | 3 | 3 | 27 | ①`tests/webhook_e2e.rs` 起一个**真实**的 mock HTTP server(`127.0.0.1:0`,closure-safe),捕获**真实请求线、headers、body**;②覆盖请求行(`POST <path> HTTP/1.1`)、`Content-Type`、`Content-Length`(与 body 长度一致)、`Connection`、`Host`、`Content-Length` 的字节级匹配;③body 字段(severity/id/message/labels/ts)从 `serde_json::Value` 取出,逐字段断言;④SendOutcome 与 HTTP 状态码的映射 7 条 (`200/202 → Sent`、`400/404/429 → Dropped`、`500/503 → Failed`、HTTP/1.0 兼容、connection-refused → Failed);⑤`WebhookChannel::body` 与线上的 JSON (除 `ts`)全等;⑥Dispatcher + WebhookChannel 端到端:每条 distinct id 必落线 + 至少一条 `sent`/`suppressed` 计数 | `tests/webhook_e2e.rs` 16 例 (含 `body_matches_webhook_channel_body_helper`、`headers_are_content_type_json_and_content_length_exact`、`dispatcher_fans_out_through_a_real_webhook_and_a_recorder`、`sequence_200_5xx_200_records_outcomes`);`cargo test -p amos-notifier` 34 lib + 8 Recorder e2e + 16 webhook_e2e + 0 doc 全绿;`cargo clippy -p amos-notifier --tests --all-features -- -D warnings` 净 |

### 2.2 窗口管理 (amos-tauri/wm)

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|-----|-----------|----------|
| F-WM-014 | Phone 形态打开多个 app 窗口 | 布局混乱,与手机语义不符 | 3 | 1 | 2 | 6 | `multi_window=false` 在窗口创建时强制拒绝 | `phone_form_factor_refuses_second_app_window` |
| F-WM-015 | 分屏窗格尺寸低于可用值 | 用户无法点击或看清内容 | 3 | 1 | 2 | 6 | `enforce_min` 在应用布局时自动提升 | `enforce_min_is_applied_to_split_panes` |
| F-WM-016 | Tablet 窗口意外可缩放 | 触摸设备无缩放交互,行为意外 | 2 | 1 | 2 | 4 | `.resizable(policy.free_resize)` 强制应用 | （隐式：Tauri 原生行为）|
| F-WM-017 | 屏幕变小后 app 窗口挂在屏幕外 | 用户在屏幕上**看不到也点不到**自己的窗口 | 3 | 1 | 2 | 6 | 每次真实屏幕变化后 `WmState::reclamp_windows()` 读平台真实几何(无宿主账本可漂移)、按 `LayoutPolicy::fit_window` 回夹,**只动需要动的窗口**;跳过 Launcher / 外部表面 / 隐藏窗口,读不出的几何计数 + `warn!` 不猜位置 | `reclamp_target_moves_only_windows_that_need_it`、`only_a_believable_reading_becomes_a_window_position` |
| F-WM-018 | 被拒的 app 窗口在状态机里**留痕**(模型有窗口、屏幕上没有) | 布局/焦点/z 序被幽灵窗口污染(REQ-A227 形状) | 3 | 1 | 3 | 9 | 判定**在注册之前**问:`WmState::check_new_app_window()`(生产 `open()` 与测试缝 `register_app()` 共用),拒绝是数据(`AppWindowRefusal`)而非字符串 | `a_refused_app_window_leaves_no_trace_in_the_model`、`a_class_without_multi_window_refuses_the_second_app_window` |
| F-WM-019 | **桌面专属 API 未做 cfg 门 ⇒ 整个 Android APK 构建中断，而所有门都绿**（实测 2026-09-16）：给桌面窗口设 `title_bar_style(Overlay)`（REQ-A249 §4.3）的那一行被一个**运行期** `if policy.form == FormFactor::Desktop` 包着 —— 运行期守卫**不能让方法在编译期存在**：`title_bar_style` 在 Android/iOS 的 Tauri 上**根本没有** ⇒ 交叉编译 `aarch64-linux-android` 时 `E0599: no method named title_bar_style found for WebviewWindowBuilder`（`wm.rs:902`），`cargo tauri android build` 在 Rust 阶段即失败 | `make android-app` **整条命令死掉** ⇒ **产不出 APK** ⇒ 装不上、真机验收（本轮正卡在这里）全部堵死 ✗；而在宿主侧它完全**隐形**：`cargo check` ✓、全部测试 ✓、`a11y`/`i18n`/`hover` 门 ✓ 全绿 ⇒ 缺陷只对**移动目标**可见 | 3 | 3 | 4 | 36 | ①**按本仓既有范式修**：`wm.rs` 里 `window_maximize` 早就是成对的 `#[cfg(desktop)]`/`#[cfg(not(desktop))]` + 「cross-target no-op that still compiles」注释 ⇒ 同一范式把该行门进 `#[cfg(desktop)]`；`let mut builder` 只在桌面被重新赋值 ⇒ 加 `#[cfg_attr(not(desktop), allow(unused_mut))]`，两个目标都零警告；②**补上缺席的那道门**：`mobile-check` 只**报告**工具链（从不失败 ✗）、`pdf-android-check`/`vector-db-check` 只交叉编译它们自己的 crate ⇒ 此前**没有任何门编译 `amos-tauri` 的 Android 目标** ✗；新增 `android-app-check` = `cargo ndk -t arm64-v8a -P 31 check -p amos-tauri --features android`（NDK 的 `cc` shim 只有带 API 后缀的名字 `aarch64-linux-android31-clang` ⇒ 环境必须由 cargo-ndk 提供 ⇒ 与 `android-audio-check` 同工具）并**并入 `verify`** | 门本身即保护：`make android-app-check` ⇒ **OK** ✓（`--features android` 走真实移动配置）；**修复判据 = 修复前同一行的实测**：修复前 `make android-app` 报 `E0599 @ wm.rs:902` ✓，修复后 `cargo tauri android build` ⇒ `Compiling amos-tauri` → **`Finished`（33.46s）** 且 `libamos_tauri_lib.so` symlink 进 `jniLibs/arm64-v8a` ✓ ⇒ **APKBUILD=OK** ✓（672MB 未 strip 调试包）；回归证据 = 宿主 `cargo check -p amos-tauri` ⇒ **Finished（4.76s）** ✓ ⇒ cfg 门没有破坏桌面路径 ✓ |


### 2.2c 桌面壳 chrome (前端)

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|-----|-----------|----------|
| F-SH-001 | **不可用的 chrome 控件假装可用**(有可读名字、能 Tab 聚焦、点下去什么都不做) | 用户点了没反应、无从判断是坏了还是没做(REQ-A261 实测:顶栏「控制中心」;REQ-A262 又抓到三处:主菜单五个标题、Dock 废纸篓、桌面右键「新建文件夹」) | 2 | 3 | 4 | 24 | 可用性=**真行为**:未接入的控件 `disabled` + `aria-disabled` + 说明性名字(i18n),而不是留一个按钮;菜单里做不到的**行**渲染成 macOS 的灰项(不是把行删掉——删掉会藏起真实缺口);模块契约让"接入"变成单文件改动(`modules/ControlCenterButton.svelte` / `DockTrashItem.svelte`);Apple 菜单则相反——**能做的真做**(系统设置… / 锁定屏幕),做不到的才灰 | `chrome-widgets.svelte.test.ts`(控制中心 / 主菜单五条 / Apple 菜单灰项)+ `dock-widgets.svelte.test.ts`(废纸篓 disabled + 名字带原因)+ `desktop-shell.svelte.test.ts`(Dock 废纸篓 + 桌面右键「新建文件夹」灰项、同级「更改壁纸…」可点) |
| F-SH-002 | 挂件写回容器模板里 ⇒ **无边界、无法独立交付**(顺序/存在性住在模板而非数据) | 每加一个指示器都要改顶栏;挂件的测试被迫挂载整条栏;两个人无法同时改两个挂件 | 2 | 4 | 3 | 24 | 容器 ↔ 挂件:注册表 `svelte/shellModules.ts` 是数据,容器只按 `modulesFor(slot)` 渲染;**五个槽位各有容器**(顶栏左右 / stage / dock / overlay,REQ-A262 补齐后四个空槽位才有挂件);把手 `ShellChromeApi` 由**壳**注入一次(不是每个容器各注入一份);外观收进 `lib/shellChrome.ts` 一处(顶栏 / dock 瓦片 / 菜单面板 / 运行点) | `shellModule.test.ts`(槽位/顺序/不变量/i18n 键/**每个槽位非空**)+ `chrome-widgets.svelte.test.ts` + `dock-widgets.svelte.test.ts`(**每个挂件单独挂载**,含 dock 瓦片)+ `topbar-container.svelte.test.ts`(容器自身 markup 只有两个槽位)+ `unwired-scan`(每个模块必须被注册表引用) |
| F-SH-003 | **快捷键写在注释/文档里,代码里没有绑定**——"声称的能力"与"真的能力"不一致(REQ-A262 实测:`lib/shellChrome.ts` 写着"⌘Space/F4 are bound",F4 从未绑定) | 文档/注释成了不可验证的承诺;下一个人据此判断"已经支持",缺口被永久掩盖(与 F-IME-003 同形) | 2 | 3 | 4 | 24 | 绑定=**数据**:浮层注册表的 `shortcuts` 一处声明,键处理器用 `moduleForShortcut("overlay", …)` 查它、挂件的 tooltip/`aria-keyshortcuts` 用 `overlayShortcutHint()` 读**同一行**;`formatShortcut()` 从同一份字段生成标签,不可能与匹配器不一致 | `shellModule.test.ts`(绑定唯一 + 匹配集合就是 `F3/F4/Meta+Space/Meta+Tab` + 严格修饰键 + 标签来自同一行)+ `desktop-shell.svelte.test.ts`(F4/F3 真的开合 + 顶栏与 Dock 的 tooltip 显示 F4) |
| F-SH-004 | **跨组件的事件通道存在、目的端处理器是空的**(看起来"接好了",其实什么都不做) | 死接线冒充集成:舞台派发 `desktop:placeholder`,壳收到后 `return;`——用户点了「新建文件夹」既没反应、也没有任何"未接入"的提示 | 2 | 2 | 4 | 16 | 意图走**壳注入的把手**(`SHELL_CHROME_API`):舞台的「打开启动台」调 `openLaunchpad()`,浮层由壳按注册表渲染;两条 `desktop:*` window 事件已删除,做不到的动作变成灰项 + 原因(与 F-SH-001 同一条规则) | `desktop-shell.svelte.test.ts`(「⌘Space / F4 打开浮层」「Esc 只关最上一层」「右键菜单:可点的可点、做不到的灰且有名」——若任一条 window 事件接线复活,这些用例会走不到那条路径)+ `chrome-widgets.svelte.test.ts`(单独挂载时没有把手 ⇒ 什么也不做,而不是抛错) |
| F-SH-005 | **同一套设备交互规则被抄进多个屏**:规则的真实内容(顺序、闸门、拒绝分支、失败后重读)只能从两个组件的实现里读出来,改一处就与另一处不一致 | 一个屏说"平台接管了,请去系统设置",另一个屏照旧去写一个**注定失败**的 `radio_set`;第三个屏(桌面控制中心)会变成第三份 | 3 | 3 | 3 | 27 | 规则收进 `lib/quickRadio.ts`:**一处**实现(`tapRadio`/`syncQuickRadios`/`readManagedSwitches`/`openRadioSettings`),命令是**端口**(`RadioCommands` + `HOST_RADIO_COMMANDS`),返回值写明"设备答了什么"(`RadioTapOutcome` + `persist`:只有设备确认过的写才落 store);三个屏(桌面控制中心 / 通知中心 / 设置)只保留各自的措辞与状态 | `quickRadio.test.ts`(**17** 例,用假端口覆盖 offline/gated/managed/applied/refused/unread 每条分支,含"平台接管的开关从未被尝试写")+ 三个屏各自的 DOM 测试(通知中心 14 / 设置 15 / 控制中心 9) |
| F-SH-006 | **开关型控件只做了一半**:能打开、不能关(再点一次没反应),或面板开着而控件不说自己在开着的状态 | 用户以为坏了:打开面板后再点那个图标,面板不消失;读屏软件也听不出它是开着的 | 2 | 3 | 3 | 18 | 把手给出**成对**的能力:`toggleOverlay(id)`(macOS 的菜单栏项就是这样:同一个点击开/关)与 `isOverlayOpen(id)`(只读,给 `aria-expanded`);`ChromeIconButton` 多一个 `expanded` prop,`undefined` ⇒ 不是展开控件、不渲染该属性 | `desktop-shell.svelte.test.ts`(「⚙️ 点开、再点关,`aria-expanded` 与之一致」)+ `chrome-widgets.svelte.test.ts`(无壳时 `aria-expanded=false`,点了不抛错)+ `control-center.svelte.test.ts`(面板内的关闭路径) |
| F-SH-007 | **浮层层序由组件的 class 决定,而不是用户打开的顺序**:每个浮层自带写死的 `z-index`,于是"后打开的在上"只在恰好对上那对组合时成立 | 用户在控制中心之上按 ⌘Space,聚光灯却可能压在下面;层级看起来随机,因为决定它的是模板里的数字 | 2 | 2 | 3 | 12 | 壳按**打开顺序**给层:`{#each openOverlays as id, index}` 外面包一层 `pointer-events-none` 的 `absolute inset-0`,其 `z-index = 100 + index`(指针事件仍由各浮层自己决定,层序与命中区互不干扰) | `desktop-shell.svelte.test.ts`(「overlays stack in the order they were OPENED」:层数组与 `z-index` 单调递增,换一个打开顺序仍然成立) |
| F-SH-008 | **系统快捷键把焦点窗口关掉 —— 漏到 "main" 上 ⇒ 整个桌面壳消失** | 用户按 ⌘W 想关掉当前 app,**整个 Launcher 关闭**,桌面一片黑;若 `focusedWindowLabel` 实现错位(如把 `state` 读成 `focused`),`wm_close` 收到 `undefined`,宿主按 `String::new` 处理 ⇒ 行为不可预期 | 3 | 2 | 3 | 18 | `DesktopShell` 的 `handleSystemShortcut` **先于**浮层快捷键:⌘W→`wm_close(label)`、`label !== "main"` 且**无焦点时是 no-op**;`focusedWindowLabel` 由 `wm_windows` 的 `focused: true` 字段读出(不是 `state: "Focused"` —— 后者只是分类),与 Dock 的运行点白点**共用同一组 5 s 定时器**;严格修饰键(`meta` 必须按下、未列出的修饰键必须缺席)防止 `w` 被当成 `⌘W` | `desktop-shell.svelte.test.ts`(「system shortcuts route through the focused window to `wm_close` / `wm_hide`」带 `label: "files"` + 「no focused window ⇒ no command reaches the host」+ 「plain ⌘+key does not fire」,**负控**:把 `focused` 读成 `state` ⇒ 用例 FAILED) |
| F-SH-009 | **Dock 右键菜单在组件已卸载后访问 `$props.label`**——DockContextMenu 调 `await invoke("wm_close", { label })` 之前先 `onclose?.()`,父级的 `{#if ctxMenu}` 立即卸载本组件;Svelte 5 中**已卸载组件的 `$props` getter 读取会抛 `Cannot read properties of null (reading 'label')`**,栈精确指向 `Dock.svelte:304:19` 那个 `get label [as label]` | Dock 右键菜单的 Quit/Hide/Show **全抛 TypeError**,菜单看起来"按了没反应";生产环境会污染控制台/诊断 ledger | 3 | 2 | 3 | 18 | DockContextMenu 的每个动作函数**先把 `label` 读到局部 `targetLabel` 再调 `onclose?.()`**(闭包捕获,不是"复制字符串"——写在文件头注释的 *Prop-read ordering* 段);这是 Svelte 5 `{#if}` + `$props()` + 异步动作的**必踩**坑,也是本轮修法第一版自己又踩一次的洞 | `desktop-shell.svelte.test.ts`(「right-click an app tile opens a dock context menu, and Quit calls `wm_close`」,**负控**:不先读 `label` 到局部 ⇒ 用例抛 `TypeError: Cannot read properties of null (reading 'label')`,栈指向 `Dock.svelte:304:19` —— **这是修复前真会抛的栈**,不是"我以为会") |
| F-SH-010 | **多选 Shift-click 直接 `replace` selection 而非 `union`** —— 一段实现里写 `selectedIds = new Set(unionRange(anchor, target))`,把"之前已经点过的、不在 anchor..target 范围内的 icon"**静默丢弃**;典型场景:Cmd-click A 选中 A,Cmd-click C 再选中 C(set = {A, C}),Shift-click E,期望 {A, B, C, D, E}(union),**实际**只剩 {B, C, D, E}(A 丢了)——用户"明明选过 A"的图标突然反白,无法判断"被某个看不见的规则清掉了",只能从头点一遍 | 用户在桌面多选上的预期=macOS Finder 的 union 语义;实现走 replace ⇒ 选区被静默吃掉且**没有任何提示**(选区计数 chip 同步变少,看起来"是 shift-click 本来就这么多") | 3 | 3 | 4 | 36 | `onIconClick` 的 Shift-click 分支改成 `selectedIds = new Set([...selectedIds, ...unionRange(anchorId, id)])` —— 范围作为增量**并入**已有选区,而不是替换;同时 Cmd-click toggle-off (`wasSelected`) 不动 `anchorId`(macOS:anchor 留在上一次真正的 add 上,而不是被 remove 的 id 上),只更新 `focusedId` —— 这两条规则共同保证"Cmd-click + Shift-click"的手势链不丢选 | `desktop-shell.svelte.test.ts`(「Cmd-click toggles one icon in/out; Shift-click unions a range (F-SH-010)」,**负控**:把 union 改回 replace ⇒ 用例 FAILED `clock should be selected after Shift-click: expected false to be true` —— **是修复前真会失败的断言**,不是"我以为会";把 toggle-off 改回 `anchorId = id` ⇒ 用例 FAILED `expected false to be true`) |
| F-SH-011 | **顶栏 Edit → Select All 与键盘 ⌘A 是两条平行的"全选"路径** —— 一处更新选区模型、一处忘改;典型场景:键盘 ⌘A 改成"全选 + 置 anchor 于最后一格",顶栏菜单的 `edit.select-all` 还**只派发**合成 keydown 事件,这条事件也许走不到 `onWindowKeyDown`(浏览器差异、合成事件不被冒泡到 window 等);用户从菜单点"Select All"图标**一个都没选中**,看起来"菜单是个装饰" | 用户在顶栏与键盘之间选择操作路径,期望**两者等价**;实际只能从一条路径选中,另一条路径"按了没反应" | 2 | 2 | 3 | 12 | 顶栏 `edit.select-all` 派发 `new KeyboardEvent("keydown", { key: "a", metaKey: true })` 到 `window`,由 `DesktopStage.onWindowKeyDown` **同一份代码**消费 —— 不是再写一份"全选图标"的循环;happy-dom 下 `window.dispatchEvent` 确实会让 window 上的 keydown listener 收到(已 probe 验证),且 `meta` 修饰键被 `onWindowKeyDown` 的严格检查通过;这是"两个 UI 入口 → 一个真源"的标准模式 | `desktop-shell.svelte.test.ts`(「topbar Edit → Select All marks every visible desktop icon as selected (F-SH-010)」,**负控**:把 `edit.select-all` 的 `run` 改成 `return`(no-op) ⇒ 用例 FAILED `Edit → Select All must select every icon, not just focus the grid: expected null to be truthy`,断言 chip 出现 + 4 个 icon 全部 `aria-selected="true"`) |
| F-SH-012 | **共享工作树里**未提交**的工作被并发会话整体回退（过程失效，本轮实测到）**：多个会话同时在同一工作树里干活，`git checkout` / `reset --hard` / `stash` 这类**树级**动作会把别人**未提交**的改动一并抹掉。本次实测：一个会话的整套改动（`Shell.svelte` 的键盘准入 + `inert` 绑定、`svelte-tests/shell.svelte.test.ts` 的用例、`lib/time.ts` 的 DST 修复、前端 `package.json` 的扫描器接线、以及 **TRACEABILITY / FMEA / CHANGELOG / §8** 的登记）在几个提交之后**全部消失** ✓ | 后果里最危险的一条是**它不会出声** ✗：树照旧能编译、老行为照旧、`git status` 甚至显示**干净**（文件被 checkout 回旧版本 ⇒ "未修改" ✓）⇒ 你以为工作还在。第二个后果是**幸存物变成孤儿** ✗：新建的文件（untracked）**活下来**，但它们依赖的改动没了 ⇒ 例如 `systemKeys.ts` 在、而 `Shell.svelte` 不再 import 它 ⇒ 扫描器把它们报成"**newly unwired exports**" ✗，读起来像代码缺陷，其实是**工作被回退**的症状 ✓ | 4 | 2 | 4 | 32 | ①**每轮结束就提交**（工作树不是账本；本次是在发现损失后才第一次提交幸存文件 ✓）；②**永不**在共享工作树里跑树级 `git checkout/reset/stash`（要回退就只对自己的路径 `git checkout -- <paths>` ✓）；③**用内容校准判断**，不要信 `git status`（本次用 `grep -c 'shellKeyIntent\\|ShortcutHud' Shell.svelte` = 0 与 `git log -1 -- <file>` 才确认 ✓）；④发现回退后**先把幸存的新文件提交/接线**，再谈重做（本次把 `testreach:*` 与 `hover/lifetime/reactfree` 的 `check` 接线补回 ✓，而**不**去重写整个会话 ✗）；⑤FMEA/追踪登记**越早提交越好**（登记被回退 = 下一次审计会重复同样的结论 ✗） | `crates/amos-tauri/frontend-ts/src/lib/desktopKeys.ts`、`crates/amos-tauri/frontend-ts/scripts/orphan-test-scan.mjs`（幸存物，本轮补接线后提交）、`crates/amos-tauri/frontend-ts/package.json`（`testreach:*` 与 `check` 链修复）。**验证**：`git log --oneline -1 -- crates/amos-tauri/frontend-ts/src/svelte/Shell.svelte` ⇒ 停在 **REQ-A290**（远早于本会话 ✓）；`grep -c 'shellKeyIntent\\|ShortcutHud\\|overlayOpen' Shell.svelte` ⇒ **0** ✓；`grep -c addLocalDays lib/time.ts` ⇒ **0** ✗（DST 修复同样丢失 ✓）；`grep -c testreach package.json` ⇒ 修复前 **0** / 修复后 **3** ✓；`node scripts/fmea-gen.mjs --check` ⇒ 修复前 **77** 条（回退到 F-SH-011 时期 ✓）、本次登记后 **78** ✓ |
| F-SH-013 | **壳从不 push 历史条目 ⇒ 平台的返回手势**退出整个应用**（REQ-A347 重接）**：宿主把返回实现成 `if (webView.canGoBack()) goBack() else exit()`（Tauri 生成的 `WryActivity`），而全库 `popstate`/`pushState` 计数为 **0** ⇒ `canGoBack()` **永远为假** ⇒ 在应用里、App Library、编辑模式、或任何浮层打开时按返回，**直接离开 AmOS** ✗ | 用户正在读一条消息，按一下返回把**整个系统界面**关掉 —— 不是"回上一屏"而是"离开 AmOS"；在**主屏**按返回本该退出、也确实退出 ⇒ 行为在根部"看起来是对的"，问题因此隐蔽 | 3 | 3 | 3 | 27 | ①**一层级一条历史条目**（`lib/backNav` 的 `levelOf`/`pushDecision`）：跨两级（主屏 → 带浮层的应用）必须 push **两条**，否则一次返回退两级；②**两个方向都幂等**（不用抑制标志）：壳自己回主屏时顺手 `history.go(-n)` 收回多余额度，随之而来的 `popstate` 发现深度已一致 ⇒ 天然不重复动作；③**根上什么都不做**（平台的"在根退出应用"是正解，壳不吞）；④**桌面形态刻意排除**（真窗口有自己的关闭语义）；⑤深链启动也算一层（否则"没打开过却无处可回" ✗） | `svelte-tests/shell.svelte.test.ts`（**+3**：应用=1 条且 `{amosDepth:1}` ✓、其上浮层=第 2 条 ✓、壳自回退 ⇒ `history.go(-1)` ✓、桌面 ⇒ **0 条** ✓）。**负控：关掉 push 分支（`if (false)`）⇒ 恰好 1 红**，报错正是 `opening an app pushed exactly one entry: expected [] to have a length of 1` ✓，md5 还原 ✓ |
| F-SH-014 | **菜单的键帽来自一个**从未定义**的 i18n 键 ⇒ 屏幕上印出键名本身（REQ-A348 收口）**：`TopbarMainMenu` 用 `t(row.shortcutKey)` **动态**取值，而 `desktop.menu.file.closeWindowShortcut` 等 5 个键在 zh/en **都不存在**；`translate()` 的兜底是 `raw ?? key` ⇒ 菜单右侧显示 `desktop.menu.file.closeWindowShortcut` ✗。同一族的第二处：**17 个行标签键**（`desktop.menu.file.closeWindow` 等）也从未定义 ⇒ 整个菜单栏显示键名 | 用户打开"文件"菜单看到的是 `desktop.menu.file.newWindow` 之类的**内部键名** ✗ —— 不是"少了一条快捷键提示"，而是**菜单不可读**；而 `i18n-scan` 全绿 ✓（它的边界明写"运行期表达式不评估"✗），所以没有任何门会红 | 2 | 4 | 3 | 24 | ①**键帽不翻译、且从绑定表派生**：`desktopShortcutLabel(kind)`（`lib/desktopKeys.ts`）渲染 `⌘W`/`⌘M`，平台键（⌘C/⌘V/⌘A）只在一处写成字面量并注明理由；②**没人绑定的行不声称有键**（⌘N 未接线 ⇒ 该行**无**键帽）；③**补齐 17 个标签键**（zh+en 同步，文案取 macOS 惯例）；④**字典"集合"一致性进测试**（`__tests__/i18n.test.ts` 的双字典键比对 ✓ —— 正是它抓到了 12 个只在 zh 的 `ringtone.*` ✗） | `svelte-tests/topbar-main-menu.svelte.test.ts`（**5 例**：键帽=字面 `⌘W`/`⌘M`、不泄漏 `desktop.menu`、⌘N 行无键帽、平台键 ⌘C/⌘V/⌘A、宿主 `menu-event` 有消费者）。**负控**：让 `desktopShortcutLabel` 返回 `""` ⇒ 恰好 1 红（`expected '关闭窗口' to contain '⌘W'`）✓；首版用例曾**两边调同一函数**（"测试在测自己" ✗）⇒ 改为字面期望后才有判别力 ✓ |
| F-SH-015 | **测试文件存在，却**没有任何 runner 会执行它**（孤岛测试，REQ-A349 成门）**：`bun`（`bun-iso-test.mjs`）只扫 `src/__tests__` 与 `src/lib/__tests__`，`vitest` 只含 `svelte-tests/` ⇒ 第三类目录（如 `src/svelte/…/__tests__/`）的文件**静静地永远不跑** ✗。实测：并发会话新建的 `src/svelte/settings/__tests__/KeyboardPage.test.ts` 正是如此，且**根本跑不起来**（DOM 测试写在 bun 的扫描根之外，手动跑即 `ReferenceError: Can't find variable: document`）✗ | 一整份"看起来有测试"的文件**从未执行**，而所有门都绿 ⇒ 它保护的代码**没有任何证据**；更隐蔽的是它会**腐烂**（接口改了没人发现）。本仓在这条路上已交过学费：`bun-iso-test.mjs` 的注释专门解释为何把 `src/lib/__tests__` 纳入扫描（"otherwise those files would silently never run"）⇒ **知道这个失败模式，却没有门守着** ✗ | 2 | 3 | 4 | 24 | ①**新增门** `scripts/orphan-test-scan.mjs`：收集前端全部 `*.{test,spec}.*`，判路径是否落在某个 runner 的根内，否则 **FAIL 并给出怎么改**（DOM 测试 → `svelte-tests/`；纯测试 → `src/__tests__/` 或 `src/lib/__tests__/`）；②**例外必须写进 `scripts/test-reach-allowlist.json` 并带理由**，且**陈旧条目会失败**（文件已可达或已不存在 ⇒ 必须删掉）⇒ 豁免不会腐烂；③**同一个"文件需要自己的运行环境"家族**：`// bun-iso-tz: <zone>` 标记让带时区的文件**独立进程 + 指定时区**运行（`bun-iso-test.mjs` 已认得 ✓），且每个这样的文件**自带"时区真的是它"的断言** ⇒ 不会变成空洞测试 | `scripts/orphan-test-scan.mjs`（**自检 9 例** ✓ + `--json`）、`scripts/test-reach-allowlist.json`、`crates/amos-tauri/frontend-ts/package.json`（`testreach:selftest`/`testreach:scan` 并入 `check` 链 ✓）。**实证**：建门时它 **FAIL 点名那一个文件** ✓；并发会话随后把文件搬进 `svelte-tests/` ⇒ 门立刻报"**豁免已不再需要**"（防腐烂设计**在真实场景当场应验** ✓）；`--selftest` 打断 `runnerFor` ⇒ 1 条失败并指出用例 ✓ |
| F-SH-016 | **一个"有测试、却没有生产者"的服务 ⇒ 功能对用户**根本不存在**，而所有门都绿（REQ-A350 收口）**：`lib/mediaExport.ts`（写共享集合的整个"写入半")有 **6 个导出、15 条测试全绿** ✓，而全库 `src/svelte/*.svelte` **没有一处**引用它 ✗ ⇒ 录音只活在 `VoiceMemosApp` 自己的内部 store 里 ⇒ Files、图库、以及**任何其它应用**都看不到它 ✗ —— 而 REQ-A313 明写"录音必须是**用户找得到的文件**" ✓ ⇒ 能力**在代码里存在、在设备上不存在** ✗ | 用户录了一段音，去 Files / 录音目录找，**什么也没有** ✗；换一台应用（或重启后换一条路径 ✗）同样找不到 ⇒ 与"根本没做这个功能"对用户完全等价 ✓。而工程侧**看不出问题**：单测全绿 ✓、`tsc` 全绿 ✓、`i18n`/`a11y`/`hover`/`lifetime` 全绿 ✓ ⇒ 只有 `unwired-scan` 的"新未接线导出"能把这种**孤儿服务**数出来 ✓（它当时报了 `mediaExport` 的 6 个导出 ✓） | 2 | 3 | 4 | 24 | ①**接线**：录音行加"保存到「录音」目录"动作 ✓ —— `blobBytes`（录音 blob）/ `buildWavBytes`（种子片段的合成音频 ⇒ **保存的就是听到的**）→ `exportNameFor(createdAt, mime)` → `exportToSharedCollection("audio", name, bytes, RECORDING_EXPORT_DIR)` ✓；②**结果如实渲染**（saved / refused / offline / empty，宿主给出新词就原样显示 ✓）并挂 `aria-live` ✓ —— **绝不静默无动作** ✓；③**我第一次的接线有一个真 bug，靠"读服务签名"抓住** ✗：`exportToSharedCollection(kind, name, bytes, dir = CAMERA_EXPORT_DIR)` 的第 4 个参数被我漏掉 ⇒ 录音会被写进 **DCIM/Camera** ✗（一个"用户找不到它"的位置 ✗）⇒ 传 `RECORDING_EXPORT_DIR` ✓ 并把教训写进注释 ✓；④**把门的判据留在原地**：`unwired-scan`（`scripts/unwired-baseline.json`）就是这条纪律的执行者 ✓ —— 新导出要么接线、要么删除、要么带理由进基线 ✓ | `svelte-tests/vmemos.svelte.test.ts`（**+2**：宿主调用序列 = `media_grant_write` → `media_save` ✓、载荷是**字节数组** ✓、**集合是 `recordings` 而非默认的相机胶卷** ✓、文件名形如 `Amos-YYYYMMDD-HHMMSS.wav` ✓、结果文本可见 ✓；拒绝 ⇒ 显示"宿主拒绝了写入"且**不**含"已保存" ✓）。**负控：把导出改成空操作 ⇒ 恰好 2 红** ✓，报错正是"那一行**什么都不显示**" ✓（= 该功能要杜绝的静默无动作 ✓），还原后 7/7 ✓。**等待改为轮询而非固定睡眠** ✓（本仓 `focusTrap` 的既有防抖纪律 ✓）。**另**：首版测试断言**硬编码文案** ✗ ⇒ 并发会话改写文案后误红 ⇒ 改为断言**字典值** `zh[...]`（本仓既有写法 ✓）⇒ 文案再改也不会误红 ✓ |


| F-SH-017 | **动作声称了一个从未发生的效果（"分享"只复制文本，且剪贴板失败时照样声称成功），而一整屏已写好的导出 UX 没有任何生产者**（REQ-A352 收口）：①`PhotosApp` 的 `shareSel`/`shareVideo` 把一段**字幕文本**写进剪贴板 ✓，然后**无条件** `wallMsg = t("photo.shared")`（= "已复制分享文本" ✗）—— 异常被 `catch {}` 吞掉 ⇒ **WebView 无安全上下文时剪贴板本就不存在**，用户仍然看到"已复制" ✗；**照片/视频的字节从未离开应用** ✗。②同一屏幕里，`camera.exportToSystem` / `exporting` / `exported` / `exportOffline` / `exportRefused` / `exportFailed` **六个键在 zh 与 en 都已写好**（按钮名、"正在写入…"、"已写入系统存储（DCIM/Camera）✅"、拒绝、无字节…✗）而全库 `grep camera\.export` **零生产者** ✗ ⇒ 整套 UX 无人可达 | 用户点了"分享"看到"已复制分享文本" ⇒ **以为已经分享出去** ✓；点了"存到系统存储"根本不存在这个按钮 ⇒ **以为相册里有、其实没有** ✗。而工程侧全绿：单测 ✓、`tsc` ✓、`i18n` 键引用扫描 ✓（键在字典里就"存在"✓）、可达性扫描 ✓ ⇒ 只有"读代码找消费者"能发现 ✓ | 2 | 4 | 3 | 24 | ①**声明跟随效果**：抽出 `copyText()`，只有 `await writeText` **真的 resolve** 才 `photo.shared` ✓，否则 `photo.shareFailed`（新增键，zh+en 同步 ✓）；视频那半同样改为 `await`（原来连 `await` 都没有 ✗）；②**接上那六个键**：新增"存到系统存储"按钮（静态图查看器 + 视频浮层各一处 ✓）⇒ `dataUrlToBytes`/`blobBytes` → `exportNameFor(ts, dataUrlMime(...))`（扩展名取自 data URL **自己的**媒体类型 ⇒ PNG 不会被存成 `.jpg` ✓）→ `exportToSharedCollection("image"|"video", name, bytes, CAMERA_EXPORT_DIR)` ✓；③**没有字节就绝不发**：演示/渐变瓦片没有 `data` ⇒ 本地直接说 `camera.exportFailed` **且完全不碰宿主写路径** ✓（在相册里写一个空文件 = 声称有一张并不存在的照片 ✗）；④`expBusy` 期间禁用按钮 ⇒ 一次动作只有一次写入/一行结果 ✓ | `svelte-tests/photos.svelte.test.ts`（**+5**：真字节进 `DCIM/Camera`、name=`Amos-20260916-180507.png`、kind=`image` ✓；拒绝 ⇒ 显示拒绝且**从不**显示"已写入" ✓；无 `data` 的瓦片 ⇒ `camera.exportFailed` 且 `media_save`/`media_grant_write` **调用数为 0** ✓；剪贴板不可用 ⇒ `photo.shareFailed` 且**不含**"已复制" ✓；剪贴板可用 ⇒ 仍显示"已复制"且 `writeText` 真被调用 1 次 ✓）。`src/__tests__/mediaExport.test.ts` **+2**（`dataUrlMime`：大小写/参数归一化 ✓；`data:;base64`、`data:base64`、`blob:`、空串 ⇒ **null 而非猜一个图片类型** ✓）。套件：photos **17 → 22/22** ✓ |
| F-SH-018 | **豁免名单（allow-list）自己没有防腐门 ⇒ 它保护的是早就不存在的文案，并且会吞掉下一个真正的死键**（REQ-A353 成门）：`scripts/i18n-allowlist.json` 用"键 + 理由"豁免那些**代码到不了**的文案 ✓，而 `i18n-scan` 只把它**打印出来**、**从不检查它是否还成立** ✗ ⇒ 键被删掉之后条目仍在 ✓（`files.preview`/`files.previewOpen`/`files.previewClose`/`files.previewFailed`/`media.showingNewest` 就是这么过期的 ✓：HEAD 里 1/1 存在 ✓，工作区已被删成 0/0 ✗，而条目照样"保护"它们 ✓）。**更贵的一面**：只要键进了名单就**跳过死键检查** ✗ ⇒ 一条陈旧的豁免等于给**下一个真正不可达的文案**发通行证 ✓（那正是 F-SH-017 那六个 `camera.export*` 的同类 ✓：它们当时**不在**名单里 ✓，所以死键检查确实抓得到 ✓ —— 名单一旦被滥用就抓不到了 ✗） | 覆盖率**虚假** ✓：一个键"有豁免"读起来像"被审过、故意留着的" ✓，实际可能已不在任何字典里 ✓ ⇒ 审计报告会长期高估字典的干净程度 ✓；而 `fmea-gen`/`unwired-scan`/`orphan-test-scan` 都有"豁免陈旧即失败"的纪律 ✓，唯独 i18n 这一份没有 ✗（同一族门的不一致 ✓） | 2 | 3 | 4 | 24 | ①新增纯函数 `staleAllowList(allow, isReferenced, inDictionaries)` ✓ —— **两类**：`live`（生产已引用该键 ⇒ 豁免什么也没遮住 ✓）与 `absent`（**两个字典里都没有**该键 ⇒ 豁免什么也没保护 ✓，判据收紧过：只在单侧缺失属于**一致性检查**的管辖、且豁免仍在为那一侧工作 ✓）；②接入 `i18n-scan` 的**硬门** ✓（打印 FAIL ✓ + 进 `--json` ✓ + 进 `process.exit` 条件 ✓ + 首行汇总显示 `N STALE` ✓）；③**进自检** ✓（3 条断言：live 陈旧 ✓ / absent 陈旧 ✓ / **仍在履行理由的条目不得报** ✓）；④与 `orphan-test-scan`（豁免不再需要 ⇒ 失败 ✓）、`unwired-scan`（基线项已接线 ⇒ 提示收缩 ✓）保持同一纪律 ✓ | `i18n-scan.mjs --selftest` ⇒ **60 条断言 / 0 失败** ✓（含新增 3 条 ✓，即上下两支都真跑到 ✓）；**实况**：新门一上线立刻报 **5 条** ✓（`files.preview`×4 + `media.showingNewest` ✓，全部由查 `HEAD:` 与工作区的**存在性差异**归因到并发会话的在制删除 ✓，非本轮引入 ✓）；入口链无需改动 ✓ —— `check` 已包含 `i18n:selftest && i18n:scan` ✓ |
| F-SH-019 | **分页的契约有三处与真正的宿主不符，且恢复点在宿主顺序里**不良定义**（REQ-A354 实测更正）**：①`mediaPaging.ts` 的模块头写"**宿主对列表做上限并回传游标**" ✗、`canLoadMore` 的注释写"宿主已经用**它自己的 `total`** 说了还剩多少" ✗ —— 而**实测**宿主 `media_list(state, collection) -> Vec<MediaItem>`（`crates/amos-tauri/src/media.rs`）**既无游标参数、也无页大小、也无总数** ✗ ⇒ 照这些注释去实现 `fetch` 的人**必然写不出能用的东西** ✗。②更实质的是：宿主与 provider 的排序是 `items.sort_by_key(|i| std::cmp::Reverse(i.ts))`（`crates/amos-media/src/hostfs.rs:126`、`provider.rs:278` ✓）—— **只有 `ts` 降序、没有任何 tiebreaker** ✗ ⇒ 同 `ts` 组内顺序是目录扫描的产物 ✓ ⇒ "**取宿主那一页的最后一项**"当恢复点**不是良定义的** ✗：边界落在同 `ts` 组内时，按时间戳恢复会**丢掉该组剩下的项**（或重复）✗。③我还把"宿主文档顺序 `(ts desc, name asc)` ⇒ 正确游标是 `{ts,name}`"**写进了登记册** ✗ —— 那是**推断错误** ✓（宿主既没有 name tiebreaker，也没有游标概念 ✓）；正确命题是**前端必须自持全序** ✓ | 用户点"加载更多"后，**同一秒拍的一批照片里有几张永远不出现** ✗（或重复出现 ✓），而界面、日志、测试**全都正常** ✓ —— 只有数数才发现 ✓；它正是这个模块自己写在头上的那种"看起来能用、直到用户数照片"的失效 ✓。另：注释与实现不符会让**下一位实现者**照错文档写代码 ✗（这类"文档说宿主有、宿主其实没有"的偏差不触发任何门 ✗） | 3 | 3 | 3 | 27 | ①**把实测契约写进模块头** ✓（引用 `hostfs.rs`/`provider.rs` 的行号 ✓，并写明"没有游标、没有页大小、没有总数" ✓）；②**把恢复点表达在前端自己的全序里** ✓：新增 `afterCursor(it, cursor)` ✓ —— `(ts desc, id asc)`，与 `newestFirst` **同一个序** ✓（跨 `ts` 比 `ts` ✓、同 `ts` 比 `id` ✓、游标自身**排除**在外 ✓）；③新增 `pageOf(all, cursor, limit)` ✓ —— 这就是屏幕要交给 `fetchInto` 的 `fetch` ✓（宿主不分页 ⇒ 切片是我们的 ✓：`findIndex(afterCursor)` 定位 ✓、`remaining` 由我们算 ✓、不在列表里的游标按**单调性**落在其后的第一项 ✓）；④**更正登记册里的错误判断** ✓（两处历史行在原句处加注 ✓，不改写历史 ✓）；⑤测试补上**最危险的那一格**：同 `ts` 组跨页 ✓ | `src/__tests__/mediaPaging.test.ts`（**+4**：同 `ts` 组跨页**不丢不重** ✓（`a,b` → `c,d` ✓）；逐页重组**逐字等于**原集合 ✓（无丢失、无重复 ✓）；列表外的游标落在其后的第一项 ✓（三种情形 ✓）；**负控**：只比 `ts` 的恢复谓词**确实会丢掉 `c`** ✓（`["d"]` ✓）而真谓词给出 `["c","d"]` ✓）。套件 **6 → 10/10** ✓；`tsc` 对我的文件 **0 错** ✓ |



| F-SH-020 | **导出的兜底路径在吞掉失败之后仍声称成功；而且那条消息是**硬编码中文**（英文用户看到中文）——`i18n-scan` 的硬编码检查只看 `.svelte` 的 **markup 段**，脚本段里的中文字面量是**门的盲区**（REQ-A355 收口，F-SH-017 同族）**：`NotesApp.svelte` 的 `doExportOne` 在"无后端"时回退到剪贴板 ✗，写法是 `try { await copySelection(text) } catch { /* clipboard unavailable */ }` 然后**无条件** `exportMsg = t("note.exportCopied")` ✗ —— 而 `copySelection`（`lib/clipboard.ts:98`）**本来就返回 boolean** ✓（`clipboardWrite(...) !== null` ✓）⇒ 返回值被丢掉 ✓；`doExportMd` 更直接：`exportMsg = "已复制 .md 到剪贴板（未连接后端）"` ✗ 是**字面量中文** ✓✓ | 用户在无宿主的 WebView 里点"导出" ⇒ 看到"**已复制到剪贴板（未连接后端）**" ✓ 而笔记**既没成文件、也没进剪贴板** ✗ ⇒ 与"数据全损"对用户等价 ✓（他关掉页面以后再也找不回来 ✓）；英文用户更糟 ✓：看到的是一句**中文** ✓（语言被无视 ✓）。工程侧为什么全绿：单测 ✓、`tsc` ✓、`i18n-scan` ✓ —— 因为该门**只扫 markup 段**（`markupOf()` ✓），**脚本段字面量不在其视野** ✗ | 3 | 3 | 4 | 36 | ①**声明跟随效果** ✓：新增 `copiedToClipboard(text)` ✓ —— `copySelection` 的 boolean **被使用** ✓，抛出的写也算失败 ✓；`doExportOne` 改为三态 ✓（真文件 ⇒ `note.exportedTo {name}` ✓；剪贴板真的落地 ⇒ `note.exportCopied` ✓；否则 ⇒ `note.exportCopyFailed` ✓ = "导出失败，且剪贴板不可用——**笔记仍在 AmOS 内，没有丢失**" ✓，先安抚且为真 ✓）；②`doExportMd` 同样改为按结果分支 ✓（成功才有 `note.exportMdCopied` ✓），并把 `"未命名"` 与 `已导入「…」` 两处**脚本段中文**一并键化 ✓（`note.untitled` ✓ / `note.imported {title}` ✓）；③4 个键 zh+en **同步** ✓；④**测试不再替缺陷背书** ✓：原有用例断言的就是那句**错误**文案 ✗ ⇒ 改为断言"**诚实失败**" ✓ 并补"剪贴板可用仍报已复制"的正例 ✓ | `svelte-tests/notes.svelte.test.ts`（**+1 并重写 2**：无剪贴板 ⇒ `note.exportCopyFailed` 且**不含** `note.exportCopied` ✓；剪贴板可用（假桥应答 `clipboard_write`）⇒ `note.exportCopied` 且**载荷真的到达**（`written[0].text` 含正文 ✓）；`.md` 路径同样断言"成功措辞只留给真的复制" ✓）。套件：notes + note-editor **47/47** ✓；`i18n-scan` 我新增的 4 键**无死键** ✓ |
| F-SH-021 | **相机应用拍下的东西永远到不了系统相册 —— 而且"看起来能看照片"的那个控件根本没有处理函数**（REQ-A356 收口）：`CameraApp` 把照片/视频只写进**自己的**私有 store（`lib/photos.ts` 的 `PHOTOS_KEY` ✓ / `lib/cameraCapture.ts` 的 MediaStore ✓）✗，于是系统图库、Files、任何其它应用都看不到 ✗；而 `camera.exportToSystem` / `exporting` / `exported` / `exportOffline` / `exportRefused` / `exportFailed` **六个键早在 zh 与 en 里写好** ✓（连"已写入系统存储（DCIM/Camera）✅"都有 ✓）却**零生产者** ✗ —— 是**先写好了 UX、再没人接线** ✓（F-SH-016 的同族 ✓）。**同屏第二个发现**：底部缩略图按钮 `aria-label={t("a11y.lastPhoto")}` **没有任何 `onclick`** ✗ ⇒ 用户点它期待"看刚拍的那张"（iOS 语义 ✓）而**什么都不发生** ✗ | 用户拍完照，去系统图库/Files 找 ⇒ **什么也没有** ✗；只有回到 AmOS 里、打开 Photos 才看得到 ⇒ 与"相机没做导出"对用户等价 ✓。而"点了没反应"的缩略图会让人以为**照片丢了** ✓（比没有这个控件更糟 ✓：它暗示有内容 ✓） | 3 | 3 | 4 | 36 | ①**接线**：新增 `exportToSystem()` ✓ —— 视频模式导出**最新的那段录像**（`captures` 里按 `ts` 取最大值 ✓，不依赖列表顺序 ✓）、否则导出缩略图那张**静默**；`dataUrlToBytes`/`dataUrlMime`（扩展名取自 data URL 自己的媒体类型 ✓）/`blobBytes` → `exportNameFor` → `exportToSharedCollection("image"\|"video", name, bytes, CAMERA_EXPORT_DIR)` ✓（`dir` 显式传 ✓）；**没有字节就不发** ✓（演示帧、或字节已丢的旧库项 ⇒ 本地直说 `camera.exportFailed` 且**不碰宿主写路径** ✓）；②**如实渲染** ✓：`hint` 那行加 `role="status"` ✓（原本只是个 `<p>` ✗ ⇒ 结果对读屏不可见 ✗），并按四种结果分支 ✓；③**只在有东西可导出时才给按钮** ✓（`last?.data \|\| (mode==="video" && captures.length>0)` ⇒ 没有捕获就没有控件 ✓，不给"按了什么都不做"的按钮 ✓）；④**未修的地方如实登记** ✓：缩略图仍**没有**处理函数 ✗（它应该是"打开刚拍的那张"✓，属于导航接线 ✓）—— 本轮只把它写进本行，不擅自改交互 | `svelte-tests/camera.svelte.test.ts`（**+3**：预置带 `data` 的照片（`last` 由 `latestPhoto(store)` 初始化 ✓ —— 而 happy-dom **没有 canvas** ⇒ 快门路径产出的是**无 `data`** 的照片 ✓、`last` 只在有像素时才设 ✓ ⇒ 靠驱动快门**测不到**这条路径 ✓，这一点写进用例注释 ✓）：真字节进 `DCIM/Camera`、`kind=image`、`name=Amos-20260916-180507.png` ✓；拒绝 ⇒ 显示拒绝且**从不**显示"已写入" ✓；**什么都没拍 ⇒ 控件不存在** ✓。套件 **13 → 16/16** ✓ |
| F-SH-022 | **门的盲区：i18n 的"硬编码文案"检查只读 `.svelte` 的 markup 段 ⇒ 脚本段里写下的用户可见文案**对每一道门都不可见**（REQ-A357 补门）**：`i18n-scan.mjs` 的 `hardcodedCopy()` 用 `markupOf()` 切出 `</script>` 之后的 HTML ✓ ⇒ `exportMsg = "已复制 .md 到剪贴板（未连接后端）"`（`NotesApp.svelte` ✗）这类**脚本段字面量不在其视野** ✗ ⇒ REQ-A355 修完才发现：**它是"出厂就带着中文"的那类缺陷，而所有门全绿** ✓（F-SH-020 因此能溜过去 ✓）。实测盲区规模 ✓：脚本段含 CJK 的字面量共 **1863** 处，其中 **1651 在 `zh.ts` 自己**（字典 ✓ 不算）；余 **212 处 / 24 文件**里**数据与文案混杂** ✓（`cityIndex.ts` 51 处城市名 ✓、`ClockApp` 的 `日一二三四五六` ✗、`FilesApp` 的 `"文档"` ✗…）⇒ **一律报红会变成 212 条的红墙** ✗（没人会读 ✓） | 后果就是 F-SH-020 的后果：**英文用户看到中文** ✓、且**没有任何门会红** ✓（`i18n-scan` ✓、`tsc` ✓、单测 ✓ 全绿 ✓）⇒ 这一类缺陷可以在"全绿"的树里长期存活 ✓；而**盲区的存在与否本身**是照不出自己的 ✗（没有门会报"我这里看不到"✓） | 3 | 3 | 4 | 36 | ①**把门补到另一半** ✓：新增纯函数 `assignedCJKCopy(src)` ✓ —— 判据**故意收窄**到真有判别力的形状："**CJK 字面量作为赋值右值或 `$state(...)`/`$derived(...)` 初值**" ✓（= 会被渲染的文案 ✓）；**数据表不算** ✓（`const CITIES = ["北京", …]` 的元素不是赋值 ✓ ⇒ 地名/种子/夹具是**数据** ✓，混进来只会把真发现埋掉 ✓）；注释跳过 ✓（注释不是文案 ✓）；②**实测收窄后的命中量 = 1** ✓（`MapsApp.svelte:34 let label = $state("北京")` ✓ ⇒ 一个带理由的豁免 ✓，**不产生红墙** ✓）；③**复用既有机制** ✓：`i18n-literal-allowlist.json`（键 = 字面量 + 必填理由 ✓）而不是新造一份基准 ✓；④**同一个"豁免不许腐烂"纪律** ✓：新增 `staleLiterals` ✓ —— 某条字面量豁免在当前源码里**再也找不到** ⇒ FAIL 并提示删除 ✓（与 REQ-A353 给键豁免加的 `staleAllowList` 同构 ✓）；⑤进 `--selftest` ✓（**5 条**断言：赋值 ✓、`$state()` 初值 ✓、数据表**不得**报 ✓、注释**不得**报 ✓、`t(...)` 调用**不得**报 ✓） | `i18n-scan.mjs --selftest` ⇒ **60 → 65 断言 / 0 失败** ✓（新增 5 条，**正反两支都真跑到** ✓）；实况 ✓：新门对当前树报 **1 处**（`北京` ✓）⇒ 加豁免后 **OK** ✓、`staleLiterals` **0** ✓、整份扫描 **exit=0** ✓；`--json` 增 `scriptCopy`/`staleLiterals` ✓；退出码纳入 ✓ |
| F-SH-023 | **一个"看起来像导航、其实没有处理函数"的控件 ⇒ 用户把它读成"我的照片没了"**（REQ-A358 收口，F-SH-021 里登记的那条 open item）：`CameraApp` 底部缩略图带 `aria-label={t("a11y.lastPhoto")}` ✓、**有 `disabled={!last}`** ✓、**显示着刚拍那张的缩略图** ✓ —— 却**没有任何 `onclick`/处理函数** ✗ ⇒ 点它**什么都不发生** ✗。而同屏另外两个控件（快门 ✓、翻转 ✓）都是真控件 ✓ ⇒ 用户逐一试过后只能得出"照片没存上"的结论 ✓ | 比**没有**这个控件更糟 ✓：它**暗示里面有内容** ✓（缩略图就是那张照片 ✓）却对点击毫无反应 ✓ ⇒ 用户以为照片丢了 ✓（iOS 语义里它该打开那张照片 ✓）。工程侧**没有门看得见它** ✗：`i18n-scan` 的"每个可交互元素有可读名" ✓ 只查**名字**（它有名字 ✓）、`a11y-scan` 同理 ✓ ⇒ **"控件有名字但没有行为"这一族没有任何门** ✗ | 3 | 3 | 4 | 36 | ①**按本仓既有范式接线** ✓：`appLinks.ts` 里早已有 `openNote` / `composeSmsTo` / `openSettingsSearch` / `dialNumber` ✓（"设好目标屏幕的 props channel ⇒ 再切 shell 表面" ✓），而 **photos 没有自己的深链** ✗ ⇒ 照抄范式补 `PhotosLink{photoId,nonce}` + `PHOTOS_CHANNEL` + `photosChannel()` + `openPhoto(photoId)` ✓；②**消费端与 Notes 同构** ✓：`PhotosApp` 在 `$effect` 里 `subscribe` ✓ ⇒ 找到该 id 就 `sel = target` ✓（打开查看器 ✓）、**消费后清空 channel** ✓（再回 Photos 不会重放 ✓）、**找不到就诚实忽略** ✓（网格照旧 ✓ **绝不凭空造一张** ✓）；③相机侧一行接线 ✓：`onclick={() => last && openPhoto(last.id)}` ✓（`disabled={!last}` 保留 ✓） | `svelte-tests/photos.svelte.test.ts`（**+2**：请求 `p1` 时查看器显示 **`1 / 2`** ✓ —— 即**被请求的那张**而不是最新的 `p2` ✓（若实现写错成"打开最新"，这里会是 `2 / 2` ✓）；**已在库里不存在的 id ⇒ 不打开查看器** ✓（断言无 `N / M` 计数 ✓））；`svelte-tests/camera.svelte.test.ts`（**+1**：点缩略图 ⇒ channel 收到 `{photoId:"p2"}` ✓ —— 断的是**请求真的发出** ✓，不依赖 shell ✓）。套件：photos **22 → 24/24** ✓、camera **16 → 17/17** ✓ |
| F-SH-024 | **"控件永远无法动作"这一族没有任何门**（REQ-A359 成门）：F-SH-023 那个缩略图（有 `aria-label` ✓、有 `disabled={!last}` ✓、显示着刚拍那张 ✓、**却没有处理函数** ✗）之所以能出厂，是因为 a11y 门只查**"有没有可读名字"** ✗、`i18n-scan` 同理 ✗ ⇒ **"有名字、无行为"看不见** ✓。判据必须是可辩护的 ✓：**没有事件处理函数、又没有别的出路（`href`/`type=submit`/`form=`/`{...rest}`）的 `<button>`** ✓，且**只排除静态** `disabled`/`aria-disabled="true"`（点了也拦下 ✓ ⇒ `DockContextMenu` 的灰项「选项 ›」是**诚实的占位** ✓）✗ —— **动态** `disabled={…}` **不排除** ✓（非禁用态仍可点 ⇒ 必须有处理函数 ✓，F-SH-023 正是这种 ✓）；`src/svelte/modules/` 下的**通用**组件不报 ✓（处理函数由调用方传入 ✓，实测 `ChromeIconButton`/`DockTileButton` ✓） | 若这类控件漏进产品：用户点它**毫无反应** ✓ 而它**暗示有内容/有动作** ✓ ⇒ 比没有这个控件更糟 ✓（F-SH-023 里用户据此以为"照片丢了" ✓）。门缺席的代价写在实测里 ✓：我第一版判据（朴素 `/<button[^>]*>/`）**报了 2 条假阳性** ✗ —— 因为 `onclick={() => {` 的 **`>`** 把标签截断 ⇒ 处理函数读不到 ✓（**同一类错误在探针阶段已经犯过一次** ✓）；改花括号/引号感知扫描后 ⇒ **0 条** ✓ | 3 | 3 | 3 | 27 | ①新规则 **R9 `r9_deadControl`** ✓ 接入 `a11y-scan.mjs` 的 `rules` 表 ✓；②**扫描器**用 `buttonTags()`（**花括号 + 引号感知** ✓ —— 专治箭头函数里的 `>` ✓，并把这次教训写进注释 ✓）；③判据边界全部**实测支撑** ✓（静态 disabled ✓ / props spread ✓ / 通用组件目录 ✓）；④自测 **+4 条断言** ✓：**用 F-SH-023 的历史形状做负控** ✓（`aria-label` + `disabled={!last}` + 无处理函数 ⇒ **必须报** ✓）、静态禁用占位 ⇒ 不报 ✓、props spread ⇒ 不报 ✓、正常按钮 ⇒ 不报 ✓；⑤顺手把自测汇总里那句**陈旧的**"15 assertion(s)" ✗ 改为不带数字的实话 ✓ | `a11y-scan.mjs --selftest` ⇒ **all assertions passed（0 failures）** ✓（含新增 4 条 ✓）；实况 ✓：对当前树 **0 条 dead-control** ✓（两个通用组件被目录规则排除 ✓、灰项被静态禁用规则排除 ✓），整份扫描 **exit=0** ✓（本扫描器的约定：只报告缺口 ✓）；**这一步本身就是"门没冤枉人"的证明** ✓ |
| F-MED-001 | **保存时的 MIME 与文件名不一致 ⇒ Android 把文件改名，产出"内容与名字不符"的文件**（REQ-A360 真机发现 ✓，**只有真机才能看见**）：`crates/amos-media/src/android.rs::save()` 给 Android 胶水传的是 **kind 的默认 MIME** ✗（`crates/amos-media/src/mapping.rs:58` `mime_for` ⇒ `MediaKind::Audio => "audio/mpeg"` ✓，**与文件名无关** ✓），而同一文件里就有**按名字**的 `kind_and_mime_for_name()` ✓ ⇒ 于是 `Amos-20260917-125338.wav` 被 MediaStore 按 `audio/mpeg` **规范化为 `Amos-20260917-125338.wav.mp3`** ✗✗（设备实测 ✓：`/storage/emulated/0/Recordings/` 下确有此名 ✓、MediaProvider 日志显示 `.pending-…wav.mp3` → `…wav.mp3` 的移动 ✓） | 用户（或任何别的应用）按名字判断类型 ⇒ **一个装着 WAV 字节的 `.mp3`** ✓：播放器可能拒播 ✓、分享时元数据错 ✓、用户以为"格式被改坏了" ✓。**门的盲区**：这条路径**只在有 Android 胶水的真机上运行** ✗（host 单测走 Mock provider ✓、前端门只看 `exportNameFor` 产出的名字 ✓ 那是对的 ✓）⇒ **没有真机就永远看不见** ✓ —— 本会话多轮"设备不可达"期间，这处缺陷是**不可观测**的 ✓ | 3 | 3 | 4 | 36 | ①**按名字取 MIME** ✓：`kind_and_mime_for_name(&name)` ✓（`.wav` ⇒ `audio/wav` ✓、`.ogg` ⇒ `audio/ogg` ✓），**名字说不出类型时才回退**到 `mime_for(kind)` ✓；②把这次真机证据与"为什么只有真机能发现"写进代码注释 ✓；③`crates/amos-media/` 动手前**干净** ✓（F-SH-012 纪律 ✓） | **真机证据链** ✓（Ragentek S5 / Android 14）—— ①`provider = "android-mediastore"` ✓、`media_list("recordings")` ⇒ **1** ✓；②`content query content://media/external/audio/media` ⇒ `_display_name=Amos-20260917-125338.wav.mp3` ✓、`_data=/storage/emulated/0/Recordings/Amos-20260917-125338.wav.mp3` ✓；③`adb shell ls -la /storage/emulated/0/Recordings` ⇒ `-rwxrwx--- 48044 … Amos-20260917-125338.wav.mp3` ✓（**48044 字节 = 真字节** ✓，前端 `buildWavBytes` 的合成音频 ✓）；④MediaProvider 日志 `Moving …/.pending-…wav.mp3 to …/…wav.mp3` ✓；⑤UI 自述 = **"已存入系统录音（Recordings）"** ✓（**与文件真的存在一致** ✓ —— 这一条是本会话最想要的结论 ✓）。`cargo test -p amos-media` ⇒ 通过 ✓ |
| F-DEV-001 | **验收探针只采样一次 ⇒ 在 MediaStore 的 `IS_PENDING` 窗口里读出"什么都没写"的假结论；配套的是"仪器的输出被丢弃"⇒ 状态没生效也无从发现**（REQ-A361 成工具能力 ✓）：真机验收里**两次**从"写完立刻读"得出"导出没写进系统集合" ✗ —— ①`media_list` 紧接写入读 ⇒ **0** ✓，②`adb shell ls /sdcard/Recordings` 也看不到 ✗；而**权威测量**（`content query content://media/...` + canonical `/storage/emulated/0/...` 路径 ✓）证明**文件一直在** ✓（48044 字节 ✓）。机理明文：Android 的 MediaStore 以 `IS_PENDING=1` 插入、**之后才发布** ✓，shell 侧的 `/sdcard`（FUSE 视角）也滞后 ✓。第二次的同类错误：我用 `>/dev/null` **丢弃了 `window.__probeExpect=…` 设置调用的输出** ✗ ⇒ 设置**没生效** ✓、正例返回 `found:false` ⇒ 看起来像被测对象坏了 ✓ | 后果是**审计结论反向** ✓：能力已经可用、却被记成"根本没写" ✓ ⇒ 若不再复核，团队会去"修"一个不存在的问题 ✓，而真正的问题（`F-MED-001` 的双扩展名 ✓）反而可能被这个假象盖住 ✓。更深一层：**探针是被反复信任的仪器** ✗ ⇒ 它的时序缺陷会**污染每一轮真机结论** ✓（本会话就有两轮被污染 ✓） | 2 | 4 | 3 | 24 | ①探针能**等自己的期望** ✓：`window.__probeExpect={collection,name\|prefix}` + `window.__probeTimeoutMs`（默认 8s ✓）⇒ 轮询并报告 `found`/`settledMs`/`timedOut` ✓（把"一次性采样"换成"**有界收敛**" ✓）；②文件头把两条规则写成纪律 ✓：**时序**（等落盘 ✓）与**路径**（文件问题一律 canonical + `content query` ✓）；③`matches` 不再假定 `.wav` ✓（图片导出同样认得 ✓）；④**真机双向验证** ✓：负控（永不出现的名字）⇒ `found:false / timedOut:true / 1402ms` ✓、正例（`prefix:"Amos-"`）⇒ `found:true / 437ms` ✓；⑤我自己的纪律：**不再丢弃仪器的输出** ✓（设置调用一律回显 ✓） | `scripts/device-probe-media-export.js`（改造 ✓ + 头注释纪律 ✓）；真机（Ragentek S5 / Android 14 ✓）实证见上 ✓；`make device-media-probe` 仍可用 ✓（无参数时为纯只读列举 ✓） |
| F-TAU-007 | **闹钟的"设备绑定"被文档标注为 seam，却从来没有被调用 ⇒ 睡着的手机叫不醒**（REQ-A362 真机测实 ✓，**只有真机能测出**）：`crates/amos-tauri/src/alarm_sched.rs` 的模块头自己写着"…that **device binding stays a caller/device seam**" ✓，`android-glue/.../AlarmGlue.kt` 也写明约定——Rust 应在**每次 `scheduler_alarm_register` 成功后**调用 `AlarmGlue.schedule(ctx, id, atMs)` ✓（取消时 `AlarmGlue.cancel` ✓，内部是 `setExactAndAllowWhileIdle(RTC_WAKEUP, …)` + `AlarmReceiver` 拉起到前台 ✓）。**实测** ✗：CDP 通过真桥登记一个 3 分钟后的闹钟 ⇒ JS promise **正常 resolve、无错误** ✓，而 `adb shell dumpsys alarm \| grep -i amos` **空** ✗、`dumpsys alarm \| grep -E 'u0a172\|com.amos.ai'` **空** ✗（该应用 uid 10172 ✓）、logcat **无任何 alarm 痕迹** ✗ ⇒ 宿主只把闹钟记进**内存 ledger** ✓，AlarmManager 里什么都没有 ✓ | **用户最在意的场景不成立** ✗：手机进入 doze / 应用被后台杀掉后，**闹钟叫不醒设备** ✓（进程活着时的轮询路径能响 ✓ ⇒ "看起来能用" ✓，正是最难发现的那种 ✓）。**门为何全绿** ✓：这条缝**只在真机的 Android 分支上运行** ✗，host 单测走 ledger ✓、前端单测只验 `nextAlarmAtMs` 的**算术** ✓（那部分是对的 ✓，DST 修复本身有效 ✓）⇒ **能力缺口与算术正确被混为一谈** ✓。**与 F-SH-016/021/023 同族** ✓（能力在代码里、在设备上不存在 ✗），**区别**：这次**文档如实标注为 seam** ✓ ⇒ 不是撒谎 ✗，而是**未落地的设备绑定** ✓ | 4 | 4 | 3 | 48 | **修法的范式本仓已有** ✓（不必发明 ✓）：①`crates/amos-jni/src/lib.rs` 的 `attached(vm)` / `with_env(vm, f)` ✓ 存在，注释明写"取代每个 provider 调用点的 `attach_current_thread`" ✓；②`crates/amos-flashlight/src/android.rs` **正是所需形状** ✓ —— "requires a real Android VM (`jni::JavaVM`) plus a `GlobalRef` to the app `Context`" ✓，由 Kotlin 侧在启动时构造 provider ✓；③处置（**待做** ✗）：新增 amos-tauri 侧的 Android alarm provider（持有 `JavaVM` + Context 的 `GlobalRef` ✓）⇒ `scheduler_alarm_register`/`_cancel` 在 `#[cfg(feature="android")]` 分支调用 `AlarmGlue.schedule/cancel` ✓，**保留**既有 ledger/轮询路径不变 ✓（防回归 ✓）；④**判据现成** ✓：`dumpsys alarm \| grep -i amos` 从**空变非空** ✓，并在 `atMs` 前后用 `dumpsys` + `logcat` 核对**真的唤醒** ✓ —— 这是 DST 链条上最后一块真机证据 ✓ | 真机（Ragentek S5 / Android 14）实证 ✓：登记调用 resolve 无错 ✓、`dumpsys alarm` 三处查询皆空 ✓、logcat 无痕 ✓；代码侧证据 ✓：`alarm_sched.rs` 的 seam 注释 ✓ + `AlarmGlue.kt` 的调用约定 ✓（**两条独立证据**支持"从未被调用" ✓） **⇒ REQ-A369 已收口** ✓：新增 `alarm_sched::android` 绑定 ✓（Kotlin 上呼 `AlarmGlue.attachContext` ⇒ Rust 持 `JavaVM` + Context `GlobalRef` ⇒ `AlarmGlue.schedule/cancel` ✓）、manifest 片段补 `AlarmReceiver` + 精确闹钟权限 ✓、生成 Activity 调 `AlarmGlue.bind` ✓（判据写进 `android-activity-wiring-check.sh` 的 REQUIRED 表 ✓）；**真机判据（`dumpsys alarm` 从空变非空 + 到点真唤醒）仍待做** ✗（设备未连 ✓）|








| F-DEV-002 | **审计登记册自己的表格坏了，而它自己的检查看不见**（REQ-A363 成门）：`scripts/fmea-gen.mjs --check` 原本只验两件事——"文档里的 F-XXX ID 都在清单里" ✓ 与"缓解代码真在仓里" ✓ ——**从不验列数** ✗ ⇒ 一条**丢了 P 单元格**的 FMEA 行（我写 F-SH-022 时丢了 P ✓ 且 impact 末尾的 `\|` **少写一个空格** ⇒ 两个单元格被并成一格 ✓，实测 **8 格 / 表头 9 格** ✓）**通过了全部检查**，却在 Markdown 里**渲染错乱** ✗ ⇒ 审计文档的**可信度**受损：读者看到的是错位的列 ✓（S/P/D/RPN 被错读 ✓） | 后果是**审计记录本身不可信** ✓：FMEA 是这轮工作的**主要产物** ✓ ⇒ 一条渲染错乱的行会让 S/P/D/RPN 与缓解措施**对不上号** ✓，而**所有门全绿** ✓（`--check` ✓、`i18n-scan` ✓、单测 ✓）⇒ 只有**人眼看渲染**才会发现 ✗（我是用一次性 python 才发现的 ✓）。同一族的更普遍形式：**结构化文档的"形状"没有任何检查** ✓ —— 数量、列数、必需的 4 个数字、必填字段 ✓ | 2 | 3 | 3 | 18 | ①新增**纯函数** `tableShapeProblems(text)` ✓ —— 判据 = "**每行的单元格数必须等于它所在表的表头**" ✓（因此文档里 2/3/5 列的**其它表格天然通过** ✓，**不需要豁免** ✓）；用 ` \| `（空格-竖线-空格）切分 ✓ ⇒ 单元格内部按 Markdown 规范写作 `\|` 的竖线**不会**被误当分隔符 ✓（这正是本项目踩过的 `awk -F'\|'` 坑 ✓）；②接入 `--check` 作为 **(c)** ✓ 并纳入 `bad` ⇒ 非零退出 ✓；③**负控用真实历史** ✓：`FMEA_DOC=<pre-fix> node scripts/fmea-gen.mjs --check` ⇒ **`line 120: 8 cells, but the header on line 118 has 9`** ✓✓ —— 点名的正是我修掉的那处 ✓；④为让负控**不必改动仓库文件** ✓，`FMEA_DOC` 支持环境变量覆盖 ✓（同时也是通用测试钩子 ✓） | 负控（真实历史 ✓）：pre-fix 文档 ⇒ **FAIL 并点名 line 120 / 8 vs 9** ✓；当前文档 ⇒ **无 FAIL** ✓（Severity 分布照常打印 ✓）；`fmea-gen --check` 全量 ⇒ **94 条** ✓ |
| F-DEV-003 | **工具的守卫是死代码：`pidof` 的\"正常退出 1\"被判成\"adb 坏了\"**（REQ-A365 收口 ✓，本行是本会话欠下的登记 ✓）：`scripts/device-ui-eval.mjs` 的 `adb()` 对**任何非零退出**都抛 `adb … failed` ✗，而**应用不存在**时 `pidof` 是**正常地**退出 1 ✓ ⇒ `targets()` 里**已经写好**的 \"app is not running\" 消息**永远到不了** ✗ ⇒ 真机上最常见的一种状态（应用被系统杀掉 ✓ —— 本机 loadavg≈21 ✓ 完全可能触发 ✓）被藏在**本工具自己的 node 栈**后面 ✗，我为此花了一次调用追栈才拿到真因 ✓ | 后果是**工具的诊断能力**失效 ✓：一条已经写好的好消息发不出来 ✓ ⇒ \"设备状态\"被读成\"工具坏了\" ✗ ⇒ 与 F-DEV-004 同族（**工具不能把真因说出口** ✓）；更普遍的形式：**\"命令的答案\"与\"命令失败了\"必须分开** ✓ | 2 | 4 | 4 | 32 | ①新增 `adbRaw()` ✓（**不判定退出码** ✓，返回 `{status, stdout, stderr, cmd}` ✓）；②`adb()` 改为**报告** `${cmd} exited ${status}` ✓ —— 把\"命令的答案\"与\"adb 自身失败\"分开 ✓；③`targets()` 改用 `adbRaw(\"shell\",\"pidof\",pkg)` ✓ ⇒ 死代码复活 ✓，消息升级为**可执行**的（`app is not running (no process for …) — launch it first: adb shell monkey …` ✓） | 真机负控（REQ-A365 ✓）：`force-stop` 后 ⇒ 报出**真因 + 启动命令** ✓✓（不再是栈 ✓）。**本行无单测保护** ✗ —— 该分支要一台设备才走得到（`--selftest` 覆盖的是纯函数 ✓），如实记录 ✓ |
| F-DEV-004 | **\"页面卡住\"只报了症状、没报原因 ⇒ 我把它误判成应用缺陷**（REQ-A366 ⑤ 自己记下的欠账 ⇒ REQ-A367 收口 ✓）：`waitForPage()` 超时只说 `page never became ready within 30000ms (last: devtools evaluate timed out)` ✗ ⇒ 读者**无法区分**\"应用坏了\"与\"设备状态不允许 WebView 跑 JS\" ✗ ⇒ 我一度去查 ANR/crash ✗（日志已被滚动冲掉 ✓），真因（`mCurrentFocus=NotificationShade` ✓ ⇒ Android **暂停被遮挡的 WebView** ✓ ⇒ JS 停止 ⇒ `Runtime.evaluate` 永不返回 ✓）是**手敲一次 `dumpsys window`** 才拿到的 ✓ —— 而这个读数本该由**工具自己**在失败时去取 ✓ | 后果是**把环境状态读成产品缺陷** ✓：一次误判的代价是**一整轮审计的方向** ✗（本会话已发生一次 ✓）；同一族更贵的形式：**观察工具说不出自己本可以说出的那句话** ✓ ⇒ 每次真机会话都要人工重复同一段取证 ✓ | 2 | 3 | 4 | 24 | ①纯函数 `explainUnready()` ✓ —— 决策表**必须包含否定分支** ✓：焦点是**本应用自己的窗口** ✓ 或 **IME 窗口** ✓（`InputMethod` 取焦**不遮挡**应用 ✓）⇒ 一律输出 **\"NOT the reason\"** ✓（**不许甩锅给环境** ✓）；②`mCurrentFocus=null`（息屏/锁屏 ✓）与**读不到该行**（`unknown` ✓）是**两种不同读数** ✓ ⇒ \"读不到\"绝不打印成\"没问题\" ✓；③失败路径**现取** `dumpsys window` / `dumpsys activity activities` ✓（`readDeviceState()` ✓，只读、尽力而为 ✓；dumpsys 自身失败只记为\"读数缺口\" ✓，绝不覆盖原始失败 ✓）；④只对**停滞形状**（`timed out` / `never became ready`）附加原因 ✓ ⇒ \"应用没在运行\"这类**已经具体**的失败不被膨胀 ✓ | `node scripts/device-ui-eval.mjs --selftest` ✓（**25 例** ✓，含 **3 条\"必须不许甩锅\"的负控** ✓）；**负控实测** ✓：把 `title.includes(pkg)` 那一支打死（`if (false)` ✓）⇒ 自测 **FAILED 1/25** ✓✓（\"门能红\"的证据 ✓）；**真机正向** ✓（S5 / Android 14 ✓，通知栏**仍在遮挡** ✓）：`node scripts/device-ui-eval.mjs --wait 4000 'document.title'` ⇒ 报症状 ✓ + `mCurrentFocus=Window{… NotificationShade}, mFocusedApp=com.amos.ai/.MainActivity, ResumedActivity=com.amos.ai/.MainActivity` ✓ + \"Android **PAUSES** an obscured WebView … collapse the shade (swipe up) or reboot the phone\" ✓（EXIT=1 ✓） |
| F-DEV-005 | **选项的值被当成了被测表达式 ⇒ 仪器把一个数字当成页面的答案，而且退出码是 0**（REQ-A367 审计中发现 ✓，同一轮修掉 ✓）：`device-ui-eval.mjs` 用 `args.find(a => !a.startsWith(\"--\"))` 取表达式 ✗，而 REQ-A366 新加的 `--wait <ms>` 带**独立取值** ✓ ⇒ `node scripts/device-ui-eval.mjs --wait 5000 'document.title'` 的表达式被解析成 **`5000`** ✗ ⇒ 打印 `5000`、**EXIT=0** ✗ —— 没有任何门会红 ✓，而这条命令正处在**真机验收取证**链上 ✓（旧逻辑一行复现 ✓：`[\"--wait\",\"5000\",\"document.title\"].find(a => !a.startsWith(\"--\"))` ⇒ `5000` ✓） | 后果与 F-DEV-001 同族但**更隐蔽** ✓：探针**回答了另一个问题** ✓ 且**看起来成功了** ✗ ⇒ 真机结论被污染 ✓（本项目的验收证据正是由这类命令产出的 ✓）。它同时暴露一个层面：**参数解析没有任何门** ✓ —— 每加一个取值旗标，就多一条\"取错 token\"的路 ✓ | 2 | 3 | 5 | 30 | ①新增纯函数 `expressionArg(argv)` ✓ + `VALUE_FLAGS` 清单 ✓（`--serial` / `--package` / `--port` / `--wait` / `--file` ✓）：**跳过\"取值旗标\"后面的那个 token** ✓ ⇒ 与参数顺序无关 ✓；②顶层改用它取值 ✓（`expression = file ? … : expressionArg(args)` ✓）；③`--selftest` 用 5 例钉住 ✓，其中 `--wait 5000 'document.title' ⇒ 'document.title'` ✓ 是**修复前必失败**的那一条 ✓ | `node scripts/device-ui-eval.mjs --selftest` ✓：`expressionArg` 5 例 ✓（`--wait` 的值不被当成表达式 ✓、表达式在前也成立 ✓、`--serial`/`--package` 的值被跳过 ✓、`--await` 前有旗标仍取到表达式 ✓、无位置参数 ⇒ `undefined`（由调用方报错 ✓）✓） |
| F-DEV-006 | **\"第一个看起来像原因的东西\"被当成了原因：通知栏只是巧合，真因是 WebView **渲染进程根本没起来** —— 而我自己第一版的渲染检查读的是 logcat 的**时间窗**（`-t 600`），那条决定性日志两分钟后已滚出窗口 ⇒ 检查静默地什么都没找到（**F-DEV-001 同族又犯一次**）**（REQ-A367 同轮实测 ⇒ REQ-A368 收口 ✓）：①**纠正是用受控变量换出来的** ✓：上一轮把\"evaluate 永不返回\"归因于 `mCurrentFocus=NotificationShade` ✓；本轮把通知栏**上滑收起** ✓（`adb shell input swipe` 有效 ✓ —— 上一轮记的\"清不掉\" ✗ 不成立 ✓），焦点随即回到 `com.amos.ai/com.amos.ai.MainActivity` ✓，**但 evaluate 依旧不返回** ✗，且 `force-stop` + 重启应用（**新 pid** ✓）仍是同一形态 ✗ ⇒ 通知栏**不构成**解释 ✓。②**真因** ✓（两条独立读数 ✓）：`logcat` ⇒ `ActivityManager: Unable to launch app com.amos.ai/10172 for service Intent { cmp=…SandboxedProcessService0:0 }: process is bad` ✓；`dumpsys activity processes` ⇒ 该应用四个 `ConnectionRecord{… u0 CR DEAD …SandboxedProcessService…}` ✓ —— **没有渲染进程 ⇒ 页面没有 JS 引擎** ✓ ⇒ 与症状（socket 在 ✓ / devtools target 在 ✓ / `title` 空 ✓ / evaluate 永不返回 ✓）完全一致 ✓；辅助读数 ✓：`ps -A` 只有主进程 ✓、进程本身约 **50% CPU 自旋** ✓（302 jiffies / 6s ✓ ⇒ 明确**不是**\"被暂停\"那一类 ✓，暂停应当接近 0 ✓）。③**我自己的仪器又犯了一次同族错** ✗（如实登记 ✓）：渲染检查第一版用 `adb logcat -d -t 600` ✓ ⇒ 实测那条日志总共出现 **12 次**但**全在启动瞬间**（16:43:38 ✓），两分钟后 `-t 600` 窗口里为 **0** 条 ✗ ⇒ 工具在真机上\"什么都没找到\" ✗；改用**当前状态读数**（`dumpsys activity processes` 的 `CR DEAD` ✓，状态在则行在 ✓）后立即命中 ✓。④**处置** ✓：`parseRendererLaunchFailure()` ✓ + `parseDeadRendererConnection()` ✓ + `hasSandboxedProcess()` ✓（**只作辅助** ✓ —— 没有健康基线时\"`ps` 里没有 sandboxed 进程\"不足以判定 ✓，故它**永远不是结论** ✓）；渲染分支放在决策表**最前** ✓（焦点读数再健康也说明不了它 ✓）；两种读数并存时并列显示 ✓、结论行只出现一次 ✓ | 后果是**审计方向**被引偏一整层 ✓：上一轮据此建议\"收起通知栏 / 重启手机\" ✓（对当时那层形态有效 ✓），而**真正的缺陷面**（渲染进程起不来 ⇒ 这个 APK 在这台设备上一行 JS 都跑不了 ⇒ 电话验收 / 闹钟绑定验收全被挡住 ✓）要等**换掉那个变量**之后才浮出来 ✓ ⇒ 教训：**\"看起来像原因\"不等于\"换掉它会变\"** ✓ —— 能换就必须换 ✓ | 2 | 3 | 4 | 24 | ①渲染分支进决策表**最前** ✓ 并给出**设备侧**处置（重启手机 ✓ —— 本轮只验证到\"重启确实执行了\" ✓，**是否清掉该状态未验到** ✗，如实记录 ✓）；②决定性读数是**当前状态**（`dumpsys activity processes` ✓）而**不是 logcat 时间窗** ✓；③`--selftest` 钉住三种读数（启动失败日志 ✓ / `CR DEAD` 连接 ✓ / `ps` 有与没有 ✓）与**组合语义** ✓（两种都在 ⇒ 两条证据 + 一条结论 ✓） | `node scripts/device-ui-eval.mjs --selftest` ✓：**41 例** ✓（渲染读数 10 例 ✓、`CR DEAD` 单独成判 ✓、结论不重复 ✓，含**\"别的应用的 bad 进程不算我们的\"** ✓ 与 **\"活的连接不算失败\"** ✓ 两条负控 ✓）；**真机实测** ✓（S5 / Android 14 ✓）：修复前（logcat 窗口版）⇒ 只报\"设备状态不是原因\" ✗；修复后 ⇒ 报出 `AMS reports its renderer connections DEAD: - ConnectionRecord{8a3483e u0 CR DEAD com.amos.ai/org.chromium.content.app.SandboxedProcessService1:1:…}` ✓ + `the WebView **renderer** is not running` ✓ + 重启建议 ✓（EXIT=1 ✓） |
| F-DEV-007 | **需求登记册（`docs/TRACEABILITY_MATRIX.md`）**一道检查都没有** —— 而它是读者被指去问"做了没 / 验过没"的那本索引**（REQ-A371 成门 ✓，**F-DEV-002 同族，换一本册子** ✓）：①**测量缺口** ✓：`grep -rl TRACEABILITY_MATRIX scripts/ Makefile .github/` ⇒ **空** ✗ ⇒ 296 行需求、**零**检查 ✓（FMEA 有 `--check` ✓、追溯矩阵**什么都没有** ✗）。②**活下来的缺陷** ✓：**(a)** `REQ-A250` **两行** ✗（macOS 窗口标题轮 ✓ + DesktopShell 接管轮 ✓）⇒ 外部文档（`docs/multi-window.md` ✓、`docs/UI_APPLE_HIG_AUDIT.md §9.1` ✓）引用该号时**分不清指哪一条** ✓；**(b)** 332 行状态列被敲成 `###（诚实边界：…` ✗，而同族 **224 行**都是 `✅（诚实边界：…` ✓ ⇒ 那一列**没有状态** ✓；**(c)** 433/455 行（`REQ-A362` ✓ / `REQ-A351` ✓）**只有 4 格** ✗ —— 分隔符写成 `）\| ✅（`（**前后少一个空格** ✓）⇒ **验证列与状态列并成一格** ✓、整行在渲染里错位 ✓ | 后果是**索引不可用** ✓：一个号指两条需求 ⇒ 追责/复用/复核都失去唯一入口 ✓；状态列不是图例符号 ⇒ "这条到底做完没有"**读不出来** ✗；而这三处**都是"人眼才会发现"的** ✓（无门 ⇒ 无信号 ✓）。同一族更普遍的形式：**任何"结构化文档"都需要一台机械检查器** ✓（FMEA 有了 ✓、追溯矩阵此前没有 ✗） | 2 | 3 | 4 | 24 | ①**新门** `scripts/trace-scan.mjs` ✓（纯函数 + `--selftest` ✓ + `TRACE_DOC` 历史钩子 ✓）：形状（每行列数 = 所在表表头 ✓ ⇒ 2/5 列表格天然通过 ✓ **不需要豁免** ✓）、**ID 唯一** ✓、**状态必须以本文图例三符号（`✅`/`🟡`/`⬜`）之一开头** ✓、**验证列不得为空** ✓（实测 0 行空 ⇒ 断言安全 ✓）；②接进 `make lint` ✓（`--selftest` + 门 ✓）；③**三处缺陷同轮修掉** ✓：较晚那条改号为 `REQ-A370` ✓（行内注明原因 ✓）、`###` 按图例改回 `✅` ✓、两处 `）\| ✅（` 补空格 ✓ | `node scripts/trace-scan.mjs --selftest` ⇒ **12/12** ✓（含 must-fail 形状：重复 ID ✓、短行 ✓、空验证列 ✓、图例外的状态 ✓、窄表**不**误报 ✓）；门 ⇒ **EXIT=0** ✓；**历史负控** ✓：`TRACE_DOC=<HEAD 的文档>` ⇒ **EXIT=1** ✓ 且**精确报出 4 项** ✓（`REQ-A250 on line 332, already registered on line 322` ✓、`line 332 [missing-status]` ✓、`line 433 REQ-A362 [short-row] 4 cells` ✓、`line 455 REQ-A351 [short-row] 4 cells` ✓）；`fmea-gen --check` ⇒ **102 条** ✓ |

| F-TAU-011 | **两个闹钟"只是 hash 相撞"就塌成同一个 `PendingIntent` ⇒ 错的那个响 / 取消打到别人身上**（REQ-A373 审计中实测 ✓，**在我上一轮刚交付的代码里** ✓）：`AlarmGlue.pending` 用 `REQUEST_BASE + id.hashCode()` 作**唯一**区分 ✗，而 `PendingIntent` 的相等性**忽略 extras** ✓ ⇒ 两个 id 只要 hash 相同，`FLAG_UPDATE_CURRENT` 就会把**同一个** PendingIntent 的 id extra 覆盖成后来者 ✗ ⇒ 前者到点会以**后者的身份**触发 ✓、取消任一个等于**同时取消两个** ✗。Java `String.hashCode` 让这种碰撞**可构造** ✓（`"Aa"`/`"BB"` 同为 2112 ✓） | 后果是**闹钟与身份脱钩** ✓：用户看到"两个闹钟都设好了" ✓，实际只有一个能被唤醒 ✓；"取消了我不要的那个"可能把**要的那个**也一起取消 ✗ —— 与 F-TAU-007 同族的"看起来能用" ✓，且发生在**入睡后** ✓ | 3 | 2 | 4 | 24 | ①身份 = `(action, data, class, requestCode)` ✓：`pending()` 增加 `setData(Uri.parse(alarmIdentity(id)))` ✓ ⇒ **每个 id 一个身份** ✓，与 hash 无关 ✓；②规则抽成**纯函数** `alarmIdentity(id)` ✓（无 Android 类型 ✓）；③**主机 JVM 回归测试** `AlarmIdentityTest` ✓（3 例：同 hash 对不同身份 ✓、稳定且按 id ✓、URI 形状 + 带 id ✓）；④**负控** ✓：把 `alarmIdentity` 改成常量 ⇒ 该测试 **3/3 失败、BUILD FAILED、EXIT=1** ✓（证明它真能红 ✓） | `bash scripts/android-glue-nv21-check.sh` ⇒ **BUILD SUCCESSFUL** ✓；Gradle 报告实测 ✓：`TEST-com.amos.ai.glue.AlarmIdentityTest.xml` **tests=3 failures=0** ✓（含 `collidingHashCodesStillGetDistinctIdentities` 在断言**前先测量** `"Aa"`/`"BB"` 同 hash ✓ —— 若前提不成立，测试会以"错的原因"通过 ⇒ 故先钉前提 ✓）；`Nv21PackerTest` 5/5 不受影响 ✓ |
| F-TAU-012 | **闹钟会响、但"把响铃界面顶到屏幕最前"在 Android 10+ 上被平台挡住 —— 且代码里曾把这句写成了已成立的事实**（REQ-A373 审计中实测 ✓ ⇒ REQ-A375 **加法式生产路径已实施** ✓、**真机未验** ✗）：`AlarmReceiver`（广播接收器 = 后台）调 `ctx.startActivity(...)` ✓，而 Android 10+ **禁止后台应用启动 Activity** ✓ ⇒ 平台**静默丢弃**并只留日志 ✗。本仓**已在 API 34 上为同一规则付过学费** ✓（「来电筛选角色对话框」报成功而对话框从未出现 ✓）；我的上一版注释却写着"brings the System UI to the foreground" ✗ ⇒ **UI 声称的能力比平台允许的多** ✓（本仓最在意的一类） | 后果是**"闹钟把我叫醒"这个核心承诺在最需要它的场景下不成立** ✓（应用被系统杀掉 / 长时间后台 ⇒ 灭屏到点：闹钟确实触发 ✓、进程被拉起 ✓，但**屏幕不会被点亮到响铃界面** ✗ ⇒ 用户"没听见" ✓）；而"进程活着时能响" ✓ 让它在日常试用里**看起来没问题** ✓ ⇒ 与 F-TAU-007/011 同族 ✓ | 4 | 4 | 3 | 48 | ①**先停止说谎** ✓：注释改为"**请** System UI 到前台，且受后台启动规则约束" ✓ + 触发时写日志 ✓ + 文档写明边界 ✓；②**生产路径已实施（加法式，REQ-A375）** ✓：`AlarmGlue.notifyAlarm` 用**全屏 Intent 通知**（`NotificationCompat.Builder.setFullScreenIntent` + `IMPORTANCE_HIGH` 通道 ✓）把响铃带到用户眼前 ✓ —— **它不替换**原有的 `startActivity` 尝试 ✓、也不替换 WebView 的响铃 ✓ ⇒ 失败模式只会是"**没有可见的响铃**"，**绝不会是"没有闹钟"** ✓（这正是它可以在真机验证之前先落地的原因 ✓）；`POST_NOTIFICATIONS` 按上一轮的判断**一并改为声明** ✓（有调用点了 ✓）并由 `PermissionWire.notificationsIfNeeded` 在 API 33+ **运行时申请** ✓（新装默认没有 ✓ ⇒ 不申请就永远静默不发 ✓）；③**验收判据可读** ✓：`logcat` 里是否有 `Background activity start ... blocked` ✓（把"平台挡了"与"我们没做"分开 ✓）；④**实测真机前先给判据与测试** ✓：`AlarmNotifyStatusTest`（主机 JVM **5 例**：无服务 ⇒ `notify-unavailable` ✓、API 33+ 未授权 ⇒ `notify-denied` ✓、用户关掉开关 ⇒ `notify-denied` ✓、**<33 无该权限 ⇒ 只看开关** ✓、齐备 ⇒ `posted` ✓）；门禁把"声明 ↔ 调用点"钉住 ✓（`android-permission-scan` 新增 `alarm-notification` 家族 ✓，含"声明了但没人申请 ⇒ not-requested"与"用了但没声明 ⇒ undeclared"两条自测 ✓）；**真机仍待** ✓：灭屏 + `force-stop` ⇒ 到点应出现**全屏响铃通知** ✓（且若 `POST_NOTIFICATIONS` 被拒 ⇒ `logcat` 有 `notify-denied` ✓、闹钟本身仍会响 ✓ —— 两条要分开记 ✓） | 代码静态 ✓ + 真机判据已写进 `docs/native-alarm-bridge.md` **验收清单第 6 条** ✓（`force-stop` + 灭屏 ⇒ 到点看 `logcat` ✓，并把"是否被平台挡住"与"进程是否被拉起"**分成两条记** ✓）；**本轮无真机读数** ✗（设备未暴露 adb 接口 ✓，USB 层可见 ✓）；`grep -c 'Background activity start'` 这条判据待设备 ✓ |
| F-DEV-008 | **索引的\*\*完备性声称\*\*没人验过：`docs/TRACEABILITY_MATRIX.md` 的头只写"约定：`REQ-<编号>`"，于是 54 个**被代码引用**的编号**查不到行** —— 而"查不到"被读者读成"没做过"**（REQ-A372 实测 ✓，**F-DEV-007 的下一层** ✓）：REQ-A371 给这本册子加了门 ✓，但门只回答"**能不能当索引读**" ✓（形状/唯一/图例/有证据 ✓），**不回答"它完备吗"** ✗ ⇒ 表头那句"约定"是**唯一一句关于完备性的话** ✓，而它是含糊的 ✗。**实测** ✓（用门自己的读数 ✓）：`danglingCitations()` 扫 `crates/**`、`scripts/**`、前端、`Makefile`、`.github` ✓ ⇒ **53** 个编号被代码引用而无行 ✓（同一轮内先读到过 **54** ✓ —— 我自己的脚本注释先引入 `REQ-A372` ✓、随后把它登记掉 ⇒ 少一个 ✓ ⇒ 这也是为什么它必须是**报告**、不能写死 ✓），且分布说明不是个别遗漏 ✓：`REQ-A297`（20+ 文件 ✓）、`REQ-A202`（14 ✓）、`REQ-A203`（12 ✓）、`REQ-A199`/`REQ-A200`（各 11 ✓）、`REQ-A256`（`wm.rs` ✓ —— 桌面壳的接管轮 ✓）、`REQ-A302/303/304/307`（`amos-media/src/protocol.rs` ✓） | 后果是**追溯链在读者那一端断裂** ✓：代码注释指向一个编号 ✓、编号在"索引"里查不到 ✓ ⇒ 只能得出"**这条需求不存在**" ✗，而它对应的工作**真实发生过** ✓（只留在 CHANGELOG/注释里 ✓）。同一族更普遍的形式：**文档自称是索引，但"索引完备性"本身没有判据** ✓ | 2 | 3 | 4 | 24 | ①**把声称改真** ✓（表头明写"**部分索引**，不是花名册" ✓ + 实测值 + 怎么看当前值 ✓）；②**让缺口持续可见** ✓：新判据 `danglingCitations()` ✓（**整词**匹配编号 ✓，排除 `docs/`/target/node_modules/gen/archive ✓）**作为报告**（`report` ✓）而**不是失败** ✗ —— 与 `android-permission-scan` 的 `stale` 同类 ✓；**故意不入 baseline** ✓（把 54 条一次性变红只会逼人把已知问题藏起来 ✗）；③**报告本身有硬断言** ✓（自测钉住 ✓）⇒ 报告坏掉会红 ✓；④**不补写那 54 行** ✓：凭编号回填一条没发生过的记录**比缺一行更坏** ✓（本仓"不编造"的纪律 ✓） | `node scripts/trace-scan.mjs --selftest` ⇒ **17/17** ✓（+5：`registeredIds` 收集 ✓、已登记不报 ✓、未登记只报一次且带文件 ✓、最热门排最前 ✓、**整词匹配 `REQ-A9` ≠ `REQ-A99`** ✓）；门 ⇒ **EXIT=0** ✓ 且打印 `report — 53 REQ id(s) are cited in code with no row …` ✓；`--json` ⇒ 53 条带文件清单 ✓（与一份**独立**的一次性 python 探针**同数** ✓）；`fmea-gen --check` ⇒ **103 条** ✓ |
| F-DEV-009 | **`docs-link-scan` 自称 hard gate（头部写着 repo is currently clean），而树**是红的**：一条被搬进来的归档快照贡献了 **119 条**断链，而「快照算不算 live 索引」**从来没人裁定过**（REQ-A381 收口 ✓）：`docs/archive/2026-09/zh/` 是**已提交**的归档副本（40/53 个文件在仓里**没有**其它相同副本 ⇒ 它们是那份内容的**唯一幸存** ✓），它的相对链接描述的是**写它的那棵树** ✓（实测：119 条里 111 条指向今天在归档**之外**存在的同名文件（即文件后来搬过 ✓）、3 条指向归档内的兄弟 ✓、5 条目标**彻底不存在** ✓）。门只把这些当成断链打印出来 ✓ —— 而**脚本头部那句 repo is currently clean 是假的** ✗（同族的上一例是 `F-DEV-008` 的「完备性声称」✓）。另有 **6 条**断链落在**三个未跟踪的根级文档**里（企业功能完成报告 / P3 桌面索引 / 快捷指令完成确认 ✓）：它们是从 `docs/` 或 `frontend-ts/` 里**拷出来**写的，却用着**原目录**的相对路径 ✗（`./docs/ENTERPRISE_GUIDE.md`、`./src/lib/shortcuts.ts`、一个把占位词当链接的 `[用户指南](计划中)` ✓） | 后果是**门自己失去了可信度** ✓：一个红着的 hard gate 让之后每一轮「`make lint` 全绿」的声称都无法核验 ✗；而归档里的 119 条会被误读成「文档坏了」✓（其实那是快照的属性 ✓） | 2 | 3 | 3 | 18 | ①**把「不做判定」变成一个**有理由、可见的决定** ✓：`docs-link-scan.mjs` 新增 `SKIP_PATHS = ['docs/archive/']` ✓ —— 注释里写下**实测依据**（40/53 无副本 ⇒ 是唯一幸存 ✓；111/3/5 的分布 ✓）与**为什么不改写**（把归档链接改到今天，会把「它当时引用的文档」变成**今天的**文档 ⇒ 对它所记录的事实说谎 ✓）；②**排除必须可见** ✓：每次运行都打印「有 **64** 个归档 markdown **未判定**」✓，`--json` 里带 `skipped` ✓（**静默跳过正是这道门要防的缺陷** ✓）；③**剩下的 6 条按链接卫生修掉** ✓：`P3_DESKTOP_PHASE1_COMPLETE.md` → 真名 `P3_DESKTOP_ENHANCEMENTS_PHASE1_COMPLETE.md` ✓；`./src/lib/…`（两条）→ `crates/amos-tauri/frontend-ts/src/lib/…` ✓；`./docs/{ENTERPRISE_GUIDE,SECURITY}.md` 与那个占位词链接**改成纯文本** ✓（原文本来就写着「待创建」／是占位词 ⇒ 渲染成链接就是**一个不成立的承诺** ✓）；④脚本头部替换成**实测的当前状态** ✓（不再自称绿 ✓） | `node scripts/docs-link-scan.mjs` ⇒ **EXIT=0** ✓（438 个文件判定 ✓、656 条相对链接全部解析 ✓、0 断链 ✓、64 个归档文件**未判定且被打印** ✓）；`--json` ⇒ `{files:438, skipped:64, checked:656, broken:0}` ✓；`--selftest` ⇒ **9/9** ✓；修掉的 6 条见左栏（三个文档**只改路径/链接形式，不动任何陈述** ✓）；`fmea-gen --check` ⇒ **111 → 112 条** ✓；诚实边界：①**归档不再被判定** ⇒ 其中若出现真正的坏链接也**不会**报 ✓（这是选择「快照不扫描」的代价，已写进脚本注释 ✓）；②那三个根级文档是**别的会话的在制品** ✓ —— 我只改链接形式，内容与结论**一字未动** ✓；③**这不是**修好了 119 条断链 ✓，而是**把它们的性质裁定清楚并让排除可见** ✓ |
| F-TAU-014 | **Rust 调 Kotlin 的静态成员，而 Kotlin 侧根本没生成静态方法 ⇒ 每次登记都在真机上抛 `NoSuchMethodError`（而我上一轮已经宣称"绑定接通" ✗）**（REQ-A376 真机实测 ✓，**只有真机能发现** ✓）：本仓惯例是给 Rust 会按名字调用的 Kotlin 成员加 `@JvmStatic`（其他 glue 共 **16 处** ✓），我的 `AlarmGlue.schedule/cancel/openExactAlarmSettings` **一处都没有** ✗ ⇒ 真机第一次调用就：`java.lang.NoSuchMethodError: no static method "Lcom/amos/ai/glue/AlarmGlue;.schedule(Landroid/content/Context;Ljava/lang/String;J)Ljava/lang/String;"` ✗。**三层各自都编译通过** ✓（Kotlin 有该方法 ✓、Rust 的签名串也对 ✓、APK 里也确实有那个类 ✓）⇒ **没有任何门看得见** ✗（`jni-*-scan` 只管 attach/find_class 两条规则 ✓；`android-glue-*` 只编译 ✓；APK 产物检查只查字符串 ✓ —— 字符串在、方法签名不在 ✓） | 后果是**功能等于零** ✓：`dumpsys alarm` 恒为 **0** ✓、`register` 每次报"Java exception was raised during method invocation" ✗ ⇒ 用户设的闹钟**永远不会响** ✓（而一上轮我据此写了"设备绑定已接通"= **不成立的声称** ✗ —— 与 F-TAU-007 同族：**"能编译"被当成了"能工作"** ✗）。附带第二处 ✗：JNI 失败后**pending 异常没清** ✗ ⇒ 它逃进 IPC 返回路径 ✓，把"本可以如实报告的状态"变成整次调用异常 ✗ | 3 | 5 | 4 | 60 | ①三个方法加 `@JvmStatic` ✓ 并在类里写下 **KOTLIN → RUST CONTRACT** 注释 ✓（说明"Rust 按名字调的成员必须注解" ✓ —— 让下一个人不必再踩 ✓）；②Rust 侧**每个** JNI 失败路径调用 `amos_jni::clear_pending()` ✓（`open_settings`/`schedule`/`cancel` 三处 ✓）⇒ 失败变成**如实的 `DeviceOutcome::Unknown(…)`** ✓ 而不是逃逸异常 ✓；③**真机复验** ✓：`register` ⇒ `{"state":"scheduled"}` ✓、`dumpsys alarm` **0 → 3** ✓、到点 `AlarmReceiver: exact alarm fired` ✓（F-TAU-007 的判据达成 ✓）；④**门已补上（REQ-A377）** ✓：`scripts/jni-contract-scan.mjs` 的 R1 把"Rust 的 `call_static_method(<class>,"m")` ⇒ 被调的 Kotlin 成员必须有 `@JvmStatic`"做成**判据** ✓（同类检查此前全仓无 ✓），**并有负控** ✓：拿掉 `AlarmGlue.schedule` 的注解 ⇒ 门点名 `crates/amos-tauri/src/alarm_sched.rs:448 missing-jvmstatic: AlarmGlue.schedule` + 指出 `AlarmGlue.kt:295` ✓，还原后 `cmp` 字节一致 ✓；同一道门的方向二把 `external fun` ↔ `Java_…` 导出成对（改名即 `missing-export` ✓，同样有负控 ✓） | `android-glue-nv21-check.sh` ⇒ **BUILD SUCCESSFUL** ✓（编译**看不到**这个缺陷 ✓ —— 这正是本行的教训 ✓）；真机（S5 / Android 14 / API 34）✓：修复前 `register threw: Java exception was raised during method invocation` ✗ + `dumpsys alarm \| grep -c amos` = **0** ✗；修复后 ⇒ `device:{state:"scheduled"}` ✓、`dumpsys alarm` = **3** ✓、`AlarmGlue: scheduled exact alarm alarm:acc-a1 at …` ✓、到点 `AlarmReceiver: exact alarm fired: alarm:acc-a1` ✓ |
| F-TAU-015 | **全屏 Intent 通知在这台 ROM 上还需要 `WAKE_LOCK`，否则抛 `SecurityException` ⇒ 响铃静默消失（而闹钟本身照常响）**（REQ-A376 真机实测 ✓，**只有真机能发现** ✓）：`AlarmGlue.notifyAlarm` 在 `POST_NOTIFICATIONS` 已授予 ✓ 的前提下抛：`notify denied for alarm:acc-a1: Neither user 10172 nor current process has android.permission.WAKE_LOCK` ✗ ⇒ 通知**没发出去** ✓（`dumpsys notification` 里当时也没有我们的记录 ✓）。设计上的**加法**让我同时拿到另一条读数 ✓：`AlarmReceiver: exact alarm fired` ✓ + `asked the System UI to foreground …` ✓ + `mCurrentFocus=com.amos.ai/MainActivity` ✓ ⇒ **闹钟响了、应用也被拉到前台** ✓，**只少了"全屏响铃"这一条** ✓ —— 这正是"失败模式被限制成'没有可见的响铃'" ✓ 的实测形态 ✓ | 后果是**最需要它的那一刻没有可见提醒** ✓（灭屏/后台时用户可能"没听见"）✓；而它**不静默** ✓：`AlarmReceiver` 打出 `no full-screen ring for …: notify-denied` ✓（我上一轮刻意留的诚实状态词 ✓ 在真机上一次就用上了 ✓）。同一族更普遍的形式：**平台对一条 API 的隐藏前置条件**（这里是一枚普通权限 ✓）不会出现在文档里，只会出现在运行期的异常里 ✓ | 3 | 4 | 2 | 24 | ①声明 `android.permission.WAKE_LOCK` ✓（**normal** 权限 ⇒ 安装即得 ✓，唤醒屏幕本就是它的用途 ✓）并在片段里写明"这是真机量到的" ✓；②把 `WAKE_LOCK` 加入 `android-permission-scan` 的 `alarm-notification` 家族 ✓ ⇒ 声明与调用点继续互相看守 ✓（门：7 家族 / **12** 权限 ✓ 全绿 ✓）；③通知内容本身不变 ✓（`IMPORTANCE_HIGH` ✓ / `CATEGORY_ALARM` ✓ / `VISIBILITY_PUBLIC` ✓ / 通道名取系统应用名 ✓） | 真机（S5 / Android 14）✓：修复前 ⇒ `notify denied … WAKE_LOCK` ✗ + `no full-screen ring …: notify-denied` ✗；修复后 ⇒ `AlarmGlue: posted the firing notification for alarm:ring-test (full-screen intent)` ✓ 且 `dumpsys notification --noredact` 里有 `NotificationRecord(… pkg=com.amos.ai … id=-273726094 tag=amos.alarm importance=4 … category=alarm vis=PUBLIC)` ✓ + 通道 `mId='amos-alarm-firing'`、`mName=Amos` ✓；`dumpsys package com.amos.ai` 含 `WAKE_LOCK` ✓；`android-permission-scan` selftest **16 断言** ✓ + 门 **OK** ✓ |
| F-TAU-016 | **Rust 调 Kotlin 的**实例**方法时，名字或**签名（元数与类型）**漂移同样是 `NoSuchMethodError` —— 而两道既有门都不看它**（REQ-A378 把门扩到边界的第三种走法 ✓，**本条没有真机读数** ✗：它是**预防性**成门，依据是 F-TAU-014 的同类形状 + 实测数据 ✓）：F-TAU-014 修的是**静态**调用（`call_static_method` ↔ `@JvmStatic` ✓），R2 修的是 `external fun` ↔ `Java_…` 导出 ✓；而 Rust 还有第三种走法 —— Kotlin 把一个**实例/桥对象**交上来（`MicPermissionGlue.bind()` 交 `this` ✓、`SmsGlue.attach(glue)` ✓、`DevCareGlue` ✓、`ClipboardGlue.attach(bridge)` 交桥对象 ✓、`AndroidClipboardGlue` ✓、`MediaStoreGlue` ✓），Rust 用 `call_method(<句柄>, <成员>, <签名>)` 调它 ✓ —— 而 JNI 对实例方法按**名字 + 签名**解析 ✓ ⇒ Kotlin 改个名（`isGranted` → `isGrantedNow`）或加个参数 ⇒ 真机第一次调用即 `NoSuchMethodError` ✗，两侧各自**照旧编译通过** ✗。**实测** ✓：全仓 `call_method` **50 处** ✓，其中 **13 处**指向本仓 glue（展开为 **18 个「名字, 签名」对**：`SmsGlue` 的 `counts/snapshot/messages/send` ✓、`DevCareGlue` 的 `scanJunk/storage/battery/installedApps/removeJunk/uninstallApp` ✓、`MicPermissionGlue` 的 `isGranted/request` ✓、`ClipboardGlue` 的 `pushTextClipboard` ✓、`AndroidClipboardGlue` 的 `primaryText/pushText` ✓、`MediaStoreGlue` 的 `listCollection/saveCollection/loadContent` ✓），其余 **37 处**是平台对象（`Intent`/`Location`/`Iterator`）⇒ 不在本仓责任内 ✓ | 后果与 F-TAU-014 同族 ✓：**第一次调用即异常、在此之前功能静默失效** ✓（闹钟那次是「注册永远失败、`dumpsys alarm` 恒 0」✓）；差别只在 JNI 的解析方式（静态 vs 实例）✓，**而这两种走法此前都不可见** ✗ | 3 | 3 | 4 | 36 | ①**加第三条判据 R3** ✓（`scripts/jni-contract-scan.mjs`）：每一处 `call_method(<glue/bridge 句柄>, <成员>, <签名>)` 对应的 Kotlin `fun` **必须存在且签名一致** ✓ —— **元数与类型**（每个参数 + 返回值 ✓，R1 也一并查 ✓）都从 JNI 描述符解析并与 Kotlin 声明的类型比对 ✓（`(Ljava/lang/String;Z)V` → 2 个参数、无返回 ✓；可空基础类型按**装箱**描述符（`Long?` → `Ljava/lang/Long;` ✓）、泛型取**擦除后的原始类** ✓、简单类名按**该文件自己的 `import`** 解析 ✓；读不出即**失败**，**映射不出来的类型则报告并计数、绝不当成一致** ✓）；②**句柄识别写死成窄规则** ✓：接收者自身含 `…glue…`/`…bridge…`，或接收者是本函数内由这类表达式绑定的标识符（`let obj = self.bridge.as_obj();` ✓）；**字符串与注释里的提及不算证据** ✓ —— 加法过程中它当场纠正过一次误报 ✗：`real_dial.rs` 的 `Intent#setData/addFlags/startActivity` 因为一句文档字符串 `call TelephonyGlue.nativeAttach at boot` 被误判为 glue 调用 ✓；③**成员名可以是包装器的参数** ✓（`call0/call1/call2` 把方法名当参数传 ⇒ 取**调用方**的字面量 ✓）；④**不是类身份检查**（在整棵 Kotlin glue 树里按「名字 + 元数」找 ✓）⇒ 边界见右栏 ✓ | `node scripts/jni-contract-scan.mjs --selftest` ⇒ **75 断言** ✓（含句柄规则 ✓、字符串不算证据 ✓、元数解析 ✓、包装器取名 ✓、平台接收者被跳过 ✓）；门 ⇒ **EXIT=0**（50 处实例调用 / 37 处平台跳过 / 18 个成员按名字 + 元数核对 ✓）；**负控 C** ✓：`MicPermissionGlue.isGranted` → `isGrantedNow` ⇒ 门点名 `crates/amos-tauri/src/mic_permission.rs:120 missing-kotlin-member` ✓；**负控 D** ✓：给 `SmsGlue.snapshot` 加一个参数 ⇒ 门点名 `crates/amos-sms/src/android.rs:79 arity-mismatch`（并指出 `SmsGlue.kt:193` 是 2 个参数 ✓）；**类型负控 E/F/G** ✓：把 `SmsGlue.snapshot(folder: String)` 的参数类型改成 `Int` ⇒ `signature-mismatch`（R3 路径 ✓）；把 `alarm_sched.rs` 里 `let sig = "(…J)…"` 的 `J` 改成 `I` ⇒ 门点名 `#3: Kotlin Long (J) vs JNI I`（R1 路径，且读得到 `let` 里拼出来的描述符 ✓）；把 `BluetoothGlue.scanState` 的返回类型改成 `Int` ⇒ `return: Kotlin Int (I) vs JNI Ljava/lang/String;` ✓；A/B/C/D 四次注入都**逐字节还原**、`cmp` 一致 ✓；诚实边界：①**没有真机读数** ✗ —— 本条是预防性成门 ✓，真机上「改名 ⇒ NoSuchMethodError」这条因果由 F-TAU-014 那一轮的同族读数支撑 ✓；②**R3 查名字 + 签名（元数 + 类型），不查类身份** ✓（树级查找 ✓）⇒ 名字恰好撞上另一个 glue 类的成员时会漏 ✓；**类型映射是一张表** ✓ ⇒ lambda / 无 `import` 的裸名**报告但不判定** ✗（那类参数的**类型漂移**看不见 ✓）；③**句柄名里没有 `glue`/`bridge` 就看不见** ✗（名字规则 ✓，故 R3 与 R1 并存 ✓）；④Kotlin 的**默认参数 / `vararg`** 会让声明元数与 JNI 调用元数不同 ✓（本仓 glue 未使用 ✓；一旦使用，门会失败并要求写进 `scripts/jni-contract-allowlist.json`，而不是放宽判据 ✓）；⑤**真机未复验** ✗） |
| F-TAU-017 | **清单片段里的组件是**平台按名字找类**的字符串 ⇒ 名字与 Kotlin 类漂开时，构建通过、启动无错，组件**永远不运行**（REQ-A379 成门 ✓，**无真机读数** ✗：预防性成门 ✓，依据是同一 seam 的真机教训 F-TAU-007 ✓ + 当前树实测 ✓）：`crates/amos-tauri/android-glue/AndroidManifest.components.xml` 的 4 条 `<service>`/`<receiver>` 决定了设备上在通话界面 / 来电筛查 / 短信常驻接收 / 精确闹钟**是否存在** ✓（该片段之所以被跟踪，正是 F-TAU-007 那一轮发现「Kotlin 编进了 APK 却没人绑定它」✓）。而每条 `android:name` 都是**平台在实例化时按名字找类**的字符串 ✓ ⇒ 打错一个字母、或 Kotlin 侧改名 ⇒ **构建通过、启动无错、logcat 无痕** ✗：`AlarmReceiver` 错 ⇒ 闹钟响了**什么都不发生** ✓（F-TAU-007 的症状出现在 seam 的另一侧 ✓）；`SmsReceiver` 错 ⇒ 短信只在应用运行时才到达 ✓；`AmosInCallService` 错 ⇒ 没有在通话界面且**没有任何错误** ✓。**既有门为何看不见** ✗：`scripts/android-glue-mirror.sh` 比的是**两个文件**里的名字是否一致 ✓（片段 ↔ 生成的 manifest ✓）—— 两边写着**同一个错名字**时它照样通过 ✓ | 与 F-TAU-007 同族 ✓：**功能在设备上静默不存在** ✓（用户读成「这个功能就是坏的 / 没有」✓），而每一道编译门都是绿的 ✓ | 3 | 3 | 4 | 36 | ①**新门** ✓ `scripts/android-component-scan.mjs`：**K1** 每条 `<service>`/`<receiver>` 必须解析到本仓 glue 里存在的类且**种类对** ✓（`<receiver>` ⇒ `BroadcastReceiver` ✓；`<service>` ⇒ `Service` 或平台已知子类 ✓）；**K2**（反向）glue 里继承组件种类的**顶层**类必须被登记 ✓；**K3** 每个 `System.loadLibrary` 的名字必须是某个 workspace crate 的 `[lib] name`（或包名 `-`→`_`）✓；②**相对名按 `tauri.conf.json` 的 `identifier` 解析** ✓（`.glue.X` → `com.amos.ai.glue.X` ✓）⇒ 命名空间不能悄悄漂 ✓；③**读不出父类 ⇒ 失败 + allowlist** ✓（不放过 ✓）；④**K4：声明**内部**的名字也判** ✓ —— `<action>` 必须是**闭合表** `PLATFORM_ACTIONS` 里的平台动作 ✓（未列出即**失败** ⇒ 因为"没见过"与"拼错"从这里长得一样 ✓，加新动作要**有意登记**并把平台要求的发送方权限写进去 ✓）、动作必须投递给**这种**组件 ✓（`<receiver>` 的动作不能挂在 `<service>` 上 ✓），且受保护广播必须声明**发送方权限** ✓（`SMS_RECEIVED` 的导出接收器缺 `BROADCAST_SMS` ⇒ **任何应用都能注入假短信** ✓）；⑤`<activity>`/`<provider>` 与 app 命名空间之外的组件、`<meta-data>` 名字**只报告不判定** ✓ | `node scripts/android-component-scan.mjs --selftest` ⇒ **40 断言** ✓（含"注释里的组件不算声明" ✓、"相对名解析" ✓、"无名组件" ✓、"读不出父类 ⇒ 失败" ✓、`[lib] name` 优先于包名 ✓、"拼错的动作是发现" ✓、"动作投给错种类的组件" ✓、"导出接收器缺发送方权限" ✓、"发送方权限写错" ✓、"`<meta-data>` 只报告" ✓）；门 ⇒ **EXIT=0**（4 个组件全部解析 ✓、3 个动作 + 发送方权限全部核对 ✓、1 条 meta-data 报告 ✓、4 个 `loadLibrary` 名字都有 crate 构建 ✓）；**负控（真树，逐字节还原 + `cmp` 一致）** ✓：**(A)** 片段里把 `AlarmReceiver` 改一个字母 ⇒ `missing-kotlin-class` ✓ **并**报反向的 `unregistered-component` ✓；**(B)** 把该 `<receiver>` 改成 `<service>` ⇒ `wrong-kind`（并指出 `AlarmReceiver.kt:28` 是 `BroadcastReceiver` ✓）；**(C)** `SensorGlue` 的 `loadLibrary` 改名 ⇒ `unknown-library` ✓；**(D)** 去掉 `SmsReceiver` 的 `BROADCAST_SMS` ⇒ `missing-sender-permission` ✓；**(E)** 把 SMS 动作拼错 ⇒ `unknown-action` ✓；**(F)** 把 `InCallService` 动作拼错 ⇒ `unknown-action` ✓；诚实边界：①**无真机读数** ✗（预防性成门 ✓）；②种类检查读父类**文本** ✓ ⇒ 自有基类要写 allowlist ✓；③K2 只认识它被指向的那一个片段 ✓；④K4 的表**闭合** ✓（未列出的动作失败 ⇒ 必须有意登记 ✓）；⑤`<activity>`/`<provider>` / 外部命名空间 / `<meta-data>` 只报告 ✓；⑥**真机未复验** ✗ |
| F-TAU-018 | **平台自己的 linter 在本仓从未运行过 ⇒ 两类只有它看得见的东西对每一道门都不可见：API 在 `minSdk` 上是否存在、以及某个调用是否需要一枚没人检查的权限**（REQ-A380 成门 ✓，依据是首跑实测 ✓）：`make android-glue-check` 只证明**编得过** ✓（compileSdk 36），而 APK 声明 `minSdk 26` ✓ ⇒ 一个 API 36 才存在的类/方法只要运行时走到就 `NoSuchMethodError` / `NoClassDefFoundError` ✓，而编译器**看不到**（符号就在 36 的 `android.jar` 里 ✓）；同理「这个调用需要 `BLUETOOTH_SCAN`」是**平台事实** ✓，编译器不知道 ✓。**实测** ✓：`:app:lintArmDebug` 首跑 **20 个 error** ✓，其中 **13 个在我们的 glue** ✓：`TetheringGlue` **5 × `NewApi`** ✓（API 36 的 `TetheringManager` / `TetheringEventCallback` / `TetheringInterface#getType` ✓、API 28 的 `Context#mainExecutor` ✓ —— 两个调用方都先判了 `SDK_INT` ✓，但 **lint 不跟随调用方里的守卫** ✗）、`BluetoothGlue` **8 × `MissingPermission`** ✓（每个调用都在 `try/catch (Throwable)` 里 ✓，lint 也看不见 ✓）；另有 **4 条 `InlinedApi` 警告指向一个真缺陷** ✓：`MediaStore.*.getContentUri(String volume)` **本身就是 API 29 的 API** ✓ ⇒ 在 API 26..28 上抛 `NoSuchMethodError`（被本类错误路径接住）⇒ **那些路径在 26..28 上等于静默死亡** ✗（而 `minSdk` 就是 26 ✓） | 后果分两半 ✓：**API 级别**错 ⇒ 26..35 的设备上运行时崩或静默死 ✓（而 34 的真机上永远看不到 ✓）；**权限**判据 ⇒ 拒绝本该被如实报告而不是崩溃 ✓（本仓的正确形态），但「没人检查」与「检查了」在代码里长得一样 ✓；**而这道门此前根本不存在** ✗ | 3 | 3 | 3 | 27 | ①**把 Android Lint 接进 `android-glue-check` 的第三阶段** ✓（`:app:lintArmDebug`，默认 `--offline`）：**我们 glue 的 error 必须为 0** ✓（有则列出 文件:行 并失败 ✗）、**warning 同样必须为 0** ✓（接受的只能**写在源码里** ✓ —— `@SuppressLint("…") // 理由` ⇒ **新 warning 就是没人做过的决定** ✓；16 条已逐条裁定 ✓：`ObsoleteSdkInt` 3 条**删掉死分支** ✓（`minSdk 26` 是权威 ✓）、`StaticFieldLeak` 6 条 / `UseKtx` 5 条 / `InlinedApi` 2 条**抑制 + 写理由** ✓）、**非 glue 的 findings 只报告** ✓（按 issue id 归类打印 ✓）、**没有报告文件 ⇒ 失败** ✓（读不到 ≠ 没问题 ✓）、分类器带**自检** ✓；②**TetheringGlue**：`@RequiresApi(TETHERING_API)` 把要求**声明在函数上** ✓ ⇒ lint **逐个调用点**核对 ✓（比函数体内再判一次更强 ✓；函数体内那版复查**故意不留** ✓ —— 有注解它就可证明是死代码，`ObsoleteSdkInt` 也这么说 ✓）；③**BluetoothGlue**：逐点 `@SuppressLint("MissingPermission")` **+ 写明理由** ✓（真实守卫是 `canScan()` 与 `try/catch` 的如实拒绝 ✓）；④**MediaStoreGlue 的真缺陷修掉** ✓：`mediaCollection(legacy, volumeBased)` —— API ≥ 29 用卷形式、以下退回 `EXTERNAL_CONTENT_URI` ✓ | 实测（`make android-glue-check`）✓：编译 **BUILD SUCCESSFUL** ✓（6 条 Kotlin 警告全是 deprecation ✓）、`Lint found 7 errors, 49 warnings` ✓ ⇒ **我们 glue 的 findings 0 条** ✓（error 0 + warning 0 ✓ —— 16 条已逐条裁定 ✓）、非 glue 的 findings 按 issue id 打印 ✓（`UnusedResources` 12 ✓、`PermissionImpliesUnsupportedHardware` 8 ✓、`GradleDependency` 8 ✓、`SelectedPhotoAccess` 2 ✓ …）；**负控（逐字节还原 + `cmp`）** ✓：拿掉 `TetheringGlue.bindCallback` 的 `@RequiresApi` ⇒ 门 **FAIL** 并点名 `TetheringGlue.kt:167/168/169/174 … [NewApi]` ✓（总 error **7 → 12** ✓，多出的 5 条全是我们 glue ✓）；诚实边界：①**只挂在 `make verify`** ✓（需 `gen/` + JDK 17 + Gradle 缓存，首次要联网 ✓），**不在 `make lint`** ✓；②**非 glue 的 findings 只报告** ✓ —— 其中 `PermissionImpliesUnsupportedHardware` / `…ChromeOsHardware` / `SelectedPhotoAccess` 其实**源自我们声明的权限** ✓（`CAMERA`/`RECORD_AUDIO`/`BLUETOOTH_*`/`READ_MEDIA_*` ⇒ Android/Play 推断需要这些硬件 ✓）⇒ **本轮的决定是接受** ✓（AmOS 装到已知硬件、走 adb 不走 Play ✓），**上 Play 前需补 `<uses-feature android:required="false">` 片段并接进手工合并流程** ✓（记为下一步 ✓）；③**真机未复验** ✗（`MediaStoreGlue` 的 26..28 回退路径只能靠平台事实 ✓）；④生成工程的 7 个 error / 49 个 warning 只打印 ✓ |







| F-TAU-013 | **闹钟账本只有"键"的上限、没有"条目数"的上限 ⇒ 调用方可无界增长它；同一处还有一次跨线程的可见性漏读**（REQ-A374 生产审计 ✓，**在我自己的代码里** ✓）：①`alarm_sched.rs` 的 `MAX_ALARM_ID_BYTES` 限住了**单个 id** ✓，而 `scheduler_alarm_register` 是 **WebView 可调用** ✓、`ExactAlarmClock` 内部是 `BTreeMap<JobId, u64>` ✓ **没有任何容量门** ✗ ⇒ 一个前端循环（或同源脚本）就能把这张表撑成无界内存 ✓；②`AlarmGlue.activity` 由**主线程**写 ✓（`attachActivity`）而由 **Rust/JNI 线程**读 ✓（`openExactAlarmSettings`）✗ —— 无 `@Volatile` 时读方可能一直看到**陈旧的 null** ✓ ⇒ 明明有前台 Activity 却回 `false` ✗（"没能打开系统页"变成假警报 ✓）；③同处还有一处**死面** ✓：`canOpenSettings()` 没有任何调用点 ✗ | 后果①：**调用方决定内存** ✓（本仓在 `CHAT_MSG_CAP`/`MAX_IME_SESSIONS`/`FrameCap` 上反复守"有界"，这里漏了 ✓）；后果②：**用户在设备上说不出真话** ✓（UI 报"需在前台操作"而其实在前台 ✓）—— 与 F-TAU-010/011/012 同族 ✓（"看到的状态"与"真实状态"分叉 ✓） | 2 | 2 | 3 | 12 | ①新增 `MAX_ALARM_ENTRIES = 256` ✓ + 命令门的 `check_alarm_capacity()` ✓：**只拒"新的 id"** ✓ —— **重登记既有 id 在满员时照常成功** ✓（每日闹钟再武装/编辑这条**绝不能坏**的路径 ✓），错误消息点名上限与出路 ✓（"cancel the ones you no longer need" ✓）；②容量按**distinct id** 计 ✓（不是调用次数 ✓）⇒ 幂等调用方不受影响 ✓；③`activity` 加 **`@Volatile`** ✓ 并把"为什么必须"写进注释 ✓（主线程写 / JNI 线程读 ✓）；④删除死面 `canOpenSettings()` ✓ | `cargo test -p amos-tauri --lib alarm_sched::` ⇒ **9/9** ✓（+1：填满到上限 ⇒ 新 id 被拒且消息含上限与"重登记仍可用" ✓、**满员时重登记既有 id 成功** ✓、取消后腾出位置 ✓、"被拒时账本没长" ✓）；`cargo check --features android` ✓、`cargo fmt --check` 干净 ✓；Kotlin：`android-glue-nv21-check.sh` ⇒ **BUILD SUCCESSFUL** ✓、我这两个文件 **0 告警** ✓、`AlarmIdentityTest` 仍 **3/3** ✓ |


| F-SH-025 | **瓦片 ↔ 屏幕：真机读到「内置应用「monitor」在此构建中没有可显示的界面」——而**当前树里那个表面是有屏幕的**（REQ-A382 更正 ✓；原诊断只对**那台机器上的 APK** 成立 ✗）**：REQ-A364 的真机读数（Ragentek S5 / Android 14 ✓：主屏第 2 页有「系统监控」瓦片 ✓、点开显示 `app.screenMissing` ✓）当时被读成该表面在这个构建里没有接线 ✗ —— 而**仓库里那行 loader 早于该轮** ✓（`d8904906`（23rd built-in app「系统监控」）**先于** `196be0d3`（本行的登记）✓，`git merge-base --is-ancestor` 实测 ✓）⇒ 真机上那份 APK **比 `d8904906` 更旧** ✓ ⇒ 这是**构建新鲜度**问题（本仓另有 `dist-freshness` 门管 dist ✓），**不是**接线缺口 ✗。**当前树实测** ✓：`APP_META` **33** 条 ↔ `ALL_APP_LOADERS` **33** 条、双向差集为空 ✓。**不过它点出的门缺口是真的** ✓（而且比本行原来以为的更隐蔽 ✓ ⇒ 见 **F-SH-026** ✓） | 后果（更正后）✓：真机上看到死表面的风险仍在 ✓（新加瓦片而没人写屏幕 ⇒ 点开只看到「内置应用「X」在此构建中没有可显示的界面」✓ —— 它**明说**了 ✓，但瓦片**暗示有内容** ✓），而**当时那条测试挡不住它** ✗；另一后果是**我自己**把「APK 旧」读成了「代码没接线」✗ ⇒ 记下这次**误诊**（同族：把一次读数当成了结论 ✓） | 3 | 2 | 2 | 12 | ①**更正 + 证据** ✓（见左栏：loader 行的提交早于登记行 ✓、当前树 33↔33 双向零缺口 ✓）；②**门补上（`F-SH-026` / REQ-A382）** ✓：`app-registry.svelte.test.ts` 改成读**真实表**（`APP_META` ↔ `ALL_APP_LOADERS` ✓）并**双向**断言 ✓ + **负控** ✓；③`ALL_APP_LOADERS` 导出以便门读真表 ✓（注释写明原因 ✓） | `svelte-tests/app-registry.svelte.test.ts` ✓（双向 + 负控 ✓，6 例 ✓）；**真树负控（逐字节还原 + `cmp`）** ✓：**(A)** 删掉 `monitor` 的 loader ⇒ **2 条红**（`tile → loader` + 解析循环 ✓）；**(B)** 给 `appMeta` 加一条 `ghostapp` 瓦片 ⇒ 比较失败并**点名 `ghostapp`** ✓ + 解析循环红 ✓；还原后 **6/6** ✓；`APP_META` 33 ↔ `ALL_APP_LOADERS` 33 ✓（独立探针同数 ✓）；诚实边界：①**真机未复验** ✗（那次读数是旧 APK ✓ —— 复验要按当前树重建 APK 再点开瓦片 ✓）；②本行**没有**设备行为结论 ✓ |
| F-SH-026 | **一致性测试验证的是**手抄的副本**（29 条 id 的 `EXPECTED` 数组），而真实瓦片表 `APP_META` 已有 **33** 条 ⇒ 新加的应用/瓦片**永远不会被检查**，而门是绿的**（REQ-A382 审计中发现 ✓，**同一轮修掉** ✓）：`svelte-tests/app-registry.svelte.test.ts` 里写着 `const EXPECTED: string[] = [ …29 个 id… ]` ✓，而 `lib/appMeta.ts` 的 `APP_META` 与 `svelte/appRegistry.ts` 的 `ALL_APP_LOADERS` 都是 **33** 条 ✓ ⇒ 那 4 个后加的应用（`webman`/`compass`/`measure`/`shortcuts` ✓）**从没进过这条断言** ✓，差集就是不被检查的面 ✓；更根本的是：**测试的判据是实测数据的一个副本** ✗ ⇒ 副本不动、门就绿 ✓（与 F-SH-024 的「控件永远无法动作」同族 ✓：**有检查，但检查的不是那件事** ✓）。**为什么它比缺门更隐蔽** ✗：文件名叫 `app-registry…test.ts` ✓、断言写着 every id resolves ✓ ⇒ **读代码会以为这里有覆盖** ✓ | 后果 ✓：**新瓦片（`appMeta` 一行）而没人写屏幕 ⇒ 所有门全绿** ✓，用户点开只看到「内置应用「X」在此构建中没有可显示的界面」✓（即 F-SH-025 ✓）；而这**正是本仓反复出现的形状** ✓：**声称（有测试）> 证据（测试测的是副本）** ✓ | 2 | 3 | 3 | 18 | ①**判据换成真实表** ✓：测试遍历 `APP_META`（瓦片）与 `ALL_APP_LOADERS`（屏幕）✓，并**双向**断言 —— **每个瓦片都有屏幕** ✓（真机那次的方向 ✓）、**每个屏幕都是瓦片** ✓（屏幕不可达的方向 ✓）；②**自证能失败** ✓：同文件先跑「比较函数必须在 fixture 上报出缺口」✓（`missingLoaders` / `unreachableScreens` ✓）；③`ALL_APP_LOADERS` 导出 ✓（注释写明：为了门能读**真表**而不是副本 ✓） | `svelte-tests/app-registry.svelte.test.ts` ✓（**6 例** ✓ 含负控 ✓）、`src/svelte/appRegistry.ts` ✓（导出 + 注释 ✓）；实测 ✓：`TILE_IDS` **33** == `LOADER_IDS` **33** ✓、双向差集均为空 ✓；**负控（逐字节还原 + `cmp`）** ✓：**(A)** 删 `monitor` loader ⇒ **2 红** ✓；**(B)** 加 `ghostapp` 瓦片 ⇒ 比较失败并点名 `ghostapp` ✓；还原后 6/6 ✓；诚实边界：①**只覆盖这两张表** ✓ —— 别的 id 来源（用户固定的 dock、第三方 `store:…` ✓）不在这条判据里 ✓；②`phone` 在桌面形态被**故意**隐藏 ✓ ⇒ 判据比的是**表**而不是形态 ✓（形态另有断言 ✓）；③**真机未复验** ✗ |
| F-SH-027 | **录音备忘录功能缺少三个关键能力**（播放进度、编辑裁剪、转写）——而门全绿（REQ-A410/411/412 本轮补全）：审计发现 `VoiceMemosApp` 只有录音→私有 store→导出到系统（F-SH-016 ✓），但用户必需的三个能力**完全缺失**：①**播放进度**（用户在播放中无法跳到指定位置）✗；②**编辑裁剪**（无法裁掉录音的无用部分）✗；③**语音转写**（无法把录音转成文字）✗。三个能力都在录音使用场景的核心路径上。 | 用户期望：录音 → 播放时能跳进度 → 不想保留的部分 → 裁剪为新录音 → 需要文字 → 转写 ✓；实际：三个能力**均不存在** ✓ —— 用户只能重录、从头听、或手打转写 ✓ | 3 | 3 | 3 | 27 | ①**播放进度** ✓：`detailMemo` 派生的详情面板 → seek slider + 持续时间显示 ✓，`progressPercent`/`clampSeekSeconds`/`seekToMs` 领域函数 ✓，`curMs`/`durMs` 实时更新 ✓；②**编辑裁剪（edit-keep）** ✓：面板加 start/end 滑块，值由 `clampTrimRange`/`trimmedDurationMs`/`isFullRange` 校验 ✓，`trimMemo` 领域函数生成新 memo（原始录音保留）✓，`OfflineAudioContext` + 手写 WAV 编码器从原始 blob 提取片段 ✓；③**ASR 转写** ✓：`transcribeAudio` RPC → `classifyTranscribe`/`classifyTranscribeError` 分派四态（idle/transcribing/done/empty/error）✓，转写结果持久化到 memo.row.transcript 字段，`attachTranscript` 领域函数保证不可变性 ✓。新增 24 个 i18n 键 zh+en 同步 ✓ | 测试  `src/__tests__/voiceMemos.test.ts`（**+9** 套：progressPercent/clampSeekSeconds/clampTrimRange/isFullRange/trimmedDurationMs/trimMemo/attachTranscript/classifyTranscribe/classifyTranscribeError/normalizeVoiceMemos 覆盖 ✓）；`svelte-tests/vmemos.svelte.test.ts`（**+4**：选中的 memo 打开详情面板含 seek slider ✓；种子 memo 不显示裁剪按钮 ✓；选中的 memo 显示转写按钮 ✓；转写按钮存在时页面不崩溃 ✓）；`bun run test` **OK**（3257 pure）、`test:svelte` **1140/98** ✓、`i18n:scan` ✓ |
| F-SH-028 | **文档化的能力开关在应用窗口里从不生效:宿主答案只被启动器问过一次,而它是 per-WebView 的模块状态**(REQ-A453,真机实测 ✓ 2026-09-19):①`lib/desktopFeatures.ts` 把宿主对 `AMOS_DESKTOP_SHORTCUTS` / `AMOS_DOCK_CONTEXT_MENU` 的答案存在**模块状态**里 ⇒ 每个窗口一个 WebView、各有一份,**不问就没有** ✓;②只有 `DesktopShell`(启动器)在挂载时调 `loadDesktopFeatures()` ✓,而 REQ-A416 之后应用窗口渲染的是 `DesktopAppWindow` ⇒ 应用窗口的 WebView **永不发问** ✗;③后果(实测):以 `AMOS_DESKTOP_SHORTCUTS=disabled` 启动 —— 宿主如实打印 `desktop shell capabilities disabled by the environment disabled=["shortcuts"]` ✓ —— 之后在 Files 窗口按 ⌘W,**仍被本窗口的前端抢走并关掉窗口** ✗,而该开关存在的意义正是让 OS 接管这些键 ✓。取证手法:`open --stderr` 经 LaunchServices 启动(应用真的成为前台,这是上一轮误诊的分水岭 ✓)+ AX `AXMenuItemCmdChar` 读出菜单真树 + `osascript` 逐键派发 ✓ | 运维按文档设了开关、宿主也如实上报,**前端却当没设** ⇒ 「开关没用」被读成「功能坏了」,而真因是半边窗口没问过 ✓(F-SH-001 家族:文档说的 ≠ 实际做的,这次落在**能力开关**上 ✓) | 2 | 3 | 3 | 18 | ①应用窗口的壳面在挂载时问一次(`DesktopAppWindow` 的 `onMount` → `loadDesktopFeatures()` ✓),与启动器同一条规则,单飞 ⇒ 不产生第二次往返 ✓;②`desktopFeatures.ts` 模块文档写明「答案住在模块状态 ⇒ per-WebView ⇒ 谁消费谁问」✓;③`docs/ENV_VARIABLES.md` 的读取方描述同步更正 ✓ | `svelte-tests/desktop-app-window.svelte.test.ts` **+2 例**:`a host answer of ['shortcuts'] stops this window claiming ⌘W`(走**宿主往返** `desktop_features_disabled`,不是测试钩子 ✓)+ **正向对照** `an EMPTY host answer keeps ⌘W closing this window`(证明前一条不是「窗口整个不再管 ⌘W」✓);**负控** ✓:删掉 `DesktopAppWindow` 那次 `loadDesktopFeatures()` ⇒ 立刻红(1 failed / 8 passed ✓),还原后 9/9 ✓ |
| F-SH-029 | **删除没有退路：文件管理器里的「删除」是永久删除**（REQ-A455，用户可感知的数据丢失）：`lib/files.ts` 只有 `deleteEntry`/`deleteEntries`（纯列表过滤），`FilesApp` 把结果直接写回 `amos.files` —— **没有确认、没有撤销、没有「放回原处」**；Dock 的废纸篓瓦片因此只能是灰的，而它自己的注释写着理由："There is no Trash to open: the Files screen has no trash view…so an 'empty the trash' action would have nothing to act on"。macOS 把废纸篓当**第一道保险**（误删是常态 ⇒ 删除是"移动"不是"销毁"）：本仓 SMS 有视图层 trash，**文件层完全没有** | 一次误点 = 文件真的没了（多选批量删除更是"一次点掉一片"）；用户对桌面 OS 的期望里，这一条失败是**不可逆**的那种（F-DEV-005 同族：能力存在与能力可用是两件事 —— 这里连能力都不存在） | 3 | 3 | 3 | 27 | ①**账本与树分开**（`amos.files.trash`，不进 `FEntry[]`、不做 `trashed` 标记 —— 否则每个既有操作都要学会忽略它）；`TrashItem` 存**整棵子树**（根在第一位）＋来源＋时刻；行按 **id** 寻址，不按名字；②三个纯函数 `moveToTrash` / `restoreFromTrash`（**具名结果**：`restored`/`restored-to-root`/`not-found`/`name-conflict`/`id-conflict`）/ `purgeFromTrash` + `normalizeTrash` 防损坏；③**上界是安全属性**（`MAX_TRASH_ITEMS=100`，最旧先出局且**回报数量**）；④**提交顺序先账本后树**（第二步失败 ⇒ 重复而非丢失）；⑤界面：列表里的「删除」改为「移到废纸篓」，破坏性动作只在废纸篓页签里（放回原处 / 永久删除 / 清空）；⑥被拒与被降级（放回根目录）用 `role="status"` 说出来；⑦`deleteEntry/deleteEntries` **删除**（`unwired-scan` 抓到它们变成死代码 —— 一个"不带账本就能删掉条目"的 helper 正是本轮要拆掉的枪） | `bun test src/__tests__/files.test.ts` **31**（+13：往返恒等 / 文件夹整棵 / 嵌套选择只记一行 / 空集与未知 id 无操作 / 上界与逐次出局计数 / 三个冲突具名 / 防损坏）；`npx vitest run svelte-tests/files.svelte.test.ts` **27**（+5 DOM：移动 / 放回 / 永久删除+空态 / **账本写失败时文件仍在树里**（钉 `commitTrash` 的顺序）/ 同名拒绝并上屏）；`store-scan` OK（新键进 `SYNC_STORES`，`cloud.test.ts` 的"恰好这一批"清单同步 +1）；`unwired-scan` OK（删掉两个 helper 后无新增死导出）；`bun run check` **EXIT=0** |
| F-SH-030 | **Dock 的图标顺序既改不了、也不跟随 store（一个功能缺口 + 一个潜伏缺陷）**（REQ-A456，2026-09-19）：①**缺口**：Dock 的图标顺序是用户数据（`layout.dock`），但**没有任何办法改它** —— `Dock.svelte` 里没有 `draggable`/`dataTransfer`/指针拖拽（全仓只有 `ShortcutsApp`/`HomeDock` 用它自己的 HTML5 拖拽），而 `docs/DESKTOP_IMPROVEMENT_PLAN.md` 把它列为"本月改进 #1（缺 Drag & Drop）"却没人动。②**潜伏缺陷（代码评审才发现，与拖拽无关）**：Dock 把布局读成**挂载时的快照** —— `$derived(getLayout(APP_META…))` 里 `getLayout` 只读 localStorage，**不读任何信号** ⇒ 别的窗口改了布局、或 `store-updated` 从宿主到达，**Dock 的图标顺序不会变**（与"正在运行"白点那条 store 通道形成对照：那条是订阅的，所以能跟）。两件事互相咬住：拖完不重画，就等于"拖了没反应"。 | 用户无法按自己的习惯排列 Dock（macOS 最基本的自定义之一）；更坏的是**两种"没反应"分不出来**：布局真的没变、还是界面没跟 —— 后者在别的窗口改布局时每次都会重演 | 2 | 3 | 3 | 18 | ①**纯函数**（`lib/amosStore.ts`）：`dockReorderIds` 按 macOS 的**槽位规则**（拖到右边落在被悬停图标的**后面**、拖到左边落在它**前面**）——`moveBefore` 那条"总是插在目标前"的规则在 Dock 里向右拖会少一格，所以没有复用它；`reorderVisibleDock` 只置换**可见**项、其余留在原槽位（`DEFAULT_DOCK` 里有 `phone` 而桌面不画它 ⇒ 朴素写回会**删掉**手机形态的 Dock 项）；两次都返回**同一个引用**表示无变化（调用方据此决定写不写）；②**组件**：布局改为订阅共享 store（`createStoreValue(LAYOUT_KEY)` + `$state`），拖拽用**委托**在容器上的 pointer 路径（`mousedown` → 4 px 阈值 → `mousemove` 取 `e.target.closest("[data-dock-item]")` → `mouseup` 落位），落位后吃掉紧随其后的那次 click（否则拖完会顺手打开 app）；写失败沿用 Dock 既有纪律（`storeError` + 保持旧顺序）；③**只有用户 app 瓦片是投放目标**（系统项的位置是产品决定，不在 `layout.dock` 里），视觉提示是新 token `DOCK_DROP_TARGET`（与"焦点环/选中环"分开：三个问题不能合成一个环） | `bun test src/__tests__/amosStore.test.ts` **35**（+4：左右两个方向的槽位规则 / 三个无操作保持同一引用 / 只置换可见项且 `phone` 留在原槽 / 无操作返回同一 layout 对象）；`npx vitest run svelte-tests/dock-reorder.svelte.test.ts` **4**（拖拽落位并保留 `phone`、渲染顺序跟着变 / **按住松手仍是点击（app 打开、顺序不变）** / 释放到系统项上什么都不发生 / 释放到 Dock 外什么都不发生）；**负控（构造 + 逐字节还原 `sha256sum -c`）**：①把 `reorderVisibleDock` 换成朴素写回 ⇒ 纯逻辑与 DOM 同时红；②把 click 抑制改成无条件 ⇒ "点击仍然打开 app"红；③**如实记录**：把容器那道 `dockAppIds.includes` 去掉**不会**让系统项用例变红（规则本身在纯层：被悬停的 id 不在 dock 里 ⇒ 返回原 layout）——所以用例注释里**写明**它钉的是"用户可见的性质"，不是那道守卫 |




### 2.2b 输入法 (amos-ime / amos-tauri)




| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|-----|-----------|----------|
| F-IME-001 | 两个窗口**共用一条拼音缓冲**(A 的候选栏显示 B 打的码、B 的提交吃掉 A 的码) | 用户看到/提交**不是自己打**的字(REQ-A258 的 G1) | 3 | 1 | 2 | 6 | 作用域拆开:`PinyinCore`(词典 + L0 学习层 + 模糊音,`Arc`,进程级)与 `PinyinInput`(每窗口一条缓冲 + 提示 + 撤销码);九条 `ime_*` 带 `window: tauri::WebviewWindow` 按 `window.label()` 归档缓冲;设备级动作(模糊音/清空学习)显式作用于每个窗口 | `two_windows_do_not_share_a_composition_buffer`、`the_undo_hint_is_per_window`、`two_sessions_over_one_core_keep_their_own_buffers`(**负控**:改成单会话 ⇒ 前两条 FAIL) |
| F-IME-002 | 每窗口一条缓冲 ⇒ 窗口标签只增不减,**无界增长** | 长会话内存/查找成本持续上涨(Power of 10 #3) | 2 | 2 | 3 | 12 | `MAX_IME_SESSIONS = 64`:达上限先丢弃"没什么可失去"的窗口(空缓冲/无撤销码/无提交提示),只有所有窗口都在打字才逐出最少使用并 `warn!` | `the_window_map_is_bounded_and_prunes_what_holds_nothing`(含"正在打字的窗口不会在有空闲可丢时被逐出") |
| F-IME-003 | 联想(下一词)**数据装了但没接线**:候选栏不呈现 ⇒ 用户看不到联想,而文档却写着"已完成" | 功能缺口被当成已交付(REQ-A260 复核 REQ-A259) | 3 | 2 | 3 | 18 | `state_of` 在**缓冲为空**时把建议作为候选返回(kind `predict`,与缓冲候选同一索引空间);键盘在 `composing \|\| suggesting` 时渲染候选栏并给 `ime-predict-tag` 标签;域侧 `PinyinInput::predictions()` 只在**两个已提交词**的上下文下给建议(拒绝噪声) | `an_empty_buffer_offers_suggestions_for_what_this_window_committed`、`predictions_need_two_committed_words_of_context`、`suggestions render with their own tag and no composition chip` |
| F-IME-004 | 联想建议**被当成用户输入**:污染学习层,或给出一个永远无效的"撤销此词" | 词典学到用户没打过的词;撤销按钮是坏承诺 | 3 | 2 | 3 | 18 | `commit_prediction` 只记录提交(供链式建议),**不**教学习层;宿主在建议分支把 `last_pick_code` 置 `None`(建议没有拼音码 ⇒ 没有可撤销的 pin) | `picking_a_suggestion_inserts_it_and_teaches_nothing`、`a_picked_prediction_continues_the_chain_without_teaching_the_learner` |
| F-IME-005 | 联想的 FST 数据使二进制变大(实测,隔离探针与应用二进制两条路一致:**+19.6 MB**) | 移动端 APK 体积上涨,装机/更新成本上升 | 2 | 1 | 1 | 2 | `amos-ime` 的 `predict` 特性(default on)是**唯一**开关;瘦身构建 `--no-default-features`:所有调用点无条件(我们自己代码里没有 `cfg`),缺数据时引擎按契约返回"没有建议"而不是假装 | `predictions_need_two_committed_words_of_context`(缺数据时同样返回空 ⇒ 路径不会 panic/假报) |

### 2.3 System UI 桥 (amos-tauri)

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|---|-----------|-----------|
| F-TAU-001 | 桥返回 null 而非结构化错误 | UI 不知根因 | 3 | 4 | 3 | 36 | `backend.ts` 改为 `{ok, error}` 形态 + 诊断账本 | `backend.test.ts` |
| F-TAU-002 | AI 桥超时未上报 | UI 卡死 | 4 | 3 | 2 | 24 | gRPC deadline + 探针 | `ai_bridge::tests` |
| F-TAU-003 | 剪贴板跨用户泄露 | 隐私 | 5 | 2 | 4 | 40 | `clipboard_guest` 审计计数;前台门控 | `clipboard_guest_link_e2e` |
| F-TAU-004 | 剪贴板后台读取 | 隐私 | 4 | 2 | 5 | 40 | `clipboard-changed` 公告只含元数据 | `clipboard-announce.test.ts` |
| F-TAU-005 | 远程驱动 (host→container) 静默失败 | 状态分叉 | 3 | 3 | 3 | 27 | `mirror_failed` warn | `lmk_reverse_drive.rs` |
| F-TAU-006 | 卸载策略被 UI 绕过 | 系统包被删 | 4 | 2 | 4 | 32 | `UninstallGuard` 单一构造点 | `devcare::the_preview_and_the_enforcement_agree` |
| F-TAU-009 | 云端 API key 写入失败 UI 谎报"已保存" | key 实际未落盘 | 4 | 2 | 4 | 32 | `persist_cloud_key` 全检查 + 0600 模式断言 | `ai_bridge::persist_cloud_key` |
| F-TAU-008 | WebView 里未捕获异常刷新即失 | 失败不可见 | 3 | 3 | 4 | 36 | `uiFailures.ts` 观察者记账本 | `uiFailures.test.ts` |
| F-TAU-010 | **"注册了"与"手机真会被叫醒"曾被混为一谈 —— 系统拒绝精确闹钟时界面照旧显示正常**（REQ-A369 收口 ✓，**F-TAU-007 的兄弟** ✓）：`scheduler_alarm_register` 原本只写内存 ledger ✓ 并答 `Ok(())` ✗ ⇒ 在 **Android 12+ 拒绝精确闹钟**时（`canScheduleExactAlarms()==false` ✓，或 `setExact…` 抛 `SecurityException` ✓），**用户看到的与真能唤醒时一模一样** ✗；`AlarmGlue.schedule` 那时也只是 `Log.w` 一行 ✗（回不到 UI ✓） | 后果是**用户以为闹钟会响** ✓ 而手机在 doze 里不会醒 ✓ —— 属于"界面在说谎"里最贵的一类 ✓（它发生在**用户睡着时** ✓）；更普遍的形式：**平台能力的"拒绝"必须是一种可显示的状态** ✓，不能只活在 logcat 里 ✗ | 2 | 3 | 4 | 24 | ①Kotlin `AlarmGlue.schedule/cancel` **返回状态词** ✓（`scheduled` / `disallowed` / `unavailable` / `denied` ✓），Rust `device_outcome_from_status` 映成**类型化**的 `DeviceOutcome` ✓（不认识的词 ⇒ `Unknown(…)` ✓，**绝不四舍五入成成功** ✓）；②命令回包从"空"变成 `AlarmArmed{id,atMs,device}` ✓ ⇒ 前端 `osAlarmArm` 记下设备态 ✓、`nativeWakeArmed()` **只在 `scheduled` 时为真** ✓；③**Clock 闹钟页出现横幅** ✓（`data-testid="alarm-native-wake"` ✓，并带宿主原词 ✓）；④`AlarmGlue.openExactAlarmSettings` 给出授权入口 ✓ | `cargo test -p amos-tauri --lib alarm_sched::` **7 例** ✓（+3：状态词映射 ✓ 含**空词/未知词**两条不可四舍五入的负控 ✓、宿主 `host_only` ✓、序列化形状 ✓）；`bun test src/__tests__/osAlarmArm.test.ts` **6 例** ✓（+2：`disallowed` ⇒ `nativeWakeArmed()===false` ✓、`scheduled` ⇒ `true` ✓）；`vitest clock.svelte.test.ts` **25 例** ✓（+1：横幅渲染且 `data-native-wake="disallowed"` ✓）；**真机验收待做** ✗（设备未连 ✓） |


### 2.3 机器人中间件 (amos-link)

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|---|-----------|-----------|
| F-LK-001 | 误报急停 | 机器人骤停 | 4 | 2 | 2 | 16 | `latched` 状态需显式恢复;`deadman` watchdog | `amos-link` 17 lib 测试 |
| F-LK-002 | 漏报急停 | 失控 | 5 | 1 | 1 | 5 | 多源汇总;**`return path`** 折叠 | `amos-link` return path 测试 |
| F-LK-003 | CRC 校验后分配(解码前) | 16 MiB 帧接收端先分配 32 MiB | 4 | 2 | 4 | 32 | `crc32_over()` 流式 CRC,**修复后负控实测** | `codec::allocation_budget` |
| F-LK-004 | 多 NIC 信标走错接口 | 5G modem 出 / Wi-Fi 收 | 4 | 3 | 3 | 36 | `AMOS_LINK_BEACON_IFACE` 钉接口;`lan_multicast.rs` 真测 | `lan_multicast.rs` |
| F-LK-005 | 节点把自己当 peer | 自我回声 | 3 | 4 | 2 | 24 | `PeerRegistry::with_local` 拒绝本机 id | `amos-link::a_node_is_never_a_peer_of_itself` |
| F-LK-006 | 匹配器在注册表锁内分配 | broker 抖动 | 3 | 3 | 3 | 27 | 栈定长数组 + 1000 matches 0 bytes 断言 | `keyexpr::allocation_budget` |
| F-LK-007 | 锁中毒 ⇒ offer_blocking 无界等待 | 控制回路卡死 | 4 | 2 | 4 | 32 | `LinkError::Closed` 类型化 + 负控 `Elapsed` | `broker::tests` |
| F-LK-008 | 静默截断 (`usize as u32`) | 数字读错 | 3 | 2 | 4 | 24 | 饱和算子 + 负控 | `count_to_u32` 单测 |
| F-LK-009 | CLI `--seconds u64::MAX` panic | 进程崩溃 | 4 | 2 | 5 | 40 | `deadline_after()` 解析期拒绝 exit 2 | `cli_smoke::arguments_that_used_to_crash_*` |
| F-LK-010 | 真实硬件 HAL 未测驱动 | 误以为电机已断电 | 5 | 1 | 4 | 20 | `StreamRobotHal` 写入前验证 + 帧计数实测 | `motor --device` 进程级 |
| F-LK-011 | 公告塞不进信标帧 ⇒ 节点在 LAN 上**静默不可见** | 每张对端表都没有它,而日志只有 `debug!` | 4 | 3 | 3 | 36 | `PeerInfo::advertising` **启动期**探针编码(逐字段界 8×128B **不等于** 512B 整帧界)+ CLI `--endpoint` 越界 exit 2 / exit 1 | `an_unemittable_advertisement_is_refused_before_the_task_starts`、`cli_smoke::an_advertised_endpoint_*`(第 5 段) |
| F-LK-012 | 时钟不一致被读成 **0 延迟**（`Timestamp::since` 对未来的戳饱和为 0） | 操作员读「刚刚到达」;`bench` 的 p50/p99 比链路更漂亮 | 3 | 3 | 3 | 27 | CLI 单一规则 `age_between`(未来 ⇒ `InTheFuture`,人类行 `age=unknown(…)`/JSON `age_ms: null`);`bench` 把不可测帧**计数不计样**(`skewed`/`latency_us.samples`);`clock_synced=false` 旁注 | `a_frame_stamped_in_the_future_has_no_age_to_print`、`a_benchmark_never_counts_an_unmeasurable_frame_as_zero_latency`、`cli_smoke::the_age_caveat_and_the_bench_sample_count_are_wired` |
| F-LK-013 | **真网络上的节点把自己的计数器留在 0**（传输不计数 ⇒ `published`/`delivered` 终生为 0，而 0 被读成「没发过」） | 操作员/判决/面板把一条正在发帧的链路读成空闲 | 4 | 3 | 4 | 48 | 传输持有节点的那一组计数器（`Transport::metrics`）：`put` 成功 ⇒ `published+1`、转发交接 ⇒ `delivered+1`；`LinkNode::with_parts` 在两组不同 Arc 时 `warn!` | `a_node_over_a_real_session_counts_what_it_publishes`（真 TCP 会话，两个 peer） |
| F-LK-014 | **老 daemon 少一个 RPC ⇒ 整次读取失败**（文档承诺的是「少显示一栏」）→ 面板变「未连接」、CLI exit 1，连已答的状态/对端表一起丢 | 升级期的现场读不到任何东西；「我们没被告知」被渲染成「没有人上报」 | 3 | 3 | 3 | 27 | 只对 `Unimplemented` 降级：回程成为 `actuations: null`（三态）+ `warn!`，其它失败照实报错；CLI 与面板同一规则；三态纯函数 `returnPathLevel` | `a_daemon_without_the_return_path_still_answers_the_panel`、`an_older_daemon_still_answers_the_status_over_a_socket`、`link-page.svelte.test.ts` |
| F-LK-015 | **QoS 只在进程内成立**：网络传输上 `Qos::sensor()`（latest-wins）交给消费者的是**最老的那一帧**；`DropPolicy::DropNewest` 从不生效（满即背压整条链路）；订阅自己的 `dropped` 恒为 0 | 相机→大脑链路上拿到陈旧帧（正是该 profile 要避免的）；丢帧数读成 0，操作员据此判断链路健康 | 4 | 3 | 4 | 48 | 远端订阅按 profile 建**同样的 sink**（`Subscription::remote_latest` / 有界队列），转发任务按可靠性分支（best-effort 满即丢并计数、reliable 满即等）；新增 `RelayCounters` 让订阅侧与节点侧计数器同源 | `a_latest_only_subscription_over_a_real_session_keeps_the_newest_frame`、`a_best_effort_queue_over_a_real_session_drops_the_newest_and_counts_it`（真 TCP 环回会话，先红后绿） |
| F-LK-016 | **平台剖面机器未解锁 ⇒ motion 被拒**：剖面机器的 `Motion` 批次**不自带 `Enable`**（与参考四足不同），运行期需要显式 `Arm`；漏 Arm 的 motion 路径会让机器「以为能走、其实收不到」 | 操作员下达 "go"，机器不动；驾驶舱/UI 把这读成链路/规划故障而不是 arming | 3 | 2 | 3 | 18 | `step_inner` 的 motion 分支**先看是否 latched**：未解锁时用剖面 `Vocabulary::arm_hint()`（`arm` 或 `enable`）点名解锁键并返回 `Refused`；`Halt` 永不被拒（cut torque 必须在任何状态下生效）；参考机的运动批次自带 `Enable`，该分支对它永不触发（参考机等价测试钉住） | `the_drone_refuses_motion_until_armed`、`the_reference_machine_never_enters_the_must_be_armed_branch` |
| F-LK-018 | **固定位姿动作上的 `speed` 被收下、校验、然后丢掉**：`Platform::parse_intent` 解析 `speed` 并做 `[0,1]` 范围检查，而 `speed_scaled: false` 的动作（`takeoff`/`rtl`/`hover`/`lane_keep`/`cycle`…）把它**丢弃** —— 实测 `takeoff` 的 `speed:0.0` 与 `speed:1.0` 产出**同一批** 4×55% 推力帧 | 地面站要求「温柔起飞」/「慢一点进近」，机器全速执行且**没有任何提示**；操作员据此以为该数字生效了（正是本仓在别处一律拒绝的「静默丢掉输入」） | 3 | 3 | 3 | 27 | `parse_intent` 在范围检查之后**拒绝显式 speed**：`action takeoff on platform drone has a fixed pose, so speed 0.1 cannot be honoured: remove it, or send the set points you want in targets`；`Arm`/`Halt` 不受影响（拼错字段不得拦住解锁与停车）；参考机的步态全是 `speed_scaled: true`，该分支对它永不触发 | `a_speed_that_cannot_be_honoured_is_refused_not_dropped`（含「每个动作要么用上要么拒绝」的全剖面扫描）、负控 1/5 |
| F-LK-019 | **同一执行器的两个设定点被 `find()` 静默取第一个**：`[{"actuator":0,"arg":10000},{"actuator":0,"arg":90000}]` ⇒ `arg:90000` 消失，**JSON 字段顺序**决定了无人机飞哪个推力 | 一条自相矛盾的指令被静默二选一；现场复盘时「我发的明明是 90000」无人能解释 | 3 | 2 | 4 | 24 | `parse_intent` 拒绝重复：`actuator 0 (thruster_1) is named twice in one intent: two set points for one actuator leave the layer choosing which of them to drop`；参考机路径冻结（其语义是「第一个赢」并逐字节为证） | `two_set_points_for_one_actuator_are_refused_rather_than_first_wins`、负控 2/5 |
| F-LK-020 | **「只有目标参数的动作」可以不带参数**：`{"action":"goto"}` 被接受并飞出默认 58% 推力位姿 —— 一个顶着「去某处」名字的**默认动作**（`waypoint` 同理） | 工具少填一个字段 ⇒ 机器动起来，而且动的是剖面作者随手写的位姿 | 3 | 2 | 3 | 18 | `ActionSpec::min_params`（`goto`/`waypoint` = 1）在参数校验**之后**检查：`… needs at least 1 of its parameters (north_mm, east_mm, altitude_mm), and this intent names none of them — a target is not a default`；有文档化默认值的动作仍是 0（车道偏移 0 = 居中） | `an_action_that_is_its_target_is_refused_without_one`（含 7 个「有默认值、不得要求参数」的反例）、负控 3/5 |
| F-LK-021 | **「机器是数据」是空话**：`Platform` 字段私有 + 六份 `const` ⇒ 部署的自有机器（±120° 机械臂、八旋翼、不同制动执行器）只能改本 crate 的文件；而自相矛盾的剖面（位姿长度与执行器表不符、两个 `Arm`、行程超出帧参数空间、看门狗排不出来）**没有任何一条规则在编译期或构造期拦它** | 一台带着自相矛盾词表的机器上线：接受了指令、写出了没人能解释的帧；`plan()` 里那些拒绝句子对六个内置剖面**永不可达**（测试无法覆盖） | 4 | 2 | 3 | 24 | 新增 `Platform::from_parts(kind, actuators, actions, envelope, arm_on_motion)` + `Platform::validate()`（18 条规则：执行器 ≥1、序号从 0 连续、≤12 且在帧参数空间内、名字唯一、行程有序且两端合法；动作键唯一、恰好一个 `Arm` 与一个 `Halt`、位姿长度/行程、非运动动作位姿惰性、参数名唯一且区间有序、`min_params` 可满足、包线可排程）。集成测试把**同一条规则**跑在六个内置剖面上（一条规则、两个调用方） | `a_misdeclared_profile_is_refused_naming_the_rule_it_broke`（18 个剖面各坏一条规则）、`a_deployment_profile_is_a_usable_machine_not_just_an_accepted_value`、负控 4-5/5 |
| F-LK-017 | **剖面参考机兼容性**：把"参考四足"换到 `Vocabulary::Profile(&quadruped)` 路径必须与 `Vocabulary::Reference` 路径**逐字节产出相同的帧**，否则迁移现存 fleet 时机器人行为漂移 | 同一架机器人从老 daemon 切到新 daemon 后步态不一致；上游控制/学习数据对不上 | 3 | 3 | 2 | 18 | `Platform::quadruped()` 用与手写 `robot_hal::plan` **同一份逻辑**构建 motion（同样的 `Enable(0)` 前导、同样 12 关节位置帧、同样的速度换算）；**等价测试** `the_reference_platform_plans_byte_identically_to_the_handwritten_path`（含前导 `Enable(0)` 这个历史怪癖，不修正——修正会破坏现有数据） | `the_reference_platform_plans_byte_identically_to_the_handwritten_path`（13 例剖面契约测试的第一条） |

### 2.4 数据完整性

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|---|-----------|-----------|
| F-DA-001 | 损坏的 localStorage 被静默回退 | 用户数据丢失 | 3 | 3 | 4 | 36 | `readJson` 区分缺失/损坏 + 隔离备份 | `amosStore.test.ts` |
| F-DA-002 | 隔离备份从未被读出 | 用户拿不回数据 | 2 | 4 | 4 | 32 | `listQuarantined` / `readQuarantine` UI 暴露 | `amosStore.test.ts` |
| F-DA-003 | 长会话无界累积 | OOM | 3 | 4 | 3 | 36 | `capTail` 200 条上限 | `bounded.test.ts` |
| F-DA-004 | 备份快照 key 与真实存储分叉 | 收藏/消息漏备份 | 4 | 3 | 4 | 48 | `SYNC_STORES` 改为模块常量键 | `settings.test.ts` |
| F-DA-005 | AI 历史会话读取未授权 | 历史泄露 | 3 | 2 | 3 | 18 | `GetHistory` RPC + session ownership 校验 | `ai_history_e2e` |
| F-DA-006 | **一份 id 由「时钟 + 一把随机数字」铸成**（联系人 / 录音的 media key / 通知行 / AI 消息气泡 / 快捷指令 / 会话 id / 分组 id / 动作 id 各自写一份公式）：冻结时钟下 5 位 base36 尾巴（≈26 bit）在 20,000 次紧循环里**撞 3 次**（REQ-A401 实测，种子可复算），而 6-9 位（≈31-53 bit）**这一次没撞** —— 差别只是算术运气 | 撞了**没有任何东西会报错**：`mediaId(captureId)` 是视频字节的存储键 ⇒ 后一次录音**覆盖前一次的字节**而 `amos.captures` 仍列两行；`normalizeNotifs` 按 id 去重 ⇒ 撞 id 的通知**重载后消失**（`newestAddedNotif` 也按 id 集合判「刚到」，连提示都没有）；`patchCur` 按 id 找正在流式的气泡 ⇒ token 落进另一个气泡 | 3 | 2 | 4 | 24 | 这一族只剩一个所有者：`lib/localId.ts`（进程内单调计数器 + 零填充 crypto 尾，唯一性由**构造**保证，不靠随机尾巴）。11 处生成器全部改走它（`contacts`/`measure`/`cameraCapture`/`webman`/`pushNotifications`/`customGroups`/`shortcuts`/`backend`/`settings.newNotifId`/`AiApp.uid`/`ShortcutsApp` 动作 id），前缀契约不变；`scripts/idgen-scan.mjs` 两条规则：**R1** 生产代码里每个 `Math.random` 都要在 `idgen-allowlist.json` 里有写明理由 + `hits` 计数的条目（给已豁免的文件再加一处即 `count-drift`），**R2**「id = `Date.now()` + `Math.random`」判 `id-idiom` 且**一律失败、不可豁免** | `src/__tests__/idUniqueness.test.ts`（12 例：冻结时钟 + 固定种子 + 2 万次紧循环，含「退役公式必须撞」的负控）、`scripts/idgen-scan.mjs --selftest`（17 断言）、`lib/localId.test.ts` |
| F-DA-007 | **身份由「时钟 + **进程内**计数器」铸成**（`notes.makeNote`、`reminders.makeId`、`calendar.makeId`、`voiceMemos.makeVoiceId`、`time` 闹钟 `add`、`messages.addConversation` 的 `c:${name}-${now}`）：**熵为零** —— 时间戳 + 一个进程启动时归零的计数器，而"壳"是**多窗口共享一个 store**（`svelte/store.ts` 把主机的 `store-updated` 镜像进每个窗口） | 两个上下文在同一毫秒铸 id 会得到**同一个字符串**（实测：两个真进程 + 冻结时钟都产出 `loyw3v28-1`，同脚本的 `localId` 因 7 位 crypto 尾而不同），而消费者全是静默失败：`normalizeVoiceMemos`/`normalizeCalendars`/`normalizeLists` **丢行**（实测 2 行进 1 行出，录音字节成孤儿）；`normalizeConversations` 曾**根本不查重**（实测两个 `c:小安`，`removeConversation` 删掉两行；且它不需要时钟巧合 —— 一个"有行没 id"的旧 store 一次加载即可复现）；`normalizeNotes`/`normalizeReminders`/`normalizeEvents` **改名**（`-1`/`-2`）⇒ 指向该 id 的引用静默失效（RAG 索引 `note:<id>`、`openNote` 通道载荷、编辑器选中行） | 3 | 2 | 4 | 24 | 六处生成器全部并入 `localId()`（进程内计数器 + 零填充 crypto 尾），生成器的 `now` 参数删除（铸 id 不再需要时钟）；`normalizeConversations` 补上与兄弟内核同款去重（`baseId` + `k++`，保持"同一输入同一输出"）；**回填/修复 id 保持"时间 + 序号"**（它们必须跨加载稳定，R3 明确不管）；`scripts/idgen-scan.mjs` **R3**（`id-from-clock`）不可豁免：`id:`/`id =` 被赋插值了时钟的模板，名字以 `Id` 结尾的函数返回这种模板（含箭头写法），或 `newX(…)` 工厂调用的第一个实参是这样一棵模板（`newPhoto()` 因此改为**自己铸 id**，相机路径改走 `newPhotoForCapture(id, now)`，调用方再也传不进一个时钟 id），按名豁免 `localId.ts` 本身 | `src/__tests__/idUniqueness.test.ts`（形状契约 + 六个生成器逐个 + 冻结时钟下各 3,000 次唯一）、`messages.test.ts`（同 id 两行现取不同 id）、`idgen-scan.mjs --selftest`（35 断言） |
| F-DA-008 | **测试套件里存在"真上网"的用例**（形态：模块**自己 catch** 网络错误并降级 ⇒ **没打桩也绿**；本次实测的历史案例是 `compass.ts::fetchDeclination` 的 **6 次真实往返**） | 绿不再说明代码对（离线/半离线走的是另一条分支）；CI/开发机的慢网或断网让套件**环境相关地红**（REQ-A400 的 5 s 超时就是这么来的）；"我们的测试不打网络"这句话**没有任何东西在验** | 3 | 3 | 3 | 27 | `scripts/net-sentinel.mjs`（测试 preload：默认 `fetch` = 记录 + 拒绝；打桩不受影响；还原到的是哨兵）+ 三处接线（`bun-iso-test.mjs` 每文件日志归因、vitest setup 钩子按用例归因、`bunfig.toml` 覆盖裸 `bun test`）；`--selftest` 3 断言 | `bun-iso-test test`（0 egress）、`svelte-tests/setup-net-sentinel.ts`、负控见 CHANGELOG REQ-A403 |
| F-DEV-010 | **覆盖率门的分母把"没有语句的行"当成可执行行**（`}` / `});` / `};`：bun 的 lcov 只发 `DA:n,0`、从不标命中）⇒ 门读数被系统性**压低约 10 个点**，于是它**长期红着**，而真实覆盖率**高于阈值** | 团队按一个错的数字决定"哪块代码最差"并投入轮次（本仓连续多轮都在追这个数）；反过来也危险：把"红"当成"代码差"会掩盖真正该看的东西（此处真实数字 94.95% > 90%，而 1,946/2,669 的"缺失"根本不存在）；"分母只算可执行行"这句话此前只在**注释/类型行**上为真 | 3 | 4 | 3 | 36 | `lib-coverage-gate.mjs` 的 `executableLinesFromSource()`：只有收尾括号的行不算可执行（注释/字符串/模板感知，`} else {` 仍算分支）；`--selftest` **14 断言**钉住两侧（结构行丢 / 真语句留），负控：阈值之上仍 FAIL、给 100% 的文件加未覆盖行数字必降；另修 `bun-iso-test.mjs` 的 `withPreload`（只给 `test` 传 `--preload`） | `node scripts/lib-coverage-gate.mjs --selftest`（14 断言）、`bun-iso-test.mjs coverage` 端到端、负控见 CHANGELOG REQ-A404 |
| F-DEV-011 | **覆盖率门的分母有个"隐形"盲区：从没被任何测试 import 的模块对整个门**不存在**（153 个模块只"看见" 150 个，其中 `sysIcons.ts` 是真逻辑：图标映射 + 电池字形，被 20+ 个组件使用）** | 一整块被测代码可以**完全不进分母**："哪块最差"的名单里没有它，绿数字与它无关；门本身也不会说"我有东西没看过"（与 F-DEV-010 同一族，方向相反：那条是分母**过大**，这条是整个文件被漏掉） | 3 | 3 | 4 | 36 | `lib-coverage-gate.mjs`：`libModulesOnDisk()` 枚举磁盘上每个 `src/lib` 模块，凡未被任何报告提到、又没在 `scripts/coverage-unmentioned-allowlist.json` 里带理由登记的 ⇒ **FAIL 并点名**（陈旧豁免同样判死）；每次运行打印 `modules: N/M measurable`；三个隐形模块（`sysIcons` / `shellChrome` / `version`）补齐真测试，并把可证明"没有自己的语句、且 bun 从不记录"的三小类行（`},` / switch 标签行 / 多行 `import { … }` 续行）从分母去掉（`--selftest` 21 断言） | `node scripts/lib-coverage-gate.mjs`（**153/153 measurable**）、`--selftest` **21** 断言、负控：放一个没人 import 的模块 ⇒ FAIL 并点名，删除即恢复；`bun run check` EXIT=0 |
| F-DEV-012 | **"注释/签名说要这么做，代码其实没做"四处**：快捷键配置的**禁用分支是个空循环**（`for (…values()) arr.findIndex(…)` 结果丢掉）且换绑只加不摘旧键 ⇒ 设置页的冲突面板为**不存在**的重叠报警（而冲突检测正是"保存前拦截无效配置"的门）；`writeKeyboardConfig` 的布尔返回值**恒为 true**（写盘失败被吞）；`uiFailures` 换 target 时**旧 target 的监听留着**（同一失败记两次，reset 只摘得掉最后一个）；`customGroups` 读侧对 `id`/`name` 严格到丢整行、却把 `apps` **逐元素**原样透传 | 用户看到的冲突/成功/唯一性都可能与真实状态不符：禁用的键仍被算作占用、被拒的保存被当成成功、一条失败在账本里记两次、分组里出现不存在的成员（UI 渲染幽灵瓦片）——都与 F-DEV-010/011 同族：**绿/成功/唯一这些结论建立在没被验过的代码上** | 3 | 3 | 4 | 36 | 覆盖 = **替换**：`removeEverywhere(id)` 先摘再挂（四组覆盖统一走它）；`writeKeyboardConfig` 改用 `writeStoreValueChecked`；`uiFailures` 抽出 `detach()`（交接与 reset 共用，且宿主 `removeEventListener` 抛错不弄崩交接）；`customGroups` 的 `apps` 逐元素 `filter(typeof === "string")`；另把 `addAppsToDock` 的错误注释改成与签名一致（过滤由调用方负责） | 测试先行（先红后修）；负控（逐字节还原 + `cmp`）：`detectConflicts` 原块还原 ⇒ **30 pass / 3 fail**、`writeKeyboardConfig` 回退 ⇒ 1 红、去掉交接 `detach()` ⇒ 1 红、`apps` 过滤回退 ⇒ 1 红；`make cov` OK（96.18% = 13,675/14,218）、`bun run check` EXIT=0 |
| F-DEV-013 | **"守卫函数"的输入形状没人守卫**（REQ-A407）：`lmk.androidLmkTasks()` 把宿主应答原样断言成 `AndroidLmkTask[]`（`invoke<…>` 只是类型声明），一个**不是数组**的应答（旧守护进程 / 半截应答）就会在对账里变成 `tasks.filter is not a function` —— 而对账是被 `void` 出去的 ⇒ **一次没人看见的未处理拒绝**，并且该函数自己承诺的守卫（"daemon down ≠ app dead，取不到就什么都不关"）当场失效；同一个对账的**坏元素**（`null` / 没有 `window_id`）也会让整轮炸掉 | 形如"读不到就不动手"的安全守卫会退化成"读不到就炸"，而炸点在一个 `void` 掉的 promise 里 ⇒ 用户看不到任何提示、窗口清理逻辑静默失效；同一类还存在于任何把宿主应答直接断言成数组/对象再 `map/filter` 的地方 | 3 | 3 | 4 | 36 | `lmk.ts`：`androidLmkTasks()` 用 `Array.isArray(raw) ? raw : null`（取不到就是 null，交给对账自己的守卫）；`staleLegacySurfaceLabels()` 的任务过滤加 `t &&`（坏元素只会让**更少**的面临时保留，方向保守）；测试用假 `__TAURI_INTERNALS__` 钉住"非数组 ⇒ 0 且一条 `wm_close` 都不发""坏元素 ⇒ 不抛" | 测试先行（1 例红 ⇒ 修 ⇒ 26→28 例绿）；`make cov` OK（96.64% = 13,741/14,219）；`lmk.test.ts` 覆盖关面/对账/事件驱动/定时对账（含 `stop()` 真的清掉定时器） |




### 2.5 Android 集成

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|---|-----------|-----------|
| F-AND-001 | 相机 use-after-free | 进程崩溃/帧损坏 | 4 | 3 | 3 | 36 | `CameraGlue` 串行化 + epoch 守卫 | `Nv21PackerTest` |
| F-AND-002 | LMK 误杀活跃应用 | 关键进程被杀 | 4 | 2 | 3 | 24 | `UninstallGuard`;`mirror_failed` warn | `lmk_reverse_drive.rs` |
| F-AND-003 | 真实电池读数缺失上报 0 | 虚假低电量告警 | 3 | 3 | 4 | 36 | `BatteryReading::chargingFrom()` UNKNOWN 省略 | `devcare::battery` |
| F-AND-004 | APK 安装超时误报 | 用户以为成功 | 3 | 2 | 2 | 12 | `AMOS_ANDROID_INSTALL_TIMEOUT=180s` 独立 | `android_test.rs` |
| F-AND-005 | JNI 调用阻塞 UI 线程 | ANR | 4 | 3 | 2 | 24 | `spawn_blocking` 五处 | 手动审计 |

### 2.6 真机设备 (amos-telephony / amos-radio / amos-sensor)

| ID | 失效模式 | 影响 | S | P | D | RPN | 当前缓解 | 测试保护 |
|----|----------|------|---|---|---|---|-----------|-----------|
| F-TEL-001 | 紧急号码(110/112/911)被录音 | 隐私违规 | 5 | 2 | 5 | 50 | `hard no-record rule for emergency` | `telephony::tests` |
| F-TEL-002 | 黑名单生效但 UI 显示"未生效" | 拦截假象 | 4 | 2 | 3 | 24 | `blocklistCheck` 实时提示 | `phone.svelte.test.ts` |
| F-RAD-001 | 飞行模式级联未完整 | 设备未真正断网 | 4 | 2 | 3 | 24 | `RadioManager` cascade + guard | `radio::tests` |
| F-SEN-001 | 传感器 mode 切换未生效 | 耗电/低质量 | 3 | 3 | 3 | 27 | `SensorManager` 显式 apply | `sensor::tests` |
| F-DEV-014 | BLE GATT 写后 read 缓存被读成"已写入"，但实际是前一次应答 | 设备状态分叉 | 3 | 3 | 3 | 27 | `ble::install_glue` 一次性注入 `JavaVM` + `Context`；读写命令走 `JValue` 严格类型，`pending_read` 仅由 Kotlin `onCharacteristicReadResult` 写入并 `take` 后即清空；`BleState.connected/address` 在 `ble_connect` 成功时原子更新；host fallback 永远 `Ok(false)` 不伪装 | `ble.test.ts` 8 例 + `cargo check -p amos-tauri --features android` |
| F-DEV-015 | NFC 写一条 NDEF 之后 UI 立即读，UI 看到的不是刚刚写的那条（tag dispatch 没启动 / 写完没刷新缓存） | 用户看到旧数据，误判"写失败" | 3 | 2 | 3 | 18 | `nfc_start_dispatch` 走 `NfcGlue.init(context)` + Kotlin `startForegroundDispatch`；`nfc_write_message` 把 `Vec<NdefRecord>` 转 `ArrayList<HashMap>` 后 JNI 调用，写完返回 `bytes_written`（payload hex 长度/2 之和）；`nfc_read_bytes` 拉 `readTagBytes()` 同步返回值 | `nfc.test.ts` 16 例 + `cargo check -p amos-tauri --features android` |
| F-DEV-016 | BiometricPrompt 在非用户手势上下文被触发 ⇒ Android 12+ 静默失败或抛 `SecurityException` | 看似成功的"指纹弹窗"其实是无意义的成功 | 4 | 2 | 4 | 32 | `biometric_authenticate` 是 `tauri::command`（必然用户手势链上的同步调用）；`BiometricGlue.authenticate` 直接 `BiometricPrompt.authenticate(promptInfo, callback)`，**绝不**在 `pending` 状态四舍五入成 `ok=true`（仅 `BiometricGlue.onBiometricResult` 把 Kotlin 真实回包写入 `BIOMETRIC_STATE` 并发 Tauri `biometric-result` 事件） | `biometric.test.ts` 14 例 + `cargo check -p amos-tauri --features android` |
| F-DEV-017 | 视频录制 metadata 进 `SensorHost` 但 snapshot 不返回（前端永远读不到已完成片段） | UI 看不到刚录的片段；用户在 Camera 里"录完即丢" | 2 | 4 | 3 | 24 | `sensor_host_record_video` 命令把 `HostVideoClip` 压入 `SensorHost.video_clips: Mutex<Vec<_>>`（环形 32 条上限），`snapshot()` 一并返回；`MAX_CLIP_BYTES = 4 GiB` 写前硬卡，溢出返 `Err` 不静默 | `cargo test -p amos-tauri --lib sensor_host::tests` + 4 GiB 负控 |
| F-DEV-018 | **一道「安装期自检」永远报错 ⇒ 它抓不到自己该抓的东西，还会把读到日志的人引向不存在的故障**（REQ-A427 真机取证）：`menu::verify_tree` 用 tauri 的 `Menu::get` 判断"声明的菜单 id 装上了没有"，而 `Menu::get` **不是树遍历**（`self.items()?.into_iter().find(...)`，只看根菜单的直接子项），本模块 11 个 id 全在六个子菜单里 ⇒ 检查对**正确**的菜单树答"全缺"。实测启动日志：`WARN amos::menu: the menu tree is missing declared items … missing=[menu.about, …, menu.enter-fullscreen]`，紧接着又是 `INFO native macOS menu bar installed`；同进程 AX 菜单栏实为 `Apple/Amos/File/Edit/View/Window/Help`，`File ▸ Close Window`（⌘W，`AXMenuItemCmdChar=W`）都在 | ①**该抓的抓不到**：REQ-A423 那类"声明处声明、安装处没有"（`menu.quit` 有处理器、有文档行、无菜单项）会与"正常的树"报**同一句话** ⇒ 恒为真的告警等于没有告警；②**误导**：下一个读日志的人（本轮：我，十分钟）会去查一个不存在的菜单缺陷；③反向也危险：有人为了"让告警消失"去动菜单构建 | 3 | 3 | 4 | 36 | 自己走树：`collect_item_ids` 递归进 `Submenu::items()`，建在最小 `MenuNode` trait 上（`MenuItemKind` 的实现 6 行；trait 存在是为了**可测**——muda 只能在主线程建菜单树，单测不在主线程，实测 panic `muda::MenuChild can only be created on the main thread`）；`verify_tree` 改为「走树得到的 id 集合」判定；测试把"只看根"的旧行为在内存树上复现 ⇒ `missing_ids` 答满 11 条，走树后为空 | `crates/amos-tauri/src/menu.rs` 的 `the_tree_check_descends_into_submenus`；`cargo test -p amos-tauri --lib menu::` **5/5**；`cargo test -p amos-tauri --lib` **521/521**；`node scripts/fmea-gen.mjs --check` |

| F-DEV-019 | **一条「给窗口命名」的命令给的是**另一个**窗口的名字 ⇒ 两个标题栏交换了身份**（REQ-A428 真机取证）：`wm_set_shell_title(app, title)` 不收调用者、无条件对 `LAUNCHER_LABEL` 执行 `set_title`（写它时只有一个壳窗口）；REQ-A416 之后每个 `wm_open` 窗口渲染自己的应用，`Shell.svelte` 的标题 `$effect` 在**每个**窗口里都会跑 ⇒ 应用窗口发来的"设置"落到启动器上。实测（`open Amos.app` + ⌘,）：AX 窗口列表 `Settings` 在 `236,69 1024×720`、`设置` 在 `0,29 1496×879` —— 按几何判定后者才是**桌面**窗口 | 桌面窗口被改名成刚打开的应用名（窗口菜单、Dock 的窗口列表、读屏都读到"设置"），而应用窗口自己留着按 label 生成的英文名 ⇒ 用户看到的标题栏**答错了"我在看什么"**；多窗口下每个新应用的标题都会覆盖桌面窗口的名字 | 3 | 3 | 3 | 27 | 命令收宿主注入的 `window: tauri::WebviewWindow`（`clipboard.rs` / `ime_*` 先例 REQ-A258），标题落回**调用者**；判定收敛为纯函数 `resolved_title(label, requested, configured)`：`Some(x)` ⇒ 规范化后给调用者；`None`（"这个表面就是壳本身"）⇒ 启动器恢复**配置里的**标题、应用窗口恢复 `app_window_title(label)`（建窗时同一个函数）；`resolve_caller_title` 返回 `(目标 label, 标题)`，因为 mock runtime 的 `set_title` 是空操作、`title()` 恒返空串 ⇒ 单测只能断言"决定"，这一半正是原缺陷错的地方 | `crates/amos-tauri/src/wm.rs` 的 `a_window_names_itself_and_never_the_launchers_title` + `the_title_rule_says_which_window_restores_what`；`cargo test -p amos-tauri --lib wm::tests::` **42/42**；`node scripts/tauri-args-scan.mjs` OK（`WebviewWindow` 属注入参数，前端契约不变）；`node scripts/fmea-gen.mjs --check` |
| F-DEV-020 | **「关闭窗口」的命令从来没有关掉过任何窗口：模型忘了它、命令回报成功、窗口还在屏幕上**（REQ-A430 真机取证）：`WmState::close` 先调 `close_core`（它**故意**删掉 `label ⇄ id` 映射，免得重启后 focus 到一个死 id），再由 `apply` 处理 `WmEvent::Closed` —— 而该分支正是**通过同一张映射**解析平台窗口（`label_for(id)`）⇒ 永远 `None` ⇒ `w.close()` 从未被调用。取证链：仪表化探针证明 ⌘W 确实到达应用窗口 WebView（`[probe] appwin key=w`）而窗口仍在；AX 直接点击 `File ▸ Close Window` 能触发处理器（三次都打印 `no app window held the OS focus`，与本条无关），说明菜单项自身没问题 | 用户按 ⌘W（或 Dock「退出」、Mission Control 关窗、LMK 收口）⇒ **什么都没发生**，而命令答"成功"；下一次 ⌘W 得到 `window 'settings' is not registered`（同样无人显示）⇒ "键坏了/菜单坏了/应用坏了"三种误诊都会出现（前两轮正是这么误诊的）；更危险的是 LMK 那条路径：容器收回了面、宿主以为已关、窗口仍在 | 4 | 3 | 2 | 24 | ①**解析放在状态半场之前**（新 `close_target<R: Runtime>`），关闭失败 `tracing::warn!` **上报**而非 `let _ =` 吞掉；②`apply` 的 `Closed` 分支改为空操作并写明"关闭只有一个地方做"（它此前是**职责重复**且必然失败的那一半）；③前端把 `host-refused` 作为新的死键原因（`refusedChordMessage`），"没有可作用窗口"与"宿主拒绝"两句不同的话 | `crates/amos-tauri/src/wm.rs` 的 `a_close_resolves_the_platform_window_before_the_state_forgets_it`；`cargo test -p amos-tauri --lib` **522/522**；`frontend-ts/src/__tests__/desktopSystemKeys.test.ts` **7/7**（含 host-refused）；**真机**：`fresh → ⌘, → 2 窗 → ⌘W → 1 窗`（窗口内 outcome 探针未出现＝窗口在 promise 落地前已销毁）；`node scripts/fmea-gen.mjs --check` |
| F-DEV-021 | **壳级「导航」观察器在每个窗口都跑 ⇒ 焦点一变，桌面应用窗口把自己导航回桌面**（REQ-A429 真机取证）：`Shell.svelte` 无条件挂 `startOsWakeHome({onHome: goHome})`、`startOsAutoOff({onSleep: lock})`、硬件键 `onNav`，而 REQ-A416 之后桌面形态的窗口**就是一个应用** ⇒ 实测：⌘, 开出设置窗口后一次普通焦点变化就让该窗口 `goHome()`，**应用窗口里渲染出桌面壳**（REQ-A416 的缺陷换个入口回来），标题也从"设置"变回英文 label 名（A427/A428 的症状走另一条路径） | 桌面形态下"应用窗口变成桌面"再次发生（这次由焦点/唤醒触发，与用户是否操作无关）；同理空闲会把它变成锁屏、硬件导航键会把它变成主屏/AI —— 每个都是"窗口不再显示它声明的那个应用" | 3 | 3 | 3 | 27 | 新 `lib/shellNav.ts::windowOwnsShellNav(form, isAppSurface)`（桌面 + app 表面 ⇒ 不拥有导航），`Shell.svelte` 的 `navigateSelf()` 在**触发时**判定（挂载时判定会答 "phone" ⇒ 守卫成死代码，注释写明）；四个导航回调全部经它 | `frontend-ts/src/lib/__tests__/shellNav.test.ts` **4/4**；`svelte-tests/shell.svelte.test.ts` **56/56**（新增：桌面应用窗口 wake 后仍是该应用 + 无 Dock；对照组：手机形态仍回桌面）；`node scripts/fmea-gen.mjs --check` |
| F-DEV-022 | **新建应用窗口的"头几秒"里 `⌘W` 没有任何人在听：窗口已在屏幕上，但键的两条路都落空**（REQ-A431 真机取证）：脚本循环实测——`⌘,` 后 **3 秒**按 `⌘W` ⇒ **3/6** 关掉，等待 **12 秒** ⇒ **4/4**。两段原因：**(a) 前端**：处理这些键的组件住在**挂载之后**的 Svelte 壳里（`DesktopAppWindow` → `modules/DesktopWindowKeys`），而窗口一建出来就已经显示（AX 里甚至排在最前）；**(b) 宿主**：同一时刻原生菜单也拿不到该键（只要壳在运行，这些 ⌘-键由 WebView 消费——`⌘Q`/`⌘N` 仍走菜单、`⌘W`/`⌘M`/`⌘,` 走 WebView 的对照即证据），而 `close_key_window` 只认平台"key 窗口"，新窗口恰恰"已在屏幕上、还没拿到 key" | 用户按下开窗的快捷键（⌘, / 点 Dock / Spotlight）后**立刻**按 ⌘W ⇒ 什么都没发生（且没有任何提示），再按一次才有效；同类：刚开窗时的 ⌘M/⌘H 同样落空。用户读到的结论往往是"这个键坏了"（前两轮正是这么误诊的） | 3 | 4 | 3 | 36 | ①**前端从第一帧就接管**：`lib/earlySystemKeys.ts` 由 `shell-entry.ts` 在挂载 `Shell` 前安装 capture 监听，只作用**本窗口自己的 app**（`#window=<label>`，绝不猜"焦点窗口"），`⌘,` 无需目标故任何窗口可用；挂载后 `handOverSystemKeys()` 交接（同一按键不会被处理两次）；②**宿主模型兜底**：`close_key_window` 平台答"没有 key 窗口"时回落到窗口管理器说聚焦的那个（不是猜：那是宿主自己的答案），启动器两条分支都永不为目标（F-SH-008），外部合成面明确不关并说明；判定收敛为纯函数 `close_target_label`；③`apply` 的 `set_focus()` 由 `let _ =` 改为**读回并上报**（"平台尚未确认焦点"正是这条缺陷的解释，此前被扔掉） | `frontend-ts/src/__tests__/earlySystemKeys.test.ts` **8/8**；`cargo test -p amos-tauri --lib menu::` **6/6**（+`the_menu_close_target_prefers_the_key_window_and_falls_back_to_the_model`）；**真机**：同一 3 秒序列 修前 **3/6** → 前端修复 **4/6** → 加宿主兜底 **6/6**；`node scripts/fmea-gen.mjs --check` |
| F-DEV-023 | **原生菜单里的条目只是个 emit 空壳 ⇒ 一个 macOS 应用的 About 项点击后什么都不出现**（REQ-A432，AX 逐项点菜单实测）：`show_about_dialog` 只做 `emit("show-about-dialog")`，而它自己的文档写着"frontend 没有消费者（没有 About 表面）"，那条事件还被 `scripts/tauri-event-allowlist.json` 明确登记为"Phase-2 桩"⇒ 处理器"成功"、日志有一行、屏幕上什么都没有 | 用户点 `Amos ▸ About Amos` ⇒ 无任何反馈（既不是"未实现"也不是"出错"，就是什么都没有）；同类形状：任何"发一个没人订阅的事件就算做完"的处理器（本仓的 `tauri-event-scan` 能报"发而无订阅"，但它被 allow-list 豁免后就变成了"有意的"） | 2 | 2 | 3 | 12 | 改用**平台自己的**面板 `orderFrontStandardAboutPanel(None)`（复用 `with_ns_app` 的主线程守卫）：名字/版本/图标全部来自 bundle 的 `Info.plist`，无自造 UI、无第二份产品名；**事件与 allow-list 条目一并删除**（`tauri-event-scan` 20 事件 / 3 豁免，无陈旧豁免） | 真机：点击后**窗口数 1→2**（面板）+ 日志 `About panel requested from menu`；`node scripts/tauri-event-scan.mjs` OK；`cargo test -p amos-tauri --lib` **524/524**；`node scripts/fmea-gen.mjs --check` |
| F-DEV-024 | **原始菜单把做不到的事显示成能做到：只有启动器时 `Close Window` / `Minimize` / `Zoom` / `Enter Full Screen` 可用，点了什么都不发生**（REQ-A433，AX 读 `enabled` 实测）：壳有意跳过启动器（F-SH-008）、启动器本身已是屏幕大小（Zoom 无处可 zoom）⇒ 四个条目点了都没反应，而**菜单里没有任何地方能解释原因**（F-SH-001 的形状发生在操作系统自己的菜单上，比页面内的死控件更难发现：没有 title、没有文案、没有 DOM 可查） | 用户看到"菜单说能用" ⇒ 点了没反应 ⇒ 归因为"应用坏了"（本仓前几轮正是这么误诊 ⌘W 的）；反过来，若有人为了"让它有反应"而让 Close 作用于启动器，就会关掉整个桌面（F-SH-008） | 3 | 3 | 3 | 27 | `menu::sync_window_items`：判据 `actionable_window_label`（平台 key 窗口 → 回落窗口管理器，与 A431 同源），在**安装完成**与**平台焦点事件**（`WindowEvent::Focused`）同步；`New Window` 不参与（不需要焦点窗口）；条目找不到 / `set_enabled` 失败都 `warn!` | 真机（AX 读回，三时刻）：只有启动器 ⇒ 四项 `false`、`New Window=true`；设置窗口聚焦 ⇒ 四项 `true`；⌘W 关掉 ⇒ 回 `false`；`cargo test -p amos-tauri --lib menu::` **7/7**（+`the_window_menu_items_are_declared_ids`）；`node scripts/fmea-gen.mjs --check` |
| F-DEV-025 | **新窗口在"还没有 JS"的几十~数百毫秒里夺走 OS key ⇒ 那段窗口里 ⌘W 无人应答（WebKit 吃掉、菜单也拿不到）**（REQ-A435，真机取证）：窗口一建出来就在屏幕上并**抢到 key**，而它自己的 WebView 还没执行任何脚本（前端钩子不存在）；同时因为 key 在新窗口上，WebKit 把 ⌘W 当作自己的键等效吞掉，原生菜单那条路也收不到（失败运行里 `focused the window after its page loaded` / `Close Window:` / `menu item activated` **一行都没有**） | 用户按下"开窗"（⌘,／点 Dock／Spotlight）后**立刻**按 ⌘W ⇒ 什么都不发生（第三次误诊为"⌘W 坏了"）；同类：那段窗口里 ⌘M / ⌘H 也落空 | 3 | 4 | 3 | 36 | **把聚焦推迟到页面加载**：`apply` 的 `FocusChanged` 在 `WmCore::page_loaded` 不含该 label 时**不调 `set_focus`**（debug 说明），改由窗口自己的 `on_page_load` 回调记录就绪并按模型焦点聚焦（读回 `is_focused()`、失败 `warn!`）；这段期间 key 留在**启动器**（它的 WebView 是活的）⇒ 由它按模型里的焦点窗口关闭新窗口；关闭时清掉该 label 的记录（重开是新窗口） | **真机**：同一脚本 3 秒序列 **3/6 → 6/6**；追踪 `focus deferred until the page loads (REQ-A435) label=settings` + `after cmd-W: windows=1`；`cargo test -p amos-tauri --lib` **525/525**；`node scripts/fmea-gen.mjs --check` |
| F-DEV-026 | **弹出菜单只有指针能用；而且同一条键盘规则"一处有、两处无"**（REQ-A436）：`DockContextMenu` / `DockOverflowMenu` / 桌面右键菜单 / 顶栏六个菜单面板都渲染 `role="menu"`，Escape 能关（Dock 的文档级监听），但**没有任何键在行之间移动焦点**；而 `DockGlobalContextMenu` **自带一份**实现（私有 `menuItems` 数组 + `currentFocusIndex` + 四键 switch）⇒ 键盘用户在其中两个菜单里**完全无法使用**，第三个菜单的规则还会与将来任何改动漂移（F-SH-005 形状：同一条规则两处实现） | 键盘用户（含读屏/无鼠标）打不开也用不了 Dock 的右键菜单；macOS 菜单是全程键盘可驱动的，"看起来是菜单、键盘走不进去"属 F-SH-001 家族 | 2 | 3 | 3 | 18 | 新 `lib/menuKeys.ts` 一处实现：`nextMenuIndex`（纯：上下环绕 / Home / End / 越界夹紧）、`menuItems`（只收**可用**的 `role^="menuitem"`：前缀以覆盖 `menuitemradio`）、`installMenuKeyboard`（打开聚焦第一行、走行、**Escape 交回本菜单 closer 并 stopPropagation**、Tab 不接管）；删掉 `DockGlobalContextMenu` 的私有实现，八处菜单接同一层（第八处是企业面板的导出下拉，REQ-A438 收掉的最后例外） | `bun test src/__tests__/menuKeys.test.ts` **11/11**；`vitest` 四文件 **131/131**（含 Dock 菜单键盘用例）；`unwired-scan` OK（新模块全部有生产调用点）；`node scripts/fmea-gen.mjs --check` |
| F-DEV-027 | **"自己的默认值，自己不知道"：壳默认中文，而硬编码的英文菜单栏是整屏唯一说英文的地方**（REQ-A437 真机实测）：`lib/i18n.currentLocale()` 在无持久值时返回 `zh`（壳的默认），`build_menu` 的标签却是英文**字面量**，两边各自表达"当前语言"这同一个事实却互不相干 ⇒ 界面全中文的用户顶上读 `File / Edit / View / Window / Help` | 用户看到"应用是英文做的/没本地化"（与 `wm_set_shell_title` 当年把中文标题写到启动器上同族：**同一个问题两处回答**）；对中文用户这直接决定第一印象 | 2 | 3 | 2 | 12 | `MenuLocale`（`parse()` 默认 **zh**、未知值回落）+ `MenuLabels` **结构体**两张表（中文措辞照 Apple 菜单词汇；产品名不译）⇒ "漏译文"是**编译错误**；`build_menu(app, labels)`；通道：`menu_set_locale` 命令（非 macOS 文档化 no-op）+ `locale.svelte.ts::setLocale` 调用并**读回宿主实际应用的语言**（不一致 `amosWarn`）；启动时 `stored_locale` 读共享存储 | **真机三状态**（AX 读回）：无键 ⇒ `文件/编辑/显示/窗口/帮助`；`en` ⇒ `File/Edit/View/Window/Help`；`zh` ⇒ 中文；`cargo test -p amos-tauri --lib` **526/526**（含"两表 16 组译文逐一不同 + 默认值"）；`cargo fmt --check` 0 diff；`vitest locale.svelte.test.ts` **5/5**；`node scripts/fmea-gen.mjs --check` |
| F-DEV-028 | **「全选」在两种意义上都不可用**（REQ-A439，真机两次取证）：①**文案**：Edit 菜单的六个预定义行由 AppKit 按**系统语言**生成，而壳默认中文 ⇒ 中文 UI 的 `编辑` 里读 `Undo / Redo / Cut / Copy / Paste / Select All`（Window 菜单的 `Minimize / Zoom` 同病）；②**⌘A**：加速键已在菜单项上注册（AX 读回 `AXMenuItemCmdChar=A`、`enabled=true`）却不触发，页面也收不到这个键（前端捕获监听器不被调用），⌘A+⌘C 的剪贴板为空；同一台机同一脚本里 TextEdit 的 ⌘A+⌘C 正常 ⇒ 故障域在 AmOS 的 WebView/菜单交互，不在系统；③**壳自己的 Edit 行**用 `window.dispatchEvent(new KeyboardEvent("keydown", {key:"a", metaKey:true}))` 假装"按了 ⌘A"——合成事件没有默认动作，实际什么都没做（F-SH-001 家族：看起来能用） | 文本字段里**无法用键盘全选**（复制/替换前的第一步）；菜单行与界面语言不一致会让用户以为没做本地化；壳菜单里那行点了没反应 | 2 | 3 | 3 | 18 | 行文案：六个 Edit 行 + 两个 Window 行改用 `*_with_text(l.<字段>)`（`MenuLabels` 新增 6 字段，中英各一份，Apple 中文词汇「拷贝」），`menu-i18n-scan.mjs` 的 R1 扩展到该形状；行为：新 `lib/editKeys.ts` 一处实现（`isSelectAllChord` 按 physical key 判、`selectAllInFocus` 聚焦字段选内容 / 否则选页面、`trackEditableFocus` 记住"AppKit 第一响应者"式的编辑焦点且**不被弹层行清掉**），壳自己的 Edit 行改调它；**⌘A 的平台通路未修**——三次改法（平台行 / 自己的 item + 加速键 / 去掉加速键）都实测无效，登记为缺口而不是假装完成 | `vitest`（`topbar-main-menu`/`desktop-app-window` 含 ⌘A 选中、无焦点不吞键、`AMOS_DESKTOP_SHORTCUTS=disabled` 仍可选）**全绿**；`bun test src/__tests__/editKeys.test.ts` **13/13**；`cargo test -p amos-tauri --lib` **526/526**；`node scripts/menu-i18n-scan.mjs`（23 字段）+ `--selftest` **7/7**；真机：编辑=撤销/重做/剪切/拷贝/粘贴/全选、窗口=最小化/缩放、点 `编辑 ▸ 全选` 选中字段；⌘A 仍无反应（记录在 `docs/mac-menu.md` §6 的实测表） |
| F-DEV-029 | **一个谁都拖不动的窗口**（REQ-A440，真机两次取证）：应用窗口自绘 28 px 标题栏（`APP_WINDOW_TITLEBAR_HEIGHT`），而 Tauri 的 overlay 窗口**只**在带 `data-tauri-drag-region` 的元素上按下才拖窗口 —— 本树该属性**一处都没有**（`draggable="true"` 只是 HTML 内容拖拽，注释却写着"让标题区可拖窗口"）；更隐蔽的是第二层：Tauri 注入的 `drag.js` 走的是 `plugin:window|start_dragging`，而 `capabilities/default.json` 只授了 `core:event:default` ⇒ 就算补上属性也**被 ACL 静默拒绝**。后果：1024×720 的窗口永远钉在 236,69，用户无法重新摆放任何应用窗口。同一轮还量到同族的另外两处"重复画系统的东西"：标题栏自绘三个红绿灯（AppKit 把**真**红绿灯画在同一条 28 px 带上 ⇒ 双影、且自绘那组永远点不到），以及窗口标题被画两遍（AppKit 在灯旁画一个 + 我们居中画一个，截图可见"文件 … 文件"） | 用户**无法移动窗口**（多窗口桌面最基本的操作，且 1024×720 在 1496×879 屏上占去大半）；重复的红绿灯/标题让人觉得界面"重影"，点自绘的红点关不掉窗口 | 3 | 3 | 2 | 18 | ① 标题栏逐元素补 `data-tauri-drag-region`（Tauri 判**事件目标**，子元素不继承）：条、标题、两侧留白；② `capabilities/default.json` 增 `core:window:allow-start-dragging` 并把"为什么"写进文件描述（**故意不授** `allow-internal-toggle-maximize`：双击缩放走 `wm_maximize`，保持窗口模型唯一真源，F-SH-005）；③ 删掉自绘红绿灯，改为等宽留白 `APP_WINDOW_TITLEBAR_INSET`（macOS 真实占位 14…66 px + 余量，唯一真源）；④ `wm.rs` 在 Overlay 下补 `hidden_title(true)`（macOS-gated；跨目标调用会 E0599——同 `title_bar_style` 的教训）；⑤ 测试从"红绿灯存在"改成"**不**自绘红绿灯 + 每个元素都是拖区"，并断言 `draggable` 不该回来 | 真机（CGEvent 真鼠标事件 + TextEdit 对照）：拖标题栏 236,69 → **376,159**（+140/+90 与拖距一致）、拖红绿灯留白也移动、双击仍只缩放一次（0,29,1496,880）、点真红点窗口关闭、全宽截图只剩**一组**红绿灯与**一个**居中标题；`AX title` 仍是"文件"（Mission Control/Dock 不失名字）；同一工具拖 TextEdit 208,126 → 328,196（证明工具本身可用）；`vitest desktop-window-titlebar` **7/7**；`cargo clippy -D warnings` 净 |
| F-DEV-030 | **用黄灯最小化过的窗口，AmOS 自己找不回来**（REQ-A441，真机两条独立见证）：macOS 的黄灯（`miniaturize:`）与 AppKit 自己的 ⌘M 会把窗口放进 Dock —— 那是**平台**的状态，而 `WmState` 只有 `Shown / Hidden / Closed`（`docs/multi-window.md` §2 的有意设计）⇒ 模型仍说 `Shown`，`wm_open("files")` 就只发一个 focus 事件，而 `set_focus()` / `show()` **都不会**还原一个最小化的窗口 ⇒ 用户按 ⌘N、点 AmOS 自己 Dock 上的图标都拿不回窗口，只有 macOS 真 Dock 的图标能救（而那个 Dock 可能是自动隐藏的） | 窗口被用户"缩小"一次就从 AmOS 自己的 UI 里消失（多窗口桌面上等于丢了一个窗口）；用户会以为应用坏了 | 3 | 3 | 2 | 18 | `WmState::reveal_window()`（问 `is_minimized()`，是则 `unminimize()` 再 show/focus），接在 **`WmEvent::Shown`** 与 **`WmEvent::FocusChanged`** 两处（⌘N 走后者——这正是漏掉的原因），并置于 REQ-A435 的"页面未加载不夺键"判定**之前**（可见性与键盘焦点是两件事）；**不给模型加第三态**（还原是唯一消化平台最小化的动作）；`docs/multi-window.md` §2 补一张"事实 ↔ 处置"表，包括"`wm_windows` 不报告最小化态 ⇒ Dock 菜单不区分隐藏/显示"这个**登记边界** | 真机：黄灯 → `AXMinimized=true`；**⌘N → `AXMinimized=false`**、窗口回到列表首位；宿主日志 `INFO amos::wm: restored a window the platform had minimized (REQ-A441) label=files`（证明走的是新路径）；`⌘W` 能关闭被最小化的窗口 ✓；手动 resize 在 hide→show 后保留（1104 ✓）；`cargo test -p amos-tauri --lib wm::` **44/44**；`clippy -D warnings` 净；`fmt --check` 0 diff |
| F-DEV-031 | **适航审计文档断言了它没有量过的事实**（REQ-A442）：`docs/POWER_OF_10.md` 规则 #10 的"现状"写「**无 `macro_rules!` 自定义宏**（搜索：`grep macro_rules crates/` **0 处生产代码**）」，而 2026-09-19 实测 `grep -rn 'macro_rules!' crates/*​/src` 有 **4 处**生产定义；同一文档规则 #7 的"165 处 / 35 文件""30 文件未审"也早已与树不符（实测 481 处 `let _`、棘轮 86 处 31 文件） | 适航语境下"审计结论"是给审查者看的事实,不是意图声明：一句未量测的"0 处"会让后来的人**放心地不查**（本轮就是读到它才去量的）；同类错误若发生在安全相关条目上（如"无 `unsafe`""无递归"）后果等同漏检 | 3 | 2 | 2 | 12 | ①**把断言变成门禁**：`scripts/rust-macro-scan.mjs` + 白名单（R1 未登记／R2 豁免过期／R3 占位理由／R4 重复条目,**无 `--update` 写入器**——豁免是人写下的决定），接入 `make lint`；②四处宏逐条登记理由（JNI 的 `&mut self` 借用顺序、Zenoh 无共同 handler trait）；③文档按实测更正（并把"棘轮只保证不新增 ≠ 逐处看过"写进表里）；④规则 #7 同步推进一个文件：`wm.rs` 6 处 `let _` 逐处过目，其中 `wm_split_swap` 的 `set_focus` 拒绝**真的被吞**⇒ 改 `tracing::warn!`，基线 5→4、total 87→86 | `node scripts/rust-macro-scan.mjs --selftest` **8/8**（含两条负控）+ 实跑 OK（4 宏全登记）；**真实树负控**：删掉白名单一条 ⇒ 立刻红在 `crates/amos-radio/src/android.rs:107`，还原后字节一致；`rust-discard-scan` 对 `wm.rs` 无 `[stale]`；`cargo test -p amos-tauri --lib` **526/526**；`clippy -D warnings` 净；`fmt --check` 0 diff；`fmea-gen --check` **139** |
| F-DEV-032 | **两道「看不见」的缺口**（REQ-A443）：①**调用流门禁只连同一文件内的函数** —— `rust-recursion-scan.mjs` 的互递归检测只在单文件里建图,于是"两个文件/两个 crate 各写一半的环"看不见;这个缺口不是猜的,是 `docs/POWER_OF_10.md` 残余风险表**自己登记**的那一行（「跨 crate 间接递归(扫描器只看 crate 内) → 扩展为 workspace-wide call graph」）。②**文档里的"验证命令"跑的不是它声称的东西**：该文档「自动校验」一节把五个 `-scan` 门禁的自测写成 `--self-test`,而这五个脚本只认 `--selftest`（`fmea-gen.mjs`/`env-doc-gen.mjs` 才是 `--self-test`）⇒ 那一行**静默跑了门禁而不是自测**,并打印一行干净的结论 | ①跨文件递归（尤其跨 crate）是"每一半都合规、合起来是递归"的形状,门禁答"无递归"而实际存在 ⇒ 与 F-DEV-001/031 同族的**漏检**；②一个**打错的验证命令不可能失败**的检查等于没有检查:读者照文档敲下命令、看到 `OK`,却从未运行过任何断言（F-DEV-005 的形状:仪器回答了另一个问题） | 2 | 2 | 3 | 12 | ①`scanWorkspace()`：在全部生产文件上建**一张** workspace 级调用图,只在该键**全 workspace 唯一**时加边（歧义即不加——假环比漏环更糟,REQ-A387 的教训）,且只报成员分布在 ≥2 个文件的环（文件内的环仍由原 pass 报,不重复）；②五个 `-scan` 脚本**拒绝不认识的 flag**（`exit 2` 并列出已知 flag）,文档同步改为 `--selftest`；③同一规则也用在规则 #7 本轮的文件上：`amos-translate` 的"消费者走了就停"从两处"只做一半"收成一处 `deliver()` | `node scripts/rust-recursion-scan.mjs --selftest` **23/23**（+5 条跨文件用例:两文件环 / 同文件不重复报 / 歧义键不加边 / 跨文件方法环 / 同名方法两类型不加边）；**真实树负控**：在 `crates/amos-clipboard/src/unix.rs` 与 `crates/amos-audio/src/ring.rs`（两个 crate）各插一个互相调用的探针 ⇒ **红**在 `crates/amos-audio/src/ring.rs:189 cross-file cycle`,还原后 sha256 一致、实跑 OK；五个脚本打 `--self-test` ⇒ **exit 2**；`cargo test -p amos-translate` **15 lib + 6 + 2 e2e 全绿**（含新的 `deliver` 三向断言）；`rust-discard-scan` 对 `amos-translate` 无 `[stale]` |
| F-DEV-033 | **守护进程「放弃」是一条静默路径：告警的接线从未被接上**（REQ-A444）。`amos-supervisor` 的告警缝 `Supervisor::with_alert_sink` 在全树**没有一个调用者**（`rust-unwired-scan` 报 `referenced nowhere`）⇒ `alert_sink` 永远是 `None`，`fire_alert` 在**两种** feature 组合下都是死代码：一个把重启预算耗尽的守护进程产出的是一个**没人能收到**的 P0；而 `alert_sink.rs` 的模块文档还写着「notifier 构建会把它接上」。**更糟的是同一分支没有任何日志** —— 重启尝试有 `warn!`、两处 spawn 失败有 `error!`，而「预算用尽、放弃」这一支什么都不打印 ⇒ 默认构建里守护进程就这样**静默地不再存在**（这正是 F-AI-012「告警未送达 ⇒ 操作员不知」的同一失效模式，换到 supervisor 侧）。 | 操作员完全不知道一个守护进程已经永远不在了：日志安静、UI 只少了它、没有任何告警。安全相关失效模式**必须**留下可见记录（DO-178C §5.4.1 —— 正是 `alert_sink.rs` 自己引用的那条），而这条路径当时连一条记录都没有 | 3 | 2 | 3 | 18 | ①**接线**：`src/bin/amos-supervisor.rs` 在 `feature = "notifier"` 时用 `NotifierSink` + **stderr fallback** 武装 sink（dispatcher 自己的「绝不静默丢 P0」保证：无通道时每个告警仍打一行），没有该 feature 时**明说** `daemon alerts: NOT wired …` 而不是安静；②**日志**：放弃分支补 `tracing::error!`（带 `daemon=` 与 `restarts=`），两种构建都有记录；③**文档纠错**：`alert_sink.rs` 模块头改写成实测状态（并写明为什么之前那句是假的）；④`scripts/supervisor-smoke.sh` 用 `--features notifier` 构建 supervisor（部署路径即武装路径） | `cargo test -p amos-supervisor --features notifier` **20 lib + 3 alert_wiring + 1 exhaustion + 1 restart_failure + 1 doc**；默认构建 **14 + 1 + 1 + 1**（`alert_wiring` 编译为空，证明门是 feature-gated）；两种配置 `clippy -D warnings` 均净；**负控**：把 `sink.fire(alert)` 改成 `let _ = &alert;` ⇒ `alert_wiring` **2/3 失败**（只断言「安静」的第 3 个仍过），还原后 sha256 一致、3/3 绿；**端到端（真实二进制）**：`target/debug/amos-supervisor run`（守护进程 `false`，预算 0）默认构建 ⇒ `daemon alerts: NOT wired …` + `ERROR … daemon exhausted its restart budget … daemon=boom restarts=0`；`--features notifier` 构建 ⇒ 同一 ERROR **外加** `[amos-notifier] no channels configured — P0 daemon.boom.crash_exhausted: …`；`rust-unwired-scan` 不再报该符号（12 → **11**，余下 11 条全在并发会话未跟踪的 crate） |
| F-DEV-034 | **「豁免是人写下的决定」这句只写在文档里：unwired 棘轮的豁免清单里没有任何理由**（REQ-A445）。`scripts/rust-unwired-baseline.json` 是一张**光秃秃的 26 条字符串**（`{ "values": [...] }`），而**门自己的失败文案**与 `docs/rust-unwired-audit.md` 都写着"理由记录在文档里" ⇒ ①`--update-baseline` **整文件重写**，所以一条新发现可以被**不写任何理由**地豁免，且**没有任何检查会失败**（"添加条目是记录在案的决定"这句断言**没有被强制**）；②一条重新被引用的条目只被**打印**一行提示，没有任何东西要求把它删掉 ⇒ 棘轮可以烂在那里；③文档里的"剩余 25 条"与清单里的 **26** 条也不一致。 | 一条**豁免**（"这个 `pub fn` 就是没人调用"）是读者**无法从代码反推**的东西：理由不在机器可读的位置、也没有门要求它存在 ⇒ 下一个人读到一张清单，只知道"被批准过"，不知道**为什么**，于是不会也不敢删。这正是 F-DEV-031/032 那一族（寄存器断言了自己没量过、没要求过的事）的第三次现形，而这次的对象是**门自己的豁免清单** | 2 | 2 | 3 | 12 | ①**每条豁免带上理由**：格式迁移为 `{entries:[{value,kind,why}]}`，26 条逐条写下 `why`，并归入四类 `kind`（`library-surface` / `inspection-api` / `host-missing` / `planned-feature`）；旧格式仍可读——读成"无理由"而不是崩溃；②**门检查清单自身**：新增 `baselineProblems()` —— **R1** 无真理由（缺失/太短/占位符）即红、**R2** 条目已重新被引用即红（棘轮必须缩、不许烂；此前只是一行提示）、**R3** 重复条目即红；③**写入器不再能悄悄豁免**：`--update-baseline` 保留已有理由、给新条目写 `UNREVIEWED` + R1 必然拒绝的占位符，并打印"其中 N 条没有真理由"；④文档按实测更正（"25" → **26**，并写明理由现在就在 JSON 里） | `node scripts/rust-unwired-scan.mjs --selftest` **21/21**（+8 条规则用例：真理由通过／缺失拒／占位拒／过短拒／R1 标记／R2 标记／R3 标记／旧 `values` 形状读成无理由）；实跑 **37 条 unwired、11 条 new（全在并发会话未跟踪的 `amos-config`/`amos-robot`）、清单 26 条、ratchet problems 0**；**真实树负控两条**：把一条 `why` 改成 `"TODO"` ⇒ `missing-reason` 红并点名；塞入一条不存在的符号 ⇒ `stale-entry` 红并点名 —— 两次还原后 **sha256 一致**；**写入器负控**：`--update-baseline` ⇒ 37 条中 26 条**保留原理由**、11 条标 `UNREVIEWED`，随即 R1 **红 11 次**（旧行为是写出 37 条无理由的 `values` 并 exit 0），还原后 sha256 一致 |
| F-DEV-035 | **切桌面只搬了账本、没搬窗口 —— 一条「报告了自己没有的效果」的路径**（REQ-A446）。`docs/SPACES_IMPLEMENTATION_PLAN.md` §一 承诺「每个桌面有独立的窗口集合」，而 `spaces_switch` 只做 `active_index = index`：面板把新桌面画成当前，**每个窗口却原封不动**；那一半的实现是三只**无人调用**的 helper（`active_space_windows` / `is_window_in_active_space` / `space_for_window`），在 unwired 棘轮里以 `planned-feature` 被豁免着 —— 于是"功能没做完"和"功能做完了"在用户眼里长得一模一样。 | 用户按 ⌃→ 切到"工作"桌面，看到的却是**同一个屏幕**：面板说换了桌面、窗口一个没动 ⇒ 功能看起来要么是坏的、要么"本来就这样"。这正是本仓反复出现的那一族：**命令报告了一个它没有产生的效果**（REQ-A430 的 `wm_close`、REQ-A428 的标题、REQ-A441 的还原） | 3 | 2 | 3 | 18 | ①**`SpaceManager::switch_plan()`**：把"一次切换要对窗口做什么"变成**纯函数**并写下三条语义 —— 未归档的窗口属于所有桌面（永不动）、Launcher 既不被隐藏也不被聚焦（藏了用户无处可点，聚焦了会从刚回来的应用手里抢键盘）、一个窗口被登记两次时由目标桌面拥有（`hide`/`show` 永不重叠）；②**`spaces_switch` 真的执行它**：先隐藏后显示，且管理器的锁在**任何平台调用之前**释放；③**失败不吞**：新增 `apply_switch_plan(hide, show, do_hide, do_focus) -> Vec<(label, why)>`，命令对每一条 `warn!` 点名（"账本动了、屏幕没动"是用户看得见、别人看不见的状态）；④棘轮**自动收紧**：两个 helper 已被引用 ⇒ R2 要求把它们从豁免清单删掉（26 → 24；`space_for_window` 留着并写明反方向没有调用者） | `cargo test -p amos-tauri --lib` **526 → 532 全绿**（`spaces` 一节 10 → 16：plan 的 4 条语义用例 + applier 的 2 条契约用例 —— 隐藏先于显示、一处失败不打断整轮、每个失败都带平台理由回来、空计划一次平台调用都不发）；`cargo clippy -p amos-tauri --lib -- -D warnings` 净；`cargo fmt -p amos-tauri -- --check` 0 diff；`tauri-args-scan` **OK**（注入的 `AppHandle`/`State` 不被当成线参：221 次调用全部可读、参数齐全）；`rust-unwired-scan` **R2 自动报出两条 stale 并已删除**（37 → 35 unwired、清单 26 → 24、ratchet problems 0）；`fmea-gen --check` **143** |
| F-DEV-036 | **「棘轮必须缩」在有些门里是真的、在另一些门里只是一行提示**（REQ-A447）。逐门量过一遍仓库里每一份 allow-list / baseline 之后：**7 道门**把"豁免过期"判成失败（`rust-macro-scan` R2、`rust-unwired-scan` R2、`unwired-script-scan`、`feature-surface-scan`、`crate-readme-scan`、`jni-exception-scan`、`jni-contract-scan`，`ci-drift-scan` 把 stale 当 finding），而 **5 道门**只打印：`unsafe-scan`、`rust-discard-scan`、`rust-recursion-scan`、`hot-loop-scan`，以及前端的 `unwired-scan` —— 最后这个更糟：它的注释写着"a stale entry is also reported, so the baseline cannot rot"，而**代码里根本没有算过 stale**（`grep stale` 只命中那行注释）。 | 一份"过期豁免"就是一张**没人撤销的许可证**：代码已经改好、条目还留着，下一个人读到的是"这里还有一个洞" —— 而洞已经填了。更坏的是**同一个仓库里两种规矩**：谁被提示、谁被拦住取决于当初写门的那一轮，读者无法从门的输出反推它到底管不管。这正是 F-DEV-031/034 那一族的第四次现形，而这次横跨**五道门**。 | 2 | 2 | 3 | 12 | ①五道门一律改成**失败**（与另外七道同一条规则、同一句文案 "the ratchet must shrink, not rot"）；②`unsafe-scan` 额外**检查基线自己的 `total`**：`declaredTotal` 必须等于今日实测的 per-file 之和（一个没人重量的数字，正坐在审查者最先读到的那一行），并抽出 `totalProblems()` 供 selftest 钉住；③前端 `unwired-scan` 新增**两类** stale：baseline 条目已重新接线、**以及 allow-list 条目已经不再命中任何洞**；④**修正每一处仍在说"reported"的策略文字**（3 份 baseline 的 `policy` 字段 + 4 个脚本头 + 2 份文档），否则就制造了本行正在审计的那种"文档与代码不符" | `node scripts/unsafe-scan.mjs --selftest` **16/16**（13 → 16，+3 条 `total` 规则用例）；四道 Rust 门实跑**全绿且 0 stale**；**真实树负控 5 条**（每条都整改后还原并 **sha256 一致**）：unsafe 塞入幽灵文件条目 ⇒ `FAIL — 1 stale baseline entr(ies)`、篡改 `total` ⇒ `FAIL … declares total 232, but the per-file counts sum to 233`、recursion 幽灵条目 ⇒ `FAIL`、hot-loop 幽灵条目 ⇒ `FAIL`、discard 把 translate 3 改回 6 ⇒ `FAIL … (baseline 6, now 3)`；前端门：新增规则**当场抓出一条真的腐烂** —— `src/lib/externalFiles.ts` 的 allow-list 豁免自 Files 屏幕接线后一直留着（**已删除**，门随即变绿，`unwired-allowlist.json` 7 条）；**并记录我自己的一次误报**：第一版 allow-list stale 用了**被该名单过滤过**的集合（`deadModules` / `findings` 都排除 allow-list 条目）⇒ 8 条伪阳性，改用未过滤集合（`unreachableModules` / `unmountedAll` / `unwiredKeys`）后才只剩那 1 条真货 |
| F-DEV-037 | **17 处「刻意丢弃」的安全性，靠的是生成文件里的一行，而全仓没有一处写过它**（REQ-A448）。`amos-tauri` 的 9 个文件里，8 个是同一族：进程级句柄 `APP` / `VM` / `GLUE` / `SINK` / `DEVICE` / `PUSHER` 用 `OnceLock::set` 只装一次，注释都写着"冗余的二次 attach 保留第一个"。**而这句话只在 Activity 不被重建时才成立** —— Activity 重建（旋转 / 改尺寸 / 语言 / UI 模式）会让 JNI 传来**新的** Kotlin 对象，`OnceLock::set` 把它丢掉 ⇒ 麦克风 / 剪贴板 / 媒体桥继续驱动**已死实例**。让"保留第一个"成立的**不在本仓的 Kotlin 里**，而在 `crates/amos-tauri/gen/`（git-ignored、由 Tauri 模板生成）那行 `android:configChanges` 上。 | 一次 `cargo tauri android init`（或一次 Tauri 模板升级）若不再写那些值，**没有任何门会红**，而设备上的后果是：麦克风权限请求打到死 Activity、容器里复制的文本再也进不了 AmOS 剪贴板、媒体桥指向旧的 Context —— 全都是"看起来正常、其实没生效" | 3 | 2 | 3 | 18 | ①把这句话写进后果最重的 4 处站点（`clipboard_glue` / `mic_permission` / `media` / `flashlight`）；②**把它变成门**：`scripts/android-glue-mirror.sh` 新增一段 —— 读生成的 manifest，要求 Activity 的 `configChanges` 含 8 个 token（`orientation` / `keyboardHidden` / `keyboard` / `screenSize` / `locale` / `smallestScreenSize` / `screenLayout` / `uiMode`），按 `|` 切分**精确比对**（子串匹配会让 `smallestScreenSize` 冒充 `screenSize`）；③**边界登记而不豁免**：`density` / `fontScale` 不在那张表里 ⇒ 改显示大小 / 字体缩放会**真的**重建 Activity，那条路径仍是已知缺口 | `bash scripts/android-glue-mirror.sh` exit 0（新增一行 + 17 个接线点仍全过）；**负控**：从生成的 manifest 删掉 `screenSize`、**保留** `smallestScreenSize` ⇒ 立刻红并精确点名 `screenSize`（证明判据是 token 而不是子串），还原后 **sha256 一致**；`cargo check -p amos-tauri --features android` 干净；`cargo fmt -p amos-tauri -- --check` 0 diff；`rust-discard-scan` 对该批 9 个文件无 `[stale]` |
| F-DEV-038 | **一个功能的最后一半「没有 UI」：命令在桥上、单测都过，而没有任何调用者**（REQ-A449）。`spaces_move_window` 从 REQ-A389 起就在桥上：Rust 命令注册、TS 导出、桥接契约单测全绿 —— **而全仓没有一处调用它**，前端 `unwired-baseline.json` 里还把它**当成豁免挂着**。于是：只有手工从 shell 归档过的窗口才会跟着桌面切换走（REQ-A446 那半也一样），而 `SpacesPanel` 上没有任何地方能归档一个窗口 ⇒ 用户看到的"虚拟桌面"是**能建、能切、能重命名，但窗口永远不属于任何桌面**。 | 功能在用户眼里要么"坏了"要么"根本不存在"：能建桌面、能切换，但窗口不会跟着走 —— 而这是整个功能的**目的**。更坏的是**门知道**：它在那份豁免清单里躺着，谁也没把它当回事。这正是 `rust-unwired-scan` 文档里那个种子缺陷（`SessionManager::get_or_create`：被文档承诺、被单测覆盖、零调用者）的**前端版本** | 3 | 3 | 2 | 18 | ①`SpacesPanel` 新增「窗口归档」一节：列 `wm_windows` 里**可归档**的窗口、每个窗口一个下拉指派到桌面；②规则收进纯函数 `fileableWindows()`（只收 `kind === "App"` 且非 `external`；Launcher 是桌面本身、System 表面不属于任何桌面、外部合成面没有我们的 `WebviewWindow`），放在 `lib/spaces.ts` 与 `prevSpaceIndex`/`newSpaceName` 同一处并有单测；③归档后**重新应用当前桌面**（`switchSpace(currentIndex)`），可见性规则仍只在宿主的 `switch_plan` 里，面板不自己判断；④三个状态都如实显示（读不到窗口列表 / 没有窗口 / 有列表），文案走 i18n；⑤棘轮自动收紧：该导出已被引用 ⇒ 从前端 unwired 基线的 116 条里**删除**（115） | `bun test src/lib/__tests__/spaces.test.ts` **18 pass**（+4 条 `fileableWindows`：只留 App／Launcher 永不出现／external 不可归档／不改入参）；`bun run test:svelte svelte-tests/spaces.svelte.test.ts` **7 pass**（+2 条：UI 与"规则仍在宿主侧"、"新文案没有字面量进 markup"）；**`bun run check` 整体 exit 0**（test + tsc + svelte-check 0 error + unwired/i18n/store/write/a11y/hover/lifetime/reactfree/testreach/idgen/sentinel/covgate 全过）；`i18n-scan` 七条判据全 OK（含"每个 `t(…)` 引用都能被字典回答且参数齐全"）；`a11y-scan` exit 0 |
| F-DEV-039 | **一个功能的「初始状态」是单行道：能归档、不能取消归档**（REQ-A450）。`move_window_to_space` 只能在桌面**之间**搬窗口，而"没有被任何桌面归档"（⇒ 每个桌面都可见）是每个窗口的**初始状态** —— 于是上一轮刚接上的归档 UI 里，一个**点错了**的窗口只能被搬到另一个桌面，**永远回不到起点**；面板也只能把它画成一个 `disabled` 的占位项（"如实显示"了一个不可达的状态）。换句话说：功能的起点不可达，用户唯一的退路是**删掉那个桌面**（而如果它是最后一个桌面，连这条路都没有）。 | 用户一次误操作换来一个再也回不去的状态：窗口被钉在某个桌面上，每次切走都看不见它，只能靠"再建一个桌面再搬过去"绕。"能进去、出不来"的功能会被用户读成**坏了**，而它的原因不是渲染、不是权限，是**缺少一条反向命令** | 2 | 3 | 2 | 12 | ①**Host**：`SpaceManager::unfile_window()` —— 把标签从**所有**桌面移出，返回"账本是否真的变了"，因而**幂等**（取消一个没归档过的窗口不是错误，调用者要的是终态而终态已成立，此时连一次平台调用都不发）；②**Command**：`spaces_unfile_window`（注册进 `generate_handler!`；管理器锁在任何平台调用前释放）；③**一条计划表达不了的事要说清楚**：取消归档后窗口"属于每个桌面"，而 `switch_plan` 只知道**目标桌面拥有什么** ⇒ 窗口不在任何一份名单里，所以命令**显式 `wm.focus` 把它显示出来**（它必须**现在**可见），失败则 `warn!` 点名；④**面板**：`<option value="">` 由 `disabled` 改为**真动作**（走 `unfileWindow`），文案与提示同步（zh/en） | `cargo test -p amos-tauri --lib` **535 pass**（+3：`unfiling_takes_a_window_out_of_every_space`（含"同时登记在两个桌面"的退化态）／`unfiling_a_window_nobody_filed_changes_nothing`（幂等 + 账本不动）／`an_unfiled_window_is_never_named_by_a_switch_plan`（"属于每个桌面"这条性质））；`cargo clippy -p amos-tauri --lib -- -D warnings` 净；`cargo fmt -p amos-tauri -- --check` 0 diff；`tauri-command-scan` **261 条命令 OK**（新命令必须被屏调用 —— 面板调用它，因此不需要豁免）；`tauri-args-scan` **222 次 invoke OK**；`bun test spaces.test.ts` **18 pass**、`bun run test:svelte svelte-tests/spaces.svelte.test.ts` **7 pass**；**`bun run check` 整体 exit 0** |
| F-DEV-040 | **同一个缺陷修了一次就没回头：兄弟守护进程还留着它**（REQ-A451）。REQ-A443 把 `amos-translate` 的 socket 清理从 `let _ =` 改成会报告（"清理失败坏的是**下一次**启动"）；而 `crates/amos-ai/src/server.rs` 里**一模一样的行**继续丢弃它的结果 —— 更刺眼的是**它上面那句注释**写着 "Remove the socket file so a stale one never blocks the next bind"：**赌注被写下过，然后被丢掉**。`serve_with_sinks` 绑定的正是这条路径，`UnixListener::bind` 对仍存在的文件答 `EADDRINUSE` ⇒ 清理失败坏的是下一次启动，这一次什么都不说。 | 一个"已修"的缺陷可以在**同一个仓里**以相同形状继续存在，因为修复落在**发现它的那个文件**，而不是**它成立的那个形状**上。逐文件的规则 #7 审计存在的理由就是它 —— 而这份清单里"已审"曾经只是**按 crate 的散文**。 | 2 | 3 | 3 | 18 | ①`remove_socket()` 抽出并**报告**失败（`warn!` 点名路径与平台理由）；②测试**量出后果**而不是断言日志：UDS 路径在 listener 之后仍在磁盘上、清理真的删掉它、而**残留的路径确实让下一次 `bind` 答 `AddrInUse`**（本 crate 没有捕获 subscriber 的测试夹具，与其造一个，不如量因果）；③基线 15 → 14、`total` 83 → 82（棘轮收紧） | `cargo test -p amos-ai --lib` **322 pass**（+1 `a_socket_that_survives_its_cleanup_blocks_the_next_bind`）；`cargo clippy -p amos-ai --lib -- -D warnings` 净；`cargo fmt -p amos-ai -- --check` 0 diff；`rust-discard-scan` 对该文件报 `[stale]` 后基线已随之收紧 |
| F-DEV-041 | **量表自己会撒谎：一次构建失败会让「丢弃」这道门少量，而它把少量的结果当成测量**（REQ-A452）。`clippyPass` 的兜底是「只要输出里有 `compiler-message` 就当成功」—— 而一次**编译失败**同样会产出 `compiler-message`（那些 error 本身）⇒ 没编译到的 crate 不贡献任何诊断 ⇒ 计数**静默变少**。REQ-A447 之后这条路的症状更糟：少掉的计数被渲染成**一堆 `stale` 基线条目**，也就是**"别人已经做过的活"**，指向没人动过的文件（实测：并发会话的 crate 编译失败时，`blocklist`/`buttons`/`clipboard_glue` 等四个文件"看起来被接上了"，而下一次构建正常的运行一条都不报）。 | "0 个发现"和"我没能看"**渲染成同一个样子**（F-DEV-005 那一族），而这里更坏：它不只是沉默，而是**把一个错误归因泼到没人动过的文件上**，读的人会去找那些文件的作者。 | 3 | 2 | 3 | 18 | ①`firstCompilerError()` 从 clippy JSON 流里取第一条 `level: "error"` 并带上主 span；②`clippyPass` 在非零退出且存在 error 时**拒绝交回计数**，抛出「could not measure: … failed to build / first error: … (file:line)」；③`main()` 捕获它并**当作门自身的失败**打印（exit 1），而不是打一坨堆栈；④selftest 从 23 → **27** 断言，四种形状（有 error／只有 warning／cargo 噪音／空流）都被钉住 | `node scripts/rust-discard-scan.mjs --selftest` **27/27**；**实跑**：并发会话的 `amos-robot/src/slam/ekf.rs` 当前编译失败 ⇒ 门输出 `could not measure … first error: this operation will always return zero … (crates/amos-robot/src/slam/ekf.rs:381)` 并 **exit 1**（旧行为：把这些文件的计数按"没见到"处理，然后报 stale）；`node --check` 语法 OK |
| F-DEV-042 | **一条重构把豁免分裂成两条输出 —— 而两条是同一处编辑**（REQ-A454，先跑门禁发现的）：`scripts/blocking-async-allowlist.json` 按 `file::fn` 键控，键写的是 `crates/amos-ai/src/server.rs::serve_with_log_sink`；`serve` 被拆成三层包装 + `serve_with_sinks_full`（REQ-A451 又把关停那一半搬进同步的 `remove_socket()`）之后，**持有阻塞站点的那一层换了名字** ⇒ 门同时打印「`server.rs` 的 `std::fs` 站点在 `async fn serve_with_sinks_full()` 里、未被豁免」（FAIL）与「`…::serve_with_log_sink` 已陈旧」（stale）。后果：读者只修一条**仍然是红的**；更坏的是**理由本身过期了**（旧理由一半在讲"关停时删 socket"，而那半已经不在这个函数里）⇒ 照旧理由续用，等于**给一个函数续一张它已不再适用的许可证** | 一条豁免要么覆盖站点、要么不存在；"旧键 + 新站点"这种**半生效**状态会让下一轮把一处原因读成两件事，或把过期的理由当成现状（F-DEV-031/034/036 那一族的又一次现形） | 3 | 3 | 3 | 27 | ①键改到**当前持有站点**的函数（`serve_with_sinks_full`），理由**按当前函数体重写**：只剩 bind 之后的 0700 hardening（在 `UnixListener::bind` 之后、accept 任务之前 ⇒ 不可能拖慢请求）；②policy 文字写明"一个原因可以产生两行输出、修法只有一处编辑"；③理由里点名 REQ-A451 搬走了那一半，免得下一个读者以为它被漏掉 | `node scripts/blocking-in-async-scan.mjs` **OK / exit 0 / 无 stale**、`--selftest` **11/11**；`node --check` 语法 OK；**负控（构造 + 逐字节还原 `sha256sum -c`）**：把键改回 `serve_with_log_sink` ⇒ 门同时打印站点与 stale 并 **exit 1** |
| F-DEV-043 | **一个 feature 没有任何步骤编译它**（REQ-A454）：`amos-ai/notifier`（把 `src/notifier_bridge/` 拉进来的告警分页接线）默认关闭，好让发给设备的守护进程保持传递依赖最小；而 `make lint` 那组 feature 步骤里**没有它的位置** ⇒ cargo 会**静默跳过** `required-features` 未满足的 target，于是那段代码**不被任何一条门禁编译**（门自己的输出：`never enabled: amos-ai/notifier`） | P0 路径（告警分页）可以在**每一道门都绿**的情况下腐烂：下一次真正需要它时才暴露编译不过或行为已变 | 3 | 3 | 3 | 27 | `make lint` 新增一步 `cargo clippy -p amos-ai --features notifier -- -D warnings`（**加之前先量过 EXIT=0**），注释写明"默认关闭 ⇒ 必须有自己的一步，否则这段能在全绿下腐烂" | `node scripts/feature-surface-scan.mjs` **OK（46 个非默认 feature 全部有步骤；159 条 cargo 调用被读）**、`--selftest` **11/11**；`cargo clippy -p amos-ai --features notifier -- -D warnings` **EXIT=0**；**负控（逐字节还原）**：删掉那一步 ⇒ `never enabled: amos-ai/notifier` + FAIL |
| F-DEV-044 | **一个脚本没有任何东西运行它**（REQ-A454）：`scripts/diagnose-android-usb.sh` —— 7 步**只读**的 macOS 真机 USB 诊断，在树里、自己的 `--help` 写得很清楚，而 Makefile / `.github/` / 每一个可达脚本都不提它 ⇒ `unwired-script-scan` 报它是死重量。后果：`adb devices` 为空时，本仓**有**这个工具而使用者找不到它（同一份文档里 §1 之后的每一步都假设设备已经出现在列表里） | 真机 bring-up 卡在**第一步**（主机看不见设备），而唯一的诊断工具没有任何入口 —— "能力存在"与"能力可用"是两件事（F-DEV-005 同族） | 2 | 3 | 3 | 18 | ①接 `make android-usb`（`.PHONY` 同步；`ARGS=--fix` 透传，且只有它会重启 adb —— 脚本本身不改任何状态）；②`docs/device-bringup-checklist.md` 新增 §0.1「设备看不见？先跑只读诊断」，把"看到什么"与"所以该怎么办"分开写 | `node scripts/unwired-script-scan.mjs` **OK（89/89 可达）**；`node scripts/phony-target-scan.mjs` **OK（49↔49）**、`make-target-doc-scan` **OK**；`make -n android-usb ARGS=--fix` ⇒ `bash scripts/diagnose-android-usb.sh --fix`；`bash -n` 干净；**负控（逐字节还原）**：把 recipe 换成 `true`、**注释留着** ⇒ `unwired: scripts/diagnose-android-usb.sh` + FAIL（同时实测了该门"注释不算边"的声明） |













---

## 3. 跨功能风险点

### 3.1 资源/内存边界 (NASA Power of 10 #2)

| 模块 | 已建立边界 | 测试 |
|------|------------|------|
| `amos-ai::pool` | `NonZeroUsize` 容量 + RAII permit | 12 任务并发 |
| `amos-ai::cache` | 32 条/300s/256 KiB | 命中/过期/LRU/超限 |
| `amos-ai::session` | 100 条上限 | 单测 |
| `amos-link::broker` | 有界 slot + `Closed` 类型化 | 锁中毒负控 |
| `amos-sms::trash` | 256 FIFO | 单测 |
| 前端 `AiApp` | `capTail(CHAT_MSG_CAP=200)` | `bounded.test.ts` |
| 前端 `InterpApp` | `capTail(SEG_CAP=200)` | 同上 |
| 前端 `notifyStore` | `NOTIF_CAP=100` | 同上 |

### 3.2 失败可见性 (Power of 10 #7)

| 模块 | 失败处理 | 测试 |
|------|----------|------|
| `backend.ts` invoke | 永不 reject,记诊断账本 | `backend.test.ts` |
| `amos-ai::server` | `Status` 类型化(InvalidArg/Unauth/Internal) | 各 e2e |
| `amos-ai::governor_service` | `mirror_failed` warn | `lmk_reverse_drive.rs` |
| 前端 `uiFailures` | 全局未捕获观察者 | `uiFailures.test.ts` |

### 3.3 静态约束 (P0-1)

| 维度 | 门禁 |
|------|------|
| 生产代码禁 `unwrap/expect/panic` | `scripts/rust-panic-scan.mjs` (39 crate 全部通过) |
| 禁 `unsafe` 无 SAFETY 注释 | `scripts/unsafe-scan.mjs` |
| 禁 `std Mutex` 跨 `.await` | `scripts/lock-across-await-scan.mjs` |
| 禁 `std::fs` 在 `async fn` 内 | `scripts/blocking-in-async-scan.mjs` |
| 禁递归(含互递归) | `scripts/rust-recursion-scan.mjs` |
| 禁 must-use 静默丢弃 | `scripts/rust-discard-scan.mjs` |
| 禁 hot spin loop | `scripts/hot-loop-scan.mjs` |

---

## 4. 残余风险(已识别且接受)

> 这些风险**当前未缓解**,**已文档化**为"已知缺口"。**任何改动必须重新评估**。
>
> 接受人必须非空;`fmea-gen.mjs --emit-residual` 会机读校验空接受人**(含 `TBD` / `(TBD)` 后缀)**。
>
> **关于本表的签字模型(诚实声明)**:AmOS 是研究/原型 OS、未做 DAL 审定(见 §6),本表
> 当前由单一开发者 (`arkSong`) 同时承担 safety / ai / security / android 四类 role 的
> 残余风险签字。该做法**不符合职责分离**(理想的合规模型是各 role 由独立负责人分别复核),
> 但在无独立审查员的现状下,这是使本表"有人签字、避免空挂"的可执行方案。**一旦项目进入任
> 何受监管/审定路径,本节必须按 role 拆分并补独立签字。**

| 残余风险 | 接受理由 | 跟进 | 接受人 | 日期 |
|----------|----------|------|--------|------|
| F-LK-002(漏报急停)在异构板上未端到端验证 | 单元测充分,真机集成属设备 bring-up | 真机验收(runbook) | arkSong (safety) | 2026-09-15 |
| F-DA-005(AI 历史)在真机 SMS 已读回执上未联调 | 设备 SMS 由系统侧负责,本服务只读自身模型 | 设备 bring-up | arkSong (ai) | 2026-09-15 |
| F-AI-013(速率限制被旁路)在恶意客户端下可能放大 | 安全层是 best-effort | 加固留给后续 P2 | arkSong (security) | 2026-09-15 |
| F-AND-005(JNI 阻塞)在真机慢路径下未实测 | 代码侧全走 `spawn_blocking`,实测属设备 | 设备 bring-up | arkSong (android) | 2026-09-15 |

---

## 5. 自动生成说明

- 本表由 `scripts/fmea-gen.mjs` 从 **代码与测试**双源扫描生成
- 任何新增 P0/P1 路径应同步在表中登记
- 任何 RPN ≥ 100 项必须由独立验证人复核
- 残余风险表 (§4) 中的项目必须**有书面接受签字**才能进入审定;`--emit-residual` 会机读校验
- 三个模式:
  - `node scripts/fmea-gen.mjs --check`         文档 ↔ inventory ↔ 代码三方一致性
  - `node scripts/fmea-gen.mjs --emit-json`     机器可读 inventory(供仪表盘消费)
  - `node scripts/fmea-gen.mjs --emit-residual` 残余风险签字状态

---

## 6. 与 DO-178C DAL 的对应

> AmOS 是**研究/原型 OS**,**未**按任何 DAL 审定。本节用于映射关系,不可作审定证据。

| DO-178C DAL | 适用系统 | AmOS 当前对应 |
|-------------|----------|---------------|
| DAL A | 灾难性失效 | F-LK-002、F-TEL-001、F-AI-010/011 — **未达 DAL A 审定** |
| DAL B | 严重/危险 | F-DA-004、F-AND-002 — 已建门禁,**仍需独立验证** |
| DAL C | 重大 | F-LK-007、F-AI-001 — 单元覆盖充分 |
| DAL D | 轻微 | 多数 S3 项 |
| DAL E | 无安全影响 | S1-S2 项 |

**结论**:AmOS **不申请**任何 DAL 审定。文档顶部"⚠️ 非审定软件"声明强制保留。
