# Amos — 需求 → 代码 → 测试 可追溯矩阵（轻量 DO-178C 风格）

> 目的：把“功能/非功能需求”与“实现文件”和“验证（单测/命令）”显式对应，便于任何一次改动可回查它覆盖了哪条需求、由哪条测试保护。
> 约定：`REQ-<编号>`。矩阵分两类——**A 应用/OS 功能**、**B 稳健性与安全（对应审计 §8 处置）**。
> 状态：✅ 已实现并有测试保护；🟡 已实现、测试待补；⬜ 待办。

---

## A. 系统 UI / 应用功能

| 需求 ID | 描述 | 设计 / 代码 | 验证（测试/命令） | 状态 |
|---|---|---|---|---|
| REQ-A1 | 主屏布局可分页/dock/隐藏，可拖拽排序并持久化 | `lib/amosStore.ts`（`getLayout/moveBefore/hideFromHome/…`）、`HomeDock.tsx`、`EditHome.tsx` | `__tests__/amosStore.test.ts`、`__tests__/dock-drag.test.tsx` | ✅ |
| REQ-A2 | 应用注册表：图标→组件一一对应；未知 id 有占位 | `apps.tsx`（`APPS`/`COMPONENTS`/`AppComponent`） | `__tests__/app-render.test.tsx`（SSR 逐个渲染不抛错） | ✅ |
| REQ-A3 | 主题（浅/深/自动）与 i18n（中/英）本地化 | `theme/`、`i18n/` | `theme.test.ts`、`i18n.test.ts`（键一致性） | ✅ |
| REQ-A4 | 备忘录：增删 + **编辑**已有便签 | `lib/notes.ts`（`editNote`）、`apps.tsx` Notes | `notes.test.ts` | ✅ |
| REQ-A5 | 信息：收发（含**单条删除**与整段清空） | `lib/messages.ts`、`CommsApps.tsx` | `trio.test.ts`（messages 段） | ✅ |
| REQ-A6 | 电话：拨号（退格/清空/**号长上限**） | `lib/phone.ts`、`CommsApps.tsx` | `trio.test.ts`（phone 段） | ✅ |
| REQ-A7 | 音乐：播放/上首/下首、**删除曲目**、空歌单不崩 | `lib/music.ts`（`removeTrack/nextIndexAfterRemoval`）、`CommsApps.tsx` | `trio.test.ts`（music 段） | ✅ |
| REQ-A8 | 相机：实时取景/拍照，**权限被拒可重试**、无摄像头降级 | `components/CameraApp.tsx` | `__tests__/camera.test.tsx` | ✅ |
| REQ-A9 | 地图：城市搜索/定位/缩放 + **拖拽平移与方向键** | `lib/maps.ts`（`tileToLatLon/panTiles/shiftCenter`）、`MapsApp.tsx` | `__tests__/maps.test.ts` | ✅ |
| REQ-A10 | 网络状态**响应式**（地图等据此降级） | `lib/useOnline.ts`、`MapsApp.tsx` | `__tests__/useOnline.test.tsx` | ✅ |
| REQ-A11 | 长会话（AI 消息/同传译文）**有界**、持久化同样封顶 | `lib/bounded.ts`、`components/BackendApps.tsx` | `__tests__/bounded.test.ts` | ✅ |
| REQ-A12 | 文件管理器：增删改移、面包屑、循环防护 | `lib/files.ts`、`components/FilesApp.tsx` | `__tests__/files.test.ts` | ✅ |
| REQ-A13 | 锁屏 / 最近使用 / 搜索 / 通知中心焦点管理 | `SystemPanels.tsx`、`useFocusTrap.ts` | `focus-trap/panels-focus/lock` 等 | ✅ |
| REQ-A14 | 备忘录进阶：搜索 / 归档与“最近删除”(恢复) / 置顶 / **字数统计** / **任务清单(勾选)** | `lib/notes.ts`（`searchNotes/setNoteState/orderPinned/noteStats/tasksOf/toggleTaskInText`）、`apps.tsx` Notes | `notes.test.ts` | ✅ |
| REQ-A15 | 相册进阶：多选批量删除 / 查看器(前后·方向键) / 幻灯片 / 设为壁纸 / **收藏(♥)+筛选** / **分享(复制摘要)** | `lib/photos.ts`（`removePhotos/neighborOf/toggleFav/favsOf/shareCaption`）、`apps.tsx` Photos | `photos.test.ts`、`photos-keys.test.tsx` | ✅ |
| REQ-A16 | 时钟进阶：世界城市 / 秒表 / 倒计时 / **闹钟**(再响·铃声·每周重复·修复同分钟复响) | `lib/time.ts`（`stopwatch/timer/alarm` reducer、`alarmKey/dayAllowed/…`）、`apps.tsx`（Clock/StopwatchCard） | `time.test.ts`、`clock.test.tsx` | ✅ |
| REQ-A17 | 计算器：**历史记录** / **物理键盘输入** | `lib/calculator.ts`（`calcEntry/addHistory/calcFromKey`）、`apps.tsx` Calculator | `calculator.test.ts`、`calculator-dom.test.tsx` | ✅ |
| REQ-A18 | 音乐进阶：删除 / **循环模式** / **进度拖拽(可键盘)** / **同步歌词** | `lib/music.ts`（`nextIndex/pctProgress/seekSeconds/DEMO_LYRICS/lyricIndex`）、`CommsApps.tsx` | `trio.test.ts`（music 段） | ✅ |
| REQ-A19 | 文件进阶：收藏 / 最近 / 搜索·排序 / **多选(删除·全选·移动)** | `lib/files.ts`（`deleteEntries/moveEntries/toggleFav/recentFiles/searchFiles`）、`FilesApp.tsx` | `files.test.ts`、`files-select.test.tsx` | ✅ |
| REQ-A20 | 信息进阶：日分组 / 已读·未读 / 左滑删 / **引用回复** | `lib/messages.ts`（`appendQuote/markRead/…`）、`CommsApps.tsx` | `trio.test.ts`（messages 段） | ✅ |
| REQ-A21 | 天气 ℃/℉ + **多城市切换**；设置 **iCloud 云开关+本地备份快照** | `lib/weather.ts`（`adjustForecast/WEATHER_CITIES`）、`lib/cloud.ts`（`readCloud/snapshotStores`）、`apps.tsx` | `weather.test.ts`、`settings.test.ts` | ✅ |
| REQ-A22 | AI 推理后端 **本地/云端一键切换**（DeepSeek 预设）+ Settings 显示当前后端 | `lib/providers.ts`（`readAiConfig/setAiConfig/envFor`）、Settings“AI 推理后端”卡、Tauri `ai_backend_switch`（`scripts/ai-backend.sh`） | `providers.test.ts`；live `chat_once`/`translate_once` | ✅ |
| REQ-A23 | 云端 key **不明文进前端**：0600 落盘 + 恢复复用 | `setAiConfig` 不存 `apiKey`；`ai_bridge` 写 `~/.amos/ai.key`(0600)；`ai-backend.sh` 持久化/回读 | `providers.test.ts`（key 不落设置）；`ls -l` 0600 实证 | ✅ |
| REQ-A24 | 生产后端编排与监管：`run-backends.sh` / `supervise-backends.sh`（supervisor 崩溃自动拉起·SIGUSR1 热重启·持久化后端配置） | `scripts/run-backends.sh`、`scripts/supervise-backends.sh`、`scripts/ai-backend.sh`、`amos-supervisor` | `bash -n`、`--print-config`、`supervisor-smoke.sh` | ✅ |
| REQ-A25 | 真实语音 native：gated 构建（sherpa ASR + Piper TTS）+ 本地模型 | `amos-tauri --features sherpa-asr,piper-tts`；模型 `models/sherpa-en-20m`、`models/piper-low`；`scripts/run-ui-release.sh` | `cargo build` exit 0；`cargo test -p amos-tauri --features sherpa-asr --lib -- --ignored local_sherpa_pipeline_selects_when_configured` ok（配好模型时真选 sherpa）；release 内嵌 UI 运行（不占 1420）；live `chat_once`/`translate_once` | ✅ |
| REQ-A26 | 持久化存储坏档归一化（备忘录/文件/相册/信息/音乐/设置/通知）+ 设置读写护栏 | `lib/{notes,files,photos,messages,music,settings,cloud,providers}.ts` 各 `normalize*`；加载先归一化 | 各 `*normalize*` 单测（畸形/缺 id/重复 id/null/primitive/非对象写） | ✅ |
| REQ-A27 | 稳健性测试：文件子树环 / 搜索排序极端 / 备忘录病态与 CRLF | `__tests__/files.test.ts`、`notes.test.ts`（pathOf/isInside/delete/move 环不挂、5000 条搜索排序、病态标记、CRLF 结构断言） | 上述单测文件 | ✅ |
| REQ-A28 | 跨系统边界全局剪贴板同步（宿主 Webview ↔ Android/Waydroid 容器文本）——宿主半 + guest 侧代理 + 通道生命周期 + **真实 UDS 传输（P2b）** + **宿主半接线（P3，env 门控默认 inert）** | `crates/amos-tauri/src/clipboard.rs`（内核 seam）、`src/clipboard_guest.rs`（宿主半协议 + 审计计数）、**`src/clipboard_guest_link.rs`（P3 接线：单一共享全双工连接、审计镜像 sink、`clipboard_guest_status`）**；`crates/amos-clipboard`（`proto.rs`、`provider.rs`、`agent.rs`、`android.rs`、`link.rs` Backoff+supervise，**`unix.rs` 真实 Unix 域套接字传输**）；设计 `docs/clipboard-container-sync.md` | 宿主 `clipboard_guest` **11/11**；`cargo test -p amos-clipboard` **59/59**（proto/echo guard、Mock provider、GuestAgent、link Backoff+supervise+**stop 谓词/空闲超时可停**、unix 8：bind↔dial 往返、split 双向、陈旧 socket 可替换而真实文件被拒、断线重拨恰一次、预算耗尽不再拨号、serve 顺序会话、drain）；`cargo test -p amos-tauri --test clipboard_e2e` **5/5**（含真实 socket 双向 e2e）；**`cargo test -p amos-tauri --test clipboard_guest_link_e2e` 2/2**（env 门控空白不敏感；真实 UDS 上后台窗口复制仍镜像、容器复制回流、自推送回显被丢、审计计数如实、`stop()` 干净退出、读仍仅前台）；`cargo check -p amos-clipboard/--features android` ✓ + clippy(all-targets)/fmt 干净 | ✅（P0+P1+P2a+P2b+**P3 宿主半**均已实现并以**真实 Unix socket** 在 host 上端到端验证；仅剩 Waydroid 宿主↔guest **命名空间桥接**与 guest 进程托管的设备侧验收，见 `docs/clipboard-container-sync.md` §7/§8） |
| REQ-A29 | 日历：iOS 对齐的月/**周**/日程视图、彩色日历、**多天全天/延续**、重复/提醒、**区域首周日**、**重复场次编辑（显示所点场次日期、序列锚点不漂移）**、OS 级到点通知 | `lib/calendar.ts`（`expandOccurrences`/`monthGrid`/**`startOfWeek`/`weekDays`**/**`shiftByWallClock`**/`allDayEnd`/`occurrencesByDay`/`isContinuation`/`atLocalTime`/`visibleEvents`/CRUD/`normalize*`）、`lib/calendarCore.ts`（`collectDueEventAlerts`+幂等 fired+**通知表坏档归一化**）、`svelte/CalendarApp.svelte`（月/周/日程三视图 + 场次相对编辑）、`svelte/osCalendarWatcher.ts`、`appMeta`/`appRegistry`/`appGroups` | `__tests__/calendar.test.ts`（**50**，含 2000 例独立暴力参考交叉验证 + `shiftByWallClock`）、`__tests__/calendarCore.test.ts`（**16**）、`svelte-tests/calendar.svelte.test.ts`（**36**，含周视图 + 重复场次编辑 + 观察器）、`svelte-tests/shell.svelte.test.ts`（+1 真实挂载）、`__tests__/reminderNotify.test.ts`（+1 姊妹回归）；插桩行全执行（calendar.ts 432/432、calendarCore.ts 94/94）；**真机 S5/Android 14 语义层验收**（uiautomator 读 WebView：日期/周视图/场次编辑器日期均正确）；覆盖门禁 93.84% ≥ 90%；`smoke:ui` 4/4 | ✅（诚实边界：无**年/日**视图、周视图为 7 日分区列表而非 iOS 小时时间轴、无自定义重复间隔/参与人、月历无跨天横条；提醒为前台协调器，进程被杀后的原生唤醒另案；真机仅语义层非像素层，见 `docs/calendar.md` §6） |
| REQ-A30 | 手机管家领域内核：清理垃圾（可重建材料唯一建模，用户数据不可表达为垃圾；有界汇总/计划；逐条诚实清理）、删除软件安全策略（系统包 + 钉死关键包拒绝）、权限只读审阅、体检折叠（未知不冒充健康） | `crates/amos-devocare`（`spec`/`error`/`provider`/`junk`/`guard`/`permissions`/`health`）；设计 `docs/devcare.md` | `cargo test -p amos-devocare` **50 例**（46 lib + 4 `tests/devcare.rs`：端到端 `scan→analyze→plan→execute` 部分失败诚实计数、审阅型需显式确认、四区域体检折叠、`CareReport` JSON 往返）；`clippy --all-targets -D warnings`（含 P0-1 `deny(unwrap/expect/panic)` 门禁） | ✅（诚实边界：真机 Android `CleanProvider`/`PackageManager` 清单、daemon 隐私账本接线与清理审计落盘为下一阶段，见 `docs/devcare.md` §7） |
| REQ-A31 | 手机管家端到端：真实文件系统后端（根目录受限、只匹配已建模垃圾、用户媒体永不返回）、Tauri 桥（**前端只能传类别、不能指名路径**；无后端诚实不可用且存储不评估）、Svelte 管家页（离线/无后端/部分清理/需确认/拒绝卸载如实渲染，未观测不显示评分）、前端 normalizer 全量不抛错 | `crates/amos-devocare/src/hostfs.rs`；`crates/amos-tauri/src/devcare.rs`；`frontend-ts/src/lib/devcare.ts`、`svelte/DeviceCareApp.svelte`、`appMeta`/`appRegistry`/`appGroups`/i18n | `cargo test -p amos-devocare` **67 例**（含 hostfs 真实临时目录：根外拒绝/符号链接/非空目录/深度上限/媒体不返回）；`cargo test -p amos-tauri --lib devcare::` **7 例**（含 `the_frontend_cannot_name_a_path_to_delete`、无后端不评估）；前端 `devcare.test.ts` **12 例** + `device-care.svelte.test.ts` **10 例**；`bun run check` **EXIT=0**（`test:svelte` 418/58） | ✅ |
| REQ-A32 | 清理/卸载接入统一审计落盘：纯领域审计事件（聚合+逐条失败+拒绝，有界、字符安全截断）、`PrivacyService.RecordAudit`（守护进程盖章时间戳、校验 outcome/principal/op、无持久 sink 诚实回 `ok=false`）、桥如实回报 `recorded/attempted/reason`、卸载经策略命令（拒绝也入审计，策略不可被 UI 绕过） | `crates/amos-devocare/src/audit.rs`；`proto/privacy.proto`；`crates/amos-ai/src/{privacy_service,privacy,audit}.rs`；`crates/amos-tauri/src/{privacy_client,devcare}.rs`；`frontend-ts/src/lib/devcare.ts`、`svelte/DeviceCareApp.svelte` | `cargo test -p amos-devocare` **74 例**（+8 `audit::`）；`cargo test -p amos-ai --lib` **214 例**（+2 `privacy_service::`，真 JSONL 重开可读/无 sink 诚实降级/非法 outcome 拒绝）+ `--test privacy_rpc_e2e` 绿 + **`--test privacy_audit_e2e`**（真 UDS + 真 daemon + 真 JSONL：清理/拒绝卸载落盘、重开可读、时间戳由守护进程盖、非法 outcome 拒绝）；`cargo test -p amos-tauri --lib` **210 例**（`devcare::` 16，+9）；前端 `devcare.test.ts` **20 例** + `device-care.svelte.test.ts` **14 例**（断言未调用 `appstore_uninstall`）；`bun run check` EXIT=0（422/58） | ✅（读回闭环见 REQ-A34） |
| REQ-A33 | 手机管家自审计修复：卸载策略**单一构造点**（预览==执法，WebView 不能提供库存）、审计 ingest 批量不因单条失败丢尾、未知值不伪造（尺寸 `null`/清单失败为错误态）、`CleanReply` 扁平 wire 由测试锁定 | `crates/amos-tauri/src/{devcare,appstore,privacy_client}.rs`；`frontend-ts/src/lib/devcare.ts`、`svelte/DeviceCareApp.svelte` | `cargo test -p amos-tauri --lib` **210 例**（`devcare::` **16**：`the_preview_and_the_enforcement_agree_for_every_package`、`the_preview_verdicts_come_from_the_single_package_builder`、`an_unknown_package_size_serializes_as_null_not_zero`、`the_clean_reply_wire_shape_is_flat`）；前端 `devcare.test.ts` **20 例** + `device-care.svelte.test.ts` **14 例**（`the uninstall preview is bridge-owned …`、`an unreadable inventory is not shown as 'no apps'`）；`bun run check` EXIT=0（422/58） | ✅ |
| REQ-A34 | 审计 trail **读回闭环**：`RecentTrail` 读同一 `AuditFile`（隐私裁决与管家动作共用 sink）、`durable` 区分「无 trail」与「无活动」、桥侧以 `audit::ACTOR` 单源过滤、管家页「近期操作」如实区分 有条目/空/无 sink/读失败 | `proto/privacy.proto`（`TrailReply`/`RecentTrail`）；`crates/amos-ai/src/{privacy,privacy_service}.rs`；`crates/amos-tauri/src/{privacy_client,devcare}.rs`；`frontend-ts/src/lib/devcare.ts`、`svelte/DeviceCareApp.svelte` | `cargo test -p amos-ai --lib` **216 例**（`privacy_service::` **9**：按 actor/resource 过滤、拒绝可见、无 sink ⇒ `durable=false`）；`--test privacy_audit_e2e` **写入 + 读回**；前端 `devcare.test.ts` **23 例** + `device-care.svelte.test.ts` **18 例**；`bun run check` EXIT=0（426/58） | ✅ |
| REQ-A35 | 手机管家自审计（第二轮）：扫描失败≠空结果（`"—"` 而非 `0 B`）、后端 `last_error` 显式渲染、审计读回不依赖清理后端、审计 outcome **单一拼写**（小写、兼容读旧 PascalCase）；并新增**桥→真 daemon→真 JSONL** 全链路 e2e | `crates/amos-ai/src/audit.rs`（`Outcome`）；`svelte/DeviceCareApp.svelte`；`crates/amos-tauri/tests/devcare_audit_e2e.rs` | `cargo test -p amos-ai --lib` **217 例**（`audit::` 6，含 canonical spelling + legacy 兼容）；`cargo test -p amos-tauri --test devcare_audit_e2e` **1 例**（桥函数打真 daemon：写入/读回/actor 过滤/落盘 `"rejected"`/守护进程盖时间戳）；`device-care.svelte.test.ts` **22 例**（+4）；`bun run check` EXIT=0（430/58） | ✅ |
| REQ-A36 | 权限审阅**以 daemon 为权威**：`GrantedAll` 一次取回全部持有者（稳定序、空集不列出）；桥不接受前端观测、形状仍由领域定；`authority` 区分「权威为空」与「读不到权威」 | `proto/privacy.proto`（`AllGrantsRequest`/`AllGrantsReply`/`GrantedAll`）；`crates/amos-ai/src/{privacy,privacy_service}.rs`；`crates/amos-tauri/src/{privacy_client,devcare}.rs`；`frontend-ts/src/lib/devcare.ts`、`svelte/DeviceCareApp.svelte` | `cargo test -p amos-ai --lib` **218 例**（`granted_all_…`）；`cargo test -p amos-tauri --lib` **211 例**（`the_permission_review_is_shaped_from_daemon_rows`、`an_empty_grant_set_reviews_to_nothing`）；`--test devcare_audit_e2e` 真 daemon 授权 → `perm_grants_all` → `permission_review_from` 读回；前端 `devcare.test.ts` **25 例** + `device-care.svelte.test.ts` **20 例**（不可达 ≠ 无授权）；`bun run check` EXIT=0（432/58） | ✅ |
| REQ-A37 | 手机管家「内存加速」（安卓手机管家模式）：可回收性 **by construction**（只 `cached`/`background`，受保护与未知档位永不入选，稳定计划）、读数取自 `system_health`、清单取自 governor、动作逐个下发（governor 是生命周期权威）；回报**不声称因果**（部分失败列出、只给加速前后可用内存） | `crates/amos-devocare/src/boost.rs`；`crates/amos-tauri/src/devcare.rs`（`memory_view`/`devcare_memory`/`devcare_boost`/`BoostOut`）；`frontend-ts/src/lib/devcare.ts`、`svelte/DeviceCareApp.svelte` | `cargo test -p amos-devocare` **79 例**（`boost::` 5）；`cargo test -p amos-tauri --lib` **213 例**（`devcare::` 19：受保护档位不入选且 cached 在前、governor 不可达不伪造读数）；前端 `devcare.test.ts` **30 例** + `device-care.svelte.test.ts` **30 例**（读数/部分失败/不可达≠无可回收/「—」/禁用）；`bun run check` EXIT=0（438/58） | ✅ |
| REQ-A38 | 手机管家**真机 Android 后端** + 存储概览：`PackageManager` 真实库存（`system` 取自 `FLAG_SYSTEM`、尺寸为 APK 真实长度或 `null`）、`ACTION_DELETE` 卸载（**只请求**：回 `launched` 而不谎报 `removed`）、`StatFs` 真实存储读数（测不到即 `measured:false` + 字节全 `null`）、应用私有子树垃圾扫描（分类与 `hostfs` 逐条一致；未 root 零售机**不假装**全盘清理）、五个命令 `spawn_blocking`（UI 线程不做 JNI）、`attach` 只安装后端不扫描 | `crates/amos-tauri/android-glue/com/amos/ai/glue/DevCareGlue.kt`、`AndroidManifest.permissions.xml`（`QUERY_ALL_PACKAGES`）；`crates/amos-tauri/src/devcare_device.rs`（feature `android`）；`crates/amos-tauri/src/devcare.rs`（`CareScanner`/`PackageSource`/`StorageTotals`/`Removal`/`devcare_storage`/`offload`）；`crates/amos-tauri/src/appstore.rs`（`InstalledEntry::system`）；`frontend-ts/src/lib/devcare.ts`（`StorageView`/`normalizeStorage`/`launched`）、`svelte/DeviceCareApp.svelte`（存储概览卡） | `cargo test -p amos-tauri --lib` **227**（`devcare::` **27**：假后端接线 `a_device_backend_drives_scan_inventory_and_storage`、`re_attaching_a_backend_drops_the_previous_snapshot`、`a_backend_that_cannot_measure_reports_unknown_storage_not_zero`、`a_platform_flagged_package_is_refused_as_a_system_app`）+ `--features android --lib` **231**（`devcare_device::` **5**：`parse_scan_reply`：正常/达上限拒/非数组/畸形/`error`）；`cargo test -p amos-devocare` **81**；`clippy --all-targets -D warnings`（default 与 `--features android`）干净；前端 `devcare.test.ts` **28 例**（`normalizeStorage` 未知≠`0`、`launched`≠`removed`）+ `device-care.svelte.test.ts` **36 例**（存储三态、launched 文案）；`bun run check` EXIT=0（全仓 `test:svelte` 458/59） | ✅（产物级验证：APK 安装成功、dex 含 glue 字符串、`.so` 导出 JNI 符号；**运行期**验收见 `docs/devcare.md` §8） |
| REQ-A39 | 手机管家**电量读数真机化**（`BatteryManager`）：与 `amos-power` 同源的 sticky `ACTION_BATTERY_CHANGED`（`level`/`scale` → `level_pct`、`status` → `charging`；**只有平台真的报了状态才发**，`BATTERY_STATUS_UNKNOWN`/缺失 ⇒ 省略，越界 `level > scale` ⇒ 读作失败）；`thermal_throttled` 取平台自己的 `PowerManager.currentThermalStatus`（API 29+，更早版本不报）；后端读数优先、仅**全未知**才回落宿主读数；折叠进耦合的 `BatteryCare` 时**未知/越界只让少说**（`level_pct`/`charging` 未知或越界 ⇒ 电池区**不评估**，档位 `round` 不截断）；`devcare_report` 改 `async` + `spawn_blocking`（JNI/`pmset` 离开 UI 线程）；宿主 sysfs 映射**不丢观测**（`Full`/`Not charging` ⇒ 插电；读不懂的条目跳过而非整盘放弃） | `crates/amos-tauri/android-glue/com/amos/ai/glue/DevCareGlue.kt`（`battery()`/`chargingFrom`/`thermalThrottled` + API-29 显式跳过）、`crates/amos-tauri/src/devcare_device.rs`（`AndroidScanner::battery`/`parse_battery_reply`）、`crates/amos-tauri/src/devcare.rs`（`BatteryReading`/`care`/`reading_from_host`/`report`）、`crates/amos-tauri/src/host_battery.rs`（`charging_from_status`/`linux::read`，`cfg(any(linux, test))`） | `cargo test -p amos-tauri --lib` **227**（`devcare::` **27**：越界/非有限 ⇒ `None`、`0`/`100` 边界为真读数、`20.6 ⇒ 21`、宿主缺标志 ⇒ 不评估、设备读数驱动报告；`host_battery::` **7**：`Not charging` ⇒ 插电、decoy 条目跳过、无可读电池 ⇒ `None`）；`--features android --lib` **231**（`devcare_device::` **5**）；`clippy --workspace --all-targets -D warnings` 干净；Kotlin `:app:compileUniversalDebugKotlin` BUILD SUCCESSFUL | ✅（**运行期**读数仍按 `docs/devcare.md` §8.2 真机验收；本轮未重建 APK） |
| REQ-A40 | 航空航天级审计（量测真实性 + 部分体检可见）：(a) **读不到尺寸的行不作为垃圾建模**——`JunkItem::size_bytes` 是 `u64`、表达不了 unknown，故绝不填 `0`（P0-3），改为**跳过**并计入**部分扫描**（`unreadable_dirs`，UI 显示「扫描不完整」）；glue 侧 MediaStore `SIZE` 为 `null` 同样跳过。(b) **部分体检必须可见**——管家页显示「已检测 / 未检测」分区（`careAreaSplit`，域枚举序），部分报告不再渲染成一个看不出漏测的自信评分 | `crates/amos-tauri/src/devcare_device.rs`（`parse_item`）、`android-glue/.../DevCareGlue.kt`（`scanPublicDownloads`）、`frontend-ts/src/lib/devcare.ts`（`CARE_AREAS`/`careAreaSplit`）、`svelte/DeviceCareApp.svelte`（`devcare-areas`/`-observed`/`-missing`）、i18n `care.assessed`/`care.notAssessed`（en+zh） | `cargo test -p amos-tauri --features android --lib devcare_device::` **5**（新增 `a_row_without_a_usable_size_is_left_out_instead_of_sized_zero`：缺/`null`/字符串尺寸一律跳过且计 partial，真 `0` 保留）；`devcare.test.ts` **29**（`careAreaSplit`：域枚举序、部分、未建模键被忽略、`null`）；`device-care.svelte.test.ts` **39**（部分报告显示未检测分区、全检测无告警、未观测不显示该行）；`bun run check` EXIT=0 | ✅ |
| REQ-A41 | 短信**余额脱敏**（显示路径，REQ-A41）：银行短信余额金额在进入 UI 前一律替换为 `***`——规则 A（任何发件人：余额关键词后有界填充窗口内的金额）、规则 B（银行短号发件人：`:`/`：` 后的金额）；关键词保持可见（`余额：***`）；**平台短信库原文不动**（视图层不改写系统库）；审计日志只记**掩码条数**、绝不记内容；幂等、单遍 O(n)、panic-free、未命中零分配 `Cow`；发件人缺失时退化为纯内容规则（脱敏不依赖发件人已知） | `crates/amos-sms/src/redact.rs`（`is_bank_sender`/`redact_balances`/`redact_for`/`REDACTION`）；`crates/amos-tauri/src/sms.rs`（`sms_snapshot`/`sms_messages` 单一漏斗 + 纯函数 `redact_threads`/`redact_messages`）；`docs/sms.md` §12 | `cargo test -p amos-sms` **41 例**（`redact::` **18**：规则 A/B、幂等、有界窗口、panic-free 模糊、全角数字不识别的诚实边界、固定向量表）；`cargo test -p amos-tauri --lib` **231 例**（`sms::` **16**，+3：预览脱敏而存储映射不动、按发件人分级、种子数据零掩码=诚实 no-op）；`clippy --all-targets -D warnings`（host 与 `--features android`）、`fmt --check`、`cargo check -p amos-sms --features android` 全干净 | ✅（诚实边界：全角数字金额不识别，已文档化；真机运行期显示效果待下次装机目验） |
| REQ-A43 | 守护进程级**生成准入池**（`AMOS_MAX_SESSIONS` 终于接线）：`crates/amos-ai/src/pool.rs` `GenerationPool`——容量 `NonZeroUsize` 类型编码（零槽位不可表示）、fail-fast 默认（饱和立刻 `Saturated` 拒绝，可选 `AMOS_GEN_POOL_WAIT_MS` 有界等待 → `WaitTimeout`），两种拒绝可区分且单调计数；RAII `PoolPermit`（tokio `OwnedSemaphorePermit`，'static 可移入流任务，任何退出路径恰好归还一次）；`stream_chat` 在安全门后、**任何会话/簿记前**获取（被拒不分配资源），bidi `chat` 每条 Prompt 生成前获取、饱和回错误 chunk 保持连接；拒绝写审计 `Rejected` + gRPC `ResourceExhausted`，细节含诚实原因；`in_flight()` ≤ `capacity()` 有 12 并发任务测试锁死 | `crates/amos-ai/src/pool.rs`；`crates/amos-ai/src/server.rs`（`generation_pool`/`pool_wait` 字段、`with_generation_gate` 构造器、两条生成路径接线）；`docs/daemon-resource-gate.md` | `cargo test -p amos-ai --lib -- pool::tests` **8 例**（零容量构造硬错、RAII 释放、饱和拒绝/有界等待超时诚实计数、等待唤醒、12 并发不超容量、默认值与 `Config` 锁步、permit `Send`）；server 集成 `generation_gate_rejects_when_saturated_then_serves_after_release`（饱和拒绝 + 不分配会话 + 释放后可复用 + 审计含诚实原因）；`cargo test -p amos-ai` **245 例** lib + 全部集成；`cargo test --workspace` **134 个测试二进制全绿 EXIT=0**；`clippy --all-targets -D warnings`、`fmt --check` 干净 | ✅（诚实边界：与 `RateLimiter` 互补——并发 vs 速率/配额；池计数进 wire proto 属后续增量） |
| REQ-A44 | 守护进程级**推理响应缓存**（默认关，`AMOS_RESPONSE_CACHE=1` 开）：`crates/amos-ai/src/cache.rs` `ResponseCache` 有界 LRU+TTL（32 条/300 s/单条 256 KiB，注入时钟确定性测试，超限**拒绝而非截断**，`hits+misses==lookups` 不变量锁定）；键 = 后端可见的**全部**输入 `(model, max_tokens, prompt, 排序后 context 全部键值对)`——会话谱系键参与，响应绝不在可能分叉的会话间错误共享；`CachingBackend` 装饰器命中按原 token 序列重放（流式契约不变）、**上游错误（infer 失败或流中途失败）绝不缓存**、metadata 名诚实追加 `+cache` 令 `get_status` 可见、`BackendStats` 纯透传不伪造遥测；默认关闭：缓存改变可观测延迟（命中近零 TTFT），必须是运营者明确选择 | `crates/amos-ai/src/cache.rs`；`crates/amos-ai/src/server.rs`（`new()` 中 `AMOS_RESPONSE_CACHE` 包装，engine 快照取包装前名）；`docs/daemon-resource-gate.md` | `cargo test -p amos-ai --lib -- cache::tests` **17 例**（存取命中、TTL 恰好边界过期可观测、LRU 逐出、覆盖刷新、超限拒绝、键规范化/全输入参与/会话键参与、并发计数一致、二次相同调用零后端调用且逐 token 一致、不同输入=不同键、infer 失败/中途失败不缓存、`+cache` 名诚实、stats 透传 None）；server 集成 `response_cache_replays_identical_prompts_when_enabled`；`cargo test --workspace` 全绿 EXIT=0；clippy `-D warnings`/`fmt --check` 干净 | ✅（诚实边界：进程内非持久化，重启即空；计数进 wire proto 属后续增量） |
| REQ-A42 | 短信**回收站**（视图层隐藏/恢复，REQ-A42）：平台短信库由默认短信应用拥有、AmOS 无删除能力，故提供**隐藏/恢复/清空（=全部恢复）**——回收站过滤统一作用于三条读取路径（`sms_snapshot` 线程列表、`sms_messages` 正文、`sms_counts` 徽标，`filtered_counts` 使徽标与可见列表**由构造一致**）；**预览改写**：回收线程最新可见消息时预览换到回收前上一条可见行（`was_latest` 按「回收前可见集」判定，连续回收正确刷新，新消息到来后覆盖失效）；仅收**当前文件夹可见快照**中存在的行（陈旧 id 诚实报「未找到」）、屏蔽发件人拒收、重复回收幂等；持久化只存 **id + 时间戳（绝不存正文）**、版本化 JSON、上限 256 FIFO 淘汰（=自动恢复显示）；**不销毁任何数据**，UI 明说「系统短信应用仍保留原短信」 | `crates/amos-sms/src/trash.rs`（`SmsTrash::add/restore/purge/filter_messages/to_json/from_json`、`DEFAULT_TRASH_CAP`）；`crates/amos-tauri/src/sms.rs`（`sms_trash_add/_list/_restore/_purge` + `apply_trash`/`filtered_counts` + `sms-trash.json` 落盘）；`frontend-ts/src/lib/backend.ts`（4 个封装 + 三态 `SmsTrashAddResult`）、`svelte/MessagesApp.svelte`（气泡删除按钮 + 回收站面板 + 状态条）、i18n `message.trash*`（en+zh）；`docs/sms.md` §13 | `cargo test -p amos-tauri --lib` **237 例**（`sms::` +6：预览换上一条可见行、整线程隐藏、未知 id 诚实报错、恢复出自然预览+徽标、新消息后覆盖失效、落盘/重载 round-trip）；`cargo test -p amos-sms` **55 例**；`clippy --all-targets -D warnings`（host 与 `--features android`）、`fmt --check`、`cargo check -p amos-sms --features android` 全干净；前端 `tsc --noEmit`/`svelte-check` 0 错 0 警、vitest **471 例** | ✅（诚实边界：非系统级删除——原文仍在平台短信库；彻底删除需默认短信应用角色，已文档化；真机 UI 待下次装机目验） |
| REQ-A45 | 相机 producer 的 **`buffer is inaccessible` use-after-free 真修复**（此前只 try/catch 丢帧）：根因是 `Nv21` 读 plane buffer 时底层 native 内存已被释放（AOSP `DirectByteBuffer.MemoryRef.free()` → `isAccessible=false`），由「`MainActivity.onStart` 每次前台都 `CameraGlue.attach` → 旧 `attach` 先 `detach()` 再重建，而旧 `detach()` 在调用线程 `reader.close()`，与 `amos-camera` 线程上的 `onImageAvailable` 竞争」触发。修法：`attach` **幂等**（同相机已在采集即返回，不拆活会话）；`detach` **串行化到相机线程**（先清字段让排队回调认不出自己，再 post close 到同一 looper，最后 `quitSafely()`；looper 已死则内联 close）；`onImageAvailable` **陈旧 reader 身份守卫**（`reader !== this.reader` 则释放图像直接返回）；`CAMERA` 授权检查**前置**（不再泄漏 handler 线程/reader）；`onOpened` 若已拆解则关 device；**纪元守卫**（`generation` 每次拆解自增，设备/会话回调带回查 epoch，迟到的 `onDisconnected`/`onConfigured` 不写进新一代状态）。打包数学抽成**不碰 Android 类型的纯函数** `Nv21Packer`（旧绝对读无上界：只在 `limit()` 内读、短窗口零填、支持 `pixelStride` 1/2 与任意 `rowStride`、luma 整行批量）；`droppedFrameCount()` 可观测量 | `android-glue/com/amos/ai/glue/Nv21Packer.kt`（纯）、`Nv21.kt`（薄适配）、`CameraGlue.kt`（生命周期）、`android-glue/tests/.../Nv21PackerTest.kt`、`scripts/android-glue-nv21-check.sh`；`docs/android-glue.md` §相机帧 | 宿主 JVM 单测 `Nv21PackerTest` **5/5**（`./gradlew :app:testArmDebugUnitTest`：NV21 V 先 U 后、I420 紧凑、semi-planar U/V 共缓冲、luma 填充跳过、短窗口零填不抛、奇数/非正几何 `null`）；`./gradlew :app:compileArmDebugKotlin`（JDK 17）BUILD SUCCESSFUL；脚本 `scripts/android-glue-nv21-check.sh` | ✅（诚实边界：`isAccessible=false` 的那条竞态**无法在宿主复现**，须真机验收——步骤见 `docs/android-glue.md`；本环境 `adb devices` 为空，未执行） |








---

## B. 稳健性 / 安全（与审计 §8 处置对应）

| 需求 ID | 描述 | 实现 | 验证 | 状态 |
|---|---|---|---|---|
| REQ-B1 | 遥测不伪造：未采集必须上报 `None`（**P0-3**） | `amos-ai/…/real.rs`（`BackendStats` 全 `Option`） | `cargo test -p amos-ai`（`stats_report_unknown…`） | ✅ |
| REQ-B2 | 存储损坏不静默丢失：告警 + 隔离备份（**P1-1**） | `lib/amosStore.ts`（`quarantineCorrupt`） | `amosStore.test.ts`（P1-1 段） | ✅ |
| REQ-B3 | IPC 失败保留根因分类（**P0-2 阶段一**） | `lib/backend.ts`（`bridgeDiag`） | `backend.test.ts`（结构化诊断） | ✅ |
| REQ-B4 | 索引越界编译期检查（**P1-5**） | `tsconfig.json`（`noUncheckedIndexedAccess`）+ 49 处修复 | `tsc --noEmit` 0 error + `bun test` | ✅ |
| REQ-B5 | `loop{}` 无无界忙等，均有终止（**P1-6**） | 见报告 §9（只读审计） | 人工核查表 §9 | ✅ |
| REQ-B6 | 生产代码禁 `unwrap/expect/panic`（**P0-1 阶段一批**） | `amos-wm/amos-tts/amos-asr` 顶部 `cfg_attr(not(test), deny(…))` | `cargo clippy -p <crate> --all-targets/--all-features -- -D warnings` | ✅（该 3 crate） |
| REQ-B7 | TS 核心 `src/lib` 行覆盖率 ≥ 90%（**P2-1**） | `scripts/lib-coverage-gate.mjs` + `package.json` + `Makefile cov` + CI | `make cov`（97.72% ≥ 90%） | ✅ |
| REQ-B8 | 核心 `src/lib` 纯逻辑均有单测覆盖 | `__tests__/` 各文件 | `bun test`（245 用例 / 37 文件全绿）；覆盖 ≥90% | ✅ |
| REQ-B9 | 遥测真实性延伸到手机管家（**P0-3**）：扫描里**读不到的尺寸绝不填 `0`**（跳过该行并计入部分扫描）；体检**未观测的分区必须可见**（UI「已检测 / 未检测」），杜绝「未测 = 健康」 | `devcare_device.rs::parse_item`、`DevCareGlue.kt::scanPublicDownloads`、`lib/devcare.ts::careAreaSplit`、`DeviceCareApp.svelte` | `devcare_device::tests::a_row_without_a_usable_size_is_left_out_instead_of_sized_zero`、`devcare.test.ts::careAreaSplit …`、`device-care.svelte.test.ts` 3 例 | ✅ |
| REQ-B10 | **测试必须确定性**（不可复现的失败不是证据）：并发/夹具用例不得把调度、墙钟或共享目录当断言——(a) `SampleRing` 的 `read()==0` 是「预算内无样本」（可被负载触发），生产者改按**剩余容量**节流而非固定 sleep；(b) `amos-appstore` 的 `installed_dir()` 原先只按**进程 id** 命名临时安装根，同进程内并行用例互相重装/擦除目录而在 `resolve_request().unwrap()` 上竞态 | `crates/amos-audio/src/ring.rs`（`concurrent_producer_consumer_loses_nothing`：`pending()/capacity()` 门控 + `0` 读按饥饿处理并给 60 s 上限）、`crates/amos-appstore/src/serve.rs`（`installed_dir()`：进程 id + **原子序号**，与 `hostfs`/`host_battery` 夹具同一惯例） | 两用例单跑 **12/12** 与 **10/10**；在 6 个 CPU 打满进程下 ring 用例连跑 **20/20** 全绿（修复前在并发构建负载下均会红）；`clippy -D warnings` + `fmt --check` 干净 | ✅ |
| REQ-C1 | Rust 生产 `unwrap/expect/panic` 全面收敛 + 全 crate 门禁（**P0-1 收敛**） | 8 个 crate `lib.rs` 均加 `cfg_attr(not(test), deny(clippy::unwrap_used/expect_used/panic))`：amos-ai/int/tauri/android/supervisor/translate/asr/tts；`amos-proto`(tonic 生成代码) 与 `amos-int-cli`(交互 CLI) 属合理排除 | `make lint`（`clippy --workspace --all-targets -D warnings`）通过 | ✅ |
| REQ-C2 | 内存应用超时会话清理收尾（**P1-3/P1-4 收尾**） | `amos-ai`：`SessionManager::cleanup_interval()` + `server::new()` 常驻 spawn 周期清扫；活动经 `touch()` 保活 | `cargo test -p amos-ai`（`touch_keeps_active_session_alive`、`background_sweeper_reaps_stale_periodically` 等 13 项通过）；clippy 干净 | ✅ |

---

*生成日期 2026-09；与 `docs/AEROSPACE_SOFTWARE_AUDIT.md` §8/§9 对应。新增需求时请继续编号并回填“设计→验证”列。*
